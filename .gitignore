/target
# Кэш сборки мутационного шага `scripts/verify_M-72.sh`. Отдельный от `/target`
# НАМЕРЕННО: общий отравляется артефактом мутированного исходника и делает
# следующий прогон в чистом дереве ложно-зелёным (разбор — в шапке того шага).
/target-mutation
**/target
*.log
.env
.env.*
/data
/journal-data
*.journal
.DS_Store
tmp/
