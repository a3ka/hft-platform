<!-- GATE-META
milestone: M-66
audited_repo: a3ka/hft-platform
audited_base: f6e9337b90e3e262e9006495764452e7122f9924
audited_head: 7f20269e3bfd10bc9d646661720150b8bed1af8c
verdict: REJECT
-->

# C-115 — M-66 rev5: review-fa blocks valid reviewed history and its own acceptance range

**Verdict: REJECT.** The harness artifact set exists and its synthetic RED/mutation suite is
substantive, but two production-form checks contradict the proposed acceptance result. Dev / merge
is blocked until both are resolved with a regression oracle.

## Audited set

- Milestone: `milestones/M-66-protocol-attestation.md`.
- Harness implementation: `scripts/check_review_fa.sh`; RED probe:
  `scripts/tests/red_review_fa.sh`; acceptance: `scripts/verify_M-66.sh`; CI wiring:
  `.github/workflows/ci.yml`.
- This is a harness-track subject. It deliberately changes neither `contracts/**` nor
  `crates/**`; therefore no T-contract or trait-signature artifact is applicable. The required
  milestone, RED probe, real acceptance script, and CI wiring are present.
- The live FA ID relevant to the handoff's M-65 claim is `GS-I-4` in
  `docs/fa/viz-backend.md`.

## Blocking findings

### B-1 — `review-fa` rejects the two mandated production-history controls

`scripts/check_review_fa.sh:197-215` accepts a reviewer artifact only when it is added in the
checked range or its full path appears in a commit body in that range. In the required
production-form invocation it rejects both controls with `S=∅`:

1. Latest code-changing commit reachable from `origin/main`,
   `955f4096fd020134973ca48edbf31d94582b6610`, against its direct parent.
2. M-65 final code commit, `f3f84aaddd69ecc101a54b8ba6cd2b71a93a22ca`, against its direct
   parent. Its commit body does not name a `research/reviews/R-*.md` path, so the existing M-65
   review artifact cannot satisfy D despite the claimed `GS-I-4` compatibility.

Both commands exit 1 with `FAIL ... S=∅`. This is a false red against the explicit required
controls in the handoff, not a synthetic-stub observation. The range/artifact rule must be
reworked and a RED regression must execute these production-form controls before this branch can
merge.

### B-2 — the submitted range makes its own acceptance guard fail

At audited head, `git merge-base origin/main HEAD` is
`41129a98d1421082f5c26595e4083e3de22c56cb`. The range from that base modifies all nine
`.claude/agents/*.md` profiles and `.claude/rules/gates.md`.

`scripts/verify_M-66.sh:291-301` makes any such process-lock modification a failed guard G. It
does not recognise the founder-approved task-5 exception. Consequently the verifier increments
`FAILED`; even if all cargo commands pass, its terminal result is `VERDICT: FAIL`, not the
reported `PASS 16/2/0`.

The milestone, its §8 acceptance, and the committed task-5 process changes need a coherent range
contract. The corrective oracle must fail for an unauthorised lock edit and pass for the exact
authorised task-5 form; merely removing guard G would weaken the stated protection.

## Non-blocking confirmations

- The aggregate condition is fail-closed: substituting `review-fa=failure` exits 1; all-success
  exits 0.
- The reviewed red probe passed `40/40`, and its mutation battery passed `17/17` in the running
  acceptance invocation.
- `check_gate_meta.sh` accepted the six `ARCHIVED-VERDICT` records and the one
  `ALLOW-SUBJECT-CHANGE` record for the audited chain (exit 0).

## Done Block

```text
$ bash scripts/next_artifact_id.sh C
C-115
exit=0

$ sed -n '392,395p' .github/workflows/ci.yml | <substitute every needs.*.result with success> | bash
All checks passed
exit=0

$ sed -n '392,395p' .github/workflows/ci.yml | <substitute review-fa=failure; all other needs.*.result=success> | bash
One or more checks failed
exit=1

$ cd /tmp/hft-critic-m66-latest-code
$ EVENT_NAME=push PUSH_BEFORE=$(git rev-parse HEAD^) PR_BASE_SHA='' bash /tmp/hft-critic-m66r5/scripts/check_review_fa.sh
FAIL  ни один review-файл не введён диапазоном и не назван полным путём в %B (S=∅) — механизм D
LATEST_MAIN_CODE_PUSH_EXIT=1

$ cd /tmp/hft-critic-m66-m65
$ EVENT_NAME=push PUSH_BEFORE=$(git rev-parse HEAD^) PR_BASE_SHA='' bash /tmp/hft-critic-m66r5/scripts/check_review_fa.sh
FAIL  ни один review-файл не введён диапазоном и не назван полным путём в %B (S=∅) — механизм D
M65_PROD_FORM_EXIT=1

$ BASE=$(git merge-base origin/main HEAD); git diff --name-only "$BASE..HEAD" | rg '^(\\.claude/|CLAUDE\\.md$|docs/04-workflow\\.md$)'
.claude/agents/architect.md
.claude/agents/critic.md
.claude/agents/engine-dev.md
.claude/agents/research-dev.md
.claude/agents/reviewer.md
.claude/agents/risk-critic.md
.claude/agents/signal-engineer.md
.claude/agents/tester.md
.claude/agents/venue-dev.md
.claude/rules/gates.md
guard G result: FAIL

$ bash scripts/tests/red_review_fa.sh
SPEC_ROWS=40
PASS  СПЕКА⇄МАНИФЕСТ: 40 строк §4 совпали в обе стороны
VERDICT: PASS (40/40)
exit=0

$ bash scripts/tests/red_review_fa.sh --battery
BATTERY: PASS (17/17)
exit=0

$ EVENT_NAME=pull_request PR_BASE_SHA=f6e9337b90e3e262e9006495764452e7122f9924 bash scripts/check_gate_meta.sh
VERDICT: PASS — вердиктов проверено: 1, до-нормативных приземлений: 6, merge'ей с milestone в subject'е: 0
exit=0
```

=== HANDOFF: CRITIC → ARCHITECT ===

## §A — Метаданные

- Дата (UTC, ISO-8601): 2026-08-20T22:45Z
- Milestone: M-66
- Статус: BLOCKED — REJECT C-115
- HEAD: 7f20269 — docs(M-66): приземление до-нормативных вердиктов + выход из subject-lock

## §B — Что я сделал

- Audited the committed harness artifacts at `7f20269`, including the required production-form
  status-check, M-65, and latest-main-code adversarial executions.
- Verified the archived verdict tokens and the RED/mutation evidence; neither offsets B-1 or B-2.

## §C — Артефакты / результаты

- `research/critiques/C-115-M-66-rev5.md`
- Done Block: recorded above; decisive executions exit 1 for B-1 and guard G.

## §D — Следующий агент + инвокация

- **Следующий агент:** `architect`
- **Paste-ready промпт:**
  ```
  Исправь REJECT C-115 для M-66 на ветке feat/M-66-review-fa. Два блокера: (1) в
  production-form push current check_review_fa.sh red на 955f409^..955f409 и
  f3f84aa^..f3f84aa с S=∅; пересмотри правило S/D и добавь RED-регрессию именно на эти
  исполнения; (2) verify_M-66.sh guard G red на законных task-5 правках .claude/** и
  gates.md в текущем range. Сохрани fail-closed защиту против неавторизованных lock edits,
  но сделай authorised task-5 form проверяемо допустимой. Обнови milestone/acceptance
  согласованно, закоммить и запушь; затем запроси новый critic circle.
  ```
- Push-статус: ⏸ verdict commit pending gate-meta and push
- ⏸ кэш оставлен — `scripts/verify_M-66.sh` ещё выполняет `cargo test --all` в этом worktree.

## §E — Риски / открытые вопросы

- B-1 is an observed compatibility failure on the exact controls the handoff required; do not
  waive it as historical without changing the stated false-positive requirement.
- The current acceptance process is still consuming the local test cache; the critic’s own
  cache cleanup is deferred until that process exits.

=== END HANDOFF ===
