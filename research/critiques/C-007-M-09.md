# C-007 — Critic Verdict — M-09 Data safety net

**Date:** 2026-07-15  
**Role:** critic, plan-time doc gate  
**Branch:** `origin/docs/M-09-plan`  
**Audited HEAD:** `0862399` — `docs(M-09): план Data safety net (PROPOSED) — recon+бэкап+метрики (P2.5)`  
**Artifacts audited, in order:** `docs/fa/ops.md` first, then `milestones/M-09-data-safety-net.md`.

## Verdict

- **ops FA (`docs/fa/ops.md`): REJECT**
- **M-09 plan (`milestones/M-09-data-safety-net.md`): REJECT**

The direction is right: OPS-I-1 is the correct C1-class guard, CT-RFC-03 is named as a blocking T1
change, Storage Box is honestly marked founder-owned, and the `venue-*` work is scoped as MD-only.
The gate still cannot pass because the FA has self-contradictory alert/metric guarantees, and the
plan is not dispatchable under the current role/scope model.

## Pre-flight

- `git fetch origin && git rev-parse --short origin/docs/M-09-plan` returned `0862399`.
- Worktree used: `/tmp/hft-critic-m09`.
- Branch diff at audited HEAD: one new file, `milestones/M-09-data-safety-net.md`.
- Boundary honored: RED oracles and CT-RFC-03 types are intentionally not present yet; this verdict
  audits the FA and plan only.

## Findings

### C1 — FA promises OPS-I-5 parity, but its own matrix references a non-existent metric

`docs/fa/ops.md` §3 lists the exported metrics at lines 58-72. That list does not include
`backup_restore_drill_ok`. The §7.1 incident matrix then makes `OPS-BKP` a P0 rule backed by
`backup_restore_drill_ok` at line 168. This directly contradicts OPS-I-5 at lines 143-144 and
the parity rule at lines 170-172: every P0/P1 rule must reference an existing metric.

This is not cosmetic. `milestones/M-09-data-safety-net.md` task 4 and Acceptance item 4 both turn
that FA parity into CI (`lines 56, 67-68`). As written, the future gate must either fail forever
against the source document or be weakened into a decorative check. Fix the source FA before RED:
define the restore-drill metric in §3 and include it in the grep/parity surface, or change the
matrix to a named observable that actually exists.

While fixing this, close the same taxonomy gap for P1 rules in §7: `gap-доля за сутки > 1%` has no
metric in §3, and `частые ресинки` has `book_resync_total` in §3 but no stable §7.1 incident row.
Do not leave P1 rules outside the promised two-way parity surface.

### C2 — FA reintroduces TD-021's false RSS measurement class

`docs/fa/ops.md` defines `recorder_rss_bytes` as the TD-016 leak metric at line 60 and uses it for
the TD-016 P1 rule at line 165. It never says this is `RssAnon` from `/proc/<pid>/status`.

That is a known foot-gun in this repo. `gates.md` §8 says RSS must be measured only as `RssAnon`
and that `docker stats` / cgroup memory include journal page cache; `testing.md` lines 137-153 make
the same rule part of oracle integrity. A generic `rss` metric can recreate the exact TD-021 false
guarantee: alerting on page cache or host accounting instead of recorder heap growth.

Fix the FA before RED by pinning the metric source and semantics. Acceptable direction: define the
exported metric as `RssAnon` bytes from `/proc/<pid>/status` or rename it so the invariant is
unambiguous. Keep the book-level convergence question separate from process heap RSS.

### C3 — M-09 is not dispatchable: `ops` has no write zone or agent profile

The plan allows a new `crates/ops/**` crate and `deploy/**` work at
`milestones/M-09-data-safety-net.md` lines 36-43, then assigns core tasks to `ops` at lines 54-56.
Current workflow roles do not define an `ops` or `ops-dev` agent. `docs/04-workflow.md` lines 17-28
list architect, critic, engine-dev, venue-dev, signal-engineer, research-dev, risk-critic,
reviewer, tester, and founder only. `scope-guard.md` lines 10-18 likewise gives no role permission
to write `crates/ops/**` or broad `deploy/**`.

So the plan cannot be executed without a scope violation: architect may not write impl code,
engine-dev is limited to existing engine crates, venue-dev is limited to `venue-*`, and research-dev
is limited to `crates/research-cli/src` plus report artifacts. Before dispatch, either introduce the
ops role/profile and matching scope-guard entry through the doc gate, or assign the work to existing
roles with explicit, narrow carve-outs.

### C4 — Plan omits the `research/data-quality/` artifact path promised by the FA

`docs/fa/ops.md` §4 says the recon divergence counter is written to `research/data-quality/` next to
gap statistics (line 84). The M-09 Allowed paths list does not include `research/data-quality/`
(`milestones/M-09-data-safety-net.md` lines 36-43), and current `scope-guard.md` grants
`research-dev` `research/reports/` but not `research/data-quality/` by default.

This leaves a choice between violating scope and silently dropping part of OPS-I-1. The plan must
either allocate the artifact path and owner, or remove/defer that FA obligation explicitly.

## Checks That Passed

- **OPS-I-1 / C1-class recon:** the FA compares local book state to an independent REST snapshot,
  includes best bid/ask and band sums, and fixes `ε_test` separately from `ε_prod`. That is the right
  shape for catching the M-08 best-bid erasure class.
- **OPS-I-9 direction:** the required behavior names `Retry-After`, 418/429 cooldown, exponential
  backoff, shared venue budget, and no symbol-wide resync storm. Future RED still needs a concrete
  configured budget, but the invariant is the right one.
- **CT-RFC-03 trigger:** the plan correctly treats the recon audit event as a blocking T1 change and
  requires the full contract-RFC package.
- **Risk boundary:** the plan forbids `risk|killswitch|oms` and order-egress; the MD-only venue
  carve-out matches `gates.md` §5.
- **Storage Box dependency:** the plan honestly names the founder-owned Storage Box dependency and
  states which tasks are blocked by it.

## Gate Integrity

- `ε_test`: viable, provided the future RED drives the real recon path and asserts alert + resync +
  `Sys` audit event, not a surrogate comparator.
- OPS-I-9: viable only after the future RED proves anti-hot-loop behavior against injected REST
  errors and failed setup, with a concrete shared budget in the test fixture.
- restore-drill: directionally sound because the plan requires a real segment and `journal::stream`,
  but its alert/metric surface is blocked by C1.
- OPS-I-5 parity: currently not viable; the source matrix already violates the promised
  "rule -> existing metric" property.

## Required Fixes Before Re-audit

1. Fix the ops FA alert/metric closure: `backup_restore_drill_ok`, gap-share, frequent-resync, and
   any other P0/P1 rule must have a stable §7.1 row and an existing §3 metric or be explicitly removed
   from the parity surface.
2. Define the TD-016 memory metric as `RssAnon`-based, or otherwise remove the false RSS guarantee.
3. Make M-09 dispatchable under scope-guard: add an `ops` role/profile and write zone, or route
   `crates/ops/**` and `deploy/**` through named existing roles with explicit carve-outs.
4. Align the `research/data-quality/` recon artifact with scope and task ownership, or remove/defer it.

=== HANDOFF: critic → architect ===

## §A — Metadata
- Date (UTC): 2026-07-15T22:40Z
- Milestone: M-09 Data safety net
- Status: REJECT
- Audited HEAD: `0862399` — `docs(M-09): план Data safety net (PROPOSED) — recon+бэкап+метрики (P2.5)`

## §B — What I Did
- Audited `docs/fa/ops.md` first as the source of OPS-I-1..9.
- Audited `milestones/M-09-data-safety-net.md` against the FA, scope-guard, gates, testing
  gate-integrity rule, DESIGN layer 7/P2.5, and contract-layer governance.
- Did not audit absence of RED or CT-RFC-03 types as a defect.

## §C — Artifacts / Results
- Verdict file: `research/critiques/C-007-M-09.md`
- FA verdict: REJECT
- Plan verdict: REJECT
- Done Block exit-codes: N/A — read-only critic role

## §D — Next Agent + Invocation
- **Next agent:** `architect`
- **Paste-ready prompt:**
  ```
  Ты — architect проекта hft-platform. Рабочий каталог /home/nous/hft-platform.
  Продолжи C-007 / M-09 doc-gate после critic REJECT в research/critiques/C-007-M-09.md.

  Исправь только architect-owned документы/правила, не пиши RED и не пиши CT-RFC-03 types до
  повторного critic-гейта:
  1. docs/fa/ops.md: закрой OPS-I-5 parity — все P0/P1 правила должны иметь stable §7.1 row и
     существующую §3 metric; особенно restore-drill (`backup_restore_drill_ok`), gap-share и
     frequent-resync.
  2. docs/fa/ops.md: зафиксируй TD-016 memory metric как `RssAnon` из `/proc/<pid>/status` либо
     убери ложную RSS-гарантию.
  3. milestones/M-09-data-safety-net.md + workflow docs if needed: сделай `crates/ops/**` и
     `deploy/**` исполнимыми под scope-guard — ops role/profile или явные узкие carve-outs.
  4. Согласуй обещанный FA artifact `research/data-quality/` с Allowed paths и владельцем либо
     явно убери/defer его.

  После коммита верни на critic re-audit: branch origin/docs/M-09-plan, expected HEAD = <new short SHA>.
  ```
- Push-status: verdict commit to be pushed by critic in this turn.

## §E — Risks / Open Questions
- Founder-owned priority/phase decision P2.5 was not audited or changed.
- Storage Box remains founder-owned dependency for backup/restore-drill task.

=== END HANDOFF ===
