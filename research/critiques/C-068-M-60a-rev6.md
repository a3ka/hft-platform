<!-- GATE-META
milestone: M-60a
audited_repo: a3ka/hft-platform
audited_base: 28a6ee37a381b1500bf3f1e92c9572489a65af9f
audited_head: d80d478
verdict: NOTE
-->

# C-068 — M-60a rev6 final critic verdict

**Date:** 2026-08-06T23:05Z  
**Agent:** critic  
**Worktree:** `/tmp/hft-critic-m60a4`  
**Branch audited:** `origin/feat/M-60-mechanisms` detached at `d80d478`  
**Verdict:** NOTE — REJECT lifted; dispatchable to dev with the notes below.

## Verdict

NOTE.

I found no BLOCKER in categories (i) or (ii) under `milestones/M-60a-docs-freeze.md`
§3bis.4 and `research/arbitration/A-005-m60a-sufficiency.md` §5.2. The rev6 probe is
red against all six A-005 mutants, green against the reference implementation, verifies
manifest names against executed scenarios, verifies manifest pairs against the §3bis.2
spec table, and gives every axis at least one legitimate scenario. The W oracle no longer
accepts the C-067 disabled-exit spoof and does not reject the simple correct wiring.

No new sixth axis, no new value on axes 1-5, and no executable W counterexample in the
accepted A-005 form was found.

## Pre-flight

```text
$ git show --name-status --format='%h %s' d80d478
d80d478 test(M-60a): rev6 по A-005 §5.1 — пункты 4 и 5, конечный список исчерпан [architect]

M	milestones/M-60a-docs-freeze.md
M	scripts/tests/red_docs_freeze.sh
M	scripts/verify_M-60a.sh

$ git diff --name-only d80d478^..d80d478
milestones/M-60a-docs-freeze.md
scripts/tests/red_docs_freeze.sh
scripts/verify_M-60a.sh
```

Artifact set is valid for this process-gate plan-time round: milestone, RED probe,
acceptance script, A-005 arbitration decision, and previous critique chain are present.
T-contracts are N/A because M-60a does not introduce runtime/T1/T2 shape.

## A-005 §5.1 closure checks

### Item 1 — axis 5

PASS. The spec declares axis 5 values at
`milestones/M-60a-docs-freeze.md:100`; the manifest covers `subject`, `содержимое файла`,
`путь файла`, `негодная причина`, plus the legitimate `токен в теле коммита` at
`scripts/tests/red_docs_freeze.sh:78-83`. Executed scenarios are at
`scripts/tests/red_docs_freeze.sh:402-429`.

### Item 2 — axis 3 values

PASS. The spec declares `токен раньше`, `база не-предок`, `evil merge`, and
`реверт-пара` at `milestones/M-60a-docs-freeze.md:98`. The manifest declares them at
`scripts/tests/red_docs_freeze.sh:68-73`; the scenarios execute at
`scripts/tests/red_docs_freeze.sh:333-369`.

### Item 3 — axis 4 legitimate scenario

PASS. The legitimate non-zone `.claude/settings.json` case is declared at
`scripts/tests/red_docs_freeze.sh:87` and executed at `scripts/tests/red_docs_freeze.sh:394-400`.
This pins A-005's `overbroad` class.

### Item 4 — manifest against spec

PASS. The probe parses §3bis.2 from the milestone and compares `(axis, kind, value)` pairs
in both directions at `scripts/tests/red_docs_freeze.sh:444-477`, then verifies a
legitimate scenario per axis at `scripts/tests/red_docs_freeze.sh:479-490`.

Raw reference run:

```text
$ BARRIER=<fresh-ref.sh> bash scripts/tests/red_docs_freeze.sh | grep -E '^(PASS  МАНИФЕСТ|PASS  СПЕКА|PASS  §3bis|VERDICT)'; echo exit=${PIPESTATUS[0]}
PASS  МАНИФЕСТ ⇄ исполнение: 26 сценариев, состав совпал в обе стороны
PASS  СПЕКА⇄МАНИФЕСТ: 29 пар (ось,вид,значение) совпали в обе стороны
PASS  §3bis.3(2): у каждой из осей (1 2 3 4 5) есть легитимный сценарий
VERDICT: PASS (26/26) — все значения пяти осей покрыты, состав сверен со спекой
exit=0
```

Mutation checks of the checking mechanism:

```text
spec += workflow_dispatch: exit=1
FAIL  СПЕКА⇄МАНИФЕСТ: объявлено в §3bis.2, НЕ покрыто сценарием:

spec -= evil merge: exit=1
FAIL  СПЕКА⇄МАНИФЕСТ: покрыто сценарием, НЕ объявлено в §3bis.2:
        ось 3|V|evil merge

section renumbered: exit=1
SETUP НЕ СОСТОЯЛСЯ: таблица §3bis.2 ... не разобрана

spec missing: exit=1
SETUP НЕ СОСТОЯЛСЯ: спеки нет: ... Состав осей и значений сверять не с чем.

axis 5 without legitimate claim: exit=1
FAIL  §3bis.3(2): у осей 5 нет легитимного сценария

manifest -= A5P: exit=1
FAIL  МАНИФЕСТ: исполнены, но НЕ объявлены: A5P
FAIL  СПЕКА⇄МАНИФЕСТ: объявлено в §3bis.2, НЕ покрыто сценарием:

A5P call removed: exit=1
FAIL  МАНИФЕСТ: объявлены, но НЕ исполнены: A5P

A1M assert flipped: exit=1
FAIL  A1M ... — ложное срабатывание
```

### Item 5 — W by execution

PASS for plan-time. The current W oracle is embedded at `scripts/verify_M-60a.sh:88-330`.
It skips conditional guard steps (`scripts/verify_M-60a.sh:139-151`), substitutes
`needs.<job>.result` values and executes the resulting guard body with `bash -e`
(`scripts/verify_M-60a.sh:154-174`), and self-tests six workflow fixtures before reading
real CI (`scripts/verify_M-60a.sh:212-297`).

External comparison:

```text
C-067 disabled exit  old:PASS/exit=0  new:FAIL/exit=1
C-066 echo-only      old:FAIL/exit=1  new:FAIL/exit=1
job not in needs     old:PASS/exit=0  new:FAIL/exit=1
correct wiring       old:PASS/exit=0  new:PASS/exit=0
real ci.yml before dev  new:FAIL/exit=1
new selftest line: PASS  W самопроверка оракула: 6 фикстур классифицированы верно
```

The real `ci.yml` failure is expected before dev: `scripts/check_docs_freeze.sh` and CI
wiring are task 1-2 deliverables, not architect artifacts.

### Item 6 — axis 1/2 untouched

PASS. Axis 1/2 values remain as A-005 specified at `milestones/M-60a-docs-freeze.md:96-97`,
and their manifest/scenarios remain covered at `scripts/tests/red_docs_freeze.sh:59-66`
and `scripts/tests/red_docs_freeze.sh:276-315`.

## Mandatory battery

```text
$ bash scripts/tests/red_docs_freeze.sh --battery; echo exit=$?
══ БАТАРЕЯ (A-005 §10): эталон обязан быть зелёным, каждый мутант — красным ══
барьеры: /tmp/red-freeze-battery-tr8Axu

PASS  эталон → exit=0  VERDICT: PASS (26/26)
PASS  showgrep → exit=1  VERDICT: FAIL (3)  [ось 5 / содержимое файла]
PASS  subjtok → exit=1  VERDICT: FAIL (1)  [ось 5 / subject]
PASS  earlytok → exit=1  VERDICT: FAIL (1)  [ось 3 / токен раньше]
PASS  existsbase → exit=1  VERDICT: FAIL (1)  [ось 3 / база не-предок]
PASS  treediff → exit=1  VERDICT: FAIL (1)  [ось 3 / evil merge]
PASS  overbroad → exit=1  VERDICT: FAIL (1)  [ось 4 / нет легитимного сценария]
PASS  always0 → exit=1  VERDICT: FAIL (21)  [вырожденный — пропускает всё]
PASS  always1 → exit=1  VERDICT: FAIL (5)  [вырожденный — блокирует всё]
PASS  без барьера → exit=1, «SETUP НЕ СОСТОЯЛСЯ» (страж на месте)

BATTERY: PASS (10/10) — эталон зелён, все мутанты красные, страж жив
exit=0
```

## Findings

### BLOCKER categories (i)/(ii)

None.

I did not find a new value on axes 1-5, did not find a structurally justified sixth axis,
and did not find an A-005-accepted executable W counterexample where the oracle declares
precondition 3 complete while the substituted failure guard exits zero, or the inverse.

### NOTE-068-1 — stale task-table label names old FR scenarios

Category: (iii), not a completeness blocker.

`milestones/M-60a-docs-freeze.md:49` still says task 1 is verified by
`red_docs_freeze.sh FR-1..FR-14`. Rev6's executable source of truth no longer has FR labels;
it has 26 named scenarios (`A1M`, `A2P`, `A3NP`, `A5P`, etc.) plus manifest/spec checks. This
does not change the oracle result, because the script is the real gate and §4 correctly says
the scenario count is not duplicated (`milestones/M-60a-docs-freeze.md:212-217`). It can,
however, mislead a dev/tester looking for FR labels.

Suggested handling: dev/tester should ignore the stale `FR-1..FR-14` text and use the current
script output and manifest names. Architect may clean this wording in a later docs touch; do
not start another critic loop for it.

### Required close-out condition — branch protection TD

Category: (iii), not a plan-time blocker for this critic round.

`milestones/M-60a-docs-freeze.md:198-208` and task 5 at
`milestones/M-60a-docs-freeze.md:53` correctly state the reviewer-owned TD requirement:
branch protection is unavailable on this private/free repo, so `status-check` can be a CI
signal but not a GitHub merge barrier. A-005 §5.3 requires that MAJOR debt to be recorded
for cycle closure. Since `TECH-DEBT.md` is reviewer-owned, this remains a reviewer close-out
condition, not a critic-side edit.

## Verify run

The aggregate acceptance is expected RED before dev because `scripts/check_docs_freeze.sh`
and CI wiring are not implemented yet.

```text
$ bash scripts/verify_M-60a.sh; echo exit=$?
--- A: барьер существует, исполним, парсится, fail-closed на пустой базе ---
FAIL  A scripts/check_docs_freeze.sh отсутствует или не парсится
--- F: RED-проба замка (состав сверяет сама проба — с исполнением и со спекой) ---
FAIL  F проба КРАСНАЯ — SETUP НЕ СОСТОЯЛСЯ: барьера нет: /tmp/hft-critic-m60a4/scripts/check_docs_freeze.sh. Проба НЕ имеет права быть зелёной,
--- F2: АНТИ-ПЛАЦЕБО — батарея эталон + восемь дырявых (спека §4.1) ---
PASS  F2 BATTERY: PASS (10/10)
--- S: САМОРЕФЕРЕНЦИЯ — M-60a обязан пройти собственный замок ---
FAIL  S замка нет — самореференцию проверять нечем
--- W: ПРЕДУСЛОВИЯ блокировки; предусловие 3 — ИСПОЛНЕНИЕМ (§3ter «б» + §6.1) ---
PASS  W самопроверка оракула: 6 фикстур классифицированы верно (...)
FAIL  W предусловие 1: scripts/check_docs_freeze.sh не встречается ни в одном теле `run:` — построен, но не подключён
FAIL  W предусловие 1: scripts/tests/red_docs_freeze.sh не встречается ни в одном теле `run:` — построен, но не подключён
FAIL  W предусловия 2-3 не проверить: ни один джоб не исполняет скрипты замка
--- P: РЕГРЕСС — соседний барьер артефактов цел ---
PASS  P VERDICT: PASS (20/20)
--- T: паритет с CI-job fmt+clippy+test (gates.md §3) ---
PASS  T fmt
PASS  T clippy
^C
exit=130
```

I interrupted the run after the T cargo-test phase produced no new output for several minutes.
The partial `/tmp/m60a-test.log` showed workspace tests progressing and then:

```text
Running tests/red_floor_work_budget.rs (...)
running 5 tests
test wb_4_healthy_restart_is_unaffected_by_the_budget ... ok
test wb_3_honest_declaration_on_cheap_corruption_still_starts ... ok
test wb_1_floor_scan_work_is_bounded_on_uniform_garbage ... ok
test wb_2_budget_exhaustion_never_yields_a_lowered_floor ... ok
test wb_0_pseudorandom_garbage_carries_no_valid_frame has been running for over 60 seconds
```

This does not change the plan-time verdict: A/F/S/W are intentionally RED before dev, while
the M-60a-specific F2/P/fmt/clippy checks observed here are green. No `.rs` file is modified
by d80d478.

## Done Block

```text
$ git status --short
{empty before verdict file write}

$ git log -1 --format='%H %s'
d80d4784d300c9d8a362ab5541da830b9ad1d738 test(M-60a): rev6 по A-005 §5.1 — пункты 4 и 5, конечный список исчерпан [architect]

$ bash scripts/tests/red_docs_freeze.sh --battery; echo exit=$?
BATTERY: PASS (10/10)
exit=0

$ BARRIER=<fresh-ref.sh> bash scripts/tests/red_docs_freeze.sh
PASS  МАНИФЕСТ ⇄ исполнение: 26 сценариев, состав совпал в обе стороны
PASS  СПЕКА⇄МАНИФЕСТ: 29 пар (ось,вид,значение) совпали в обе стороны
PASS  §3bis.3(2): у каждой из осей (1 2 3 4 5) есть легитимный сценарий
VERDICT: PASS (26/26) — все значения пяти осей покрыты, состав сверен со спекой
exit=0

$ python3 <old/new W harness>
C-067 disabled exit  old:PASS/exit=0  new:FAIL/exit=1
C-066 echo-only      old:FAIL/exit=1  new:FAIL/exit=1
job not in needs     old:PASS/exit=0  new:FAIL/exit=1
correct wiring       old:PASS/exit=0  new:PASS/exit=0
real ci.yml before dev  new:FAIL/exit=1

$ bash scripts/verify_M-60a.sh; echo exit=$?
exit=130  # interrupted in T cargo-test phase after A/F/S/W RED and F2/P/fmt/clippy observed
```

## Handoff

Next: dev for M-60a tasks 1-2, then architect task 3, then tester/reviewer.

Paste-ready prompt:

```text
Ты — dev проекта hft-platform. Реализуй M-60a tasks 1-2 на ветке feat/M-60-mechanisms после critic NOTE C-068.

STARTUP-ПРОТОКОЛ: CLAUDE.md → .claude/rules/{gates,testing,scope-guard,commit-discipline,branch-hygiene,handoff-block}.md → свой dev-профиль.

Предмет:
- milestone: milestones/M-60a-docs-freeze.md
- critic verdict: research/critiques/C-068-M-60a-rev6.md
- arbitration decision: research/arbitration/A-005-m60a-sufficiency.md
- RED probe: scripts/tests/red_docs_freeze.sh
- acceptance: scripts/verify_M-60a.sh

Задачи:
1. Написать scripts/check_docs_freeze.sh так, чтобы bash scripts/tests/red_docs_freeze.sh проходил GREEN: база из события, fail-closed, токен FOUNDER-APPROVED только в теле каждого собственного коммита, зона .claude/rules/** + .claude/agents/** + .claude/wrappers/** + CLAUDE.md + docs/04-workflow.md, M/A/D/R, push/pull_request, формы истории из §3bis.2.
2. Подключить docs-freeze job в .github/workflows/ci.yml: реальный run scripts/check_docs_freeze.sh + scripts/tests/red_docs_freeze.sh, fetch-depth достаточен для диапазона, status-check.needs включает job и guard исполняемо падает при needs.<job>.result=failure.

Не трогай:
- milestones/M-60a-docs-freeze.md, scripts/tests/red_docs_freeze.sh, scripts/verify_M-60a.sh, research/critiques/**, TECH-DEBT.md.

Перед Done Block:
- bash scripts/tests/red_docs_freeze.sh --battery; echo exit=$?  # ожидание exit=0
- bash scripts/verify_M-60a.sh; echo exit=$?  # ожидание exit=0 после задач 1-2 и при отсутствии unrelated cargo flakes
- git log origin/feat/M-60-mechanisms..HEAD --format='%h %s'  # только твои коммиты
- git push origin HEAD:feat/M-60-mechanisms

NOTE C-068:
- task table still says FR-1..FR-14, but rev6 source of truth is the current scenario manifest/output in red_docs_freeze.sh.
- branch-protection MAJOR TD is reviewer-owned at close-out; do not edit TECH-DEBT.md.
```

