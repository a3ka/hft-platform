# C-080 — M-63 rev2: plan-time critic

**Вердикт:** REJECT — NOT REVIEWED: architect artifact set incomplete.
**Дата (UTC):** 2026-08-13T22:17Z
**Предмет:** `origin/feat/M-63-ci-cost` @ `57a2263`
**Роль:** critic
**Модель:** codex/gpt-5; RAW-гейт `gates.md` §1 не сработал (нет T1, нет раскладки журнала).

## 0. Pre-flight

Проверенный worktree:

```text
$ pwd && git remote -v | head -1
/home/nous/hft-platform
origin  https://github.com/a3ka/hft-platform.git (fetch)

$ git log --oneline origin/main..HEAD
57a2263 docs(M-63): rev2 по C-073 — пятая ось «источник changed-files», упаковка, полный список джобов, зависимость от M-60a [architect]
9d4fcfb docs(M-63): C-073 critic verdict REJECT [critic]
f827edd docs(M-63): спека — CI платит за то, что проверяет; замер 9 из 11 прогонов по docs [architect]

$ git rev-list --left-right --count origin/main...HEAD
112 3

$ git diff --name-status origin/main...HEAD
A       milestones/M-63-ci-cost.md
A       research/critiques/C-073-M-63.md
```

T-contracts / trait signatures: N/A for this process/CI milestone; `origin/main...HEAD` does
not touch `contracts/**`, `crates/**`, or `*/tests/**`. The required M-63 RED/verify artifacts
are still mandatory because `docs/04-workflow.md` and `critic.md` require architect to commit
RED + acceptance before dev.

## 1. Findings

### F1 — BLOCKER: committed artifact set still has no RED test and no verify script

M-63 itself names architect-owned artifacts as tasks 4-5:

```text
milestones/M-63-ci-cost.md:66 | RED-набор scripts/tests/red_ci_cost.sh | architect | OPEN
milestones/M-63-ci-cost.md:67 | scripts/verify_M-63.sh | architect | OPEN

$ rg --files | rg '(^scripts/tests/red_ci_cost\.sh$|^scripts/verify_M-63\.sh$)'
<no output>
```

Per `.claude/agents/critic.md`, critic does not proceed as if this were dev-ready when
RED tests and verify script are absent: verdict is `NOT REVIEWED — ARCHITECT ARTIFACTS
INCOMPLETE`. This is not a NOTE. There is no execution proof that the gate catches what it
promises, and no anti-placebo battery can be audited.

### F2 — BLOCKER: rev2 is stale against current `origin/main`; the CI surface changed under it

The branch is 112 commits behind `origin/main`. Its local `ci.yml` still has only:

```text
HEAD jobs=build-test,security,delivery,protected-artifacts,contracts,status-check
HEAD status-check.needs=[build-test, security, delivery, protected-artifacts, contracts]
```

Current `origin/main` has:

```text
origin/main jobs=build-test,security,delivery,protected-artifacts,docs-freeze,contracts,artifact-ids,status-check
origin/main status-check.needs=[build-test, security, delivery, protected-artifacts, contracts, docs-freeze, artifact-ids]
```

It also has `scripts/tests/red_commit_paths.sh` wired inside `protected-artifacts`
(`origin/main:.github/workflows/ci.yml:70-78`). The stale M-63 tree has neither
`scripts/check_docs_freeze.sh`, nor `scripts/tests/red_docs_freeze.sh`, nor
`scripts/check_artifact_ids.sh`, nor `scripts/next_artifact_id.sh`.

M-63 lines 6-10 acknowledge the dependency: RED must not be written before M-60a is in this
branch. That precondition is still false for the committed tree being reviewed. Any RED suite
written on this branch would test the old CI topology and miss current mandatory barrier
surfaces.

### F3 — BLOCKER: C-073 F3/F5 are only partially addressed in rev2 text

Rev2 adds `docs-freeze` and `artifact-ids`, but the mandatory job list is still not coherent:

- `milestones/M-63-ci-cost.md:47` lists `verify_design_claims` as a doc-gate, but current
  `origin/main:.github/workflows/ci.yml` has no `verify_design_claims` job.
- `milestones/M-63-ci-cost.md:118` forbidden-list coverage omits `verify_design_claims`
  while claiming "любой существующий барьерный джоб".
- `milestones/M-63-ci-cost.md:127` acceptance step M proves only `protected-artifacts` and
  `docs-freeze` on a docs commit; it does not name `contracts`, `artifact-ids`,
  `status-check`, or the `red_commit_paths` step now living under `protected-artifacts`.

This leaves the original C-073 risk alive: a dev implementation can preserve scripts while
removing or skipping a barrier in CI aggregation. The milestone must first define the job/step
classes from the actual merge-preview CI tree, then RED must execute that model.

## 2. Answers to the requested checks

- **Completeness of axes:** improved since C-073 (`changed-files source` and packaging were
  added), but not auditable without `red_ci_cost.sh`; current CI also adds surfaces that the
  branch does not contain.
- **Stop rule:** text exists in §4.4, but it does not compensate for missing RED/verify.
- **Anti-placebo:** not presented. `--battery` is only promised in milestone text; no executable
  battery exists.
- **Execution proof:** absent. No command in the committed artifact set computes GitHub-style
  path decisions or proves the promised failure modes.
- **Staleness / partial replacement by main:** yes. M-60a/M-61 and the commit-path hook work
  changed the CI/process surface after this branch forked. M-63 must rebase/merge `origin/main`
  before its RED set is written.

## 3. Required next step

Architect must sync M-63 onto current `origin/main`, regenerate the closed job/step list from
that merge-preview tree, and commit the missing architect artifacts:

- `scripts/tests/red_ci_cost.sh`
- `scripts/verify_M-63.sh`
- updated `milestones/M-63-ci-cost.md` if the current CI topology changes the acceptance table

Then rerun critic on the committed artifact set.
