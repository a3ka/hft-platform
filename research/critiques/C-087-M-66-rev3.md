# C-087 — M-66 protocol-attestation rev3 critic verdict

**Verdict:** APPROVE  
**Date (UTC):** 2026-08-14T20:04Z  
**Subject branch:** `origin/docs/M-66-protocol-attestation`  
**Subject commit:** `cfc9665`  
**Milestone:** `milestones/M-66-protocol-attestation.md`  
**Role:** critic, strong model (`gates.md` §1 / dispatcher mandate), third round

## Scope

I audited the committed artifact set from the merge-base with `origin/main`, not from the
moving tip of `main`:

```text
$ MB=$(git merge-base HEAD origin/main); echo MB=$MB; git diff --name-status "$MB"..HEAD
MB=785d0de35a94cde83fd819733b36556f8f2b45f4
A	milestones/M-66-protocol-attestation.md
A	research/critiques/C-082-M-66-protocol-attestation.md
A	research/critiques/C-085-M-66-rev2.md
```

The branch remains a spec-stage artifact set: the barrier, RED probe, and verify script are
not committed yet.

```text
$ test -f scripts/tests/red_review_fa.sh; echo red_review_fa=$?; test -f scripts/check_review_fa.sh; echo check_review_fa=$?; test -f scripts/verify_M-66.sh; echo verify_M_66=$?
red_review_fa=1
check_review_fa=1
verify_M_66=1
```

That formal deviation from `.claude/agents/critic.md` is unchanged from `C-082`/`C-085`;
this pass follows the dispatcher's narrowed third-round scope: only whether `C-085` B-1 is
closed, with a regression check for already-closed B-2..B-5.

## Verdict

### B-1 closed

Rev3 adds exactly the missing boundary scenarios to axis 7:

```text
$ git diff --unified=40 cfc9665^..cfc9665 -- milestones/M-66-protocol-attestation.md
+| 7 | waiver с ПУСТОЙ причиной: `FA-WAIVER: crates/recorder — ` | W7EMPTY | **FAIL** (`C-085` B-1: порог был объявлен и не запиннен) |
+| 7 | причина на ОДИН символ короче порога (11) | W7SHORT | **FAIL** (нижняя сторона границы) |
+| 7 | причина РОВНО в порог (12) | W7EXACT | **PASS** (верхняя сторона границы) |
```

This pins the declared predicate `FA-WAIVER: crates/<имя> — <причина ≥12 символов>` on both
negative sides and the acceptance boundary:

- an implementation that does not check length is red on `W7EMPTY`;
- an implementation that accepts any non-empty reason is red on `W7SHORT`;
- an implementation that accidentally requires `>12` instead of `>=12` is red on `W7EXACT`.

I do not see a remaining way for an implementation that ignores the threshold to satisfy
the normative table, assuming the RED probe really materializes the table as specified.
The new axis 7 rows are also consistent with §2 W: empty and 11-character reasons fail,
exactly 12 passes, which matches `≥12`.

### B-2..B-5 remain closed

Rev3 only touched the three W7 rows, so the previously closed sections did not materially
move. Spot checks confirm the closure evidence is still present:

```text
$ rg -n "ЧАСТЬ.*TD-105|механизир|status-check|W8NONEEDS|W8NOIF|W8NOCALL|W8OK|ДЕВЯТИ|Startup reading" milestones/M-66-protocol-attestation.md
11:**Долг:** покрывает **ЧАСТЬ** `TD-105` (`пропуск-PR-гейта-обнаруживаем-но-не-предотвратим`)
17:остаётся открытым долгом (close-out §7.6). Слово «механизирует» из rev1 снято: оно обещало
286:| 8 проводка CI | джоб `review-fa` отсутствует в `needs` `status-check` | W8NONEEDS | wiring-секция пробы FAIL |
287:| 8 | джоб есть в `needs`, но нет в условии `if` шага агрегата | W8NOIF | FAIL (вторая рукописная копия списка — §0 стр. 21) |
288:| 8 | джоб не зовёт барьер или не зовёт пробу | W8NOCALL | FAIL |
289:| 8 | полная проводка (реальный `ci.yml` чекаута) | W8OK | PASS — легитимный, гоняется КАЖДЫМ прогоном пробы в CI |
368:| 5 | architect + **FOUNDER-APPROVED** ... строка предъявления в **ДЕВЯТИ** профилях `.claude/agents/*.md` ...
```

Profile count still matches the rev2 fix:

```text
$ grep -l "Startup reading" .claude/agents/*.md | sort; echo count=$(grep -l "Startup reading" .claude/agents/*.md | wc -l)
.claude/agents/architect.md
.claude/agents/critic.md
.claude/agents/engine-dev.md
.claude/agents/research-dev.md
.claude/agents/reviewer.md
.claude/agents/risk-critic.md
.claude/agents/signal-engineer.md
.claude/agents/tester.md
.claude/agents/venue-dev.md
count=9
```

### No TD-140-style fixed-count mismatch found

I grep-checked for fixed scenario/claim counts. The milestone does not claim a total number
of RED scenarios or claims; it says the probe counts and compares the manifest against the
table by composition. The remaining numeric claims are unrelated measurements or local
scenario values.

```text
$ rg -n "[0-9]+ (сценари|claim|строк|ось)|ровно [0-9]+|число сценар|число claim|состав батареи|W7EMPTY|W7SHORT|W7EXACT" milestones/M-66-protocol-attestation.md
283:| 7 | waiver с ПУСТОЙ причиной: `FA-WAIVER: crates/recorder — ` | W7EMPTY | **FAIL** ...
284:| 7 | причина на ОДИН символ короче порога (11) | W7SHORT | **FAIL** ...
285:| 7 | причина РОВНО в порог (12) | W7EXACT | **PASS** ...
378:1. [задача 2] `bash scripts/tests/red_review_fa.sh` — exit 0, число claim'ов СЧИТАЕТСЯ и
```

### Arbitration judgment

I agree with the architect's same-reason assessment. `C-082` B-1 was a hole in the stated
invariant: NO-FA crates could pass without an FA echo or an explicit waiver. `C-085` B-1 was
a narrower defect in the new waiver mechanism: the declared `≥12` reason threshold was not
pinned. Those are connected by the same area of the spec, but not the same reject reason.

The architect also did not dispute `C-085`; the commit accepts the finding and adds the
boundary rows. I therefore do not see a live architect-critic methodological dispute to
escalate from this pass.

## §9 Recheck Coverage

Covered here:

- code/repository claims by command: merge-base diff, rev3 parent diff, artifact presence,
  W7 rows, B-2..B-5 regression greps, profile count, fixed-count grep, and
  `verify_design_claims.sh`;
- authority: critic wrote only `research/critiques/C-087-M-66-rev3.md`; no edits to
  `milestones/`, `docs/`, `contracts/`, `crates/`, `PROJECT-STATE.md`, or `TECH-DEBT.md`;
- connectivity: `C-082`, `C-085`, `gates.md` §0/§1/§9, `testing.md` boundary and
  anti-placebo rules, `docs/04-workflow.md` §3, and `docs/05-contract-layer.md` were checked.

APPROVE: `C-085` B-1 is closed; B-2..B-5 remain closed.
