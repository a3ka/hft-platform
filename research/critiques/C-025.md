# C-025 — M-35 CT-RFC-05 risk-critic gate

**Date (UTC):** 2026-07-25T18:06:39Z  
**Agent:** risk-critic  
**Branch audited:** `origin/feat/M-35-margin-inventory` at `a174696`  
**Worktree:** `/tmp/hft-risk-critic-m35-1785002677` (detached, clean before verdict)  
**Base/range audited:** `1732f91..a174696`  
**Trigger:** `.claude/rules/gates.md` §5 — `crates/contracts/**` + first authenticated data source  
**Verdict:** PASS

## Executive verdict

M-35 may proceed to `venue-dev` task 2. The Binance key is factually read-only, the milestone/task-2 surface is
read-only market-data collection, no order-egress path is introduced, and `MarginInventory` remains a raw
supply-pool MD event outside risk/OMS/order execution.

This PASS does **not** waive the deployment control: `ipRestrict:false` is confirmed and must be flipped to
the VPS IP `167.233.192.131` before/reviewer §8 production deployment.

## Checks

### 1. Key permissions — PASS

I used `/home/nous/hft-platform/.env` without printing key, secret, or signature. `git check-ignore` confirms
`.env` is ignored by `.gitignore:4` in both the primary checkout and this worktree; no `.env` file is tracked.

Signed probe:

```text
GET /sapi/v1/account/apiRestrictions
HTTP 200
enableReading: True
enableSpotAndMarginTrading: False
enableWithdrawals: False
enableFutures: False
enableMargin: False
enableInternalTransfer: False
permitsUniversalTransfer: False
enableVanillaOptions: False
ipRestrict: False
```

The key cannot place spot/margin trades, withdraw, use futures, use margin trading permission, or transfer
internally. `ipRestrict:false` is the remaining exposure and is recorded as a deployment condition, not a
pre-implementation blocker.

Sanity probe:

```text
GET /sapi/v1/margin/available-inventory?type=MARGIN
HTTP 200
available_assets_count: 402
has_USDT: True
has_USDC: True
has_updateTime: True
```

### 2. No order-egress — PASS

Current M-35 diff touches contracts, schema, fixtures, RED tests, milestone/survey, verify script, and critic
artifacts. It does not touch `crates/risk/**`, `crates/oms/**`, `crates/killswitch/**`, or any order path.

Evidence:

- `milestones/M-35-margin-inventory.md:47-52` specifies signed GET with read-only env key and no order-egress.
- `milestones/M-35-margin-inventory.md:77-84` allows `crates/venue-binance/src/lib.rs` for parse/auth poll and
  explicitly forbids risk/ks/oms and any order-egress.
- `scripts/verify_M-35.sh:36-45` has MI-I-3 canary for `newOrder|/order|submit_order|cancel_order|POST.*order`
  whenever the margin path appears.
- Repo grep over M-35 surfaces found no actual submit/cancel/order endpoint implementation; the only order hits
  are the forbidden/canary text and pre-existing descriptive contract prose such as "forced order" liquidation.

`venue-dev` task 2 must stay within this read-only surface: signed GET only, no POST, no order endpoint, no
trading signature beyond the Binance USER_DATA read signature required by this endpoint.

### 3. Secret discipline — PASS with deployment condition

Pass:

- `.env` is git-ignored and not tracked.
- M-35 fixtures/schema/changelog/tests contain no API key/secret material.
- The milestone requires `BINANCE_API_KEY`/`BINANCE_API_SECRET` from env, not journal/git.
- No secret was printed during this review.

Deployment condition:

- `ipRestrict:false` is confirmed by live `apiRestrictions`. Before §8 deployment, the key must be restricted to
  `167.233.192.131`; reviewer §8 must verify this with a redacted `apiRestrictions` proof after the VPS key is
  installed.

### 4. Contract safety — PASS

`MarginInventory` is additive and outside money movement:

- `crates/contracts/src/lib.rs:24-26` sets `SCHEMA_VERSION` to 4 for CT-RFC-05.
- `crates/contracts/src/lib.rs:301-314` appends `MdPayload::MarginInventory` after `L2Delta`, preserving
  discriminator 7 and old variants 0..6.
- `crates/contracts/tests/ct_rfc05.rs:45-73` guards old Funding bytes decoding under schema 4.
- Changed files do not include risk/OMS/killswitch/order code.

Executed tests:

```text
cargo test -p contracts --test ct_rfc05
  3 passed; exit=0

cargo test -p contracts --test red_schema
  2 passed; exit=0

cargo test -p contracts --test ct_rfc01 --test red_rfc02
  10 passed; exit=0

cargo test -p venue-binance --test red_margin_inventory
  error[E0432]: no parse_available_inventory in the root
  exit=101  # expected compile-RED before venue-dev task 2
```

### 5. Design honesty — PASS

The corrected milestone and survey no longer claim a borrow/repay ledger:

- `milestones/M-35-margin-inventory.md:9-20` frames the milestone as proxy-collector and says raw ledger is not
  available.
- `research/data-quality/margin-source-survey.md:411-446` records the read-only re-probe and states `available`
  is raw supply-side pool, not outstanding loan volume or borrow/repay ledger.
- Contract comments and generated schema keep the same boundary: raw available pool now, downstream proxy with
  `derived-from-available-inventory` caveat later.

## Non-blocking condition

`ipRestrict:false` is acceptable only until deployment. It must be flipped to `167.233.192.131` before §8
production rollout. If reviewer §8 sees `ipRestrict:false` after the VPS env key is installed, that is a
deployment blocker.

## Handoff §D

**Next agent:** `venue-dev` task 2.

**Push/branch:** bootstrap from `origin/feat/M-35-margin-inventory` after this risk verdict commit.

**Paste-ready prompt:**

```text
Ты — venue-dev для M-35 task 2 @ origin/feat/M-35-margin-inventory (risk-critic PASS C-025).
CWD /home/nous/hft-platform. По-русски. Забутстрапься с origin в своём worktree.
Реализуй ТОЛЬКО read-only margin path:
- parse_available_inventory(json, assets: &[&str]) -> Vec<MdEvent>;
- signed GET poll /sapi/v1/margin/available-inventory?type=MARGIN using env BINANCE_API_KEY/BINANCE_API_SECRET;
- recorder wiring for USDT/USDC MarginInventory events;
- no order-egress: no POST order, no submit/cancel, no trading/withdraw path;
- secrets only via env, never log key/secret/signature, never write secrets to journal/fixtures/git.
Acceptance: MI-I-1..4 GREEN, scripts/verify_M-35.sh PASS, MI-I-3 canary GREEN. Push your GREEN commit to feat/M-35 before handoff.
```

**Reviewer §8 reminder:** production deployment must prove `ipRestrict:true`/restricted to `167.233.192.131`
after key install, plus healthy recorder and fresh `MarginInventory` USDT+USDC events.

## Confidence

High for PASS. The only remaining risk is operational deployment hygiene (`ipRestrict`), explicitly assigned
to venue-dev/reviewer §8 and not a blocker for read-only implementation.
