---
GATE-META:
  milestone: milestones/M-66-protocol-attestation.md
  audited_repo: origin/feat/M-66-fixture
  audited_base: 785d0de35a94cde83fd819733b36556f8f2b45f4
  audited_head: 551668a0c5491b04cd2cefe17898b61aad806277
  verdict: REJECT
---

# C-089 — M-66 protocol-attestation rev4

## Verdict: REJECT

The rev4 harness closes the prior substantive findings: the B-1 and B-2 regressions, the
`fetch-depth: 0` comment-only false green, and the waiver-substring class all turn the RED
probe red. The milestone is nevertheless blocked by a fifth, undeclared failure in its own
mandatory CI-parity command. `cargo test --all` is not reliable on the audited tree: the
same `gateway` test failed once in ten immediate repetitions. A gate that happens to pass on
most runs is not green.

## Audited artifact set

| Required artifact | Result |
|---|---|
| T-contracts / trait signatures | N/A: M-66 §6 declares no T1 or `crates/**` change; the audited range contains neither `contracts/**` nor `crates/**`. |
| RED specification | PASS: `scripts/tests/red_review_fa.sh` is committed before the barrier (`caa67b8` before `b6fc6af`), has 40 executed claims, a setup guard per scenario, and passes in the reference tree. |
| Acceptance gate | PRESENT: `scripts/verify_M-66.sh` includes CI parity at lines 283–289 and gives a non-zero final verdict on any unexpected failure. |
| CI wiring | PASS: the `review-fa` job is connected to both `status-check.needs` and its result predicate. |
| Milestone | PRESENT: rev4 accurately names the 40 scenarios and 15 mutants; task statuses and the four intentionally open checks agree with the run. |

## B-1 — BLOCKER: CI parity is flaky beyond the four declared open checks

`scripts/verify_M-66.sh:288–289` correctly requires `cargo test --all`. Its normal run has
the four stipulated failures only through T7, then produces a fifth failure:

```text
FAIL  cargo test --all (exit=101)
      ↳ thread 'o1_snapshot_allocation_does_not_grow_with_state' (...) panicked at crates/gateway/tests/red_snapshot_noclone.rs:235:5:
      ↳ O-1 (TD-097): книга выросла в 8 раз — аллокации `snapshot()` выросли в 5.8 раза (5350 → 31170 байт), при том что ВЫХОД не изменился (×1.00).
      ↳ test result: FAILED. 2 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out
VERDICT: FAIL
exit=1
```

This is not a one-off result that can be waived as a host hiccup. Repeating the exact test
ten times at the same audited SHA produced one independent exit 101:

```text
RUN_01 exit=0 test result: ok. 3 passed; 0 failed
RUN_02 exit=101 test result: FAILED. 2 passed; 1 failed
RUN_03 exit=0 test result: ok. 3 passed; 0 failed
RUN_04 exit=0 test result: ok. 3 passed; 0 failed
RUN_05 exit=0 test result: ok. 3 passed; 0 failed
RUN_06 exit=0 test result: ok. 3 passed; 0 failed
RUN_07 exit=0 test result: ok. 3 passed; 0 failed
RUN_08 exit=0 test result: ok. 3 passed; 0 failed
RUN_09 exit=0 test result: ok. 3 passed; 0 failed
RUN_10 exit=0 test result: ok. 3 passed; 0 failed
REPEAT_FAILED=1/10
```

The failing assertion is `crates/gateway/tests/red_snapshot_noclone.rs:235` (`ratio < 2.5`).
M-66 did not introduce the test — `git diff audited_base..audited_head -- crates/` is empty —
but its acceptance script explicitly promises full CI parity. The gate cannot claim a clean
core run while its required command is probabilistic. This is an existing `TD-097` / gateway
test problem, not permission to weaken M-66's verify script or to call a lucky retry green.

### Required condition for APPROVE

Architect must route the `TD-097` test/implementation defect to its owner and present a
deterministic repair. The next M-66 round must show:

1. `cargo test --all` exit 0 reliably on the audited/merge-preview tree; and
2. `bash scripts/verify_M-66.sh` has **only** the four explicitly legitimate failures: T5,
   T6 `TECH-DEBT.md`, T6 `PROJECT-STATE.md`, and T7. Do not mask or remove CI parity.

## Passed rev4 regression checks

- **B-1:** restoring `grep -qF "${id}"` in `check_review_fa.sh` made the real probe fail
  `B4DEAD` and `B4DEADPFX` (38/40), then the source was restored.
- **B-2:** replacing the non-empty `LIVE_CRA` branch with `if false` made the real probe fail
  `B4EMPTYU` (39/40), then the source was restored.
- **N-5:** removing the executable `with:` / `fetch-depth: 0` lines while preserving the
  comment made the actual verify T3 report `review-fa не содержит fetch-depth: 0`.
- **Waiver analogue:** replacing the anchored waiver predicate at
  `scripts/check_review_fa.sh:231` with a substring predicate made `W7EMPTY`, `W7SHORT`, and
  `W7PFX` pass incorrectly (37/40). The committed predicate is anchored and requires the
  12-character reason.
- **Order and cleanup:** every scenario builds a separately registered fixture; battery
  recursion unsets the parent registry; normal execution reports all 40 manifest entries.
  After completion no `/tmp/red-review-fa-*` fixture directories remained (only pre-existing
  regular log files matching that prefix).

## Note

`docs/workflow/harness-track.md`, required by the critic bootstrap for `scripts/**` and CI
subjects, is absent from the audited tree. This did not cause the REJECT above; the audit used
the available binding rules, milestone, FA source, scripts, CI workflow, and the two requested
review verdicts. Restore or locate that canonical harness track before the next harness audit.

## Done Block

```text
$ git rev-parse origin/feat/M-66-fixture
551668a0c5491b04cd2cefe17898b61aad806277

$ base=$(git merge-base origin/main 551668a); echo "$base"
785d0de35a94cde83fd819733b36556f8f2b45f4

$ bash scripts/tests/red_review_fa.sh
PASS  МАНИФЕСТ ⇄ исполнение: 40 сценариев, состав совпал
SPEC_ROWS=40
PASS  СПЕКА⇄МАНИФЕСТ: 40 строк §4 совпали в обе стороны
VERDICT: PASS (40/40)
exit=0

$ bash scripts/tests/red_review_fa.sh --battery
BATTERY_SPEC_NAMES=15
PASS  СПЕКА⇄БАТАРЕЯ: состав мутантов совпал по именам
BATTERY: PASS (17/17)
exit=0

$ # B-1 mutant: grep -qF "${id}"; then restore
FAIL  B4DEAD синтаксически валидный, но мёртвый ID — ПРОШЛО
FAIL  B4DEADPFX мёртвый ID, содержащий живой как префикс — ПРОШЛО
VERDICT: FAIL (2 нарушений; 38/40 сценариев прошли)
exit=1

$ # B-2 mutant: LIVE_CRA branch → false; then restore
FAIL  B4EMPTYU U=∅ при непустом LIVE_CRA — тихий отказ гейта — ПРОШЛО
VERDICT: FAIL (1 нарушений; 39/40 сценариев прошли)
exit=1

$ # N-5 mutant: executable `with:` / `fetch-depth: 0` removed, comment retained
FAIL  T3 review-fa job / status-check wiring не готов — задача 3 OPEN
      ↳ review-fa не содержит fetch-depth: 0
exit=1

$ # waiver substring mutant; then restore
FAIL  W7EMPTY пустая причина waiver — ПРОШЛО
FAIL  W7SHORT причина на один символ короче порога — ПРОШЛО
FAIL  W7PFX waiver называет крейт-суффикс тронутого — ПРОШЛО
VERDICT: FAIL (3 нарушений; 37/40 сценариев прошли)
exit=1

$ bash scripts/verify_M-66.sh
PASS  T2 RED-проба против реального барьера (exit=0)
PASS  T3 review-fa job подключён к CI и status-check
PASS  T4 мутационная батарея red_review_fa.sh --battery (exit=0)
FAIL  T5 founder-approved строка предъявления отсутствует (0/9 профилей; gates.md review-fa=0) — задача 5 ждёт founder
FAIL  T6 TECH-DEBT.md не изменён в диапазоне M-66 — reviewer close-out задача 6 OPEN
FAIL  T6 PROJECT-STATE.md не изменён reviewer'ом — close-out ещё не выполнен
FAIL  T7 docs/fa/derive.md и/или docs/fa/recorder.md отсутствуют — follow-up задача 7 OPEN
FAIL  cargo test --all (exit=101)  # undeclared fifth failure, B-1
VERDICT: FAIL
exit=1

$ for i in $(seq 1 10); do cargo test -p gateway --test red_snapshot_noclone --quiet; done
RUN_02 exit=101; all other runs exit=0
REPEAT_FAILED=1/10
exit=0

$ EVENT_NAME=push PUSH_BEFORE=785d0de35a94cde83fd819733b36556f8f2b45f4 bash scripts/check_docs_freeze.sh
exit=0

$ EVENT_NAME=push PUSH_BEFORE=785d0de35a94cde83fd819733b36556f8f2b45f4 bash scripts/check_protected_artifacts.sh
OK: защищённые артефакты целы на HEAD (785d0de..HEAD; проверка по РЕЗУЛЬТАТУ, не по способу)
exit=0

$ EVENT_NAME=push PUSH_BEFORE=785d0de35a94cde83fd819733b36556f8f2b45f4 bash scripts/check_artifact_ids.sh
OK: ни один коммит диапазона 785d0de..HEAD не ввёл второй носитель под занятым идентификатором
exit=0

$ bash scripts/verify_design_claims.sh --merge-preview origin/main
VERDICT: PASS (0 нарушений)
exit=0

$ bash scripts/next_artifact_id.sh C
C-089
exit=0
```

## Next agent

Architect: keep the four declared lifecycle checks intact, obtain a deterministic TD-097
repair in its owning scope, then submit the resulting committed artifact chain for a new
critic round.
