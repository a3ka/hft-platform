# C-029 - M-38a TD-045 re-review critic verdict

**Date:** 2026-07-27
**Branch:** `feat/M-38a-cvd-session-ledger`
**Audited HEAD:** `1d38a1986fb388134598167c7d8627bc2653fe69`
**Milestone:** `milestones/M-38a-cvd-session-ledger.md`
**Verdict:** PASS

## Scope

Re-review of the TD-045 architect fix set after reviewer PR-REJECT. Audited only the committed
artifact delta in `1d38a19` plus the existing gateway reducer/merge implementation that the new
RED is expected to fail against.

## Pre-flight

PASS.

- `1d38a19` changes only `crates/gateway/tests/red_gateway_window.rs` and
  `milestones/M-38a-cvd-session-ledger.md`.
- `crates/contracts/**` / T1 are untouched.
- Existing CVD session-ledger implementation is unchanged by this re-review fix set.
- `scripts/verify_M-38a.sh` already includes the full `red_gateway_window` test binary, so the
  TD-045 paired RED participates in the acceptance gate.

## Checks

### 1. TD-045 anti-placebo paired RED

PASS. The new oracle fails on audited HEAD, which still contains the old UTC-day merge criterion.

```text
cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_past_session_survives_overlap
=> FAILED
=> TD-045: S1 обязана уцелеть в merged, пока финальное окно её пересекает ... merged.vp=[20279]
=> test result: FAILED. 0 passed; 1 failed; 5 filtered out; exit=101
```

Evidence:

- New test: `crates/gateway/tests/red_gateway_window.rs:471-551`.
- Old failing implementation still present: `crates/gateway/src/lib.rs:1245-1254` drops VP rows via
  `row.session_id >= utc_session_id(frame.at_ms)`, not by the reducer window criterion.

### 2. Paired vantage completeness

PASS. The RED set now covers both directions of the invariant:

- `windowed_live_eq_replay_overlap_multistep` covers "S1 dropped" when the final window is entirely
  inside S2 (`lo = d2+29s`); it remains GREEN.
- `windowed_live_eq_replay_past_session_survives_overlap` covers "S1 survives" while the final
  window still intersects S1 (`lo = d2-15s`); it fails on the current old merge criterion.

Command evidence:

```text
cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_overlap_multistep
=> test result: ok. 1 passed; 0 failed; 5 filtered out; exit=0
```

### 3. Task #11 design adequacy

PASS. The milestone now gives engine-dev the missing structural datum and the correct criterion:

- `SeriesBundle` carries per-session `vp_session_max_time_s: Vec<(session_id, max_time_s)>`.
- `evict_series_bundle_under_window` / `Snapshot::apply` use the same reducer criterion:
  `vp_session_max_time_s[sid] < lo_time_s`.
- No second schema bump is required because v7 is not yet on `main`.

This satisfies both named TD-045 vantages:

- survive: `lo = d2-15s`, `S1 max = d2-1s`, so `S1 max >= lo` and S1 is retained;
- drop: `lo = d2+29s`, `S1 max = d2-1s`, so `S1 max < lo` and S1 is removed.

Implementation note for engine-dev: merge/update the VP session-max map consistently with
`volume_profile` rows. A fix that only changes the retain predicate but does not carry/merge the
per-session max metadata cannot be equivalent to `Reducer::evict_window_state`.

### 4. T1 / TD-046 / CVD-ledger boundaries

PASS.

- T1 is not touched; session remains derived from `ts_exch_ms` / bucket time, not from a new journal
  field.
- TD-046 is correctly kept as NOTE: latent timeframe alignment footgun, current prod default
  `GATEWAY_TIMEFRAME_MS=1000` is aligned.
- The accepted CVD-ledger code path remains out of the `1d38a19` diff.

Formatting check:

```text
cargo fmt --all -- --check
=> exit=0
```

## Findings

No blocking findings.

## Recommended Next Action

Dispatch `engine-dev` on `origin/feat/M-38a-cvd-session-ledger` for M-38a task #11 only:

1. Add `SeriesBundle.vp_session_max_time_s: Vec<(i64, i64)>` as part of v7.
2. Emit it from reducer/full snapshots and frame deltas, sorted by session id.
3. Merge/update the VP session-max metadata in `Snapshot::apply`.
4. Replace the old `session_id < utc_session_id(frame.at_ms)` whole-drop logic with
   `vp_session_max_time_s[sid] < lo_time_s`.
5. Keep CVD ledger semantics unchanged.
6. Run `cargo test -p gateway --test red_gateway_window` and then `bash scripts/verify_M-38a.sh`.
7. Push GREEN implementation commits intra-chain to `origin/feat/M-38a-cvd-session-ledger` after
   `git log origin/feat/M-38a-cvd-session-ledger..HEAD` shows only engine-dev task #11 commits.

## Handoff

=== HANDOFF: critic -> engine-dev ===

## §A - Metadata
- UTC datetime: 2026-07-27T21:54:12Z
- From agent: critic
- Milestone: M-38a - CVD session-anchored ledger
- Branch: `origin/feat/M-38a-cvd-session-ledger`
- Audited HEAD: `1d38a1986fb388134598167c7d8627bc2653fe69`
- Verdict: PASS
- Verdict artifact: `research/critiques/C-029.md`

## §B - What I checked
- Verified TD-045 paired RED is anti-placebo and fails on the current old VP merge criterion.
- Verified the existing whole-drop overlap vantage still passes.
- Verified task #11 design uses the reducer-equivalent `max_time_s < lo_time_s` criterion.
- Verified T1/contracts are untouched and TD-046 is correctly left as NOTE.

## §C - Command evidence
- `cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_past_session_survives_overlap` -> exit=101, expected RED.
- `cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_overlap_multistep` -> exit=0.
- `cargo fmt --all -- --check` -> exit=0.

## §D - Next agent + invocation
- **Next agent:** `engine-dev`
- **Task:** M-38a task #11 only.
- **Invocation:** run engine-dev on `feat/M-38a-cvd-session-ledger`.
- **Paste-ready prompt for engine-dev:**
  ```text
  Ты - engine-dev. Работай в чистом worktree от origin/feat/M-38a-cvd-session-ledger после critic PASS C-029.

  Milestone: milestones/M-38a-cvd-session-ledger.md
  Scope: task #11 only, TD-045.
  Allowed implementation path: crates/gateway/src/lib.rs.
  Forbidden: crates/contracts/**, crates/risk/**, crates/killswitch/**, crates/venue-*/**, crates/gateway/tests/**, scripts/verify_*.sh, milestones/** except Status column if your workflow requires it.

  Implement:
  - SeriesBundle carries per-session vp_session_max_time_s: Vec<(i64, i64)> as part of v7, no second schema bump.
  - Reducer/full snapshots and frame deltas emit VP session max times sorted by session id.
  - Snapshot::apply / evict_series_bundle_under_window use the same criterion as Reducer::evict_window_state: drop a VP session only when vp_session_max_time_s[sid] < lo_time_s.
  - Remove the old utc-day predicate row.session_id < utc_session_id(frame.at_ms).
  - Keep the accepted CVD session-ledger behavior unchanged.

  Required checks:
  - cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_past_session_survives_overlap
  - cargo test -p gateway --test red_gateway_window windowed_live_eq_replay_overlap_multistep
  - cargo test -p gateway --test red_gateway_window
  - bash scripts/verify_M-38a.sh; echo exit=$?

  Commit atomically as engine-dev for task #11. Before pushing, run:
  git log origin/feat/M-38a-cvd-session-ledger..HEAD --format='%h %s'
  Push intra-chain only if that range contains only your task #11 implementation commit(s):
  git push origin HEAD:feat/M-38a-cvd-session-ledger
  Then emit Done Block + handoff to tester.
  ```

## §E - Outstanding items / risks
- TD-046 remains NOTE/follow-up, not a blocker for M-38a.
- Engine-dev must preserve the accepted CVD ledger behavior while touching shared `SeriesBundle` merge code.

=== END HANDOFF ===
