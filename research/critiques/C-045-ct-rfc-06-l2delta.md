# C-045 — `docs/rfc/CT-RFC-06-l2delta.md` plan-time gate

**Date (UTC):** 2026-08-02T00:00Z
**Agent:** critic
**Branch audited:** `origin/docs/ct-rfc-06-l2delta` at `cdd2dd6`
**Worktree:** `/tmp/hft-critic-rfc06`
**Scope:** DOC-гейт класс A (`.claude/rules/gates.md` §9) — plan-time review документа,
опровергающего посылку мандата M-45 («новый T1-вариант `L2Delta`») и вместо этого закрывающего
T1-смежные вопросы (эпохи, восстановимость, детерминизм, объёмы, план приземления).

**Verdict: PASS**

## Резюме

Документ (366 строк, STATUS: PROPOSED) исключительно хорошо сверен с кодом. Каждое проверяемое
утверждение — SHA, номера строк, содержимое кода, цифры замеров, цитаты других докс — проверено
мной независимо (не по тексту документа) и **совпадает**. Не найдено ни одного фактического
расхождения. Central claim документа (§0.2: `L2Delta` уже существует в T1, CT-RFC не нужен для
M-45) подтверждён.

## Главная проверка — опровержение посылки мандата (§0.2)

**ПОДТВЕРЖДЕНО.** `MdPayload::L2Delta` (дискриминант 6, поля `bids/asks/first_update_id/
final_update_id/prev_final_update_id/ts_exch_ms`) реально существует в `crates/contracts/
src/lib.rs:293-300` на `4ccefbc` — байт-в-байт совпадает с формой, которую документ ратифицирует
в §1. `git cat-file -e f635bd2^{commit}` / `7a237f7^{commit}` — оба OK, subject-строки совпадают
с цитатой документа (M-18 merge + TD-031 schema-epoch isolation). `milestones/
M-18-l2delta-capture.md`: STATUS ✅ CLOSED 2026-07-21, содержит критик C-017 APPROVE + risk-critic
C-018 PASS. Allow-list-константы `L2DELTA_CAPTURE_SYMBOLS = &["BTCUSDT"]` реально существуют в
`crates/venue-binance/src/lib.rs:485` и `crates/venue-binance-futures/src/lib.rs:460` с
doc-комментариями, дословно совпадающими по смыслу с цитатой в RFC. `docs/DESIGN.md` §17
(строки 564+) прямым текстом содержит «Механизм есть (M-18), CT-RFC не нужен» — ровно как
процитировано документом. Вывод §8.1 (M-45 = расширение allow-list, без contract-пакета) —
корректное следствие.

## Проверенная фактура (сырой вывод команд, по пунктам инвокации)

**1. SHA — все 5 существуют и совпадают по subject:**
```
f635bd2 OK: merge(M-18): CT-RFC-04 L2Delta — сырые book-дельты (BTC-only spot+perp) [reviewer APPROVED]
7a237f7 OK: merge(M-18): TD-031 schema-epoch isolation — reviewer APPROVED
d896b98 OK: merge(M-51): DET-I-1/2/3 — бит-идентичный реплей потока, проекций и редьюсеров (TD-007)
163939a OK: merge(M-50): скан пола видит крупные события — JR-I-9, TD-053 CLOSED
6122fce OK: research(M-45): карта влияния L2Delta — поломка match, объёмы, риски миграции
```
Плюс `cdd2dd6` (сама ветка) и `4ccefbc` (заявленный HEAD `origin/main` на момент написания) — оба
существуют. 0 из 7 цитируемых SHA — фантомные (контраст с C-044: там было 3 из 4 несуществующих).

**2. Карта exhaustive-`match` — независимый грep подтверждает ровно ПЯТЬ мест, не три:**
Найдены все 8 вариантов `MdPayload` (`Funding, L2Delta, L2Snapshot, Liquidation,
MarginInventory, MarginRate, OpenInterest, Trade`) грепом по enum-декларации. Скан ВСЕХ `.rs`
файлов вне `tests/` на ≥5 из 8 упомянутых вариантов даёт РОВНО эти 6 кандидатов:
`research-cli/src/bin/latency_probe.rs`, `recorder/src/lib.rs`, `venue-binance-futures/src/
lib.rs` (5 упоминаний), `journal/src/segments.rs`, `sim/src/exchange.rs`,
`journal/examples/dump.rs`. Прочёл каждый: `venue-binance-futures/src/lib.rs` — КОНСТРУКТОР
(`payload: MdPayload::X { .. }`), не `match`-scrutinee — не считается. Оставшиеся 5 файлов —
все содержат exhaustive `match &md.payload`/`match p`/`match m.payload` БЕЗ `_ =>`,
все 8 вариантов явно поимённо перечислены (сгруппированы `|` где применимо). Построчно совпадает
с таблицей §8.2 документа (владелец крейта, номер строки — с точностью до пары строк, документ
сам честно предупреждает о дрейфе строк). Это ровно карта из `research/measurements/
m-45-l2delta-impact.md` §1.1 (тот файл сам уже насчитывает 5, не 3 — расхождение «3 vs 5» в
документе относится к исходному МАНДАТУ задачи M-45, а не к самому измерению; сверено `git show
6122fce:research/measurements/m-45-l2delta-impact.md`).

**3. Эпохи (§3) — механизм реально в коде, не предложение:**
`SegmentHeader.epoch_id: String` — реальное поле (`contracts/src/lib.rs:97-108`), doc-comment
дословно «Стабильный ключ эпохи... Смешение эпох — ЯВНОЕ решение». `decide_open_segment`
(`journal/src/segments.rs:1950+`) реально требует `header.source == cfg.source && ... &&
header.epoch_id == cfg.epoch_id && header.schema_version == ...` (строки 52-55) — machine-level
изоляция подтверждена. `recorder/src/main.rs:479-482` реально читает `EPOCH_ID` из env с
дефолтом `default_epoch_id_now()`. Решает ли E-001? Частично и честно признано документом:
механизм даёт машинную различимость НА БУДУЩЕЕ (новый `epoch_id` ДО рестарта), но не
восстанавливает ретроактивно уже смешанные 123 млн событий E-001 — документ этого не заявляет,
только предотвращение повтора. Корректно.

**4. `JR-I-10` (§4) — сформулирован исполнимо, предложен (не ратифицирован).** Инвариант
(«для любого seq S... существует достижимый якорь L2Snapshot с seq≤S либо покрывающий
checkpoint») — формально проверяемое утверждение, RED-оракул явно описан (сравнение с
constructed сценарием prune при непокрытых дельтах). Механика, на которую он опирается
(`compact_segment`/`compact_closed_segments` — `journal/src/segments.rs:2791+/2962+`;
`RetentionPlan.offload_and_prune` — `2156+`; `checkpoint_covered_through_seq`/
`allow_prune_without_checkpoint` — `2103+/2117+/2121+`) — вся подтверждена чтением кода,
строки совпадают. Замечание (не блокер): §9 (founder-решения) явно перечисляет только 4 пункта
для подписи, и `JR-I-10` не назван отдельным пунктом (подразумевается под п.1 «ратификация
CT-RFC-06 as whole», но не explicit) — architect стоит явно включить `JR-I-10` в список при
следующей правке, если предполагается его закрепление как sacred-инвариант наравне с `RK-I-*`.

**5. `DET-I-1` на смешанном журнале (§5) — обосновано по существу:**
`book::Books::apply` (`book/src/lib.rs:322-350`) реально матчит `L2Snapshot`/`L2Delta` в одном
методе, `_ => {}` для остального — подтверждено чтением. `sim/src/exchange.rs:227-288` реально
игнорирует `L2Delta` явным комментированным armом (двойной счёт книги). `recon_loop.rs:55-61`
реально матчит ТОЛЬКО `L2Snapshot`, `_ => return` для всего остального включая `L2Delta` —
подтверждает и claim §5 «recon сегодня не деградирует», и честно поднятый риск (окно слепоты
при инверсии ролей). `TECH-DEBT.md:747` — TD-007 CLOSED 2026-08-02, M-51, `d896b98` — совпадает.
Математика «12 дней прод-факт» (2026-07-21 M-18 → 2026-08-02 RFC-дата) — корректна.

**6. TD-053/`SCHEMA_VERSION` (§2, §6) — совпадает с фактическим состоянием:**
`TECH-DEBT.md:1112` — TD-053 CLOSED 2026-08-01, M-50, `163939a` — подтверждено.
`READABLE_SCAN_MAX_CARRY = 64*1024` (`segments.rs:1568`), `FRAME_LEN_SANITY_CAP = 64*1024*1024`
(`segments.rs:431`) — оба существуют, номера строк точны. Числа замера сверены с исходником
`research/measurements/td-053-event-size.md` (коммит `3efaabc`, НЕ ancestor `4ccefbc` — отдельная
research-ветка, но содержимое процитировано документом верно): макс. `L2Delta` реальный —
32 799/32 807 B = 50.1% — дословное совпадение; синтетический порог — 3236 уровней/сторону,
71 237 B — дословное совпадение; `L2Snapshot` bucket-cap 3000 уровней = 66 032 B = 100.8% —
дословное совпадение. `SCHEMA_VERSION` history: `git log -S "SCHEMA_VERSION: u32 = 3"` →
`f36f8ca` (TD-031, 2→3); `git log -S "= 4"` → `e06e48a` (CT-RFC-05 MarginInventory, 3→4) —
оба подтверждают текст §2 документа буквально.

**7. Объёмы (§7) — все цифры из первички, не оценки:**
`docs/06-data-layer-and-storage.md` §2 (проверено чтением коммита `0307035`, датированного
именно 2026-07-14, как и цитирует RFC): «8.83 GB/сут сырых, ~1 GB/сут zstd (9.1×), L2Snapshot
~96% байт, 20–31 KB каждый, 1800–2850 уровней, ~1/с» — дословное совпадение с таблицей §7 RFC.
§5.1 того же файла: «2.5–3× меньше сверх zstd [verify-at-impl]» — дословное совпадение, включая
честный тег `[verify-at-impl]` (RFC его не срезал). `milestones/M-45-persist-l2delta.md:17-18`:
«Замер (2026-07-27, живой Binance WS): ~14 уровней, ~7 Гц → ≈1.5 KB/с... ~0.1 KB/с средний...
+0.1–0.3 GB/сут» — дословное совпадение с таблицей RFC. Ни одно число в §7 не оказалось
придуманным или искажённым.

**8. `RN-8`/`RN-18` (§8.1 п.4) — реальные закреплённые классы уроков**, многократно
цитируются в `.claude/rules/gates.md` и `TECH-DEBT.md` независимо от этого RFC.

## Что НЕ проверял

- `crates/journal/src/segments.rs:2103-2400` построчно целиком (retention-код) — проверил только
  цитируемые сигнатуры/константы, не всю логику `RetentionPlan`-сборки.
- Не запускал `cargo build --workspace`/`cargo clippy --all-targets` сам — полагаюсь на то, что
  E0004 математически обязан сработать на 5 exhaustive-match местах при добавлении НОВОГО
  варианта (это не текущая ситуация — L2Delta уже есть; проверка актуальна для гипотетического
  CT-RFC-07/HL, §8.2).
- Полный текст `docs/07-cockpit-backend-roadmap.md` §14 (упомянут вскользь, не цитируется дословно
  как источник факта) — не читал.
- Достижимость Hyperliquid `l2Book` семантики (§9.3) — не код-факт, а продуктовый вопрос,
  оставлен founder'у самим документом; не аудировал отдельно.

## Handoff

PASS → architect передаёт документ на risk-critic (contracts-тематика = RISK-BLOCK,
`.claude/rules/gates.md` §5, как сам документ и указывает в шапке "Гейты впереди"), затем founder
(§9, 4 явных пункта решения). Единственная non-blocking рекомендация: явно включить `JR-I-10` в
список founder-решений §9 (или обосновать, почему он не sacred и не требует явной подписи) при
следующей итерации — не блокирует текущий гейт.

=== HANDOFF: CRITIC → ARCHITECT ===

## §A — Метаданные
- Дата (UTC, ISO-8601): 2026-08-02T00:00Z
- Milestone: CT-RFC-06 (docs/ct-rfc-06-l2delta)
- Статус: DONE (critic-гейт пройден)
- HEAD: cdd2dd6 — docs(CT-RFC-06): §6-§9 — TD-053 CLOSED (M-50/JR-I-9), объёмы по замерам, план с картой 5 match (не 3), решения founder'а

## §B — Что я сделал
- Проверил опровержение посылки мандата (§0.2): `L2Delta` реально существует в T1 (CT-RFC-04/M-18), подтверждено кодом и всеми цитируемыми SHA.
- Независимо перегреп карту exhaustive-`match` по `MdPayload` — подтвердил ровно 5 мест (не 3), совпадает с §8.2.
- Проверил все 7 цитируемых SHA (`git cat-file -e`) — все существуют.
- Сверил эпох-механизм (`SegmentHeader.epoch_id`, `decide_open_segment`, `EPOCH_ID` env), retention-механизм, DET-I-1 на смешанном журнале, TD-053/SCHEMA_VERSION числа, объёмы §7 — все построчно совпадают с кодом/докс.
- Написал вердикт `research/critiques/C-045-ct-rfc-06-l2delta.md`, закоммитил на эту ветку.

## §C — Артефакты / результаты
- `research/critiques/C-045-ct-rfc-06-l2delta.md` (создан)
- N/A Done Block — read-only роль, критик не запускает build/test workspace

## §D — Следующий агент + инвокация
- **Следующий агент:** `risk-critic`
- **Paste-ready промпт:**
  ```
  RISK-BLOCK гейт для CT-RFC-06 (ветка docs/ct-rfc-06-l2delta @ cdd2dd6, docs/rfc/CT-RFC-06-l2delta.md,
  366 строк). Critic-вердикт PASS (research/critiques/C-045-ct-rfc-06-l2delta.md) — фактура
  документа проверена и совпадает с кодом. Документ ОПРОВЕРГАЕТ посылку мандата M-45: T1-вариант
  L2Delta уже существует (CT-RFC-04/M-18), новый contract-пакет не нужен, M-45 сводится к
  расширению allow-list. Твоя задача — RISK-BLOCK-проверка (asymmetric cost): §4 (JR-I-10,
  якорная достаточность при ретеншене/компакции — sacred-кандидат), §5 (DET-I-1 на смешанном
  журнале, recon-петля слепнет при инверсии ролей якоря — RK-I-8-смежный риск), §9 founder-пункты
  (особенно п.2 — инверсия ролей асимметрична по цене ошибки, дефолт «не инвертировать» до
  апдейта recon). Вердикт KILL/CONCERNS/PASS в research/critiques/C-0NN.
  ```
- Push-статус: ⏸ commits ready; я запушу сразу после коммита вердикта (мой шаг, read-only роль коммитит только verdict-файл)

## §E — Риски / открытые вопросы
- `JR-I-10` не назван explicit-пунктом в §9 founder-решениях (только косвенно под п.1) — non-blocking рекомендация архитектору, не блокер этого гейта.
- Числа TD-053 (`research/measurements/td-053-event-size.md`, commit `3efaabc`) цитируются документом верно, но сам файл лежит на research-ветке, НЕ ancestor `main`/RFC-ветки на момент проверки — рекомендация: смёржить/сослаться так, чтобы источник был читаем без отдельного `git show <ветка>:<путь>`.

=== END HANDOFF ===
