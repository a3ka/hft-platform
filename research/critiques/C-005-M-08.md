# C-005 — Critic Verdict — M-08 Data durability

**Date:** 2026-07-13T17:07Z  
**Milestone:** `milestones/M-08-data-durability.md`  
**Audited commit:** `4c30461` (`feat/M-08`)  
**Audited range:** `ec1062e..4c30461` — CT-RFC-02, M-08 milestone, T1 forms,
journal/research skeleton signatures, RED tests, verify script, deploy CI gate.  
**Critic:** critic (plan-time gate), per `.claude/rules/gates.md`.
**Trigger match:** `crates/contracts/**` T1 touch + >=5 commits, so critic is mandatory
per `.claude/rules/gates.md:7-18`.

## Verdict

**REJECT**

## Verdict Justification

The direction is correct and several RED oracles are strong, especially segment rotation,
epoch filtering, and bounded stream memory. Dev dispatch is blocked because the T1
contract-RFC is incomplete under the project's own governance, the legacy segment
classification is currently specified as a fail-open fallback, and the RED suite leaves
material holes in E7/E8 and grid equivalence.

## Pre-flight Verification

- Commit chain present: PASS. Range `ec1062e..4c30461` contains RFC, milestone, T1
  forms, protocol/API skeletons, RED tests, verify script, and deploy gate.
- Contract-RFC completeness: FAIL. `docs/05-contract-layer.md:48-51` and
  `docs/fa/contracts.md:71-77` require Rust type change, generated JSON Schema,
  schema bump, migration note, valid/invalid fixtures, CHANGELOG, and at least one test
  in one atomic RFC. This range has Rust type change, schema bump, migration note, and
  tests, but no generated schema, no fixtures, no CHANGELOG, and no `schemars` dependency.
- RED state checked: PASS as RED. `bash scripts/verify_M-08.sh; echo exit=$?` produced
  `VERDICT: FAIL (9 провалов)`, `exit=1`.

## Findings

### CRITICAL @ HIGH-confidence

**C1 — CT-RFC-02 touches T1 but omits required JSON Schema, fixtures, and CHANGELOG.**

- Evidence: `docs/05-contract-layer.md:48-51` requires an atomic contract-RFC containing
  generated JSON Schema, valid/invalid fixtures, CHANGELOG, and tests in the same PR.
- Evidence: `docs/fa/contracts.md:71-77` repeats the same requirement and says
  rationale-only/schema-only PRs are not reviewed.
- Evidence: `docs/rfc/CT-RFC-02-journal-provenance.md:107-115` itself lists generated
  JSON Schema, fixtures, and CHANGELOG as part of this atomic change.
- Evidence: `crates/contracts/Cargo.toml:10-15` has `serde`, `serde_json`, and `postcard`,
  but no `schemars`; `find . -maxdepth 5 ... schemas/fixtures/CHANGELOG` returned no
  CT-RFC-02 schema, fixtures, or changelog artifacts.
- Required fix: before dev dispatch, either complete the RFC package with generated
  schema, valid/invalid fixtures, CHANGELOG, and a CT-I-4 verify test, or do not land the
  `crates/contracts/**` T1 change in this milestone. Deferring CT-I-4 to an impl task is
  not acceptable for a T1-shape change because §4 explicitly requires it in the same RFC.

**C2 — Legacy no-header imputation is specified too broadly and can misclassify corrupt or foreign data as `OwnCapture`.**

- Evidence: `docs/rfc/CT-RFC-02-journal-provenance.md:81-84` says if parsing the first
  frame as `SegmentHeader` fails, the segment is treated as schema 1 with implied
  `OwnCapture`.
- Evidence: the only legacy RED case is a valid old-format segment
  (`crates/journal/tests/red_segments_epochs.rs:41-91`). There is no negative oracle for
  corrupt bytes, a headerless vendor import, a foreign old-format segment, or a malformed
  first frame.
- Why this blocks dev: the central risk of CT-RFC-02 is silent mixing of data epochs.
  A broad "header parse failed -> OwnCapture" rule is exactly a silent provenance
  assignment path. A dev can implement the RFC literally and classify a bad or foreign
  segment as our own capture.
- Required fix: make legacy imputation narrow and fail-closed. Acceptable shapes:
  add a magic/type discriminator for new segment headers and treat missing header as
  legacy only when the first frame strictly decodes as old `Event` and the segment matches
  the recognized pre-RFC legacy constraints; or keep a manifest/fingerprint for the
  existing 8.3 GB legacy segment. Add RED tests proving corrupt/foreign/headerless vendor
  segments error instead of receiving `own-legacy-pre-rfc02`.

### MAJOR

**M1 — E8 gap-statistics has no RED oracle.**

- Evidence: E8 promises `research/data-quality/gaps-<epoch>.json` and report linkage
  (`milestones/M-08-data-durability.md:51-53`).
- Evidence: Task 5 requires gap statistics as part of research-cli work
  (`milestones/M-08-data-durability.md:79`).
- Evidence: `crates/research-cli/tests/red_stream_grid.rs:138-185` covers bounded memory,
  and `:181-234` covers partial in-memory equivalence. It never asserts a gaps artifact,
  gap counts, or report-time requirement.
- Required fix: add a sacred RED test that builds a journal with intentional
  `ConnDown/ConnUp` and wall-clock gaps, runs the research path, and asserts a deterministic
  `research/data-quality/gaps-<epoch>.json` payload. Also add a verify check so reports
  cannot omit the gap artifact.

**M2 — E7 recorder bounded-RSS is not covered by a recorder-level oracle.**

- Evidence: E7 promises a bounded-RSS recorder oracle (`milestones/M-08-data-durability.md:51`).
- Evidence: Task 4 explicitly includes recorder bounded-RSS
  (`milestones/M-08-data-durability.md:78`).
- Evidence: the milestone says E7 is covered by writer-path tests plus VPS eyes-on
  (`milestones/M-08-data-durability.md:111-114`), but those tests exercise `Journal::open_with`
  and `append`; they do not run recorder receive/write loops, channel behavior, venue fan-in,
  heartbeat handling, or any recorder-owned buffers.
- Required fix: add a recorder-level bounded-memory RED with a fake `EventKind` producer and
  counting allocator or equivalent RSS recorder. If the team intentionally wants E7 to be
  eyes-on only, remove it from RED/Task 4 acceptance and state that tradeoff explicitly.

**M3 — `red_stream_grid` equivalence is incomplete.**

- Evidence: `crates/research-cli/tests/red_stream_grid.rs:223-234` compares `params_hash`,
  `intents`, `fills`, `net_pnl_e8`, `turnover_e8`, and only the length of `returns`.
- Missing: return values, `sharpe`, `max_drawdown_e8`, `params`, and ledger side effects.
  A streamed implementation can alter the return series values, Sharpe, drawdown, or ledger
  records while still passing this equivalence oracle.
- Required fix: compare the full `CellResult` semantics: exact `returns` vector,
  `sharpe` within a named tolerance or exact deterministic value, `max_drawdown_e8`,
  `params`, and the ledger records/count/content written by both paths.

**M4 — Disk fail-closed oracle is useful but too easy to satisfy and misses the promised `Sys`/alert behavior.**

- Evidence: E4 requires alert + halt with explicit `Sys` event
  (`milestones/M-08-data-durability.md:48`).
- Evidence: Task 3 repeats `halt + Sys-событие + алерт`
  (`milestones/M-08-data-durability.md:77`).
- Evidence: the RED test only sets `min_free_bytes = u64::MAX` and asserts `append` returns
  `Err` (`crates/journal/tests/red_retention.rs:53-60`), then checks `free_bytes` is not a
  trivial stub (`:69-79`).
- Required fix: strengthen the oracle by using `free_bytes(dir) + 1` instead of only
  `u64::MAX`, asserting a specific storage-guard error, proving no event/frame/seq advance
  occurred after the failed append, and verifying the promised alert/`Sys` observable or
  removing that promise from E4/Task 3.

**M5 — `verify_M-08.sh` is a real gate, but it has false-positive/weak structural checks.**

- Evidence: the script has `set -euo pipefail`, an explicit failure aggregator, final verdict,
  and exit 1 on failures (`scripts/verify_M-08.sh:1-72`).
- Evidence: actual run returned `VERDICT: FAIL (9 провалов)`, `exit=1`.
- Evidence: `T11a` failed on a comment in `crates/journal/src/segments.rs:7`, not on a
  production hardcoded segment path. The grep is declared as "prod-path" but includes prose
  comments (`scripts/verify_M-08.sh:59`).
- Evidence: `T12` only greps for `needs: ci` anywhere in the YAML
  (`scripts/verify_M-08.sh:65`), which a comment or unrelated job could satisfy.
- Required fix: narrow `T11a` to real production constants/usages, not comments; parse
  `.github/workflows/deploy.yml` structurally enough to assert `jobs.deploy.needs` includes
  `ci`; add CT-I-4 schema/fixture checks from C1.

**M6 — Deploy is gated on CI, but the deploy script itself is not fully fail-closed.**

- Evidence: `deploy` has `needs: ci` (`.github/workflows/deploy.yml:57-60`), and the `ci`
  job blocks on non-success or timeout (`.github/workflows/deploy.yml:39-55`).
- Evidence: `workflow_dispatch` remains enabled (`.github/workflows/deploy.yml:12-22`), but
  manual runs still pass through the same `deploy.needs: ci` edge. I do not treat this as a
  CI-bypass by itself.
- Evidence: the SSH script uses `set -uo pipefail`, not `set -e`
  (`.github/workflows/deploy.yml:70-74`). If `git fetch` or `git reset` fails, the script can
  continue and run Docker against a stale VPS checkout.
- Required fix: use `set -euo pipefail` in the SSH script and split the fetch/reset commands
  so any failed checkout update aborts before `docker compose up`.

### MINOR / NOTES

**N1 — `ColdCopyProof` constructor privacy is structurally correct, but the compile-fail comment is not an executable gate.**

- Evidence: `ColdCopyProof` has a private field (`crates/journal/src/segments.rs:124-127`),
  so external crates cannot construct it directly.
- Evidence: the `compile_fail` block lives in an integration test file comment
  (`crates/journal/tests/red_retention.rs:12-17`), which is documentation, not a reliable
  doctest gate for the library API.
- Suggested fix: move the compile-fail example to public API docs in `segments.rs` or add a
  `trybuild` test if the project wants executable proof of the type barrier.

## Requested Checks

1. **CONTRACT-RFC completeness:** FAIL. Of the seven `05 §4` requirements, the range has
   Rust T1 change, schema bump, migration note, and tests. It lacks generated JSON Schema,
   valid/invalid fixtures, and CHANGELOG. `Event`/`EventKind` were not changed
   (`crates/contracts/src/lib.rs:75-94`), which is good. Initial `DataSource` order is covered
   by a postcard discriminant RED (`crates/contracts/tests/red_rfc02.rs:80-96`), but future
   changes still need the generated schema/fixture gate.
2. **Migration / CT-I-3:** REJECT until C2 is fixed. Imputing the known legacy prod segment
   is valid, but the current fallback is too broad and lacks negative tests.
3. **Anti-placebo RED:** PARTIAL. `red_stream_bounded` is strong: it checks correctness,
   absolute memory, and 16 MiB -> 64 MiB size independence
   (`crates/journal/tests/red_stream_bounded.rs:100-157`). `red_segments_epochs` catches
   "read every segment in the directory" (`crates/journal/tests/red_segments_epochs.rs:94-150`).
   `red_retention` has the right `ColdCopyProof` type direction, but disk fail-closed is
   under-specified (M4). `red_stream_grid` is not equivalent enough (M3).
4. **Oracle holes E1-E9:** E1/E2/E3/E5/E6 are materially covered. E4 is partial. E7 and E8
   are real holes, not merely eyes-on items, if they remain task/acceptance requirements.
   E9 is directionally covered, with hardening needed in M5/M6.
5. **Fail-closed disk:** PARTIAL. `u64::MAX` catches absence of a threshold check, but not
   the real production degradation semantics promised by E4.
6. **Scope / carve-out A3:** PASS. The carve-out is declared
   (`milestones/M-08-data-durability.md:66-69`), and the diff in
   `crates/research-cli/src/grid.rs:238-262` is only `JournalSource` plus
   `run_grid_streamed(...)` with a `todo!()` body. No implementation logic was added there.
7. **Verify + deploy:** Verify is a real fail gate and returned the expected RED exit, but
   needs the structural hardening above. Deploy now waits on CI by job dependency, and
   `workflow_dispatch` does not bypass `needs`; the SSH script still needs `set -e`.
8. **Prod-risk / eyes-on:** The §8 plan is necessary but not sufficient. Before reviewer
   close-out, eyes-on must explicitly prove: no deletion on the first run; old 8.3 GB segment
   unchanged and readable via the constrained legacy path; new segment has a valid header;
   seq continuity across old/new segments; recorder heartbeat fresh; segment grows; RSS stable;
   disk free/threshold reported; and cold-copy/retention is either dry-run/no-prune or proven
   against a non-production fixture before touching the sole prod copy.

## Verify Output Summary

Command executed:

```bash
bash scripts/verify_M-08.sh; echo exit=$?
```

Result:

- `VERDICT: FAIL (9 провалов)`
- `exit=1`
- PASS: fmt, clippy, contracts RED, recorder tests, upper-layer regression, explicit
  `EpochFilter` grep, single `SegmentHeader` definition, deploy `needs: ci` grep.
- FAIL: T3 rotation, T4 stream bounded, T5 legacy/epochs, T6 retention/disk guard, T7
  journal regression, T9/T9b research stream grid, T11a hardcoded segment grep, T11b
  remaining `journal::read_all` in research-cli.

## Recommended Next Action

Architect should revise architect-owned artifacts only, then re-run critic:

1. Complete CT-RFC-02 schema/fixtures/CHANGELOG/CT-I-4 verify package.
2. Tighten legacy fallback and add corrupt/foreign/headerless-vendor negative RED tests.
3. Add E8 gap-statistics RED and recorder-level E7 RSS RED, or explicitly remove them from
   RED/acceptance.
4. Strengthen `red_stream_grid` equivalence and disk fail-closed oracles.
5. Harden `verify_M-08.sh` structural checks and `deploy.yml` SSH fail-closed behavior.

## Confidence

High. I read the local rules (`gates`, `testing`, `scope-guard`, `handoff-block`),
contract and journal architecture docs, TECH-DEBT entries TD-006/TD-011/TD-013/TD-015,
CT-RFC-02, M-08, all new/changed source and RED files, deploy/CI workflows, and ran the
M-08 verify script at audited HEAD `4c30461`.

=== HANDOFF: critic → architect ===

## §A — Метаданные
- Дата (UTC, ISO-8601): 2026-07-13T17:07Z
- Milestone: M-08-data-durability
- Статус: BLOCKED (critic REJECT before dev)
- HEAD: 4c30461 — docs(M-08): RED-список, carve-out A3; gates: уточнение причины TD-015 (по reviewer)

## §B — Что я сделал
- Проверил commit range `ec1062e..4c30461` и обязательный critic trigger.
- Прочитал CT-RFC-02, M-08, contract/journal docs, TD ledger, changed source, RED tests, verify script, deploy/CI workflows.
- Запустил `bash scripts/verify_M-08.sh; echo exit=$?`.

## §C — Артефакты / результаты
- Verdict file: `research/critiques/C-005-M-08.md`
- Verdict: REJECT.
- Verify: `VERDICT: FAIL (9 провалов)`, `exit=1`.
- Blocking classes: incomplete T1 contract-RFC package, fail-open legacy imputation, missing/weak RED oracles for E7/E8/grid/disk, verify/deploy hardening.

## §D — Следующий агент + инвокация
- **Следующий агент:** `architect`
- **Paste-ready промпт:**
  ```
  Ты — architect. Рабочий каталог /home/nous/hft-platform, ветка feat/M-08.

  Прочитай critic verdict `research/critiques/C-005-M-08.md`. Исправь только architect-owned
  artifacts M-08 до dev dispatch:
  1. CT-RFC-02: добавь generated JSON Schema, valid/invalid fixtures, CHANGELOG и CT-I-4 verify
     gate, либо убери T1 change до готовности полного RFC package.
  2. Legacy migration: сузь no-header imputation до распознанного legacy-сегмента и добавь RED,
     где corrupt/foreign/headerless-vendor segment fail-closed, а не получает OwnCapture.
  3. E8/E7: добавь gap-statistics RED и recorder-level bounded-RSS RED, либо явно убери эти
     promises из RED/acceptance.
  4. Усиль `red_stream_grid` до full CellResult + ledger equivalence; усиль disk-guard oracle
     до explicit storage-guard/no-write/Sys-or-alert semantics.
  5. Почини `verify_M-08.sh` false-positive/weak structural checks и сделай deploy SSH script
     `set -euo pipefail`.

  Не пиши implementation bodies. После commit'ов покажи raw output:
  bash scripts/verify_M-08.sh; echo "exit=$?"
  Затем re-dispatch critic на обновлённый commit range.
  ```
- Push-статус: ⚠ NOT pushed to main; blocked by plan-time critic REJECT. Feat branch already at audited `origin/feat/M-08` commit `4c30461`.

## §E — Риски / открытые вопросы
- Dev dispatch remains blocked until this REJECT is addressed or founder explicitly overrides.
- Highest production risk is silent provenance misclassification of legacy/corrupt/foreign segments before purchased history enters the lake.

=== END HANDOFF ===

---

## Re-audit (rev 2) - 2026-07-13T18:18Z

**Audit range:** `4c30461..3f0a923` (6 architect commits)

**Audited HEAD:** `3f0a923` - `docs(process): branch-hygiene — worktree на роль, вердикт критика коммитится`

**Command run:** `bash scripts/verify_M-08.sh; echo exit=$?`

**Observed result:** `VERDICT: FAIL (10 провалов)`, `exit=1`.

The failure shape is mostly the intended RED state: T3/T4/T5/T6/T7/T9/T9b/T9c are
implementation tasks 2-5, and T8c is the new TD-016 root oracle. Form/process gates now
pass: CT-I-4 schema/fixtures/CHANGELOG, T11a/T11c/T11d, and T12 deploy gating.

## Verdict

**REJECT.** Dev dispatch is still blocked, but the reason has narrowed. C-005's main
technical holes are substantially repaired; the remaining blockers are governance/scope
coherence and one migration oracle gap around the sole live legacy segment.

## Blocking Findings

### R1 - TD-016 is in the verify gate, but not in the milestone task/owner chain

- Evidence: `scripts/verify_M-08.sh:63-65` runs `cargo test -p venue-binance --test
  red_book_bounded` as T8c.
- Evidence: `milestones/M-08-data-durability.md:74-83` has no task for `venue-dev` or
  `crates/venue-binance/src/**`; `milestones/M-08-data-durability.md:130-131` still routes
  `engine-dev (2,3,4) -> research-dev (5) -> tester -> reviewer`.
- Evidence: `.claude/rules/scope-guard.md:71-73` says no spec/no work; with a RED in the
  gate but no owned task, no dev agent has a clean mandate to make the full verify gate green.

**Required fix:** add an explicit M-08 task for TD-016 (`venue-dev`, cap
`venue-binance` book levels to 5000/side, preserve top-of-book/OBI signal surface), update
Allowed/Forbidden paths, and update the handoff chain. Suggested order: `venue-dev` for
TD-016 can run in parallel with `engine-dev` journal tasks; `research-dev` follows the
journal stream/API work; then tester/reviewer.

### R2 - Claimed carve-out A4 is not committed, while architect already touched venue source

- Evidence: `.claude/rules/scope-guard.md:10` forbids architect writes to impl code
  (`crates/*/src/`).
- Evidence: `milestones/M-08-data-durability.md:60-70` declares only the journal skeleton
  allowance and carve-out A3 for `research-cli/src/grid.rs`; there is no A4 for
  `crates/venue-binance/src/lib.rs`.
- Evidence: the diff in `crates/venue-binance/src/lib.rs` is only test-seam visibility
  (`OrderBook`, `DepthDiff`, fields, `apply_diff_to_book` made `pub`), not logic. That makes
  the source change technically narrow, but it is still outside the committed architect zone.

**Required fix:** either commit a narrow A4 in the milestone ("visibility only for
`OrderBook`/`DepthDiff`/`apply_diff_to_book`, no logic") or remove the source change and
place the RED in a crate-local test surface. As committed, reviewer scope should reject this.

### R3 - Dev-facing prose still states rejected legacy/Sys behavior

- Evidence: `docs/rfc/CT-RFC-02-journal-provenance.md:77-99` correctly replaces fail-open
  imputation with `SEGMENT_MAGIC` + `journal.legacy.json` + fingerprint declaration, but
  `docs/rfc/CT-RFC-02-journal-provenance.md:106` still says headerless legacy reads as an
  imputed `OwnCapture` header.
- Evidence: `milestones/M-08-data-durability.md:77` and `:88-100` still describe the old
  headerless segment behavior as imputed `OwnCapture`.
- Evidence: `milestones/M-08-data-durability.md:49` and `:78` still promise a `Sys` event on
  disk fail-closed, while the corrected oracle uses `storage_status().writable=false` and
  no-write semantics. The removed `Sys` promise is reasonable because the writer cannot
  safely append once the storage guard has tripped, but the milestone still instructs the
  opposite.

**Required fix:** align the RFC invariant table, task table, RED list, and acceptance prose
with the rev-2 contract: no default legacy imputation, declared legacy only after manifest
fingerprint check, and disk guard observable via storage error/status/no byte/seq advance
rather than a journal `Sys` append.

### R4 - Legacy declaration should force size/shrink validation, not only first-MiB hash

- Evidence: `docs/rfc/CT-RFC-02-journal-provenance.md:95-99` stores
  `size_bytes_at_decl`, but `crates/journal/tests/red_segments_epochs.rs:160-180` only proves
  a changed prefix is rejected. There is no RED for "same first MiB, file truncated below
  declared size".
- Risk: the live 8.3 GB segment is the irreplaceable migration object. A naive implementation
  can compare the first MiB only, ignore `size_bytes_at_decl`, and still classify a truncated
  or replaced same-prefix file as declared `OwnCapture`.

**Required fix:** add a negative RED that truncates a declared legacy segment below
`size_bytes_at_decl` and requires `Err`, or explicitly amend the RFC to accept first-MiB-only
identity as sufficient. My recommendation is fail-closed on `current_size < size_bytes_at_decl`;
growth after declaration is acceptable, shrink is not.

## Closed / Accepted Items

- **C1 contract-RFC package:** closed. `docs/05-contract-layer.md:48-51` has the seven
  atomic RFC requirements; this range adds Rust type changes, generated schemas, schema
  equality gate, valid/invalid fixtures, version bump, migration note, tests, and
  `crates/contracts/CHANGELOG.md`. One advisory remains: the generated schemas use schemars'
  default draft-07 while `docs/05-contract-layer.md:20-22` says JSON Schema 2020-12. I am not
  treating that as the current blocker because CT-I-4 is now generated and gated, but it should
  be clarified before close-out.
- **C2 fail-closed classification:** mostly closed. `red_segments_epochs` now covers
  undeclared headerless segments, wrong fingerprint, corrupt bytes, declared vendor staying
  vendor, and default `OwnCaptureOnly` exclusion. No remaining silent path to `OwnCapture` is
  visible except the size/shrink oracle gap in R4.
- **M1 gap statistics:** closed. `red_gaps.rs` catches both silent gaps and ConnDown/ConnUp
  gaps and requires deterministic artifacts.
- **M2 E7/TD-016 diagnosis:** accepted. `recorder/tests/red_rss_bounded.rs` passing is useful:
  it narrows the leak away from the writer. `venue-binance/tests/red_book_bounded.rs` is a
  strong oracle for the observed root (`apply_diff_to_book` accumulates stale levels). I do
  not require duplicate oracles for `venue-hyperliquid` or `venue-binance-futures` at this
  gate: Hyperliquid parses exchange snapshots without a long-lived local full-book map, and
  futures rebuilds on snapshot. Reviewer §8 must still verify live RSS and book-depth/OBI
  sanity after the TD-016 fix.
- **M3 stream-grid equivalence:** closed. `red_stream_grid` now compares full `CellResult`
  fields and ledger records, not just weak PnL/intents/fills.
- **M4 disk guard:** closed as an oracle, pending R3 doc cleanup. The strengthened test checks
  `free_bytes + 1`, storage-guard error classification, no seq/byte movement, and
  `storage_status().writable=false`.
- **M5/M6 verify/deploy:** closed. `scripts/verify_M-08.sh` has an explicit FAIL aggregator;
  `scripts/check_deploy_gate.py` parses YAML structurally; `.github/workflows/deploy.yml`
  has `deploy.needs: ci`, fail-closed CI wait, SSH `set -euo pipefail`, and split
  fetch/reset.
- **N1 compile-fail doctest:** closed. The `ColdCopyProof` constructor remains private and
  the doctest was moved into documentation where rustdoc can enforce it.

## Required Next Action

Architect should make a small process/spec repair commit only:

1. Add committed A4 and a real TD-016 `venue-dev` task/allowed path/handoff order.
2. Remove stale `OwnCapture` imputation and disk-guard `Sys` prose from CT-RFC-02 and M-08.
3. Add the legacy shrink RED around `size_bytes_at_decl` or explicitly document why it is not
   required.

After that, re-run critic on the new range. Do not dispatch dev yet.

=== HANDOFF: critic -> architect ===

## §A — Метаданные
- Дата (UTC, ISO-8601): 2026-07-13T18:18Z
- Milestone: M-08-data-durability
- Статус: BLOCKED (critic REJECT before dev)
- HEAD: 3f0a923 — docs(process): branch-hygiene — worktree на роль, вердикт критика коммитится

## §B — Что я сделал
- Re-audited `4c30461..3f0a923` in isolated worktree `/tmp/hft-critic-M08`.
- Ran `bash scripts/verify_M-08.sh; echo exit=$?`.
- Checked CT-RFC-02, M-08, contracts schemas/fixtures, journal/research/recorder/venue REDs,
  deploy gating, and scope/branch-hygiene rules.

## §C — Артефакты / результаты
- Updated verdict file: `research/critiques/C-005-M-08.md`
- Verdict: REJECT.
- Verify: `VERDICT: FAIL (10 провалов)`, `exit=1`; failures are implementation tasks
  2-5 plus TD-016, while form gates now pass.

## §D — Следующий агент + инвокация
- **Следующий агент:** `architect`
- **Paste-ready промпт:**
  ```
  Ты — architect. Рабочий каталог /home/nous/hft-platform, ветка feat/M-08.

  Прочитай `research/critiques/C-005-M-08.md`, секцию "Re-audit (rev 2)".
  Исправь только architect-owned artifacts:
  1. Добавь committed carve-out A4 для `crates/venue-binance/src/lib.rs` (только pub visibility
     test seam, без логики) И/ИЛИ убери source-touch, если тест можно сделать crate-local.
  2. Добавь явную задачу M-08 для TD-016: `venue-dev`, `crates/venue-binance/src/**`,
     cap `MAX_BOOK_LEVELS_PER_SIDE = 5000`/side, preserve top-of-book/OBI surface; обнови handoff
     chain и Allowed/Forbidden.
  3. Убери stale prose: legacy no-header больше НЕ получает имputed `OwnCapture`; disk guard
     больше НЕ обещает `Sys` append после запрета записи, observable = storage-guard error +
     no seq/byte movement + `storage_status().writable=false`.
  4. Добавь RED на declared legacy segment truncated below `size_bytes_at_decl` -> Err, либо
     явно зафиксируй в RFC, почему first-MiB-only identity достаточна.

  Не пиши implementation bodies. После commit'ов покажи:
  bash scripts/verify_M-08.sh; echo "exit=$?"
  Затем re-dispatch critic на новый commit range.
  ```
- Push-статус: ⏸ commits ready; critic verdict commit should be pushed to shared `feat/M-08`
  before the next agent bootstraps.

## §E — Риски / открытые вопросы
- Dev dispatch remains blocked until A4/TD-016 ownership and stale legacy/Sys specs are fixed.
- TD-016 touches production market-data behavior; reviewer §8 must include RSS plus book-depth
  and OBI sanity, not only container health.

=== END HANDOFF ===
