# C-056 — вердикт критика: арх-аудит 2026-08 + CT-RFC-08 (ось времени) + правка testing.md

**Гейт:** plan-time / doc-гейт класс A (`gates.md` §9) + **RAW-гейт** (`gates.md` §1: предмет —
T1 `crates/contracts` и семантика записи журнала ⇒ critic на СИЛЬНОЙ модели).
**Предмет:** `origin/feat/arch-audit` @ `2640cb3` (три коммита: `27dcdb2`, `5dcd38a`, `2640cb3`).
**База кода:** `origin/main` @ `0b4a398` (та же, на которой снят аудит).
**Замеры:** worktree `/tmp/hft-critic-audit` (detached), все команды ниже воспроизводимы на нём.
**Дата:** 2026-08-03.

---

## ВЕРДИКТ

| Артефакт | Вердикт |
|---|---|
| `docs/rfc/CT-RFC-08-time-axis.md` (rev1) | **REJECT** — B1…B5 (дизайн-дыры и неполный консюмер-набор) |
| `docs/plans/arch-audit-2026-08.md` | **NOTE** + одна блокирующая правка **B6** (прямое противоречие с собственным RFC) |
| `.claude/rules/testing.md` (новый раздел) | **NOTE** (N7–N9) — правка по существу верна, но ~на 2/3 дублирует два действующих раздела |

Фактура аудита проверена независимо и **держится**: из 16 пунктов §0 перепроверено 15
(п.11 — состояние VPS — вне моего доступа, отмечаю как НЕ перепроверенное мной), расхождений
по существу нет. Блокирует не фактура, а инженерная часть RFC.

---

## §1. Проверка фактов аудита (замером, не на веру)

| № | Утверждение аудита | Моя проверка | Итог |
|---|---|---|---|
| 1 | `journal/src/lib.rs:110,167` — `epoch: SystemTime::now()`; `:205-213` — `now.duration_since(self.epoch)` | открыл все три места | **ПОДТВЕРЖДЕНО** |
| 2 | `alpha/src/lib.rs:191-192` — stale-expiry по mono | `let threshold = sample.ts_event_mono_ns.saturating_add(horizon_ns); let fresh = ev.ts_mono_ns <= threshold;` | **ПОДТВЕРЖДЕНО** |
| 3 | `sim/src/exchange.rs:108,141` — latency-модель на mono | `effective_ts_mono_ns: self.last_ts_mono_ns + draw.delta_submit_ns` / `if self.pending[i].effective_ts_mono_ns <= ts_mono_ns` | **ПОДТВЕРЖДЕНО** |
| 4 | `grep ts_mono_ns crates/gateway*/src` → 0 | `grep -rn ts_mono_ns crates/gateway/src crates/gateway-serve/src` → пусто | **ПОДТВЕРЖДЕНО** |
| 5 | `gateway-serve/src/lib.rs:499-502` — Text/Binary игнорируются | открыл: `// MVP: replay-контролы НЕ реализованы (только чтение).` | **ПОДТВЕРЖДЕНО** |
| 6 | `docker-compose.yml:146-147` — TCP-healthcheck | `test: ["CMD-SHELL", "timeout 2 bash -c '</dev/tcp/127.0.0.1/${GATEWAY_PORT:-8080}'..."]` | **ПОДТВЕРЖДЕНО** |
| 7 | `concurrency` нет ни в одном workflow | `grep -n concurrency .github/workflows/*.yml` → exit=1 | **ПОДТВЕРЖДЕНО** |
| 8 | `ci.yml` — `cargo install cargo-audit` + сетевой fetch | `ci.yml:31-35` | **ПОДТВЕРЖДЕНО** |
| 9 | диспропорция строк по крейтам | `find crates/<c>/src -name '*.rs' \| xargs cat \| wc -l`: gateway 3349, journal 4479, alpha 238, signals 440, gateway-serve 1834, ops 2941, research-cli 3687 | **ПОДТВЕРЖДЕНО** (расхождение по числу тест-файлов journal 36 vs 37 / ops 15 vs 16 — следствие non-recursive glob `tests/*.rs` vs `find` c `tests/common/mod.rs`; вывод не меняется) |
| 10 | консюмеры `alpha` — portfolio/strategy/sim/research-cli | `grep -rn "alpha::" crates/*/src` → ровно эти четыре крейта | **ПОДТВЕРЖДЕНО** |
| 12 | состав записи из дефолтов кода | `recorder/src/main.rs:374,381,388` — `env_csv("BINANCE_SYMBOLS", &["BTCUSDT","ETHUSDT"])` и т.д. | **ПОДТВЕРЖДЕНО** |
| 13 | 32 (не 34) `Severity: **MAJOR**`; TD-084 = wsprobe, не healthcheck | `grep -c 'Severity: \*\*MAJOR\*\*' TECH-DEBT.md` → 32; `TECH-DEBT.md:96` — `TD-084 wsprobe--secret-hex-эвристика...` | **ПОДТВЕРЖДЕНО** |
| 14 | 28.5/142.5/66.3 s задокументированы | `TECH-DEBT.md:152-156` | **ПОДТВЕРЖДЕНО** |
| 11 | прод жив (VPS) | **не проверял** (вне доступа этой сессии) | НЕ ПЕРЕПРОВЕРЕНО |

Дополнительно проверено по мандату:
- **JR-I-13 действительно первый свободный номер** — `docs/fa/journal.md:138` («Первый свободный
  номер — `JR-I-13`»). Коллизии нет.
- **Н-1** (кто консюмер `alpha`) — верно; уточнение: `strategy` — консюмер И `alpha`, И самой оси
  времени (см. B2), то есть «три консюмера оси» в §3 Н-2 занижено до четырёх.
- **Н-3** — верно по обоим числам.

---

## §2. БЛОКИРУЮЩИЕ находки (CT-RFC-08 rev1)

### B1. Заявленное закрытие TD-081/AL-I-4 — ЛОЖНАЯ ГАРАНТИЯ: на данных новой эпохи фикс не работает

RFC §2.5 фиксит alpha детектом **регрессии** оси (`ev.ts_mono_ns < last_seen`) и сам же признаёт:
«**На schema-5 данных ветка мертва**». Но исходный дефект TD-081 — не регрессия как таковая,
а то, что **сэмпл переживает простой писателя**. O2 регрессию убирает, а простой в ось не
включает (§2.1 прямым текстом: «простой писателя в ось не входит»). Значит на schema-5:

- `mono_base` = high-water хвоста ⇒ сэмпл, снятый последним перед остановкой, имеет
  `ts_event_mono_ns ≈ mono_base`;
- после рестарта (пауза 3 дня — деплой, инцидент, ретеншен) первые события имеют
  `ts_mono_ns = mono_base + ε`, где ε — наносекунды с момента open;
- `fresh = ev.ts_mono_ns <= sample.ts_event_mono_ns + horizon_ns`
  (`crates/alpha/src/lib.rs:191-192`) → **true** в течение всего `horizon_ms` осевого времени
  после рестарта, независимо от реальной трёхдневной паузы.

Дефект сужается («вечно свежи» → «свежи ещё horizon_ms после рестарта»), но НЕ исчезает, и
детект регрессии его не ловит **по построению**. Milestone по этому RFC закрыл бы TD-081 ложно —
ровно класс «ложное [ЕСТЬ]», против которого написан сам RFC (§1).

**Что требуется от rev2.** Stale-expiry по смыслу требует ФИЗИЧЕСКОГО времени: либо
`ts_wall_ms` (уже в каждом событии, DESIGN §3 «wall_ms для отчётов» — здесь это не отчёт, а
измерение давности данных), либо явный маркер границы сессии в потоке. Второго СЕЙЧАС НЕТ:
`SysEvent` = `Heartbeat | ConnUp | ConnDown | ReconDivergence`
(`crates/contracts/src/lib.rs:157-167`) — «писатель перезапустился» не эмитируется, а
`SegmentHeader` — не `Event` и до консюмера не доходит. Если выбран путь маркера — это НОВЫЙ
вариант enum, то есть настоящее аддитивное изменение T1 со своей ценой; если `ts_wall_ms` — надо
назвать поведение при отводе часов. Любой из вариантов меняет §2.5 и оценку «≤6 коммитов».

### B2. Консюмер-набор НЕПОЛОН: `crates/strategy` не назван ни в §0.3, ни в §2.5

```
$ grep -rn "ts_mono_ns" crates/*/src
crates/strategy/src/lib.rs:102:  .retain(|_, f| ev.ts_mono_ns <= f.submitted_ts_mono_ns.saturating_add(ttl_ns));
crates/strategy/src/lib.rs:241:  submitted_ts_mono_ns: ev.ts_mono_ns,
```

`DirectionalStrategy` истекает in-flight интенты по той же оси (`ST-I-3`, комментарий в коде:
«In-flight expiry по event-time (никакого wall-clock, M-07 D4)»). Это ЧЕТВЁРТЫЙ консюмер того же
класса, что alpha: под текущей реализацией — регрессия оси через рестарт, под O2 — замирание оси
на время простоя (B1). RFC утверждает «Сломанные консюмеры (**три**)» и в §2.5 перечисляет фиксы
для alpha/sim/gateway — `strategy` не упомянут нигде. Milestone, собранный по §2.5, оставит его
как есть, а §3 не содержит для него оракула.

(Транзитивно затронуты и `sim/src/strategy_backtest.rs:119`, `research-cli/src/grid.rs:388` —
они прокидывают `ts_mono_ns` филлов дальше в метрики.)

### B3. Якорь `mono_base` НЕ ОПРЕДЕЛЁН на двух реальных путях `open` — обоих, введённых ради выживания при порче

RFC §2.2 п.1 знает ровно два случая: «хвост читается» и «журнал пуст → 0». Код знает больше:

**(а) Операторская декларация (M-49).** `resolve_next_seq_or_declared`
(`crates/journal/src/segments.rs:2158-2200`) существует ИМЕННО для случая, когда хвост
**нечитаем** (`is_tail_unreadable`) — тогда `next_seq` берётся из
`journal.force-next-seq.json`. На этом пути «`ts_mono_ns` последнего события хвоста» физически
недоступен. Естественный дефолт (`0`) даёт разрыв оси там, где доверие к данным и так подорвано.
RFC обязан сказать явно: декларация несёт и `mono_base`, либо open на этом пути отказывает,
либо ось получает другой якорь.

**(б) `meta` может быть ВПЕРЕДИ читаемого хвоста.**
```
crates/journal/src/segments.rs:1466-1476
pub(crate) fn resolve_next_seq_with(dir, meta_path) -> io::Result<u64> {
    match latest_segment(dir)? {
        None => Ok(meta_seq),                     // сегментов нет, но seq ПРОДОЛЖАЕТСЯ с меты
        Some((_idx, path)) => Ok(meta_seq.max(tail_last_seq_of(&path)? + 1)),
    }
}
```
У `seq` есть ВТОРОЙ durable источник (`journal.meta`), у оси времени — нет. Каталог, где мета
есть, а сегментов нет (restore из холодной копии, ручной карантин сегментов по
`docs/ops-journal-tail-unreadable.md`), даёт `next_seq > 0` и `mono_base = 0` ⇒
`seq_a < seq_b ∧ ts_mono_a > ts_mono_b` — **JR-I-13 нарушен ровно в том сценарии, ради которого
инвариант и вводится**. Симметричное решение: high-water оси persist'ится там же, где `next_seq`
(мета + декларация), и восстанавливается как `max(мета_high_water, хвост)`.

### B4. Якорь «последнее событие хвоста» ≠ high-water: на боевом журнале JR-I-13 ложен с первого же дня

Прод-журнал (157.9M событий, schema ≤4) состоит из сессий, каждая из которых считала mono с нуля.
Значение последнего события хвоста — это mono ПОСЛЕДНЕЙ (возможно, короткой) сессии, то есть
маленькое; при этом в журнале ранее лежат события с mono на порядки больше (длинная сессия →
десятки суток в наносекундах). Первый schema-5 сегмент стартует НИЖЕ исторического максимума, и
формулировка §2.1 — «для любых событий **одного журнала** `seq_a < seq_b ⇒ mono_a <= mono_b`» —
на реальном каталоге ложна сразу после включения. Оговорка §2.1 про «сегменты до эпохи честности»
спасает чтение старого, но не спасает СШИВКУ старого с новым.

Требуется одно из: (i) якорь = `max` по читаемому хвосту (и честная цена скана в §4), либо
(ii) JR-I-13 явно сужается до «событий эпохи ≥ 5», а оракул **JR-I-13a обязан гоняться на
СМЕШАННОМ каталоге (legacy-сегменты + новые)**, иначе это фикстура счастливого пути — тот самый
класс, который `testing.md` §«Фикстура „счастливого пути“» запрещает.

### B5. Атомарный CT-RFC-пакет: §4 «JSON Schema не меняется» неверно и не сходится с МАШИННЫМ гейтом

`docs/05-contract-layer.md` §4 требует в ОДНОМ PR семь артефактов, и это уже машинный
блокирующий job: `.github/workflows/ci.yml:111-112` → `scripts/verify_ct_rfc_atomic.sh`. Скрипт
при любом касании `crates/contracts/src/**` требует в диффе: `docs/rfc/CT-RFC-NNN-*.md`,
`crates/contracts/schema/*.json`, `CHANGELOG.md`, `fixtures/valid/*.json`,
`fixtures/invalid/*.json`, `crates/contracts/tests/*.rs`. Против этого:

- **§4 RFC утверждает «JSON Schema не меняется» — фактически неверно.** Схема генерируется
  schemars из докстрок: `crates/contracts/schema/event.schema.json:4` — это докстрока структуры
  `Event`. Нормативная докстрока §2.1, повешенная на поле `ts_mono_ns` (сейчас поле в схеме без
  `description`, `event.schema.json:21-25`), схему ИЗМЕНИТ. Если dev поверит §4 и докстроку
  повесит не туда/не регенерирует — гейт красный.
- **`fixtures/invalid/*.json` для семантического изменения естественным образом не выражается**
  (межсобытийную монотонность JSON Schema не описывает). Планировать это надо в RFC, а не
  оставлять dev'у выбор между красным CI и фиктивной фикстурой.

### B6. (аудит) §1 п.2 прямо противоречит собственному RFC в той же ветке

`docs/plans/arch-audit-2026-08.md:58`: «Лечится одним CT-RFC **без бампа схемы** — предложен:
`docs/rfc/CT-RFC-08-time-axis.md` (этой же веткой)». `CT-RFC-08` §2.4 предлагает **бамп
`SCHEMA_VERSION` 4 → 5**. Два документа одного пакета утверждают противоположное о цене изменения.
Документ — источник исполнения (`gates.md` §9), поэтому это не опечатка: следующий агент,
читающий §5 P1-4 плана, спланирует milestone без бампа. Правится одной строкой, но правится ДО
мержа.

---

## §3. NOTE (не блокируют, но обязаны попасть в rev2 / в milestone)

**N1. Ответ на открытый вопрос §2.4 (бамп схемы).** Бамп законен уже сегодня:
`docs/05-contract-layer.md` §4 требует bump `schema_version` в составе ЛЮБОГО contract-RFC, а
`decide_open_segment` использует версию как машинный маркер изоляции сегмента
(`crates/journal/src/segments.rs:2278`) — семантическая эпоха изолируется тем же механизмом, что
и вариантная. Но докстрока самой константы (`crates/contracts/src/lib.rs:13-19`) определяет её
как «эпоха эмитируемых **вариантов**» и «Bump'ится при КАЖДОМ новом варианте». Это T1-докстрока —
её расширение («…и при любом изменении СЕМАНТИКИ эмитируемых данных при неизменной форме»)
обязано войти В ЭТОТ ЖЕ RFC, а не остаться «вопросом критику». Заодно закрывается класс Д1.

**N2. Побочный эффект бампа на пиннящие acceptance-скрипты.** `scripts/verify_M-35.sh:44`
пиннит `SCHEMA_VERSION: u32 = 4` и станет красным навсегда (прецедент уже есть:
`scripts/verify_M-18.sh:34` пиннит `= 3`). В CI они не гоняются (`ci.yml` запускает только
`verify_delivery_M-08.sh`, `verify_contracts.sh`, `verify_ct_rfc_atomic.sh`), поэтому это не
блокер — но milestone обязан назвать это ожидаемым либо перевести пины на версионно-агностичную
форму (как сделано в M-38a задача #4).

**N3. Канарейка §3 неполна.** RFC требует сверить докстроку `contracts` и `DESIGN.md` §3
(`docs/DESIGN.md:151`). То же обещание живёт ещё в двух местах:
`docs/05-contract-layer.md:51` («Время: `ts_mono_ns` (порядок)») и `docs/fa/journal.md:68`
(«`ts_mono_ns` — для внутренней **длительности**/порядка» — под новой семантикой ложно через
границу сессии). Плюс комментарии двух DET-оракулов ссылаются на `SystemTime::now()` в
`journal/src/lib.rs:205` как на обоснование своей конструкции
(`crates/journal/tests/red_det_replay_digest.rs:177-179`,
`crates/journal/tests/red_det_restart.rs:31`) — после перехода на `Instant` они станут ложными
[ЕСТЬ]; сами оракулы, судя по конструкции (различение по окнам ОДНОГО журнала), остаются
валидными, но обоснование надо переписать.

**N4. Порядок операций в `open_with` — в ЗАПРЕТНЫЙ СПИСОК milestone'а** (`testing.md`
§«Спека правки существующего кода…»). `mono_base` обязан сниматься ДО `decide_open_segment`:
`crates/journal/src/lib.rs:136` (`resolve_next_seq_or_declared`) идёт перед `:141`
(`decide_open_segment`). Бамп схемы гарантированно откроет НОВЫЙ сегмент (reuse требует
совпадения `schema_version`, `segments.rs:2278`), и якорь, снятый после, увидит пустой хвост → 0.

**N5. Проверено за milestone (чтобы не тратить круг): счётчик `mono_clamped_total` в heartbeat
безопасен.** `ops::watchdog::HeartbeatSample` (`crates/ops/src/watchdog.rs:125-133`) — без
`deny_unknown_fields` (единственное вхождение в репо — `research-cli/src/strategy_cell.rs:32`),
поле аддитивно, watchdog не сломается.

**N6. §5 «что RFC НЕ решает» — добавить шардирование.** JR-I-13 — свойство ОДНОГО журнала. В
модели CT-RFC-07 (проект готов) шард = свой писатель и свой якорь; слияние шардов по `seq` даст
немонотонный `ts_mono_ns`. Пока не реализовано — но именно поэтому дешевле записать сейчас.

**N7. `testing.md`: новый раздел на 2/3 дублирует действующие.** П.1 (оракул точки входа) —
обобщение §«Канарейка ops-пути ИСПОЛНЯЕТ вызывателя, а не грепает вызываемое» (строки 310-327),
п.2 (независимый эталон + «сломай — покраснеет?») — §«Мутационный контроль» (272-293) плюс
§«Оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ» (156). Единственное действительно новое — п.3 (DoD
«механизм на пути» + TD «built-not-wired» MAJOR). Это ровно то, против чего возражает C-5 самого
аудита («мораторий на новые ПРОЗАИЧЕСКИЕ правила; новый урок — либо скрипт-гейт, либо одна
строка в существующем разделе»): пакет нарушает собственную рекомендацию в том же коммите.
Предложение: п.1-2 сложить строками в существующие разделы, п.3 оставить отдельно.

**N8. Место для п.3 — не `testing.md`, а `gates.md` §4.** «Механизм без подключения не мержится»
адресовано reviewer'у на PR-гейте (DoD/merge), а не автору оракулов. В `testing.md` его прочтёт
architect и не прочтёт тот, кто применяет.

**N9. Статус «PROPOSED» внутри ЗАГОЛОВКА раздела.** Ни один другой раздел `testing.md` статуса
в заголовке не несёт. После мержа заголовок останется «(PROPOSED, аудит 2026-08-03)» — правило,
у которого в названии написано «предложено», не является обязательным для следующего агента.
Reviewer обязан снять маркер при переводе `PROPOSED → ACTIVE`.

**N10. Исполнимость «оракула точки входа» при RED-first — проверил, правило выполнимо.**
Точка входа может быть закоммичена как compile-RED (санкционировано `gates.md` §8: RED живёт на
feat-ветке, не в main), а дешёвые формы уже существуют в репо (`HFT_CRON_PRINT_ARGV=1`,
sidecar M-46, T6-канарейка M-53). Но в тексте не определено, что считать «механизмом несущего
пути» (перечисление «запись журнала, read-path, ops-cron, venue-ingest» — примеры, не критерий),
и не названы дешёвые формы. Без этого правило либо обходится, либо превращается в требование
поднимать WS-сессию с токеном в каждом milestone'е. Добавить критерий + список допустимых
дешёвых форм.

**N11. Приоритеты §5 плана — граница founder'а.** `gates.md` §9 класс A п.4: правка, меняющая
ФАЗЫ/ПРИОРИТЕТЫ, требует founder ★. Аудит корректно вынес §6 на подпись, но сам список P0/P1/P2
— это тоже переупорядочивание очереди относительно `milestones/BACKLOG.md`. Reviewer не должен
трактовать P0 как принятый приоритет без явной подписи.

---

## §4. Что в пакете сделано ХОРОШО (фиксирую, чтобы не потерялось при rev2)

- Опровержения Н-1…Н-5 проверены и держатся; калибровка «одна ось из трёх сломана, и она вне
  прод-пути» — существенная и верная поправка к исходной гипотезе (замер §0 п.4 подтверждён).
- Находка C-1 (sim как второй сломанный консюмер) — настоящая, воспроизводится по коду.
- §2.3 «отвергнутые альтернативы» с явными причинами и §5 «что RFC не решает» — правильная форма
  contract-RFC; O3 отвергнут по верному основанию (квантование до мс убивает ns-дельты sim).
- Выбор «не мигрировать 157.9M событий» — единственно верный (append-only, CT-I-3).
- Дисциплина ветки соблюдена: класс A, `PROPOSED`, не в `main`, критик до реализации.

---

## §5. Условия снятия REJECT

rev2 обязан:
1. Переписать §2.5 alpha (и sim) в терминах ПРОСТОЯ писателя, а не регрессии оси; назвать
   источник физического времени (`ts_wall_ms` или новый `SysEvent`-маркер) и его цену (B1).
2. Внести `crates/strategy/src/lib.rs:102` в §0.3/§2.5 + оракул в §3 (B2).
3. Определить `mono_base` на пути операторской декларации и при `meta > хвоста`; предложить
   persist high-water рядом с `next_seq` (B3).
4. Заменить якорь «последнее событие хвоста» на high-water либо сузить JR-I-13 до эпохи ≥5;
   оракул JR-I-13a — на СМЕШАННОМ (legacy+new) каталоге (B4).
5. Переписать §4 «Совместимость и цена» под реальный атомарный пакет
   `verify_ct_rfc_atomic.sh`, включая ответ по invalid-фикстуре (B5).
6. Снять противоречие «без бампа схемы» в `docs/plans/arch-audit-2026-08.md:58` (B6).
7. Учесть N1-N6 в RFC, N7-N10 — в правке `testing.md`.

Повторный круг — тот же гейт (RAW, сильная модель), `gates.md` §9 «петля самоправки»: rev2
остаётся в ТОМ ЖЕ цикле на той же ветке.

---

*Критик, 2026-08-03. Read-only на код; никаких файлов вне `research/critiques/` не тронуто.*
