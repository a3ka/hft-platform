# C-024 — M-35 CT-RFC-05 MarginInventory plan-time gate

**Date (UTC):** 2026-07-25T15:50:46Z  
**Agent:** critic  
**Branch audited:** `origin/feat/M-35-margin-inventory` at `e06e48a`  
**Worktree:** `/tmp/hft-arch-m35` (`feat/M-35-margin-inventory`, clean before verdict)  
**Base/range audited:** `1732f91..e06e48a`  
**Scope:** contract-RFC/doc-gate, `docs/05-contract-layer.md` §4 + `.claude/rules/gates.md` §1/§5/§9  
**Verdict:** REJECT

## Pre-flight artifact set

Architect committed the required plan-time artifact shape:

```text
2e84783 docs(M-35): спека margin-inventory collector ...
  A milestones/M-35-margin-inventory.md

e06e48a feat(M-35): CT-RFC-05 — MdPayload::MarginInventory ...
  M crates/contracts/schema/event.schema.json
  M crates/contracts/src/lib.rs
  A crates/contracts/tests/ct_rfc05.rs
  A crates/venue-binance/tests/red_margin_inventory.rs
  A scripts/verify_M-35.sh
```

The artifact set is complete enough to review, but it is not safe to pass to `risk-critic`/`venue-dev`
because the plan rests on a committed source-proof contradiction.

## Blocking findings

### B1 — Milestone cites a non-existent feasibility proof and contradicts the committed survey

`milestones/M-35-margin-inventory.md:4-5` says reachability is proven by
`margin-source-survey.md §9` and that `available-inventory -> Δ = borrow/repay utilization`.
There is no committed §9 in `research/data-quality/margin-source-survey.md`; the file ends at §8.

Worse, the committed survey says the opposite of the milestone's execution premise:

- `research/data-quality/margin-source-survey.md:21-31` says public market-wide borrow/repay volume is
  not reachable and warns architect not to spec a collector under that source.
- `research/data-quality/margin-source-survey.md:255` classifies Available Inventory as supply ceiling,
  not outstanding loan volume.
- `research/data-quality/margin-source-survey.md:333-349` says no timeline "borrowed vs repaid / net"
  exists publicly and that inventory proxy must not be presented as borrow volume.
- `research/data-quality/margin-source-survey.md:399-405` repeats that the survey is a negative result
  and recommends rejecting or reformulating M-35 with an explicit proxy caveat.

The milestone can validly choose a proxy, but the committed record must say that honestly. Right now the
milestone claims a proof that is not present and overrides the only committed source memo without committing
the alleged read-only re-probe evidence. That is a plan-time blocker because `venue-dev` would implement a
collector whose source-of-truth rationale is not auditable from git.

Required fix: commit the missing evidence or rewrite the milestone as a proxy-collector milestone. Minimal
acceptable evidence is a redacted, non-secret proof artifact that records endpoint, timestamp, HTTP status,
response shape/fields for USDT+USDC, and the exact interpretation boundary: raw `available` pool only;
downstream Δ is a proxy/utilization indicator, not borrow/repay ledger. If the private re-probe contradicts
the old survey, update the survey rather than citing a non-existent §9.

### B2 — CT-RFC-05 omits mandatory changelog/fixture artifacts from `docs/05-contract-layer.md` §4

`docs/05-contract-layer.md:58-61` says every T1 shape change must include, in one PR, Rust type change,
regenerated schema, version bump, migration note, valid/invalid fixtures, CHANGELOG update, and at least
one test referencing the new shape.

The branch has the Rust type, schema, version bump, and tests, but no M-35 diff touches
`crates/contracts/CHANGELOG.md` or `crates/contracts/fixtures/**`. `crates/contracts/CHANGELOG.md:1-6`
still starts with CT-RFC-04 and has no CT-RFC-05 section.

Required fix: add CT-RFC-05 changelog entry and at least one valid `MarginInventory` fixture plus one
invalid fixture or explicitly amend the governance rule before using a weaker fixture policy. The existing
`red_schema` test verifies schema/code equality, but it does not replace the committed fixture requirement.

## Checks that passed

- **CT-I-3 additive enum shape:** `MdPayload::MarginInventory` is appended after `L2Delta`, matching
  discriminator 7; `SCHEMA_VERSION` is 4 (`crates/contracts/src/lib.rs:24-26`, `:301-314`).
- **Old segment decode guard:** `ct_rfc05::mi_i_1_pre_rfc05_funding_still_decodes` exists and passed.
- **Schema regen:** `event.schema.json` contains `MarginInventory` with `available_e8` and `ts_exch_ms`;
  `red_schema` passed.
- **Design-honesty in T1 comments:** the variant and generated schema describe raw available pool, not
  `MarginRate`, and push Δ/utilization downstream with provenance caveat.
- **RED-first staging:** `red_margin_inventory` is compile-RED on missing
  `venue_binance::parse_available_inventory`, as expected before task 2.
- **Allowed paths / gates:** milestone declares architect, venue-dev, forbidden order-egress, critic and
  risk-critic gates.

## Executed checks

```text
cargo test -p contracts --test ct_rfc05
  3 passed; exit=0

cargo test -p contracts --test red_schema
  2 passed; exit=0

cargo test -p contracts --test ct_rfc01 --test red_rfc02
  10 passed; exit=0

cargo test -p venue-binance --test red_margin_inventory
  error[E0432]: no parse_available_inventory in the root
  exit=101  # expected compile-RED before venue-dev task 2

bash scripts/verify_M-35.sh
  PASS: fmt clean
  FAIL: clippy -D warnings
  PASS: MI-I-1 (ct_rfc05) GREEN
  PASS: CT-I-4 schema regen
  PASS: CT-RFC-01/02 regressions
  FAIL: MI-I-2/4 red_margin_inventory
  PASS: SCHEMA_VERSION == 4
  PASS: MI-I-3 canary n/a
  VERDICT: FAIL (2); exit=1
```

The full verify failure is acceptable only as a staged pre-dev state: clippy and MI-I-2/4 fail because the
venue parser is intentionally compile-RED. It must be PASS after task 2.

## Verdict rationale

REJECT is about the plan-time source-proof and governance completeness, not the Rust enum mechanics. The
contract type/schema/test portion is mostly coherent, but the milestone cannot proceed to risk review or
venue implementation while it cites a non-existent §9 and leaves mandatory CT-RFC changelog/fixtures out of
the committed set.

## Handoff §D

**Next agent:** `architect` (repair loop, not `risk-critic` yet).

**Required architect repair:**
1. Reconcile `milestones/M-35-margin-inventory.md` with `research/data-quality/margin-source-survey.md`:
   either commit the missing read-only re-probe proof and update the survey, or reformulate M-35 explicitly
   as a raw available-inventory proxy with no claim that it is borrow/repay ledger.
2. Add CT-RFC-05 `crates/contracts/CHANGELOG.md` entry and committed valid/invalid fixtures, or explicitly
   change the governance rule before using a no-fixture exception.
3. Re-run critic on the repaired branch.

**Then, if critic verdict becomes NOTE:** route to `risk-critic` before `venue-dev`, per gates §5:
verify read-only key (`enableReading` only, `apiRestrictions` redacted proof), no order-egress in the margin
path, env-only secret injection, no secret in git/logs, and IP restriction plan for `167.233.192.131`.

**Do not dispatch `venue-dev` task 2 before the critic REJECT is closed and risk-critic has passed or emitted
an explicit non-KILL disposition.**

## Confidence

High. The blocker is directly evidenced by committed files and executed tests; it does not depend on external
Binance availability or secret material.

---

## Re-review r2 — 2026-07-25T17:43:54Z

**Branch audited:** `origin/feat/M-35-margin-inventory` at `0999929`  
**Range:** `239e796..0999929`  
**Re-review scope:** C-024 blockers B1/B2 only, plus targeted contract gates  
**Re-review verdict:** PASS

### r2 Commit Set

```text
0999929 fix(M-35): C-024 REJECT — proxy-collector reframe (§9 committed: available=supply-пул/proxy, НЕ ledger; §8 стоит) + CHANGELOG CT-RFC-05 + valid/invalid fixtures [architect]
M crates/contracts/CHANGELOG.md
A crates/contracts/fixtures/invalid/event-margin-inventory-missing-ts.json
A crates/contracts/fixtures/valid/event-margin-inventory.json
M milestones/M-35-margin-inventory.md
M research/data-quality/margin-source-survey.md
```

### B1 Closure — PASS

The non-existent `margin-source-survey.md §9` citation is now real and the milestone no longer presents
`available-inventory` as a borrow/repay ledger.

Evidence:

- `milestones/M-35-margin-inventory.md:3-7` records rev2 as a `proxy-collector` and cites committed §9 facts
  from the read-only probe.
- `milestones/M-35-margin-inventory.md:9-20` states the public ledger is not available, `available` is raw
  supply-side pool, and utilization/flow is only a downstream proxy with caveat.
- `research/data-quality/margin-source-survey.md:411-446` adds the redacted proof artifact: signed
  `/sapi/v1/margin/available-inventory?type=MARGIN`, HTTP 200, USDT/USDC values, `updateTime`, asset count,
  and the interpretation boundary.
- `research/data-quality/margin-source-survey.md:399-407` still keeps the negative-result §8 warning for raw
  borrow/repay ledger, so the repair is not internally contradictory.

### B2 Closure — PASS

CT-RFC-05 now has the mandatory changelog and fixtures required by `docs/05-contract-layer.md` §4.

Evidence:

- `crates/contracts/CHANGELOG.md:6-22` records schema 3→4, discriminator 7, raw supply-pool semantics,
  downstream proxy caveat, schema regeneration, and fixture paths.
- `crates/contracts/fixtures/valid/event-margin-inventory.json` is present and parses.
- `crates/contracts/fixtures/invalid/event-margin-inventory-missing-ts.json` is present and is rejected by
  `red_schema`.

### Targeted checks

```text
cargo test -p contracts --test ct_rfc05
  3 passed; exit=0

cargo test -p contracts --test red_schema
  2 passed; exit=0

cargo test -p contracts --test ct_rfc01 --test red_rfc02
  10 passed; exit=0

cargo test -p venue-binance --test red_margin_inventory
  error[E0432]: no parse_available_inventory in the root
  exit=101  # expected compile-RED before venue-dev task 2

bash scripts/verify_M-35.sh
  PASS: fmt clean
  FAIL: clippy -D warnings
  PASS: MI-I-1 (ct_rfc05) GREEN
  PASS: CT-I-4 schema regen
  PASS: CT-RFC-01/02 regressions
  FAIL: MI-I-2/4 red_margin_inventory
  PASS: SCHEMA_VERSION == 4
  PASS: MI-I-3 canary n/a
  VERDICT: FAIL (2); exit=1
```

The full verify failure remains expected at this stage: `red_margin_inventory` is the RED spec for
`venue-dev` task 2, and clippy over `venue-binance --all-targets` fails through the same missing parser symbol.
It must turn PASS after task 2.

### Remaining required gate

This PASS only closes the contract/doc critic gate. Because M-35 touches `crates/contracts/**` and introduces
the first authenticated data source, `.claude/rules/gates.md` §5 still requires `risk-critic` before
`venue-dev`.

Risk-critic focus:

- verify the Binance key is read-only (`enableReading` only; redacted `apiRestrictions` evidence);
- verify no order-egress in the margin path (`submit`/`cancel`/order endpoint/trading signature);
- verify secrets enter through environment only and are not written to git, journal, logs, or fixtures;
- verify the VPS/IP-restrict plan for `167.233.192.131`.

### Handoff §D

**Next agent:** `risk-critic`.

**Paste-ready prompt:**

```text
Ты — risk-critic для M-35 CT-RFC-05 @ origin/feat/M-35-margin-inventory (после critic PASS C-024 r2).
CWD /home/nous/hft-platform. По-русски. Забутстрапься с origin в своём worktree.
Проверь safety/risk поверхность первого authenticated data source + contracts:
- read-only Binance key: enableReading only, apiRestrictions redacted proof, no trade/withdraw permission;
- margin path has no order-egress: no submit/cancel/order endpoint/trading signature;
- secrets only via env, never in git/journal/logs/fixtures;
- VPS deploy/IP restriction plan for 167.233.192.131;
- CT-RFC-05 stores raw available supply pool only; downstream proxy caveat preserved.
Verdict KILL / CONCERNS / PASS in research/critiques/C-NNN.md, commit to feat/M-35 branch.
If PASS/CONCERNS non-blocking, hand off to venue-dev task 2: parse_available_inventory + auth poll + recorder wiring.
```

**Do not dispatch `venue-dev` task 2 before risk-critic has produced a non-KILL disposition.**

### Confidence

High for closing C-024 B1/B2. Secret/read-only operational proof is intentionally left to risk-critic, not
resolved by this doc-gate pass.
