<!-- GATE-META
milestone: R-4
audited_repo: a3ka/hft-platform
audited_base: 9669d97970756bdda96d1422c9a3226bdd2ea0f5
audited_head: f231c7d781de3d7943da3ff2b43a030408c388f5
verdict: REJECT
-->

# C-097 — харнесс-барьеры, круг 3: сторож деплоя + барьер ресурсных оракулов — REJECT

## Предмет и границы аудита

Аудированы ДВА предмета одним вердиктом (мандат founder'а):

| предмет | ветка | вершина |
|---|---|---|
| **A. Сторож деплоя** (`C-096` rev3) | `origin/feat/deploy-catchup-watchdog` | `f231c7d` |
| **B. Барьер ресурсных оракулов** (`C-095` rev2) | `origin/fix/resource-oracle-barrier` | `33c4f5a` |

Обе ветки на момент аудита имеют **нулевое отставание** от `origin/main` (`9669d97`) —
проверено, а не принято на слово:

```
$ git -C /tmp/adv-wd rev-list --count HEAD..origin/main   → 0
$ git -C /tmp/adv-ro rev-list --count HEAD..origin/main   → 0
```

Это предъявляется первым, потому что предыдущий круг (`C-096` B-6) вынес ЛОЖНУЮ находку
ровно из-за прогона на отставшем дереве (§4). Все проверки существования файлов, строк и
правил в этом вердикте велись на дереве слияния (`git merge-tree --write-tree HEAD
origin/main` → `4a72c93`), как требует `gates.md` §8.

Роль — адверсарий харнесс-трека (`harness-track.md` §3/§5 п.3), свежий контекст. Проверка
велась **ЛОМАНИЕМ**: построено **26 обманных стабов** (16 против A, 10 против B), каждый
прогнан против барьеров в прод-форме; отдельно — мутационный контроль по обоим вопросам
`testing.md` и замер уборки фикстур числом. Работа шла в отдельных worktree
(`/tmp/adv-wd`, `/tmp/adv-ro`); общий чекаут не тронут.

Предмет B трогает `crates/journal/tests/**`, `crates/gateway/tests/**`,
`crates/research-cli/tests/**`. Тронутый модуль — `journal`; живой инвариант его FA,
относящийся к предмету напрямую: **`JR-I-10`** (`docs/fa/journal.md:131` — «ограниченность
работы пола: скан `readable_floor` работает в ограниченном бюджете; исчерпание бюджета ⇒
`Unknown` (отказ), не оптимистичный ответ»). Именно этот класс — «оракул меряет границу
ресурса» — и пиннят четыре правленых файла, поэтому барьер B охраняет исполнимость
`JR-I-10` и его соседей, а не абстрактную гигиену.

---

## §0 — Что ПОДТВЕРЖДЕНО

### 0.1 Позитивный контроль обоих предметов — зелёный

```
$ bash scripts/tests/red_deploy_catchup.sh --battery
сценариев: 51   PASS: 51   FAIL: 0
--- батарея: мутантов 11, расхождений kill-set 0
своих каталогов (red-catchup-*): до 0, после 0
VERDICT: PASS                                                    exit=0

$ bash scripts/tests/red_resource_oracles.sh
VERDICT: PASS (16/16)                                            exit=0

$ python3 scripts/deploy_catchup.py check-wiring                 exit=0
$ python3 scripts/deploy_catchup.py check-aggregate              exit=0
$ bash scripts/check_resource_oracles.sh   (12 оракулов)         exit=0
```

Числа: **51 сценарий + 11 мутантов** у A, **16 сценариев** у B, **12** ресурсных оракулов
на реальном дереве. Честная реализация зелена целиком — `harness-track.md` §5 п.2 выполнен.

### 0.2 Что в предмете A закрыто ПО-НАСТОЯЩЕМУ

`C-096` B-1/B-2/B-3 закрыты не декларацией, а кодом, и это подтверждено мутацией (§2):
`_step_is_disarmed`, `_invokes`, самопроверка A6 — каждая пиннится своим сценарием и роняет
пробу. Уборка фикстур у A — с НЕВАКУУМНЫМ стражем (§3). Батарея из 11 мутантов сверяет
РАВЕНСТВО kill-set'ов, а не «хоть что-то упало», — это сильнее нормы трека.

### 0.3 Что в предмете B закрыто по-настоящему

Правки `C-095` F-2 (комментарий ≠ объявление) и F-3 (расширенная регулярка) РАБОТАЮТ:
мутация каждой роняет свой сценарий (§2.2). Стабы R1 (счётчик в `mod` с отступом), R5
(`build.rs`), R6 (CRLF), R7 (несколько аллокаторов) — все пойманы.

---

## §1 — Таблица стабов

### 1.1 Предмет A — сторож деплоя: **16 стабов, ПРОПУЩЕНО 12**

Воспроизведение любого: мутация манифеста через YAML, затем
`CATCHUP_DEPLOY_YML=<файл> python3 scripts/deploy_catchup.py check-wiring` (S1–S8) либо
`CATCHUP_CI_YML=<файл> CATCHUP_REPO_ROOT=. python3 scripts/deploy_catchup.py check-aggregate`
(S9–S16).

| # | стаб | что подменено | исход барьера | пойман? |
|---|---|---|---|---|
| S1 | инверсия гейта CI | `jobs.catchup.if` → `… conclusion != 'success'` | exit=0 `VERDICT: PASS` | **НЕТ** |
| S2 | `if`-ВЫРАЖЕНИЕ | шаг `decide` → `if: github.event_name == 'schedule'` | exit=0 `PASS` | **НЕТ** |
| S3 | `timeout-minutes: 0` | на шаге `decide` | exit=0 `PASS` | **НЕТ** (безвреден, см. ниже) |
| S4 | `set +e` в теле | `set +e` + завершающий `echo` | exit=0 `PASS` | **НЕТ** |
| S5 | вызов через переменную | `CATCHUP=true  # …deploy_catchup.py` / `$CATCHUP decide` | exit=0 `PASS` | **НЕТ** |
| S6 | `uses:` вместо `run:` | шаг `decide` → композитный экшен | exit=1 `W10` | да |
| S7 | джоб удалён | `jobs.pop("catchup")` | exit=1 `W10` | да |
| S8 | инверсия решения | `deploy.if` → `decision != 'DEPLOY'` | exit=0 `PASS` | **НЕТ** |
| S9 | барьер проводки не зовётся | из `ci.yml` удалён шаг `check-wiring` | exit=0 `PASS` | **НЕТ** |
| S10 | `\|\| echo <текст>` | `check-aggregate … \|\| echo пропускаем` | exit=0 `PASS` | **НЕТ** |
| S11 | строка `exit 0` после | `check-aggregate` + `\nexit 0` | exit=0 `PASS` | **НЕТ** (безвреден) |
| S12 | мутационный контроль снят | удалён шаг `--battery` | exit=0 `PASS` | **НЕТ** |
| S13 | проба не гоняется | шаг пробы → `if: github.actor == 'nobody'` | exit=0 `PASS` | **НЕТ** |
| S14 | `timeout-minutes: 0` на ДЖОБЕ | `jobs.deploy-catchup` | exit=0 `PASS` | **НЕТ** (безвреден) |
| S15 | fail-closed через `env:` | условие агрегата вынесено в переменную | exit=1 `A3` | да, но **ЛОЖНО** |
| S16 | шаги → внешний shell-файл | `bash .ci/catchup_all.sh` | exit=1 `A6` | да |

**Честный учёт вредоносности.** Из 12 пропущенных **девять вредоносны** (S1, S2, S4, S5,
S8, S9, S10, S12, S13) и **три безвредны по исходу** (S3, S11, S14): они пропущены барьером,
но приводят к КРАСНОМУ, а не к тихому зелёному. Я проверил это исполнением, а не рассуждением —
`gates.md` §8 «отчёт — гипотеза, прогон — факт»:

```
$ printf 'python3 -c "import sys; sys.exit(1)"\nexit 0\n' > s11b.sh
$ bash --noprofile --norc -eo pipefail s11b.sh; echo exit=$?
exit=1        # errexit срабатывает РАНЬШЕ; S11 не обезвреживает шаг
```

S15 — не пропуск, а **ложное срабатывание**: честный рефакторинг (вынос
`${{ needs.*.result }}` в `env:`) барьер объявляет «падать всегда». Это отдельная находка
(H-6), а не заслуга.

### 1.2 Предмет B — барьер ресурсных оракулов: **10 стабов, ПРОПУЩЕНО 6**

Воспроизведение: `ROOT=<фикстура> bash scripts/check_resource_oracles.sh`.

| # | стаб | исход | пойман? |
|---|---|---|---|
| R1 | процессный счётчик внутри `mod {}` с отступом | exit=1 | да |
| R2 | `static CUR: OnceLock<AtomicUsize>` | exit=0 «процессных счётчиков не найдено» | **НЕТ** |
| R3 | `lazy_static! { static ref CUR: AtomicUsize … }` | exit=0 | **НЕТ** |
| R4 | аллокатор объявлен МАКРОСОМ из другого файла; счётчик + 2 теста у потребителя | exit=0 | **НЕТ** |
| R5 | `#[global_allocator]` в `build.rs` | exit=1 | да |
| R6 | тот же дефект, файл в CRLF | exit=1 | да |
| R7 | два аллокатора: честный + дефектный | exit=1 (1 из 2) | да |
| R8 | процессный счётчик + два `#[tokio::test]` | exit=0 «соседей нет» | **НЕТ** |
| R9 | счётчик вынесен в соседний модуль `meter.rs` | exit=0 «учёт потоковый» | **НЕТ** |
| R10 | дефектный оракул + слово `thread_local!` в КОММЕНТАРИИ | exit=0 «учёт потоковый» | **НЕТ** |

**R8 — не гипотетика.** В репозитории **28** тестов с атрибутом, отличным от `#[test]`:

```
$ grep -rn --include='*.rs' -E '^\s*#\[(tokio::test|rstest|test_case)' . | grep -v /target/ | wc -l
28
```

Сегодня ни один из семи файлов, которые барьер пропускает как «одиночные», таких атрибутов
не содержит (проверено поимённо) — значит класс **латентный, а не сработавший**. Но именно
латентность барьер и обещал обезвреживать: его собственный комментарий
(`scripts/check_resource_oracles.sh:105-107`) говорит «как только в „безопасный“ файл
допишут второй тест, барьер покраснеет в ТОМ ЖЕ PR». Для `#[tokio::test]` он не покраснеет
никогда.

---

## §2 — Мутационный контроль (оба вопроса `testing.md`)

### 2.1 Вопрос (а): привязана ли каждая НОВАЯ проверка к своему сценарию

**Предмет A — привязана, и проба это РЕПОРТИТ:**

| мутация предмета | покрасневшие сценарии | проба |
|---|---|---|
| `_step_is_disarmed` → `return None` | W17, W18, A7, A8 | `VERDICT: FAIL` exit=1 |
| `_invokes` → `return True` | A4, A9, A10 | `VERDICT: FAIL` exit=1 |
| самопроверка A6 → `if False:` | A9 | `VERDICT: FAIL` exit=1 |
| **`_job_is_disarmed` → `return None`** | **НИ ОДНОГО** | `VERDICT: PASS` **exit=0** |

**Предмет B — привязана, но проба это НЕ РЕПОРТИТ:**

| мутация барьера | покрасневший сценарий | проба |
|---|---|---|
| снят фильтр «комментарий ≠ объявление» (F-2) | RO-15 печатает `FAIL` | `VERDICT: PASS (15/15)` **exit=0** |
| регулярка откачена к прежней (F-3) | RO-16 печатает `FAIL` | `VERDICT: PASS (15/15)` **exit=0** |

Это и есть главная находка вердикта — H-1 ниже.

### 2.2 Достижимость мутируемых путей — предъявлена, а не предположена

Мандат требует доказать, что мутируемая ветка ИСПОЛНЯЕТСЯ, прежде чем объявлять проверку
слепой. Предъявляю по каждой:

- `_job_is_disarmed` — путь достижим, код работает:
  ```
  $ EXPR='jobs["catchup"]["continue-on-error"]=True' … && python3 scripts/deploy_catchup.py check-wiring
  FAIL W10: джоб `catchup` обезврежен (continue-on-error: true на самом джобе)
  exit=1
  ```
  Барьер ловит; **сценария в пробе нет** — `grep -n continue-on-error
  scripts/tests/red_deploy_catchup.sh` даёт только W17 (шаг `decide`) и A7 (шаг агрегата),
  ни одного на ДЖОБ. Проверка без оракула — ровно «оракул зелёный, а мутация проверяемых
  строк его не роняет» (`testing.md`, «Что нарушение выглядит как»).
- F-2 / F-3 — мутации состоялись (`cmp` показал различие), сценарии RO-15/RO-16 покраснели,
  то есть пути исполняются.
- S2/S4/S5 — достижимость проверена ИСПОЛНЕНИЕМ в форме прода
  (`bash --noprofile --norc -eo pipefail`, §5 H-2).

### 2.3 Вопрос (б): что ослаблено РЯДОМ

**Предмет B — да, ослаблено, и сторожа у послабления нет.** Условие `tests >= 2`
(`check_resource_oracles.sh:111`) — сознательная замена ИНВАРИАНТА на ПРОКСИ «есть ли кому
аллоцировать параллельно». Само по себе оно обосновано (7 из 13 ложных красных на реальном
дереве). Но прокси считается литералом `#[test]`, а этот счёт — прокси ВТОРОГО порядка, и
у него нет ни одного сторожа: R8 показывает, что `#[tokio::test]` обнуляет счёт при живых
соседях. Обратная мутация (вернуть послабление) не ловится ничем, потому что RO-11 — мина —
построена на том же литерале, что и дыра.

Второе послабление того же класса: `thread_local!` засчитывается по НАЛИЧИЮ строки (`:92`).
Автор называет это «пределом 4», но R10 показывает цену: **одна строка комментария**
`// TODO: перевести на thread_local!` переводит дефектный оракул в «учёт потоковый».
Это тот же класс «упоминание ≠ объявление», который автор закрыл для `global_allocator`
по находке F-2, — и оставил открытым на 40 строк ниже, в том же файле.

**Предмет A — ослаблений не выявлено.** Правки B-1…B-5 добавляют проверки, ни одна не
снимает существующую; батарея из 11 мутантов после правок сохраняет пустую базовую линию и
точное равенство kill-set'ов.

---

## §3 — Уборка фикстур (`harness-track.md` §5 п.5)

Замер ЧИСЛОМ, до и после каждого прогона:

| проба | своих каталогов до | после | страж утечки |
|---|---|---|---|
| `red_deploy_catchup.sh --battery` | 0 | 0 | **есть**, роняет прогон |
| `red_resource_oracles.sh` | (не меряет) | — | **ОТСУТСТВУЕТ** |

Общий счёт `/tmp` до и после обоих прогонов — **24149 → 24149**, `tmp.*` — **2716 → 2716**:
ни одна проба не течёт фактически.

**Страж A не вакуумен** — сломал уборку точечно (`cleanup() { return 0; … }`), прогнав
мутанта на его штатном пути:

```
сценариев: 51   PASS: 51   FAIL: 0
своих каталогов (red-catchup-*): до 1, после 2 (уборка — trap EXIT + реестр)
УТЕЧКА ФИКСТУР: осталось 1 своих каталогов — реестр/trap не отработали
VERDICT: FAIL                                                        exit=1
```

51 сценарий из 51 зелёный — и прогон всё равно красный. Правка `C-096` B-4 (снимать базу ДО
создания `WORK`) сделана верно: без неё обе величины содержали бы `WORK` и разность была бы
нулевой при любом поведении `cleanup()`.

**У пробы B стража нет вовсе.** Строка `каталогов-фикстур в реестре: $(wc -l < "${REG}")`
(`red_resource_oracles.sh:188`) печатает число СОЗДАННЫХ каталогов, а не оставшихся после
уборки: это не замер «после прогона», которого требует §5 п.5. Отдельно: проба B создаёт
фикстуры голым `mktemp -d` (`:30`, `:35`, `:161`), то есть под префиксом `tmp.` — тем же,
под которым в `/tmp` уже лежит 2716 чужих каталогов. Если стража когда-нибудь добавят «как
у A», он будет мерить ОКРУЖЕНИЕ, а не свой инвариант, — ровно тот дефект, который автор
только что исправил на предмете A по находке B-4.

---

## §4 — Спор `C-096` B-6: **прав АВТОР**

Утверждение критика: «ярус S» не существует, `grep -rn "ярус S" --include=*.md .` — ни
одного вхождения; `reading-map.md` знает A/B/C и никакого S.
Утверждение автора: критик мерил на отставшем дереве; ярус S внесён `24dbc1e` и живёт в
`docs/workflow/reading-map.md:128-141`.

Проверено на дереве слияния `4a72c93`:

```
$ git grep -n 'Ярус S' 4a72c93 -- '*.md'
4a72c93:docs/workflow/reading-map.md:128:### Ярус S — СОСТОЯНИЕ МИРА (не документ, но обязателен)

$ git show 4a72c93:docs/workflow/reading-map.md | sed -n '136,138p'
gh run list --branch main --limit 5             # красен ли main ПРЯМО СЕЙЧАС
gh run list --workflow=deploy.yml --limit 3     # доехало ли до прода
```

Ссылка автора точна до строки, и цитируемая им «вторая из трёх команд» — действительно
`gh run list --workflow=deploy.yml`. Хронология снимает последние сомнения:

```
$ git log -1 --format='%ad' --date=iso 24dbc1e   → 2026-08-17 10:08:37 +0000
$ git log -1 --format='%ad' --date=iso 46a5c06   → 2026-08-17 16:14:57 +0000
$ git merge-base --is-ancestor 24dbc1e origin/main  → ДА
```

Правило существовало в `main` **за шесть часов до** аудированного коммита. Критик работал
на `audited_head: 46a5c06` (его собственная шапка GATE-META), то есть на ветке, не забравшей
`main`, — и его грепу вхождений не нашлось честно, но не истинно. Это ровно тот класс,
против которого написан `gates.md` §8 («Документ проверяется на ДЕРЕВЕ СЛИЯНИЯ, а не на
ветке»); B-6 — ложная находка, автор её оспорил правомерно, правка `f231c7d` (назвать путь
и строки) уместна.

**Одна оговорка, не меняющая исхода спора.** Ярус S живёт в `reading-map.md`, но во
ВПРЫСКИВАЕМОМ ядре не назван: `grep -c 'ярус S' CLAUDE.md .claude/agents/architect.md` даёт
`0` и `0`, а `CLAUDE.md` §2 рекламирует карту как «ярусы A/B/C». Компенсация слабого канала
эскалации существует как правило, но опирается на ярус, о котором стартовый протокол не
упоминает. Это NOTE к процессному слою, а не находка против этой ветки, и на снятие REJECT'а
не влияет.

---

## §5 — Находки

### H-1 (BLOCKER, предмет B) — пять новых сценариев не могут уронить пробу; фикс `C-095` F-1 ИНЕРТЕН

`scripts/tests/red_resource_oracles.sh:189-194` — блок вердикта и `exit 1` стоят **посреди**
файла, а сценарии RO-12…RO-16 (те самые, что добавлены по находке F-1) идут **после** него
(`:195-278`). Их `fail()` инкрементирует `FAILED`, но `FAILED` больше никогда не читается:
последняя строка файла (`:281`) печатает `VERDICT: PASS (${PASSED}/${PASSED})` БЕЗУСЛОВНО, и
скрипт завершается кодом последнего `echo` — нулём.

Воспроизведение — стабом, который `C-095` F-1 объявил убитым («стаб, судящий ТОЛЬКО по числу
`#[test]`»):

```
$ BARRIER=/tmp/adv-stubs/ro_stub_bytests.sh bash scripts/tests/red_resource_oracles.sh
FAIL  RO-12 ложное срабатывание (exit=1): барьер судит по ЧИСЛУ ТЕСТОВ, а не по учёту
FAIL  RO-13 ложное срабатывание (exit=1)
FAIL  RO-14 ложное срабатывание (exit=1): барьер пиннит ИМЯ, а не потоковый учёт
FAIL  RO-15 exit=1, ожидалось 2: барьер путает УПОМИНАНИЕ с ОБЪЯВЛЕНИЕМ
VERDICT: PASS (12/12) — барьер ловит процессный учёт, не ловит ложно, fail-closed на пустоте
$ echo exit=$?
exit=0
```

Четыре сценария из пяти КРАСНЫЕ — вердикт `PASS`, код возврата 0. CI-джоб
`resource-oracles` (`.github/workflows/ci.yml:315-317`) зовёт эту пробу напрямую и получит
зелёное. То есть весь ответ на `C-095` F-1 проверяется ничем, а два действительно рабочих
фикса (F-2, F-3) не защищены от регресса: §2.1 показывает, что мутация каждого роняет свой
сценарий — и проба всё равно печатает `PASS (15/15)`.

Отдельно ложь в самом вердикте: `(${PASSED}/${PASSED})` — тавтология, печатающая `N/N` при
любом числе провалов. `16/16` в честном прогоне и `12/12` в стабе выглядят одинаково
убедительно и не значат ничего.

**Класс, а не опечатка.** `harness-track.md` §5 п.1 требует «проба КРАСНАЯ против набора
обманных стабов»; здесь проба структурно неспособна быть красной на трети своих сценариев.
Ровно тот дефект, ради которого трек вообще заводит адверсария (§6: «автор себя не
проверяет»).

**Условие снятия:** перенести блок вердикта в конец файла (после RO-16), печатать
`PASSED/(PASSED+FAILED)` вместо `PASSED/PASSED`, и предъявить прогон против стаба
«судящего по числу тестов» с exit≠0.

### H-2 (BLOCKER, предмет A) — `_step_is_disarmed` перечисляет СУФФИКСЫ; класс не закрыт

`scripts/deploy_catchup.py:537` — `_SWALLOW_SUFFIXES = ("|| true", "|| :", "; true", "; :",
"|| exit 0")`. Это белый список пяти литералов, а не проверка свойства «падение шага не
роняет джоб». Замер предмета:

```
'cmd || true'       -> команда глушит код возврата     (ловится)
'cmd || echo skip'  -> None                            (НЕ ловится)
'set +e'            -> None                            (НЕ ловится)
if: "github.event_name == 'schedule'"  -> None         (НЕ ловится)
```

Обе непойманные формы обезвреживают шаг в ПРОД-ФОРМЕ GitHub
(`bash --noprofile --norc -eo pipefail {0}`) — проверено исполнением:

```
$ cat s4body.sh
set +e
python3 -c 'import sys; print("decision=HOLD"); sys.exit(2)'
echo готово
$ bash --noprofile --norc -eo pipefail s4body.sh; echo ШАГ exit=$?
decision=HOLD
готово
ШАГ exit=0            # честная форма того же шага даёт exit=2

$ cat s10b.sh
python3 -c "import sys; sys.exit(1)" || echo пропускаем
$ bash --noprofile --norc -eo pipefail s10b.sh; echo ШАГ exit=$?
пропускаем
ШАГ exit=0
```

Последствие для S2/S4 — восстановление ровно того дефекта, который `C-093` R-2 назвал
блокером: `decision=HOLD`, шаг зелёный, джоб зелёный, ран «Deploy to VPS» терминально
зелёный, внешне неотличимый от успешной доставки. `check-wiring` при этом печатает
`VERDICT: PASS`. Комментарий W10 (`:487-493`) обещает ловить именно этот класс, а `if:`-ветка
`_step_is_disarmed` (`:555-559`) сравнивает лишь с `false`/`${{ false }}`/`0`.

**Условие снятия:** проверять СВОЙСТВО, а не литералы — любой `|| <что угодно>` после
предмета, наличие `set +e` в теле, любой статический `if:`, не сводящийся к `true`; плюс
сценарии пробы на каждую форму.

### H-3 (BLOCKER, предмет A) — W2/W4 проверяют ПОДСТРОКУ; инверсия оператора проходит

`scripts/deploy_catchup.py:401` (`if "success" not in cond`) и `:419` (`if
"needs.catchup.outputs.decision" not in cond or "'DEPLOY'" not in cond`) — текстовые
вхождения. Инверсия оператора сохраняет обе подстроки:

```
S1: jobs.catchup.if = "github.event_name == 'workflow_run'
                       && github.event.workflow_run.conclusion != 'success'"
    → check-wiring exit=0, VERDICT: PASS
    → сторож работает ТОЛЬКО когда CI КРАСЕН; добор поверх красного CI

S8: jobs.deploy.if  = "… needs.catchup.outputs.decision != 'DEPLOY' …"
    → check-wiring exit=0, VERDICT: PASS
    → деплой едет РОВНО на решениях SKIP и HOLD
```

Обе строки — то, что W2/W4 объявляют невозможным своим же текстом сообщения («добор поверх
красного CI (ослабление fail-closed гейта запрещено Р-4)»; «на workflow_run деплой поедет
без решения сторожа»). Это тот же класс, что `C-096` B-3, который автор закрыл для A1
введением `_invokes` («проверка по ВЫЗОВУ, а не по вхождению подстроки»), — и не перенёс на
условия джобов, хотя `harness-track.md` требует чинить КЛАСС.

**Условие снятия:** сверять условие вычислением (подставить модель `conclusion=failure` /
`decision=HOLD` и потребовать, чтобы джоб не исполнялся) — механизм для этого уже написан
и работает: `_agg_expand` + `_agg_exec` (`:631-647`).

### H-4 (MAJOR, предмет A) — W10 ищет `decide` ПОДСТРОКОЙ, хотя `_invokes` уже есть

`scripts/deploy_catchup.py:504` —
`if isinstance(run, str) and "deploy_catchup.py" in run and " decide" in run:`. Рядом, в
том же файле, живёт `_invokes()` (`:578`), написанная по находке B-3 именно против этого.
Результат — S5:

```yaml
run: |
  CATCHUP=true  # временно вместо: python3 scripts/deploy_catchup.py
  $CATCHUP decide
```

`found_decide` = True (обе подстроки на месте), `_step_is_disarmed` = None → `check-wiring`
exit=0 `VERDICT: PASS`. Исполняется `true decide`; предмет не запускается ни разу:

```
$ bash --noprofile --norc -eo pipefail s5body.sh; echo ШАГ exit=$?
ШАГ exit=0     # упоминаний deploy_catchup: 1, вызовов: 0
```

**Условие снятия:** заменить подстроку на `_invokes(catchup_job, "python3
scripts/deploy_catchup.py")` с дополнительной проверкой аргумента `decide` в том же
сегменте.

### H-5 (MAJOR, предмет A) — самопроверка A6 введена только для `check-aggregate`

`cmd_check_aggregate` требует, чтобы CI звал `deploy_catchup.py check-aggregate` (`:681`),
и обосновывает это классом `TD-106`/`TD-062` («гейт есть, но не гейтит»). Тот же класс для
СОСЕДНИХ шагов не закрыт — их удаление из `ci.yml` ненаблюдаемо:

| стаб | что перестаёт исполняться в CI | барьер |
|---|---|---|
| S9 | `check-wiring` — ВЕСЬ блок W1…W10 (rollback, permissions, concurrency, SHA-якорность, эскалация HOLD) | exit=0 `PASS` |
| S12 | `red_deploy_catchup.sh --battery` — мутационный контроль (`harness-track.md` §5 п.2) | exit=0 `PASS` |
| S13 | `red_deploy_catchup.sh` — сама проба, отключена через `if: github.actor == 'nobody'` | exit=0 `PASS` |

A1 не ловит S9, потому что проверяет префикс `python3 scripts/deploy_catchup.py` — а его
даёт оставшаяся строка `check-aggregate`. То есть барьер, который есть, охраняет сам себя и
не охраняет ни один соседний шаг.

**Условие снятия:** требовать по вызову каждый из четырёх шагов (`check-wiring`,
`check-aggregate`, проба, `--battery`) и прогонять `_step_is_disarmed` по всем шагам джоба
(последнее уже делается — `:684-687`, но S13 проходит из-за H-2).

### H-6 (MAJOR, предмет A) — A3 объявляет честный рефакторинг «падающим всегда»

`_agg_run_script` (`:622`) склеивает только тела `run:`; `env:` шага не читается. Стандартная
форма условия

```yaml
- env: { R: "${{ needs.deploy-catchup.result }}" }
  run: if [[ "$R" != "success" ]]; then exit 1; fi
```

даёт `rc_red = rc_green = 1`, и барьер печатает:

```
FAIL A3: АНТИ-ПЛАЦЕБО: при всех success агрегат вернул exit=1 ('catchup red').
```

Диагноз ложен: агрегат fail-closed и корректен. Ложное красное на честной форме — та же
болезнь, за которую автор справедливо чинил барьер B по `C-095` F-1 («11 красных из 13, из
которых семь не сломаны»).

Смежное: `_agg_exec` исполняет условие через `bash -c` (`:644-646`), тогда как прод гоняет
его `bash --noprofile --norc -eo pipefail`. Гейт, проверенный не тем вызовом, каким его
зовёт прод, не проверен (`testing.md`, целостность гейта, свойство 1). Предел не назван в
коде.

### H-7 (MAJOR, предмет A) — имя воркфлоу дублировано; дрейф даёт DEPLOY поверх упавшего VPS

`deploy_catchup.py` берёт ИМЯ ДЖОБА из манифеста (`vps_job_name`, `:212`) и справедливо
гордится тем, что фильтр путей тоже берётся из манифеста («Дублирование списка дало бы дрейф
двух строк… Здесь он закрыт по построению: источник один», `:121-127`). Для ИМЕНИ ВОРКФЛОУ
это неверно: `deploy.yml:1` объявляет `name: Deploy to VPS`, а шаг сбора ранов
(`deploy.yml:159`) хардкодит `select(.name=="Deploy to VPS")`. `check-wiring` `wf["name"]`
не читает вовсе:

```
$ grep -n 'wf.get("name")\|wf\["name"\]' scripts/deploy_catchup.py
(ничего)
```

Переименование воркфлоу — одна строка — обнуляет выборку. Пустая выборка НЕ отвергается:

```
$ CATCHUP_RUNS_JSON=<[]> … python3 scripts/deploy_catchup.py decide
decision=DEPLOY
reason=… ни один Deploy-ран вершины … не исполнял 'Deploy (build on VPS)' — класс «деплой не стартовал»
exit=0
```

То есть вершина, чей VPS-джоб РЕАЛЬНО упал, классифицируется как «не стартовал» и уходит в
авто-добор — прямое нарушение различителя Р-4, который сам предмет объявляет своим сердцем
(`:23-29`: «Деплой, уже УПАВШИЙ на VPS-шаге для той же вершины, автоматом НЕ повторяется»).
`classify_runs` даёт `(None, "")` на пустом списке вакуумно, а `cmd_decide` трактует это
как положительное доказательство безопасности — при том, что шапка модуля обещает обратное
(`:31-35`).

**Условие снятия:** сверять `wf["name"]` с литералом, который использует шаг сбора (или
подставлять имя из манифеста в шаг), и назвать в коде, чем пустая выборка отличается от
«ранов не было».

### H-8 (MAJOR, предмет A) — проверка `_job_is_disarmed` без единого оракула

Мутация `_job_is_disarmed` → `return None` не роняет ни одного сценария; проба даёт
`VERDICT: PASS` exit=0 (§2.1). Путь достижим и код работает (§2.2), то есть это не мёртвая
ветка, а НЕЗАКРЕПЛЁННАЯ. `testing.md`: «Оракул зелёный, а мутация проверяемых строк его не
роняет» — прямой признак нарушения.

**Условие снятия:** сценарии `W19-catchup-job-continue-on-error` и
`A11-агрегат-job-continue-on-error`.

### H-9 (MAJOR, предмет B) — «упоминание ≠ объявление» починено для аллокатора и НЕ починено для `thread_local!`

`scripts/check_resource_oracles.sh:92` — `threadlocal="$(grep -cE 'thread_local!' "${f}")"`.
Строка в комментарии засчитывается как потоковый учёт (R10):

```
// TODO: перевести на thread_local! когда дойдут руки
static CUR: AtomicUsize = AtomicUsize::new(0);   ← процессный счётчик остаётся
→ exit=0, "OK  …: учёт потоковый"
```

Это буквально та же ошибка, которую `C-095` F-2 нашла для `global_allocator` и которую автор
закрыл awk-фильтром на 40 строк выше (`:58-69`). Класс объявлен закрытым, а закрыт один из
двух его экземпляров в одном файле.

### H-10 (MAJOR, предмет B) — детектор и счётчик привязаны к ФАЙЛУ; вынос за файл снимает барьер

R4 и R9 — две формы одного дефекта:

- **R9** — `mod meter;` рядом: аллокатор и два теста в `a.rs`, `static CUR: AtomicUsize` в
  `meter.rs`. Барьер смотрит только файл с аллокатором → «учёт потоковый», exit=0.
- **R4** — аллокатор объявлен `macro_rules!` в `src/macros.rs`, а `install_alloc!();` +
  счётчик + два теста в `tests/a.rs`. Файл-потребитель не опознан оракулом ВООБЩЕ; на учёт
  встал файл с макросом, где тестов ноль → «безопасен», exit=0.

Плюс **R2/R3** — тип счётчика вне регулярки: `OnceLock<AtomicUsize>`, `lazy_static! { static
ref … }`. Регулярка после правки F-3 знает `pub`/`mut`/полный путь, но по-прежнему требует,
чтобы `Atomic…` шёл СРАЗУ после двоеточия. Обёртка любого рода снимает барьер.

### H-11 (MAJOR, предмет B) — у пробы B нет стража утечки, а `mktemp` без префикса делает его будущим замером окружения

См. §3. `harness-track.md` §5 п.5 требует «замер „каталогов после прогона“ предъявлен
числом»; строка `:188` печатает размер РЕЕСТРА, то есть число созданных каталогов. Асимметрия
особенно заметна на фоне того, что автор в том же круге правильно починил этот же замер на
предмете A (`C-096` B-4).

### H-12 (NOTE, предмет B) — сводка барьера противоречит своему же телу

На реальном дереве:

```
OK    crates/journal/tests/red_open_bounded.rs: процессный счётчик, но тестов в бинаре 1 …
… (ещё шесть таких строк) …
VERDICT: PASS — оракулов проверено: 12, процессных счётчиков не найдено
```

Найдено семь. Формулировка «процессных счётчиков не найдено» ложна и, что важнее, скрывает
латентный список — те самые файлы, которые H-10/R8 переводят в дефектные одной правкой.

### H-13 (NOTE, предмет B) — комментарий в CI отстал от предмета

`.github/workflows/ci.yml` (ветка B): «Проба барьера (RO-1..RO-11)» и «11 сценариев» —
сценариев 16. Литерал, живущий отдельно от предмета, врёт; автор сам сформулировал это
правило в соседней ветке (`ci.yml`, джоб `deploy-catchup`: «счёт печатает сама проба»).

---

## §6 — Вердикт

# REJECT (оба предмета)

Автор заявил, что чинил КЛАСС, а не симптомы. Проверка ломанием этого не подтверждает:
**26 стабов, 18 пропущено**. Каждый из шести классов, объявленных закрытыми, закрыт в ОДНОЙ
точке и оставлен открытым в соседней — в том же файле, теми же руками, в том же коммите:

| класс | закрыт | оставлен открытым |
|---|---|---|
| проверка по ВЫЗОВУ, не по подстроке | A1 (`_invokes`) | W2, W4, W10 (H-3, H-4) |
| «упоминание ≠ объявление» | детектор `global_allocator` (F-2) | `thread_local!` (H-9) |
| «гейт есть, но не гейтит» | A6 для `check-aggregate` | `check-wiring`, проба, батарея (H-5) |
| обезвреживание шага | пять литеральных суффиксов | `\|\| echo`, `set +e`, `if`-выражение (H-2) |
| страж утечки фикстур | проба A (B-4) | проба B (H-11) |
| источник истины один, дрейфа нет | `push_paths`, `vps_job_name` | имя воркфлоу (H-7) |

Сверх того — **H-1**: пять сценариев, добавленных в ответ на прошлый вердикт, структурно не
способны уронить пробу. Это не находка «сверх плана», а отказ самого механизма проверки:
круг 2 закрывался предъявлением этих сценариев, и предъявление было пустым.

**Блокеры (снимаются обязательно):** H-1, H-2, H-3.
**MAJOR (снимаются либо получают TD-карточку с severity и владельцем):** H-4…H-11.
**NOTE:** H-12, H-13.

**Условие снятия REJECT'а — круг 4 обязан предъявить сырым выводом:**

1. прогон `red_resource_oracles.sh` против стаба «судящего по числу `#[test]`» с **exit≠0**
   и честным счётчиком вида `PASSED/(PASSED+FAILED)`;
2. прогон КАЖДОГО из 18 пропущенных стабов этого вердикта с **красным** исходом барьера
   (для S3/S11/S14 достаточно письменного разбора, почему форма безвредна, — они помечены
   выше как безвредные по исходу и блокером не являются);
3. мутацию `_job_is_disarmed` → `return None`, роняющую хотя бы один сценарий (H-8);
4. мутацию каждой НОВОЙ проверки круга 4, роняющую ровно свой сценарий, — с ненулевым кодом
   возврата пробы;
5. замер уборки фикстур пробы B числом «до/после» + прогон со сломанной уборкой, дающий
   красное (H-11).

Спор `C-096` B-6 разрешён в пользу АВТОРА (§4); B-1…B-5 признаны закрытыми в объёме §0.2,
но их классы — нет.

Маршрут остаётся харнесс-треком: предмет — `scripts/**` и `.github/workflows/**`, `crates/**`
затронуты только в `tests/`, полный milestone-цикл не требуется (`harness-track.md` §4,
три вопроса дают «нет»).

---

## Done Block

```text
$ git -C /tmp/adv-wd rev-parse HEAD; git -C /tmp/adv-ro rev-parse HEAD
f231c7d781de3d7943da3ff2b43a030408c388f5
33c4f5aa687cafdb08e372f8ad9db35b6802a6d5

$ git -C /tmp/adv-wd rev-list --count HEAD..origin/main; git -C /tmp/adv-ro rev-list --count HEAD..origin/main
0
0

$ git -C /tmp/adv-wd merge-tree --write-tree HEAD origin/main | head -1
4a72c93e44a3de333d825807a266207e5a484095

# ── §0 позитивный контроль (агрегировано) ────────────────────────────────────
$ bash scripts/tests/red_deploy_catchup.sh --battery 2>&1 | tail -6
--- батарея: мутантов 11, расхождений kill-set 0
сценариев: 51   PASS: 51   FAIL: 0
своих каталогов (red-catchup-*): до 0, после 0 (уборка — trap EXIT + реестр)
VERDICT: PASS
exit=0

$ bash scripts/tests/red_resource_oracles.sh 2>&1 | tail -1; echo exit=$?
VERDICT: PASS (16/16) — барьер ловит процессный учёт, не ловит ложно, fail-closed на пустоте
exit=0

$ python3 scripts/deploy_catchup.py check-wiring    >/dev/null; echo exit=$?   → exit=0
$ python3 scripts/deploy_catchup.py check-aggregate >/dev/null; echo exit=$?   → exit=0
$ bash scripts/check_resource_oracles.sh | tail -1
VERDICT: PASS — оракулов проверено: 12, процессных счётчиков не найдено      exit=0

# ── §1.1 стабы A: 16 построено, 12 пропущено (полный вывод красного) ─────────
S1 catchup.if != 'success'      exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S2 decide if:-выражение         exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S3 decide timeout-minutes: 0    exit=0  VERDICT: PASS      ← ПРОПУЩЕН (безвреден)
S4 decide set +e                exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S5 decide через $CATCHUP        exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S6 decide как uses:             exit=1  FAIL W10           ← пойман
S7 джоб catchup удалён          exit=1  FAIL W10           ← пойман
S8 deploy.if != 'DEPLOY'        exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S9 нет шага check-wiring        exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S10 check-aggregate || echo     exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S11 check-aggregate + exit 0    exit=0  VERDICT: PASS      ← ПРОПУЩЕН (безвреден)
S12 нет шага --battery          exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S13 проба if: github.actor      exit=0  VERDICT: PASS      ← ПРОПУЩЕН
S14 джоб timeout-minutes: 0     exit=0  VERDICT: PASS      ← ПРОПУЩЕН (безвреден)
S15 fail-closed через env:      exit=1  FAIL A3            ← ложное срабатывание
S16 шаги → внешний .sh          exit=1  FAIL A6            ← пойман

$ bash --noprofile --norc -eo pipefail s4body.sh; echo ШАГ exit=$?
decision=HOLD
готово
ШАГ exit=0
$ bash --noprofile --norc -eo pipefail -c 'python3 -c "print(\"decision=HOLD\"); import sys; sys.exit(2)"'; echo ШАГ exit=$?
decision=HOLD
ШАГ exit=2                                   ← честная форма: джоб КРАСНЫЙ
$ bash --noprofile --norc -eo pipefail s10b.sh; echo ШАГ exit=$?
пропускаем
ШАГ exit=0
$ bash --noprofile --norc -eo pipefail s11b.sh; echo ШАГ exit=$?
ШАГ exit=1                                   ← S11 НЕ обезвреживает (errexit)

# ── §1.2 стабы B: 10 построено, 6 пропущено ──────────────────────────────────
R1 счётчик в mod с отступом        exit=1  ← пойман
R2 OnceLock<AtomicUsize>           exit=0  VERDICT: PASS   ← ПРОПУЩЕН
R3 lazy_static! static ref         exit=0  VERDICT: PASS   ← ПРОПУЩЕН
R4 аллокатор через macro_rules!    exit=0  VERDICT: PASS   ← ПРОПУЩЕН
R5 #[global_allocator] в build.rs  exit=1  ← пойман
R6 файл в CRLF                     exit=1  ← пойман
R7 два аллокатора (1 честный)      exit=1  ← пойман
R8 два #[tokio::test]              exit=0  VERDICT: PASS   ← ПРОПУЩЕН
R9 счётчик в соседнем модуле       exit=0  VERDICT: PASS   ← ПРОПУЩЕН
R10 thread_local! в комментарии    exit=0  VERDICT: PASS   ← ПРОПУЩЕН

$ grep -rn --include='*.rs' -E '^\s*#\[(tokio::test|rstest|test_case)' . | grep -v /target/ | wc -l
28

# ── §2 мутационный контроль ──────────────────────────────────────────────────
$ # A: _step_is_disarmed → return None
FAIL W17-decide-continue-on-error / W18-decide-глушит-код / A7 / A8
упали: W17 W18 A7 A8 ; VERDICT: FAIL ; ПРОБА exit=1
$ # A: _invokes → return True
упали: A4-джоб-пустышка A9-барьер-не-зовётся A10-echo-с-обоими-именами ; exit=1
$ # A: A6 → if False:
упали: A9-барьер-не-зовётся ; exit=1
$ # A: _job_is_disarmed → return None
VERDICT: PASS ; ПРОБА exit=0                 ← H-8: не пиннится ничем
$ # достижимость того же пути:
$ EXPR='jobs["catchup"]["continue-on-error"]=True' … check-wiring
FAIL W10: джоб `catchup` обезврежен (continue-on-error: true на самом джобе)   exit=1

$ # B: снят фильтр «комментарий ≠ объявление» (F-2)
FAIL  RO-15 exit=1, ожидалось 2: барьер путает УПОМИНАНИЕ с ОБЪЯВЛЕНИЕМ
VERDICT: PASS (15/15)                        ПРОБА exit=0    ← H-1
$ # B: регулярка откачена (F-3)
FAIL  RO-16 exit=0, ожидалось 1: регулярка не знает 'pub static' / 'std::sync::atomic::'
VERDICT: PASS (15/15)                        ПРОБА exit=0    ← H-1

$ # B: стаб «сужу только по числу #[test]» — тот, что C-095 F-1 объявил убитым
FAIL  RO-12 / FAIL  RO-13 / FAIL  RO-14 / FAIL  RO-15
VERDICT: PASS (12/12)                        ПРОБА exit=0    ← H-1

# ── §3 уборка фикстур ────────────────────────────────────────────────────────
$ find /tmp -maxdepth 1 -type d -name 'red-catchup-*' | wc -l   → до 0
$ bash scripts/tests/red_deploy_catchup.sh --battery >/dev/null; echo exit=$?  → exit=0
$ find /tmp -maxdepth 1 -type d -name 'red-catchup-*' | wc -l   → после 0
$ find /tmp -maxdepth 1 -mindepth 1 | wc -l   → 24149 до и после обоих прогонов
$ find /tmp -maxdepth 1 -name 'tmp.*'  | wc -l →  2716 до и после обоих прогонов

$ # страж утечки A не вакуумен: cleanup() { return 0; … }
сценариев: 51   PASS: 51   FAIL: 0
своих каталогов (red-catchup-*): до 1, после 2
УТЕЧКА ФИКСТУР: осталось 1 своих каталогов — реестр/trap не отработали
VERDICT: FAIL                                exit=1
$ grep -nE 'TMP_AFTER|УТЕЧКА|own_dirs' /tmp/adv-ro/scripts/tests/red_resource_oracles.sh
(ничего)                                     ← H-11

# ── §4 спор B-6 ──────────────────────────────────────────────────────────────
$ git grep -n 'Ярус S' 4a72c93 -- '*.md'
4a72c93:docs/workflow/reading-map.md:128:### Ярус S — СОСТОЯНИЕ МИРА (не документ, но обязателен)
$ git log -1 --format='%ad' --date=iso 24dbc1e   → 2026-08-17 10:08:37 +0000
$ git log -1 --format='%ad' --date=iso 46a5c06   → 2026-08-17 16:14:57 +0000
$ git merge-base --is-ancestor 24dbc1e origin/main; echo $?   → 0
$ grep -c 'ярус S' CLAUDE.md .claude/agents/architect.md      → 0 / 0

# ── §5 H-7 ───────────────────────────────────────────────────────────────────
$ python3 -c "import yaml; print(yaml.safe_load(open('.github/workflows/deploy.yml')).get('name'))"
Deploy to VPS
$ grep -n 'Deploy to VPS' .github/workflows/deploy.yml
1:name: Deploy to VPS
159:                   | select(.name==\"Deploy to VPS\")
$ grep -n 'wf.get("name")\|wf\["name"\]' scripts/deploy_catchup.py
(ничего)
$ CATCHUP_RUNS_JSON=<пустой []> … python3 scripts/deploy_catchup.py decide
decision=DEPLOY
exit=0
```

## Cross-references

- `docs/workflow/harness-track.md` §3 (маршрут), §5 (гейт трека), §6 (пределы)
- `.claude/rules/testing.md` — анти-плацебо; мутационный контроль (оба вопроса);
  целостность гейта (свойства 1, 3, 4); «оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ»
- `.claude/rules/gates.md` §4 (артефакт гейта, GATE-META), §8 (дерево слияния, push-scope), §12
- `research/critiques/C-093-deploy-catchup.md`, `C-095-resource-oracle-barrier.md`,
  `C-096-deploy-catchup-rev2.md`
- `docs/fa/journal.md:131` (`JR-I-10`) — класс инвариантов, который охраняет предмет B
