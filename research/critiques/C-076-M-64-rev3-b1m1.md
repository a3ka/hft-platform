# C-076 — M-64 export contract rev3: critic round 3 (B1/M1 only)

**Вердикт:** NOTE.
**Дата (UTC):** 2026-08-11.
**Предмет:** `origin/feat/M-64-export-contract`, `milestones/M-64-export-contract.md` rev3.
**Проверенное дерево:** `20f9267c441112c308f3a8514fa631c2f17a8c35` —
`fix(M-64): M1 закрыт ФАКТОМ — ссылка на Н-7.3 стала разрешимой на дереве ветки [architect]`.
**Предыдущий verdict прочитан первым:** `research/critiques/C-075-M-64-rev3.md`.
**Scope round 3:** только B1 и M1. F1/F2/F3 из C-075 не переоткрывались; новой оси/значения
по stop-rule §4.3 не предъявляю.
**Модель:** средняя; RAW-гейт не сработал.

## 0. Pre-flight

```text
$ git status --porcelain
<пусто>

$ git branch --show-current
critic/M-64-c076-1786450232

$ git rev-parse HEAD
20f9267c441112c308f3a8514fa631c2f17a8c35

$ git rev-parse origin/feat/M-64-export-contract
20f9267c441112c308f3a8514fa631c2f17a8c35
```

Работа велась в отдельном worktree, чтобы не мешать параллельной M-62 RED.

## 1. B1 — команды §1.1 / §1.1bis / §1.2

**Итог:** CLOSED. Командные ячейки в §1.1, §1.1bis, §1.2 самодостаточны; прежнего `то же`
в command-ячейках нет. Заявленные exit-коды совпали, включая ожидаемые non-zero для проверок
отсутствия.

### §1.1

| команда из ячейки | факт |
|---|---|
| `grep -n 'SeriesBundle' crates/gateway/src/lib.rs` | exit=0; `SeriesBundle` найден в `crates/gateway/src/lib.rs` |
| `sed -n '55,70p' crates/gateway-serve/src/lib.rs` | exit=0; показывает `wire::ServeMsg{Snapshot, Frame, Error}` |
| `sed -n '36,66p' crates/gateway/src/lib.rs` | exit=0; показывает комментарий bump'ов и `GATEWAY_SCHEMA_VERSION: u32 = 8` |
| `grep -rn EXPORT_SCHEMA_VERSION crates/research-cli/src/` | exit=0; найден `EXPORT_SCHEMA_VERSION = 1` |
| `git ls-tree -r --name-only HEAD \| grep -c 'export.*schema'` | output `0`, exit=1; совпадает с заявленным `grep -c` absence-case |
| `ls scripts/check_export_contract.sh` | exit=2; файла нет, как заявлено |
| `ls scripts/verify_contracts.sh` | exit=0; контрольный аналог журнала существует |
| `ls scripts/diff_export_schema.sh` | exit=2; файла нет, как заявлено |
| `ls scripts/diff_contract_schema.sh scripts/diff_contract_schema.py` | exit=0; контрольные contract-diff файлы существуют |
| `grep -n 'VB-I-4\|VB-I-6' docs/fa/viz-backend.md` | exit=0; обе нормы найдены |
| `grep -c schemars crates/*/Cargo.toml` | exit=0; `crates/contracts/Cargo.toml:1`, `crates/gateway/Cargo.toml:0` |

### §1.1bis

| команда из ячейки | факт |
|---|---|
| `sed -n '41,43p' crates/gateway/src/lib.rs` | exit=0; bump 4→5 |
| `sed -n '44,49p' crates/gateway/src/lib.rs` | exit=0; bump 5→6, semantic anchor change at same shape |
| `sed -n '50,54p' crates/gateway/src/lib.rs` | exit=0; bump 6→7, CVD policy/form change |
| `sed -n '55,65p' crates/gateway/src/lib.rs` | exit=0; bump 7→8, provenance fields |

Advisory precision note: the explanatory line before the table also gives
`grep -n '^/// [0-9]:' crates/gateway/src/lib.rs` → `41, 44, 50, 55` (exit=0). The exit is
correct, but the actual output additionally includes line `72` for `CKPT_SCHEMA_VERSION`. This is
not a B1 blocker because the command-column rows are exact and executable, but the prose proof line
would be cleaner if narrowed to the `GATEWAY_SCHEMA_VERSION` block or if the explicit line list is
removed.

### §1.2

| команда из ячейки | факт |
|---|---|
| `grep -n 'GW-I-4\|snapshot(C)' crates/gateway/tests/red_gateway_live_eq_replay.rs` | exit=0; `GW-I-4` / `snapshot(C)` oracle text found |
| `grep -n 'GS-I-5\|snapshot_msg обязан обернуть' crates/gateway-serve/tests/red_serve_passthrough.rs` | exit=0; `GS-I-5` and `snapshot_msg` passthrough assertion found |
| `grep -n 'GS-I-4\|wire-roundtrip\|schema_version' crates/gateway-serve/tests/red_serve_passthrough.rs` | exit=0; `GS-I-4`, roundtrip, `schema_version` found |
| `sed -n '112,116p' crates/gateway-serve/tests/red_serve_passthrough.rs` | exit=0; shows `to_string(&msg)` → `from_str::<ServeMsg>` → `assert_eq!` |

Control scan:

```text
$ sed -n '21,72p' milestones/M-64-export-contract.md | rg -n -i '\|[^|]*то же[^|]*\|'
<пусто>
exit=1
```

## 2. M1 — BACKLOG tracking and false-claim removal

**Итог:** CLOSED.

```text
$ grep -n 'Н-7.3' milestones/BACKLOG.md
377:| Н-7.3 | **Носитель объявленной семантики** (`unit`/`anchor`/`provenance` → генерируемый реестр, fail-closed на поле без аннотации) | без него шаг `S` M-64 неисполним; `description` не годится — `diff_contract_schema.py:91` относит его к doc-only |
exit=0

$ grep -c 'Н-7\.' milestones/BACKLOG.md
5
exit=0
```

`milestones/M-64-export-contract.md` §4.4 теперь не утверждает ложное "кандидат вынесен в
BACKLOG" без факта. Текст на строках 292-299 называет точный адрес `Н-7.3`, даёт проверочную
команду и объясняет, что предыдущая ошибка была следствием непушенного BACKLOG-коммита.

`§6bis` больше не содержит старую ложную атрибуцию: он говорит, что находки `Н-7.1`...`Н-7.5`
живут в `milestones/BACKLOG.md`, и это подтверждено счётчиком `5`. Строка `Н-7.3` структурно
присутствует на проверенном дереве.

## 3. Findings

Блокирующих findings по B1/M1 нет.

### NOTE N1 — §1.1bis пояснительный grep шире заявленного списка строк

Командные ячейки §1.1bis проходят, но не-табличная пояснительная команда
`grep -n '^/// [0-9]:' crates/gateway/src/lib.rs` возвращает также строку 72. Это не ломает
закрытие B1, потому что каждая строка таблицы имеет свою точную `sed`-команду с exit=0, но
следующая правка может убрать лишний шум из доказательства.

## 4. Handoff

=== HANDOFF: CRITIC → ARCHITECT ===

## §A — Метаданные
- Дата (UTC, ISO-8601): 2026-08-11T00:00:00Z
- Milestone: M-64-export-contract
- Статус: BLOCKED по founder-order; critic NOTE по round 3 B1/M1
- HEAD: `20f9267` — `fix(M-64): M1 закрыт ФАКТОМ — ссылка на Н-7.3 стала разрешимой на дереве ветки [architect]`

## §B — Что я сделал
- Прочитал `C-075` до milestone.
- Проверил только B1/M1, без переоткрытия F1/F2/F3.
- Прогнал все команды из command-колонок §1.1, §1.1bis, §1.2 и сверил заявленные exit-коды.
- Проверил `Н-7.3` и счётчик `Н-7.*` в `milestones/BACKLOG.md`.

## §C — Артефакты / результаты
- Verdict: `research/critiques/C-076-M-64-rev3-b1m1.md`.
- B1: CLOSED.
- M1: CLOSED.
- Blocking findings: none.

## §D — Следующий агент + инвокация
- **Следующий агент:** `architect`
- **Paste-ready промпт:**
  ```
  Ты architect проекта hft-platform. Прочитай startup-протокол и verdict
  research/critiques/C-076-M-64-rev3-b1m1.md на ветке
  origin/feat/M-64-export-contract. Round 3 critic: NOTE, B1/M1 closed.
  Добавь mechanical appendix / Critic Verdict section в milestone без смены BLOCKED-статуса
  founder'а. Не стартуй dev до завершения миграции К1/К6b.
  ```

## §E — Риски / открытые вопросы
- N1 advisory: пояснительный `grep -n '^/// [0-9]:' crates/gateway/src/lib.rs` в §1.1bis
  шире заявленного line-list; command-ячейки таблицы при этом проходят.

=== END HANDOFF ===
