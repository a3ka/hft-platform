<!-- GATE-META
milestone: PR-28
audited_repo: a3ka/hft-platform
audited_base: 177b7ec8e50af1f6ec14a602172a1f35c2fb6efc
audited_head: bb1dda4ca7682796c33d49fd57b42a97ae9573fa
verdict: PASS
-->

# C-123 — адверсарий круга 3, PR #28 — PASS, закрытие состоялось и КОНЕЧНО

## Предмет и границы

Ветка `fix/resource-oracle-barrier`, вершина `bb1dda4` (снята `git rev-parse
origin/fix/resource-oracle-barrier` в этой сессии), merge-base с `origin/main` = `177b7ec`.
`origin/main` с базы ушёл на 7 коммитов (`40c7cce`, PR #59) — **только docs**: диф
`177b7ec..origin/main` по трём предметным файлам ПУСТ, `git merge-tree` — ноль
конфликт-маркеров. Роль — адверсарий харнесс-трека, круг 3 после `C-122`
(`harness-track.md` §3), свежий контекст, worktree `/tmp/hft-adv-28c` (detached,
`branch-hygiene.md` п.2).

Рамка круга задана `C-122` явно: проверяются ДВА пункта закрытия (H-1, H-2) и ничего
больше; детекторные находки за пределами — вне права круга (`П-017` A2, override).
Рамка соблюдена: всё новое ниже — либо перепроверка заявленного, либо NOTE.

Тронутый модуль предмета — `crates/journal` (+ gateway, research-cli). Живой инвариант FA
— **`JR-I-10`** (`docs/fa/journal.md:131`, открыт в этой сессии: ограниченность работы
скана пола; ресурсные оракулы `red_*_bounded.rs` — его сторожа, барьер охраняет честность
их замера). Диф ветки `crates/**` не трогает — барьер `check_review_fa.sh` даст SKIP;
инвариант назван по существу, не для галочки.

## §1 — H-1 ЗАКРЫТ: прод-форма предъявлена сценарием, оба экспоната гибнут

Базлайн: проба **25/25 PASS**, остаток фикстур **0**; барьер на реальном дереве PASS.

1. **Stub-A гибнет — воспроизведён ПРАВИЛЬНО.** Guard `[ -z "${ROOT:-}" ] && exit 0`
   вставлен ДО присваивания `ROOT="${ROOT:-${REPO_ROOT}}"` (дефект первой авторской
   репродукции — проверка после присваивания — учтён; вставка подтверждена построчно).
   Проба: **RO-28 FAIL «ОЖИДАЛСЯ exit=1 … (exit=0)» → 24/25, exit=1.**
2. **M4 гибнет.** `ROOT="${ROOT:-${REPO_ROOT}/crates}"` → RO-28 FAIL (exit=1, но зона
   `scripts/tests/red_inline.rs` в выводе отсутствует — grep обеих зон её ловит) →
   24/25, exit=1. Молчаливое сужение дефолтной зоны больше не проходит.
3. **RO-25/26/27 не ослаблены:** M1 → FAIL RO-25; M2 → FAIL RO-26 **и** RO-28 (23/25);
   M3 → FAIL RO-27. Прежние пины держат.
4. **Отрава в ОБЕИХ обещанных зонах** — фикстура RO-28 кладёт дефект и в
   `crates/x/tests/`, и в `scripts/tests/`, exit проверяется строго `-eq 1` (падающий
   стаб с exit=2 не пройдёт), обе зоны — грепом по выводу.
5. **Экспонат B назван пределом, и предел РЕАЛЕН.** `§ПРЕДЕЛЫ` п.9 — предел жанра.
   Проверено построением: Stub-C (ключ `[ -e "${ROOT}/.git" ] && exit 0` — generic-признак
   репозитория, в фикстурах пробы `.git` нет) — проба **25/25 PASS**, прод-форма на
   отравленном repo-подобном дереве с `.git`-маркером — **exit=0 вечно** (честный барьер
   там же — exit=1). Это ровно «по чему угодно» из п.9: различитель — свойство фикстур,
   стаб подстроен под пробу. Кодом не закрывается, названо честно — условие `C-122`
   выполнено в обеих половинах (сценарий + названный предел).

`C-122` просил строку предела «в шапке пробы»; автор положил её в `§ПРЕДЕЛЫ` барьера.
Это соответствует норме «перечень пределов живёт в ОДНОМ месте» (сам же `C-121` B-1) и
принимается как выполнение по существу; п.9 сам оговаривает «предел ЖАНРА, не этого
барьера».

## §2 — H-2 ЗАКРЫТ: перечень полон, завышения покрытия нет

Каждый пункт `§ПРЕДЕЛЫ` с проверяемым детекторным утверждением прогнан фикстурой против
честного барьера (двухтестовые формы, где применимо):

| пункт | фикстура | exit | текст точен? |
|---|---|---|---|
| п.7a | многострочный `#[tokio::test(\n flavor…)]` ×2 | 0 | да — ложное зелёное |
| п.7b | `#[cfg_attr(feature = "x", test)]` ×2 | 0 | да — ложное зелёное |
| п.8 | `static CUR: Mutex<usize>` + 2 теста | 0 | да — ложное зелёное |
| п.4 | `thread_local!` по наличию + счёт в атомике | 0 | да |
| п.5 | `#[test_case]` ×2 | 0 | да |
| п.6 | `/* #[global_allocator] */` + счётчик + 2 теста | 1 | да — ложное КРАСНОЕ |

Класс B-1-наоборот (пункт-«предел», который на деле ловится) — **не найден**: все шесть
проверяемых пунктов сходятся с фактическим поведением. Обе формы `C-121` B-7 плюс
`cfg_attr` теперь названы. Детекторных правок ноль — остановка доводки покрытия
(`П-017`) не тронута.

## §3 — H-3 (дискреционный) взят и работает; остаток — NOTE ниже

- Сводка называет увиденное: на реальном дереве `PASS — оракулов проверено: 12;
  процессных счётчиков найдено: 7, у всех по одному тесту в бинаре` — и семь OK-строк
  все с «тестов в бинаре 1» (сверено грепом).
- Литерал «11 сценариев / RO-1..RO-11» из `ci.yml` и «RO-1..RO-9» из шапки пробы убраны;
  вместо них — явный запрет второго носителя числа. Убрать, а не поправить — правильнее
  починки: литерал, живущий отдельно от счётчика, разошёлся бы снова.

## §4 — Оси круга 3 (мандат): устойчивость прод-формы

- **`ROOT=` пустой строкой** ≡ прод-дефолту (`:-` ловит пусто): exit=0, та же сводка. OK.
- **Вызов из чужого cwd** абсолютным путём: OK (SELF_DIR от `$0`).
- **`TMPDIR` с пробелами** (`/tmp/ro adv tmp`): проба 25/25, остаток 0, каталог пуст. OK.
- **Symlink на барьер** из `/tmp/roln`: `REPO_ROOT=/tmp` → сканирует ВЕСЬ `/tmp`
  (1380 «оракулов», 510 FAIL, соседние worktree). Направление — ГРОМКОЕ ложное красное,
  не тихий пропуск; прод так не зовёт (CI: `bash scripts/check_resource_oracles.sh` из
  чекаута). NOTE N-5, не условие.
- **Страж утечки:** `cleanup(){ :; }` → «создано: 25; ОСТАЛОСЬ: 24» → FAIL, exit=1;
  остаток убран вручную до нуля. (Первый прогон этой пробы был невалиден по МОЕЙ вине —
  копия пробы в `/tmp` теряет дефолтный `BARRIER=${SELF}/../…`; повторено с явным
  `BARRIER=`. Названо, чтобы Done Block не выглядел противоречивым.)
- **Вечно-зелёный стаб, проходящий 25/25:** существует (Stub-C, §1 п.5) — и целиком
  внутри названного предела п.9. Новым классом не является; REJECT на нём был бы
  пересмотром решения владельца, а не находкой.

## §5 — NOTES (не условия merge; на усмотрение автора при следующем касании)

- **N-1.** Сводка «у всех по одному тесту в бинаре» жёстко зашита: счётчик при НУЛЕ
  тестов тоже инкрементирует `SAFE_COUNTERS` (OK-строка честно печатает «тестов в
  бинаре 0», сводка скажет «по одному»). Сегодня на дереве все семь — ровно 1,
  ложь латентна. Класс B-5 в миниатюре, порождён самим фиксом B-5.
- **N-2.** Фикс сводки не запинен: нейтрализация `SAFE_COUNTERS=$((…+1))` — проба
  25/25 зелёная, сводка возвращается к «не найдено». Пин потребовал бы 26-го сценария —
  рост ради дискреционного NOTE; сознательно не требую (`§3bis(1)`).
- **N-3.** Порядок пунктов `§ПРЕДЕЛЫ`: 1…5, 7, 8, 9, 6 — п.6 физически после п.9.
  Номера уникальны, ссылки работают; чистая косметика.
- **N-4.** Знаменатель при утечке дрейфует («сценариев: 26») — известно с `C-122` H-3,
  как было.
- **N-5.** Symlink-вызов судит весь `/tmp` (см. §4). Громко-красное, не пропуск.

## Ограничитель §3bis(1) — рост числом

- Этот круг (`7d7152c..bb1dda4`, без вердикт-файла C-122): барьер **+28/−1**, проба
  **+37/−1**, `ci.yml` **+5/−4** — итого **+70/−6**.
- Предметные файлы теперь: барьер **256** строк, проба **434**, всего **690** (+ ~20
  строк джоба). Три круга адверсария сходились монотонно: находки 7 → 3 → 0 условий.

## Вердикт: PASS

Оба пункта закрытия `C-122` выполнены и перепроверены исполнением: H-1 — сценарий
прод-формы убивает Stub-A (в корректной репродукции) и M4, предел жанра назван и
подтверждён построением Stub-C; H-2 — перечень пределов полон, завышения покрытия нет.
H-3 взят сверх обязательного и работает. Ни одна NOTE не тянет на условие: N-1/N-2 —
латентная косметика сводки, N-3/N-4/N-5 — оформление и не-прод-оси.

**Закрытие КОНЕЧНО, спираль не возобновляется:** круг 3 не породил ни одного нового
условия; единственный оставшийся способ обмануть пробу (стаб, знающий её) назван пределом
жанра и в принципе не закрываем кодом — дальнейшие круги проверяли бы убывающее ничто.
Механизм цены стоил: три круга держали и реальную дыру прод-формы (Stub-A прошёл бы CI
молча), и молчаливое сужение зоны (M4).

По гейту трека (`harness-track.md` §5): п.1–п.3 предъявлены здесь и в `C-121`/`C-122`,
п.5 — остаток 0 предъявлен числом; п.4 (`status-check` в PR) — вне моей роли, снимается
автором при merge. Merge-препятствий не вижу: merge-preview чист, `main` предметные файлы
не трогал, агрегат сверен `C-122` B-2 и с тех пор `ci.yml` в `main` не менялся (диф пуст).

## Done Block (сырой вывод — команды этой сессии)

```
$ git rev-parse origin/fix/resource-oracle-barrier
bb1dda4ca7682796c33d49fd57b42a97ae9573fa
$ git merge-base origin/main bb1dda4                     # origin/main = 40c7cce (docs-only с базы)
177b7ec8e50af1f6ec14a602172a1f35c2fb6efc
$ git merge-tree 177b7ec origin/main bb1dda4 | grep -c '^<<<<<<<'
0
$ git diff 177b7ec..origin/main --stat -- .github/workflows/ci.yml scripts/check_resource_oracles.sh scripts/tests/red_resource_oracles.sh
(пусто)

$ bash scripts/tests/red_resource_oracles.sh | tail -3
каталогов-фикстур создано: 25; ОСТАЛОСЬ после уборки: 0 (префикс ro-probe-…)
сценариев: 25   PASS: 25   FAIL: 0
VERDICT: PASS (25/25) …                                   # exit=0
$ bash scripts/check_resource_oracles.sh | tail -1
VERDICT: PASS — оракулов проверено: 12; процессных счётчиков найдено: 7, у всех по одному тесту в бинаре (соседей нет, замер чист)   # exit=0
$ bash scripts/check_resource_oracles.sh | grep -c 'но тестов в бинаре 1'
7

# H-1 (стабы/мутанты через BARRIER=<копия>, дерево не тронуто):
stubA ([ -z ROOT ] && exit 0 ДО присваивания) → FAIL RO-28 (exit=0)   24/25 exit=1
m4    (ROOT default → …/crates)               → FAIL RO-28 (exit=1, scripts-зона пропала)  24/25 exit=1
m1 → FAIL RO-25 · m2 → FAIL RO-26 + RO-28 (23/25) · m3 → FAIL RO-27   — все exit=1

# Экспонат B / предел п.9 (Stub-C: [ -e "${ROOT}/.git" ] && exit 0 после присваивания):
probe_vs_stubC = 25/25 exit=0
repo-подобное дерево (.git + отрава), прод-форма:  stubC exit=0  · честный exit=1

# H-2 (фикстуры → exit честного барьера):
p7a-multiline=0 · p7b-cfg_attr=0 · p8-mutex=0 · p4-tl-presence=0 · p5-test_case=0 · p6-block-comment=1

# Страж утечки (cleanup(){ :; }, явный BARRIER=):
каталогов-фикстур создано: 25; ОСТАЛОСЬ после уборки: 24 … FAIL … exit=1; вручную → 0

# Оси:
ROOT= (пусто) exit=0 · чужой cwd exit=0 · TMPDIR='/tmp/ro adv tmp' 25/25, остаток 0
symlink /tmp/roln/check.sh → ROOT=/tmp, VERDICT: FAIL (510 из 1380)   # громкое красное

# N-1/N-2:
фикстура счётчик+0 тестов → OK «тестов в бинаре 0» + VERDICT «у всех по одному тесту»
m5 (нейтрализация SAFE_COUNTERS) → проба 25/25 exit=0; сводка снова «не найдено»

# рост:
git diff --numstat 7d7152c..bb1dda4 → ci.yml 5/4 · барьер 28/1 · проба 37/1 (+ C-122 190/0)
wc -l барьер/проба → 256 · 434
```

## Cross-references
- `research/critiques/C-121-…` (круг 1) · `research/critiques/C-122-…` (круг 2, условия закрытия)
- `docs/workflow/harness-track.md` §3/§5/§6 · `.claude/rules/testing.md` «Целостность гейта» п.1
- `docs/fa/journal.md` `JR-I-10` · `П-017` A2/override · `TD-157`
