# C-022 — M-22 Read Gateway plan-time critic verdict

**UTC:** 2026-07-22T19:41:46Z  
**Agent:** critic  
**Branch audited:** `origin/feat/M-22` at `4962237`  
**Base:** `d8f9379` (`origin/main`)  
**Worktree:** `/tmp/hft-critic-m22` (detached from `origin/feat/M-22`)  
**Verdict:** REJECT

## Scope Read

Read:

- `milestones/M-22-read-gateway.md`
- `docs/fa/viz-backend.md`
- `docs/07-cockpit-backend-roadmap.md`
- `docs/06-data-layer-and-storage.md`
- `docs/05-contract-layer.md`
- `docs/03-integration-contract.md`
- `docs/DESIGN.md`
- `crates/gateway/{Cargo.toml,src/lib.rs,tests/*.rs}`
- `scripts/verify_M-22.sh`
- `crates/journal/src/{lib.rs,segments.rs}`
- `crates/contracts/src/lib.rs`

Protocol/context read:

- `.claude/agents/critic.md`
- `.claude/rules/gates.md`
- `.claude/rules/testing.md`
- `.claude/rules/scope-guard.md`
- `.claude/rules/branch-hygiene.md`
- `docs/04-workflow.md`

The generic bootstrap paths `.codex/agents/critic.toml` and `.claude/rules/critic-protocol.md`
do not exist in this hft worktree; the local critic profile is `.claude/agents/critic.md`.

Commit set over `d8f9379`:

```text
270f37a architect <architect@noreply.local> docs(M-22): Read Gateway milestone — snapshot+live+replay; GW-I-1..6; bounded stream (C-021 NOTE-2)
45dd6be architect <architect@noreply.local> feat(M-22): task #1 — gateway contract-skeleton + GW-I-1..6 RED oracles [architect]
4962237 architect <architect@noreply.local> feat(M-22): task #2 — verify_M-22.sh acceptance gate [architect]
```

## Pre-flight

Five-item committed artifact set check:

1. **Milestone:** PASS. `milestones/M-22-read-gateway.md` exists, status `PROPOSED`, critic gate mandatory for new crate.
2. **T-designate skeleton:** PASS with scope note. `crates/gateway/src/lib.rs` contains T-designate DTOs and public signatures with `unimplemented!()` bodies; no working reducer implementation is present.
3. **RED tests:** PASS for presence/reachability. Four test files cover bounded memory, read-only journal, live/replay fold, export v2, and deep-band provenance.
4. **Acceptance script:** PASS for shape. `scripts/verify_M-22.sh` uses a FAIL aggregator, final `VERDICT`, non-zero exit on FAIL, workspace clippy, and grep canaries.
5. **Committed scope:** PASS. `HEAD~3..HEAD` touches only `milestones/M-22*`, `crates/gateway/**`, root `Cargo.toml`/`Cargo.lock`, and `scripts/verify_M-22.sh`. `crates/contracts/**` is untouched; `SemanticEvent` is not introduced.

Diff scope:

```text
M Cargo.lock
M Cargo.toml
A crates/gateway/Cargo.toml
A crates/gateway/src/lib.rs
A crates/gateway/tests/red_gateway_bounded.rs
A crates/gateway/tests/red_gateway_export_v2.rs
A crates/gateway/tests/red_gateway_live_eq_replay.rs
A crates/gateway/tests/red_gateway_readonly.rs
A milestones/M-22-read-gateway.md
A scripts/verify_M-22.sh
```

Tool checks:

```text
bash scripts/verify_M-22.sh; echo exit=$?
FAIL GW-I-1..6 tests (cargo test -p gateway)
PASS clippy --workspace --all-targets -D warnings
PASS no read_all / Vec<Event> materialization in gateway/src (NOTE-2)
PASS no journal-writer symbols in gateway/src (GW-I-1 read-only)
PASS gateway/src uses journal::stream (bounded read path)
PASS recorder does NOT depend on gateway (no reverse dep)
VERDICT: FAIL (1 failed)
exit=1
```

The RED state is expected before implementation. Focused test targets also compile and fail on
`unimplemented!()` as intended:

```text
cargo test -p gateway --test red_gateway_readonly       -> exit=101
cargo test -p gateway --test red_gateway_live_eq_replay -> exit=101 (boundary fixture itself passes)
cargo test -p gateway --test red_gateway_export_v2      -> exit=101
```

## Verdict

REJECT before dispatching `engine-dev`.

The M-22 artifact set is close, and the scope/contract posture is materially good: T1 is untouched,
the new gateway contract is T-designate, risk-critic is not triggered because this is read-only
MD/viz infrastructure with no order-egress, and the RED tests are reachable. The blocker is gate
integrity: current oracles do not actually prove several binding parts of the Read Gateway contract.

## Four-phase rubric

### Phase 1 — Artifact completeness / scope

PASS. The commit chain contains the milestone, gateway skeleton, RED tests, verify script, and
workspace registration. The diff is limited to the requested files. `crates/contracts/**`,
`risk`, `killswitch`, `oms`, `venue-*`, and recorder implementation paths are not modified.

The architect skeleton in `crates/gateway/src/lib.rs` is acceptable as RED bootstrap for a new
crate because `milestones/M-22-read-gateway.md:69-78` explicitly limits architect code there to
T-designate types and `unimplemented!()` signatures, and implementation ownership is assigned to
`engine-dev` for `crates/gateway/src/**`.

### Phase 2 — Datalayer / contract consistency

FAIL. The docs and signatures name the right datalayer contract, but the RED set does not prove it.
M-22 requires production reads via `journal::stream(dir, EpochFilter)` and explicit epoch discipline
(`milestones/M-22-read-gateway.md:30-36`). `docs/fa/viz-backend.md:96-101` repeats that gateway is a
bounded-memory consumer of stream/epochs/retention, not `Vec<Event>` over a 15 GB journal.

The current tests use `EpochFilter::OwnCaptureOnly`, but every fixture segment is also
`DataSource::OwnCapture`; there is no mixed own/vendor/synthetic fixture. Therefore ignoring
`EpochFilter` is not observable.

### Phase 3 — RED anti-placebo / degraded inputs

FAIL on cursor and tail-memory coverage, PASS on the degraded data shapes that are present.

Good coverage exists for:

- asymmetric L2 input: `red_gateway_live_eq_replay.rs:91-100`;
- multiplicity in one bucket split by cursor: `red_gateway_live_eq_replay.rs:91-99`,
  `red_gateway_live_eq_replay.rs:167-187`;
- segment boundary precondition: `red_gateway_live_eq_replay.rs:134-140`;
- deep-band provenance: `red_gateway_export_v2.rs:103-125`;
- snapshot bounded-memory at production-ish scale: `red_gateway_bounded.rs:112-159`.

Missing coverage is blocking because the missing properties are named as M-22 invariants, not
nice-to-have refinements.

### Phase 4 — Acceptance script / branch hygiene

FAIL only because one canary is decorative for the binding stream requirement. The script itself is
a real aggregator and exits non-zero on RED. Clippy is workspace-wide with `-D warnings`. The
reference WS binary is correctly marked optional and outside the M-22 core verdict.

Branch hygiene is OK for the audited input: branch is a feat branch, RED is not on main, and commit
authors match the architect identity.

## Einhard pre-checks

- **Pick-rationale:** PASS. Critic is mandatory: M-22 introduces a new crate (`gates.md` §1.4) and
  edits a Class-A milestone.
- **B4-pre / scope:** PASS with local carve-out. Base scope-guard does not yet list `crates/gateway`,
  but M-22 explicitly extends engine-dev ownership and limits architect source to a compile-only
  contract skeleton.
- **B5-pre / contract governance:** PASS. T1 `contracts::Event` / `EventKind` are untouched, no
  `SemanticEvent` is added, and gateway/export DTOs stay T-designate.
- **I0-pre / invariant integrity:** FAIL. `EpochFilter` semantics, cursor advancement, and bounded
  tail reads are not fully observable in the current RED suite.
- **Cross-module:** PASS for dependency direction. `recorder` does not depend on `gateway`; no
  order/risk path is introduced.

## Blocking Findings

### B1 — `EpochFilter` / `journal::stream` is specified but not proven

Evidence:

- M-22 design requires the production path to be `journal::stream(dir, EpochFilter)`, with default
  `OwnCaptureOnly` and no silent vendor/synthetic mixing:
  `milestones/M-22-read-gateway.md:30-36`.
- Datalayer already makes epoch mixing an explicit type-level decision:
  `crates/journal/src/segments.rs:81-104`.
- The canonical bounded production reader is `journal::stream`:
  `crates/journal/src/segments.rs:844-846`.
- The verify positive canary is `grep -rqE 'journal::stream|::stream\(' $SRC_FILES`
  (`scripts/verify_M-22.sh:35-36`), but it passes today on doc-comments in
  `crates/gateway/src/lib.rs:12` and `crates/gateway/src/lib.rs:134-135`; no implementation call exists.
- All gateway RED fixtures create only `DataSource::OwnCapture` segments:
  `red_gateway_bounded.rs:85`, `red_gateway_live_eq_replay.rs:61`,
  `red_gateway_readonly.rs:19`, `red_gateway_export_v2.rs:34`.

Why this blocks:

A gateway implementation that ignores the passed `EpochFilter`, or reads through a manual bounded
segment path instead of `journal::stream`, can still satisfy the current test data because there is
no excluded epoch to observe. This violates the datalayer contract (`docs/06` journal-first,
epochs/provenance discipline) and re-opens the C-021 NOTE-2 failure mode at the exact path M-22 is
supposed to close.

### B2 — Cursor semantics are not an oracle

Evidence:

- The public contract defines `Cursor { upto_seq }`, `Snapshot.cursor`, `Frame { from, to }`, and
  `frames_since(...) -> (Vec<Frame>, Cursor)`:
  `crates/gateway/src/lib.rs:36-65`, `crates/gateway/src/lib.rs:102-119`,
  `crates/gateway/src/lib.rs:146-155`.
- The skeleton docs and milestone say snapshot and live tail must form a non-overlapping cover over `seq`:
  `crates/gateway/src/lib.rs:36-40` and `milestones/M-22-read-gateway.md:37-40`.
- The live/replay tests compare only `series` JSON after folding:
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:160-164` and
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:183-187`.
- The returned cursor from `frames_since` is ignored:
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:180-181`.

Why this blocks:

An implementation can produce correct-looking series while leaving `Snapshot.cursor`, `Frame.from`,
`Frame.to`, or the returned `frames_since` cursor stale/wrong. That would break live clients
immediately: reconnect/tail reads can duplicate frames, skip frames, or never advance, while the
current oracle remains GREEN.

### B3 — GW-I-2 names `frames_since`, but bounded-memory is only tested for `snapshot`

Evidence:

- GW-I-2 states `snapshot`/`frames_since` over the journal must be O(1) memory and catch `read_all`:
  `milestones/M-22-read-gateway.md:63`.
- The bounded-memory oracle only measures `gateway::snapshot(...)`:
  `crates/gateway/tests/red_gateway_bounded.rs:131-148`.
- `frames_since` and `replay` are exercised only on small fixtures in
  `red_gateway_live_eq_replay.rs`.

Why this blocks:

The live tail path can materialize a long history or collect the whole stream into an intermediate
buffer and still pass the current resource oracle. The grep canary catches literal `read_all` and
literal `Vec<Event>`, but it does not catch equivalent materialization such as collecting the stream
before reducing it. This leaves the TD-011/C-021 production-scale failure mode open for the actual
live path.

## Non-blocking Checks

### Scope / T1 contract

PASS. Root `Cargo.toml` only registers `crates/gateway`; `Cargo.lock` only adds the new package
entry. `crates/contracts/**` is unchanged. M-22 correctly keeps export v2 / gateway DTOs as
T-designate and does not introduce `SemanticEvent`.

### Read-only / no order-egress

PASS at plan-time. The gateway tests assert segment bytes are unchanged around read calls
(`red_gateway_readonly.rs:83-107`), and `verify_M-22.sh:30-42` adds source canaries for writer
symbols and reverse recorder dependency. No order path, risk, killswitch, OMS, or venue order-egress
surface is present.

### Deep-band provenance

PASS for the scoped M-22 export DTO. `red_gateway_export_v2.rs:72-78` requests 0.1% and 3% bands;
`red_gateway_export_v2.rs:103-125` requires non-empty provenance for deep rows. This aligns with
`docs/fa/viz-backend.md:66-79`: deep bands are diff-reconstructed and validated only up to about
1.3%; Tardis is for history, not a magic depth oracle.

### RED reachability

PASS. All test targets compile. In current RED state they fail on the intended
`unimplemented!()` gateway bodies, while `boundary_is_multi_segment` proves the segment-boundary
fixture itself is real.

## Required Before Re-review

Architect needs to tighten the M-22 RED/verify artifact set so the binding properties above are
observable before `engine-dev` starts. The re-review should specifically show that:

- `EpochFilter`/provenance selection affects gateway output, not just function signatures;
- the stream canary proves code, not comments;
- cursor advancement and frame window bounds are asserted;
- `frames_since` has production-scale bounded-memory coverage comparable to `snapshot`.

No implementation design is prescribed here; these are defects in the current plan-time gate.

## Handoff

REJECT → return to `architect` for a M-22 RED/verify re-spin, then route back through critic before
dispatching `engine-dev`.

## Confidence

High. The audit read the milestone, gateway source/tests, acceptance script, datalayer/contract
docs, local workflow rules, and executed the verify script plus focused gateway test targets.

---

## Re-review r2 — 2026-07-22T20:10:57Z

**Agent:** critic
**Branch audited:** `origin/feat/M-22` at `1808c0a`
**Range:** `4c44741..1808c0a`
**Worktree:** `/tmp/hft-critic-m22r2`
**Re-review verdict:** REJECT

### r2 Commit Set

```text
73ed92b architect <architect@noreply.local> fix(M-22): r2 — GW-I-7 EpochFilter oracle + GW-I-8 cursor-bounds + GW-I-2 frames_since bounded [architect]
e3b60ad architect <architect@noreply.local> docs(M-22): r2 — GW-I-7/8 инварианты, frames_since(max_events), C-022 re-spin [architect]
1808c0a architect <architect@noreply.local> fix(M-22): r2 — verify канарейки по КОДУ, не doc (strip comments, call-form journal::stream) [architect] (C-022 B1)
```

Scope over r2:

```text
M crates/gateway/src/lib.rs
M crates/gateway/tests/red_gateway_bounded.rs
A crates/gateway/tests/red_gateway_epoch_filter.rs
M crates/gateway/tests/red_gateway_live_eq_replay.rs
M crates/gateway/tests/red_gateway_readonly.rs
M milestones/M-22-read-gateway.md
M scripts/verify_M-22.sh
```

Scope remains limited to the M-22 plan-time artifact set. T1 `crates/contracts/**`, journal
implementation, recorder, venues, risk, killswitch, and OMS remain untouched.

### Tool Checks

```text
cargo test -p gateway --tests --no-run; echo exit=$?
Executable tests/red_gateway_bounded.rs
Executable tests/red_gateway_epoch_filter.rs
Executable tests/red_gateway_export_v2.rs
Executable tests/red_gateway_live_eq_replay.rs
Executable tests/red_gateway_readonly.rs
exit=0

bash scripts/verify_M-22.sh; echo exit=$?
FAIL GW-I-1..6 tests (cargo test -p gateway)
PASS clippy --workspace --all-targets -D warnings
PASS no read_all / Vec<Event> materialization in gateway/src (NOTE-2)
PASS no journal-writer symbols in gateway/src (GW-I-1 read-only)
FAIL gateway/src CALLS journal::stream( (bounded read path — code, not doc)
PASS gateway/src threads EpochFilter (GW-I-7 no silent epoch mix)
PASS recorder does NOT depend on gateway (no reverse dep)
VERDICT: FAIL (2 failed)
exit=1

cargo test -p gateway --test red_gateway_epoch_filter; echo exit=$?
2 failed on crates/gateway/src/lib.rs:143 unimplemented!()
exit=101

cargo test -p gateway --test red_gateway_live_eq_replay; echo exit=$?
1 passed, 4 failed on crates/gateway/src/lib.rs:143 unimplemented!()
exit=101
```

The RED state is still intentional. The new `journal::stream(` positive canary failing in RED is
acceptable: it is a GREEN-gate predicate that should become PASS only after `engine-dev` writes the
real production read call.

### Closed From C-022

**B2 cursor/frame bounds: CLOSED for the original defect.** `red_gateway_live_eq_replay.rs` now uses
the returned `frames_since` cursor through `drain_frames`, asserts monotonic progress, contiguous
`Frame.from/to`, empty-batch cursor stability, `snapshot(LATEST).cursor == last_seq`, and
`snapshot(START)` empty-series semantics. This closes the old `_end` ignored / wrong `Frame.from/to`
gap.

**B3 history-materialization skip: PARTIALLY CLOSED.** `red_gateway_bounded.rs` now measures
`frames_since` near the tail on 16 MiB vs 64 MiB journals and will catch a naive
`stream.collect::<Vec<Event>>()` before skipping to `after`.

**B1 stream canary: CLOSED for comment false-positive.** `verify_M-22.sh` strips line comments and
requires call-form `journal::stream(` rather than matching doc text.

### Remaining Blocking Findings

#### R2-B1 — EpochFilter is still only a `snapshot` oracle

Evidence:

- M-22 states gateway must never fold epochs rejected by the passed `EpochFilter`:
  `milestones/M-22-read-gateway.md:37-47`.
- The new mixed-epoch RED covers only `gateway::snapshot(...)`:
  `crates/gateway/tests/red_gateway_epoch_filter.rs:96-99` and
  `crates/gateway/tests/red_gateway_epoch_filter.rs:122-131`.
- The live/replay tests still run only `EpochFilter::OwnCaptureOnly` over an OwnCapture-only fixture:
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:58-64`,
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:144-145`,
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:188-195`.
- The bounded tail test also uses only OwnCapture fixtures:
  `crates/gateway/tests/red_gateway_bounded.rs:80-88`,
  `crates/gateway/tests/red_gateway_bounded.rs:174-179`.

Why this still blocks:

An implementation can honor `EpochFilter` in `snapshot` but silently use `EpochFilter::All` in
`frames_since` or `replay`; r2 tests do not observe that. That leaves live-push/replay free to mix
Vendor/Synthetic into the cockpit while initial snapshot remains clean.

#### R2-B2 — Explicit epoch selection is not proven distinct from `All`

Evidence:

- The milestone promises `Explicit([own,vendor])` has its own third value:
  `milestones/M-22-read-gateway.md:75`.
- The test comment says Explicit excludes Synthetic and should sit between Own and All:
  `crates/gateway/tests/red_gateway_epoch_filter.rs:124`.
- The assertion only checks `own_vendor != own`:
  `crates/gateway/tests/red_gateway_epoch_filter.rs:133-137`.

Why this still blocks:

An implementation that treats `EpochFilter::Explicit(_)` as `EpochFilter::All` passes this test
because `all != own`. That violates the explicit-epoch governance rule the oracle claims to protect.

#### R2-B3 — `max_events` cap is specified but not asserted

Evidence:

- r2 changes the API and milestone to `frames_since(after, max_events)` and promises batch size
  bounded by `max_events`: `crates/gateway/src/lib.rs:146-160`,
  `milestones/M-22-read-gateway.md:40-42`.
- `drain_frames` calls `frames_since(..., 3)` but never asserts that the first call stops at the
  third event or that the batch/window obeys the cap:
  `crates/gateway/tests/red_gateway_live_eq_replay.rs:134-168`.
- The production-scale bounded-memory test calls `frames_since(..., usize::MAX)`:
  `crates/gateway/tests/red_gateway_bounded.rs:174-179`.

Why this still blocks:

An implementation can ignore `max_events`, return the entire tail in one call, and still satisfy the
current cursor/fold checks on the small live fixture. The near-tail memory test catches
materializing history before the cursor, but it does not prove the new output cap that r2 relies on
to keep live batches bounded.

### Non-blocking Notes

- `verify_M-22.sh:14-16` still labels the aggregate as `GW-I-1..6` even though r2 adds GW-I-7/8.
  This is label drift, not the blocking issue.
- The milestone line `Cursor{after_seq: Option<u64>}` is wording drift from the actual
  `Cursor { upto_seq }` type. The tests now pin the intended behavior, so this is not a separate
  blocker.

### Re-review Handoff

REJECT → return to `architect` for another M-22 RED/verify re-spin, then route back through critic
before dispatching `engine-dev`.

---

## Re-review r3 — 2026-07-22T20:22:22Z

**Agent:** critic
**Branch audited:** `origin/feat/M-22` at `72b8e5e`
**Range:** `ccb7969..72b8e5e`
**Worktree:** `/tmp/hft-critic-m22r3`
**Re-review verdict:** PASS

### r3 Commit Set

```text
06c6cd8 architect <architect@noreply.local> fix(M-22): r3 — EpochFilter on frames_since/replay + Explicit≠All + max_events cap asserted [architect]
72b8e5e architect <architect@noreply.local> docs(M-22): r3 — GW-I-7/8 расширены (frames_since/replay/explicit/max_events cap) [architect]
```

Scope over r3:

```text
M crates/gateway/tests/red_gateway_epoch_filter.rs
M crates/gateway/tests/red_gateway_live_eq_replay.rs
M milestones/M-22-read-gateway.md
```

Scope remains limited to architect-owned RED/milestone artifacts. T1 contracts, gateway impl bodies,
journal, recorder, venues, risk, killswitch, and OMS remain untouched.

### Tool Checks

```text
cargo test -p gateway --tests --no-run; echo exit=$?
Executable tests/red_gateway_bounded.rs
Executable tests/red_gateway_epoch_filter.rs
Executable tests/red_gateway_export_v2.rs
Executable tests/red_gateway_live_eq_replay.rs
Executable tests/red_gateway_readonly.rs
exit=0

cargo test -p gateway --test red_gateway_epoch_filter; echo exit=$?
4 failed on the intended unimplemented! bodies:
- snapshot
- frames_since
- replay
exit=101

cargo test -p gateway --test red_gateway_live_eq_replay; echo exit=$?
1 passed (boundary fixture), 5 failed on intended unimplemented! bodies
exit=101

cargo test -p gateway --test red_gateway_bounded; echo exit=$?
failed on intended snapshot unimplemented! after building the 64 MiB fixture
exit=101

bash scripts/verify_M-22.sh; echo exit=$?
FAIL GW-I-1..6 tests (cargo test -p gateway)
PASS clippy --workspace --all-targets -D warnings
PASS no read_all / Vec<Event> materialization in gateway/src (NOTE-2)
PASS no journal-writer symbols in gateway/src (GW-I-1 read-only)
FAIL gateway/src CALLS journal::stream( (bounded read path — code, not doc)
PASS gateway/src threads EpochFilter (GW-I-7 no silent epoch mix)
PASS recorder does NOT depend on gateway (no reverse dep)
VERDICT: FAIL (2 failed)
exit=1
```

The `journal::stream(` positive canary still fails in RED because the skeleton has no implementation;
that is acceptable for plan-time. It should flip only when `engine-dev` writes the real production
read path.

### Closure of r2 Blockers

**R2-B1 — EpochFilter outside `snapshot`: CLOSED.** r3 adds direct `frames_since` and `replay`
coverage on the same mixed OwnCapture/Vendor/Synthetic journal:

- `frames_since_honors_epoch_filter` drains frames with `OwnCaptureOnly` and `All` and compares raw
  frame JSON, without depending on `Snapshot::apply`.
- `replay_honors_epoch_filter` does the same for `replay`.

An implementation that uses `All` in either tail/replay path regardless of the passed filter now
returns identical own/all frames and fails the oracle.

**R2-B2 — Explicit collapsed to `All`: CLOSED.** r3 changes the explicit test to compare
`Explicit([own,vendor])` against both `OwnCaptureOnly` and `All`. This closes the specific r2
hole where `Explicit(_)` could be treated as `All` and still pass.

**R2-B3 — `max_events` cap not asserted: CLOSED.** r3 adds
`frames_since_respects_max_events_cap` over an 8-event contiguous fixture with `max_events=3`.
The test asserts that the first call covers no more than 3 seqs and leaves tail. A whole-tail
batch returns `covered=8` or advances to `last`, so it fails.

### Non-blocking Notes

- `milestones/M-22-read-gateway.md:3-5` still says `re-spin r2`. This is status-label drift only;
  the r3 diff and this verdict identify the actual audited range.
- The explicit test proves `Explicit([own,vendor]) != OwnCaptureOnly` and `!= All`, but does not
  assert the exact expected CVD value `0`. Given the required production path through
  `journal::stream(dir, filter)`, this is sufficient for the r2 blocker and not worth another
  plan-time cycle.
- `verify_M-22.sh` still labels the aggregate as `GW-I-1..6` although r2/r3 added GW-I-7/8. This
  is label drift, not a gate defect.

### Re-review Handoff

PASS → dispatch `engine-dev` for M-22 implementation tasks 3-5 against the RED suite.
