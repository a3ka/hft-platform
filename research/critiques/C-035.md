# C-035 - M-48 rev4 checkpoint bootstrap + ops critic verdict

**Date:** 2026-07-28T23:09:47Z
**Agent:** critic
**Worktree:** `/tmp/hft-critic-m48-rev4`
**Branch:** `origin/feat/M-48` audited via detached critic worktree
**Audited HEAD:** `795e105` - `test(M-48): rev4 - закрыт C-034 R2 (регрессия покрытия через ВОЗВРАЩАЕМОЕ значение)`
**Base:** `origin/main @ e0299c6`
**Verdict:** NOTE

## Pre-flight

Five-item artifact gate passed at `795e105`:

1. Milestone present: `milestones/M-48-checkpoint-bootstrap-and-ops.md`.
2. Acceptance script present and executable as a real aggregate gate: `scripts/verify_M-48.sh`.
3. RED tests present: `crates/gateway/tests/red_checkpoint_bootstrap_truncated.rs` and `crates/gateway/tests/red_gateway_schema_version.rs`.
4. Protocol/FA update present from the M-48 chain: `docs/fa/viz-backend.md` adds VB-I-11 and schema version 8 rationale.
5. Scope is architect-owned artifact scope only: milestone, RED tests, verify scripts, FA doc, prior critic artifacts.

Command checks:

```text
$ cargo test --manifest-path /tmp/hft-m48-mono-scratch.CjwNW8/Cargo.toml
running 2 tests
test higher_cursor_updates_checkpoint ... ok
test lower_cursor_returns_err_and_does_not_regress_checkpoint ... ok
test result: ok. 2 passed; 0 failed

$ bash scripts/verify_M-48.sh; printf 'exit=%s\n' "$?"
VERDICT: FAIL (13 проверок)
exit=1
```

The M-48 aggregate RED profile is expected: fmt/build pass; M-48 fails on missing `Snapshot.history_start_seq` / `Snapshot.history_truncated`, schema version 7 vs expected 8, and missing ops wrappers. Existing M-38b regression tests included in the gate remain green.

## Verdict justification

NOTE: C-034 R2 is closed. The lower-cursor oracle now pins both channels: `advance_to(low)` over a higher valid checkpoint must return `Err`, and the checkpoint directory must remain byte-identical. The paired higher-cursor test still proves the guard is not over-broad.

This is dispatchable to `engine-dev`. The remaining failures are intended implementation REDs, not plan-time artifact blockers.

## Findings

No blocking findings.

## Closure checks

**R2 return-value channel is closed.**

The test now captures the lower-cursor result and asserts `res.is_err()` before the byte comparison: `crates/gateway/tests/red_checkpoint_bootstrap_truncated.rs:607-623`. The original no-write check remains: `crates/gateway/tests/red_checkpoint_bootstrap_truncated.rs:624-634`. The paired forward-progress test remains: `crates/gateway/tests/red_checkpoint_bootstrap_truncated.rs:640-671`.

I could not run the committed integration file selectively because the same file intentionally compile-fails on the new M-48 `Snapshot` fields. To verify the architect's current-code claim independently, I ran an external scratch crate against the audited libraries by path; it executed the two monotonicity scenarios and passed. Code inspection matches that result: the current implementation returns `Err` when `ckpt_cursor > upto`: `crates/gateway/src/lib.rs:2163-2172`.

**No remaining lower-coverage publication channel found.**

`checkpoint::advance()` is only `advance_to(..., Cursor::LATEST)`, so it does not introduce a lower explicit cursor path: `crates/gateway/src/lib.rs:2097-2104`. The production `gateway-checkpoint` binary calls `advance_to`, exits nonzero on `Err`, and writes `covered_through_seq` only after success from the returned cursor: `crates/gateway/src/bin/gateway-checkpoint.rs:266-300`. A repository grep found no second production writer for the coverage artifact outside this binary path.

**R1 remains closed.**

No active script hard-pin to public gateway schema version 7 remains. The grep hits are historical comments or the generic `red_gateway_schema_version` oracle.

## Direct answers to architect

1. **Does the lower-cursor test now assert `Err` before bytes?** Yes.
2. **Does it execute and pass against current code?** Yes via external scratch crate: 2 tests passed, including the `Err` requirement.
3. **Can lower `covered_through_seq` still leak through `advance()` or binary publication?** I do not see a remaining channel. `advance()` is LATEST-only; the binary exits before writing coverage on lower-cursor `Err`.
4. **Can all M-48 oracles pass while TD-048 remains 382 s?** I do not see a plan-time oracle bypass for the default bootstrap + serve path. Final truth still depends on reviewer §8 production measurement, which the milestone correctly keeps as decisive.

## Next agent

Next agent: `engine-dev`.

Paste-ready prompt:

```text
Ты — engine-dev проекта hft-platform. Реализуй M-48-checkpoint-bootstrap-and-ops (TD-048 MAJOR) на ветке feat/M-48 после critic NOTE C-035.

Обязательное чтение перед кодом:
- milestones/M-48-checkpoint-bootstrap-and-ops.md
- docs/fa/viz-backend.md (VB-I-11)
- research/critiques/C-032.md, C-033.md, C-034.md, C-035.md
- scripts/verify_M-48.sh

Задачи:
1. Снять безусловный fail-loud в gateway::checkpoint::advance_to при пустом ckpt и усечённом журнале: бутстрап на earliest доступном seq легален.
2. Добавить history_start_seq + history_truncated в Snapshot и заголовок чекпоинта; значение брать из первого реально свёрнутого события, не из header.first_seq.
3. Оставить fail-loud только для разрыва checkpoint↔journal: earliest_seq > ckpt.cursor + 1 => Err и ничего не писать. Сохранить монотонность: advance_to ниже уже сохранённого курсора => Err и байты ckpt не меняются.
4. Поднять GATEWAY_SCHEMA_VERSION 7→8 и пробросить новые поля через gateway-serve на провод.
5. Добавить deploy/bin/gateway-checkpoint-cron.sh + запись в deploy/cron.d/. Wrapper обязан поддерживать HFT_CRON_PRINT_ARGV=1: печатает фактический argv без docker/записи и выходит 0.
6. Обновить deploy/bin/journal-retention-cron.sh: передавать --checkpoint-coverage=<path>, поддерживать HFT_CRON_PRINT_ARGV=1, путь обязан совпадать с --coverage-out чекпоинтера.
7. Довести bash scripts/verify_M-48.sh до VERDICT: PASS, затем оформить Done Block. Не трогай crates/*/tests/** и scripts/verify_*.sh.
```

## Confidence

High. The previous bypass is now pinned by return value plus bytes, and the remaining red failures are the intended engine-dev implementation surface.
