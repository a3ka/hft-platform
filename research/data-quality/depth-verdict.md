# M-32 — ВЕРДИКТ достоверности глубины стакана (task 5, синтез Q1+Q2 → founder-решение)

**Автор синтеза:** architect. **Дата:** 2026-07-24 (UTC). **Ветка:** `feat/M-32-depth-verification`.
**Источники:** Q1 `research/data-quality/depth-sources-survey.md` (research-dev), Q2
`research/data-quality/depth-lifetime-results.md` (research-dev, прогон BTCUSDT segment 78),
depth-probe `depth-probe-{binance,staleness}.md`. **Принцип:** C-020/M-10 «сначала провалидируй
ИЗМЕРИТЕЛЬ, потом доверяй» — доведён до глубины (чего раньше НЕ делали).

Этот документ — **вердикт-гейт**: TPP-полосы НЕ включаются в export-контракт до founder-решения ниже.

## FOUNDER-РЕШЕНИЕ (2026-07-24, граница C, INTG-I-3) — APPROVED с поправкой диапазона

> **✅ APPROVED (founder, 2026-07-24):** строить TPP-полосы глубины на нашей diff-реконструкции с честной
> пометкой `depth_band_provenance: "diff-reconstructed, validated<=1.3%"` (реш. i/ii/iii ниже приняты разом).
> **ПОПРАВКА founder'а: диапазон полос — 1.5%–60%** (полный TPP-набор 1.5/3/5/8/15/30/60), НЕ 3–30%.
>
> **✅ Граница верификации ЗАКРЫТА (M-33, 2026-07-24):** живость доказана для **1.5–30%** (M-32) И для
> **30–60% ([3000,6000) bps)** (M-33, тот же gap-free segment 78). Замер: **bid 30–60% cancel_fraction=0.622**
> (n=156, устойчива), **ask=0.366** (n=41, шумная). Тренд по глубине монотонно ↓ (0.867→0.796→0.622 bid) —
> дальние уровни «стикче» (крупные лимиты, меньше churn), НО cancel_fraction всё ещё > 0.5 (bid) ⇒
> **ЖИВАЯ-РАЗРЕЖЕННАЯ (alive-sparse), НЕ замёрзшая у потолка.** Порог founder-флага (drop >50% от соседней
> [1500,3000) ПО ОБЕ стороны) НЕ сработал: bid drop 21.9%, ask drop 33.5% — оба <50%. ⇒ 30–60% **live-verified**
> (founder-решение 1.5–60% + provenance покрывает; нового решения НЕ требуется). Caveat: разрежённость
> (n=156/41 за 3.4ч) + ask-шум → band-суммы 30–60% интерпретировать с бОльшей неопределённостью, чем 1.5–30%.

---

## ⛔ ОСНОВАНИЕ РЕШЕНИЯ (ii) ПОРАЖЕНО — статус «live-verified» ОТОЗВАН (2026-08-03)

**Что произошло.** Метрика `cancel_fraction`, на которой стоит весь вердикт живости ниже,
измеряет НЕ то, что утверждается в §2. Установлено по коду
(`crates/research-cli/src/depth_lifetime.rs:155-196`, `states` не очищается никогда):
`fate` уровня фиксируется на ПЕРВОМ `size=0` и больше не меняется, поэтому перерождение цены
невидимо. Фактически считается **доля distinct-ЦЕН, получивших хотя бы один `size=0` за окно**,
а не доля жизней уровня, закончившихся отменой.

**Почему это меняет вывод, а не уточняет его.**
1. Смещение направлено В СТОРОНУ вывода: уровень, отменённый однажды и после этого стоящий
   вечно — буквально сигнатура фантома TD-016 — засчитывается как `cancelled`.
2. У величины нет предела-оценки: «хотя бы раз за окно» насыщается с ростом окна → 1.0 для
   любой цены независимо от живости, и быстрее — на плотной сетке. Значит `NEAR 0.981` vs
   `FAR 0.805` и тренд M-33 `0.867→0.796→0.622` сравнивают **уровни насыщения, а не физику**.
   Числа нельзя починить коэффициентом — их надо переснять другой величиной.

**Статус полос 1.5–60%: `live-verified` → `verification pending` (TD-098 → M-58).**

**Замок (арбитраж `research/arbitration/A-002-depth-metric-tpp.md`, `gates.md` §0,
обязателен к исполнению):** до снятия запрещено (З-1) включать в прод-выдачу полосы глубже
1.3% и (З-2) опираться на числа `cancel_fraction` как на доказательство живости.
Замок снимается АВТОМАТИЧЕСКИ переснятием segment 78 исправленной per-life метрикой (M-58),
если новые числа подтверждают живость — новой founder-подписи для этого не требуется.

**Что заморозкой НЕ является:** сбор и запись данных не трогаются; код gateway/полос/провенанса
остаётся как есть; решение (i) «эталона глубже 1.3% нет, вендора не покупать» — В СИЛЕ; инвариант
`VB-I-5` (обязательность провенанс-пометки) — В СИЛЕ, правится только его мотивировка;
research-использование чисел с явной пометкой дефекта — разрешено.

**Источники:** `research/reviews/R-031-retro-audit.md` §A.1 (первичная находка),
`docs/plans/depth-metric-tpp-question.md` (предмет), `A-002` (решение).
**Историю ниже НЕ переписываем** — она осталась такой, какой была на момент подписи.

---

## ⚠️ M-58 ПЕРЕСНЯТО per-life метрикой — **замок A-002 ОСТАЁТСЯ** (2026-08-04, ред. 2)

Segment 78 переснят исправленным анализатором (единица учёта — ЖИЗНЬ уровня, не цена).
Условия прогона, полная таблица 14 строк и сырой вывод — `depth-lifetime-results.md` §M-58.

> **Поправка к редакции 1 (по `R-033` F-1/F-2).** Первая редакция этого раздела объявляла
> «замок СНЯТ» и утверждала, что четыре полосы «не переснимались». **Оба утверждения неверны.**
> Все 14 строк были посчитаны ТЕМ ЖЕ прогоном (`BANDS_BPS` — константа из 7 полос, `analyze`
> пре-инициализирует все, пример печатает все без фильтра); в файл перенесли 6 из 14. Среди
> неперенесённых оказались ДВА ХУДШИХ числа таблицы — `ask [300,500) = 0.419` и
> `ask [800,1500) = 0.247`. Направление отбора совпало с направлением вывода: решение приняли
> по подмножеству, из которого выпало опровержение, лежавшее в том же stdout. Это тот же класс,
> что «фикстура счастливого пути», применённый к транскрипции замера.

### Полная таблица (segment 78, per-life)

| полоса | bid `cancel_fraction` | n (lives) | ask `cancel_fraction` | n (lives) |
|---|---:|---:|---:|---:|
| `[0,150)` | 0.992 | 315200 | 0.990 | 293010 |
| `[150,300)` | 0.867 | 4012 | 0.882 | 2009 |
| `[300,500)` | 0.713 | 652 | **0.419** ⚠ | 167 |
| `[500,800)` | 0.883 | 1669 | 0.620 | 184 |
| `[800,1500)` | 0.964 | 14457 | **0.247** ⚠ | 85 |
| `[1500,3000)` | 0.981 | 23945 | 0.933 | 1292 |
| `[3000,6000)` | 0.873 | 463 | **0.403** ⚠ | 67 |

### Вердикт — пополосно и посторонне

- **bid, все семь полос — живость ПОДТВЕРЖДЕНА** (0.713–0.992). Ниже 0.5 нет ни одной полосы;
  наименьшая `[300,500) = 0.713`.
- **ask — СМЕШАННЫЙ результат.** Подтверждена на `[0,150)`, `[150,300)`, `[1500,3000)`
  (0.882–0.990). **Опровергнута на `[300,500) = 0.419`, `[800,1500) = 0.247`,
  `[3000,6000) = 0.403`** — большинство жизней там не завершается отменой. `[500,800) = 0.620`
  — пограничная при n=184.
- **Профиль по ask НЕ монотонен по глубине** (0.882 → 0.419 → 0.620 → 0.247 → 0.933 → 0.403):
  это не «чем глубже, тем мертвее», а провалы на РАЗРЕЖЕННЫХ полосах (n = 167 / 184 / 85 / 67
  против 1292 и 2009 у подтверждённых). Разрежённость и незавершённость здесь идут вместе, и
  различить «уровень стоит, потому что его не трогают» от «уровень стоит, потому что он
  фантом» этот замер НЕ может.

### Почему замок НЕ снят

`A-002` путь (a) снимает заморозку автоматически, только если новые числа **подтверждают**
живость; при опровержении вопрос уходит founder'у. Здесь исход **третий — смешанный**: bid
подтверждает, ask опровергает на трёх полосах из шести глубже 1.3 %. Milestone не имеет
полномочий объявить замок снятым по такому исходу, и **З-1/З-2 остаются в силе целиком**:
полосы глубже 1.3 % в прод-выдачу не включаются, `cancel_fraction` не служит доказательством
живости для ask-полос выше.

Что при этом установлено твёрдо и замком не отменяется: **метрика починена** (дефект
переспецификации закрыт, оракулы дискриминирующие), **измеритель больше не насыщается**, и
**bid-сторона живость подтверждает на всех глубинах**. Решение о том, включать ли bid-полосы
без ask-стороны, — состав прод-выдачи, то есть воля владельца (`gates.md` §0.1), и оформлено
в `docs/PENDING-SIGNATURE.md` П-011.

### Предсказание о направлении смещения ОПРОВЕРГНУТО замером

`R-031` и `A-002` ожидали смещения ВВЕРХ («отменён однажды, дальше заморожен ⇒ считается
cancelled» — сигнатура фантома). Фактически доминировал другой эффект: множество завершённых
отменённых жизней схлопывалось в ОДНУ цену, поэтому старая метрика **занижала** живость на
глубине (bid `[3000,6000)`: 0.622 → 0.873). Дефект метрики был реален — величина зависела от
длины окна и плотности сетки, — но вывод о живости он не завышал. Опровергнутое предсказание —
законный результат; подгонять его задним числом запрещено (`DESIGN.md` §16.3).

`depth_band_provenance: "diff-reconstructed, validated<=1.3%"` (`VB-I-5`) остаётся обязательным
для ВСЕХ полос глубже 1.3%: живость и биржевая правда о notional — разные утверждения
(решение (i) в силе, эталона глубже 1.3% по-прежнему нет).

### ОГРАНИЧЕНИЕ M-33 — founder-флаг слеп к ОДНОСТОРОННЕЙ заморозке

Пре-регистрированный порог M-33 требовал падения `cancel_fraction` >50% от соседней полосы
**ПО ОБЕ стороны** (конъюнкция). Такой флаг по построению не срабатывает при заморозке ОДНОЙ
стороны — а это ровно то, что наблюдается: ask `[800,1500) = 0.247` против bid 0.964, разрыв
почти четырёхкратный, и флаг молчит. Ограничение названо, но НЕ чинится в M-58 (`R-031`
RETRO-NOTE): порог задавался пре-регистрацией, его пересмотр — отдельное решение, а не правка
задним числом.

### Что осталось непроверенным

Один сегмент, один символ (BTCUSDT spot), 3.4 часа — ограничение унаследовано от M-32
(§Risks-5) и пересъёмкой не снято. Мульти-сегментная агрегация по-прежнему требует
resync-восстановления (§Risks-1). Разрежённые ask-полосы (n = 67…184 жизни за 3.4 ч) требуют
БОЛЬШЕГО окна прежде, чем по ним можно выносить суждение о живости.

## 1. Что спрашивали (два вопроса, которые НЕ были решены до M-32)

- **Q1:** есть ли источник глубины ГЛУБЖЕ 1.3% с БИРЖЕВОЙ правдой? (прежнее «паритет с Bookmap/TPP» —
  была НЕПРОВЕРЕННАЯ инференция architect'а).
- **Q2:** достоверны ли наши полосы 3-30% БЕЗ более глубокого эталона — эмпирически (staleness + order-flow)?

## 2. Что установили ФАКТАМИ

### Q1 — эталона глубже 1.3% НЕТ (паритет CONFIRMED)
Обзор первоисточников (Binance API/docs, Tardis, Kaiko, Amberdata, Bookmap/dxFeed, OKX, TensorCharts):
**достижимого эталона глубже ~1.3% от mid не существует ни у биржи, ни у одного опрошенного вендора.**
Все упираются в тот же потолок: REST-snapshot предельного размера (**5000 spot / 1000 futures**) +
инкрементальный WS `@depth` diff. Binance сам документирует: уровни за пределами снапшота узнаются
ТОЛЬКО когда меняются (diff). **Инференция «паритет с Bookmap/TPP» → CONFIRMED** (уточнение: паритет по
*глубине-через-diff*; глубокие полосы = diff-реконструкция ДЛЯ ВСЕХ в классе, включая Bookmap/TPP).
⇒ **Q2(в) cross-source recon = N/A** (сверять не против чего).

### Q2 — измеритель провалидирован: дальние полосы ЖИВЫЕ, поток ВЕРЕН
Прямой замер на СЫРОМ L2Delta (BTCUSDT, gap-free **segment 78**, 3.4 ч, 121k дельт, 291k сделок):

| метрика | NEAR (≤1.3%, эталон-зона) | FAR (3-30%, TPP-полосы) | смысл |
|---|---|---|---|
| **cancel_fraction** (доля уровней с явным `size=0` от биржи) | **0.981** | **0.805** | дальние уровни РЕАЛЬНО отменяются биржей в 80.5% случаев — ЖИВЫЕ, не «зависают вечно» (не фантом TD-016) |
| **frozen** (жив на конце окна, не отменён) | ~2% | ~19.5% | не доказанный фантом: длинный хвост крупных лимитов + рождённые к концу окна |
| **order-flow consistency_rate** | **0.950** (обе стороны) | — | 95% сделок находят соответствующий book-decrement в потоке ⇒ diff-поток ВЕРНО отражает исполненный flow |
| **gaps / censored** (segment 78) | **0 / 0** (обе стороны) | — | внутри сегмента непрерывность чейна `U/u/pu` идеальна ⇒ resync не конфаундит (то, что shell-notional depth_probe развести НЕ мог) |

**Ключ:** это то, что depth_probe (shell-notional над снапшотами) сделать НЕ мог — он путал resync-обнуление
с churn. Здесь измеряются СОБЫТИЯ ОТМЕН напрямую, а sequence-gap де-конфаундит (DV-I-3, censored).
**cancel_fraction FAR = 0.805 ≈ того же порядка, что NEAR = 0.981** ⇒ дальние полосы — живая ликвидность,
которую биржа активно ведёт, а НЕ мёртвые фантомные уровни.

## 3. ВЕРДИКТ — три решения на founder-подпись

Формат `{what, from, to, rationale, report_ref}` (`docs/03-integration-contract.md` §3). Ни один агент
НЕ подставляет авто-approve — решения принимает founder.

### Решение (i) — достижим ли эталон глубже 1.3%?
- **what:** источник биржевой глубины глубже ~1.3% от mid для TPP-полос 3-30%.
- **from:** открытый вопрос («вендор y/n»).
- **to:** **НЕТ / не покупать** — эталона глубже 1.3% не существует ни у биржи, ни у вендоров.
- **rationale:** Q1 паритет CONFIRMED; все источники = 5000/1000-cap + WS diff. Вендор глубину НЕ решает.
- **report_ref:** `depth-sources-survey.md` §1-§8.

### Решение (ii) — достоверны ли полосы 3-30% по staleness + order-flow?
- **what:** доверять ли значениям TPP-полос 3-30%, посчитанным на нашей diff-книге.
- **from:** «НЕДОСТОВЕРНЫ (TD-016 фантом-подозрение)».
- **to:** **ДОСТОВЕРНЫ как diff-реконструкция** (с caveat) — дальние уровни живые (cancel_fraction FAR=0.805),
  поток верен (consistency=0.950), sequence непрерывен (gaps=0). Фантом-подозрение TD-016 ЭМПИРИЧЕСКИ снято
  для дальних полос. Caveat: ~19.5% FAR frozen НЕ доказаны фантомом, но и не исключены полностью (длинный
  хвост / оконное усечение) — это НЕ биржевой факт, а diff-реконструкция.
- **rationale:** измеритель провалидирован (Q2); staleness+order-flow дают положительный вердикт на gap-free эталоне.
- **report_ref:** `depth-lifetime-results.md` (АВТОРИТЕТНЫЙ ЭТАЛОН, segment 78).

### Решение (iii) — TPP на реальном эталоне ИЛИ diff-provenance? → ✅ APPROVED (founder)
- **what:** на чём строить TPP Bid/Ask/Delta полосы **1.5–60%** (founder-диапазон; полный набор 1.5/3/5/8/15/30/60).
- **from:** не решено (блокер TD-016).
- **to:** **на нашей diff-книге, честно помечая `depth_band_provenance: "diff-reconstructed, validated<=1.3%"`**
  (VB-I-5). Эталона глубже нет (реш. i), значит diff-реконструкция — ЕДИНСТВЕННО достижимое, и она
  провалидирована как «живая» для **1.5–30%** (реш. ii). Фронт/AI НЕ выдают эти полосы за биржевой факт.
  **30–60% — live-verified РАЗРЕЖЕННОЙ (M-33: bid cancel_fraction=0.622); provenance + caveat разрежённости.**
- **rationale:** осознанный выбор (не случайный): нет эталона + измеритель провалидирован ⇒ diff-provenance
  с честным caveat — то же, что делают Bookmap/TPP, но с явной маркировкой.
- **report_ref:** `docs/fa/viz-backend.md` §4 (VB-I-5), `depth-verdict.md` (этот файл).

## 4. §Risks / limitations (BINDING)

1. **КАСКАДНЫЕ GAP'Ы — мульти-сегментный прогон невалиден (находка tester §E п.2).** Прогон по 3 сегментам
   (78+79+80) дал `gaps=201908` и `cancel_fraction=1.000` — **АРТЕФАКТ fail-closed**, НЕ «всё отменяется»:
   после ПЕРВОГО sequence-gap на границе сегментов анализатор (DV-I-3, как `book::apply_l2delta`) блокирует
   running-state → КАЖДЫЙ последующий contiguous-тик = gap (каскад). **Вердикт опирается ТОЛЬКО на gap-free
   segment 78.** Мульти-сегментная агрегация требует resync-восстановления между сегментами — follow-up,
   НЕ блокирует этот вердикт.
2. **Валидировано ≤1.3% против биржи невозможно (реш. i).** cancel_fraction/order-flow — НЕОБХОДИМЫЕ, не
   достаточные признаки: они доказывают, что уровни живут-и-отменяются и поток верен, но НЕ сверяют notional
   дальних полос с биржевой правдой (её глубже 1.3% нет). Отсюда caveat provenance (реш. iii).
3. **ask-сторона дальних полос статистически разрежена** (сотни уровней за 3.4 ч vs тысячи на bid) —
   cancel_fraction ask FAR шумнее; для TPP bid-сторона устойчивее. Не блокер, но учитывать в интерпретации.
4. **~19.5% FAR frozen** — не доказаны фантомом; длинный хвост крупных лимитов / рождение к концу окна.
   Наблюдать на большем окне (single-segment) при желании ужесточить.
5. **Один сегмент / один символ / один период.** Вердикт — по BTCUSDT spot, 3.4 ч. Расширение на ETH/futures/
   другие окна — при масштабировании TPP (не блокер MVP, но провенанс обязателен ВЕЗДЕ).

## 5. Что дальше (follow-up, вне этого вердикта)
- **✅ Верификация 30–60% ВЫПОЛНЕНА (M-33, `depth-band-3060`):** полоса `[3000,6000)` добавлена в анализатор,
  переснята на segment 78 → **ЖИВАЯ-РАЗРЕЖЕННАЯ** (bid cancel_fraction=0.622 n=156, ask=0.366 n=41; drop от
  [1500,3000) 21.9%/33.5% <50% ⇒ не замёрзшая). 30–60% → live-verified с provenance + caveat разрежённости.
  Числа: `depth-lifetime-results.md` §M-33.
- **TPP-построение (разблокировано founder-решением):** отдельный milestone — Bid/Ask/Delta по полосам
  1.5/3/5/8/15/30/60 на diff-книге с `depth_band_provenance` (VB-I-5) на КАЖДОЙ серии глубже 1.3%.
- **M-31** (эвикция/TD-016) — ортогональная корректность поддержки книги (bounded + dead-near recon ≤1.3%).
- **Follow-up:** resync-восстановление в lifetime-анализаторе для мульти-сегментной агрегации (§Risks-1).

**Статус гейта:** ✅ **APPROVED founder'ом (2026-07-24)** — diff-реконструкция с provenance, диапазон 1.5–60%.
1.5–30% и 30–60% — живо-верифицированы (M-32 + M-33); все полосы 1.5–60% готовы к TPP с provenance
(30–60% — с caveat разрежённости).
