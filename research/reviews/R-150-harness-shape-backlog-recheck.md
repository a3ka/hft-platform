<!-- GATE-META
milestone: П-024
audited_repo: a3ka/hft-platform
audited_base: adbf6ef67ffb8343eaa773becb975a67f87d7343
audited_head: 933accdf7b5c3e02b9a10236dc4432b16d317a1f
verdict: REJECT
-->

# R-150 — перепроверка §9: сворачивание барьера формы milestone-спеки (`П-024` исполнено)

Независимый Fable-агент со свежим контекстом (`gates.md` §9). Предмет — ветка
`docs/harness-shape-to-backlog`, один коммит `933accd` над базой `adbf6ef` (merge PR #118):
`milestones/BACKLOG.md` (+12/−1), `docs/PENDING-SIGNATURE.md` (+15), шесть вердиктов
`C-173`…`C-179` + `README.md` в `docs/archive/abandoned-gate-verdicts-2026-08-29/`.
Проверено на ДЕРЕВЕ СЛИЯНИЯ: `origin/main` за время жизни ветки уехал на PR #119
(`37a6358`, влит 2026-08-29T16:13:17Z — через ДВЕ минуты после коммита предмета), и именно
это дало главный блокер. Я НЕ пересматриваю решение founder'а остановить предмет — граница C;
предмет перепроверки: точность записи, полномочия, правдивость утверждений о коде.

**Вердикт: REJECT** — три блокера, все дешёвые в снятии; конструкция правки (архив, сохранение
ветки, оформление границы C) при этом верна и подтверждена исполнением.

---

## Блокеры

### Б-1 — «у ТРЁХ барьеров ветка события не проверена пробой» опровергнуто на дереве слияния: дефект у ОДНОГО, и мера была неверной

- Где: `docs/archive/abandoned-gate-verdicts-2026-08-29/README.md` §«Что из восьми кругов
  оставлено себе» п.2 (таблица пяти проб + «Три барьера… имеют непроверенную ветку события;
  последний зовёт барьер позиционной базой… то есть проводка события не исполняется вовсе»);
  `docs/PENDING-SIGNATURE.md` — абзац «Ценность круга 8 забрана» под `П-024` («у ТРЁХ из
  них… ветка события не проверена пробой вовсе»); тот же текст в теле коммита `933accd`.
- Факт на дереве слияния: `TECH-DEBT.md` в `origin/main` несёт `TD-181` (влита PR #119,
  «замеры перепроверены reviewer'ом»), которая ровно эту гипотезу **перепроверила
  мутационным ИСПОЛНЕНИЕМ и исправила**: «дефект у ОДНОГО барьера из трёх заявленных, и не
  в той ветке события». `red_docs_freeze` пиннит ОБЕ ветки (мутант `pull_request` →
  `VERDICT: FAIL (1)`, мутант `push` → `FAIL (4)`); `red_gate_meta` пиннит `pull_request`
  сценарием `GM-27`; не запиннена только ветка `pull_request` у
  `red_protected_artifacts` (33 вызова, `push`×32 + пустое событие ×1, `pull_request` — 0).
  Отдельно опровергнуто «проводка события не исполняется вовсе»: ни один из 33 вызовов
  позиционного аргумента не передаёт, `case`-блок исполняется на каждом.
- Почему замер разошёлся: `grep -c 'EVENT_NAME=push'` считает ФОРМУ ЗАПИСИ, а пробы вызов
  ПАРАМЕТРИЗУЮТ (`EVENT_NAME="$2"`). Класс — `testing.md` «оракул обязан мерить ТО, ЧТО
  ОБЕЩАЕТ»; сам README тремя абзацами ниже проповедует «проверка по ВЫЗОВУ, а не по тексту».
  Таблица счёта при этом воспроизводится точно (см. «Что подтверждено» п.6) — ложен не счёт,
  а ВЫВОД из него.
- Воспроизведение:
  ```
  $ git show origin/main:TECH-DEBT.md | sed -n '1710,1760p'   # TD-181 целиком
  $ for p in red_artifact_ids red_review_fa red_docs_freeze red_gate_meta red_protected_artifacts; do
      git show origin/main:scripts/tests/$p.sh | grep -c 'EVENT_NAME=push'; done
    → 2 1 0 2 0   # счёт воспроизводится — и TD-181 показывает, что он мерил не то
  ```
- Смягчающее названо честно: PR #119 влит в 16:13:17Z, коммит предмета — 16:11:21Z; автор
  физически не мог видеть карточку. Но `gates.md` §8 заведён ровно про это: «документ,
  правдивый на ветке, оказывается ложью после merge; расхождение прогонов — блокер, не
  примечание». «Заведено карточкой долга reviewer'у» — правда (`TD-181` существует), но
  карточка гипотезу ИСПРАВИЛА, а предмет подаёт гипотезу как установленный замер.
- Условие снятия: переписать п.2 README и абзац под `П-024` по факту `TD-181` (один барьер,
  ветка `pull_request`, мера grep-по-литералу названа ошибочной с указанием, чем заменена);
  тело коммита не переписывается — след поправки остаётся в тексте документов.

### Б-2 — колонка «круг» в таблице README противоречит заголовкам самих вердиктов, именам файлов и собственной сноске README

- Где: `docs/archive/abandoned-gate-verdicts-2026-08-29/README.md` §«Состав — восемь кругов,
  все REJECT», строки таблицы `C-173`…`C-178`.
- Таблица говорит: `C-173`=круг 3, `C-175`=4, `C-176`=5, `C-177`=6, `C-178`=7. Сами вердикты
  (побайтные копии — проверено, см. ниже) самоопределяются на единицу меньше:
  ```
  $ grep -h '^# C-1' docs/archive/abandoned-gate-verdicts-2026-08-29/C-17*.md
  # C-173 — REJECT: … round 2
  # C-175 — REJECT: … round 3
  # C-176 — REJECT: … round 4
  # C-177 — REJECT: … round 5
  # C-178 — REJECT: … round 6
  # C-179 — REJECT: … round 8
  ```
  С этим согласны: суффиксы имён файлов (`-r2`…`-r6`, `-r8`); сноска ТОГО ЖЕ README двумя
  строками ниже таблицы («Круг 2 (`C-172`) переномерован в `C-173`» — таблица же зовёт
  `C-173` кругом 3); `П-024` («круг 6 — невыписанная группа» = `C-178`; «круг 7 — дефект,
  найденный вообще не адверсарием, а первой живой спекой» — таблица отдаёт круг 7
  адверсарному `C-178`); и `C-179:31` («Round 7 genuinely fixes the `grep -q`/pipefail
  defect»). Пять строк из семи сдвинуты на единицу; кругу 7 вердикта НЕ существует.
- Условие снятия: исправить колонку (`C-173`=2, `C-175`=3, `C-176`=4, `C-177`=5, `C-178`=6,
  `C-179`=8) и добавить строку круга 7 без артефакта (SIGPIPE, найден живой спекой, фикс —
  коммит «fix(harness): M-72 …» на ветке) — иначе архивная запись, единственное назначение
  которой точность, лжёт о том, какой круг какой.

### Б-3 — «781 строка (422 исполняемых)» — счёт круга 6, выданный за счёт финала из восьми кругов

- Где: README («Счёт против основания: **781 строка** в двух артефактах (422 исполняемых),
  восемь кругов гейта, 95 сценариев, 42 мутации, ~90 с CI»); `milestones/BACKLOG.md` новая
  строка («Счёт: 781 строка (422 исполняемых)»).
- Замер по ревизиям ветки (`wc -l` + `grep -cvE '^[[:space:]]*(#|$)'` на паре
  `scripts/check_milestone_shape.sh` + `scripts/tests/red_milestone_shape.sh`):
  ```
  679b114 (суд r2): 448/273   9ec104f (r3): 560/335   d0b9abb (r4): 601/358
  107e5b0 (r5): 703/412       cda4600 (r6): 781/422
  dee67f1 (суд r8) и вершина ветки dcab8fb: 910/467
  ```
  781/422 — состояние, которое судил круг 6. Финал после восьми кругов — **910/467**. При
  этом 95 сценариев и 42 мутации — числа ФИНАЛА (подтверждены исполнением, см. ниже): одно
  предложение смешивает замеры двух ревизий. `П-024` (в базе, вне предмета) честно называл
  781/422 при «семи кругах»; README перенёс число под «восемь кругов» не перемерив. Тот же
  класс, что «строка протухла на ШЕСТЬ кругов», который правка сама чинит, и тот же
  прецедент, что `R-137` Б-1: истинное число (910 > 781) довод УСИЛИВАЕТ, но число, поданное
  как замер, обязано быть замером.
- Смежное, той же строкой: «~90 с CI на каждый push» не воспроизводимо названной средой —
  джоб `milestone-shape` в проверенных ранах ветки не исполнялся (оба рана несут
  единственный джоб «fmt + clippy + test (ветка)»); локальный прогон одной пробы — 153 с
  (`/usr/bin/time`: `probe-wall=153.38s`). `П-024` называл ~70 с. Провенанс числа не назван.
- Условие снятия: в README и BACKLOG — 910/467 (или 781/422 с явной пометкой «на круге 6»);
  цифру CI либо подкрепить командой (длительность шага в реальном ране), либо назвать оценкой.

## Точностные находки (в блокер не тянут)

- **Н-1.** «Снос освободил бы `C-173`…`C-179`» (README, BACKLOG, тело коммита). Исполнение:
  без ветки аллокатор даёт `C-175` — `C-174` держит `origin/docs/ops-restart-alert`
  (`C-174-ops-restart-alert.md`), а разрывы аллокатор не занимает (`gates.md` §12).
  Освобождаются `C-175`…`C-179` (пять номеров), не семь. МЕХАНИЗМ и решение держать ветку
  при этом верны — коллизия при сносе реальна (см. «Что подтверждено» п.2).
- **Н-2.** «ВОСЕМЬ КРУГОВ, ВСЕ REJECT» (BACKLOG; README «восемь кругов, все REJECT»).
  REJECT-вердиктов СЕМЬ (`C-101` + шесть); круг 7 вердикта не имеет — его дефект нашла живая
  спека, не адверсарий (`П-024`, подтверждено `C-179:31`). Кругов восемь, REJECT'ов семь.
- **Н-3.** Комментарий CI ветки (`ci.yml`, у шага самопроверки) говорит «батареи из 36
  ослаблений» при имени шага и факте 42 — протухший комментарий на ветке, не в предмете;
  называю, потому что предмет ссылается на батарею числом.

## Что подтверждено — исполнением, не чтением

1. **Merge-preview:** `bash scripts/verify_design_claims.sh --merge-preview origin/main`
   → `VERDICT: PASS (0 нарушений)`, `verify-exit=0` (exit снят отдельным прогоном, не от `tail`).
2. **Центральное утверждение об аллокаторе — ВЕРНО, предъявлено исполнением.**
   `next_artifact_id.sh` строит занятость по `refs/remotes/origin` ∪ `refs/heads` (`:27`);
   класс `C` читает ТОЛЬКО `research/critiques/` (`:93`), класс `M` — `milestones|docs/archive`
   (`:91`); скрипт между `adbf6ef` и `origin/main` не менялся (`git diff … | wc -l` → 0).
   Исполнение в клоне с восстановленной вселенной рефов: с веткой → `C-180`; после удаления
   `refs/remotes/origin/feat/harness-milestone-shape` + локальной ветки → `C-175`. Снос =
   повторная выдача занятых номеров; основание решения НЕ УДАЛЯТЬ ветку стоит (диапазон — Н-1).
3. **Побайтность копий:** `git show origin/feat/harness-milestone-shape:research/critiques/<f> | cmp -s - <копия>`
   для всех шести → `IDENTICAL`, `byte-diff-fails=0`.
4. **`C-101`** в `docs/archive/abandoned-gate-verdicts-2026-08-24/` — `verdict: REJECT` ✓.
   **`C-172`:** файла нет ни на одном ref'е (перебор `for-each-ref` × `ls-tree` по полной
   вселенной рефов — пусто); след переномерации — тело `767ebae`
   («renumber C-172 verdict C-173 after collision», `ALLOW-ARTIFACT-DELETE`) ✓.
5. **95 сценариев / 42 мутации — ИСПОЛНЕНИЕМ:** чистый чекаут вершины ветки,
   `bash scripts/tests/red_milestone_shape.sh` → «батарея ослаблений: поймано 42 из 42
   (ожидалось 42)», «PASS=95 FAIL=0 (сценариев: 95)», `VERDICT: PASS`, `probe-exit=0`,
   остаточных песочниц 0.
6. **Таблица пяти проб воспроизводится** (`grep -c 'EVENT_NAME=push/pull_request'` на
   `origin/main`): `2/1, 1/1, 0/1, 2/0, 0/0` — как в README; `check_protected_artifacts.sh:47`
   = `raw="${1:-}"` ✓. Ложен вывод из счёта, не счёт (Б-1).
7. **«Проба ни разу не исполняла форму `push`» — о пробе ПРЕДМЕТА верно и НЕ грепом:**
   `run_barrier` в `red_milestone_shape.sh` (`dee67f1:301`) зашивает
   `EVENT_NAME=pull_request` литералом; все прочие вызовы барьера в пробе — тоже (`:443`,
   `:446`, `:471`). `C-179` B-14 подтверждён по вызову.
8. **«Строка протухла на ШЕСТЬ кругов»:** после прежней редакции («круг 2 СДЕЛАН… 24/24»)
   легло шесть адверсарных вердиктов `C-173`…`C-179` ✓.
9. **Полномочия:** `git show --numstat 933accd` — ровно девять заявленных файлов; зона §11
   (`.claude/**`, `CLAUDE.md`, `docs/04-workflow.md`), `PROJECT-STATE.md`, `TECH-DEBT.md`,
   `crates/**`, `scripts/**` — не тронуты; `FOUNDER-APPROVED` не требуется. Запись под
   `П-024` оформлена как ЗАПИСЬ подтверждения founder'а («Founder подтвердил исполнение»)
   при правиле, подписанном ДО круга 8, — граница C соблюдена.
10. **Архив не числится гейт-классом:** прод-форма
    `EVENT_NAME=push PUSH_BEFORE=adbf6ef… bash scripts/check_gate_meta.sh` на диапазоне →
    `VERDICT: PASS — вердиктов проверено: 0`, `exit=0`. Шесть архивных шапок CI не роняют.
11. **Связность:** `docs/workflow/oracle-blindness-class-2026-08-28.md` §5 несёт `Р-3`, §6 —
    `М-3` (`М-1/М-2/М-3` «спроектированы, а не построены» — совпадает с «не изображается
    сделанным»); сосед `abandoned-gate-verdicts-2026-08-24/README.md` непротиворечив (та же
    логика «предмет не в `main` — архив, а не воскрешение»); новая строка BACKLOG не
    противоречит приписке «Ветки и спас-рефы НЕ удалять» — усиливает её второй причиной.

## Done Block

```
$ bash scripts/verify_design_claims.sh --merge-preview origin/main >/dev/null 2>&1; echo exit=$?
exit=0
$ bash scripts/next_artifact_id.sh C            # реальный репозиторий, ветка жива
C-180  exit=0
$ bash scripts/next_artifact_id.sh C            # клон, вселенная рефов восстановлена, ветка удалена
C-175  exit=0
$ cmp шести копий против git show origin/feat/harness-milestone-shape:…  → byte-diff-fails=0
$ bash scripts/tests/red_milestone_shape.sh     # чекаут вершины ветки
… батарея ослаблений: поймано 42 из 42 (ожидалось 42)
PASS=95 FAIL=0 (сценариев: 95)
VERDICT: PASS   probe-exit=0   (стена: 153.38 s)
$ EVENT_NAME=push PUSH_BEFORE=adbf6ef67ffb8343eaa773becb975a67f87d7343 bash scripts/check_gate_meta.sh; echo exit=$?
VERDICT: PASS — вердиктов проверено: 0, до-нормативных приземлений: 0, merge'ей с milestone в subject'е: 0
exit=0
$ for rev in cda4600 dee67f1; do … wc -l / grep -cvE …; done
cda4600: total=781 exec=422        # круг 6 — число из README
dee67f1: total=910 exec=467        # финал, который судил круг 8
$ git show origin/main:TECH-DEBT.md | grep -n 'TD-181' | head -1
1711:- **TD-181** `ветка-pull_request-барьера-protected-artifacts-не-запиннена-…`
```

## Итог

Конструкция правки здорова: побайтный архив, сохранение ветки-носителя номеров (механизм
подтверждён исполнением), корректное оформление границы C, барьеры CI предмет держат. Ложны
три ЧИСЛА/ВЫВОДА в документах, чьё единственное назначение — точная запись: перенесённый на
живые барьеры «замер» опровергнут перепроверкой reviewer'а уже лежащей в `main` (Б-1),
колонка кругов сдвинута на единицу против самих вердиктов (Б-2), счёт строк взят с ревизии
двумя кругами раньше финала (Б-3). Все три снимаются правкой двух файлов предмета; повторная
перепроверка после снятия — по этому же файлу, круг 2.
