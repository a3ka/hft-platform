<!-- GATE-META
milestone: P-020
audited_repo: a3ka/hft-platform
audited_base: 1a91fe5a28eff43c47091a583af5d6025643e7f2
audited_head: bd2042863de7835a3c08e37c0978709288aedcd6
verdict: REJECT
-->

# R-132 — перепроверка §9: запись `П-020` (предел ответа gateway 2 000 000 Б)

**Вердикт: REJECT.** Величина 2 000 000 Б и сама подпись founder'а сомнению НЕ подвергаются
— граница C соблюдена, предмет подписи не расширен. REJECT — о ТЕКСТЕ записи: два ложных
фактических утверждения (Б-1, Б-2) сидят внутри абзаца, обличающего ложные утверждения, —
ровно класс `R-114` Б-1 (ложное утверждение о состоянии в записи решения founder'а), за
который тот вердикт уже выносил REJECT. Плюс четыре точностных находки (Н-3…Н-6).

**База аудита:** `audited_base` — вершина `origin/docs/founder-decisions-2026-08-26`
(`1a91fe5`), НЕ `main`: ветка предмета стековая, PR #101 отсужен `R-131` и здесь не
пересуживается. Предмет — ровно один коммит `bd20428` (`docs/PENDING-SIGNATURE.md` +71/−0).

Перепроверяющий — независимый Fable-агент со свежим контекстом; автор записи — сторона.

---

## Б-1 (блокер) — «литерал `2000000` впервые появляется в коммитах самого дева» — ЛОЖНО

`docs/PENDING-SIGNATURE.md:1312-1314` (дерево `bd20428`): «литерал `2000000`
впервые появляется в коммитах самого дева» (контекст называет `3419177`, `4b9dfab`).

Воспроизведение:

```
$ git log --all --format='%h %ai %s' -G'(^|[^0-9])2000000([^0-9]|$)' -- crates/ | sort -k2 | head -2
5fe8c58 2026-08-25 19:40:30 +0000 test(PL-I-5): C-157 R1 — ручка предела переименована в байты: GATEWAY_MAX_RESPONSE_BYTES [architect]
$ git show 5fe8c58 | grep -n '2000000' | head -3
35:+    assert_startup_rejected("2000000bytes", "число с суффиксом");
49:+    assert_startup_rejected("2000000.0", "дробное значение не парсится");
58:+    for v in ["1", "2000000", "100000000"]
```

Литерал `2000000` как значение ЭТОГО предела первым внёс сам architect — RED-коммитом
`5fe8c58` (2026-08-25 19:40, `crates/gateway-serve/tests/red_egress_cap_startup.rs`), на
СУТКИ раньше дева (`3419177`, 2026-08-26 14:47). Вне предметного контекста литерал ещё
старше (`f55b6cc`, 2026-07-10, фикстура latency: строки `+    2000000,`).

Существо абзаца НЕ рушится: подписи на момент `3419177` действительно не существовало
(см. §Хронология). Но довод «число выдумал дев» ложен — дев взял литерал из sacred-теста
architect'а и из §5.1 спеки («Предложение: 2 МБ»). Ложная опора в абзаце про ложную
атрибуцию — самоопровержение записи, и оставлять её нельзя.

## Б-2 (блокер) — «каталога `research/decisions/` не существует» — ложно как абсолют

```
$ for r in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin | grep -v HEAD); do
    out=$(git ls-tree "$r" research/decisions/ 2>/dev/null); [ -n "$out" ] && echo "$r: $out"; done
origin/feat/M-10-rebased: 100644 blob b546774 research/decisions/D-001-obi-trackA-kill.md
$ git grep -l 2000000 origin/feat/M-10-rebased -- research/decisions; echo exit=$?
exit=1
```

Каталог СУЩЕСТВУЕТ на `origin/feat/M-10-rebased` и несёт один несвязанный файл (`D-001`,
OBI kill; литерала `2000000` в нём нет — существо утверждения выживает). Запись, чьим
предметом является точность утверждений о состоянии, обязана квалифицировать квантор:
«ни в `main`, ни на живых ветках; единственное исключение — несвязанный `D-001` на
архивной `feat/M-10-rebased`».

## Н-3 — атрибуция чисел `425` и `29 454` к `A-022` неверна

```
$ git show origin/feat/M-71-egress-cap:research/arbitration/A-022-m71-oracle-judges-its-own-construction.md | grep -c 425
0
$ ... | grep -c '29 454'
0
```

Таблица «байт с сокета» (`425` / `29 454` / `2 804 778` / `2 100 084`) живёт в
`milestones/M-71-egress-cap.md` §0quater («Замеры с сокета (мои, этим прогоном)») на ветке
`feat/M-71-egress-cap`. В самом `A-022` из четырёх чисел есть только `2 804 778` и
`2 100 084` (строки 36, 148, 183). Три строки шкалы `П-020` ссылаются «(`A-022`)» на числа,
которых в `A-022` нет, — нарушение правила цитаты. Правка: источник строк `425`/`29 454` —
`M-71` §0quater. Семантика разметки при этом ВЕРНА: контроли/дыры расставлены как в
первоисточнике.

## Н-4 — «наименьшее круглое значение, зажатое замерами с ДВУХ сторон» — переоценка

`1 000 000` ТОЖЕ зажато замерами с двух сторон: пропускает 854 810 (запас 17.0 %,
пересчитано: 1 000 000/854 810 = 1.170) и отвергает обе дыры. Его исключает не замер, а
СУЖДЕНИЕ о достаточности запаса («сломает при первом уплотнении») — суждение законное, но
оно суждение. Честная форма: предел выбран внутри интервала (854 810, 2 100 084);
2 000 000 — его круглая точка с запасом 134 % к легитимному потолку; 1 000 000 отвергнут
по критерию запаса. Замер даёт интервал; точку в нём выбрал человек — и именно поэтому
она founder-подпись, а не следствие арифметики. Нынешняя формулировка выдаёт выбор за вывод.

## Н-5 — указатель «§16.2 шаги 3, 5-7» расходится с именованным списком

Именованные механизмы «разделяемая проекция, cursor-resume и сжатие» = шаги 5, 7, 6
таблицы `DESIGN` §16.2. Шаг 3 (byte-offset tail-follower) — CPU steady-state, стену
ПАМЯТИ (98 ГБ) не двигает и в списке не назван. Правка: «(§16.2 шаги 5-7)».

## Н-6 — шкала объявляет одну единицу для двух разных объектов

Шапка шкалы: «байты `serde_json::to_vec` ответа». Это единица уровня 1 (строки §2.2:
62 688 / 854 810 / 45 261 937). Четыре строки §0quater (425 / 29 454 / 2 804 778 /
2 100 084) — байты С СОКЕТА, конверт включён. Смешение единиц в одной таблице — ровно
класс, который `A-022` запретил («каждый оракул НАЗЫВАЕТ свой объект»), и сама запись
опирается на 13-байтовую разницу этих единиц абзацем ниже. Численно пренебрежимо,
терминологически — нет: назвать единицу «байты сообщения, для строк §0quater — с сокета».

---

## Что ПОДТВЕРЖДЕНО (существо записи держится)

1. **Все числа шкалы совпадают с первоисточниками.** §2.2: `62 688` = прод-дефолт
   `[0.001]`; `854 810` = `[0.013]` (граница валидации глубины); `45 261 937` =
   канонический набор. §0quater: `425`/`29 454` (контроли), `2 804 778`/`2 100 084`
   («КРАСНЫЙ — предмет»). Разметка контроли/дыры — по первоисточнику.
2. **Вся арифметика верна** (пересчитано): 4706×, 67.9×, 31.9×, 2.34×, 0.95×, 0.71×,
   0.04×; запасы 134 % и 17 %; превышение 5.004 %; ×722 = 45 261 937/62 688; ×22.6 над
   пределом; 16×2 МБ = 32 МБ; 10 000×2 МБ = 20 ГБ.
3. **«ОДИН объект» — подтверждено.** `M-71` §0: 2 804 765 Б (25 000 сделок, `to_vec`);
   `A-022:36`: `C-158` предъявил «2 804 778 против 2 804 765» — тот же Snapshot, провод
   против локальной сериализации; `A-021:101-102`: «legacy-конверт … — 13–15 Б,
   константа»; 2 804 778 − 2 804 765 = 13 ∈ [13,15]. Вывод «честного трафика выше 855 КБ
   в замерах нет» держится (legacy-путь классифицирован дырой первоисточником).
4. **`DESIGN` §16/§16.1 процитирован верно:** цена сессии ≈9.8 MiB, потолок ≈700 сессий,
   «10 000 × 9.8 MiB ≈ 98 GB» на машине 7.5 GB — дословно (`docs/DESIGN.md:582-586`).
5. **`П-014`:** подпись на ВКЛЮЧЕНИЕ полос дана («включать», 17.08;
   `docs/PENDING-SIGNATURE.md:749`) — «полосы уже подписаны» верно. Следствие «включить
   как есть невозможно» — арифметика ×22.6, держится.
6. **`sub`-id:** `A-021:102-103` — «v1-конверт — 45 Б + `|sub|`, где `sub` — клиентская …
   сегодня не ограничена»; «не ограничен ничем» — верная передача. Маршрут (правка
   `CT-RFC-09` §2.2, отдельная подпись) совпадает с `A-021` («НЕ предписано — назван
   только маршрут»). `docs/rfc/CT-RFC-09-ws-session.md` §2.2 существует в `main`.
7. **Хронология ложной атрибуции — подтверждена доступными артефактами:** `3419177`
   (комментарий «Дефолт — 2 000 000 (founder 2026-08-26, §5.1)») = 14:47:33Z; первый
   артефакт подписи `bd20428` = 16:03:18Z; на 14:47 `docs/PENDING-SIGNATURE.md` кончался
   `П-019` на всех рефах. «Ничем не обеспечено в момент написания» — истинно. Абзац
   Уместен: он защищает целостность ИМЕННО этой записи и корректно маршрутизирует фикс
   (зона engine-dev, пункт reviewer'а на PR-гейте `M-71`) — сведением счётов не является.
   Но его доказательная база несёт Б-1/Б-2 и в нынешней редакции абзац сам нарушает
   норму, которую защищает.
8. **Полномочия и граница C:** зона `docs/PENDING-SIGNATURE.md` — architect; замок §11 не
   задет (зона не пересекается; `check_docs_freeze` exit=0); подписана ВЕЛИЧИНА, запись
   не решает за founder'а ни включение полос (уже `П-014`), ни `CT-RFC-09` (названа
   отдельная подпись), ни `M-70` (названо следствие, не решение).
9. **Барьеры в прод-форме CI** (`EVENT_NAME=pull_request`, `PR_BASE_SHA=1a91fe5`), решение
   по коду возврата: `check_docs_freeze`=0 · `check_protected_artifacts`=0 ·
   `check_artifact_ids`=0 · `check_gate_meta`=0 · `check_review_fa`=0 (SKIP: диапазон не
   трогает `crates/**` — предел барьера, названный M-66) · `check_context_budgets`=0.
   `bash scripts/verify_design_claims.sh --merge-preview origin/main` → exit=0,
   `VERDICT: PASS (0 нарушений)`. Облачный CI не судился: GitHub Actions в момент прогона
   нездоров (очереди, `startup_failure`) — зелёным НЕ объявляю, находкой не считаю.

## Ответы на три вопроса мандата

1. **«Наименьшее круглое» — нечестно в нынешней форме** (Н-4): выбор из интервала выдан
   за вывод из замеров. Находка, правка формулировки обязательна.
2. **Обоснование.** Запись цитирует подпись дословно и мотивировку founder'у прямо не
   приписывает — записывать инженерное основание рядом с подписью законно, ПРИ явной
   атрибуции. Два места её размывают: (а) нет строки «обоснование — architect'а; предмет
   подписи — величина, не мотивировка»; (б) «это следствие принято сознательно» —
   утверждение о сознании founder'а, командой не предъявляемое; опора — handoff §9 спеки
   (следствие для `M-70` названо founder'у до подписи), и запись обязана ссылаться на неё,
   а не утверждать чужое состояние ума. Требование к rev2, не самостоятельный блокер.
3. **Абзац об атрибуции** — уместен и хронологически подтверждён (см. п.7), но обязан
   быть переписан без Б-1/Б-2.

## Условие APPROVED (rev2 записи, один коммит на этой же ветке)

1. Б-1: заменить на верное — литерал первым внёс RED-набор architect'а (`5fe8c58`,
   25.08); довод абзаца — «подписи не существовало ни в одном артефакте на момент
   `3419177`», а не «дев выдумал число».
2. Б-2: квалифицировать квантор (архивная `feat/M-10-rebased` несёт несвязанный `D-001`).
3. Н-3: источник `425`/`29 454` — `M-71` §0quater (ветка `feat/M-71-egress-cap`), не `A-022`.
4. Н-4: «выбран внутри интервала (854 810, 2 100 084); 1 000 000 отвергнут по критерию
   запаса» — вместо «наименьшее круглое, зажатое замерами».
5. Н-5: «шаги 5-7». Н-6: единица строк §0quater — байты с сокета.
6. Вопрос 2: строка об атрибуции обоснования + замена «принято сознательно» на ссылку на
   handoff §9 `M-71`.

Дельта-перепроверка по названным пунктам, не новый полный круг.

FA-примечание: диапазон не трогает `crates/**` — `check_review_fa` даёт SKIP; живой
инвариант FA не требуется, waiver в теле коммита вердикта.

## Done Block

```text
$ git rev-parse origin/docs/founder-decisions-2026-08-26 bd20428
1a91fe5a28eff43c47091a583af5d6025643e7f2
bd2042863de7835a3c08e37c0978709288aedcd6
$ git show bd20428 --stat --format='' | tail -1
 1 file changed, 71 insertions(+)
$ git show origin/feat/M-71-egress-cap:research/arbitration/A-022-*.md | grep -c 425   → 0
$ git log --all -G'(^|[^0-9])2000000([^0-9]|$)' -- crates/ | head → 5fe8c58 (25.08, architect)
$ for s in docs_freeze protected_artifacts artifact_ids gate_meta review_fa context_budgets; do
    EVENT_NAME=pull_request PR_BASE_SHA=1a91fe5 bash scripts/check_$s.sh; echo "$s=$?"; done
  → все exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main; echo exit=$?
  VERDICT: PASS (0 нарушений)
  exit=0
```
