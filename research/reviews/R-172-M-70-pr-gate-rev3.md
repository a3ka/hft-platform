<!-- GATE-META
milestone: M-70
audited_repo: a3ka/hft-platform
audited_base: 547bf24311897155afd9b5bdb98d55ae1a721bc2
audited_head: 7b16eec266e841c439a9f2d15470cfd5134accf1
verdict: REJECT
-->

# R-172 — M-70 (включение полос глубины), PR-гейт круга 3: **REJECTED**

**Роль:** reviewer (`gates.md` §4 — UNCONDITIONAL) · **Дата (UTC):** 2026-09-04T00:40Z
**Предмет:** `547bf24..7b16eec` на `origin/docs/M-70-rev2` — четыре коммита поверх ревизии,
судимой `R-171`.
**Мандат:** отчёт engine-dev'а по задаче 4 в АДДИТИВНОЙ форме (`R-171` Б-1).
**Предыдущие круги:** `R-170` (маршрут), `R-171` (REJECT: `VB-I-4` ломается формой, `VB-I-2`
красен, бамп не чинит).

**Главное одной строкой.** Находка `R-171` Б-1 закрыта верно и предъявлена прогоном —
аддитивная форма работает, `VB-I-4` восстановлен. Но **страж, которым эта форма была
ОПРАВДАНА, свою цену не сторожит**: мутация пути эвикции оставляет весь набор `gateway`
зелёным. Это замер, а не мнение (Б-1 ниже). Плюс гейт по-прежнему красен, и **exit-код в
Done Block'е dev'а не соответствует прогону** (Б-3).

---

## Block-scope — PASS

Разделение зон соблюдено ПОКОММИТНО, проверено, а не предположено:

```
$ for c in 68b7157 57d1f10 a41f571 7b16eec; do echo "--- $c"; git show --numstat --format='' $c | awk '{print "   ",$3}'; done
--- 68b7157 [reviewer]   research/reviews/R-171-M-70-pr-gate-rev2.md
--- 57d1f10 [architect]  milestones/M-70-depth-bands-enablement.md
                         scripts/verify_M-70.sh
--- a41f571 [architect]  crates/gateway/tests/{red_depth_cadence,red_depth_from_book,
                         red_depth_label_dictionary,red_depth_point_provenance,
                         red_depth_provenance_by_reach,red_depth_semantics,
                         red_gateway_bounded,red_gateway_export_v2}.rs
                         scripts/verify_M-70.sh
--- 7b16eec [engine-dev] crates/gateway/src/lib.rs
```

Коммит engine-dev'а трогает РОВНО `crates/gateway/src/lib.rs` — ни одного файла `*/tests/**`.
**RED-first не нарушен:** sacred-оракулы правил только `[architect]`. Атомарность соблюдена,
каждый коммит называет свою задачу. `Allowed paths` §2 милестоуна не превышены.

**Отдельно засчитан `SCOPE VIOLATION REQUEST` dev'а.** Спека `57d1f10` упразднила
`DepthPoint`, а гейт (`verify_M-70.sh:213,233`) продолжал требовать `^pub struct DepthPoint`.
Dev остановился вместо того, чтобы править sacred-гейт, — это ровно тот инструмент, который
`scope-guard.md` предписывает, и он стоил одного круга вместо переделки набора.

## Block-C — N/A, проверено командой

```
$ git diff --name-only 547bf24..7b16eec -- crates/contracts docs/rfc | wc -l
0
```

T1 не тронут ⇒ contract-RFC не требуется. Форма кокпита — T-designate
(`05-contract-layer.md` §2).

## Block-risk — N/A

```
$ git diff --name-only aacab22..7b16eec -- crates/risk crates/killswitch crates/oms 'crates/venue-*' | wc -l
0
```

`gateway` — read-only консюмер журнала (`VB-I-3`), order-egress отсутствует как класс.
`risk-critic` в цепочке не требуется (`gates.md` §5).

## Block-DoneBlock — ФОРМА сырая, ОДНО число ложно (см. Б-3)

Все тестовые прогоны отчёта воспроизведены и сошлись поимённо. Не сошёлся exit-код гейта.

---

# Находки

## Б-1 — БЛОКЕР. `DB-I-4c` НЕ СТОРОЖИТ путь, который называет своей добычей (зона: architect)

`DB-I-4c` — не рядовой оракул: **им оправдана вся форма rev8**. Спека §2bis.-1: «рассинхрон
длин СТАНОВИТСЯ ВЫРАЗИМЫМ… Поэтому он получает собственный страж… Инвариант, защищённый
оракулом, лучше инварианта, защищённого формой ценой другого инварианта». Док-комментарий
самого оракула (`red_depth_point_provenance.rs:284-286`) называет добычу дословно:

> «реализацию, наполняющую два массива ПОРОЗНЬ — например пишущую точку и забывающую метку,
> **или обрезающую один массив эвикцией окна, а второй нет**»

**Замер (мутационный контроль, `testing.md` §«Мутационный контроль»). Мутация 1 — эвикция
режет ТОЛЬКО `series`:**

```
$ # crates/gateway/src/lib.rs, evict_series_bundle_under_window (:2189-2199) заменено на:
$ #     row.series.retain(|pt| pt.0 >= lo_time_s);       // series_provenance НЕ тронут
$ cargo test -p gateway --no-fail-fast 2>&1 | grep -E "^test .* FAILED"
test gw_i_4_holds_when_the_tail_frame_is_delta_only ... FAILED
```

**Ровно один красный — и он ВНЕШНИЙ, тот же, что и без мутации.** `db_i_4c_lengths_match_on_every_row`
ЗЕЛЁН против реализации, которая на каждой оконной эвикции разъезжает длины массивов.

**Мутация 2 — merge-путь не обновляет метки** (контроль охвата: он ПОКРЫТ):

```
$ # Snapshot::apply: current.series = merged_series; метки оставлены прежними
$ cargo test -p gateway --no-fail-fast 2>&1 | grep -E "^test .* FAILED"
test gw_i_4_holds_when_the_tail_frame_is_delta_only ... FAILED
test provenance_survives_merge_when_reach_changes ... FAILED
test mid_stream_snapshot_completeness_merges_same_bucket ... FAILED
```

**Мутация 3 — позитивный контроль, `finish_ref` пишет метку не для каждой точки:**

```
$ # if i > 0 { series_provenance.push(...) }
$ cargo test -p gateway --no-fail-fast --test red_depth_point_provenance --test red_gateway_export_v2
test db_i_4_two_points_of_one_row_carry_their_own_provenance ... FAILED
test db_i_4c_lengths_match_on_every_row ... FAILED
test deep_band_carries_provenance ... FAILED
```

**Что это значит.** Оракул НЕ вакуумен — на пути `finish_ref` он краснеет как обещано
(мутация 3). Он УЗОК: фикстура `db_i_4c` строит снимок одним вызовом
`gateway::snapshot(dir, …, Cursor::LATEST)` при `Selector.window_ms = None`
(`red_depth_point_provenance.rs:121,135-138`) — то есть **не исполняет ни оконную эвикцию, ни
merge**. Оба массива в `finish_ref` наполняются одним циклом, поэтому на этом пути их длины
равны ПО ПОСТРОЕНИЮ: оракул проверяет свойство там, где оно не может быть нарушено, и молчит
там, где может.

Класс назван `testing.md` §«Оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ»: страж, чей собственный
док-комментарий обещает ловить эвикцию, эвикцию не исполняет. И `VB-I-10` (bounded-window)
делает этот путь не гипотетическим — прод работает именно с `window_ms = Some(W)`.

**Почему БЛОКЕР, а не NOTE.** Цена аддитивной формы была принята под обещание, что её
сторожит оракул. Обещание не исполнено на половине путей. Merge, состоявшийся сейчас, ввёл бы
в `main` форму, чей единственный новый инвариант держится на конструкции реализации, а не на
проверке, — при том что ровно эту опору (`форма делает рассинхрон невыразимым`) `R-171` Б-1
уже отверг как купленную ценой другого инварианта.

**Зона — architect** (`*/tests/**` sacred). Reviewer описывает дефект и не проектирует фикс
(`gates.md` §4, граница reviewer↔architect); замер выше называет, ЧТО именно не покрыто:
эвикция окна (`window_ms = Some(W)`) и, для полноты симметрии, вход merge-пути с уже
разъехавшимися длинами.

## Б-2 — БЛОКЕР (перенесён из `R-171` Б-3/Б-4). Задачи 6 и 7 не исполнены; `VB-I-4` требует бампа

```
$ bash scripts/verify_M-70.sh 2>&1 | grep -E '^(FAIL|VERDICT)'
FAIL: cargo test --all --quiet
FAIL: task #6 версия схемы НЕ поднята (база 9, HEAD 9): смена формы выдачи обязана объявляться бампом (VB-I-4)
FAIL: task #7 — в записи GATEWAY_BANDS нет полос: 0.015 0.03 0.05 0.08 0.15 0.3 0.6
VERDICT: FAIL (3)
```

Форма выдачи В ЭТОЙ ревизии СМЕНИЛАСЬ: с `DepthRow` снято поле `depth_band_provenance`,
добавлено `series_provenance`. `VB-I-4` (`docs/fa/viz-backend.md:201`) состоит из ДВУХ
половин, и вторая — «форма меняется только с bump». Первая восстановлена (v1-потребитель
читает), вторая не исполнена: `GATEWAY_SCHEMA_VERSION = 9` и на базе, и на вершине.

**Это не претензия к dev'у.** Он остановился по диспетчу: задача 6 ждёт решения founder'а об
очерёдности с `M-72`, задача 7 объявлена спекой последней. Но состояние вершины от мотива не
зависит: **вершина, где форма сменена, а версия нет, в `main` не входит**. Развязка —
дispatch, а не код: см. §E.

## Б-3 — БЛОКЕР ДОСТОВЕРНОСТИ. Done Block называет `exit=0` там, где гейт даёт `exit=1`

Отчёт dev'а, §C:

> `$ bash scripts/verify_M-70.sh ; echo exit=$?` → `VERDICT: FAIL (3)` … `exit=0`

Прогон на чистом чекауте вершины (`/tmp/hft-reviewer-m70-rev3`, HEAD `7b16eec`):

```
$ bash scripts/verify_M-70.sh > /tmp/m70-verify.out 2>&1; echo "VERIFY_EXIT=$?"
VERIFY_EXIT=1
$ tail -2 /tmp/m70-verify.out
VERDICT: FAIL (3)
VERIFY_EXIT=1
```

Гейт fail-closed и ведёт себя правильно: `VERDICT: FAIL (3)` ⇒ `exit=1`. **Ложно именно число
в отчёте.** Значение находки не в арифметике: `exit=0` acceptance-скрипта — это ДОСЛОВНОЕ
условие №1 права на auto-push (`commit-discipline.md` §Auto-push). Отчёт, в котором рядом
стоят `VERDICT: FAIL` и `exit=0`, внутренне противоречив, и противоречие смотрит ровно в ту
сторону, куда работа уезжает в `main`. `gates.md` §3: **решение принимается по коду возврата**;
`commit-discipline.md`: **каждое утверждение подкреплено командой и её exit-кодом**.

Остальные строки Done Block воспроизведены и сошлись поимённо — 3/3, 2/2, 4/4, 8/9, fmt/clippy
чисто. Класс — не «тесты соврали», а «пруф не перечитан перед отправкой».

## Н-1 — NOTE. `cargo test --all` красен РОВНО в одной точке; это внешний дефект, и он получает карточку

Заявление dev'а («красное — только `gw_i_4`») проверено ПОЛНЫМ прогоном рабочего пространства,
а не выборочным:

```
$ cargo test --all --no-fail-fast 2>&1 | grep -E "^test .* FAILED"
test gw_i_4_holds_when_the_tail_frame_is_delta_only ... FAILED
```

Один красный на весь workspace. Сырое расхождение:

```
left  (клиент: snapshot(C)+frames): [confirmed, not-observed band=0.020000 reach=0.005000, confirmed]
right (полный реплей):              [confirmed, not-observed band=0.020000 reach=0.000015,
                                     not-observed band=0.020000 reach=0.005000, confirmed]
```

Расходится НАБОР точек (3 против 4), а не значения меток. `R-171` Б-2 установил замером, что
на merge-base оракул зелен и ключи точек `M-70` не менял; спека §2bis.-2 назвала механизм
чтением кода (кадр строится СВЕЖИМ `Reducer::new` на каждый вызов — `lib.rs:1862` — и у него ПУСТАЯ книга,
а депт-серия считается ИЗ книги). Разрез предмета по `П-022` **принят**: дефект предсуществует в `main`,
его развязка — конструкция инкрементального пути, а не задача милестоуна о составе выдачи.

**Карточка заведена: `TD-199`** (`VB-I-2`/`GW-I-4`, MAJOR) — номер выдан механизмом
(`scripts/next_artifact_id.sh TD`). Она уходит в `main` отдельным PR reviewer'а, а не ждёт
merge'а `M-70`: долг, видимый только на заблокированной ветке, невидим.

## Н-2 — NOTE. `VB-I-5` в FA называет поле, которого после этой правки НЕТ (зона: architect, маршрут `gates.md` §9)

```
$ sed -n '202p' docs/fa/viz-backend.md
| VB-I-5 | Серия глубже 1.3% несёт `depth_band_provenance`; отсутствие поля → серия невалидна …
$ grep -n 'pub depth_band_provenance' crates/gateway/src/lib.rs
328:    pub depth_band_provenance: Option<String>,      # ← это ЯЧЕЙКА heatmap, не DepthRow
```

На `DepthRow` поля больше нет: носителем инварианта стал `series_provenance`. СУЩЕСТВО
`VB-I-5` сохранено (глубокая полоса несёт метку, и `deep_band_carries_provenance` это
проверяет поточечно), но ТЕКСТ FA называет поле, которого в этой структуре не существует.
Это дословно класс `TD-138` («документ обосновывает инвариант механизмом, которого нет»),
и он опасен ровно тем, что следующий читатель сверится с текстом. Правка FA — зона architect'а
и триггер критика (`gates.md` §9: `docs/fa/*.md` при изменении инвариантов). Правка обязана
лечь ВМЕСТЕ с бампом (задача 6), не раньше: до бампа она была бы ложью о `main`.

## Н-3 — NOTE. Три sacred-оракула описывают форму, которой больше нет

```
$ grep -rn 'DepthPoint' crates/gateway/tests/
red_depth_cadence.rs:198,200 · red_depth_semantics.rs:147 · red_gateway_bounded.rs:324
```

Все три — комментарии вида «ФОРМА СМЕНИЛАСЬ (задача 4): ряд — `Vec<DepthPoint>`». Тесты
ЗЕЛЁНЫ (код переведён на кортежи), врут только тексты. Dev назвал это сам и правильно не
тронул (sacred). Зона — architect, тем же кругом, что Б-1.

## Н-4 — NOTE. `zip` на merge- и evict-путях усекает МОЛЧА

`Snapshot::apply` (`lib.rs:1988-2004`) и `evict_series_bundle_under_window` (`:2189-2199`)
складывают массивы через `.zip(...)`. При равных длинах это верно; при неравных — `zip`
останавливается на коротком и **молча теряет точки**, вместо того чтобы упасть. Милестоун §2.1
запрещает ровно этот класс: «молча УСЕКАТЬ выдачу… тихая обрезка запрещена» (`PL-I-7`).
Сегодня недостижимо (продюсер держит длины равными), но конструкция превращает нарушение
`DB-I-4c` на входе в потерю данных на выходе — то есть страж, который Б-1 просит расширить,
и здесь единственная защита. Не блокер: путь недостижим через прод-вход; назван, чтобы не
считался проверенным.

## Н-5 — NOTE. Ветка отстала от `main` на 6 коммитов; `strict: false`

```
$ bash scripts/check_branch_health.sh | grep 'M-70'
docs/M-70-rev2      6 отст   28 своих   11 новых   0 сут
```

Защита `main` не требует свежести ветки (`gates.md` §8, «Предел защиты назван честно»).
Перед merge'ем обязателен прогон
`bash scripts/verify_design_claims.sh --merge-preview origin/main` и базовой тройки CI на
ДЕРЕВЕ СЛИЯНИЯ. К моменту APPROVED отставание вырастет — пункт держать в условиях merge'а,
а не считать снятым.

---

# Что проверено сверх находок (и прошло)

```
$ grep -cE '^PASS' /tmp/m70-verify.out          → 38 шагов PASS
$ grep -E '^PASS: (cargo fmt|cargo clippy)' /tmp/m70-verify.out
PASS: cargo fmt --all -- --check
PASS: cargo clippy --all-targets --all-features -- -D warnings
$ cargo test --all --no-fail-fast                → один красный (Н-1), остальное зелено
$ bash scripts/check_branch_health.sh            → VERDICT: PASS (ДУБЛЬ только по M-75)
$ git ls-remote --heads origin | grep -i 'm.70'  → одна ветка, 7b16eec — работа достижима из origin
$ gh run list --branch main --limit 5            → CI+Deploy success; VPS HEAD 571403d
```

Аддитивность формы предъявлена не словом: `red_gateway_export_v2::DepthRowV1` — структура,
знающая только `series: Vec<(i64,i64)>`, — разбирает выдачу вершины (`2 passed`), тогда как на
`547bf24` тот же оракул падал с `Error("invalid type: map, expected a tuple of size 2")`.
**`R-171` Б-1 закрыт по существу.**

**Ярус C прочитан ГРЕПОМ ПО ПРЕДМЕТУ** (`reading-map.md` §2), не целиком:
`grep -n 'TD-159\|TD-161\|VB-I-2' TECH-DEBT.md` → строки 100, 101, 109 (`TD-169`), 119
(`TD-179`), 838, 869, 1308, 1607 — смежные карточки класса `VB-I-2` живы и не покрывают
дельта-хвост (поэтому `TD-199` — новая карточка, а не дубль);
`grep -n 'delta-only\|дельта-хвост' TECH-DEBT.md` → ни одной заводящей записи предмета.

# FA-attestation (M-66)

Диф трогает `crates/gateway/src/**` и `crates/gateway/tests/**` ⇒ FA — `docs/fa/viz-backend.md`.
Названы ЖИВЫЕ инварианты проверяемой ревизии, каждый — предмет находки, а не украшение:

- **`VB-I-4`** (`docs/fa/viz-backend.md:201`) — «export v2 аддитивен; форма меняется только с
  bump». Первая половина ВОССТАНОВЛЕНА этой ревизией, вторая не исполнена — **Б-2**.
- **`VB-I-5`** (`:202`) — «серия глубже 1.3 % несёт `depth_band_provenance`». Существо
  сохранено, ТЕКСТ разошёлся с кодом — **Н-2**.
- **`VB-I-2`** (`:199`) — «live == replay». Красен на дельта-хвосте — **Н-1**, карточка `TD-199`.
- **`VB-I-10`** (`:207`) — bounded-window: именно он делает путь эвикции прод-путём, а не
  гипотезой — **Б-1**.
- **`MD-I-8`** (`docs/fa/viz-backend.md` §5, семейство `MD-I`) — обязательство 4: число и
  провенанс сняты ОДНИМ наблюдением. Реализация держит `BTreeMap<i64,(i64,f64)>` и считает
  метку из `reach` ТОЙ ЖЕ записи — исполнено верно.

`FA-WAIVER` не требуется: у обоих тронутых крейтов (`gateway`, `gateway-serve`) FA есть.

---

# ВЕРДИКТ: **REJECTED**

Merge отказан. **Работа dev'а по задаче 4 принята по существу** — аддитивная форма верна,
зона соблюдена, остановка на `SCOPE VIOLATION REQUEST` была правильной. Отвергнута ВЕРШИНА:
на ней не исполнены две задачи, форма сменена без бампа, и новый страж не покрывает половину
своих путей.

**Условие APPROVED:** Б-1 закрыт (оракул краснеет против мутации эвикции), Б-2 закрыт (задачи
6 и 7), Н-1 остаётся внешним долгом `TD-199`, и `bash scripts/verify_M-70.sh` даёт
`VERDICT: PASS`, `exit=0` на чистом чекауте — **при том, что `M-70` не станет зелёным целиком,
пока жив дефект `TD-199`** (§2bis.-2 спеки; зависимость реальна и названа обеими сторонами).

**Порядок:**

1. **founder** — решение о очерёдности задачи 6 с `M-72` (бамп `GATEWAY_SCHEMA_VERSION` 9→10)
   и о судьбе `TD-199`: чинить отдельным предметом ДО `M-70` либо держать `M-70` на паузе.
   Без этого решения ни один агент не вправе двигаться дальше — оно назначает, что вообще
   считается вершиной.
2. **architect** — Б-1 (расширить `DB-I-4c` на путь эвикции, `window_ms = Some(W)`), Н-3
   (тексты трёх оракулов), Н-2 (правка `VB-I-5` в FA — тем же кругом, что бамп, маршрут
   `gates.md` §9).
3. **engine-dev** — Б-2 (задачи 6 и 7) после решения founder'а; Б-3 — перечитывать exit-коды
   перед отправкой Done Block.

---

# Поправка reviewer'а к собственному прогону (дописано 2026-09-04T15:05Z)

Вердикт выше не меняется ни в одном пункте. Меняется то, что о НЁМ можно утверждать честно:
три отступления от `.claude/agents/reviewer.md` предъявляются здесь, а не умалчиваются.

**1. Маршрут `REJECT` отклонён от дефолта профиля — основание называется, а не подразумевается.**
`.claude/agents/reviewer.md` §Handoff: «REJECT/CHANGES REQUESTED → dev-агент, который делал
impl (SVR-response цикл, не self-fix у architect)». Я адресовал вердикт founder'у, далее
architect'у, далее engine-dev'у. Отклонение обосновано и остаётся в силе:

- **работа dev'а по задаче 4 претензий не имеет** — SVR-цикла к нему нет: `7b16eec` трогает
  ровно `crates/gateway/src/lib.rs`, форма верна, аддитивность предъявлена прогоном;
- **Б-1 лежит в `*/tests/**`** — sacred, dev туда не пишет по `scope-guard.md`; адресовать
  блокер dev'у значило бы предписать ему нарушение;
- **Б-2 (задачи 6, 7) — действительно dev'ская**, но её содержание зависит от очерёдности
  бампа с `M-72`, а это граница C: назначает founder, не агент (`gates.md` §0.1).

Дефолт профиля описывает обычный случай «impl не сошёлся со спекой»; здесь не он. Правило
не нарушено по существу, но отклонение обязано быть НАЗВАНО — иначе следующий круг прочтёт
маршрут как норму.

**2. `docs/05-contract-layer.md` открыт ПОСЛЕ того, как я на него сослался.** Block-C выше
утверждает «форма кокпита — T-designate (`05-contract-layer.md` §2)», а в момент написания
основанием была цитата в `R-171` и текст милестоуна, не сам документ. Это ровно то, что
запрещает `reading-map.md` §3 (правило цитаты) и что стоит второй строкой в startup-чтении
профиля. Документ прочитан; **утверждение выдержало проверку и УСИЛИЛОСЬ**:

```
$ sed -n '/T-DESIGNATE, НЕ T1/,+6p' docs/05-contract-layer.md
… Живут T-designate в своих крейтах (export/gateway/ai-copilot), меняются
**аддитивно** (bump `export_schema_version`; старые консюмеры не ломаются), **БЕЗ contract-RFC**.
$ sed -n '/^- \*\*Ломающее\*\*/p' docs/05-contract-layer.md
- **Ломающее** (удаление/переименование поля, смена типа, порядок enum) — major bump …
```

**Следствие для Б-2, которого в первой редакции не было:** бампа требует не только `VB-I-4`,
но и `05` §2 прямым текстом — «меняются аддитивно, bump; старые консюмеры не ломаются» — а
снятие поля `depth_band_provenance` со строки есть удаление поля (§4, «Ломающее»). Для
T-designate это не влечёт contract-RFC, но объявление бампом влечёт. Основание Б-2 стало
двойным, а не одиночным.

**3. `docs/DESIGN.md` и `docs/SESSION-HANDOFF.md` §0/§0bis (ярус A ведущей сессию роли) в
начале прогона открыты не были.** `SESSION-HANDOFF` §0/§0bis прочитан постфактум: раздел
датирован 14-18.08, `M-70` в индексе пайплайна не значится, на вердикт не влияет.

**ЗАКРЫТО тем же днём: `DESIGN.md` прочитан ЦЕЛИКОМ (1 048 строк), и чтение дало
СОДЕРЖАТЕЛЬНУЮ добавку — то есть пробел был не формальным.**

- **`DESIGN` §17 меняет СРОЧНОСТЬ `TD-199`.** Раздел объявляет `L2Delta` предусловием
  продукта и предписывает инверсию ролей: «`L2Delta` (100 мс) — основной поток книги;
  `L2Snapshot` — редкий якорь (частота якоря 10–30 с)». Условие срабатывания `TD-199` —
  окно кадра БЕЗ якорного снимка. Сегодня якорь идёт 1 Гц; после `M-45` он станет в 10–30
  раз реже ⇒ **доля кадров, попадающих в дефект, вырастет на порядок** — вместе с ростом
  честности данных, ради которого инверсия делается. Чинить дешевле ДО инверсии. Добавка
  внесена в карточку отдельным PR.
- **`DESIGN` §22 числит семейство `VB-I` как `[ЧАСТИЧНО]`** — заявлено 11 инвариантов, в
  оракулах 8. Значит Н-2 (текст `VB-I-5` разошёлся с кодом) и Б-1 (страж уже нового
  инварианта покрывает половину путей) — не два случайных промаха, а два носителя ОДНОГО
  системного пробела покрытия в этом семействе. Для architect'а это довод считать их одним
  кругом работы, а не двумя правками.
- Прочее ярус-A-содержание (§1 `DET-I-1`, §4 `RK-I-*`, §6 границы A/B/C, §21 ворота
  execution) предмета `M-70` не касается: диф `gateway` — read-only консюмер журнала
  (`VB-I-3`), order-путь отсутствует как класс. Ни один вывод вердикта не изменился.

**Чего в этом списке НЕТ и почему.** «Вердикт не вынесен PR-комментарием» отступлением не
является: PR на `docs/M-70-rev2` не существует, а создаёт его reviewer при APPROVED
(профиль, обязанность 7). Носитель гейта по `gates.md` §4 — ФАЙЛ, и он предъявлен: этот
документ закоммичен и запушен на ветку предмета до завершения работы.
