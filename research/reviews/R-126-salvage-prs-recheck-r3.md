<!-- GATE-META
milestone: M-60a
audited_repo: a3ka/hft-platform
audited_base: fcbbcb7225e8ab80ab5b2528f7f621717afa8617
audited_head: 322646646960549427a79e4530717826fc8eb623
verdict: REJECT
-->

# R-126 — круг 3 (PR #80 `6387d5b`, PR #81 `3226466`): строка M-69 подтверждена ПРОГОНОМ, блокер один — приписка стоит НАД НЕ ТОЙ ТАБЛИЦЕЙ

**Проверяющий:** независимый Fable-агент со свежим контекстом (`gates.md` §9), тот же, что
`R-124`/`R-125`. Рамка круга 3 — самая узкая: (i) строка M-69 §0ter · (ii) две строки,
поправленные сверх вердикта (M-66, барьер ресурсных оракулов) · (iii) абзац `NOTE-3` в
README · (iv) приписка о методе в шапке таблицы. Принятое ранее не пересуживалось.

Дерево слияния: worktree detached `0536505` (`origin/main`) + `6387d5b` + `3226466`
(оба merge чисты). Пять барьеров на нём — все `exit=0` (`gate_meta`: «вердиктов проверено:
2» — `R-124`/`R-125` прошли); `verify_design_claims` → `PASS (0 нарушений)`, `H-FACTS-SHA` 7/7.

**Вердикт: REJECT — один блокер Б-6, пункт (iv).** Пункты (i)–(iii) чисты; ключевое
утверждение строки M-69 я подтвердил СОБСТВЕННЫМ ПРОГОНОМ гейта на ветке.

---

## (i) Строка M-69 — каждое утверждение, включая прогон гейта

| утверждение строки | моя команда | вывод | совпало |
|---|---|---|---|
| четыре круга + арбитраж | `git ls-tree … origin/feat/M-69-window-guard \| grep M-69` + `grep -m1 verdict:` по каждому | `C-099` REJECT · `C-100` REJECT · `C-104` REJECT · `A-014` DECISION · `C-132` NOTE | ✅ |
| «PR #32 OPEN (draft), снять с draft» | `gh pr view 32 --json state,isDraft` | `OPEN draft=true` | ✅ |
| «флип фактуры сделан (`00886e3`)» | `git log -1 00886e3` | есть на ветке: «флип фактуры к свершившемуся факту после GREEN» | ✅ |
| «dev GREEN; `verify_M-69` PASS 13/0» | **прогон**: worktree на `00886e3`, `bash scripts/verify_M-69.sh` | **13 × PASS, 0 × FAIL, `VERDICT: PASS`, exit=0** (fmt · clippy · 6 RED-оракулов · 3 grep-канарейки · docker-compose дефолт · `cargo test --all`) | ✅ |

Оговорка, важная для метода (NOTE-8, не блокер): до этого прогона «PASS 13/0» не имел
ПРЕДЪЯВЛЕННОГО источника нигде в git — последний показанный прогон на ветке был RED-этапом
(`10753df`: «VERDICT FAIL, 4 красных из 13»), а команда в ячейке покрывает только перечень
вердиктов. Утверждение оказалось истинным, но до настоящего вердикта было непроверяемым из
артефактов. Теперь прогон предъявлен здесь.

## (ii) Две строки сверх вердикта — новой лжи нет

- **M-66:** `gh pr view 6` → `CLOSED` ✅; носители — `docs/M-66-protocol-attestation` и три
  ветки-дубля ✅ (`check_branch_health` круга 1: «предмет M-66 живёт на 4 ветках»).
- **Барьер ресурсных оракулов:** `git cat-file -e origin/main:scripts/check_resource_oracles.sh`
  → ЕСТЬ ✅; `grep -c resource-oracle ci.yml` → `3` ✅; `gh pr view 28` → `MERGED` ✅.

## (iii) NOTE-3 в README — снят

Вывод показан тремя строками — ровно так его печатает команда (мой прогон круга 2 давал
именно три строки `2dcad4882ac7`). Пояснительный абзац фактов не искажает. ✅

## (iv) Б-6 (БЛОКЕР, PR #80) — приписка стоит над §0, а построчно проверялась §0ter

`grep -n 'ПОСТРОЧНО' docs/SESSION-HANDOFF.md` → **строка 95, единственное вхождение** — в
шапке таблицы **§0** («ПАЙПЛАЙН», `:88`), а НЕ §0ter (`:431+`), где сделаны все правки.
Приписка утверждает: «Таблица проверена ПОСТРОЧНО 2026-08-24… Каждое утверждение об этапе
теперь несёт КОМАНДУ прямо в ячейке». Для таблицы, над которой она стоит, ложны ОБА тезиса:

1. Ни одна строка §0 команды в ячейке не несёт (M-57, M-58, M-59, M-60a…).
2. Построчная проверка §0 не проводилась — под припиской стоят опровергаемые командой строки:
   - **M-63**: «rev2 запушен… круга не было… Ветка 12 позади» — предмет **ЗАКРЫТ БЕЗ
     ИСПОЛНЕНИЯ 2026-08-15** (`git show origin/main:milestones/M-63-ci-cost.md` — шапка
     закрытия; уникальный груз перенесён в `main` байт-в-байт), origin-ветка удалена;
   - **M-60b/M-60c**: «не начаты, стоянка» — `scripts/check_gate_meta.sh` в `main` озаглавлен
     «M-60b G3», работает в CI и судил этот самый вердикт;
   - **M-59**: «BLOCKED… | feat/M-59-lifetime-memory» — `git rev-parse origin/feat/M-59-…` →
     ref удалён (локальная копия — предок `main`, 0 уникальных коммитов);
   - **M-62**: «где лежит: feat/M-62-segment-metadata» — ref удалён.

Сверх того, даже для §0ter тезис «КАЖДАЯ ячейка несёт команду» неточен: строка «барьер формы
milestone» называет результаты замера, но команды в ячейке не несёт (она в теле `c398f13`).

Ирония — третий раз тот же класс: заявление о методе («проверена построчно») само оказалось
утверждением о состоянии, расходящимся со своим предметом — таблицей, над которой стоит.
Что именно делать — выбор автора (перенести приписку к §0ter и сузить формулировку; либо
довести §0 до заявленного — тогда четыре строки выше требуют тех же правок); я дефект
описываю, фикс не проектирую (`gates.md` §4).

## Условие снятия REJECT

Одна правка PR #80: привести приписку в соответствие с фактом (место И формулировка), и —
если приписка останется над §0 — привести §0 к заявленному. Круг 4 — точечно это место.
(i)–(iii) приняты настоящим вердиктом и повторной проверки не требуют.

## Done Block (агрегированный)

```
$ git rev-parse origin/main → 053650533222945b6b680fb997680c1ac79d3473
$ merge 6387d5b → clean · merge 3226466 → clean (worktree /tmp/recheck3-merge)
$ пять барьеров (PUSH_BEFORE=0536505…) → все exit=0; verify_design_claims → PASS (0), H-FACTS-SHA 7/7
$ gh pr view 32 → OPEN draft=true · pr 6 → CLOSED · pr 28 → MERGED
$ bash scripts/verify_M-69.sh (worktree @ 00886e3) → 13 PASS / 0 FAIL / VERDICT: PASS / exit=0
$ git show origin/main:milestones/M-63-ci-cost.md | head → шапка «ЗАКРЫТ БЕЗ ИСПОЛНЕНИЯ 2026-08-15»
$ git rev-parse origin/feat/M-59-lifetime-memory / M-62 / M-63 → все три удалены
$ grep -n 'ПОСТРОЧНО' docs/SESSION-HANDOFF.md → :95 (шапка §0, единственное)
$ grep -m1 'M-60b' scripts/check_gate_meta.sh → «M-60b G3 …» (в main)
```

**Уборка:** worktree'ы `/tmp/recheck3-merge` и `/tmp/recheck3-m69` (включая его `target/`
от прогона гейта) сняты `worktree remove --force`; числа — в Handoff. Резерв: `R-126` взят
CAS-механизмом; после merge носителя — `--release R-126` (вместе с `R-124`/`R-125`).

FA-инвариант: диапазон PR не трогает `crates/**` (`check_review_fa` → SKIP); мой прогон
`verify_M-69` кода не менял — read-only исполнение гейта architect'а. Живой инвариант
предмета M-69, предъявленный прогоном: `GW-I-14` (fail-closed разбор `GATEWAY_WINDOW_MS`).
