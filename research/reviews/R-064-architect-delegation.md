# R-064 — перепроверка §9: «Делегирование клонам» + «вердикт клона не закрывает plan-time гейт»

- **Предмет:** ветка `docs/architect-delegation`, ДВА коммита поверх `origin/main`:
  `5f3f747` (+21 строка, раздел «Делегирование клонам») и `8d474e7` (+11 строк туда же,
  «вердикт клона НЕ закрывает plan-time гейт»). Предмет расширен ПОСЛЕ запуска проверяющих;
  расширение объявлено диспетчером явно, оба коммита судятся как один предмет на `8d474e7`.
  Перезапуск круга не требуется: вердикт до расширения закоммичен не был.
- **Проверяющий:** независимый Fable со свежим контекстом (`fable-recheck`); автор правки —
  architect, в вопросе сторона. Совмещённый круг: норма founder'а 13.08 («анализ существующего
  + оценка рациональности») + перепроверка `gates.md` §9 (а)/(б)/(в).
- **Дата:** 2026-08-13. Дерево проверки: `/tmp/rev-deleg-r064` (detached @ `8d474e7`);
  общий чекаут не переключался.

## ВЕРДИКТ

**ВНЕСТИ СОКРАЩЁННЫМИ ОБА (переписанными). По §9 — REJECT текущей редакции** (блокеры
Б-1…Б-5; точный текст замены — §7). Решение founder'а обязано лечь именно в профиль —
носитель выбран верно и это подтверждено замером; но редакция несёт две фактические ошибки,
три коллизии с действующими нормами (две опровергаются практикой в день внесения, в том числе
мандатом этого самого круга) и одно ложное обобщение в кандидате на механизацию.

**Нормативных строк из 32 (21+11): ~15 после переписывания.** Коммит 1: 11 из 15 текстовых
(4 — нарратив). Коммит 2: ~4 из 10 текстовых несут НОВУЮ норму (2 — дубль триггеров §9,
2 — нарратив, остальное — повтор строки 25 того же файла и ложный предикат §4.1/§4.2).

---

## §1. Рациональность (норма founder'а 13.08) — коммит 1

### 1.1 Носитель — ВЕРЕН

Аудит (`docs/plans/process-layer-audit-2026-08-13.md` §4) выстроил носители по измеренной
силе: барьер → обвязка → шаблон → **профиль роли** → текст в ядре. По кандидатам, которые
автор мог пропустить:

- **обвязка `.claude/wrappers/*`** — не носитель: обвязок 6 (`pi-dev`…`pi-venue-dev`, сырой
  `ls` в §3), architect через обвязку не проходит; норма адресована architect'у.
- **шаблон Done Block / verify-скрипты / CI** — не носители: норма срабатывает в момент
  ЗАПУСКА субагента; ни одна из этих точек этот момент не наблюдает.
- **НО half-механизация существует и не названа** — см. N-1: половина нормы («только
  `subagent_type: architect`») — ФОРМА, кандидат — PreToolUse-hook на Agent-tool в
  `.claude/settings.json` (файла в репо нет — проверено; hooks в среде работают — наблюдаю их
  в собственной сессии). «Механизировать нечем» из тела коммита верно только для половины
  «не отдавай конструкцию». Правка обязана назвать кандидата и либо опровергнуть пробой, либо
  записать долгом.

Цена: профиль 4 246 → 6 150 → 7 560 байт (оба коммита: +3 314, +78 % к профилю; ≈+3 % к
впрыску клона architect'а; **0 байт** восьми прочим ролям и ядру 100 461). Размещение — ровно
по рекомендации аудита («правило, выносимое в файл роли, должно быть вынесено»).

### 1.2 Дубли — в коммите 1 почти нет; в коммите 2 есть (см. §6)

```
$ git grep -n -iE 'subagent_type|general-purpose' origin/main -- CLAUDE.md .claude docs/04-workflow.md
(пусто — нормы о делегировании через subagent_type в зоне НЕТ)
```

«Отчёт клона — гипотеза» — повтор `gates.md` §8 (строка 299) с цитатой источника; допустимо
как указатель, при правке §8 разойдётся — в замене оставлена чистая ссылка. Пара5 пересекается
с `CLAUDE.md` «Агенты не вызывают друг друга» — не противоречит (клон = та же роль), ссылка
добавлена в замене.

### 1.3 «Клон как арбитр/перепроверяющий §0/§9» — разрешение ЧЕСТНОЕ, с одним условием

Свежий контекст у не-fork клона есть механически (fork наследует контекст и под §0/§9 НЕ
годится — правка различия не называет, учтено в замене). Рамка автора наследуется через
КОНТЕКСТ, не через профиль: профиль — зоны и формат, не позиция по предмету; §9 прямо
допускает «ВТОРОЙ агент со свежим контекстом, модель та же». Обход смысла возникает ровно в
одном случае — предмет проверки есть САМ профиль architect'а: судья грузит проверяемый текст
как собственную инструкцию. В этом круге спасла дисциплина веток (мой впрыснутый профиль —
доредакционный, предмет жил на ветке) — условие внесено в замену (§5 А-1).

### 1.4 Короче — можно

Коммит 1: 4 строки нарратива («Замер 13.08…») — жанр, вырезанный `9a9f533`; нарратив целиком
живёт в теле коммита и в аудите, в файле достаточно ссылки. Тело коммита заявляет «двенадцать
строк» при диффе 21 (§5 А-3).

## §2. Коммит 2 (`8d474e7`) — прямые вопросы диспетчера

### 2.1 Повторение ли это ошибки, за которую аудит отклонил П-1/П-2? — НЕ в чистом виде, но наполовину

**Чем коммит 2 ЛУЧШЕ П-1/П-2** (выполняет требования сегодняшнего аудита §4):
`COGNITIVE-ONLY` объявлен явно; кандидат на механизацию назван конкретно; объяснено, почему
барьер не строится тем же коммитом (отдельный круг по норме founder'а). Ни одна из 33 строк
П-1/П-2 этого не делала. И нормативная дельта РЕАЛЬНА: асимметрия §9 в корпусе написана
только в одну сторону («вердикт критика… засчитывается как перепроверка»), обратное
направление НИГДЕ не названо — механизм сегодняшнего отказа был именно misread этой
асимметрии, а не незнание строки 25. Текст, называющий действующий механизм отказа, — не то
же, что П-1/П-2, повторявшие нормы, чей механизм автор знал.

**Чем коммит 2 ПОВТОРЯЕТ ошибку:** (i) добавляет текст в файл, где текст того же смысла
(строка 25: «Не байпасит critic-гейт когда триггер сработал») уже не удержал автора, и НЕ
ссылается на него — в файле теперь ДВЕ формулировки одного запрета; (ii) дублирует список
триггеров §9 дословно — при правке §9 копия молча разойдётся (класс `R-055` Б-2, названный
аудитом поимённо); (iii) 2 строки нарратива M-61.

**Решение:** дельту (асимметрию) внести — но канонически она лечится ОДНОЙ строкой в самом
`gates.md` §9, где живёт порождающая формулировка: после «двух отдельных кругов не требуется»
добавить «(обратное неверно: перепроверка §9 вердикта критика НЕ заменяет)». Это закрывает
misread у ИСТОЧНИКА для всех читателей §9 ценой ~90 байт ядра. В профиле остаётся 3-4 строки:
ссылка на строку 25 + маршрут + кандидат-барьер (§7). Дубль триггеров — убрать.

### 2.2 Реализуем ли барьер? — ДА, дёшево; НО предикат коммита ложно обобщает

**Б-5 (фактическая ошибка):** предикат «правились §4.1/§4.2 его спеки» обобщает ЛОКАЛЬНУЮ
разметку M-61. Проверено исполнением: заголовок `### 4.1` есть ровно в ДВУХ спеках из
`milestones/`, и семантика РАЗНАЯ — M-61 §4.1 «Инвариант», M-60a §4.1 «Батарея анти-плацебо»;
канонический шаблон спеки (`04-workflow.md` §2/§6: Objective / paths / §Tasks / Contract
impact / RED / Acceptance) секций 4.1/4.2 не имеет вовсе. Барьер и жирная норма, привязанные
к «§4.1/§4.2», для большинства спек не срабатывают по построению — класс той же ошибки, что
аудит поймал в П-2 («правило, ложное для доминирующего случая»).

**Работоспособный предикат** (класс `check_docs_freeze.sh`, EVENT_NAME=push + PUSH_BEFORE,
покоммитно/по диапазону): в диапазоне есть НЕ-статусная правка `milestones/M-NN-*.md`
(carve-out статус-колонки — уже норма scope-guard) И нет нового/изменённого
`research/critiques/C-*`, называющего `M-NN`, И нет waiver-трейлера в теле коммита
(`CRITIC-WAIVED: изложение — <причина>` по образцу `FOUNDER-APPROVED`) → FAIL. Требование
именно `C-*` (не `R-*`) механизирует и саму асимметрию §9: перепроверка рождает `R-*` и
барьер её не засчитает. Оценка: ~60–100 строк + `red_*` проба; предупреждающий контур (как
docs-freeze — branch protection недоступен). Известные края, решаемые в круге механизма:
критика в более раннем push (лечится waiver'ом с именем покрывающего `C-*`), close-out-правки.

**Следовательно, ответ на «если барьер дёшев — текст не нужен»:** почти. После барьера от
коммита 2 остаётся одна строка-ссылка. ДО барьера честный интерим — 3-4 строки (§7) + 1
строка в §9. Кандидат в редакции коммита — НЕ ВНОСИТЬ (Б-5); барьер — завести отдельным
кругом с предикатом выше.

### 2.3 Расширение предмета под работающим гейтом

Дефект назван самим диспетчером и телом коммита 2 — согласен, что это тот же класс, что
сегодняшняя недействительная приёмка. Смягчение здесь: расширение объявлено явно, оба SHA
судятся одним вердиктом, перезапуск не нужен (вердикт не был закоммичен). Общая норма — не
предмет этого круга.

## §3. §9(а) — утверждения, проверенные исполнением

```
$ git log --oneline origin/main..origin/docs/architect-delegation
8d474e7 docs(agents): вердикт клона не закрывает plan-time гейт — цепочка после него [architect]
5f3f747 docs(agents): граница делегирования клонам — в профиль architect'а... [architect]

$ git cat-file -s origin/main:.claude/agents/architect.md ; git cat-file -s 5f3f747:... ; git cat-file -s 8d474e7:...
4246 / 6150 / 7560

$ ls .claude/wrappers/
dispatch-mandate.md pi-dev.sh pi-engine-dev.sh pi-research-dev.sh pi-signal-engineer.sh pi-tester.sh pi-venue-dev.sh README.md

$ ls .claude/settings.json
ls: cannot access '.claude/settings.json': No such file or directory

$ git ls-tree origin/main --name-only -- process/ ; ls process
(пусто) / ls: cannot access 'process': No such file or directory   # висячая ссылка §Handoff — ДО правки

$ grep -ln 'M-61' research/critiques/*.md
C-069-M-61-artifact-ids.md · C-070-M-61-rev2.md · C-071-M-61-rev3.md   # ровно три, кругов 4-5 нет — замер коммита 2 ТОЧЕН

$ grep -lE '^#{2,3} (§)?4\.1' milestones/M-*.md
milestones/M-60a-docs-freeze.md · milestones/M-61-artifact-ids.md      # §4.1 — НЕ общий шаблон (Б-5)
$ (семантика) M-61 §4.1 «Инвариант…» / M-60a §4.1 «Батарея анти-плацебо…» — РАЗНАЯ

$ grep -n 'Маршрут' docs/04-workflow.md
30: Маршрут: architect авторит milestone → (critic-гейт если триггер) → founder диспетчеризует dev → tester → reviewer → merge
# цепочка коммита 2 сходится; нюанс: критик УСЛОВЕН по триггеру — жирная строка без оговорки чуть шире маршрута

$ grep -rn 'число топиков' docs/plans/ | head -1
docs/plans/projection-memory-measurement.md:254: **Что двигает стену — не число топиков, а ОКНО.**
$ git grep -n 'commit -- \.' origin/main -- research/reviews/R-062*
R-062-commit-paths-barrier.md:108: **`git commit -- .` ПРОХОДИТ и берёт весь индекс**
```

- Числа коммита 1 сходятся с аудитом: 100 461 байт ядра (§1.1 аудита дословно), ≈6.3
  запуска/сутки (88 артефактов / 14 суток). Все «четыре дефекта делегированного замера»
  найдены в источниках: аудит П-2 §3.2а (pathspec нового файла), аудит П-1 §2.2 (метрика
  `Compiling` — плацебо), `R-062:108` (`git commit -- .`), `projection-memory-measurement.md:378`
  (окно × глубина). Асимметрия §9 — подтверждена текстом §9 (одностороннее «засчитывается»).
- **«`subagent_type: architect` грузит этот профиль» — подтверждено прямым наблюдением:** я
  запущен как `subagent_type: architect` (SubagentStart: «Agent architect started»), мой
  системный промпт содержит профиль дословно — в ДОРЕДАКЦИОННОЙ версии (предмет жил на ветке).
  Доказывает механизм наследования И чистоту круга от самореференции.
- **Непроверяемое изнутри — помечаю честно:** выбор МОДЕЛИ клона — свойство харнесса.
  Наблюдаемый факт: frontmatter пиннит `model: opus`, я исполняюсь на Fable ⇒ запуск нёс
  override; без override клон получает opus — см. Б-4.

## §4. §9(б) — полномочия

```
$ EVENT_NAME=push PUSH_BEFORE=$(git merge-base origin/main 8d474e7) bash scripts/check_docs_freeze.sh; echo exit=$?
exit=0   # оба коммита несут FOUNDER-APPROVED в СВОЁМ теле, проверка покоммитная
```

Причины токенов честные и предметные (решение founder'а названо в обоих). Зона
`.claude/agents/**` — в Writes architect'а. Граница C не переступлена (процессный слой; не
деньги/фазы/промоушены). §9(б) — ПРОЙДЕН.

## §5. §9(в) — связность. Блокеры

- **Б-1 — фактическая ошибка: «запуск невозможен»** (коммит 1). Харнесс architect-сессии
  ПЕРЕЧИСЛЯЕТ `critic`/`engine-dev`/`tester`/`reviewer`/`risk-critic` как запускаемые agent
  types — наблюдаю список в собственной сессии; их профили лежат в `.claude/agents/*.md` того
  же механизма. Запуск ВОЗМОЖЕН — он ЗАПРЕЩЁН; это разные утверждения. Смежно: «живут на
  ДРУГОМ харнессе» верно для 6 ролей с pi-обвязкой; у critic/reviewer/risk-critic обвязки НЕТ.
  Архитектор, прочитавший «невозможно» и увидевший обратное, спишет всю секцию — класс П-2.
- **Б-2 — внутренняя коллизия абзаца 2** (коммит 1): «арбитраж §0 передаётся» ↔ «выбор
  развязки не передаётся». Арбитр §0 по определению ВЫНОСИТ решение (`A-008` выбрал развязку).
  Нужно различие: не передаётся АВТОРСТВО конструкции; СУЖДЕНИЕ по готовой — передаётся, и
  вердикт клона вправе конструкцию опровергнуть (сама норма родилась из такого случая).
- **Б-3 — коллизия с binding-нормами о вердиктах** (коммит 1): «Продукт клона коммитится
  ТОЛЬКО после того, как architect прогнал его САМ» противоречит `branch-hygiene.md` п.4
  (вердикт коммитится ролью «в том же ответе») и `gates.md` §9 (вердикт — файл, закоммиченный
  проверяющим ДО push). Опровергается мандатом ЭТОГО круга (клон коммитит и пушит R-064 сам).
  Нужен скоуп: продукт-в-ПРЕДМЕТ — после личного прогона; вердикт-артефакт — клон сам.
- **Б-4 — модель и строка 11** (коммит 1): (i) «рутина уходит дешёвым субагентам» (строка 11)
  повисает при «субагенты ТОЛЬКО клоны» — клон по frontmatter = opus; (ii) §0/§9 требуют Fable
  для арбитра/перепроверяющего, frontmatter пиннит `model: opus`, секция о модели молчит ⇒
  «клон-арбитр» без override нарушает §0 молча. Лечится: модель называется в запуске.
- **Б-5 — ложное обобщение «§4.1/§4.2»** (коммит 2): см. §2.2 — заголовок `### 4.1` есть в
  2 спеках из всех, семантика разная, канонический шаблон таких секций не имеет. И жирная
  норма, и кандидат-барьер в этой формулировке не срабатывают на большинстве спек по построению.

Ноты: **N-1** — класс контура коммита 1 не объявлен (`COGNITIVE-ONLY`; §11 требует; коммит 2
объявляет — коммит 1 нет), при механизируемой половине (hook, §1.1); **N-2** — 4+2 строки
нарратива; **N-3** — висячая ссылка `process/handoff-template.md` в §Handoff (ДО правки;
каноничный путь `.claude/rules/handoff-block.md`; правка файл трогала — закрыть строкой);
**N-4** — повтор §8 с цитатой; **N-5** — дубль строки 25 и триггеров §9 в коммите 2 (§2.1);
**N-6** — «критик → dev → tester → reviewer» без оговорки условности критика по триггеру.

## §6. Адверсарное расширение (за пределами перечня мандата)

- **А-1 — самореференция:** правка СОБСТВЕННОГО профиля судится клоном, который этот профиль
  грузит. Если предмет сидит в рабочем дереве общего чекаута — судья получает проверяемый
  текст как ДЕЙСТВУЮЩУЮ инструкцию (враждебная правка вида «клон не выносит REJECT»
  самоисполнилась бы). Условие в замене: перепроверка правки `architect.md` запускается только
  пока предмет живёт НА ВЕТКЕ.
- **А-2 — права клона = полный Writes architect'а** (docs/**, `*/tests/`, verify): «замерный»
  клон технически может коммитить в sacred. Мандат обязан СУЖАТЬ зону явно; ответственность —
  на запустившем (`gates.md` §8). В замене — одна строка.
- **А-3 — заявление против диффа:** тело коммита 1 говорит «Двенадцать строк», numstat — 21.
  Класс `branch-hygiene.md` п.9 (заявленное ≠ показанное) — в коммите О НОРМЕ ДИСЦИПЛИНЫ.
  Нормативных строк — 11: неточны оба числа.
- **А-4 — скиллы/механизмы, запускающие агентов** (team/autopilot и т.п.), под литеральное
  «субагенты ТОЛЬКО клоны» попадают — правильно, но не названо; SendMessage к ранее
  запущенным не-клонам — переходный случай, не оговорен (минорно).

## §7. ТОЧНЫЙ ТЕКСТ ЗАМЕНЫ (вместо 32 строк обоих коммитов; +1 правка строки 11; +1 строка в §9)

**(а) Строка 11 профиля:** «…рутина уходит дешёвым субагентам.» → «…рутина уходит клонам на
дешёвой модели (model override при запуске).»

**(б) Раздел целиком (заменяет обе правки):**

```markdown
## Делегирование клонам (решение founder'а 2026-08-13) — COGNITIVE-ONLY, барьера нет

Субагент запускается ТОЛЬКО `subagent_type: architect` — клон с этим профилем и СВЕЖИМ
контекстом (fork, наследующий контекст, под §0/§9 не годится). Модель НАЗЫВАЕТСЯ в запуске:
frontmatter её не гарантирует; для перепроверки §9 и арбитража §0 — Fable, для разведки —
дешёвая. `general-purpose` с ролевым промптом ВМЕСТО профиля запрещён: промпт ловит лишь то,
о чём вспомнил автор, профиль — бэкстоп.

Клону передаётся исполнение и СУЖДЕНИЕ по готовой конструкции: замер, разведка, перепроверка
`gates.md` §9, арбитраж §0, аудит, черновик по принятому решению. Не передаётся АВТОРСТВО
конструкции: инвариант, оси, kill-set формулируются в мандате ДО запуска — но вердикт клона
вправе конструкцию ОПРОВЕРГНУТЬ. Мандат СУЖАЕТ зону клона явно (полный Writes профиля клон
наследует). Правка самого этого файла судится клоном, только пока предмет живёт НА ВЕТКЕ.

Продукт, идущий в предмет (спека/оракул/док/тип), коммитится после личного прогона
architect'ом; вердикт-артефакт гейта клон коммитит на ветку сам (`branch-hygiene.md` п.4).
Отчёт клона — гипотеза, факт — git и прогон (`gates.md` §8). Прочие роли отсюда НЕ
ЗАПУСКАЮТСЯ — запрет, не невозможность: им — Handoff-блок через founder'а (`CLAUDE.md`).

**Вердикт клона НЕ закрывает plan-time гейт** («Не байпасит critic-гейт» выше). `gates.md` §9
несимметричен: вердикт КРИТИКА засчитывается за перепроверку, обратное — НЕТ. Дальше — маршрут
`04-workflow.md` §2 (критик — по триггерам §9, здесь не дублируются); правка нормативных
секций спеки милестоуна (инвариант достаточности, оси, acceptance) — всегда форма, всегда
критик. Кандидат-барьер (отдельный круг): не-статусная правка `milestones/M-NN-*.md` в
push-диапазоне без `C-*`, называющего M-NN, и без waiver-трейлера → FAIL.

Основание границы — тела коммитов `5f3f747`/`8d474e7` + `docs/plans/process-layer-audit-2026-08-13.md`.
```

**(в) `gates.md` §9, канонический фикс асимметрии** (отдельный коммит с токеном, та же ветка):
после «засчитывается как перепроверка — двух отдельных кругов не требуется» добавить:
«(обратное неверно: перепроверка §9 вердикта критика НЕ заменяет — misread, стоивший M-61
кругов 4-5 без критика)». Первая половина — норма; хвост-нарратив опционален.

Замена снимает Б-1…Б-5, N-1/N-2/N-4/N-5/N-6, А-1/А-2. N-3 (`process/handoff-template.md` →
`.claude/rules/handoff-block.md`) — рекомендую той же правкой, блокером не является.

## Done Block

```
$ git log --oneline origin/main..origin/docs/architect-delegation → 8d474e7, 5f3f747 (два коммита)
$ EVENT_NAME=push PUSH_BEFORE=$(git merge-base origin/main 8d474e7) bash scripts/check_docs_freeze.sh; echo exit=$? → exit=0
$ git cat-file -s {origin/main,5f3f747,8d474e7}:.claude/agents/architect.md → 4246 / 6150 / 7560 (+3314)
$ grep -ln 'M-61' research/critiques/*.md | wc -l → 3 (C-069/070/071; кругов 4-5 нет — замер коммита 2 точен)
$ grep -lE '^#{2,3} (§)?4\.1' milestones/M-*.md | wc -l → 2 (M-60a, M-61; семантика разная — Б-5)
$ ls research/reviews/ | grep -c R-064 → 0 до этого файла (последний занятый — R-063)
```

Read-only по предмету соблюдён: сам предмет не правил; продукт круга — только этот файл.
