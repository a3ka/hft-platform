<!-- GATE-META
milestone: M-87
audited_repo: a3ka/hft-platform
audited_base: 6378bb7045b410d7f94382aae9931b469b8f52cd
audited_head: 32ccc078257687ca3c8b4c711483fa15f5cdc92c
verdict: NOTE
-->

# R-199 — перепроверка §9: `П-030` (что обещаем пользователю — СРЕДНИЙ вариант)

**Роль:** architect-клон на свежем контексте (Fable), перепроверка по `gates.md` §9.
Автор правки — сторона; я её не защищаю. **Зона:** только вердикт-файл.
**Предмет:** ветка `docs/p030-user-promise`, один коммит `32ccc07`, +61 строка в
`docs/PENDING-SIGNATURE.md` (запись `## П-030`, строки 2220–2277).
**Дерево слияния:** `origin/main` = `6378bb7`.

## Вердикт: NOTE

Воля владельца записана верно и не превышена: секция «РЕШЕНИЕ» повторяет ровно ту строку
таблицы трёх развилок, которую founder выбрал словом «средний вариант»; всё сверх выбора
вынесено под отдельный заголовок «Что из этого следует — и чего НЕ следует» и опирается на
уже принятый текст плана (§4 п.3, §15.4–§15.6), а не на новое суждение автора. Два
оставшихся вопроса §13 (носитель решения о полосах; состав заморозки) названы открытыми
явно (строки 2222–2224), а не закрыты молча.

Две находки уровня NOTE — обе о ССЫЛКАХ, не о содержании решения. Ни одна не требует
второго круга; правка Б-1 — одна строка, и её стоит внести до merge.

## Б-1 (NOTE) — п.2 ссылается на `§4 милестоуна M-87`, которого на дереве слияния НЕТ

Запись (строка 2261–2262): «"Явный отказ" здесь — исход из перечня §4 милестоуна `M-87`».

```
$ ls milestones/ | grep -i 'M-87'; echo "rc=$?"                       → rc=1
$ git ls-tree --name-only origin/main milestones/ | grep -i M-87; echo rc=$?  → rc=1
$ for b in $(git for-each-ref --format='%(refname:short)' refs/remotes/origin); do
    git ls-tree --name-only "$b" milestones/ | grep -i M-87 | sed "s|^|$b: |"; done
  → origin/feat/M-87-serving-circuit-breaker: milestones/M-87-serving-circuit-breaker.md
$ git rev-parse origin/feat/M-87-serving-circuit-breaker            → 6aa2d4740ceab9abc8d9665df28e60f1a9ceae28
$ git log --oneline origin/main..origin/feat/M-87-serving-circuit-breaker | wc -l → 91
$ git merge-base --is-ancestor origin/feat/M-87-serving-circuit-breaker origin/main → NOT MERGED
$ gh pr list --head feat/M-87-serving-circuit-breaker --state all   → []
```

Файл живёт ТОЛЬКО на `feat/M-87-serving-circuit-breaker` — 91 коммит впереди `main`, PR не
открыт, состояние по собственной спеке «COMPILE-RED, заявленное» (§14.1quinquies-ter).
Читатель `main` после merge'а этой записи по ссылке ничего не найдёт.

**Что на самой ветке `M-87` — перечень есть, и подходящий исход в нём есть** (это проверка
«особо-2» из мандата; класс `TD-138` НЕ подтверждается по существу — механизм спроектирован,
а не выдуман):

```
$ git show origin/feat/M-87-serving-circuit-breaker:milestones/M-87-serving-circuit-breaker.md | sed -n '71,80p'
## 4. Контракт допуска — ПЯТЬ исходов, и ни одного молчаливого
| `UNSUPPORTED` | запрос вне политики допуска (§6) | отвечает исходом, параметры НЕ подменяет |
```

`UNSUPPORTED` с «параметры НЕ подменяет» — ровно тот исход, который запись имеет в виду.

**Ни один барьер эту ссылку не видит — и это названный предел, а не упрёк барьерам:**

```
$ bash scripts/check_archived_refs.sh; echo rc=$?     → PASS (ловит только ВЫНЕСЕННЫЕ артефакты, шапка скрипта)
$ bash scripts/verify_design_claims.sh                 → PASS [4-МЁРТВЫЕ-ФАЙЛЫ]: только пути вида docs/*.md
$ sed -n '2220,2277p' docs/PENDING-SIGNATURE.md | grep -n 'M-87'   → 1 совпадение, голый id без пути
```

**Источник ТОГО ЖЕ перечня, который в `main` есть:** план §4 п.1, строки 87–88:

```
$ sed -n '87,88p' docs/plans/scale-program-2026-09-21.md
   1. **Живой путь принимает только ГОТОВОЕ состояние.** Промах слепка распознаётся ДО чтения
      журнала; исходы: `READY` · `WARMING` · `NOT_READY` · `UNSUPPORTED` · `OVERLOADED`.
```

и §4 п.3 (строки 95–96): «Политика публичного запроса … **Без молчаливой подмены запрошенных
параметров**» — то есть само требование «названный отказ, не подмена» — уже принятый текст
плана (направление принято founder'ом 2026-09-21, `ROADMAP.md:108`), а не вставка автора.

**Рекомендация (одна строка):** в п.2 назвать источником перечня план §4 п.1 (он в `main`),
а `M-87` — поставкой, где перечень реализуется, с оговоркой, что она ещё на ветке. Либо
оставить как есть, признав, что ссылка оживёт только с merge'ем `M-87`; тогда это должно быть
сказано в записи, а не подразумеваться.

## Б-2 (NOTE) — «Предел» приписывает будущий механизм `S1a`; принуждающая половина живёт в `M-87` (S0)

Запись (строки 2273–2275): «Механизма … сегодня НЕ СУЩЕСТВУЕТ — он появляется вместе с
версионированным профилем расчётов в `S1a`».

Половина «не существует сегодня» — истинна для `main`:

```
$ git cat-file -e origin/main:crates/gateway-serve/src/admission.rs
  fatal: path 'crates/gateway-serve/src/admission.rs' does not exist in 'origin/main'   rc=128
$ git grep -n 'ServingOutcome' origin/main -- crates/                                    → (пусто)
```

и подтверждена замером самой `M-87` §6 (таблица, строки 362–365 файла на ветке): оба валидатора
`main` (`session.rs:70-106`, `gateway/src/lib.rs:3022-3146`) проверяют форму селектора, но
«каноничность набора; список разрешённых инструментов; допустимые профили» — НЕ проверяют.
Клиент вправе прислать произвольный профиль; на `main` в обещание сегодня «просачивается» всё.

Половина «появляется с `S1a`» — неточна. Механизм, который НЕ ПУСКАЕТ недоказанную величину
в выдачу, — это `AdmissionPolicy.allowed_profiles` + `admit(policy, sel) → Unsupported`
(`M-87` §4.1, строки 146–148 файла на ветке: «Конечный перечень профилей. Селектор, не
совпавший НИ С ОДНИМ, ⇒ `Unsupported`»). Это S0. `S1a` (план §15.5) даёт СОДЕРЖАНИЕ перечня —
версионированный профиль с доказательствами собираемости. Правило `П-030` держится на обеих
половинах: S0 — «ничего вне списка», S1a — «в списке только доказанное». Приписывать механизм
одной `S1a` значит оставить читателя с мыслью, что до `S1a` принуждать нечем — тогда как
принуждающая часть уже спроектирована и стоит в очереди раньше.

**Тег `COGNITIVE-ONLY` применён правомерно, а не как отговорка.** На `main` механизма нет
(команды выше); формальное требование «обязывающее правило живёт вместе с механизмом»
(`.claude/agents/architect.md`, `gates.md` §11) очерчивает зону `.claude/rules/**` ·
`.claude/agents/**` · `CLAUDE.md` · `docs/04-workflow.md` — `docs/PENDING-SIGNATURE.md` в неё
не входит, тег здесь добровольный и честный. Цитата §11 существует:

```
$ grep -n 'механизировать нечем' .claude/rules/gates.md   → 469
```

**Рекомендация:** в «Пределе» назвать обе половины: принуждение — `M-87` §4.1
(`allowed_profiles`), содержание — `S1a`.

## Что проверено и сошлось — (а) утверждения о документах и коде

| утверждение записи | команда | результат |
|---|---|---|
| §15.4 разделяет технический каталог и обещание пользователю | `sed -n '334,346p' docs/plans/scale-program-2026-09-21.md` | две строки «что материализуем постоянно (инженерное)» / «что обещаем пользователю (граница C, ваша подпись)» — дословно |
| цитата §15.4 «наличие опорных уровней 1s/1m/1h не означает запрета 5m-графика; но возможность корректно его собрать обязана быть ДОКАЗАНА» | там же, строки 345–346 | совпадает дословно (в плане продолжение «для соответствующих величин» — запись его несёт до цитаты) |
| §15.5 вводит версионированный профиль расчётов → `S1a` | `sed -n '348,357p'` | «несекретный версионированный профиль расчётов (уровни, единицы, смысл диапазонов, идентификатор определения)» → `S1a`; §5 плана озаглавлен «S1a — три контракта и определения» |
| §15.6 — положительные проверки в приёмку `S3` | `sed -n '359,369p'` | таблица из четырёх строк; две, цитируемые записью, — строки 1–2 таблицы, смысл передан верно |
| «первый из трёх вопросов §13»; «два оставшихся — носитель; заморозка» | `sed -n '238,277p'` | §13 несёт четыре пункта, п.2 «СНЯТ С ПОДПИСИ»; остаются п.1, п.3, п.4 — запись точна; `ROADMAP.md:108`: «Ждут подписи ТРИ пункта» — согласуется |
| глубина истории ≈ два месяца, замер 2026-09-19 | `git grep -n '2026-09-19' origin/main -- docs/ \| grep -i глубин` | `docs/ROADMAP.md:105`: «Глубина истории — 2 месяца 4 дня» (самый ранний сегмент 15 июля 2026, замер 2026-09-19) — не выдумано |
| «общий `invalid_selector`» существует | `git grep -n invalid_selector origin/main -- crates/` | `crates/gateway-serve/src/lib.rs:739,812,872,882,964` |
| `history_truncated` существует | `git grep -n history_truncated origin/main -- crates/` | `crates/gateway-serve/src/bin/wsprobe.rs:360` и далее |
| прецедент Р-2 «вставка сверх подписанного текста» | `grep -n 'Р-2' docs/04-workflow.md` | строки 70, 82 — существует; класс: норма назначила исполнителя, которого подписанный текст не называл |

Прогон утверждений документов — на ветке и на дереве слияния, расхождений нет:

```
$ bash scripts/verify_design_claims.sh; echo rc=$?                            → VERDICT: PASS (0 нарушений) rc=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main; echo rc=$? → VERDICT: PASS (0 нарушений) rc=0
```

## (б) Полномочия

- **Граница C — верно.** §15.4 прямо: «что обещаем пользователю — … (граница C, ваша
  подпись)». `gates.md` §0.1: founder решает «чего мы ХОТИМ» — обещание платящему пользователю
  есть воля владельца, не инженерная правота. Автор не присвоил решение.
- **Сверх подписанного — нет.** Founder ответил «средний вариант» на таблицу трёх развилок;
  секция «РЕШЕНИЕ» = формулировка средней строки этой таблицы, без добавлений. Пп. 1–4
  «следствий» — выведены из уже принятого плана (§4 п.3, §15.5, §15.6), помечены как следствия
  отдельным заголовком, и п.4 сужает решение («НЕ называет слои и глубину»), а не расширяет.
  Класс Р-2 (назначение исполнителя, которого подписанный текст не называл) — не воспроизведён.
- **Два оставшихся вопроса §13 — открыты явно** (строки 2222–2224). Наблюдение без находки:
  п.1 «перечень живёт в версионированном профиле (`S1a`)» предполагает, что профиль станет
  носителем, — это и есть предмет открытого §13 п.3. Запись п.3 не подписывает и говорит об
  этом сама; §15.5 — уже принятый текст. Оставляю как наблюдение: при подписании п.3 эту
  строку стоит перечитать.

## (в) Связность

```
$ grep -c '^## П-030' docs/PENDING-SIGNATURE.md                    → 1  (дубля нет)
$ grep -n '^## П-' docs/PENDING-SIGNATURE.md | head -12             → порядок в файле хронологический append
                                                                      (П-008 раньше П-007; П-005 и П-007 дважды) — П-030 в конце уместна
$ grep -n '^## П-' docs/PENDING-SIGNATURE.md | grep -i 'обеща\|пользовател' → только П-030 (дубля предмета нет)
$ git diff --name-only origin/main..HEAD                             → docs/PENDING-SIGNATURE.md (зона §11 не тронута; FOUNDER-APPROVED не требуется)
$ bash scripts/check_archived_refs.sh; echo rc=$?                    → PASS  rc=0
$ EVENT_NAME=pull_request PR_BASE_SHA=6378bb7… bash scripts/check_gate_meta.sh; echo rc=$?    → PASS  rc=0
$ EVENT_NAME=pull_request PR_BASE_SHA=6378bb7… bash scripts/check_artifact_ids.sh; echo rc=$? → OK    rc=0
$ bash scripts/check_branch_health.sh                                → PASS; docs/p030-user-promise: 1 коммит впереди, 0 сут
$ bash scripts/next_artifact_id.sh R                                 → R-199
```

Замечание о форме: без `EVENT_NAME` оба барьера падают fail-closed («барьер зовут не так, как
его зовёт CI», rc=2/rc=1) — это верное поведение, а не находка; прод-форма взята из `ci.yml:260,420`.

## Пределы этой перепроверки

- Дословный ответ founder'а («средний вариант») мне предъявлен мандатом, не транскриптом; я
  проверил, что запись не выходит за таблицу, которую founder видел, — саму таблицу воспроизвёл
  автор, и её тождество с предъявленной founder'у я подтвердить не могу.
- `M-87` судился по содержимому ветки `6aa2d47`; спека там живая (91 коммит, COMPILE-RED по
  собственному заявлению) и может измениться до merge'а.

## Done Block

```
$ git rev-parse origin/docs/p030-user-promise                       → 32ccc078257687ca3c8b4c711483fa15f5cdc92c
$ git rev-parse origin/main                                          → 6378bb7045b410d7f94382aae9931b469b8f52cd
$ git log --oneline origin/main..origin/docs/p030-user-promise        → 32ccc07 docs: П-030 — … [architect]
$ git show --numstat --format='' 32ccc07                              → 61  0  docs/PENDING-SIGNATURE.md
$ bash scripts/verify_design_claims.sh --merge-preview origin/main    → VERDICT: PASS  rc=0
$ bash scripts/check_archived_refs.sh                                 → VERDICT: PASS  rc=0
```
