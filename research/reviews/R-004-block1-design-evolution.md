# R-004 — PR-гейт Блока 1: эволюция `DESIGN.md` v1 → v2 (AlphaQuant)

- **Роль:** reviewer (PR-гейт, UNCONDITIONAL — `.claude/rules/gates.md` §4 + §9 DOC-гейт, класс A)
- **Дата (UTC):** 2026-07-31T23:45Z
- **Ветка:** `docs/design-evolution`, HEAD `a72d08e`; база — `origin/main` на branch-point `8c1890e`
- **Проверено в изолированном worktree:** `/tmp/hft-rev-block1` (`git worktree add --detach`,
  `.claude/rules/branch-hygiene.md` §1 — общий чекаут не переключался)
- **Предыдущие гейты в цепочке:** `research/critiques/C-041-design-evolution.md` (critic, REJECT),
  `research/critiques/C-042-design-evolution-risk.md` (risk-critic, CONCERNS / 2 BLOCKING)

---

## ВЕРДИКТ: ❌ REJECT (CHANGES REQUESTED)

Три блокера, все — докс-уровня, все дешёвые. Ядро правки (каркас §1–§8 бит-в-бит цел,
§13–§23 содержательны, §21.1 исполняем, §22 в основном честен) принимается; блокеры —
**сломанные этим же дифом cross-references и одна ложная гарантия**, то есть ровно те
пункты, которые DOC-гейт §9 шаг 3 вменяет reviewer'у.

> **merge заблокирован до подписи founder'а (П-001).** Независимо от вердикта: правка меняет
> ФАЗЫ и ПРИОРИТЕТЫ ⇒ `.claude/rules/gates.md` §9, класс A, пункт 4 требует founder ★.
> Подписи нет. `STATUS: PROPOSED` **не переводился** в `ACTIVE`, merge в `main` **не делался**,
> `docs/PENDING-SIGNATURE.md` П-001 остаётся 🟡 (в 🟢 не переводится — вердикт не APPROVED).

---

## Блок-scope — ✅ PASS

```
$ git diff --stat 8c1890e..HEAD
 docs/06-data-layer-and-storage.md                 |  34 +-
 docs/07-cockpit-backend-roadmap.md                |  19 +-
 docs/08-arch-improvement-roadmap.md               |  11 +-
 docs/09-roadmap-v2.md                             |  13 +-
 docs/DESIGN-v2.md                                 | 787 ----------------------
 docs/DESIGN.md                                    | 747 ++++++++++++++++++--
 docs/fa/killswitch.md                             |   4 +
 docs/fa/oms.md                                    |   4 +
 docs/fa/risk.md                                   |   8 +
 docs/product/alphaquant-source.md                 | 455 +++++++++++++
 research/critiques/C-041-design-evolution.md      | 358 ++++++++++
 research/critiques/C-042-design-evolution-risk.md | 287 ++++++++
 12 files changed, 1868 insertions(+), 859 deletions(-)
```

- `crates/**`, `.claude/**`, `milestones/**`, `TECH-DEBT.md`, `PROJECT-STATE.md`, `scripts/**` —
  **не затронуты**. Кода в дифе нет ⇒ Block-C (contract-RFC) и RISK-BLOCK по коду неприменимы.
- Артефакты гейтов закоммичены СВОИМИ ролями (`branch-hygiene.md` §3):
  `e1c9e20 critic <critic@noreply.local>`, `e136e4b risk-critic <risk-critic@noreply.local>`;
  остальные 11 коммитов — `architect <architect@noreply.local>`. Untracked-вердиктов нет.
- Коммиты атомарны по шагам Б1.1–Б1.7 + отдельные коммиты на каждую находку C-041/C-042.

## Блок-риск — ✅ PASS (risk-critic в цепочке, находки закрыты фактически)

`C-042` (risk-critic) присутствует в цепочке ДО этого вердикта, обе BLOCKING-находки
перепроверены мной самостоятельно, не по отчёту:

**F-1 (покрытие `RK-I` заявлено 3, фактически 0)** — исправлено верно. Замер:

```
$ grep -rnE "\bRK-I-[0-9]+" --include='*.rs' crates/*/tests/
crates/alpha/tests/red_alpha.rs:106:/// (fail-closed: неизвестное не двигает деньги — зеркало RK-I-3).
crates/sim/tests/red_strategy_backtest.rs:161:/// ... прямой предок recon-mismatch RK-I-8).
crates/venue-hyperliquid/tests/red_fail_closed_values.rs:3://! Тот же принцип, что RK-I-3: ...
crates/venue-hyperliquid/tests/red_parse_trades.rs:128:/// Тот же принцип, что RK-I-3: ...
crates/portfolio/tests/red_portfolio.rs:3://! ⚠ Это pre-risk sanity, НЕ риск-гейт. ...
```

Все пять — комментарии-аналогии в ЧУЖИХ крейтах, один прямо отрицает, что является
риск-гейтом. Оракулов `RK-I` — **ноль**. Строка §22 (`10 / 0 / PENDING execution-фазы`)
и §21:746–749 — фактически верны.

**F-2 (исполняемые ворота входа в execution)** — восстановлены как §21.1 (`docs/DESIGN.md:779–802`),
6 пунктов, каждый с колонкой «как проверяется» (файл-оракул / прогон / архитектурная проверка
reviewer'ом), состояние «0 из 6» названо явно. Ворота **исполняемы**, не декларативны — принято.

Фактическая проверка отсутствия крейтов:

```
$ ls -d crates/risk crates/killswitch crates/oms
ls: cannot access 'crates/risk': No such file or directory
ls: cannot access 'crates/killswitch': No such file or directory
ls: cannot access 'crates/oms': No such file or directory
```

⇒ `DEFERRED`-шапки `docs/fa/{risk,killswitch,oms}.md` («Проверка: `ls crates/` — каталога нет»)
верны и не конфликтуют с §4: §4 остаётся источником канона `RK-I-1..10`, что явно сказано
в шапке `fa/risk.md`.

## Блок-каркас (не переписан ли v1) — ✅ PASS, проверено дифом, не доверием

```
$ git show 8c1890e:docs/DESIGN.md > /tmp/base-DESIGN.md
$ sed -n '35,216p' /tmp/base-DESIGN.md > /tmp/b1_8.md   # §1..§8 базы
$ sed -n '64,245p' docs/DESIGN.md   > /tmp/h1_8.md      # §1..§8 HEAD
$ diff -u /tmp/b1_8.md /tmp/h1_8.md; echo "exit=$?"
exit=0
```

**§1–§8 бит-в-бит идентичны v1.** §9 — только добавлен §9.1 (уроки, не выполненные).
§11/§12 — перемаркированы как ИСТОРИЧЕСКОЕ с сохранением исходного текста и переводом
ссылок `P1/P3/P4` на актуальные вехи (C-041 Ф3 закрыт). §10 обновлён фазами — по прямому
решению founder'а, пройденный фундамент P0/P1/P2/P2.5/P-COCKPIT показан ✅ DONE, не отброшен.
§0: отменённая НЕ-цель оставлена **зачёркнутой** (`docs/DESIGN.md:55–58`), не удалена.

## Блок-ссылки (`DESIGN.md §N`) — ⚠ CONCERNS, детали в Б1/Б2

```
$ grep -rhoE "DESIGN\.md[^)]{0,6}§[0-9]+(\.[0-9]+)?" --include=*.md --include=*.rs --include=*.sh . \
  | grep -oE "§[0-9]+(\.[0-9]+)?" | sort -uV | tr '\n' ' '
§0 §1 §1.7 §2 §3.0 §4 §6 §7 §7.1 §8 §8.1 §8.3 §9 §10 §11 §12 §13 §14 §16 §21 §22
$ ... | wc -l
88
```

88 вхождений. Все §0–§14, §16, §21, §22 резолвятся в разделы **той же темы**, что в v1 —
ни одна ссылка не указывает на раздел, сменивший смысл (проверено сопоставлением каждого
номера с заголовком: §4 → «Инварианты риск-слоя», §10 → «Фазовый роадмап», и т. д.).
Самые нагруженные: §10 (23 ссылки), §4 (15), §0 (11) — все целы.

**Не резолвятся:** `§1.7`, `§3.0`, `§7.1`, `§8.1`, `§8.3` — все пять в
`docs/plans/plan-design-migration.md` (строки 280, 302, 343, 345, 532). Это нумерация
**DESIGN-v2**, документа, удалённого этим дифом. Пример:
`docs/plans/plan-design-migration.md:343` — «`DESIGN.md` §7.1: замена источника обязана быть
тривиальной; §12 LIC-I-1» — в актуальном мастере Source Adapter это §18, LIC-I — §22.
См. Б1.

---

## БЛОКЕРЫ

### Б1 (BLOCKING) — `docs/09-roadmap-v2.md` объявлен IN EFFECT, но 20 раз ссылается на файл, удалённый этим же дифом, и в двух местах утверждает ОБРАТНОЕ решению founder'а

**Файл:строки.** `docs/09-roadmap-v2.md` — строки 58, 61, 75, 98, 113, 118, 125, 133, 152,
174, 188, 189, 195, 209, 230, 236, 241, 244, 271, 275 (20 вхождений `DESIGN-v2`).

```
$ git log --oneline --diff-filter=D 8c1890e..HEAD -- docs/DESIGN-v2.md
206f6eb docs(design): Б1.4 — DESIGN-v2.md влит в мастер и удалён
$ ls docs/DESIGN-v2.md
ls: cannot access 'docs/DESIGN-v2.md': No such file or directory
$ grep -c "DESIGN-v2" docs/09-roadmap-v2.md
20
```

Этот же диф (`6ff175f`) поднял файл до `STATUS: IN EFFECT` и `DESIGN.md:281–282` назначил его
**рабочей декомпозицией фаз** — то есть документом, в который агент идёт за деталями Ф0–Ф6.
Он приходит и читает:

- `docs/09-roadmap-v2.md:275–276` (секция `## Cross-references`) — «`docs/DESIGN-v2.md` —
  архитектура, на которую опираются фазы (§7 L2Delta, §7.1 Source Adapter, §6 read-path,
  §8 AI, §9 шов execution, §14 внешние решения)» — файла нет, нумерации нет;
- `docs/09-roadmap-v2.md:230` — «`docs/DESIGN-v2.md` → **заменяет** `docs/DESIGN.md`» —
  прямо противоположно решению founder'а об АДДИТИВНОЙ эволюции, ради которого делался Блок 1;
- `docs/09-roadmap-v2.md:242` — «`docs/DESIGN.md` §0 — НЕ-цель «Не многопользовательская SaaS»
  **удаляется**» — фактически она оставлена зачёркнутой (`DESIGN.md:55`), и это отдельный
  пункт чек-листа founder'а в П-001;
- `docs/09-roadmap-v2.md:280` — «`/tmp/alphaquant.txt` §16 — продуктовый roadmap-первоисточник» —
  **это ровно находка C-041 Ф2** (опора на файл вне репозитория). В `DESIGN.md` её закрыли
  коммитом `de80193`, в живой подчинённый документ фикс не доехал.

Почему блокер, а не NOTE: ссылки сломал **этот** диф (удаление `DESIGN-v2.md`), проверка
«cross-references не сломаны» — именной пункт reviewer'а в DOC-гейте (`gates.md` §9, шаг 3),
а документ — не архивный, он этим же коммитом повышен до действующего. Плюс два места
дезинформируют читателя о СУТИ подписываемого решения — а следующий читатель здесь founder.

Проверено, что за пределами `09-roadmap-v2.md` висячих ссылок на `DESIGN-v2` нет, кроме
`research/critiques/C-040/C-041/C-042` — это неизменяемые артефакты гейта под
`protected-artifacts`, историческая запись; их трогать нельзя и не нужно.

### Б2 (BLOCKING) — мастер-документ 15 раз цитирует источник, путь которого нигде не называет

`docs/DESIGN.md` содержит 15 цитат (на 14 строках) вида `(AlphaQuant §5.1)`, `(AlphaQuant §7.1)`,
`(AlphaQuant §9.2)`, `(AlphaQuant §12.2)`, `(AlphaQuant §17)` — строки 374, 386, 491, 493,
567, 571, 652 (×2 в одной строке), 677, 681, 700, 702, 708, 763, 775.

```
$ grep -n "product/" docs/DESIGN.md
{пусто}
$ grep -n "AlphaQuant" docs/DESIGN.md | grep -v "AlphaQuant §"
16:**многопользовательским SaaS-терминалом AlphaQuant**; ...
37:**Тезис (v2).** **AlphaQuant — многопользовательская SaaS-платформа ...
276:Роадмап v1 ... заменён **на месте** роадмапом AlphaQuant
303:### Продолжение — роадмап AlphaQuant (Ф0–Ф6, SaaS)
373:**Терминологическая коллизия ...** В AlphaQuant «Journal» — память решений
```

Файл-источник в репозиторий внесён (`docs/product/alphaquant-source.md`, 454 строки, нумерация
подразделов совпадает: `5.1` — строка 103, `7.1` — 157, `9.2` — 228, `12.2` — 321 ⇒ цитаты
физически резолвимы). Но **сам мастер-документ нигде не связывает строку «AlphaQuant §N» с этим
путём**: ни в списке подчинённых документов (`DESIGN.md:10–12` — там только `00`–`05`), ни в §0,
ни в §13. Требование C-041 Ф2 — чтобы консюмер (агент) мог резолвить ссылку — закрыто наполовину:
файл закоммичен, указатель не проведён. Форма цитат унифицирована (`a72d08e`), адресат не назван.

Сюда же, тем же одним движением: список подчинённых документов `DESIGN.md:10–12` устарел —
в нём нет `06`, `07`, `08`, `09`, хотя все четыре — живые и на них §10/§13/§16 ссылаются.

### Б3 (BLOCKING) — §18 заявляет как существующий механизм изоляции то, что код в комментарии называет ВОИД

`docs/DESIGN.md:621–623`, в блоке «**Что для этого шва УЖЕ есть [ЕСТЬ]** (проверено в коде
2026-07-29)»:

> «**изоляция эпох fail-closed:** reuse сегмента требует совпадения заголовка
> (source/**provenance**/epoch_id + SCHEMA_VERSION), при несовпадении открывается НОВЫЙ
> сегмент (`decide_open_segment`) — механизм несмешения уже существует»

Код сравнивает все четыре поля (`crates/journal/src/segments.rs:1705+`), но его собственный
комментарий и `crates/contracts/src/lib.rs:16–19` говорят прямо:

> «изоляция сегмента (reuse требует совпадения schema_version), а НЕ provenance: в
> runtime-контейнере `git rev-parse` недоступен → provenance — КОНСТАНТА на всех деплоях
> (TD-031), **поэтому изоляция по provenance ВОИД**»

`TECH-DEBT.md:112–124` (TD-031, OPEN) подтверждает: schema-гейт закрыл смешение ЭПОХ СХЕМЫ,
корень «provenance = константа» **не исправлен**. Практическое следствие ровно для сценария §18
(«прямой WS биржи → licensed-provider → ончейн-нода»): два источника, оба `DataSource::OwnCapture`,
при той же schema-эпохе и том же `epoch_id` НЕ различаются машинно — сегмент будет
переиспользован, а не открыт новый. Это и есть тот класс, который §18 объявляет закрытым
(«механизм несмешения уже существует») и на котором стоят `SRC-I-1..3`.

Почему блокер: это ЛОЖНАЯ ГАРАНТИЯ в разделе, помеченном `[ЕСТЬ]` и «проверено в коде» — тот
самый класс дефекта, ради которого DOC-гейт §9 и заводился, и второй за ночь случай, когда
покрытие заявлено больше фактического (после `RK-I` 3→0). Диагноз мой, дизайн формулировки —
зона architect (`gates.md` §4, граница reviewer↔architect): я описываю дефект, не переписываю раздел.

---

## NOTES (не блокируют merge, но должны быть закрыты или заведены в долг)

- **N1 — §22, шапка «замер 2026-07-29» устарела.** `docs/DESIGN.md:807`. Две строки таблицы
  перемерены 31.07 (`RK-I` по C-042, `VN-I` по C-041), остальные — от 29.07. Читатель не
  может отличить свежие числа от перенесённых, а «перенос числа из старого замера» — ошибка
  №1 этой ночи. Я перемерил **всю** таблицу на 31.07 (см. ниже) — расхождений, кроме N2, нет.
- **N2 — строка `JR-I` (`7 / 2`) занижает обе цифры.** `docs/fa/journal.md` объявляет
  `JR-I-1..7` (7), но `JR-I-8` («честность `next_seq`») существует де-факто с M-49 и имеет
  НАСТОЯЩИЕ оракулы (`crates/journal/tests/red_tail_integrity.rs`,
  `red_tail_integrity_prodscale.rs`, `red_tail_integrity_operator*.rs` — assert'ы «JR-I-8
  НАРУШЕН»), а в семейный FA так и не внесён. Занижение — безопасная сторона, но корень
  (инвариант живёт в `SESSION-HANDOFF.md`, не в FA) — долг маркировки того же класса, что
  закрытый в C-041 Ф1 `VN-I`.
- **N3 — §4 читается как действующий закон без пометки состояния.** §4 (15 ссылок по репо,
  второй по нагруженности раздел) бит-в-бит из v1 и не содержит указателя на §21/§22, где
  честно сказано «крейтов нет, оракулов 0». Честность документа держится на том, что читатель
  дойдёт до §21. Тот же разрыв — в `CLAUDE.md` («Риск fail-closed. `RK-I-1..10` sacred») и
  `gates.md` §5 (RISK-BLOCK триггерится на `crates/risk/**`, которого нет). Правка `CLAUDE.md`
  и `.claude/rules/*` — вне этого дифа (Блок 3, `docs/plans/plan-design-migration.md:302` это
  уже предусматривает); здесь — только как явный NOTE, чтобы не потерялось.
- **N4 — шапка `DESIGN.md:22–27` («Решения founder'а (v1, **сохраняют силу**)») содержит
  утверждение, опровергнутое §10.** «венью = Hyperliquid первым, **Binance с P5**» — Binance
  и Binance-futures MD-адаптеры закрыты в P1 (`DESIGN.md:289`, крейты существуют), а
  P-нумерации, по §11:337–341, «в актуальном роадмапе больше нет». Строка помечена как
  сохраняющая силу, хотя в этой части неверна.

## Что я перепроверил самостоятельно (сырой вывод) — таблица §22 целиком

Метод: `docs/` — сколько РАЗНЫХ ID семейства объявлено; `crates/*/tests/` — сколько разных ID
упомянуто в оракулах.

```
$ for F in SM-I VB-I OPS-I ST-I CT-I GW-I JR-I RK-I INTG-I BK-I VN-I; do
    D=$(grep -rhoE "\b${F}-[0-9]+" docs/ | sort -u -V | wc -l)
    T=$(grep -rhoE "\b${F}-[0-9]+" --include='*.rs' crates/*/tests/ | sort -u -V | wc -l)
    echo "$F docs=$D tests=$T"; done
SM-I   docs=10  tests=10
VB-I   docs=11  tests=9
OPS-I  docs=10  tests=8
ST-I   docs=10  tests=8
CT-I   docs=15  tests=5
GW-I   docs=0   tests=12
JR-I   docs=8   tests=3
RK-I   docs=10  tests=3   (все 3 — комментарии-аналогии, оракулов 0 — см. Блок-риск)
INTG-I docs=7   tests=0
BK-I   docs=8   tests=0
VN-I   docs=9   tests=2
```

Сверка с таблицей `docs/DESIGN.md:811–821`:

| Семейство | В документе | Мой замер 31.07 | Вердикт |
|---|---|---|---|
| SM-I | 10 / 10 | 10 / 10 | ✅ |
| VB-I | 11 / 9 | 11 / 9 | ✅ |
| OPS-I | 10 / 8 | 10 / 8 (ops, recorder, venue-binance — свои оракулы, не аналогии) | ✅ |
| ST-I | 10 / 8 | 10 / 8 | ✅ |
| CT-I | 6 / 5 | 6 / 5 (`docs/05-contract-layer.md` объявляет ровно `CT-I-1..6`; `CT-I-7..15` — из `docs/plans/plan-design-migration.md`, статус PROPOSED, в счёт не идут) | ✅ |
| GW-I | 0 / 12 | 0 / 12 | ✅ (обратный дрейф назван честно) |
| JR-I | 7 / 2 | 7 объявлено в FA (+`JR-I-8` вне FA) / 2 подтверждённых оракула | ⚠ N2 |
| RK-I | 10 / **0** | 10 / **0** | ✅ (C-042 F-1 закрыт фактически) |
| INTG-I | 7 / **0** | 7 / 0 | ✅ |
| BK-I | 8 / **0** | 8 / 0 | ✅ |
| VN-I | 9 / суита 40 тестов, помечено 2 | `crates/venue-hyperliquid/tests/` = **40** `#[test]` в 5 файлах; формально помеченных `VN-I-N` — **2** (`VN-I-5`, `VN-I-7`) | ✅ (C-041 Ф1 закрыт фактически) |

```
$ grep -rho "#\[test\]" crates/venue-hyperliquid/tests/ | wc -l
40
$ ls crates/venue-hyperliquid/tests/
red_fail_closed_values.rs  red_malformed_envelope.rs  red_parse_l2book.rs
red_parse_trades.rs        red_provenance_md_only.rs
```

## Внутренняя непротиворечивость §0 ↔ §4 ↔ §10 ↔ §21 — ✅ PASS (с N3)

- §0:46–48 — тезис v1 (fail-closed риск, нет LLM в торговом цикле) объявлен сохранённым и
  отнесён к отложенному execution-слою со ссылками на §4 и §21;
- §10:320–328 — `risk`/`killswitch`/`oms` **DEFERRED, не отменены**; копи-трейдинг закрыт
  решением founder'а (не отложен) — формулировки §0, §10 и §21 согласованы между собой;
- §21:746–749 + §21.1 — честная констатация «0 крейтов / 0 оракулов / 0 из 6 ворот»;
- §22 — то же число в таблице.

Противоречий не найдено. Единственный разрыв — N3 (§4 сам о своём состоянии молчит).

## Чего я НЕ проверял (честная граница)

- Продуктовое содержание `docs/product/alphaquant-source.md` (454 строки) — не моя зона;
  проверял только физическое наличие в git и резолвимость номеров подразделов.
- Числа `1.06 с vs 382 с` (§14, чекпоинты M-48) и `~8.83 GB/сут`, «125 сегментов, 85 GB»
  (§23.2) — не перемерял на проде; это замеры прошлых milestone'ов, не введённые этим дифом.
- Тесты/CI не гонял: диф не содержит кода, `crates/**` не тронут.

## Что требуется для APPROVED

Закрыть Б1, Б2, Б3 (зона architect — все три докс-правки; reviewer описывает дефект, фикс
проектирует architect, `gates.md` §4). Правки в ОТВЕТ на этот REJECT остаются в ТОМ ЖЕ цикле
гейта на этой же `PROPOSED`-ветке (`gates.md` §9, «петля самоправки»): re-review reviewer'ом,
нового вложенного гейта не требуется. C-041/C-042 повторно не гоняются — их находки закрыты
и перепроверены здесь фактически.

После этого — и ТОЛЬКО после подписи founder'а по П-001 — reviewer переводит
`STATUS: PROPOSED → ACTIVE` и мержит в `main` с деплой-гейтом `gates.md` §8.
