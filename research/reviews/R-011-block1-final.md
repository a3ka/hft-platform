# R-011 — PR-гейт (финальный круг), Блок 1: эволюция мастер-документа в редакцию AlphaQuant

**Роль:** reviewer · **Дата:** 2026-08-01 · **Гейт:** `.claude/rules/gates.md` §9 класс A (шаг 3
«reviewer — PR-гейт»), §4 PR-time
**Ветка:** `docs/design-evolution` · **HEAD:** `3ac5d28` · **База:** `origin/main` (merge-base `8c1890e`)
**Предыдущие круги:** `C-041` (critic, REJECT — Ф1–Ф4 устранены), `C-042` (risk-critic, CONCERNS —
F-1/F-2 устранены), `R-004` (reviewer, REJECT — Б1/Б2/Б3).

## ВЕРДИКТ: ❌ REJECTED — merge НЕ выполнен

**Подпись founder'а.** `docs/PENDING-SIGNATURE.md` П-001 — ✅ ПОДПИСАНО founder'ом 2026-08-01
(`origin/main` `f5fe12e`). Подпись СНИМАЕТ блокировку merge по границе C, но **не заменяет
PR-гейт** (`gates.md` §9 шаг 3/4 — это разные шаги, а не альтернативы). Гейт прогнан; найдены
два блокера, оба — тот самый класс, ради которого этот круг и назначался: **документ утверждает
о состоянии кода/инфраструктуры то, чего нет.** Подпись остаётся в силе и ждёт устранения:
после фикса — RE-review в ТОМ ЖЕ цикле (`gates.md` §9 «Петля самоправки»), новой подписи не нужно.

---

## Часть 1 — статус трёх блокеров R-004: ВСЕ ТРИ ЗАКРЫТЫ

### Б1 (R-004) — висячие ссылки на удалённый `DESIGN-v2.md` — ✅ ЗАКРЫТ

```
$ grep -c "DESIGN-v2" docs/DESIGN.md docs/09-roadmap-v2.md; echo "exit=$?"
docs/DESIGN.md:0
docs/09-roadmap-v2.md:0
exit=1
```

`docs/09-roadmap-v2.md:3` теперь: `STATUS: IN EFFECT (влито в docs/DESIGN.md §10, 2026-07-31).
Канон фаз — docs/DESIGN.md`; строка 7 — «при расхождении источник правды — §10». Противоречие
founder'у («DESIGN-v2 заменяет DESIGN») снято. Оставшиеся вхождения `DESIGN-v2` в репо —
`research/critiques/C-040..C-042`, `research/reviews/R-004` (protected-artifacts, историческая
запись) и `docs/SESSION-HANDOFF.md` (журнал сессий; на `origin/main` строка 146 уже фиксирует
«`DESIGN-v2.md` удалён»). Правки не требуют.

### Б2 (R-004) — 15 цитат источника без пути — ✅ ЗАКРЫТ

Путь назван в шапке (`docs/DESIGN.md:14–15`): «Продуктовый источник, на который ссылаются цитаты
вида «AlphaQuant §N» ниже: `docs/product/alphaquant-source.md`». Файл в репозитории (455 строк).
Проверено разрешение КАЖДОЙ цитаты в целевой раздел источника:

```
$ for s in 5.1 7.1 7.2 7.3 7.4 9.1 9.2 11.2 12.1 12.2 13 15 17; do grep -nE "^$s\.? " docs/product/alphaquant-source.md; done
103:5.1. Сущности продукта      157:7.1. Принцип           159:7.2. Роли AI
187:7.3. AI Event               192:7.4. Contextual AI tools
215:9.1. Сохраняемое проф. ядро 228:9.2. Execution realism 274:11.2. Личный агент пользователя
302:12.1. Что продаётся агентам 321:12.2. Permission model 366:15. Бизнес-модель  409:17. Риски
```

Ни одной неразрешимой цитаты.

### Б3 (R-004) — §18 заявлял существующим то, что код называет пустым — ✅ ЗАКРЫТ

§18 (`docs/DESIGN.md:619–645`, блок «Что для этого шва уже есть, по фактам кода») теперь разбит по пунктам, а не одним блоком `[ЕСТЬ]`; ключевой
пункт помечен **[ЧАСТИЧНО]** с прямым признанием: изоляция держится `SCHEMA_VERSION`, а не
provenance, потому что `git rev-parse` в контейнере недоступен ⇒ provenance — константа.
Сверено с кодом на `origin/main`:

```
$ git show origin/main:crates/contracts/src/lib.rs | sed -n '16,19p'
/// изоляции сегмента (reuse требует совпадения schema_version), а НЕ provenance: в
/// runtime-контейнере `git rev-parse` недоступен → provenance — КОНСТАНТА на всех деплоях
/// (TD-031), поэтому изоляция по provenance ВОИД.
$ git grep -n "enum EpochFilter" origin/main -- crates/
origin/main:crates/journal/src/segments.rs:87:pub enum EpochFilter {
```

Утверждения §18 совпадают с кодом. (Замечание по форме — см. N-1 ниже.)

---

## Часть 2 — собственные проверки этого круга

### 2.1 Каркас §0–§12 цел

```
$ grep -c "^## §" docs/DESIGN.md
25
$ diff <(git show origin/main:docs/DESIGN.md | grep "^## §") \
       <(git show HEAD:docs/DESIGN.md | grep "^## §" | head -14); echo "exit=$?"
exit=0
```

Все 14 заголовков §0–§12 (включая §1.5) совпадают ПОБУКВЕННО, нумерация не сдвинута.

```
$ git diff -U2 origin/main..HEAD -- docs/DESIGN.md | grep -c "^@@"
3
```

Три ханка: шапка (1–11), §0 (22–35), и от строки 231 (конец §9) до конца файла. **§1–§8 и первая
часть §9 не тронуты ни одной строкой** — то, на чём стоят ~58 внешних ссылок. §10 переписан на
месте (заявлено в шапке), §11/§12 помечены `[ИСТОРИЧЕСКОЕ]`, §13–§23 добавлены в конец.

### 2.2 Ссылки `DESIGN.md §N` — рабочие

Все внешние ссылки на §10/§11/§12 (`PROJECT-STATE.md:4`, `docs/01:187`, `milestones/BACKLOG.md:39`,
`docs/fa/{risk,oms,killswitch,venues,alpha,portfolio,strategy}.md`, `.claude/rules/gates.md:410`,
`.claude/rules/testing.md:41`, `docs/07:19`, `docs/09:3,7`) резолвятся в разделы ТОЙ ЖЕ темы: §10
остался «Фазовый роадмап», P-нумерация сохранена в таблице «Пройденный фундамент», поэтому ссылки
вида «§10, между P2 и P3 добавлена P2.5» продолжают читаться. Ни одна ссылка не ведёт на раздел,
сменивший тему.

### 2.3 DEFERRED-шапки `fa/risk` / `fa/killswitch` / `fa/oms` — верны

```
$ ls crates/ | grep -E "^(risk|killswitch|oms)$"; echo "exit=$?"
exit=1
```

Каталогов нет — шапки «крейта не существует» точны. Конфликта с §4 нет: шапки явно называют §4
источником канона `RK-I-1..10` («этот документ — детализация»), а §4 (не тронут этим дифом)
остаётся определением инвариантов. §21.1 (восстановленные по C-042 F-2 ворота входа в execution,
«состояние 0 из 6») с этим согласован.

### 2.4 Таблица §22 — ПЕРЕПРОВЕРЕНА ГРЕПОМ ПОСТРОЧНО

Метод: `git grep -hoE "\b<FAM>-I-[0-9]+" <rev> -- docs/` (заявлено) и `-- crates/*/tests/`
(в оракулах), уникальные ID. Замер на ОБОИХ деревьях: `HEAD` ветки и `origin/main` (реальность
после merge — `main` ушёл вперёд на 50 коммитов, M-49/M-50/алертинг).

```
FAM     docs(HEAD)   tests(HEAD)   docs(main)   tests(main)
SM-I    10           10            10           10
VB-I    11           9             11           9
OPS-I   10           8             10           8
ST-I    10           8             10           8
CT-I    15           5             15           5
GW-I    0            12            0            12
JR-I    8            3             8            4
RK-I    10           3             10           3
INTG-I  7            0             7            0
BK-I    8            0             8            0
VN-I    9            2             9            2
```

Построчный вердикт по таблице §22 (`docs/DESIGN.md:830–842`):

| Строка §22 | Заявлено/В оракулах в документе | Замер | Вердикт |
|---|---|---|---|
| SM-I | 10 / 10 `[ЕСТЬ] полное` | 10 / 10 | ✅ честно |
| VB-I | 11 / 9 | 11 / 9 | ✅ честно |
| OPS-I | 10 / 8 | 10 / 8 | ✅ честно |
| ST-I | 10 / 8 | 10 / 8 | ✅ честно |
| CT-I | 6 / 5 | 15 / 5 в греп-замере | ✅ по сути (см. N-2) |
| GW-I | 0 / 12 «обратный дрейф» | 0 / 12 | ✅ честно |
| JR-I | 7 / **2** | 8 / **4** на `main` | ⚠ F-1 (занижено) |
| RK-I | 10 / **0** PENDING | 10 / 3 попадания — **все 5 комментарии-аналогии** | ✅ честно, C-042 F-1 закрыт |
| INTG-I | 7 / 0 | 7 / 0 | ✅ честно |
| BK-I | 8 / 0 | 8 / 0 | ✅ честно |
| VN-I | 9 / сноска «40 тестов, 2 помечены» | 40 `#[test]`, 2 ID | ✅ честно, C-041 Ф1 закрыт |

Пруф по двум ранее солгавшим строкам:

```
$ git grep -nE "RK-I-[0-9]+" origin/main -- 'crates/*/tests/*'
crates/alpha/tests/red_alpha.rs:106:/// (fail-closed: ... — зеркало RK-I-3).
crates/portfolio/tests/red_portfolio.rs:3://! ⚠ Это pre-risk sanity, НЕ риск-гейт. Настоящий барьер (`RK-I-1..10`,
crates/sim/tests/red_strategy_backtest.rs:161:/// ... прямой предок recon-mismatch RK-I-8).
crates/venue-hyperliquid/tests/red_fail_closed_values.rs:3://! Тот же принцип, что RK-I-3: ...
crates/venue-hyperliquid/tests/red_parse_trades.rs:128:/// Тот же принцип, что RK-I-3: ...
$ git grep -h "^#\[test\]" origin/main -- 'crates/venue-hyperliquid/tests/*' | wc -l
40
```

Пять попаданий `RK-I-*` — все до единого комментарии-аналогии в чужих крейтах, ни одного оракула;
«0» в §22 и формулировка §21 («упоминания `RK-I-*` в коде — комментарии-аналогии») **подтверждены
фактом**. VN-I: 40 тестов, 2 маркированных ID — сноска точна.

**Итог 2.4:** ложных ЗАВЫШЕНИЙ покрытия в таблице §22 нет — обе прошлые лжи (RK-I, VN-I)
устранены по факту. Осталось одно занижение (F-1) и один вопрос формы (N-2). Но ложное
утверждение о состоянии переехало из §22 в §10 и §22-SRC — см. блокеры.

---

## БЛОКЕРЫ

### Б-1 (BLOCKING) — §10 объявляет фазу P2.5 пройденной, а её acceptance-ворота — выполненными; ни то, ни другое не верно, и документ противоречит сам себе трижды

**Файл:строка.** `docs/DESIGN.md:295` — строка таблицы **«Пройденный фундамент (✅ DONE — не
пересчитывается)»**:

```
| **P2.5 Data safety net** ✅ (в основном; хвост докатывается в Ф0) | ops-слой: cold-copy журнала
offsite, recon локальной книги с REST-снапшотом биржи, `/metrics`+алерты P0/P1/P2 | backup
restore-drill проходит; инъецированная порча книги (ε_test) ловится recon'ом и алертит;
двусторонний паритет «инцидент→алерт→метрика» | ★ принято |
```

Три утверждения в этой строке ложны на момент merge:

1. **«cold-copy журнала offsite»** в колонке содержимого выполненной фазы. Его нет.
2. **«backup restore-drill проходит»** — настоящее время, в колонке acceptance-ворот фазы,
   помеченной ✅. Drill не проводился ни разу.
3. **«★ принято»** — подпись founder'а на приёмку фазы. Приёмка restore-drill'а стоит в очереди
   как НЕ выполненная — в этой же таблице, двумя строками ниже.

**Документ противоречит сам себе** (все цитаты — из этого же дифа):

```
$ grep -n "offsite" docs/DESIGN.md
295:| **P2.5 Data safety net** ✅ ... cold-copy журнала offsite ...
316:| **Ф0 Необратимость данных** 🚧 в работе | ... offsite-бэкап+restore-drill (R1) ...
     | ★ приём restore-drill'а          ← та же таблица: ещё не сделано, подпись ещё не дана
909:**только dry-run** (offsite-переноса НЕТ — R1).                        ← §23.2
943:1. **Сейчас (блокер):** offsite COLD (R1) + алертинг (§23.1) — до любого роста нагрузки.
```

`docs/DESIGN.md:440` (§14, таблица проекций): `COLD: архив сегментов ... [ЧАСТИЧНО]: компакция
ЕСТЬ; offsite-копии НЕТ (R1)`. `docs/DESIGN.md:273` (§9.1 «уроки, которые мы ЕЩЁ НЕ выполнили»):
`Offsite-копия данных | … | журнал в ЕДИНСТВЕННОЙ копии; ретеншен-apply ни разу не выполнялся (R1)`.

**Реальность на `origin/main`:**

```
$ git show origin/main:docs/PENDING-SIGNATURE.md | sed -n '82,88p'
### 2. Storage Box (Hetzner) — журнал по-прежнему в ОДНОЙ копии
141.5 млн событий существуют на одном диске. ... Инфраструктура восстановления готова;
**второй копии нет**.
Это единственный оставшийся риск, который не закрывается кодом.
$ git show origin/main:TECH-DEBT.md | grep -n "НИ РАЗУ"
505:  `--mode apply` не было выполнено НИ РАЗУ — offsite cold-выгрузки на прод-журнале не произошло.
$ git show origin/main:PROJECT-STATE.md | grep -n "restore-drill"
992:- **M-09 остаётся 🚧 ACTIVE:** task 3 (сохранность/restore-drill — заблокирован Storage Box founder ★)
```

**Почему это блокер, а не NOTE.**
Заголовок таблицы — «✅ DONE — **не пересчитывается**»: это прямая инструкция следующему агенту
не перепроверять строку. Помеченный так пробел не переоткрывается никогда — он становится
постоянным. Речь при этом о **R1 — единственном риске проекта, который не закрывается кодом**
(формулировка самого founder'а в П-003): 141.5 млн событий в одной копии. Из четырёх фаз таблицы
только у P2.5 колонка acceptance не несёт вердикта («— ЗЕЛЁНЫЙ» / «— ПРИНЯТО», как у P0/P1/P2/
P-COCKPIT) — то есть её критерии читаются как утверждения о факте, и хедж «(в основном; хвост
докатывается в Ф0)» не называет, ЧТО именно в хвосте, — а в хвосте вся сеть безопасности данных.
Это ровно тот класс дефекта, который за сутки трижды ловили в этом документе (`C-041` Ф1 VN-I,
`C-042` F-1 RK-I, `R-004` Б3 §18), только направление опаснее: **не «занижено покрытие», а
«заявлена несуществующая защита»**.

Фикс не проектирую (`gates.md` §4, граница reviewer↔architect): дефект описан, зона — architect.

### Б-2 (BLOCKING) — §22 SRC-I-3 помечен `[ЕСТЬ]` и обещает различимость источников, которую §18 этого же документа объявляет отсутствующей

**Файл:строка.** `docs/DESIGN.md:866`:

```
| SRC-I-3 | Провенанс каждого события неизменяем (задним числом не проставляется — уже закреплено
в T1) и виден в реплее: **читатель всегда может отличить источники**; `EpochFilter` обязателен [ЕСТЬ] |
```

**Противоречие с §18** (`docs/DESIGN.md:630–640`, тот же диф, коммит `3ac5d28` — фикс Б3):

> `provenance` строится через `git rev-parse` в рантайме, а в контейнере без git это КОНСТАНТА на
> всех деплоях — изоляция ИМЕННО ПО ИСТОЧНИКУ этим не гарантирована. Два разных источника той же
> schema-эпохи и того же `epoch_id` ... **машинно НЕ различаются**

Подтверждено кодом (`crates/contracts/src/lib.rs:16–19` на `main`: «изоляция по provenance ВОИД»,
TD-031 OPEN). `source_kind`, который должен закрыть пробел, — п.1 списка `[ПЛАН]` в §18.

Строка §22 читается как «инвариант уже выполнен» (`[ЕСТЬ]` + «уже закреплено в T1» + утверждение
в настоящем времени), тогда как выполнен из неё только последний клауз про `EpochFilter`.
Это тот же дефект, что `C-042` F-1 (ложное покрытие `RK-I` в таблице инвариантов), в той же
таблице, найденный после того как F-1 устранили. Таблица инвариантов — место, куда агент идёт
за ответом «на что можно опереться»; ложное `[ЕСТЬ]` здесь дороже любого другого места документа.
Отдельно: SRC-I-3 — предусловие смены источника данных (§18/§20), то есть путь, где ошибка
означает молча смешанные потоки в бессмертном журнале.

---

## Находки не-блокирующие (устранить в том же цикле)

**F-1 (MAJOR) — §22, строка JR-I: `7 / 2` занижает и заявленное, и покрытие.**
Замер: `docs(main)=8`, `tests(main)=4` (`JR-I-1`, `JR-I-4`, `JR-I-8`, `JR-I-9`). `JR-I-8` (M-49,
честность `next_seq`) несёт ЧЕТЫРЕ выделенных sacred-файла оракулов (`crates/journal/tests/
red_tail_integrity{,_prodscale,_operator,_operator_prodscale}.rs`), `JR-I-9` (M-50, скан пола
видит крупные события) — суиту `fs_1..fs_10`. Замер в §22 датирован 2026-07-29 и честно помечен
датой, но после merge документ становится ACTIVE-мастером поверх `main`, где эти два инварианта —
самая свежая защита event-store'а, названного в §13 единственным источником истины продукта.
Класс — тот же, что `C-041` Ф1 (VN-I «0 тестов» при живой суите), направление — занижение.

**F-2 (MINOR) — §9.1 и §23.1: «алертинга НЕТ вовсе» устарело.**
`docs/DESIGN.md:272` («алертинга НЕТ вовсе (R10в)») и §23.1 п.1 («[ПЛАН], первым шагом —
cron-watchdog → Telegram»). На `origin/main` алертинг смержен (`cc5197c`, вердикт `R-010` после
четырёх кругов), `crates/ops/src/bin/ops-watchdog.rs` + `scripts/watchdog_cron.sh` +
`docs/runbooks/alerting.md` существуют, прод проверен §8 eyes-on; не хватает только
`TELEGRAM_BOT_TOKEN` + строки cron (П-003, действие founder'а). Направление занижающее (алерт
действительно ещё не доставляется), поэтому MINOR, но формулировка «нет вовсе» после merge
неверна фактически.

**N-1 (NOTE) — §18 цитирует код по номерам строк, которые уже уехали.**
`docs/DESIGN.md:630` ссылается на `crates/journal/src/segments.rs:1749–1758`; на `origin/main`
`decide_open_segment` — строка 1882 (`git grep -n "fn decide_open_segment" origin/main`). Так же `TECH-DEBT.md:112–124` (TD-031, цитируется в `docs/DESIGN.md:632`) — на `main` в этих строках другой долг. Суть проверяема по имени
символа, но номера строк гниют за дни. Якорить на символ/ID, не на строку.

**N-2 (NOTE) — §22, строка CT-I `Заявлено 6`.**
Греп по `docs/` даёт 15 ID (`CT-I-1..15`): `CT-I-7..15` — предложения Блока 2 в
`docs/plans/plan-design-migration.md` (STATUS: PROPOSED), канон же — `docs/05-contract-layer.md`
§6 (`CT-I-1..6`). Цифра 6 по сути верна; стоит назвать источник счёта, иначе следующая
машинная сверка «объявлено ⟹ проверено» (сам CT-I-14 из того плана) даст ложное расхождение.

**N-3 (NOTE) — `docs/plans/plan-design-migration.md` продолжает адресовать нумерацию `DESIGN-v2`**
(`§1.7`, `§3.0`, `§7.1`, `§8.1`, `§8.3` — строки 280, 302, 343, 345, 532; отмечено ещё в `R-004`).
Файл — PROPOSED-план, по которому пойдёт Блок 2; после удаления `DESIGN-v2.md` эта нумерация не
резолвится (актуально: Source Adapter — §18, LIC-I — §22). Вне зоны этого дифа, поэтому NOTE, но
в Блок 2 нельзя входить с ним как есть.

---

## Что проверено и зелено (для протокола)

- Scope: диф ветки — `docs/**` + `research/{critiques,reviews}/**`, ни одной строки в
  `crates/**`, `contracts/**`, `scripts/**`, `milestones/**`. Класс A по `gates.md` §9, зона
  architect'а не превышена.
- Artefacts: `C-041`, `C-042`, `R-004` закоммичены на ветке (`branch-hygiene.md` п.3),
  protected-artifacts не нарушен.
- Merge-совместимость: пересечение изменённых файлов ветки и `main` — ПУСТО
  (`comm -12` по двум `git diff --name-only` от merge-base), конфликтов при `--no-ff` не будет.
  Ветка отстаёт от `main` на 50 коммитов, но ни один файл `main` не удаляется мержем.
- Атомарность коммитов: 18 коммитов, каждый — одна находка/задача с ссылкой (`Б1.1`…`Б1.7`,
  `C-041 Ф1/Ф2/Ф3+Ф4`, `C-042 F-1/F-2`, `R-004 Б1/Б2/Б3`), идентичности ролей соблюдены.
- risk-critic в цепочке: `C-042` присутствует, обе находки устранены (проверено фактом, п. 2.4).

## Условие снятия REJECT

Устранить Б-1 и Б-2 (+ F-1, F-2 в том же заходе), вернуть на RE-review в ЭТОТ ЖЕ цикл гейта
(`gates.md` §9 «Петля самоправки» — новый вложенный гейт не заводится). Подпись founder'а П-001
остаётся действующей и повторно не запрашивается: она дана на СОДЕРЖАНИЕ решения (аддитивная
эволюция в AlphaQuant), а блокеры — о фактической точности утверждений, не о решении.

`STATUS: PROPOSED` в шапке `docs/DESIGN.md` **не меняется** — перевод в `ACTIVE` делает reviewer
на мерже, мержа нет.
