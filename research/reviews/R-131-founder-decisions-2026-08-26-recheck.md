<!-- GATE-META
milestone: P-019
audited_repo: a3ka/hft-platform
audited_base: 38671e7f978b0c614a98e08b337520d9d03ddfcd
audited_head: 0aaae1c5dcc94bb51a2cd7eaf60c744bbe74edfb
verdict: REJECT
-->

# R-131 — перепроверка §9: решения founder'а 2026-08-26 (`П-017` A2 + `П-019` + `tester.md` 1bis) — **REJECT**

**Роль:** независимый перепроверяющий (`gates.md` §9, Fable, свежий контекст; НЕ автор).
**Дата (UTC):** 2026-08-26 · **Предмет:** ветка `docs/founder-decisions-2026-08-26`,
`0440258` (`docs/PENDING-SIGNATURE.md` +86) и `0aaae1c` (`.claude/agents/tester.md` +29/−1),
два коммита поверх `origin/main` = `38671e7` (сверено: `git rev-parse origin/main` →
`38671e7…`, merge-base = тот же SHA — ветка не отстаёт, дерево предмета = дерево слияния).
**Чекаут:** свой worktree `/tmp/hft-recheck-r131` (detached `0aaae1c`), чужие деревья не тронуты.
**Мутация — в отдельном `git clone`** `/tmp/hft-mut-r131` (не `cp -a`): ловушка с общим
`.git` учтена, ветка предмета после мутации сверена — `0aaae1c` на месте.

**Ярус C прочитан ГРЕПОМ по предмету, не целиком:** `TECH-DEBT.md` по `TD-167` (найдено:
карточка `:100` + заводящая строка `:1149`); дополнительно — поиск предшественников класса
«роль прогнала не тот список» по `research/reviews|critiques|arbitration` и `TECH-DEBT.md`
(паттерны `не тот список|неполный список|прогнал не|мандат.*ссылк`).

**Предъявление FA (M-66):** диапазон `38671e7..HEAD` НЕ трогает `crates/**` — барьер
`check_review_fa.sh` даёт `SKIP`; называю это явно, тело моего коммита несёт
`FA-WAIVER: crates/none — …`.

---

## Вердикт одной строкой

Оба решения founder'а зафиксированы верно по существу, полномочия и границы соблюдены,
все механические барьеры зелёные, мутация наблюдаемости воспроизведена. **REJECT — за две
ложные цитаты в двух самых долгоживущих нормоносителях** (подписанная запись `П-019` и
впрыскиваемый профиль `tester.md`): находки `R-130` перебиты между идентификаторами
B-2/B-3, а перечень срабатываний класса противоречит собственному итогу «шесть». Это ровно
класс «документ X говорит Y без открытия X», против которого написаны обе правки. Правка —
один корректирующий коммит; условие APPROVED — в конце.

---

## Блокирующие находки

### Б-1 — `П-019` и `tester.md` приписывают `R-130` B-3 то, что в `R-130` является частью B-2; настоящий B-3 не упомянут вовсе

`docs/PENDING-SIGNATURE.md:1190` (строка таблицы «Замер, из-за которого решение принято»):

> `| B-3 | пять запретов A-023 §4.2 подпёрты пересказом вместо сырого stdout | …`

`.claude/agents/tester.md:52`:

> `…а пять запретов подпёрты пересказом вместо сырого stdout (B-3 — прямое нарушение…)`

Открываю `R-130` (единственная ревизия, `origin/feat/M-68-rev3`):

```
$ git show origin/feat/M-68-rev3:research/reviews/R-130-M-68-a023-closing-commit.md \
  | grep -n '^\*\*B-'
→ B-1 — tester прогнал СПИСОК ИЗ ТЕЛА КОММИТА, а не список A-023 §4.3.
→ B-2 — команда №9 прогнана на НЕ ТОМ диапазоне.
→ B-3 — FA-WAIVER в отчёте tester'а выписан на ложной посылке.
```

Факт «таблица запретов подпёрта прозой … пересказом вместо сырого stdout» в `R-130` —
**вторая половина B-2**, не B-3. Число «пять» в `R-130` при этой находке не стоит вовсе
(оно выводимо из «пять из пяти запретов §4.2» в итоге, но это МОЙ вывод, не цитата).
Настоящий B-3 (`FA-WAIVER` на ложной посылке — FA крейта живёт в `docs/fa/viz-backend.md`)
из обеих правок исчез, а его идентификатор занят чужим содержимым.

Почему это блокер, а не изложение: `П-019` подана как ЗАМЕР-основание подписанного решения,
`tester.md` — впрыскивается роли при каждом старте; будущий греп «`R-130` B-3» из любого из
этих носителей приводит к находке, которой в `R-130` под этим номером нет. Это тот самый
класс, за который `A-023` §1.1 п.1 осудил `C-164` («приписывает C-162 то, чего тот не
требовал — симптом из `reading-map.md` §3»), и тот, за который профиль architect'а уже
дважды получал REJECT (`R-114` Б-1, `R-121` Б-1). Заодно ложной становится и вводная
`docs/PENDING-SIGNATURE.md:1183` «три находки одного корня, B-1/B-2/B-3»: у настоящего B-3
корень другой (незнание карты FA, не мандат-ссылка).

**Что править:** перебить таблицу `П-019` на честную структуру `R-130` — B-1 (не тот список,
8 из 10), B-2 (неверный диапазон + запреты пересказом), B-3 (FA-WAIVER на ложной посылке,
класс `TD-165`) — либо явно сказать, что решение опирается на B-1 и B-2, а B-3 иного корня;
в `tester.md:52` пересадить «пересказ вместо stdout» с `B-3` на `B-2`. «Пять» оставить можно
только как выводимое из §4.2 число с указанием источника, не как цитату вердикта.

### Б-2 — перечень срабатываний в амендменте `П-017` A2 называет СЕМЬ предметов и итожит «шесть»

`docs/PENDING-SIGNATURE.md:967`:

> «`C-160` F1 (пин к 8), `C-162` F1b (эпоха 7), `C-164` (ложное самоописание) плюс четыре
> битые ссылки … — **шесть срабатываний на ОДНОМ милестоуне**»

Счёт по перечню: 1 + 1 + 1 + 4 = **семь**. Пересечения нет: пин-8, эпоха-7 и самоописание
живут в `crates/gateway/tests/red_gateway_schema_version.rs`, четыре ссылки — в
`milestones/M-68-depth-from-book.md` (`A-023` §2.1, таблица; в ней C-164 F1a — это `:224` и
`:243`, то есть ДВЕ из четырёх ссылок, а не самоописание). Арбитр итогового числа не называл
(«≥3 срабатывания», §2.4); «шесть» пришло, судя по дословному совпадению, из `TD-167`
(`TECH-DEBT.md:100`, зона reviewer'а, уже в `main`) — и там тот же дефект перечня.

Согласованное прочтение «шести» существует, и правка обязана выбрать его явно: под шапкой
класса «нормативный литерал вне объявления» срабатываний ровно шесть — `C-160` F1 + `C-162`
F1b + четыре ссылки §2.1 (две из которых нашёл `C-164` F1a); «ложное самоописание» `C-164` —
экземпляр СОСЕДНЕГО подкласса («документ ложно описывает себя»), который сам амендмент
пятнадцатью строками ниже объявляет немеханизируемым — то есть в профиль срабатываний ПРОБЫ
он не входит. Порог ≥3 выполнен при любом счёте — решение founder'а находка не подрывает;
подрывается точность записи, которая существует ради точности.

**Что править:** либо `…C-160 F1 (пин к 8), C-162 F1b (эпоха 7) плюс четыре битые ссылки
(две — C-164 F1a) — шесть…`, либо честное «семь» с расширенной шапкой класса. Тем же заходом
сказать reviewer'у про тот же перечень в `TD-167` (его зона, не автора).

---

## (а) Утверждения о коде — каждое командой, на дереве слияния

Ветка = два коммита строго поверх `origin/main` (`38671e7`), merge-base совпадает с `main`
⇒ дерево предмета И ЕСТЬ дерево слияния. Дополнительно:
`bash scripts/verify_design_claims.sh --merge-preview origin/main` → **exit=0, VERDICT: PASS (0 нарушений)**.

Поимённо по мандату:

1. **Цитаты `A-023` §2.2/§2.3 в амендменте — дословны.** Открыт `A-023` (branch-carrier):
   «значение нормы проба ПАРСИТ из объявления (своего литерала не имеет)» ✓; «маркер именует
   событие (`M-NN`/дата), а не выдаёт безымянную индульгенцию» ✓; «голый `\b9\b` запрещён как
   решающий признак — он не отличает версию 9 от задачи 9» ✓; «проба … сама под пробу с
   fail-closed setup … и под адверсария харнесс-трека» — амендмент пишет «fail-closed setup
   на каждый сценарий … под адверсария со свежим контекстом»: «на каждый сценарий» — из
   `testing.md` §«Целостность гейта» п.3, «свежий контекст» — требование самого
   `harness-track.md` §6; арбитру не приписано ничего, чего он не говорил. «Проба 1 …
   ОТКЛОНЕНА арбитром по существу» ✓ (§2.2: «ОТКЛОНЯЕТСЯ в предложенной форме»).
   Единственная неточность конструкции — состав перечня срабатываний: **Б-2**.
2. **Счёт «шесть»** — НЕ сходится с собственным перечнем: **Б-2** выше.
3. **Числа из `R-130`:** «восемь команд вместо десяти» — буквально (B-1: «все 8 закрывающих
   команд; в §4.3 их десять») ✓; «не прогонялась `verify_M-68.sh`» — буквально (B-1: «Не
   прогнано ни разу до меня … bash scripts/verify_M-68.sh») ✓; «одна команда на неверном
   диапазоне» — буквально (B-2, команда №9) ✓; «пять запретов подпёрты пересказом» — в
   `R-130` НЕ буквально и не под тем идентификатором: **Б-1**.
4. **`commit-discipline.md` «пересказ = NOT REVIEWED»** — открыт:
   `commit-discipline.md:61` «Пересказ … без сырого вывода = "NOT REVIEWED — RESUBMIT WITH
   DONE BLOCK" от reviewer'а». Сжатие в кавычках допустимо-точное; секция — §Done Block ✓.
5. **`check_context_budgets.sh` не считает профили** — подтверждено кодом и прогоном:
   `builtin_table()` (`scripts/check_context_budgets.sh:69-78`) несёт РОВНО 6 файлов
   `.claude/rules/*` + `CLAUDE.md`, ни одного `.claude/agents/*`; прогон на дереве предмета
   (с выросшим `tester.md`): `VERDICT: PASS — 7 файлов, 111943 B из 114900 B`, **exit=0** ✓.
6. **«`A3` не задет»** — открыт `П-017` A3: мораторий на «новые нормативные единицы во
   впрыскиваемом ядре»; предмет разморозки — скрипт-проба, механизм, вне ядра — для
   АМЕНДМЕНТА утверждение верно ✓. Оговорка ниже — N-2.
7. **Нумерация не сдвинута** — `tester.md:37` `1bis.`, `:85` п.7 на месте; греп по корпусу:
   на пункты профиля ссылаются ПО ИМЕНИ («ШАГ 0» — 10+ файлов, остаётся п.2), ссылок вида
   «tester.md п.N» не найдено — ломать нечего ✓.
8. **Четыре барьера в прод-форме CI** (`EVENT_NAME=pull_request`,
   `PR_BASE_SHA=38671e7…` — форма из `.github/workflows/ci.yml:59-62,100-103,200-203,270-271`),
   решение по коду возврата (первый мой замер через пайп был невалиден — `$?` принадлежал
   `tail`; перепрогнано без пайпа):
   ```
   check_docs_freeze          exit=0
   check_protected_artifacts  exit=0  (OK: … целы на HEAD (38671e7..HEAD…))
   check_artifact_ids         exit=0  (OK: ни один новый артефакт не введён — класс П
                                       файлового носителя не имеет, барьеру не подведомствен)
   check_context_budgets      exit=0  (VERDICT: PASS — 7 файлов, запас 2957 B)
   ```
9. **Мутация наблюдаемости — воспроизведена в изолированном клоне** (`git clone`, свой
   `.git`; НЕ `cp -a`):
   ```
   $ git log -1 --format=%B | grep -c 'FOUNDER-APPROVED'   → 0 (после amend без токена)
   $ EVENT_NAME=pull_request PR_BASE_SHA=38671e7… bash scripts/check_docs_freeze.sh
   exit=1                                                   ← токена нет — барьер красный
   # восстановление: git reset --hard FETCH_HEAD; grep -c 'FOUNDER-APPROVED' → 1
   # ветка предмета: git rev-parse origin/docs/founder-decisions-2026-08-26 → 0aaae1c… (не тронута)
   ```
   Предел, найденный попутно (вне предмета, автору не вменяется): fail-путь барьера НЕМОЙ —
   `scripts/check_docs_freeze.sh:105` голый `exit 1` без строки FAIL; в CI красный джоб виден,
   но причину лог не называет. Кандидат в харнесс-очередь, не блокер.

## (б) Полномочия

- **Зона:** `docs/PENDING-SIGNATURE.md` — `docs/**`, зона architect'а (вне
  reviewer-owned `PROJECT-STATE`/`TECH-DEBT` — оба НЕ тронуты: диф ровно два названных
  файла, `git show --stat` обоих коммитов); `.claude/agents/tester.md` — зона architect'а ✓.
- **Замок §11 покоммитно:** `0aaae1c` (трогает `.claude/agents/*`) несёт
  `FOUNDER-APPROVED: решение founder'а 2026-08-26, запись П-019 …` (≥12 символов) ✓;
  `0440258` зону §11 не трогает — токен не требуется ✓. Барьер кусается — мутация п.9.
- **Граница C (`gates.md` §0.1):** ничего не делегировано — оба решения приняты founder'ом
  («да, разрешаю» ×2, дословность зафиксирована), архитектор — писарь; арбитру авторитет
  не занят (см. вопрос 1 ниже); порог разморозки — тот же, что founder сам применил 22.08 ✓.
- **Scope-guard:** правок кода, контрактов, тестов, verify-скриптов — ноль ✓.

## (в) Связность

- `A-020` (`research/arbitration/…:152-160`) — в `main`, цитата дословна ✓.
- `harness-track.md` — в `main`, маршрут применим (проба = `scripts/`-харнесс, не
  прод-процесс; три вопроса §4 дают три «нет») ✓.
- `TD-167` — в `main` (`TECH-DEBT.md:100`, `:1149`) ✓ (с оговоркой Б-2 о перечне).
- `binding-requires-mechanism` — `docs/workflow/binding-requires-mechanism.md` в `main` ✓.
- `gates.md` §3/§11, `commit-discipline.md` §Done Block, `handoff-block.md`,
  `reading-map.md` §2 — действующие тексты ✓.
- **N-1 (носитель не назван):** `A-023` и `R-130` в `main` ОТСУТСТВУЮТ
  (`git cat-file -e origin/main:research/arbitration/A-023-…` → exit=128; то же `R-130`) —
  живут только на `feat/M-68-rev3`, которая вливается лишь после GREEN реализации M-68.
  До того читатель `main`, следуя ссылкам `П-017`/`П-019`, упирается в отсутствующие файлы.
  Прецедент решён в самом `П-017`: «вердикты `C-113`/`C-114` на ветке
  `docs/plan-abc-2026-08-20`» — носитель назван. Тем же корректирующим коммитом назвать
  ветку-носитель для `A-023`/`R-130`.

## Четыре вопроса по существу

1. **Правило границы `A-020` — исправленная формулировка честна.** Текст `A-020:156-157`
   открыт: антецедент — «два круга подряд», по классу предъявлен один вердикт; правка
   говорит именно это («триггер НЕ взведён … предъявлен ОДИН вердикт») и цитирует правило
   дословно. Авторитет арбитра не занимается: ранний выбор fail-closed конструкции ничем в
   `A-020` не запрещён и не нуждается в его санкции. Одна стилистическая неточность, не
   блокер: цитируемый фрагмент — ПРЕДПИСАНИЕ правила (консеквент), а `П-019` подаёт его как
   «основание, которым `A-020` его мотивирует»; мотивировка у `A-020` — абзацы про круги
   6–7, не эта строка. Смысла не искажает, при правке по Б-1/Б-2 можно поправить заодно.
2. **Место нормы — принимающая сторона выбрана ВЕРНО, дефекта нет.** Пишущая сторона нормой
   УЖЕ накрыта (`handoff-block.md` п.3 «промпт самодостаточен») — и класс воспроизвёлся
   сквозь неё: это прямое свидетельство, что третья строка прозы там ничего бы не добавила
   (`binding-requires-mechanism`: после повторения класса проза запрещена — а на пишущей
   стороне это было бы именно повторение прозы). Отказ принимающей стороны самоисполним И
   ПРИНУЖДАЕТ пишущую: мандат-ссылка теперь возвращается `INVALID`, то есть давление на
   место рождения дефекта создано, а не снято. Остаток называю честно: покрыт ТОЛЬКО tester;
   мандаты critic/reviewer/арбитра могут по-прежнему называть проверку ссылкой (в `R-130`
   сам reviewer спасал положение). Это остаток объёма founder-решения («инструкцию для роли
   тестера»), не дефект правки; станет вторым кругом класса — `A-020` п.1 взведётся честно.
3. **`COGNITIVE-ONLY` законен — класс ПЕРВЫЙ раз.** Поиск предшественников
   (`research/reviews|critiques|arbitration`, `TECH-DEBT.md`; паттерны в шапке): точного
   класса «мандат назвал проверку ссылкой → роль прогнала не тот список» до `R-130` нет.
   Соседние классы существуют и УЖЕ несут свои пункты в том же профиле: M-54 «не тот
   предмет» (ШАГ 0, п.2), `TD-036`/RN-18 «SHA не на origin» (п.1) — это иная ось (identity
   предмета, не состав списка); `R-067:198` «неполный список» — о перечне джобов CI, не о
   мандате роли. Первое повторение ⇒ вторая ветвь `binding-requires-mechanism` (тег +
   причина) применена правомерно, причина немеханизируемости названа и верна (мандаты живут
   в переписке, CI видит артефакты).
4. **Разморозка НЕ шире подписанного.** Амендмент называет ОДИН барьер таблицей, повторяет
   «на ПРОЧИЙ новый харнесс заморозка действует по-прежнему», отдельным абзацем перечисляет,
   чего разморозка НЕ даёт (не закрывает `TD-167`; не распространяется на пробу 1). Кандидат
   на механизацию из `П-019` (закрывающая проверка исполняемым артефактом) разрешённым НЕ
   читается: и `П-019`, и `tester.md` 1bis прямо пишут «строить … нельзя до отдельной
   разморозки `П-017` A2», разморозка 26.08 «названа поимённо и покрывает только пробу
   самосогласованности». Протечки нет.

## Прочие наблюдения (не блокеры)

- **N-2 — `A3` и сам `tester.md` 1bis.** «`A3` не задет» верно для ПРОБЫ (механизм). Новый
  BINDING-пункт в профиле роли — нормативная единица во впрыскиваемом (роли tester) файле;
  формально мораторий A3 сформулирован про «ядро», операционализированное бюджетом
  (`CLAUDE.md` + rules), профили в него не входят — но дух A3 (рост обязательного чтения)
  правка задевает: +29 строк каждому старту tester'а. Покрыто это тем, что мораторий и
  разморозку выдаёт одна и та же подпись: founder явно велел «поправить инструкцию для роли
  тестера». Фиксирую для следа, действий не требую.
- **N-3 — немой fail-путь `check_docs_freeze.sh:105`** — см. (а) п.9. Вне предмета.

## Условие APPROVED

Один корректирующий коммит на этой же ветке (для `tester.md` — со своим
`FOUNDER-APPROVED`, причина прежняя — исполнение того же решения):

1. **Б-1:** `docs/PENDING-SIGNATURE.md:1183-1190` — таблица замера приведена к настоящим
   B-1/B-2/B-3 `R-130` (или явная оговорка, что решение стоит на B-1/B-2, а B-3 иного
   корня); `.claude/agents/tester.md:52` — «пересказ вместо stdout» пересажен с `B-3` на
   `B-2`; счёт «пять» либо снят, либо помечен как выводимый из §4.2, а не цитата.
2. **Б-2:** `docs/PENDING-SIGNATURE.md:967` — перечень и итог согласованы (класс-чистые
   «шесть» без «C-164 (ложное самоописание)» в перечне, либо «семь» с расширенной шапкой);
   след правки оставлен, не молчаливая замена.
3. **N-1:** ветка-носитель `feat/M-68-rev3` названа при первых ссылках на `A-023` и `R-130`
   (по прецеденту `П-017` B).

Повторной полной перепроверки не требуется: правка по пунктам 1–3 — изложение фактов уже
проверенных источников; достаточно предъявить diff и грепы построчно в ответе автора. Всё
остальное — полномочия, границы, барьеры, мутация, четыре вопроса по существу — ПРОЙДЕНО.

## Done Block (перепроверяющего)

```
$ git rev-parse origin/main && git merge-base origin/main HEAD
38671e7f978b0c614a98e08b337520d9d03ddfcd
38671e7f978b0c614a98e08b337520d9d03ddfcd
$ bash scripts/verify_design_claims.sh --merge-preview origin/main; echo exit=$?
VERDICT: PASS (0 нарушений)   exit=0
$ EVENT_NAME=pull_request PR_BASE_SHA=38671e7… bash scripts/check_docs_freeze.sh;          echo exit=$?  → exit=0
$ EVENT_NAME=pull_request PR_BASE_SHA=38671e7… bash scripts/check_protected_artifacts.sh;  echo exit=$?  → exit=0
$ EVENT_NAME=pull_request PR_BASE_SHA=38671e7… bash scripts/check_artifact_ids.sh;         echo exit=$?  → exit=0
$ bash scripts/check_context_budgets.sh; echo exit=$?
VERDICT: PASS — 7 файлов, 111943 B из 114900 B бюджета (запас 2957 B)   exit=0
# мутация (изолированный clone /tmp/hft-mut-r131):
$ … amend без FOUNDER-APPROVED … bash scripts/check_docs_freeze.sh; echo exit=$?  → exit=1
$ git rev-parse origin/docs/founder-decisions-2026-08-26  → 0aaae1c… (ветка предмета цела)
$ git ls-remote origin 'refs/reserved/R-13*'  → 08eb05f… refs/reserved/R-131
```
