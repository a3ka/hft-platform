# R-026 — PR-гейт M-53 / `TD-083` (P0): прод-read-path был функционально мёртв

**Reviewer:** reviewer · **Дата (UTC):** 2026-08-03 · **Ветка:** `feat/TD-083` → `main`
**Merge-commit:** `af2ee7b` · **HEAD после merge:** `5dd7396`
**Гейты:** `.claude/rules/gates.md` §3 (паритет verify↔CI), §4 (PR-time, UNCONDITIONAL),
§5 (RISK-BLOCK — не применяется, обоснование ниже), §8 (post-merge деплой-гейт).

## ВЕРДИКТ: **APPROVED** — merged в `main`, задеплоен, **подтверждён прогоном против живого прода**

`TD-083` (P0) **ЗАКРЫТ**. Все четыре симптома, воспроизведённые мной в `R-025`, опровергнуты
на живом VPS новым бинарём (§7): кадры идут (`frames_received` 20/10/15 против 0), CPU в
простое 0.00 % против 100.04 %, `CLOSE_WAIT = 0`, второй и третий клиенты подключаются.
Milestone M-53 → **DONE**.

Открытым остаётся отдельный, СМЕЖНЫЙ дефект — латентность первого снапшота (28.5–142.5 s):
это ось `TD-085` (суточный чекпоинт) + `TD-093(б)` (двойной счёт на коннект), а не цена тика,
которую чинил M-53. Именно так его и квалифицирует §11 milestone'а. Детали — находка №6.

---

## Block-scope — ЧИСТО

15 коммитов, 9 файлов (три точки против `origin/main`). Зоны и авторство (`%an`) совпадают
без единого пересечения:

| Файл | Автор коммитов | Зона по `scope-guard.md` |
|---|---|---|
| `crates/gateway/src/lib.rs` | engine-dev | engine-dev ✅ |
| `crates/gateway-serve/src/lib.rs` | engine-dev | engine-dev ✅ |
| `crates/gateway/tests/red_push_seek_bounded.rs` | architect | sacred, architect-only ✅ |
| `crates/gateway/tests/red_frames_seek_bound.rs` | architect | sacred, architect-only ✅ |
| `crates/gateway-serve/tests/red_ws_liveness_under_load.rs` | architect | sacred, architect-only ✅ |
| `scripts/verify_M-53.sh` | architect | sacred, architect-only ✅ |
| `milestones/M-53-td083-push-seek.md` | architect | architect-only ✅ |
| `research/reports/M-47-…`, `M-53-engine-dev-report.md` | engine-dev | своя роль ✅ |
| `research/reports/M-53-tester-report.md` | tester | своя роль ✅ |

Проверено машинно: `git log --format='%an' <base>..HEAD -- '*/tests/*'` → **только
`architect`**. Ни один dev-коммит не касается `*/tests/**`, `scripts/`, `milestones/`.
`crates/contracts/**` не тронут вовсе.

**RED-first порядок соблюдён.** Оракулы предшествуют реализации в обоих заходах:
`36da104` (architect, O-1/O-2) → `e722d4a` (dev); `ec1095f`/`eb8be3d`/`7fdf624`
(architect, переписанные нетавтологичные оракулы + O-3 + гейт) → `bd05dcd`/`7b8e538` (dev).

**Атомарность.** Одна задача — один коммит, subject с номером задачи. Бандлов нет.

**RN-21 (`branch-hygiene.md` п.8) — соблюдён по букве, нарушен по духу.**
Из `*/tests/**` и `scripts/` в итоговом диффе **не удалено ничего**
(`git diff origin/main...HEAD --diff-filter=D --name-only` → пусто). Инцидент с индексом
dev'а разрешён без потерь. **НО тот же класс сработал в обратную сторону и прошёл незамеченным
всеми, кроме этого гейта** — см. находку №1.

## Block-DoneBlock — сырой stdout, не пересказ

Прогоны есть от трёх независимых ролей: architect (12/12), tester на чистом чекауте
(`research/reports/M-53-tester-report.md`: `verify_M-53.sh` PASS 12/12, `verify_M-46.sh` PASS,
`cargo test --workspace` **785 passed / 0 failed**, fmt/clippy exit=0) и **reviewer лично**
(мой Done Block ниже, свой worktree `/tmp/hft-rev-m53`, `origin/feat/TD-083`).
Расхождений между тремя прогонами нет.

## Block-C (contracts) — НЕ ПРИМЕНЯЕТСЯ

`crates/contracts/**` не тронут (T7 гейта проверяет это машинно, PASS). Contract-RFC не
требуется, форма T1 не менялась.

## Block-risk (RISK-BLOCK, `gates.md` §5) — НЕ ПРИМЕНЯЕТСЯ, проверено диффом

`crates/risk/**`, `crates/killswitch/**`, `crates/oms/**`, `crates/venue-*/**` — не тронуты.
Диф целиком лежит на **read-path** (`gateway` — редьюсер журнала, `gateway-serve` — WS-выдача).
Order-egress отсутствует: ни submit/cancel, ни подписи торговых действий, ни записи в журнал.
`risk-critic` не требуется — это тот же carve-out, что для MD-only venue-адаптера.

---

## Содержательная проверка фикса (не только цвет гейта)

Milestone примечателен тем, что **предыдущий набор оракулов был зелёным при мёртвом проде**.
Поэтому я перепроверял не «12/12 PASS», а то, на что оракулы реально давят.

**1. `LiveReducer::pump` больше не тавтологичен — подтверждено чтением кода.**
`frames_since` внутри `pump` не вызывается (`crates/gateway/src/lib.rs`, тело `pump`): один
проход `journal::stream_from(self.cursor)`, кадр строится на свежем `Reducer`, чей vwap-
аккумулятор засеян из персистентного `self.vwap` (O(1)). `ReadStats` берутся с ЭТОГО ЖЕ
прохода — то есть меряют всю работу тика, а не половину (task #2b закрыт по существу).

**2. Эталон в оракуле действительно независим.**
`td083_pumped_frames_fold_into_full_replay_snapshot`: `snapshot(START)` + свёртка кадров
`pump` ≡ `snapshot(LATEST)`. Это свойство **невозможно удовлетворить, вернув чужой результат** —
оно связывает кадры с независимо посчитанным состоянием журнала. Прежняя проверка
(`pump` vs `frames_since`, где `pump` возвращал `frames_since`) сравнивала функцию сама с собой.

**3. Персистентно переносится ТОЛЬКО `VwapAcc.sum_pv`/`sum_v`** — и это корректно ровно
потому, что `frames_since` тоже стартует со свежего редьюсера и сеет через `seed_vwap`
только vwap. Остальные ряды дельта-only по построению. Байт-идентичность доказана и парным
сравнением, и независимой свёрткой — двумя разными способами.

**4. Живость (O-3) — фикстура честно названа ограниченной.**
`red_ws_liveness_under_load.rs` работает на `#[tokio::test]` (current_thread — форма прода) и
проверяет три независимых свойства, включая **деградированный вход** (грубый обрыв без
close-handshake — именно тот случай, у которого не было ветки выхода). Ограничение названо в
шапке файла прямым текстом: на тестовом журнале тик дёшев, прод-зависание фикстура НЕ
воспроизводит; цена тика меряется отдельными оракулами. Это правильная честность, а не дыра.

**5. Владение `LiveReducer` через `spawn_blocking` разобрано корректно.** `live` передаётся во
владение задаче и возвращается в ОБЕИХ ветках `pump` (Ok и Err); единственный невосстановимый
путь — паника blocking-таска — закрывает соединение явно, а не продолжает с потерянным
состоянием. Ветка `stream.next()` поллится на каждой итерации `select!` независимо от
`pending_read` — уход клиента детектируется без кадров (task #4 закрыт по существу).

---

## Находки

### 1. БЛОКИРУЮЩАЯ ПО ПРОЦЕССУ (устранена мной): удалён артефакт гейта

Коммит `870ab9d` (architect, `docs(M-53): остаток после фикса…`) **удалил
`research/reports/M-53-engine-dev-report.md`** — 188 строк, Done Block задач #2a/#2b/#2c и §5
«известные ограничения». Тот же коммит в теле milestone'а (§10) на этот файл **ссылается**:
«`research/reports/M-53-engine-dev-report.md` §5» — то есть ссылка вела в пустоту, а источник
трёх пунктов «остатка» исчез из дерева.

Класс — зеркальный **RN-21**: architect предупредил dev'а о своём push'е в его ветку, но
собственный индекс, в котором лежало удаление чужого файла, не проверил.
`branch-hygiene.md`: «Коммит НЕ ИМЕЕТ ПРАВА удалять … артефакты гейтов и аудит-трейл».

**Действие reviewer'а:** файл восстановлен байт-в-байт из `93e3fea` коммитом `5dd7396`,
содержимое не редактировалось. Аудит-трейл цел.

### 2. Барьер `protected-artifacts` даёт ЛОЖНОЕ срабатывание на rename через merge → `TD-092`

Мой merge-push уронил CI на job `Protected artifacts (gate trail)`:

```
FAIL  milestones/M-47-td083-push-seek.md: артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял
      (значит его выбросил MERGE — evil merge / -s ours / rename внутри мержа)
```

**Дефекта в коммитах нет.** `ec1095f` legitimно ПЕРЕИМЕНОВАЛ milestone (номер M-47 был занят):
`R095 milestones/M-47-td083-push-seek.md → milestones/M-53-td083-push-seek.md`. У барьера ЕСТЬ
ровно этот carve-out (`scripts/check_protected_artifacts.sh:129-137` — «переехал в защищённый
путь»), но он **недостижим через merge-коммит**: строка 124 зовёт
`git log --diff-filter=DR -M "${base}..HEAD" -- "${path}"` **без `--full-history`**, а
history-simplification не спускается в side-ветку, если путь TREESAME по первому родителю.
Воспроизведено локально:

```
$ git log --diff-filter=DR -M 09e5cb6..HEAD -- milestones/M-47-td083-push-seek.md
(пусто)
$ git log --full-history --diff-filter=DR -M 09e5cb6..HEAD -- milestones/M-47-td083-push-seek.md
ec1095f test(M-53): нетавтологичные оракулы TD-083 … номер M-47 был занят — переименовано замером
```

С `--full-history` барьер нашёл бы rename и выдал `NOTE … переехал … (остался под защитой)`.
Это **ровно тот класс, который барьер и создавался ловить, но с обратным знаком**: гейт
объявляет evil merge там, где его нет. Цена — красный `main` и заблокированный Deploy P0-фикса.
Зона правки — architect (`scripts/` sacred), заведено **`TD-092`**.

### 3. Канарейка T6 ловит ТЕКСТ, а не вызов — тот же класс, что `TD-088`

`verify_M-53.sh` T6: `grep -rn "LiveReducer::" crates/gateway-serve/src/`. В текущем файле этому
grep'у удовлетворяют И реальный вызов (`LiveReducer::resume`, строка 380), И **строки логов**
(`"LiveReducer::pump failed …"`, 555; `"LiveReducer::pump blocking task panicked …"`, 570), И
комментарии (434). Откат врезки с сохранением лог-строки оставил бы канарейку зелёной — то
есть защита от повторения класса M-45 слабее, чем заявлено. Не блокирую (врезка проверена
чтением кода и подтверждена оракулами T4/T4b/T5), но фиксирую как ещё один экземпляр `TD-088`.

### 4. `td083_tick_wallclock_does_not_grow_with_history` меряет wall-clock — принятый риск

Оракул O-B сознательно нарушает урок TD-078 («оракул не должен мерить CI-машину»), и
обоснование в шапке теста корректное: `stats` самого `pump` — именно то, что оказалось
половинным, поэтому внешняя мера единственная честная; порог ×4 при ожидаемом эффекте ×8.
Риск флаки на нагруженном раннере остаётся ненулевым. Отдельного долга не завожу — при первой
же флаке это повод для architect'а переехать на счётчик работы.

### 5. `resume()` без чекпоинта делает лишний полный проход журнала

`LiveReducer::resume` при отсутствии чекпоинта декодирует ВЕСЬ журнал только ради честного
`ReadStats.events_decoded` (форсинг оракула `resume_without_checkpoint_reports_full_replay`),
после чего первый `pump()` проходит те же события ЕЩЁ РАЗ. На проде чекпоинт есть, путь не
исполняется; при потере чекпоинта коннект станет вдвое дороже. Не блокирую — фиксирую вместе
с §10.2 milestone'а (двойная стоимость на connect) в `TECH-DEBT` под `TD-093`.

---

## §7 — Прогон против ЖИВОГО прода (чек-лист §11 milestone'а) — **TD-083 УСТРАНЁН**

Метод — тот же sidecar, что в `R-025`, но с **контрольным замером на СТАРОМ бинаре**,
снятым мной за час до деплоя тем же инструментом: A/B, а не сравнение с чужим протоколом.
Авторизация — через `--token` (JWT подписан сырыми UTF-8 байтами секрета вне харнесса):
прод-секрет 64 hex-символа, документированный путь `--secret` по-прежнему не работает
(**`TD-084` подтверждён действующим**). Секрет в отчёт не копируется.

| # | Что снято | Было (СТАРЫЙ бинарь, мой контроль 13:3x) | Стало (НОВЫЙ бинарь, 13:54–14:06) | Вердикт |
|---|---|---|---|---|
| 1 | `frames_received` | **0** (окно 25 s) | **20 / 10 / 15** (три клиента подряд) | **PASS** |
| 2 | CPU через 5 мин после ухода клиента | **100.04 %** (замерено через 60 s) | **0.00 %** (T+0, T+5 мин, T+90 s) | **PASS** |
| 3 | `CLOSE_WAIT` после ухода клиента | таск не завершался (R-025: 10 за 4 мин) | **0** во всех замерах (netns контейнера) | **PASS** |
| 4 | ВТОРОЙ клиент после первого | connect-timeout ×2 (R-025) | **подключился**, получил снапшот + 10 кадров; третий — тоже | **PASS** |
| 5 | латентность первого `Snapshot` | **13 900 ms** | **28 488 / 142 469 / 66 290 ms** | см. находку №6 |
| 6 | размеры | `Snapshot` 1.22 MiB (R-025); `Frame` — **замерить не удалось** | `Snapshot` **1 675 510 B (1.60 MiB)**; **max `Frame` = 48 841 B (47.7 KiB)** | зафиксировано |
| 7 | осмысленность | — | биды ниже асков, VWAP/CVD/OHLCV согласованы | **PASS** |

**Пункт 1 — главный.** `frames_received > 0` получен трижды подряд: 20, 10 и 15 кадров.
Это прямое опровержение симптома, ради которого milestone и существует: live-push сходится,
панель будет обновляться, а не застывать на снапшоте.

**Пункт 7 — сырые цифры (`snapshot.json` клиента #1).** Книга не скрещена: best bid
`63154.52` < best ask `63160.00`; `cob` монотонен по обеим сторонам (asks 63158.01 / .02 / .03).
`ohlcv` last `close = 63158.01` совпадает с книгой. `vwap` last = **64476.15** — выше спота
(≈63.15k), что корректно: VWAP all-time БЕЗ ресета (M-36), тот же порядок, что в `R-025`
(64489.71). `cumulative_delta` last = **122.63** — не абсурден. Ни одна из десяти серий не
пуста (`heatmap` 2332, `volume_bubbles` 892, `cob` 42, `ohlcv`/`vwap`/`cvd` по 57).
`history_truncated=true`, `history_start_seq=16049334` — ожидаемо.

### Находка №6 (по пункту 5): латентность КОННЕКТА выросла и НЕСТАБИЛЬНА — но это ось `TD-085`+`TD-093`, не `TD-083`

Три подряд подключения к одному и тому же бинарю при практически одинаковом backlog'е
(`covered_through_seq=153325272` от 04:00 против `next_seq` 156.47M → 156.60M, т.е. хвост
≈3.28M событий, за время прогона вырос на 1.6 %) дали **28.5 s → 142.5 s → 66.3 s**.

- Рост **не монотонный** ⇒ это не «каждое подключение дороже предыдущего», а большой разброс
  на одной и той же работе (I/O по сжатым сегментам + конкуренция с recorder'ом).
- Против моего контроля на старом бинаре (13.9 s) это от **×2 до ×10**. Множитель ×2
  объясняется предсказанной в milestone §10.2 двойной стоимостью коннекта (состояние считается
  дважды: для `live` и внутри `snapshot_from_checkpoint`) — это **`TD-093(б)`**. Остаток —
  дисперсия чтения 3.28M событий, которая существует ровно потому, что чекпоинт суточный
  (**`TD-085`**, поправка: cron ЕСТЬ, проблема в частоте).
- **Предмет `TD-083` — цена ТИКА, а не коннекта** — устранён: после снапшота кадры идут
  ровно, CPU в простое 0.00 %, память 11.7 MiB стабильна. Ось коннекта отдельная и по-прежнему
  открыта.
- **Продуктовое следствие, которое нельзя терять:** пользователь панели ждёт первый экран
  от 0.5 до 2.5 минут. Это не блокер close-out M-53 (milestone §11 прямо говорит: кратный рост —
  находка про `TD-085`), но это следующий по значимости дефект UX чтения. Занесено в `TD-085`
  и `TD-093` как замер, а не как впечатление.

## §8 — Post-merge деплой-гейт (`gates.md` §8) — ПРОЙДЕН

**CI/Deploy.** Merge-push `5dd7396` уронил CI на `Protected artifacts` — **ложное срабатывание
барьера, не дефект кода** (находка №2, `TD-092`); `Deploy` при этом честно fail-closed
(`Gate on CI` → failure, `Deploy (build on VPS)` → skipped). После следующего коммита
(`e8d039e`, docs) CI зелёный полностью — что и подтверждает транзиентность: тот же код,
тот же `main`, другой диапазон коммитов.

```
$ gh run view 30818969822 --json conclusion,jobs      # CI на e8d039e (main)
success  Protected artifacts=success  fmt + clippy + test=success  Delivery gate=success
         cargo audit=success  Contracts gate=success  All checks passed=success

$ gh workflow run deploy.yml --ref main ; gh run view 30819843394
success  Gate on CI (fail-closed)=success  Deploy (build on VPS)=success
```

**Почему деплой запущен вручную (`workflow_dispatch`) — и почему это записано, а не умолчано.**
`deploy.yml` триггерится по путям `crates/**`; коммит с кодом (`5dd7396`) был заблокирован
`TD-092`, а следующий (`e8d039e`) — docs-only и деплой не триггерит. Без ручного запуска
P0-фикс остался бы в `main` и НЕ доехал бы до прода. Ручной запуск — обход последствия чужого
дефекта гейта, а не самого гейта: он состоялся ПОСЛЕ зелёного CI на том же SHA, и внутренний
`Gate on CI (fail-closed)` отработал штатно. Правильное решение — `TD-092`.

**Eyes-on на VPS (сырое, T+90 s после ухода последнего клиента).**

```
$ cd /root/hft-platform && git log --oneline -1
e8d039e docs(review): R-026 — M-53/TD-083 APPROVED + merge; ...

$ docker run --rm --entrypoint ls hft-platform-recorder:local -la /usr/local/bin/
-rwxr-xr-x 1 root root 4527448 Aug  3 13:52 gateway-serve     ← пересобран (было 4458048, 09:07)
-rwxr-xr-x 1 root root 5428720 Aug  3 13:52 wsprobe

$ docker stats --no-stream hft-gateway-serve hft-recorder
hft-gateway-serve CPU=0.00% MEM=11.71MiB / 7.559GiB
hft-recorder      CPU=1.88% MEM=21.82MiB / 7.559GiB
CLOSE_WAIT=0    threads(/proc/1/task)=1

$ docker ps
hft-gateway-serve | Up 13 minutes (healthy)
hft-recorder      | Up 13 minutes (healthy)

$ cat .../recorder.heartbeat ; date +%s000
{"events":190526,"free_bytes":79767412736,"next_seq":156640339,"segment_index":165,
 "ts_wall_ms":1785765991027,"writable":true}
now=1785765991000        ← отставание heartbeat 0 ms

$ df -h /
/dev/sda1  150G  70G  75G  49% /
```

Запись данных не пострадала ни на одном шаге: `next_seq` растёт (156.28M → 156.64M за прогон),
`writable=true`, свободно 75 GiB. Дефект был и остался целиком на read-path.

**Вердикт §8: ПРОЙДЕН.** Прод жив и по записи, и — впервые — по чтению.

---

## Done Block (reviewer, агрегированный — `.claude/rules/commit-discipline.md`)

```
$ git worktree add /tmp/hft-rev-m53 --detach origin/feat/TD-083
HEAD is now at 6cbca13 test(M-53): прогон на чистом чекауте — вердикт PASS

$ git diff origin/main...HEAD --stat | tail -1
 9 files changed, 1550 insertions(+), 137 deletions(-)

$ git diff origin/main...HEAD --diff-filter=D --name-only
(пусто — ничего не удалено из tests/ и scripts/, RN-21 проверен)

$ git log --format='%an' <merge-base>..HEAD -- '*/tests/*' | sort -u
architect

$ bash scripts/verify_M-53.sh; echo exit=$?
PASS  T0 crates/gateway/tests/red_push_seek_bounded.rs
PASS  T0 crates/gateway/tests/red_frames_seek_bound.rs
PASS  T1 cargo build --workspace
PASS  T2 clippy --workspace --all-targets -D warnings
PASS  T2b cargo fmt --all --check (совпадает с ci.yml:20)
PASS  T3 seek-оракулы GREEN
PASS  T4 LiveReducer-оракулы GREEN (включая td083_* против НЕЗАВИСИМОГО эталона)
PASS  T4 нетавтологичные проверки на месте (эталон = полный реплей, не frames_since)
PASS  T4b оракулы живости GREEN (3 сценария: при живом клиенте / после ухода / после обрыва)
PASS  T5 набор M-46 (gateway-serve) GREEN — регресса нет
PASS  T6 gateway-serve вызывает LiveReducer (не только упоминает)
PASS  T7 crates/contracts/** не тронут
VERDICT: PASS
exit=0

$ git log origin/main..HEAD --format='%h %an %s'   # push-scope перед merge-push
(16 коммитов, все — M-53: architect / engine-dev / tester / reviewer; чужих нет)

$ git push origin HEAD:main
   09e5cb6..5dd7396  HEAD -> main
```
