<!-- GATE-META
milestone: R-096
audited_repo: a3ka/hft-platform
audited_base: f6e9337b90e3e262e9006495764452e7122f9924
audited_head: 4ffc2e6c8f44145ca1f9583afe85ea8fe23150f8
verdict: NOTE
-->

# R-096 — перепроверка §9: `П-017` (решения founder'а 2026-08-20)

- **Предмет:** ветка `docs/P-017-founder-decisions-2026-08-20` @ `4ffc2e6`, база
  `origin/main` @ `f6e9337`. Диф — ровно один файл, `docs/PENDING-SIGNATURE.md`,
  **+106 / −0** (`git diff --stat`, `git diff | grep -c '^+'` → 107 с заголовком).
- **Роль:** независимый Fable-агент со свежим контекстом (`gates.md` §9); НЕ автор правки.
- **Дерево слияния:** `git merge-base f6e9337 4ffc2e6` → `f6e9337` = `origin/main`, ветка —
  один коммит поверх актуального main. Дерево ветки ТОЖДЕСТВЕННО дереву слияния; все
  проверки ниже выполнены на нём (`/tmp/hft-recheck-p017`, detached @ 4ffc2e6).

**Вердикт: NOTE** — запись честна, полномочия соблюдены, все несущие утверждения
воспроизведены замером; четыре находки ниже — не блокирующие, ни одна не меняет вывода
записи и ни одна не завышает аргумент в её пользу. Merge не блокируется.

---

## (а) Утверждения о коде — каждое прогнано на дереве слияния

| # | утверждение записи | команда / замер | результат |
|---|---|---|---|
| 1 | `venue-binance/src/lib.rs:33` `MAX_REL_DIST` ±60 % | `sed -n '25,35p'` | ✅ `:33` = `const MAX_REL_DIST: f64 = 0.60;` |
| 2 | `bucket_levels` `:948-979` | `sed -n '945,982p'` | ✅ `fn bucket_levels` = 948, закрывающая `}` = 979; `rel > MAX_REL_DIST → continue` внутри |
| 3 | `:27`, `:853-855` — `limit=5000` | `sed` | ✅ `:27` `REST_DEPTH_LIMIT = "5000"`; `fetch_snapshot` `:853-855` подставляет его в URL |
| 4 | `:259` `state.book = None` (Gap) | `sed -n '244,262p'` | ✅ ровно `:259`, в ветке `DiffAction::Gap` |
| 5 | `:246` — единственное ЧТЕНИЕ `resyncing` | `grep -n resyncing` → 12 вхождений | ✅ читается только `:246` (`if !state.resyncing`); `:100/:158/:247/:262/:314/:332` — записи, `:150/:168/:257/:473` — комментарии/строка лога |
| 6 | `gateway/src/lib.rs:1035-1036` — метка по ШИРИНЕ полосы | `sed -n '1028,1040p'` | ✅ `:1035-1036` `depth_band_provenance: (row.band_pct_e8 > 1_300_000).then(...)` — только глубина полосы, состояние книги не участвует |
| 7 | `gateway-serve/src/lib.rs:500-510` — клиентские кадры выбрасываются | `sed -n '495,515p'` | ✅ `:507-510` `Message::Text/Binary` → пустая ветка «replay-контролы НЕ реализованы»; `:500-502` — комментарий GS-I-3 о read-and-ignore |
| 8 | `check_gate_meta.sh:278-291` — К-4 не читает `verdict:` | `sed -n '272,295p'`, `grep -n verdict` | ✅ К-4 (`:265` заголовок, цикл `:278-291`) грепает ТОЛЬКО литерал `M-NN` в `research/reviews/R-*.md` дерева слияния; `verdict:` читается в других секциях (`:203/:210/:230` — валидация полей изменённых файлов), К-4 его не читает — R-файл с `verdict: REJECT` проходит К-4 |
| 9 | `docker-compose.yml:134,197` — `GATEWAY_BANDS` дефолт `0.001` | `sed` | ✅ `:134` env, `:197` argv, оба `${GATEWAY_BANDS:-0.001}` |
| 10 | depth-probe: BTCUSDT −1.29/+1.33 %, ETHUSDT −4.50/+4.40 %, фьючерсы (limit=1000) −0.18/+0.19 % | `grep` по `research/data-quality/depth-probe-binance.md` | ✅ `:122/:123/:125` — все шесть чисел дословно |
| 11 | `fa/viz-backend.md:89-95` — замок З-1 действующий, снимаемый через M-58 | `sed -n '85,98p'` | ✅ `:92-95` «⛔ SUSPENDED… Замок A-002 З-1/З-2… Снимается автоматически переснятием per-life метрикой (M-58)» — FA действительно противоречит `П-014` (который снял З-1 подписью founder'а путём (b), `PENDING-SIGNATURE.md:749+`, раздел «Что этим снято») |
| 12 | `commit-discipline.md:89` / `gates.md:313` — сегодня мержит reviewer | `sed` | ✅ `:89` «именно reviewer вливает работу после APPROVED»; `:313` «feat-ветка… которую reviewer мержит» |
| 13 | `branch-hygiene.md` п.6 — замер про единую git-личность | чтение файла | ✅ п.6: замер 2026-08-03, все 14 worktree с подписью `reviewer` |
| 14 | M-65: 1609 строк реализации / оракул 1381 / 8 вердиктов | `git diff --numstat $(merge-base) origin/fix/M-65-battery-recalibration` | ✅ `crates/gateway-serve/src/*` added=**1609** (deleted=8); `tests/red_ws_session.rs` = **1381**; вердиктов ровно **8** (C-077, C-078, C-098, R-057, R-075, R-080, R-086, R-093) |
| 15 | «три ветки из 24 трогают `crates/*/src`, живой предмет один» | по всем `refs/remotes/origin`, диф от merge-base | ✅ src трогают ровно **3**: `feat/M-10-rebased` (5 ф., tip 2026-07-22 — месяц молчит), `salvage/M-59-…` (1 ф., salvage-снимок), `fix/M-65-battery-recalibration` (4 ф.) — живой предмет один. База «24» воспроизводится: сейчас веток 26, минус предметная и минус `docs/plan-abc-2026-08-20` (создана ПОЗЖЕ записи: tip 17:54 против коммита 14:43) = **24**; пустых (диф 0 от merge-base) ровно **3**; 3+18+3=24, раскладка 8+6+4+3=21 арифметически сходится |
| 16 | 433 / 14 / 80 за 14 суток | `git rev-list --count --since='2026-08-06' origin/main` → **433**; `git rev-list --count --since='2026-08-06' origin/main -- ':(glob)crates/*/src/**'` → **14**; добавленных файлов `research/{critiques,reviews,arbitration}` → **80** | ✅ все три числа воспроизведены. Методологическая заметка: поколмитный `diff-tree` (без history simplification) даёт **12** не-merge коммитов — оба числа поддерживают тот же вывод (14/433 ≈ 3 %) |
| 17 | запас `gates.md` 17 B | `bash scripts/check_context_budgets.sh` | ✅ `gates.md 47883 B / 47900 B (запас 17 B)`, VERDICT: PASS |
| 18 | `/home/nous/salvage-2026-08-19` не существует | `ls` → exit=2 | ✅ отсутствует (соседние `salvage-2026-08-1{2..6}` существуют — именно НАЗВАННЫЙ в У-1 каталог отсутствует) |
| 19 | `/mnt/journal-cold` пуст, создан 14.07 | ssh VPS: `ls | wc -l` → **0**; `stat -c %w` → **2026-07-14 18:43** | ✅ дословно |
| 20 | `research/registry/` пуст; backtest-отчётов ноль | `ls` | ✅ каталога `research/registry/` НЕТ (см. Н-3); в `research/reports/` — 8 dev/tester-отчётов милестоунов, продуктовых backtest-отчётов ноль |
| 21 | вердикты C-113 REJECT / C-114 ESCALATE на `docs/plan-abc-2026-08-20`; C-098 REJECT | `git show <ref>:… | sed 1,8p` | ✅ GATE-META: C-113 `verdict: REJECT`, C-114 `verdict: ESCALATE`; C-098 (`fix/M-65-…`) `verdict: REJECT`; в C-114 секция «Подтверждённые исправления» существует (`:29`) |

## (б) Полномочия

1. **Замок §11 / `check_docs_freeze.sh`.** Функция зоны открыта (`in_zone`, `:61-66`):
   `.claude/rules/* | .claude/agents/* | .claude/wrappers/* | CLAUDE.md | docs/04-workflow.md`.
   `docs/PENDING-SIGNATURE.md` в зоне **НЕТ** → токен `FOUNDER-APPROVED` коммиту НЕ нужен,
   и его отсутствие в `4ffc2e6` — не нарушение. Прогон барьера на диапазоне: PASS (Done Block).
2. **Запись не приземляет чужие гейты — раздел «Чего эта запись НЕ делает» ПРАВДИВ.**
   Диф: +106/−0, один файл, чистое ДОПИСЫВАНИЕ в конец (`П-014` @ `:749` не тронут);
   зона §11 не тронута; ни один ref не удалён (все 26 веток на месте, C-113/C-114/C-098/C-094
   живы на своих ветках); вердикты не сняты. Решение «владелец merge» записано, но НЕ
   исполнено — исполнение явно отослано в отдельный круг §11+§9. ✅
3. **Граница C (`gates.md` §0.1).** Запись фиксирует решения САМОГО founder'а дословными
   цитатами; сама фиксация затребована founder'ом («надо где-то зафиксировать»). Ни состав
   данных, ни промоушены, ни фазы, ни деньги записью не двигаются. Формат «✅ ПОДПИСАНО» —
   прецедент `П-013`/`П-014`. Отдельная подпись не требуется. ✅
4. **Номер `П-017` корректен (`gates.md` §12).** По всем `refs/remotes/origin`: `П-017`
   существует ТОЛЬКО на предметной ветке (уникален); максимум занятых — `П-016`
   (ветки `critic/C-110..112-verdict-gate`, `docs/branch-disposition-2026-08-19`) →
   `П-017` = max+1. ✅ Номер этого вердикта: max R = `R-095`, `R-096` свободен по всем refs.

## (в) Связность и ссылки

| ссылка | адресат | где резолвится |
|---|---|---|
| `П-014` | `docs/PENDING-SIGNATURE.md:749` | main ✅ |
| `C-113` / `C-114` | `research/critiques/C-113-plan-abc.md`, `C-114-plan-abc-rev2.md` | ветка `docs/plan-abc-2026-08-20` — запись сама называет ветку ✅ |
| `C-094` | `research/critiques/C-094-M-68.md` | ветка `feat/M-68-depth-from-book` ✅ |
| `C-098` | `research/critiques/C-098-M-65-r4.md` | ветка `fix/M-65-battery-recalibration` ✅ |
| `TD-020` | `TECH-DEBT.md` (13 вхождений) + `docs/09-roadmap-v2.md:311` | main ✅ |
| `A-002` | `research/arbitration/A-002-depth-metric-tpp.md` | main ✅ |
| `M-58` | `milestones/M-58-depth-metric.md` | main ✅ |
| `reading-map.md` §2 | ярус A содержит «`docs/PENDING-SIGNATURE.md` — только индекс `grep -n '^## П-'`» | main ✅ — несущая посылка записи («видно в новой сессии механикой чтения») верна |
| `docs/09-roadmap-v2.md` | существует | main ✅ |

`R-095` в тексте `П-017` НЕ упоминается (grep по дифу → 0) — висячей ссылки нет.

## Честность формулировок («⇒» против цитат)

- **A1** «сделаем позже» ⇒ «отложено, не отменено» — вывод не шире цитаты; риск НЕ смягчён:
  назван числами, единственность копии и невосстановимость — проверены (журнал в одной
  копии, cold пуст). ✅
- **A2** — вывод повторяет цитату («заморозку на новый наложим»); опровержение посылки
  замером НЕ отменяет решение и прямо помечено как «чтобы решение не исполнялось по неверной
  картине». Замер воспроизведён (стр. 15 таблицы (а)). ✅ Честно в обе стороны.
- **A3** «Ок» ⇒ мораторий в границах предложения, основание 433/14/80 + 17 B — воспроизведено. ✅
- **B владелец merge** — вывод («меняется исполнитель, не состав гейтов») не шире цитаты
  («…после всех тестов, критиков и ревьюверов»); `COGNITIVE-ONLY` предел назван и ВЕРЕН
  (К-4 действительно не читает `verdict:` — проверено, стр. 8). Ничто не выдано за механизм. ✅
- **«не обрезает»** — все пять пунктов фиксации воспроизведены кодом и замером; формулировка
  «верно НЕ во всех режимах» точна: steady-state ±60 % из diff-книги, старт/gap — REST 5000,
  барьера эмиссии нет. ✅
- **M-65** — «нужен» подтверждён C-114; маршрут повторяет цитату founder'а («сначала надо
  исправить реджект… и только потом»). ✅

## Находки (все — NOTE, не блокируют)

**Н-1. Размер журнала не сходится с сегодняшним замером.** Запись: «50 G / 342 сегмента».
Замер 2026-08-20 (эта перепроверка, ssh): **55 G / 343 сегмента** (`du -sh` → 55G;
`ls | grep -c ^segment` → 343). +1 сегмент — рост за часы; разница 5 G при одном сегменте
(~150–200 MB) означает, что «50 G» снято другим методом или раньше и округлено вниз.
Направление ошибки — ПРОТИВ аргумента записи (риск A1 занижен, не завышен) — потому NOTE.
*Условие снятия:* при следующем касании `П-017` заменить на замер с датой и командой,
либо убрать точное число, оставив «в одной копии» (что и есть несущее утверждение).

**Н-2. «данные копятся пятую неделю» — арифметика даёт шестую.** Старейший сегмент
`segment-00000001.jrnl.zst` — 15.07; 15.07→20.08 = 36 суток = 5 полных недель + 1 день,
т.е. идёт ШЕСТАЯ неделя. Мелочь; направление снова против собственного аргумента (недооценка).
*Условие снятия:* то же, что Н-1 — при следующем касании.

**Н-3. «research/registry/ пуст» — каталога НЕ СУЩЕСТВУЕТ.** `ls research/registry/` →
No such file or directory. Функционально для аргумента («ни одного сигнала в реестре»)
эквивалентно, но «пуст» и «отсутствует» — разные проверяемые предикаты. *Условие снятия:*
при следующем касании — «`research/registry/` не существует».

**Н-4. Число «14» воспроизводится только через history simplification.** 
`git rev-list --count --since='2026-08-06' origin/main -- ':(glob)crates/*/src/**'` → **14**;
поколмитный `diff-tree` по тем же 433 коммитам → **12** не-merge коммитов (список в Done
Block перепроверки). Оба числа дают тот же вывод (≈3 % кодовых коммитов); методика в записи
не названа. *Условие снятия:* необязательно; при следующем касании назвать команду замера.

## Пределы разбора (что НЕ проверял)

- **Дословность цитат founder'а** — транскрипт сессии founder↔architect мне недоступен;
  проверена только согласованность выводов «⇒» с цитатами, КАК ОНИ ЗАПИСАНЫ. Если цитата
  искажена при записи — это отсюда не ловится в принципе (названный предел §9).
- **«~27 M событий»** в журнале — не пересчитывал (требует чтения 343 zst-сегментов на VPS);
  порядок согласуется с 55 G / известным форматом.
- **Классификация 18 непустых не-src веток на «харнесс 8 / RED 6 / документы 4»** —
  арифметику сверил (18+3+3=24), пометочную принадлежность каждой ветки не перепроверял.
- **Содержание C-113/C-114 по существу плана A-B-C** — вне предмета: здесь проверялось
  существование, вердикты и цитируемость, не правота критика.
- **`git diff` M-65 living-branch кода** по существу — вне предмета (М-65 идёт своим кругом).

## Done Block

```
$ git -C /tmp/hft-recheck-p017 log -1 --oneline
4ffc2e6 docs(П-017): решения founder'а 2026-08-20 зафиксированы в очереди решений [architect]

$ git merge-base f6e9337 4ffc2e6
f6e9337b90e3e262e9006495764452e7122f9924          # ветка = main+1, дерево = дереву слияния

$ git diff --stat f6e9337 4ffc2e6
 docs/PENDING-SIGNATURE.md | 106 ++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 106 insertions(+)

$ git rev-list --count --since='2026-08-06' origin/main
433
$ git rev-list --count --since='2026-08-06' origin/main -- ':(glob)crates/*/src/**'
14
$ git log --since='2026-08-06' --diff-filter=A --name-only --format= origin/main \
    -- 'research/critiques/*' 'research/reviews/*' 'research/arbitration/*' | sort -u | grep -c .
80

$ bash scripts/check_context_budgets.sh | grep gates
OK    .claude/rules/gates.md                     47883 B /   47900 B (запас 17 B)   # exit=0

$ ls /home/nous/salvage-2026-08-19; echo exit=$?
ls: cannot access '/home/nous/salvage-2026-08-19': No such file or directory
exit=2

$ ssh root@167.233.192.131 'ls /mnt/journal-cold | wc -l; stat -c %w /mnt/journal-cold; \
    du -sh .../journal-data/_data; ls .../_data | grep -c ^segment'
0
2026-07-14 18:43:06
55G
343

$ MB=$(git merge-base origin/main origin/fix/M-65-battery-recalibration); \
  git diff --numstat $MB origin/fix/M-65-battery-recalibration -- 'crates/gateway-serve/src/*' \
    | awk '{a+=$1} END {print a}'
1609
```

Прогонов барьеров на диапазоне (docs-freeze, gate-meta, protected-artifacts, artifact-ids)
— см. push-протокол коммита этого вердикта: они гонялись ПОСЛЕ его добавления, чтобы судить
итоговое состояние ветки, и решались по коду возврата.
