<!-- GATE-META
milestone: A-015
audited_repo: a3ka/hft-platform
audited_base: 92f6e65a90382506eb3fcc4ab09d4744ffeaade4
audited_head: 8454f8faa5841ba0b8c47ca71d37ee9a0a93f28c
verdict: APPROVE
-->

# R-116 — перепроверка §9, круг 2: запись founder-решения по `A-015` §6 после `R-114` REJECT

**Предмет:** ветка `docs/founder-decision-a015-2026-08-23`, вершина `8454f8f` (7 коммитов от
`92f6e65`, включая merge `290b34a` со свежим step1). **Проверяющий:** независимый Fable-агент
со свежим контекстом; не автор правки и не автор вердикта круга 1. Прогоны — на detached
worktree `8454f8f` (ветка занята чужим деревом — запасной путь `branch-hygiene.md` п.2) и на
копии `origin/main` (`686327f` — main уехал на PR #69 после круга 1; учтено).

**Вердикт: APPROVE.** Условие снятия Б-1 исполнено точно и без смягчения; N-1/N-2 исправлены,
обе правки подтверждены моими прогонами; правка чужого вердикта (`ed07ff5`) — допустимая в
предъявленном узком виде, вынужденность доказана исполнением логики барьера, содержание находок
и `verdict: REJECT` не тронуты. Блокеров нет. Шесть NOTE ниже — четыре наследованы от круга 1
и не закрыты (авторам следующих кругов), две новые (прецедент правки артефакта гейта; предъявление
ложной посылки founder'у).

---

## (а) Утверждения о коде — каждое моим прогоном

**П-1. Б-1 переписан по факту, и факт таков.**
```
$ gh pr view 49 --json state,mergedAt,mergeCommit --jq '.state, .mergedAt, .mergeCommit.oid'
MERGED / 2026-08-21T15:09:50Z / 3914ee67268b859a5f482e6a2dd42d0bb6e9f708
$ git show origin/main:scripts/verify_design_claims.sh | grep -c FACTS
30
$ grep -n 'verdict:' research/critiques/C-117-facts-marker-rev2.md | head -1
6:verdict: NOTE
```
Все три утверждения нового п.2 («PR #49 ВЛИТ 21.08 (`3914ee6`)», «был круг `C-117` (вердикт
`NOTE`)», «детектор `FACTS` сегодня в `main`») — истинны. Формулировка «маркер остаётся там,
где стоял» снята; «(б) не выбран» теперь означает ровно то, что означает.

**П-2. Врезка про ложную посылку — сама фактически верна.** `A-015` §6 на ветке
`fix/M-65-battery-recalibration` действительно описывает (б) как «PR #49 отклонён `C-116`»
(строка 151 файла `research/arbitration/A-015-m65-n2-absent-config.md` на той ветке — открыт
мной `git show`); профиль `origin/main:.claude/agents/architect.md` действительно несёт
«в `main` детектора нет» и «→ `0`» (строки 127–128 — открыт мной); инцидент `25c323d`
существует и его subject — «§Tasks 13 догнала §11 … третье расхождение носителей», что
подтверждает довод «(а) и (б) бьют в разные поверхности».

**П-3. N-1 закрыт.** `grep -n 'fn claims' crates/gateway/tests/red_catalog_equivalence.rs` →
`239`; тело ассерта — до `:248`. Ссылка step1 `:239-248` теперь верна.

**П-4. N-2 закрыт — узкая мутация различает правку.** Мутировано мной: `:770`
`let observed = truth_indices(dir);` → `let observed: Vec<u32> = Vec::new();` (только вход
свидетеля), прогон `cargo test -p gateway --test red_segment_meta_bound`:
```
test result: FAILED. 8 passed; 2 failed
  sm8_per_segment_compaction_keeps_catalog_truthful  … panicked at red_segment_meta_bound.rs:771:5
  sm9_compaction_midstate_does_not_duplicate_index   … panicked at red_segment_meta_bound.rs:771:5
      SETUP НЕ СОСТОЯЛСЯ — независимый обход каталога не нашёл НИ ОДНОГО сегмента
```
Числа, имена тактов, строка и сообщение — дословно как в `5d274e5` и step1 §4. Оба падения —
на добавленном свидетеле. Файл восстановлен (`git checkout --`, tracked-porcelain пуст).

**П-5. Замер A пересчитан мной, скриптом из спеки §4.** На `origin/main` (`686327f`, СВЕЖЕЕ
ревизии записи) → `9 3 6`, те же шесть функций — замер step1 §1 держится даже после PR #69.
На дереве предмета → `9 4 5` (кандидат `assert_catalog_truthful` закрыт свидетелем `3b5e5b0`).
5 ложных из 6 = 83 % — арифметика записи верна.

**П-6. Замер B пересчитан мной.** Дерево предмета → `340 6` (M-09, M-38b, M-58, M-62; 6/340 =
1.8 %). Сошлось с записью и step1 §5.

**П-7. Дерево слияния.** `bash scripts/verify_design_claims.sh --merge-preview origin/main` →
`VERDICT: PASS (0 нарушений)`, exit=0.

**П-8. Барьеры прод-проводкой** (`EVENT_NAME=push PUSH_BEFORE=92f6e65`): check_gate_meta,
check_docs_freeze, check_artifact_ids, check_protected_artifacts, check_review_fa,
check_context_budgets — **все exit=0**. Заявление `8454f8f` «все exit=0» подтверждено.
`review-fa` даёт документированный `SKIP` (диапазон трогает только
`crates/gateway/tests/**` — не-прод пути, `C-115` B-1); когнитивная половина нормы M-66
исполняется здесь: живой инвариант тронутого модуля — **`VB-I-2`** (live == replay,
`docs/fa/viz-backend.md:149`): свидетель `3b5e5b0` пиннит правдивость `SegmentCatalog` против
независимого обхода журнала — ровно предусловие того, что live-хвост и replay читают один
состав сегментов.

**П-9. История не переписана.** `git merge-base --is-ancestor 848bfef HEAD` → ДА;
`cc751bc` (вердикт круга 1) — тоже предок; step1 внесён merge-коммитом `290b34a`, не
rebase'ом. `audited_head` вердикта R-114 остаётся предком вершины — линия вердикта цела.

## (б) Полномочия — правка чужого вердикта `ed07ff5`

**Проверено исполнением, а не numstat'ом.** Полный междукоммитный дифф
`git diff cc751bc ed07ff5 -- research/reviews/R-114-…` — ровно ОДИН ханк, одна строка шапки
`milestone: П-017` → `A-015`. Текст находок, Б-1, все NOTE и `verdict: REJECT` — байт-в-байт
не тронуты. После `ed07ff5` файл не менялся: `git diff ed07ff5 HEAD -- <файл>` → 0 строк,
`git log ed07ff5..HEAD -- <файл>` → пусто.

**Вынужденность доказана исполнением логики барьера.** `check_gate_meta.sh:220-226`: case
`[A-Za-z]*-[0-9]*` — кириллическая «П» в класс не входит, `П-017` падает в ветку `*)` → `bad`
(«не похож на идентификатор артефакта»); `A-015` проходит и case, и regex
`^[A-Za-z]+-[0-9]+[a-z]?$`. Прогнано мной дословно тем же case-выражением. То есть с шапкой
`П-017` ветка была КРАСНОЙ по gate_meta — правка не косметика, а условие существования
вердикта как машинно-валидного артефакта.

**Оценка по существу: допустимо — в этом узком виде и только в нём.** Что делает случай
допустимым: (1) поле — машинный заголовок, не содержание; (2) ошибочное значение продиктовано
мандатом самого автора (проверить текст мандата командой нельзя — он живёт в переписке; предел
назван); (3) правка НЕ смягчает вердикт — REJECT остаётся и становится предъявимым барьеру;
(4) отдельный коммит, называющий себя и причину; (5) обязательный следующий круг (этот)
перепроверил дифф. **Прецедентом смягчения это стать не может ровно потому, что условия
(1)–(5) в него встроены**: любой шаг за них — правка содержания, слияние с другим коммитом,
отсутствие следующего круга — выводит случай из допустимого. Два уточнения — N-А и N-Б ниже.

**Founder'у не приписано сверх сказанного.** Дословная цитата — «A. Принимаю рекоммендацию
(а)»; три следствия признаны законными выводами ещё кругом 1 и не менялись; новая врезка
явно делит «оценка architect'а» / «решение founder'а», право пересмотра оставлено founder'у,
пере-предъявление честно названо не-сделанным. **Граница C** не задета: предмет процессный,
ни денег, ни состава данных, ни фаз. **Замок §11**: диапазон трогает
`docs/PENDING-SIGNATURE.md`, `docs/plans/**`, `research/reviews/**`,
`crates/gateway/tests/**` — ни один путь не входит в зону (`.claude/rules/**` ·
`.claude/agents/**` · `.claude/wrappers/**` · `CLAUDE.md` · `docs/04-workflow.md`); токен не
нужен, и это не рассуждение: check_docs_freeze покоммитно прошёл прод-проводкой (П-8).

## (в) Связность

Все пути и идентификаторы диапазона существуют и означают заявленное (перекрёстно — П-7:
merge-preview PASS). `C-116`/`C-117` — файлы на дереве, вердикты REJECT/NOTE соответственно.
`3914ee6` — реальный merge-commit PR #49 (П-1). Ссылка «`A-015` §6» разрешается только на
ветке `fix/M-65-battery-recalibration` — наследованная N-4 круга 1, не исправлена (это
ссылка-идентификатор, не путь: файл существует и означает заявленное, потому NOTE, не
блокер). Порядок приземления — N-В ниже.

## NOTE

- **N-А (новая).** Довод коммита `ed07ff5` «`git show --numstat` подтверждает одну строку» —
  недостаточное доказательство невинности правки: numstat показывает РАЗМЕР (1/1), а не
  СОДЕРЖАНИЕ строки. Доказательство — сам ханк диффа плюс перепроверка следующим кругом (оба
  здесь предъявлены). В будущих случаях предъявлять дифф, не numstat.
- **N-Б (новая, процессная).** Нормы о правке чужого артефакта гейта не существует:
  `branch-hygiene.md` п.4 говорит, КТО коммитит вердикт, `protected_artifacts` ловит только
  УДАЛЕНИЕ. Кандидат правила (founder/harness-трек, не этот круг): «содержимое чужого
  вердикта не правит никто; машинные поля GATE-META — только при красном барьере, отдельным
  коммитом с причиной, с обязательной перепроверкой следующим кругом»; кандидат механизации —
  барьер иммутабельности `R-*/C-*/A-*` сверх шапки без явного `ALLOW`-токена.
- **N-В (порядок приземления; развитие N-3/N-4 круга 1).** Merge `290b34a` внёс содержимое
  PR #66 (step1 + правка sacred-оракула `3b5e5b0`) в ЭТУ ветку: вольётся она первой — PR #66
  поглощён, и его собственный круг не случится. Судьбу назвать явно при приземлении: либо
  #66 первым, либо закрыть его как поглощённый с записью, что оракул-правка судилась двумя
  кругами (R-114 П-3 широкая мутация + R-116 П-4 узкая). Слово «doc-часть», которое цитировал
  N-3 круга 1, в текущей записи грепом не находится — но факт остаётся: +12 строк в
  `crates/gateway/tests/**` едут коммитом типа `docs(plans)`.
- **N-Г (наследована N-4).** «`A-015` §6» упоминается в `П-017` без оговорки, что артефакт
  живёт только на `fix/M-65-battery-recalibration`; читатель `main` не разрешит ссылку до
  приземления той ветки.
- **N-Д (наследована N-5).** Числа (5 из 6 / 83 %; 6/340 = 1.8 %) живут в step1 и `П-017`
  (плюс тела коммитов); правки круга 2 новых носителей не добавили, расхождения нет (я
  пересчитал оба — сходятся), но класс «расхождение носителей» остаётся открытым по существу.
- **N-Е (наследована N-6, для Handoff).** TD-карточка остаточного риска класса «вакуумный
  ассерт» reviewer'у по-прежнему не заведена — в диапазоне нет ни handoff-следа, ни TD-ссылки.
  Смежно: «норма binding-requires-mechanism этим удовлетворяется» (следствие 1) станет
  строго истинным при ПРИЗЕМЛЕНИИ барьера B; до того класс держится самой записью `П-017`
  как записью остаточного риска — что норме соответствует, но проговорить это стоило бы.
- **N-Ж (для founder-facing handoff).** Врезка «вопрос поставлен с ложной посылкой» и оценка
  «по существу выбор не меняется» лежат в записи, которую прочтёт следующая СЕССИЯ, — но
  «founder вправе пересмотреть» работает, только если врезку предъявили ЕМУ. В ближайший
  founder-facing Handoff — явной строкой, не ссылкой.

## Done Block

```
$ git worktree add --detach /tmp/hft-recheck-r116 origin/docs/founder-decision-a015-2026-08-23
HEAD is now at 8454f8f  (ветка занята /tmp/hft-arch-pending — запасной путь branch-hygiene п.2)
$ git merge-base origin/main HEAD
92f6e65a90382506eb3fcc4ab09d4744ffeaade4        # origin/main уехал на 686327f (PR #69) — merge-base прежний

$ gh pr view 49 --json state,mergedAt,mergeCommit --jq '…'
MERGED / 2026-08-21T15:09:50Z / 3914ee67268b859a5f482e6a2dd42d0bb6e9f708
$ git show origin/main:scripts/verify_design_claims.sh | grep -c FACTS
30
$ grep -n 'verdict:' research/critiques/C-117-facts-marker-rev2.md | head -1
6:verdict: NOTE

$ python3 <скрипт A из спеки §4>     # origin/main 686327f
9 3 6   (те же шесть функций)
$ python3 <скрипт A>                 # дерево предмета 8454f8f
9 4 5
$ python3 <скрипт B>                 # дерево предмета
340 6   (M-09, M-38b, M-58, M-62)

$ grep -n 'fn claims' crates/gateway/tests/red_catalog_equivalence.rs
239:fn claims(label: &str) {

$ sed -i '770s|let observed = truth_indices(dir);|let observed: Vec<u32> = Vec::new();|' … && cargo test -p gateway --test red_segment_meta_bound
test result: FAILED. 8 passed; 2 failed   (sm8, sm9 — оба panicked at :771, сообщение свидетеля)
$ git checkout -- crates/gateway/tests/red_segment_meta_bound.rs; git status --porcelain | grep -v '^??'
(пусто)

$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -2
VERDICT: PASS (0 нарушений); exit=0

$ for s in gate_meta docs_freeze artifact_ids protected_artifacts review_fa context_budgets; do EVENT_NAME=push PUSH_BEFORE=92f6e65… bash scripts/check_$s.sh; done
exit=0 × 6   (review_fa: SKIP — только не-прод пути крейтов, C-115 B-1)

$ git diff cc751bc ed07ff5 -- research/reviews/R-114-…   # один ханк: milestone: П-017 → A-015
$ git diff ed07ff5 HEAD -- research/reviews/R-114-… | wc -l
0
$ case "П-017" in [A-Za-z]*-[0-9]*) …;; *) echo bad;; esac
bad — FAIL формы подтверждён; A-015 проходит case+regex

$ git merge-base --is-ancestor 848bfef HEAD && echo ДА
ДА
```
