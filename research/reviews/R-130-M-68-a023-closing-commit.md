<!-- GATE-META
milestone: M-68
audited_repo: a3ka/hft-platform
audited_base: 3b496208a64edbf00a66b93986ff8529d0c93aa9
audited_head: 6540d7527ef52fa3faeee33ab3916b71ceb6b28b
verdict: APPROVE
-->

# R-130 — M-68: закрывающий коммит `A-023` §4 — **APPROVED** (plan-time; в `main` НЕ идёт)

**Роль:** reviewer (PR-time гейт, `gates.md` §4 — UNCONDITIONAL) · **Дата (UTC):** 2026-08-26
**Предмет:** ветка `feat/M-68-rev3`, вершина `6540d75` — «docs(M-68): A-023 §4 — черновая
нумерация и ложное самоописание сняты; проверено командами [architect]»; parent `3ed3e2c`.
**Основание вызова:** `A-023` §5 — «перепроверка §4.3 + свежий взгляд на класс §2.1 —
reviewer, PR-time (безусловно)» + `A-023` §3.2 п.4.
**Чекаут:** свой worktree `/tmp/hft-reviewer-m68`, detached от `origin/feat/M-68-rev3`
(SHA сверен с мандатом), НЕ чужое дерево (`branch-hygiene` §1).

**Ярус C прочитан ГРЕПОМ по предмету, не целиком** (`reading-map.md` §2): `TECH-DEBT.md` по
`TD-158|TD-161|M-68|depth_provenance`, `PROJECT-STATE.md` по `M-68|GATEWAY_SCHEMA_VERSION`.

**Предъявление FA (M-66).** Диф трогает `crates/gateway/tests/**`. FA этого крейта — НЕ
отсутствующий `docs/fa/gateway.md`, а `docs/fa/viz-backend.md` (`scope-guard.md`:
«`crates/gateway` = viz-backend Read Gateway, Слой 8, M-22»). Живые инварианты, названные
по факту проверки: **`VB-I-11`** (`:198` — провенанс истории, bump `7→8`, ровно та строка
чейнджлога, которую тронутый ассерт переписывает с M-48 на M-68) и **`VB-I-6`** (`:193` —
прецедент «смена семантики ⇒ bump», на который спека M-68 опирается в задаче 9). FA-WAIVER
НЕ нужен и не выписывается.

---

## Block-scope — ПРОЙДЕН

`git diff --name-only HEAD^..HEAD` даёт **ровно два файла**, названные `A-023` §4.1/§4.2:
`crates/gateway/tests/red_gateway_schema_version.rs`, `milestones/M-68-depth-from-book.md`.
Оба — зона architect'а (`scope-guard.md`: `*/tests/` RED-спеки, `milestones/M-NN-*.md`).
`crates/**/src/**` не тронут, `crates/contracts/**` не тронут, `scripts/verify_M-68.sh` не
тронут, `deploy/**` не тронут. Процессный слой (`gates.md` §11) не задет ⇒ токен
`FOUNDER-APPROVED` не требуется. Удалений защищённых артефактов нет.

## Block-C (contracts) — НЕПРИМЕНИМ

`crates/contracts/**` в дифе отсутствует; шаг `H` гейта («`crates/contracts` не тронут») —
PASS в моём прогоне. Contract-RFC не требуется.

## Block-risk — НЕПРИМЕНИМ

Диф не трогает `crates/risk|killswitch|oms|venue-*`. Шаг `K` гейта («book/venue/journal/
роадмап не тронуты диапазоном») — PASS. `risk-critic` по `gates.md` §5 не требуется.

## Block-DoneBlock — предъявлен МОИМ прогоном; в отчёте tester'а два пробела

Вердикт по существу от этого не меняется (все десять команд `A-023` §4.3 прогнаны мной и
дали ожидаемое, сырой вывод ниже), но пробелы называются, а не замалчиваются.

**B-1 — tester прогнал СПИСОК ИЗ ТЕЛА КОММИТА, а не список `A-023` §4.3.** Отчёт называет
«все 8 закрывающих команд»; в §4.3 их **десять**. Совпадение с телом `6540d75` буквальное,
включая `cargo fmt --all -- --check`, которого в §4.3 нет. Не прогнано ни разу до меня:
- `git diff HEAD^..HEAD -- …red_gateway_schema_version.rs | grep -E '^[+-].*assert'`
  (ожидание exit=1 — ассерты не тронуты);
- `bash scripts/verify_M-68.sh` (ожидание exit=1, `VERDICT: FAIL (8)` — базовая линия не
  сдвинута). Это единственная команда §4.3, проверяющая, что закрывающая правка не
  зазеленила и не покрасила гейт сверх ожидаемого; её пропуск — пропуск свойства, а не
  строки.
Обе выполнены мной, обе дали ожидаемое.

**B-2 — команда №9 прогнана на НЕ ТОМ диапазоне.** §4.3 требует `git diff --name-only
HEAD^..HEAD` (ожидание: ровно два файла). В отчёте — `git diff --name-only $(git merge-base
HEAD origin/main)..HEAD`, вернувший **14 файлов** всей ветки. Этим диапазоном утверждение
«дифф в границах двух названных файлов» не доказывается в принципе, а таблица запретов
подпёрта прозой («pre grep -c assert = 5; post = 5»), то есть пересказом вместо сырого
stdout (`commit-discipline.md`). Строка «§Tasks не перенумерован» в той таблице пришла
битой (`^## | ^###`) — свидетельство отсутствует как таковое.

**B-3 — FA-WAIVER в отчёте tester'а выписан на ложной посылке.** «`docs/fa/gateway.md`
отсутствует ⇒ назвать живой инвариант невозможно» — неверно: FA крейта живёт под
продуктовым именем модуля (`docs/fa/viz-backend.md`), что прямо записано в `scope-guard.md`.
Waiver не нужен; см. шапку этого вердикта. Класс совпадает с открытым `TD-165` (норма о
месте `FA-WAIVER` расходится со своим барьером) — карточку не дублирую.

## Block — перепроверка `A-023` §4.3 (все ДЕСЯТЬ команд, сырой вывод)

```
$ pwd
/tmp/hft-reviewer-m68
$ git log -1 --oneline
6540d75 docs(M-68): A-023 §4 — черновая нумерация и ложное самоописание сняты; проверено командами [architect]
$ git rev-parse HEAD^
3ed3e2c14b4ba268306ae6aa0210ea4125ae0372

### 1
$ grep -nE 'задач[а-яё]+ 6' milestones/M-68-depth-from-book.md; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 2
$ grep -n 'Новая задача 5:' milestones/M-68-depth-from-book.md; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 3
$ grep -n 'инвалидацией (задача 4)' milestones/M-68-depth-from-book.md; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 4
$ grep -n 'просто нет' crates/gateway/tests/red_gateway_schema_version.rs; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 5
$ grep -n 'не дублирует числом' crates/gateway/tests/red_gateway_schema_version.rs; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 6
$ grep -c '8→9' crates/gateway/tests/red_gateway_schema_version.rs; echo "exit=$?"
3
exit=0                                   ← ожидалось >=2, факт 3 (след не стёрт)
### 7
$ sed -n '/fn schema_version_constant_matches_expected/,/^}/p' crates/gateway/tests/red_gateway_schema_version.rs | grep -n 'M-48'; echo "exit=$?"
exit=1                                   ← ожидалось 1
### 8   (в отчёте tester'а ОТСУТСТВУЕТ)
$ git diff HEAD^..HEAD -- crates/gateway/tests/red_gateway_schema_version.rs | grep -E '^[+-].*assert'; echo "exit=$?"
exit=1                                   ← ожидалось 1: ни одна строка с `assert` не добавлена и не удалена
### 9   (в отчёте tester'а — НЕ ТОТ диапазон)
$ git diff --name-only HEAD^..HEAD; echo "exit=$?"
crates/gateway/tests/red_gateway_schema_version.rs
milestones/M-68-depth-from-book.md
exit=0                                   ← ожидалось: ровно эти два файла
### 10  (в отчёте tester'а ОТСУТСТВУЕТ)
$ bash scripts/verify_M-68.sh >/tmp/rv-m68.log 2>&1; echo "exit=$?"; tail -1 /tmp/rv-m68.log
exit=1
VERDICT: FAIL (8)                        ← ожидалось exit=1, VERDICT: FAIL (8)

$ grep -E '^(PASS|FAIL|VERDICT)' /tmp/rv-m68.log
PASS: cargo fmt --all -- --check
FAIL: cargo clippy --all-targets --all-features -- -D warnings
FAIL: cargo test --all --quiet
FAIL: cargo test -p gateway --test red_depth_from_book --quiet
PASS: A состав набора — 9 оракулов (ожидалось ровно 9: d1 d2 d3 d4 d5 d7 d7b d8 d8b)
FAIL: B SETUP НЕ СОСТОЯЛСЯ — якоря мутации 'MUT-ANCHOR C-M68-1' в реализации НЕТ.
FAIL: cargo test -p gateway --test red_depth_recompute_cost --quiet
FAIL: D GATEWAY_SCHEMA_VERSION >= 9 (на момент спеки было 8)
FAIL: cargo test -p gateway --test red_gateway_schema_version --quiet
PASS: cargo test -p gateway --test red_gateway_bounded --quiet
PASS: cargo test -p gateway --test red_snapshot_noclone --quiet
PASS: cargo test -p gateway --test red_gateway_live_eq_replay --quiet
FAIL: cargo test -p gateway --test red_depth_provenance_by_reach --quiet
PASS: H crates/contracts не тронут
PASS: I GATEWAY_BANDS в docker-compose.yml не тронут
PASS: J selector_fingerprint не переписан
PASS: K book/venue/journal/роадмап не тронуты диапазоном
VERDICT: FAIL (8)
```

**Восемь красных — поимённо те, что перечислены спекой `:481`–`:490`** как ожидаемые на
committed-RED. Ни одного лишнего, ни одного недостающего: базовая линия НЕ сдвинута
закрывающим коммитом.

## Block — запреты `A-023` §4.2 проверены ИСПОЛНЕНИЕМ

| запрет | команда | результат |
|---|---|---|
| три рантайм-ассерта не тронуты | `git diff … \| grep '^[+-].*assert'` → exit=1; `grep -c 'assert_eq!'` pre/post = `2`/`2`; `grep -nE 'assert'` pre `110,132,158,162` → post `123,147,173,177` (сдвиг +13 у первого — рост шапки; +15 у остальных — шапка плюс сообщение ассерта теста 1, выросшее на две строки; состав тот же) | ✓ |
| исторические литералы не удалены | `grep -nE 'M-38a: 6→7\|M-48: 7→8\|M-68: 8→9'` → `:24`, `:30`, `:67`, `:68` | ✓ |
| §Tasks не перенумерован | `grep -cE '^\| [0-9]+ \|'` pre/post = `24`/`24`; таблицы нет в дифе | ✓ |
| реализация не тронута | в `--name-only HEAD^..HEAD` нет ни одного `src/` | ✓ |
| `verify_M-68.sh`, полосы, состав выдачи не тронуты | в `--name-only HEAD^..HEAD` отсутствуют; шаги `I`/`J` гейта PASS | ✓ |
| тело коммита не объявляет закрытым без команды рядом | тело несёт блок «ЗАКРЫВАЮЩИЕ КОМАНДЫ … прогнаны, сырой вывод» с exit-кодами | ✓ (с оговоркой B-1: список тела — восемь строк, не десять) |

Отдельно: правка сообщения ассерта теста 1 (`M-48:` → `M-68:`) запрет НЕ нарушает — она
ПРЕДПИСАНА `A-023` §4.2 вторым пунктом («добор условия `C-162` п.2»). Логика ассерта
(`assert_eq!(GATEWAY_SCHEMA_VERSION, EXPECTED_SCHEMA_VERSION, …)`) не изменена.

## Block — свежий взгляд на класс `A-023` §2.1 (моя собственная вычитка)

**Спека.** Полная опись ссылок на номера задач против действующей таблицы §4
(`:284`–`:296`), команда `grep -nE 'задач[а-яё]*[[:space:]]+[0-9]+'`:

| строка | ссылка | таблица §4 | вердикт |
|---|---|---|---|
| `:44` | «в черновике rev3 — 5, в действующей таблице §4 — 7» | 7 = охват `depth_reach_*` | ✓ якорная форма, читатель уходит в верную строку |
| `:114` | задача 9 | 9 = bump | ✓ |
| `:224` | задача 8 добавляет `depth_levels_visited` | 8 = счётчик уровней | ✓ |
| `:229` | врезка к задаче 1 | 1 = помощник с фикс. сигнатурой | ✓ |
| `:236`, `:243`, `:326`, `:348` | задача/бюджет 8 | 8 | ✓ (`:243` теперь согласован с `:348` — один факт, один номер) |
| `:274` | «см. §4, строки о чекпоинте и о bump'е версии» | 6 и 9 | ✓ и ЛУЧШЕ номера: версионно-агностичная ссылка не протухает |
| `:298`, `:351`, `:488`, `:489` | задача 9 | 9 | ✓ |
| `:462`, `:490` | задача 7 | 7 | ✓ |
| `:483`–`:487` | задачи 8 / 1,2,4,7,9 / 1 | — | ✓ (колонка — «что его зазеленит», не «чья это задача») |

Битых ссылок класса §2.1 **не осталось**. Отдельно проверил кажущееся расхождение, которое
легко принять за экземпляр класса: `verify_M-68.sh:56` называет шаг `B` «задача 4», а спека
`:486` в строке шага `B` пишет «задача 1». Противоречия НЕТ — колонка спеки называет то,
что шаг ЗАЗЕЛЕНИТ (якорь мутации вводится задачей 1), а заголовок шага называет инвариант,
который шаг пиннит (задача 4, «каждая полоса»); действующая таблица §4 в оракульной колонке
задачи 4 прямо называет «шаг B гейта». Реоткрытия не требуется.

**Оракул.** Проверил, что новое самоописание шапки само выдерживает свою же команду. Полная
опись вхождений значения (`grep -nE '(^|[^0-9_])9([^0-9_]|$)'`): `:24` (история
«M-38a/M-48/M-68»), `:45` (датированный заголовок секции «M-68 (2026-08-25): 8 → 9»),
`:48`/`:49`/`:51` («задача 9» — НОМЕР ЗАДАЧИ), `:50` (транскрипт падения, прошедшее время),
`:68` (чейнджлог константы), `:73` — единственное **объявление**
`const EXPECTED_SCHEMA_VERSION: u32 = 9`. Ни одного утверждения в настоящем времени о
текущем значении вне объявления ⇒ тест на протухание `A-023` §1.3 файл проходит, и шапка
теперь обещает ровно столько, сколько показывает команда.

## Условие APPROVED и его границы

**APPROVED** относится к ЗАКРЫВАЮЩЕМУ КОММИТУ `6540d75` и означает ровно то, что назвал
арбитр в `A-023` §3.2 п.3: plan-time гейт M-68 закрыт, круг 7 критика не созывается,
architect диспетчеризует `engine-dev`.

**Чего APPROVED НЕ означает — названо явно:**

1. **В `main` эта ветка сейчас НЕ идёт.** Набор — committed-RED: `verify_M-68.sh` даёт
   `exit=1`, `cargo test --all` красен по конструкции (COMPILE-RED `d6a`/`d6b`).
   `gates.md` §8: «RED до реализации не живёт в `main`». Ветка вливается PR'ом ПОСЛЕ
   GREEN'а реализации и второго PR-гейта.
2. **Это не вердикт по СУЩЕСТВУ предмета M-68.** Суть (каденция, полосы, состав выдачи,
   `П-014`) закрыта кругами 4–6 и решением `A-023`; здесь судится исполнение §4.
3. **Close-out M-68 (PROJECT-STATE + деплой-гейт §8) не наступил** и наступит на merge'е
   после реализации.

## Остаток, переданный явно

`A-023` §5 назначает reviewer'у TD-запись остаточного риска класса «документ ложно описывает
себя / ссылается на номер, которого таблица не подтверждает». Механическая проба (маркерная
форма, `A-023` §2.2) арбитром ПРИНЯТА по конструкции, но строится только после разморозки
`П-017` A2 подписью founder'а. До подписи класс — `COGNITIVE-ONLY`.

Карточка заведена **`TD-167`** (номер выдан `scripts/next_artifact_id.sh`, зарезервирован).
Завожу её СЕЙЧАС, а не в close-out M-68: `binding-requires-mechanism` удовлетворяется второй
ветвью («признай остаточный риск записью»), и запись, отложенная на неделю реализации, эту
ветвь не исполняет. Требование `A-023` §5 этим исполнено с опережением, а не обойдено.

---

## Вердикт

**APPROVED** — закрывающий коммит `6540d75` соответствует `A-023` §4 поимённо: десять из
десяти команд §4.3 дали ожидаемое в моём прогоне, пять из пяти запретов §4.2 соблюдены,
диф — ровно два предписанных файла, базовая линия committed-RED не сдвинута
(`VERDICT: FAIL (8)`, exit=1, восемь красных — те самые). Класс §2.1 при независимой
вычитке битых экземпляров больше не даёт. `A-023` §3.3 не сработал: ни одна команда не
провалена, диф в границах, тело коммита ничего не объявляет закрытым без команды.

Пробелы прогона tester'а (B-1, B-2, B-3) — находки о ГЕЙТЕ, не о предмете: они закрыты этим
вердиктом, потому что пропущенные команды прогнаны здесь. Именно на этот случай `A-023` §5
и держит reviewer'а безусловным бэкстопом.

**Дальше:** architect диспетчеризует `engine-dev` по §Tasks M-68.
