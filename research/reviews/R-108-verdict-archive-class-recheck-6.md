<!-- GATE-META
milestone: PR-56
audited_repo: a3ka/hft-platform
audited_base: 40c7cce30ff10eb787caaafccb4b809794c503ee
audited_head: 45868c0eff5309dd18ca4aa9c12dbac15c8c73c4
verdict: PASS
-->

# R-108 — перепроверка `gates.md` §9, круг 6: PR #56, равенство СТРОКИ

- Роль: клон architect (Fable, свежий контекст). `R-107` прочитан целиком с ветки;
  «Находки» `R-100`…`R-103` — целиком; `П-017` A2/A3 — открыты.
- Предмет: ветка `harness/verdict-archive-class`, вершина `45868c0`. Диф круга —
  ТОЛЬКО `scripts/tests/red_verify_design_claims.sh` (`git diff 6537cfa..45868c0 --stat`:
  1 файл, +23/−18). `origin/main` = `40c7cce` (снято в этом прогоне).

## ВЕРДИКТ: PASS

Прямо и без оговорок: условие APPROVED `R-107` выполнено буквой, все пятнадцать стабов
пяти кругов гибнут, и девять моих новых адверсарных стабов круга 6 гибнут тоже — включая
все оси, названные мандатом (две строки, порядок, пробел, stderr, молчание при непустом
классе, порча исключения при верной строке). Утверждение `R-107` «шестого адверсарного
круга по этой поверхности не существует» ПРОВЕРЕНО ломанием, а не принято на веру, и в
границах названной поверхности — подтверждено: наблюдаемый канал наблюдателя один
(строка stdout + exit-код), и строковое равенство `[ "${got}" = "${want}" ]` по всему
выводу `grep 'V-АРХИВ'` покрывает его целиком. Что осталось ВНЕ поверхности — названо
ниже (§«Остаточная поверхность»), оно латентно, лгущей строки не производит и открытым
множеством не закрывается чёрным ящиком в принципе.

## Проверка конструкции — сравнение действительно строковое и по всему выводу

`scripts/tests/red_verify_design_claims.sh:711-713` (V-4) и `:753-755` (V-7):
`got="$(echo "${out}" | grep 'V-АРХИВ')"` собирает ВСЕ строки вывода, несущие `V-АРХИВ`
(вторая строка ⇒ многострочный `got` ⇒ неравенство); `want="$(expected_varhiv '…')"` —
литерал-константа с подставленным списком; ассерт — `[ "${rc}" -eq 0 ] && [ "${got}" =
"${want}" ]`. Нормализации нет нигде: ни trim, ни регистр, ни пословный греп.
`run_verify` (`:170-171`) захватывает ТОЛЬКО stdout ⇒ печать в stderr даёт пустой `got` ⇒
FAIL. Порядок имён в `want` — порядок кортежа, то есть запиннен. Пределы `R-103` F-2/F-3
(экстенсионально тождественная копия носителя; пустой каталог класса) названы докстрокой
`expected_varhiv` — условие `R-107` о докстроке выполнено.

## Мутационный контроль — 15/15 прошлых стабов, таблица коммита воспроизведена строка в строку

Все мутации — на копиях (`BARRIER=/tmp/rc56f/*.sh`), рабочее дерево не тронуто.
Базовый прогон: **67 PASS / 0 FAIL**; состав сценариев против `42d0b33` ИДЕНТИЧЕН
(diff перечней `scenario_*` пуст) — прежние сценарии не ослаблены и не удалены.

| стаб | заявка коммита `45868c0` | мой прогон |
|---|---|---|
| M1 исключение снято | V-2 V-5 V-6 | V-2 V-5 V-6 ✓ |
| M2 класс на весь research/ | V-3 V-4 V-7 | V-3 V-4 V-7 ✓ |
| MC2 critiques выпал | V-4 V-5 V-7 | V-4 V-5 V-7 ✓ |
| MC6 объявление обходит research/ | V-4 V-7 | V-4 V-7 ✓ |
| MC7 кортеж без слэшей | V-4 V-7 | V-4 V-7 ✓ |
| MC9 `present[:2]` | V-4 (только) | V-4 ✓ (F-2 `R-107` признана и исправлена) |
| NC1 переобъявление reports/ | V-4 V-7 | V-4 V-7 ✓ |
| NC1h переобъявление hypotheses/ | V-4 V-7 | V-4 V-7 ✓ |
| mc8a без isdir | V-7 | V-7 ✓ |
| mc8b печать-литерал | V-7 | V-7 ✓ |
| NC1ns reports без слэша | V-4 V-7 | V-4 V-7 ✓ (лидер `R-107` F-1 — мёртв) |
| S1 вложенный путь | V-4 V-7 | V-4 V-7 ✓ |
| S1all все имена вложенными | V-4 V-7 | V-4 V-7 ✓ |
| S2 точка в имени | V-4 V-7 | V-4 V-7 ✓ |
| S3 проза INFO инвертирована | V-4 V-7 | V-4 V-7 ✓ |

15/15, ноль расхождений — впервые за шесть кругов таблица автора совпала с прогоном
полностью.

## Круг 6 — девять новых стабов, все гибнут

Оси канала строки (все → V-4 V-7): **t1** наблюдатель печатает ДВЕ строки `V-АРХИВ` —
многострочный `got` ≠ `want`; **t2** печать в stderr — `got` пуст (доказано:
`2>/dev/null | grep -c` → 0, `2>&1 >/dev/null | grep -c` → 1); **t3** не печатает вовсе
при непустом классе — отсутствие наблюдаемо, не только сбой (`testing.md` §4 св-во 4);
**t4** хвостовой пробел; **t5** порядок имён развёрнут.

Оси «исключение испорчено, строка ВЕРНА» (стдаут-строка каждого сверена с оригиналом на
реальном дереве — байт-идентична): **x2a** arbitration/ выпал только из `is_excluded` →
V-6; **x2b** critiques/ → V-5; **x2c** reviews/ → V-2; **x3** `is_excluded` молча расширен
на `research/reports/` → V-3. Матрица «каталог × поведение» закрыта по всем трём именам
класса и по названному соседу, независимо от объявления.

## Остаточная поверхность — названа честно, блокером не является

Стаб **xh**: `is_excluded` молча расширен на `research/hypotheses/` (каталог вне класса и
вне фикстур) — проба 67/0, вывод гейта на реальном дереве БАЙТ-ИДЕНТИЧЕН оригиналу
(`diff` пуст — битых ссылок в hypotheses сегодня нет). Это НЕ опровержение `R-107`, по
трём основаниям: (а) строка `V-АРХИВ` остаётся истинной — она объявляет АРХИВНЫЙ КЛАСС, а
не полноту всех исключений (`docs/plans/**` без маркера точно так же исключён и в строке
не значится с самого рождения гейта); (б) регресс латентен — тот же класс, что MC7 в
`R-101` F-3, ранжированный там латентным NOTE, ранг предметом задан; (в) множество
«посторонних каталогов, которые можно молча исключить» открыто — конечная проба его не
перечислит, граница запиннена в самой вероятной точке протечки (V-3, сосед по
`research/`) и во всех трёх именах класса (V-2/V-5/V-6, мои x2a-c). Это предел ЛЮБОГО
deny-list-гейта, существующий с рождения `is_excluded`, а не поверхность, которую `R-107`
объявил закрытой. Фикс не проектирую (граница reviewer↔architect, `gates.md` §4);
наблюдение оставляю ведущему architect'у как кандидата в докстроку пределов — решение за
ним, PASS от этого не зависит.

Косметика (не находка): `fail()` — `echo` без `-e`, поэтому `\n` в новых FAIL-сообщениях
V-4/V-7 печатается литералом. Живёт только в красном пути, счёт FAIL не искажает.

## Ответ на вопрос мандата о конечности

Закрытие СОСТОЯЛОСЬ. Спираль держалась на прокси; прокси убран — и 24 стаба из 24
(15 прошлых + 9 моих) гибнут об один ассерт. Механизм цену оправдал: шесть кругов купили
не «ещё один греп», а терминальную форму, у которой канал покрыт весь и пределы названы
письменно. Основание считать круг 6 последним по этой поверхности — не обещание автора, а
структура: у наблюдателя один выходной канал, и он сравнивается целиком.

## Полномочия и связность — OK

- Диф круга: один файл `scripts/tests/**` — sacred architect'а, автор architect. Замок §11
  не задет (`git show 45868c0 --name-only`: зона §11 не тронута), `FOUNDER-APPROVED` не
  требуется. Co-author трейлера нет. ✓
- `П-017` A2/A3: предмет — исполнение существующего `TD-064` по решению founder'а
  2026-08-22 (названо в коде и мандатах кругов), не новый харнесс; впрыскиваемое ядро
  дифом не тронуто. ✓
- `check_review_fa`: диф ветки (merge-base `d61904d`..`45868c0`) вне `crates/**` → SKIP;
  FA-инвариант не требуется. ✓
- Merge-preview на свежем tip'е: `VERDICT: PASS (0 нарушений)`, exit=0 (`40c7cce`). ✓
- §0: шестой круг — порог арбитража давно перейдён; founder решил вести кругами
  (зафиксировано мандатами), спора сторон нет: круг 6 подтверждает исполнение вердикта
  круга 5, расхождений не выявил. ✓

## Done Block

```
$ git -C /tmp/hft-recheck-56f log -1 --format='%H %s'
45868c0eff5309dd18ca4aa9c12dbac15c8c73c4 fix(harness): R-107 — прокси убран, сравнивается СТРОКА целиком [architect]

$ bash scripts/tests/red_verify_design_claims.sh | awk '/^PASS/{p++}/^FAIL/{f++}END{print "PASS="p" FAIL="f+0}'
BASELINE: PASS=67 FAIL=0

$ for m in m1 m2 mc2 mc6 mc7 mc9 nc1 nc1h nc1ns mc8a mc8b s1 s1all s2 s3; do BARRIER=/tmp/rc56f/$m.sh …; done
m1: FAIL=3 [V-2,V-5,V-6] · m2: FAIL=3 [V-3,V-4,V-7] · mc2: FAIL=3 [V-4,V-5,V-7] ·
mc6: FAIL=2 [V-4,V-7] · mc7: FAIL=2 [V-4,V-7] · mc9: FAIL=1 [V-4] ·
nc1: FAIL=2 [V-4,V-7] · nc1h: FAIL=2 [V-4,V-7] · nc1ns: FAIL=2 [V-4,V-7] ·
mc8a: FAIL=1 [V-7] · mc8b: FAIL=1 [V-7] ·
s1: FAIL=2 [V-4,V-7] · s1all: FAIL=2 [V-4,V-7] · s2: FAIL=2 [V-4,V-7] · s3: FAIL=2 [V-4,V-7]

$ for m in x2a x2b x2c x3 t1 t2 t3 t4 t5; do BARRIER=/tmp/rc56f/$m.sh …; done
x2a: FAIL=1 [V-6] · x2b: FAIL=1 [V-5] · x2c: FAIL=1 [V-2] · x3: FAIL=1 [V-3] ·
t1: FAIL=2 [V-4,V-7] · t2: FAIL=2 [V-4,V-7] · t3: FAIL=2 [V-4,V-7] ·
t4: FAIL=2 [V-4,V-7] · t5: FAIL=2 [V-4,V-7]

$ bash /tmp/rc56f/t2.sh . 2>/dev/null | grep -c 'V-АРХИВ'; bash /tmp/rc56f/t2.sh . 2>&1 >/dev/null | grep -c 'V-АРХИВ'
0
1          # t2 печатает ТОЛЬКО в stderr — и проба его валит (got пуст)

$ BARRIER=/tmp/rc56f/xh.sh bash scripts/tests/red_verify_design_claims.sh | grep -c '^FAIL'; \
  diff <(bash scripts/verify_design_claims.sh .) <(bash /tmp/rc56f/xh.sh .) && echo ИДЕНТИЧНЫ
0
ИДЕНТИЧНЫ   # остаточная ось: латентна, лгущей строки нет

$ diff <(git show 42d0b33:scripts/tests/red_verify_design_claims.sh | grep -oE '^scenario_[a-z0-9_]+\(\)') \
       <(grep -oE '^scenario_[a-z0-9_]+\(\)' scripts/tests/red_verify_design_claims.sh) && echo ИДЕНТИЧЕН
ИДЕНТИЧЕН   # состав сценариев не менялся

$ git log -1 --format='%H' origin/main; bash scripts/verify_design_claims.sh --merge-preview origin/main | grep -E '^VERDICT'; echo exit=$?
40c7cce30ff10eb787caaafccb4b809794c503ee
VERDICT: PASS (0 нарушений)
exit=0

$ git diff 6537cfa..45868c0 --stat
 scripts/tests/red_verify_design_claims.sh | 41 +++++++++++++++++--------------
 1 file changed, 23 insertions(+), 18 deletions(-)
```
