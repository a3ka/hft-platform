<!-- GATE-META
milestone: M-87
audited_repo: a3ka/hft-platform
audited_base: 3a2e4607882756f660d664be5d2ba45780af0a4d
audited_head: 332fafa226bcbe56446d039f5bcbc2bdac5ee78a
verdict: APPROVE
-->

# R-205 — M-87 «предохранитель выдачи»: PR-гейт круга условий `R-204`

**Вердикт: APPROVED.** Оба условия `R-204` исполнены одним коммитом architect'а, и исполнение
предъявлено не словом, а прогонами: блокер `Б-1` снят, его снятие **привязано мутацией в обе
стороны** (мутация E возвращает красное дословно, мутация F краснеет на обратном классе),
приёмка милестоуна зелена целиком (**32 PASS / 0 FAIL / 1 SKIP**, `exit=0`), и **все 11
барьеров репозитория зелены на ДЕРЕВЕ СЛИЯНИЯ**, а не только на ветке. Ветка вливается.

Круг был ровно того размера, что назначил `R-204`: **одна цифра и одна пустая строка**. Кода
не тронуто ни строки — и это правильно: код закреплён мутациями прошлых трёх кругов, трогать
его было бы вредом (`scope-guard.md` §«Три жёстких правила» п. 2).

---

## §0 — Предмет: вершина взята КОМАНДОЙ, дважды

`.claude/agents/reviewer.md` + `docs/04-workflow.md` §2 «Предмет гейта — ВЕТКА».

```
$ git fetch origin && git rev-parse origin/feat/M-87-serving-circuit-breaker
332fafa226bcbe56446d039f5bcbc2bdac5ee78a
$ git merge-base --is-ancestor 332fafa origin/feat/M-87-serving-circuit-breaker; echo $?
0                     # SHA из мандата — предок вершины (здесь: он же и есть вершина)
```

- **Повторный `fetch` перед записью вердикта: вершина НЕ сдвинулась** — `332fafa` → `332fafa`.
- `origin/main` = `949a606` — **тот же**, против которого `R-204` снимал merge-preview. Ветка
  отстаёт на 7 коммитов; `strict:false` (`gates.md` §8) применим, поэтому всё судится на
  ДЕРЕВЕ СЛИЯНИЯ, а не на ветке (см. §Барьеры и Done Block).
- Свой чекаут: `/tmp/hft-reviewer-m87-r205` (detached `332fafa`), общий чекаут не тронут.
  Дерево слияния собрано отдельным worktree `/tmp/hft-r205-mp`
  (`git merge --no-ff origin/main` → `dfc60b7`), выброшен после замеров.
- Диапазон аудита: `3a2e460..332fafa` — **129** коммитов, `3a2e460 = git merge-base
  origin/main 332fafa`.
- Номер вердикта взят механизмом и **зарезервирован** (`gates.md` §12):
  `next_artifact_id.sh R` → `R-205`, `reserve_artifact_id.sh R` → `R-205 ← 474c01b`.

## Блок-scope — PASS, с одним названным NOTE

**Диф СОБСТВЕННО круга** — `git diff --numstat 43223ad 332fafa`:

```
1	1	docs/DESIGN.md
0	1	milestones/M-87-serving-circuit-breaker.md
597	0	research/reviews/R-204-M-87-r202-conditions-pr-gate.md
```

Две строки правки + мой предыдущий вердикт (артефакт гейта, приземлён коммитом `a18e947`).
Оба правочных файла — зона architect'а (`scope-guard.md`: `docs/`, `milestones/M-NN-*.md`);
метка `[architect]` в subject'е стоит.

**NOTE (не нарушение): `docs/DESIGN.md` НЕ входит в §12 «Allowed paths» милестоуна.** §12
перечисляет architect'у `milestones/M-87-*.md` · `crates/gateway{,-serve}/tests/**` ·
`scripts/verify_M-87.sh` · `docs/fa/viz-backend.md`. Правка `DESIGN.md` лежит вне этого
перечня, но **внутри глобальной зоны architect'а** и **предписана моим же условием `R-204`
п. 1 дословно** («Зона — architect (`docs/**`)»). Трактую так же, как `R-204` трактовал
`docs/ROADMAP.md`: NOTE, не превышение. Обратное означало бы, что условие вердикта нельзя
исполнить, не нарушив милестоун.

**Запрещённые зоны за ВЕСЬ диапазон — пусто, проверено грепом, не памятью:**

```
$ git diff --name-only 3a2e460..332fafa | grep -E '^crates/(contracts|risk|killswitch|oms|venue-|journal|book)/'
{пусто}
```

Полный состав диапазона (129 коммитов, по каталогам): `crates/gateway-serve/{tests×20,src×5,
src/bin×1,tests/m87_registry×1}` · `crates/gateway/{tests×1,src×1}` · `research/{critiques×16,
reviews×5,arbitration×4}` · `docs/{DESIGN.md,ROADMAP.md}` · `docs/fa/viz-backend.md` ·
`scripts/verify_M-87.sh` · `milestones/M-87-*.md` · `docker-compose.yml`.

`docker-compose.yml` (+50/−0 за диапазон) — объявлен §12 как зона engine-dev; **состава
записи (граница C, `П-008`) не меняет**, и это подтверждено не чтением, а барьером:
`check_rollout_composition.sh` → `PASS связка: состав не менялся против базы
(BTCUSDT,ETHUSDT)`. Добавленный `GATEWAY_ALLOWED_SYMBOLS` — ручка политики ВЫДАЧИ (чтения),
а не перечень записываемых инструментов.

## Блок-C (контракты) — N/A

`crates/contracts/**` в дифе диапазона отсутствует, `GATEWAY_SCHEMA_VERSION` не бампается.
Contract-RFC не требуется.

## Блок-risk — N/A (risk-critic НЕ требуется)

`gates.md` §5 привязан к `risk|killswitch|oms|venue-*|contracts` — ни один путь не тронут
(греп выше). `gateway`/`gateway-serve` — read-only консюмер журнала (`VB-I-3`,
`docs/fa/viz-backend.md:206`), order-egress отсутствует. §9 (safety-документы) не
срабатывает: тронуты `docs/fa/viz-backend.md` и `docs/DESIGN.md` §22 — не
`fa/risk|killswitch|oms`, не `RK-I-*`/`INTG-I-*`, не анти-оверфит гейт.

## Блок-RED-first — PASS

Диф круга по коду и тестам — **ноль**:

```
$ git diff --name-only 43223ad 332fafa -- 'crates/*/tests/*' 'crates/*/src/*' | wc -l
0
```

За весь диапазон тесты правили **36** коммитов, и **все 36 несут метку `[architect]`**
(`git log --format='%s' 3a2e460..332fafa -- 'crates/*/tests/*' | grep -oE '\[[a-z-]+\]$' |
sort | uniq -c` → `36 [architect]`). Ни один dev не правил sacred-спеку.

## Блок-DoneBlock — PASS

Приёмка воспроизведена **мной, на своём чистом чекауте**, прод-формой вызова (без аргументов):
`32 PASS / 0 FAIL / 1 SKIP`, `VERDICT: PASS`, `exit=0`. Числа **посчитаны**
(`grep -cE '^PASS'`), а не сверены с обещанием. Единственный `SKIP` — `task9` (фактические
лимиты снимаются на проде `docker inspect`, шаг деплой-гейта §8). Сырой вывод — в Done Block.

## Блок «механизм на пути» (DoD, `gates.md` §4) — позиция не изменилась

Задача 24 остаётся `🟡 ЧАСТИЧНО`: бюджет вызова с прод-пути снят и не подключён — законный
вариант «б», разрешённый `R-201`. Карточка «built-not-wired» MAJOR заводится на close-out
(§Долг п. 1). Merge не блокирует: долг **виден**, а не подразумевается.

---

# УСЛОВИЯ `R-204` — ИСПОЛНЕНЫ ОБА

## Условие 1 — `DESIGN.md` §22, `в оракулах` 8 → 9. ✅ ИСПОЛНЕНО

Коммит `332fafa`, `1+/1−` в `docs/DESIGN.md:920`:

```
- | VB-I | viz-агрегаты | 11 | 8 | [ЧАСТИЧНО] |
+ | VB-I | viz-агрегаты | 11 | 9 | [ЧАСТИЧНО] |
```

**Число 9 — не принято на слово, а измерено независимо от барьера:**

```
$ grep -rhoE '\bVB-I-[0-9]+\b' crates/*/tests/ | sort -uV | tr '\n' ' '
VB-I-1 VB-I-2 VB-I-3 VB-I-4 VB-I-5 VB-I-6 VB-I-9 VB-I-10 VB-I-11        # девять
```

**Оракул `VB-I-9` НЕ СНЯТ** — условие `R-204` требовало этого явно:

```
$ grep -rn 'VB-I-9' crates/*/tests/
crates/gateway-serve/tests/red_jwt_verify.rs:1:      … (D6/VB-I-9b)          # половина, не токен
crates/gateway-serve/tests/red_m87_secret_form.rs:12:  … охраняет `VB-I-9`.  # ← введён поставкой
crates/gateway-serve/tests/red_m87_secret_form.rs:99:  `VB-I-9`: то, что подписал …
```

**Заявление коммита о происхождении девятого — проверено, ПРАВДА:** на `origin/main` в
`crates/*/tests/**` живёт только `VB-I-9b` (`git grep -c 'VB-I-9' origin/main -- 'crates/*/tests/*'`
→ одно попадание, `red_jwt_verify.rs:1`), голый токен `VB-I-9` приносит именно ветка. Оракул
законен по существу: он судит ТРАКТОВКУ ключа при stateless-проверке JWT — ровно предмет
`VB-I-9` (`docs/fa/viz-backend.md:212`: «auth = ТОЛЬКО stateless verify подписанного JWT, без
user-БД-lookup»). Лгал документ, а не код, и исправлен документ.

`[ЧАСТИЧНО]` остаётся верным: 9 < 11.

**Предъявление прогоном на ДЕРЕВЕ СЛИЯНИЯ (как требовало условие 3, по коду возврата):**

```
$ bash scripts/verify_design_claims.sh --merge-preview origin/main
PASS  [2-ПОКРЫТИЕ] §22: VB-I — заявлено=11, в оракулах=9 — подтверждено замером (loose=9)
VERDICT: PASS (0 нарушений)
exit=0
```

И прод-формой вызова (без аргументов) на самом мерж-коммите `dfc60b7`: `VERDICT: PASS`, `exit=0`.

## Условие 2 — пустая строка §13 убрана, таблица цельна. ✅ ИСПОЛНЕНО

Тот же коммит, `0+/1−` в `milestones/M-87-*.md`. Проверено структурно, а не по дифу:

```
$ awk 'NR>=503 && NR<=521 && $0 ~ /^$/ {print NR": EMPTY"}' milestones/M-87-serving-circuit-breaker.md
{пусто}                      # строки 503 (шапка) … 521 (задача 18) — таблица непрерывна
$ grep -cE '^\| *[0-9A-Za-zбисbis]+ *\| *(⏳|✅|🟡|❌)' milestones/M-87-serving-circuit-breaker.md
28                           # столько же строк-задач, сколько было: ни одна не потеряна
```

Задачи 17–27 и 18 снова внутри таблицы. Ближайшие пустые строки — 499/502 (граница между
блоком задач 1–10 и шапкой блока `R-196`) и 522 (после задачи 18), то есть вне таблицы.

## Условие 3 — приёмка перепрогнана целиком + design-claims. ✅ ИСПОЛНЕНО

`verify_M-87.sh` → 32 PASS / 0 FAIL / 1 SKIP, `exit=0`; `verify_design_claims.sh
--merge-preview origin/main` → `exit=0`. Оба прогона — мои, не пересказ. Решение принято по
коду возврата (`gates.md` §3), а не по тексту.

## Условие 4 — отставание на 7 коммитов закрыто предъявлением на дереве слияния. ✅

`strict:false` означает, что зелёный чек можно снять на старой базе. Поэтому я не ограничился
merge-preview одного скрипта: **все 11 барьеров агрегата прогнаны на настоящем мерж-коммите**
(§Барьеры). Синхронизация ветки не потребовалась — merge чистый, конфликтов нет
(`git merge --no-ff origin/main` → 8 файлов, 492+/118−, ни одного конфликта).

---

# БЛОКЕРОВ НЕТ

---

# ПРИМЕЧАНИЯ (не блокируют)

**Н-1 — НОВАЯ находка: колонка `Заявлено` для `VB-I` тоже протухла, но НЕ этой веткой.**
Шапка §22 определяет колонки как «заявлено в docs / упомянуто в тестах»
(`docs/DESIGN.md:915`). Замер:

```
$ grep -hoE '\bVB-I-[0-9]+\b' docs/fa/viz-backend.md | sort -uV | tr '\n' ' '
VB-I-1 … VB-I-11 VB-I-12          # двенадцать
$ sed -n '920p' docs/DESIGN.md
| VB-I | viz-агрегаты | 11 | 9 | [ЧАСТИЧНО] |      # заявлено=11
```

`VB-I-12` завёл коммит `4460269` (работа M-88), и он **уже в `origin/main`** — расхождение
наследуется, ветка его не вносит. Барьер его не видит: `verify_design_claims.sh` сверяет с
замером ТОЛЬКО колонку «в оракулах», а «Заявлено» лишь проверяет на числовую форму
(`:551`). Это тот же класс, что `Б-1` вердикта `R-204`, в непокрытой барьером половине
таблицы. Не блокер (не введено этой поставкой, агрегат зелен, `DESIGN.md` — зона architect'а);
в долг — §Долг п. 6.

**Н-2 — пределы барьера `task-status` остаются, позиция `R-204` Н-1/Н-2/Н-3 не изменилась.**
Наличие заявления об остатке проверяется, правдивость — нет (мутация C `R-204`); `2bis` вне
регэкспа; `🟡` попадает в предфильтр, но отбрасывается `case`-веткой. Цена сегодня нулевая
(измерено там же). В долг — §Долг п. 4. Правдивость обоснований задач 15 и 16 я не принимал
на веру и в этом круге: они не менялись (`git diff 43223ad 332fafa -- milestones/` — только
удалённая пустая строка), а их соответствие дереву предъявлено замером в `R-204` §Условие 1,
который я здесь подтверждаю точечно:
`grep -c 'feed_tail_within(' crates/gateway-serve/src/lib.rs` → **0** (бюджет по-прежнему не
подключён — статус `OPEN` задачи 16 верен).

**Н-3 — приёмка милестоуна по-прежнему ЗЕЛЕНЕЕ агрегата CI на 13 джобов из 17**
(перенос `R-204` Н-6). Именно поэтому `Б-1` дожил до четвёртого круга. В этом круге дефект
не сработал только потому, что я прогнал барьеры **сам, руками, на дереве слияния** — то
есть защита держится на ролевой дисциплине, а не на механизме. Лечить внутри `verify_M-87.sh`
неправильно (четвёртая копия того же списка); предмет harness-трека под `П-017` A2. В долг —
§Долг п. 5.

**Н-4 — §9 перепроверка: покрыта ЭТИМ вердиктом, пункты (а)–(в) поимённо.** Диф круга трогает
`docs/DESIGN.md` — уставная зона `gates.md` §9. Модель сильная, контекст свежий (сессия
открыта с нуля, предмет читан с ветки, а не из памяти прошлого круга), автор правки — не я.
- **(а) каждое утверждение О КОДЕ — командой на дереве слияния.** Утверждений в правке два, и
  оба числовые: «в оракулах = 9» (проверено `grep -rhoE` по `crates/*/tests/` → девять
  уникальных ID; независимо — барьером на мерж-коммите, `exit=0`) и «прибавился именно
  `VB-I-9`» (проверено `git grep` по `origin/main` против ветки). Утверждения тела коммита о
  законности оракула сверены с `docs/fa/viz-backend.md:212`.
- **(б) полномочия.** `docs/DESIGN.md` + `milestones/M-87-*.md` — зона architect'а
  (`scope-guard.md` §Таблица владения). Граница C не затронута: ни весов, ни лимитов, ни
  состава записи, ни промоушена (барьер `check_rollout_composition.sh` → PASS, состав не
  менялся). **Замок §11 НЕ тронут** — проверено, а не предположено:
  `git diff --name-only origin/main..HEAD | grep -E '^(\.claude/|CLAUDE\.md|docs/04-workflow\.md)'`
  → пусто; `check_docs_freeze.sh` на дереве слияния молчит, `exit=0`.
- **(в) связность и висячие ссылки.** `verify_design_claims.sh` на мерж-коммите: 182
  проверенных пути в `docs/rfc/**` существуют, `VERDICT: PASS`; `check_archived_refs.sh` →
  `PASS базовая линия актуальна (унаследованных ссылок: 29)`; таблица §13 после правки цельна
  (замер выше). Мёртвых ссылок правка не вносит — она удаляет одну пустую строку и меняет
  одну цифру.
  **Один остаточный дефект связности НАЙДЕН и назван — Н-1** (колонка `Заявлено` против FA);
  он наследуется из `main`, а не вносится, поэтому не блокер.

**Н-5 — задача 24 «built-not-wired»: карточка, а не оракул** (перенос `R-204` Н-5). Что
ловится: возврат холостого чтения. Что НЕ ловится: отсутствие самого предохранителя по
байтам/событиям.

---

# МУТАЦИОННЫЙ КОНТРОЛЬ — два вопроса, оба отвечены прогоном

`testing.md` §«Мутационный контроль»: «к чему привязан фикс» и «что ослаблено рядом».
Обе мутации — на дереве слияния `/tmp/hft-r205-mp` @ `dfc60b7`, прод-формой вызова
(`bash scripts/verify_design_claims.sh`, без аргументов — так её зовёт CI), с откатом после
каждой.

## E — привязка фикса: вернуть `9` → `8`, ждём КРАСНОЕ

```
$ sed -i '920s/| 11 | 9 |/| 11 | 8 |/' docs/DESIGN.md && sed -n '920p' docs/DESIGN.md
| VB-I | viz-агрегаты | 11 | 8 | [ЧАСТИЧНО] |
$ bash scripts/verify_design_claims.sh
FAIL  [2-ПОКРЫТИЕ] §22: семейство VB-I — документ заявляет 'в оракулах'=8, реальный замер
      (анти-плацебо) strict=9, loose (любое упоминание)=9
VERDICT: FAIL (1 нарушений)
exit=1
```

**Блокер `R-204` Б-1 воспроизведён ДОСЛОВНО, тем же текстом.** То есть APPROVED здесь стоит
не на «правка выглядит правильной», а на измеренной причинности: ровно эта цифра и ничто
иное держит агрегат.

## F — что ослаблено рядом: снять оракул, оставив `9` в документе, ждём КРАСНОЕ

Первый вопрос проверяет привязку, второй — не куплена ли зелень ценой соседнего инварианта.
Опасность направления «8 → 9» именно в этом: число, поднятое под факт, не должно стать
разрешением снять оракул незаметно.

```
$ sed -i 's/`VB-I-9`/`VB-I-НЕЙТР`/g' crates/gateway-serve/tests/red_m87_secret_form.rs
$ git diff --numstat
2	2	crates/gateway-serve/tests/red_m87_secret_form.rs
$ bash scripts/verify_design_claims.sh
FAIL  [2-ПОКРЫТИЕ] §22: семейство VB-I — документ заявляет 'в оракулах'=9, реальный замер
      (анти-плацебо) strict=8, loose (любое упоминание)=8
VERDICT: FAIL (1 нарушений)
exit=1
```

**Барьер симметричен, и после правки число ПИННИТ существование оракула.** Снять `VB-I-9`
молча стало невозможно: удаление оракула красит тот же джоб с обратным знаком. Ничего не
ослаблено — защищённость выросла в обе стороны.

## Откат и базовая линия

```
$ git checkout -- docs/DESIGN.md crates/gateway-serve/tests/red_m87_secret_form.rs
$ git status --porcelain
{пусто}
```

Базовая линия дерева слияния: `verify_design_claims.sh` → `VERDICT: PASS`, `exit=0`.
Worktree `/tmp/hft-r205-mp` снесён после замеров; рабочий чекаут `/tmp/hft-reviewer-m87-r205`
чист, `HEAD = 332fafa`.

---

# Предъявление FA (M-66)

Диапазон трогает `crates/gateway-serve/src/**` и `crates/gateway/src/lib.rs`. По таблице
маппинга `check_review_fa.sh:190-199` оба крейта отображаются в `docs/fa/viz-backend.md`
(префиксы `VB` и `GS`); собственного `fa/gateway-serve.md` не существует — факт, сам по себе
долг (перенос `R-201`/`R-202`/`R-204`). Живые инварианты на проверяемой ревизии `332fafa`,
использованные ПО СУЩЕСТВУ:

- **`VB-I-9`** (`docs/fa/viz-backend.md:212`) — «**Граница плоскостей (D6):** `gateway-serve`
  НЕ читает/не пишет application-БД; auth = ТОЛЬКО stateless verify подписанного JWT, без
  user-БД-lookup». **Несущий инвариант всего круга:** именно его охраняет оракул
  `red_m87_secret_form.rs`, чьё появление и подняло строгий счёт до девяти. Условие 1
  проверялось против ЭТОГО текста — «законен ли оракул, называющий себя `VB-I-9`»; ответ да:
  трактовка секрета при stateless-проверке подписи есть предмет инварианта, а не соседний.
- **`VB-I-11`** (`:214`) — «Провенанс ИСТОРИИ (M-48, `TD-048`) … система обязана НЕ ВЫДАВАТЬ
  ЭТО ЗА ДРУГОЕ». **Класс, по которому я судил Б-1 и сужу Н-1:** таблица, объявляющая восемь
  оракулов при девяти существующих (или одиннадцать инвариантов при двенадцати), выдаёт одно
  за другое — тот же вид нечестности, перенесённый с данных на документ. Снятие Б-1 закрывает
  половину, наследованная половина названа в Н-1, а не умолчана.
- **`VB-I-10`** (`:213`) — «Bounded-window snapshot (M-37, `TD-039`). Память
  `snapshot`/`frames_since` ограничена ОКНОМ … иначе host-OOM (прод: RSS 7.3GB)». **Опора
  оценки остатка задачи 15:** `read_dir` + чтение заголовков сегментов на горячем warm-пути
  есть цена, платимая на КАЖДОЙ сессии внутри этой границы; статус `OPEN` верен по этому
  инварианту, и обоснование в §13 называет причину, а не протухшую ссылку.
- **`GS-I-1`** (`:289`, помечен там же как `VB-I-9a`) — граница плоскостей со стороны
  транспорта. **Опора Блока-risk N/A:** `gateway-serve` остаётся тонкой read-only оболочкой,
  order-egress отсутствует, значит `gates.md` §5 не срабатывает по существу, а не по формальному
  несовпадению путей.
- `VB-I-3` (`:206`) — read-only Read Gateway; проверено, что диапазон его не нарушает.

**Ярус C — что искал грепом** (`reading-map.md` §2; `TECH-DEBT.md` + `PROJECT-STATE.md` вместе
≈ 226 k токенов, целиком не читаются, и я их ПИШУ): `TECH-DEBT.md` по `TD-21[0-9]`,
`built-not-wired` (**8** попаданий), `gateway-serve` (**72**), `TD-218`; `PROJECT-STATE.md` по
`M-87` (**0** попаданий — милестоун ещё не приземлён, ожидаемо). Найденное и относящееся к
предмету: `TD-217` (`контракт-обновления-серий-готов-на-сервере-фронт-его-не-исполняет`) —
прямой предшественник карточек §Долг п. 1–2, форму записи беру оттуда; `TD-218` — существует
как строка таблицы `TECH-DEBT.md:170`, но БЕЗ заводящей карточки-слага, из-за чего
`next_artifact_id.sh TD` врёт (отдаёт `TD-218` при занятом номере — проверено сегодня). Номера
карточек беру `TD-219`+, форму записи `TD-218` достраиваю сам на close-out; `TECH-DEBT.md` —
моя зона.

---

# ДОЛГ — карточки на close-out (заводит reviewer ПОСЛЕ merge)

Шесть: четыре перенесены из `R-204` Н-7, одна там же новая, одна новая здесь.

1. «бюджет вызова не подключён к пути выдачи» — **MAJOR**, built-not-wired (задачи 16/24).
2. «экспорт счётчиков наружу процесса не построен» — **MAJOR**, built-not-wired (§11 п. 5,
   `A-037` У-7).
3. «лимиты объявлены, не применены» — заводится **ТОЛЬКО** при нулях `docker inspect` на
   post-merge гейте §8 (замер 2026-09-21 дал `NanoCpus=0 Memory=0`).
4. «барьер `task-status` проверяет наличие заявления, а не его правдивость; `2bis` вне
   регэкспа; `🟡` в мёртвой ветке `case`» — остаточный риск `R-204` Н-1/Н-3 (Н-2 здесь).
5. «шаблон `verify_M-NN.sh` не держит паритет с агрегатом CI — 13 джобов из 17 вне приёмки»
   (Н-3).
6. **НОВАЯ:** «колонка `Заявлено` таблицы §22 барьером не сверяется с FA; для `VB-I`
   расхождение 11 против 12 уже наследуется из `main`» (Н-1).

---

# Барьеры репозитория — прод-форма события, ДЕРЕВО СЛИЯНИЯ

Прямой прогон против вершины ветки даёт у барьеров честный fail-closed («база НЕ предок
HEAD») — ветка отстаёт на 7 коммитов. На `pull_request` CI чекаутит мерж-коммит, у которого
база предок; собрал такой коммит локально (`dfc60b7`) и прогнал по нему, `EVENT_NAME=pull_request`,
`PR_BASE_SHA=949a606`:

```
check_artifact_ids        exit=0  ни один коммит диапазона не ввёл второй носитель под занятым ID
check_docs_freeze         exit=0  (молчание — зона §11 не тронута)
check_protected_artifacts exit=0  защищённые артефакты целы на HEAD
check_gate_meta           exit=0  VERDICT: PASS — вердиктов проверено: 25, до-нормативных: 0
check_review_fa           exit=0  (R-204/R-202/R-201 → GS-I-1 …)
check_archived_refs       exit=0  PASS базовая линия актуальна (унаследованных ссылок: 29)
check_roadmap_sync        exit=0  close-out в диапазоне не обнаружен — роадмап не обязан меняться
check_secret_material     exit=0  ключевого материала в отслеживаемых файлах нет
check_context_budgets     exit=0  7 файлов, 112960 B из 114900 B (запас 1940 B)
check_resource_oracles    exit=0  оракулов проверено: 14; процессных счётчиков 7
check_rollout_composition exit=0  состав не менялся против базы (BTCUSDT,ETHUSDT)
verify_design_claims      exit=0  VERDICT: PASS (0 нарушений)          ← бывший блокер Б-1
```

**Ни одного красного джоба агрегата.** Прошлый круг блокировал ровно последний из этих
прогонов; сегодня он зелен и его зелень привязана мутацией E.

---

# Done Block — приёмка на СВОЁМ чистом чекауте

```
$ pwd; git rev-parse HEAD
/tmp/hft-reviewer-m87-r205
332fafa226bcbe56446d039f5bcbc2bdac5ee78a

$ git status --porcelain
{пусто}

$ git fetch origin && git rev-parse origin/feat/M-87-serving-circuit-breaker   # вершина — командой
332fafa226bcbe56446d039f5bcbc2bdac5ee78a

$ git rev-parse origin/main; git rev-list --count 332fafa..origin/main
949a606f9b26d61a8b7973c70c43b2e86b212fc6
7                                          # ветка ОТСТАЁТ — strict:false применим

$ timeout 2700 bash scripts/verify_M-87.sh
… 32 × PASS …
SKIP  task9: ФАКТИЧЕСКИ применённые лимиты, запас для recorder'а и поведение НА лимите
      снимаются на проде (docker inspect) — шаг деплой-гейта §8; замер 2026-09-21 дал
      NanoCpus=0 Memory=0 при живом описании сервисов
VERDICT: PASS
exit=0

$ grep -cE '^PASS' /tmp/r205-verify-tip.txt; grep -cE '^FAIL' /tmp/r205-verify-tip.txt; grep -cE '^SKIP' /tmp/r205-verify-tip.txt
32
0
1

# условие круга — зелено на вершине:
PASS  task-status: каждая из 2 открытых задач НАЗЫВАЕТ свой остаток

# CI-паритет внутри verify (три команды build-test):
PASS  CI-паритет: cargo fmt --all -- --check
PASS  CI-паритет: cargo clippy --all-targets --all-features -- -D warnings
PASS  CI-паритет: cargo test --all
PASS  D-1(б): библиотечный корпус зелен — passed=258 failed=0

# БЫВШИЙ БЛОКЕР — теперь зелен, на дереве слияния:
$ bash scripts/verify_design_claims.sh --merge-preview origin/main
PASS  [2-ПОКРЫТИЕ] §22: VB-I — заявлено=11, в оракулах=9 — подтверждено замером (loose=9)
VERDICT: PASS (0 нарушений)
exit=0

# мутации: E вернула красное ДОСЛОВНО (привязка фикса), F покраснела в обратную сторону
# (число пиннит оракул). Обе откачены, оба дерева чистые.
```

**Вердикт: APPROVED.** Условия `R-204` исполнены минимальным вмешательством — одна цифра и
одна пустая строка, ни строки кода, — и каждое предъявлено прогоном, а не прозой. Бывший
блокер снят, его снятие привязано мутацией в обе стороны, приёмка зелена целиком, все
барьеры агрегата зелены на дереве слияния. Мержу через PR; остаточный долг — шесть карточек
на close-out, и он назван, а не подразумевается.
