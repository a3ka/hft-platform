<!-- GATE-META
milestone: П-024
audited_repo: a3ka/hft-platform
audited_base: 8a2ad91887657460dbd7963a3081aacea6ceeb27
audited_head: 328a356a0898f775a94596a2af10e4718504b013
verdict: APPROVE
-->

# R-152 — перепроверка §9, круг 2: закрытие Б-1..Б-3 и Н-1 по `R-150`

Независимый Fable-агент со свежим контекстом; тот же проверяющий, что вынес `R-150`
(круг 1). Предмет — дельта `8a2ad91..328a356` (один коммит, три файла: README архива
+66/−25, `docs/PENDING-SIGNATURE.md` +13/−3, `milestones/BACKLOG.md` 1/1).

**Вердикт: APPROVE.** Все четыре условия снятия `R-150` исполнены, и каждое подтверждено
моим ИСПОЛНЕНИЕМ, не чтением правки. Три примечания ниже — точностные, merge не держат.

## Проверка условий снятия — исполнением

### Б-1 (замер против `TD-181`) — ЗАКРЫТ

Новый текст п.2 README и абзаца под `П-024` соответствует `TD-181` (`origin/main`): один
барьер, форма `pull_request`, мера grep-по-литералу названа ошибочной, «проводка не
исполняется вовсе» снято, класс назван (`Р-1`). Мутационные прогоны воспроизведены МНОЮ
по конструкции `TD-181` (мутант: ветка `pull_request` читает `PUSH_BEFORE`):

```
BARRIER=<мутант> red_protected_artifacts.sh → exit=0, VERDICT: PASS (33/33)  ← выжил
BARRIER=<мутант> red_docs_freeze.sh         → exit=1, VERDICT: FAIL (1)      ← ловит
BARRIER=<мутант> red_gate_meta.sh           → exit=1, VERDICT: FAIL (1)      ← ловит
```

Сходится с `TD-181` и с текстом правки — кроме одного числа, см. примечание П-1.

### Б-2 (колонка кругов) — ЗАКРЫТ

Таблица приведена к заголовкам вердиктов: `grep -h '^# C-1' C-17*.md` даёт round 2/3/4/5/6/8 —
колонка совпадает построчно. Строка круга 7 «вердикта нет» добавлена и согласована с `П-024`
(«круг 7 — дефект, найденный не адверсарием») и с `C-179:31` («Round 7 genuinely fixes…»).
Сноска о переномеровании `C-172`→`C-173` больше не противоречит таблице (`C-173` = круг 2).
След первой редакции оставлен явно. Внутренних противоречий не нашёл.

### Б-3 (счёт строк) — ЗАКРЫТ

Таблица замера по ревизиям сверена с моим независимым замером из `R-150` — совпадает по всем
шести точкам (448/273 · 560/335 · 601/358 · 703/412 · 781/422 · 910/467); финал 910/467
назван финалом, 781/422 — состоянием круга 6. `95` сценариев и `42/42` мутаций подтверждены
прогоном пробы ещё в `R-150`. Стоимость CI из счёта убрана; «джоб `milestone-shape` на ветке
не исполнялся ни разу» согласуется с триггерами `ci.yml` (только `pull_request` и `push` в
`main`; PR у ветки не было) и с составом проверенных ранов (единственный джоб
«fmt + clippy + test (ветка)»). Провенанс обеих локальных цифр (89.92 c у автора, 153 c у
меня) назван честно: величина зависит от хоста.

### Н-1 (механизм занятости) — ЗАКРЫТ, подтверждён на ТЕКУЩЕЙ вселенной рефов

Состояние сменилось после круга 1: ветка `docs/ops-restart-alert` (носитель `C-174` из
`R-150` Н-1) снесена в спас-реф — `git ls-remote origin | grep ops-restart-alert` →
`refs/salvage/2026-08-29-final/docs-ops-restart-alert`, и `C-174-ops-restart-alert.md` в нём
есть (проверено `ls-tree` по временно подтянутому рефу). Исполнение в свежем клоне с
`--prune`-зеркалом настоящего `origin`:

```
с веткой feat/harness-milestone-shape        → next C = C-180
после удаления её рефов                      → next C = C-172   (max по остальным = C-171)
```

Ровно как в правке: снос роняет максимум со 179 до 171, восемь выдач прошли бы сквозь
`C-172`…`C-179` — семь реальных вердиктов (шесть архивных + `C-174` на спас-рефе).

### Общее

`bash scripts/verify_design_claims.sh --merge-preview origin/main` → exit=0 (код возврата
снят отдельным прогоном). Зона: три файла дельты — BACKLOG/PENDING/архив, §11 не тронут,
reviewer-файлы не тронуты. Правка `П-024` — дописка к записи об исполнении, не новое
решение. Конфликта с параллельной веткой `docs/renumber-R148-collision` по
`docs/PENDING-SIGNATURE.md` нет: ханки не пересекаются (1673/1734 против 1752+), новых
ссылок на `R-148` дельта не вводит.

## Примечания (точностные, merge не держат)

- **П-1.** README, PENDING и (в предмете B) `session-handover-2026-08-31.md` печатают
  `red_gate_meta … FAIL (5)`. `TD-181` печатает `FAIL (1)`; мой мутант конструкции `TD-181`
  даёт `FAIL (1)`. Текст подаёт число как воспроизведение прогона reviewer'а — при другом
  числе. Вероятно, мутант автора был шире (конструкция в тексте не названа). Условие снятия:
  либо назвать конструкцию мутанта, дающую 5, либо привести число к `TD-181`.
- **П-2.** «Правило `Р-1`… нарушенное автором правила В ДЕНЬ ЕГО НАПИСАНИЯ» (тело коммита,
  PENDING; README — «вписал в корпус в тот же день»). Замер: `Р-1 (мера и путь)` введено
  `85ff023` **2026-08-28**; ошибочный замер — 29.08. На день позже, не в тот же. Ирония
  остаётся, число — нет.
- **П-3** (повтор `R-150` Н-2, не отработано): «ВОСЕМЬ КРУГОВ, ВСЕ REJECT» (BACKLOG,
  заголовок раздела README) теперь стоит прямо над таблицей, где у круга 7 вердикта НЕТ.
  REJECT-вердиктов семь на восемь кругов. После Б-2 рассогласование заголовка с таблицей
  стало виднее, чем было.

## Done Block

```
$ bash scripts/verify_design_claims.sh --merge-preview origin/main >/dev/null 2>&1; echo exit=$?
exit=0
$ BARRIER=$M/pa.sh bash scripts/tests/red_protected_artifacts.sh >…; echo $?   → 0; VERDICT: PASS (33/33)
$ BARRIER=$M/df.sh bash scripts/tests/red_docs_freeze.sh >…; echo $?           → 1; VERDICT: FAIL (1)
$ BARRIER=$M/gm.sh bash scripts/tests/red_gate_meta.sh >…; echo $?             → 1; VERDICT: FAIL (1)
$ bash scripts/next_artifact_id.sh C          # свежий клон, зеркало origin с --prune
C-180
$ git update-ref -d refs/remotes/origin/feat/harness-milestone-shape && bash scripts/next_artifact_id.sh C
C-172
$ git log -S 'Р-1 (мера и путь)' --format='%h %ci' -- docs/workflow/oracle-blindness-class-2026-08-28.md | tail -1
85ff023 2026-08-28 14:08:52 +0000
```

Условия П-1/П-2 снимаются одной правкой текста; повторного круга §9 для них не требую —
проверит автор своим PR, след — этим файлом.
