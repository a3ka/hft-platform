<!-- GATE-META
milestone: M-74
audited_repo: a3ka/hft-platform
audited_base: 6f628cb04b6cbf42aacd85b94f32ad7fbd21e934
audited_head: adf24ab058060095c1c00383b593a5f7ac2df24e
verdict: REJECT
-->

# R-177 — перепроверка `gates.md` §9: `П-028` (приём restore-drill'а по ручному прогону)

**Роль:** независимый проверяющий (`gates.md` §9), свежий контекст, автором правки не являюсь.
**Предмет:** ветка `docs/p028-drill-accepted`, один коммит `adf24ab`, один файл
`docs/PENDING-SIGNATURE.md`, +40 строк — новая запись `П-028`.
**Дерево проверки:** ветка И дерево слияния с `origin/main` (`gates.md` §8: «документ
проверяется на дереве слияния, а не на ветке»).

## ВЕРДИКТ: REJECT — три блокера

Содержательно запись ВЕРНА: каждое проверяемое утверждение о фактах и о коде подтвердилось
исполнением (§4), формула «цель достигнута ≠ набор задач выполнен» удержана, границу C
запись не переступает и лишнего не разблокирует (§6). Блокеры — не в существе решения, а в
СВЯЗНОСТИ (`gates.md` §9 пункт «в») и в остаточной живой лжи рядом (класс, названный в
мандате). Все три чинятся дёшево; переигрывать решение founder'а не требуется и не
предлагается.

---

## §1 — Что судилось (диапазон снят командой)

```text
$ git log --format="%H %s" origin/main..adf24ab
adf24ab058060095c1c00383b593a5f7ac2df24e docs(pending): П-028 — ПОДПИСАН приём restore-drill'а по ручному прогону [architect]

$ git show --numstat --format="" adf24ab
40	0	docs/PENDING-SIGNATURE.md

$ git merge-base origin/main adf24ab
9503d11476424150e56196426488a27f92d35275

$ git rev-parse origin/main
6f628cb04b6cbf42aacd85b94f32ad7fbd21e934
```

Мандат называл диапазон «один коммит, один файл». Подтверждено: коммит трогает ТОЛЬКО
`docs/PENDING-SIGNATURE.md`. Двухточечный дифф `origin/main..HEAD` дополнительно показывает
`docs/ROADMAP.md` — это НЕ правка ветки, а отставание базы: ветка стоит на `9503d11`, а
`origin/main` ушёл на `6f628cb` (PR #175 переписал ту же строку роадмапа). Слияние чистое,
строку роадмапа берёт версия `main`. Замечания к ветке по этому поводу нет — отмечаю, чтобы
следующий читатель не принял отставание базы за скрытую правку.

---

## §2 — БЛОКЕРЫ

### Б-1 (BLOCKER, связность) — единственный носитель доказательства не существует в `main`

`docs/PENDING-SIGNATURE.md:1783` и `:1789` ссылаются на `milestones/M-74-restore-drill.md`
§0quater как на источник СЫРОГО ВЫВОДА («**Чем подтверждено** (сырой вывод — …)»). Этого
файла нет ни в `main`, ни на дереве слияния — он живёт на ОДНОЙ невлитой ветке.

```text
$ git ls-files | grep -c "M-74"
0
$ git ls-tree -r --name-only HEAD -- milestones/ | grep M-74 ; echo "rc=$?"
rc=1

$ for r in $(git for-each-ref --format="%(refname)" refs/remotes/origin); do git cat-file -e "$r:milestones/M-74-restore-drill.md" 2>/dev/null && echo "HAS $r"; done
HAS refs/remotes/origin/docs/M-73-closeout-architect

$ git ls-tree -r --name-only HEAD -- scripts/ | grep verify_M-74 ; echo "rc=$?"
rc=1
```

Последствие конкретное, а не формальное. Запись `П-028` заведена ровно потому, что «решение,
живущее только в переписке, не существует». После слияния она сама оказывается решением,
чьё ЕДИНСТВЕННОЕ основание живёт только на ветке: числа `2 170 780`, отпечаток
`96a231ce9731…` и `rc=4` не проверяемы из `main` ничем. Ветка `docs/M-73-closeout-architect`
по роадмапу (п.6) ещё только ждёт вливания, а `scripts/verify_M-74.sh`, на краснотý
которого запись ссылается, в `main` тоже отсутствует.

Это named-класс `gates.md` §9: «висячая ссылка документа (`C-075` M1)» — находка, которую
каждый раз давал незапушенный/невлитый предмет.

**Условие снятия — любое из двух, на выбор автора:**
(i) влить `docs/M-73-closeout-architect` (несущую §0quater и `verify_M-74.sh`) в `main`
ДО или ВМЕСТЕ с этой веткой — тогда ссылка перестаёт висеть; либо
(ii) сделать запись САМОДОСТАТОЧНОЙ: перенести в `П-028` сам fenced-блок сырого вывода из
§0quater (≈15 строк, дословно, без дописанных от руки строк) и оставить ссылку на §0quater
как на контекст, а не как на носитель доказательства.

### Б-2 (BLOCKER, связность) — `§«Три запрета» милестоуна` не существует НИГДЕ

`docs/PENDING-SIGNATURE.md:1805` адресует читателя к разделу «Три запрета» милестоуна. Такого
раздела нет ни на дереве слияния, ни в самом файле `M-74-restore-drill.md` на ветке, где он
лежит; слово «запрет» встречается там дважды и в другом смысле (`Forbidden paths`).

```text
$ grep -rIn "Три запрета" --include="*.md" . | cat
docs/PENDING-SIGNATURE.md:1805:(§«Три запрета» милестоуна и `docs/ROADMAP.md` п.2). Включение `RETENTION_MODE=apply` без

$ git grep -In "Три запрета" origin/docs/M-73-closeout-architect -- "*.md" | wc -l
0

$ git show origin/docs/M-73-closeout-architect:milestones/M-74-restore-drill.md | grep -inc "запрет"
2
$ ... | grep -in "три запрета" | wc -l
0
```

Ссылка тем важнее, что она — единственный адрес для «двух оставшихся запретов», то есть для
того, что подпись НЕ разблокирует. Читатель, пришедший проверить границу подписи, приходит в
пустоту. Отдельно: перечисления не сходятся. Тело коммита называет двумя запретами
«том чекпоинтов не смонтирован» и «`/mnt/journal-cold` — пустой локальный каталог», а
`docs/ROADMAP.md` п.2 (единственная вторая ссылка) называет двумя другими: (а)
`RETENTION_MODE=dry-run` и (б) отказ «checkpoint coverage artifact absent». Из записи
невозможно понять, какие два имеются в виду.

**Условие снятия:** либо назвать существующий раздел (и завести его, если он нужен), либо
перечислить два запрета ПРЯМО в `П-028` одной строкой каждый, оставив ссылкой только
`docs/ROADMAP.md` п.2 — которая существует и проверяема.

### Б-3 (BLOCKER, класс «протухшее живёт рядом с новым») — `DESIGN.md:297` продолжает утверждать обратное

`П-028` опирается на `DESIGN` §10 как на источник ворот. В ТОМ ЖЕ документе, на дереве
слияния, живёт недатированная статусная строка P2.5:
`backup restore-drill — **НЕ ПРОВОДИЛСЯ НИ РАЗУ**`. После слияния `main` одновременно
утверждает «drill исполнен и принят 2026-09-08» и «drill не проводился ни разу».

Это ровно тот класс, который в мандате назван устойчивым за автором и который воспроизвёлся
сегодня четырежды. `docs/DESIGN.md` — зона architect'а: правка доступна автору в этом же
круге. Строка `DESIGN.md:318` (ворота Ф0) в правке НЕ нуждается — Ф0 несёт четыре ворот, и
принято одно из них.

**Условие снятия:** привести `DESIGN.md:297` в соответствие — назвать drill исполненным
ручным прогоном 2026-09-08 со ссылкой на `П-028`, сохранив «повторяемость не исполнена».
Заодно там же ложно с 2026-08-29 «Storage Box не заведён» (опровергается `П-023`), но это
предсуществующая протечка, и её исправление — не условие снятия Б-3, а рекомендация.

---

## §3 — Класс-проверка: ПЕРЕЧЕНЬ ПРОВЕРЕННЫХ ФОРМ И СЧЁТ

Мандат требует судить закрытие ПО КЛАССУ, а не по одной формулировке. Пять форм, каждая —
самостоятельное утверждение, которое после подписи становится ложным. Прогон на ДЕРЕВЕ
СЛИЯНИЯ:

```text
# дерево слияния: adf24ab + origin/main(6f628cb)
$ git log --oneline -1
8322312 merge-preview (local only)

$ grep -rIn -- "НЕ ПРОВОДИЛСЯ НИ РАЗУ" docs/DESIGN.md docs/SESSION-HANDOFF.md docs/fa/ docs/PENDING-SIGNATURE.md PROJECT-STATE.md TECH-DEBT.md | cut -d: -f1,2
docs/DESIGN.md:297
docs/PENDING-SIGNATURE.md:1175
  (совпадений: 2)

$ grep -rIn -- "копия, а не бэкап" docs/DESIGN.md docs/SESSION-HANDOFF.md docs/fa/ docs/PENDING-SIGNATURE.md PROJECT-STATE.md TECH-DEBT.md | cut -d: -f1,2
docs/SESSION-HANDOFF.md:465
docs/PENDING-SIGNATURE.md:1701
  (совпадений: 2)

$ grep -rIn -- "Storage Box не заведён" docs/DESIGN.md docs/SESSION-HANDOFF.md docs/fa/ docs/PENDING-SIGNATURE.md PROJECT-STATE.md TECH-DEBT.md | cut -d: -f1,2
docs/DESIGN.md:297
docs/PENDING-SIGNATURE.md:1174
PROJECT-STATE.md:359
PROJECT-STATE.md:451
TECH-DEBT.md:3621
  (совпадений: 5)

$ grep -rIn -- "заблокирован Storage Box" docs/DESIGN.md docs/SESSION-HANDOFF.md docs/fa/ docs/PENDING-SIGNATURE.md PROJECT-STATE.md TECH-DEBT.md | cut -d: -f1,2
docs/fa/ops.md:84
docs/DESIGN.md:297
PROJECT-STATE.md:902
  (совпадений: 3)

$ grep -rIn -- "ждут ★ приёма" docs/DESIGN.md docs/SESSION-HANDOFF.md docs/fa/ docs/PENDING-SIGNATURE.md PROJECT-STATE.md TECH-DEBT.md | cut -d: -f1,2
docs/DESIGN.md:297
  (совпадений: 1)

```

Разбор попаданий — не все они находки, и различать обязательно:

| место | статус | почему |
|---|---|---|
| `docs/DESIGN.md:297` | **ЖИВАЯ ЛОЖЬ ⇒ Б-3** | недатированная статусная строка нормативного документа |
| `docs/PENDING-SIGNATURE.md:1175` | не находка | датированный fenced-блок ЗАМЕРА внутри `П-018` (25.08) — историческая фактура, а не действующее утверждение |
| `docs/PENDING-SIGNATURE.md:1701` | **NOTE Н-5** | таблица `П-023` «Что сделано… на 2026-08-29» датирована, но у `П-023` нет обратной ссылки на `П-028`; читатель, пришедший грепом на `П-023`, видит «удаление не начнётся до `R1b`» и не узнаёт, что `R1b` исполнен |
| `docs/SESSION-HANDOFF.md:465` | **NOTE Н-6** | «`R1b` restore-drill НЕ проводился» — строка живого индекса, зона architect'а |
| `docs/fa/ops.md:84` | NOTE Н-7 | «task 3, заблокирован Storage Box» — блокировка снята 29.08; сама task 3 честно не сделана |
| `PROJECT-STATE.md:359/451/902`, `TECH-DEBT.md:132/1921/3621` | **вне зоны автора ⇒ Н-8** | reviewer-owned; architect в них не пишет. Обязаны быть НАЗВАНЫ в handoff'е reviewer'у, а не молча оставлены |
| `docs/ROADMAP.md:75`, колонка «Состояние» | **вне зоны автора ⇒ Н-9** | «⏳ приём founder'а + перенос остатка» после слияния наполовину протухает; колонка «Состояние» по таблице владения самого роадмапа — reviewer-only |

Счёт: **5 форм проверено, 13 попаданий, из них 1 блокирующее (Б-3), 4 NOTE в зоне автора,
7 вне зоны автора (адресуются reviewer'у), 1 историческая фактура — не находка.**

---

## §4 — Пункт «а»: утверждения, проверенные ИСПОЛНЕНИЕМ — все ВЕРНЫ

```text
$ grep -n "restore-drill проходит поверх" docs/DESIGN.md | cut -c1-90
318:| **Ф0 Необратимость данных** 🚧 в работе | retention/com
$ grep -no "★ приём restore-drill.а" docs/DESIGN.md
318:★ приём restore-drill'а

$ sed -n "405p" docs/fa/ops.md
- **`OPS-I-3` restore-drill:** периодически (раз в месяц) холодная копия **скачивается и
$ sed -n "471p" docs/fa/ops.md
| OPS-I-3 | Холодная копия периодически ВОССТАНАВЛИВАЕТСЯ и читается (drill), а не только создаётся |

$ sed -n "478p" docs/fa/ops.md | cut -c1-260
| OPS-I-10 | **ОБЪЯВЛЕНА ⟹ ЭМИТИТСЯ (урок TD-027, task 4C).** Каждая метрика §3 имеет НАЗВАННЫЙ продюсер-сейм (§3 продюсер-карта) и РЕАЛЬНО инкрементируется в

$ sed -n "498p" docs/fa/ops.md
| `OPS-BKP` | бэкап не восстанавливается | restore-drill провален (P0) | `backup_restore_drill_ok` |

$ grep -rn backup_restore_drill_ok crates/
crates/recorder/src/metric_emit.rs:22://! - `backup_restore_drill_ok`         — deferred (task 3, OPS-I-2/3).
crates/ops/src/metrics.rs:110:        name: "backup_restore_drill_ok",
crates/ops/src/alerts.rs:112:    // OPS-BKP — restore-drill провален (`backup_restore_drill_ok == 0` после попытки). P0
crates/ops/src/alerts.rs:117:        metric: "backup_restore_drill_ok",
crates/ops/src/alerts.rs:118:        summary: "backup_restore_drill_ok == 0 — restore-drill провален (бэкап не бэкап)",
crates/ops/src/alerts.rs:223:        ("OPS-BKP", "backup_restore_drill_ok") => {
crates/ops/src/alerts.rs:225:            "backup_restore_drill_ok == 0".to_string()

$ grep -rn "set_gauge(\"backup_restore_drill_ok" crates/ ; echo rc=$?
rc=1
```

| утверждение `П-028` | проверка | исход |
|---|---|---|
| `DESIGN` §10 объявляет ворота «restore-drill проходит поверх восстановленного журнала» | `DESIGN.md:318` | **ВЕРНО** |
| колонка founder — «★ приём restore-drill'а» | `DESIGN.md:318` | **ВЕРНО** |
| `OPS-I-3` требует «периодически (раз в месяц) … скачивается и читается» | `docs/fa/ops.md:405`, `:471` | **ВЕРНО** — «повторяемость НЕ исполнена» опирается на существующий текст |
| `OPS-I-10` — «объявления НЕДОСТАТОЧНО, метрика обязана РЕАЛЬНО эмититься» | `docs/fa/ops.md:478` | **ВЕРНО** дословно |
| правило `OPS-BKP` ждёт `backup_restore_drill_ok` | `docs/fa/ops.md:498` | **ВЕРНО** |
| метрика НЕ производится | 7 совпадений в `crates/`: объявление реестра (`ops/src/metrics.rs:110`), правило алерта (`ops/src/alerts.rs`), комментарий «deferred» (`recorder/src/metric_emit.rs:22`); продюсера нет — `set_gauge("backup_restore_drill_ok"` не встречается (`rc=1`) | **ВЕРНО** |

Числа `§0quater` сверены построчно с записью:

```text
$ git show origin/docs/M-73-closeout-architect:milestones/M-74-restore-drill.md | sed -n "62,68p"
    -- --exact reference_reader_for_shell_probe --nocapture
DRILL_READER rc=0 segments_read=3 events_read=2170780 seq_first=16049334 \
  seq_last=509116924 intra_segment_continuous=true \
  digest=96a231ce97316518856045415a7fdf089ce5106f427c239cdc07fb3dbbdb9c81 reason=
время чтения: 65 с

ДЕТЕРМИНИЗМ — повторное чтение той же копии:

$ ... | sed -n "70,76p"
                                                      ↑ отпечаток СОВПАЛ

НЕГАТИВНЫЙ КОНТРОЛЬ — зелёное обязано что-то значить:
$ 32 байта в середине segment-00000297.jrnl.zst инвертированы
DRILL_READER rc=4 events_read=0 digest= reason=corrupt:frame crc mismatch
```


$ git show origin/docs/M-73-closeout-architect:scripts/verify_M-74.sh | sed -n "3,4p"
#
# ГЕЙТ НАПИСАН ДО РАБОТЫ И ОБЯЗАН БЫТЬ КРАСНЫМ. Задачи 2/2b/3/4/5 открыты; шаги на них
```

`events_read=2170780` ⇒ «2 170 780» ✓ · `digest=96a231ce9731…` и повтор ⇒ «отпечаток
ВОСПРОИЗВОДИМ» ✓ · `rc=4 events_read=0 digest=` на инвертированных 32 байтах СЖАТОГО
сегмента `00000297.jrnl.zst` ⇒ «`rc=4`, ноль событий, отпечатка нет» ✓ ·
`intra_segment_continuous=true` ⇒ «непрерывность внутри сегментов держится» ✓ · отбор
`00000001`/`00000297`/`00000593` ⇒ «15 июля / 17 августа / 8 сентября», 593 индекса ✓.
Три свойства (читается / детерминировано / отказывает на порче) предъявлены все три —
запись их не преувеличивает.

**Формула на внутреннюю непротиворечивость — ДЕРЖИТ.** «Цель достигнута, набор задач не
выполнен» стоят одной строкой рядом, с явным запретом смешивать. Ни одного места, где
запись читалась бы как «милестоун закрыт», я не нашёл: `verify_M-74.sh` назван красным,
задачи названы несделанными, автоматизация названа долгом.

---

## §5 — Пункт «б»: полномочия и границы — НАРУШЕНИЙ НЕТ

- **Граница C не подменена.** Запись фиксирует решение founder'а («ПОДПИСАНО founder'ом
  2026-09-08»), нигде не подставляет «автоматическое approve» и не выносит инженерного
  суждения вместо владельца. `gates.md` §0.1 соблюдён.
- **Авторство ≠ решение.** `docs/PENDING-SIGNATURE.md` — зона architect'а; запись оформлена
  как ПРОТОКОЛ чужого решения, а не как собственное. Не перепутано.
- **Номер уникален.** `П-028` встречается один раз. (Предсуществующие дубли `П-005`,
  `П-007`, `П-011`, `П-018` этой правкой не введены и не усугублены. Отмечаю попутно:
  аллокатор `gates.md` §12 класс `П` НЕ обслуживает — номер здесь берётся глазом, и это
  объясняет, откуда взялись четыре старые коллизии.)
- **Замок §11 — токен ИЗБЫТОЧЕН (Н-1).** `docs/PENDING-SIGNATURE.md` в зону
  `check_docs_freeze.sh` не входит.
- **Запись НЕ обещает больше подписанного.** Раздел «что разблокирует / чего НЕ делает»
  удерживает: снимается ОДНО условие `П-023` (доказанная читаемость), удаление безопасным
  не объявляется, `RETENTION_MODE=apply` прямо назван способным заполнить диск. `П-023`
  проверен: он ставит условием `R1b` и порядок «копия → восстановление → и только потом
  удаление» — именно это условие запись и снимает, ни больше.

```text
$ grep -c "^## П-028" docs/PENDING-SIGNATURE.md
1
$ grep -o "^## П-[0-9]\+" docs/PENDING-SIGNATURE.md | sort | uniq -d
## П-005
## П-007
## П-011
## П-018
$ bash scripts/next_artifact_id.sh П 2>&1 | tail -2
FAIL  неизвестный класс 'П' — допустимы: TD R C A M

$ sed -n "63p" scripts/check_docs_freeze.sh
    .claude/rules/*|.claude/agents/*|.claude/wrappers/*|CLAUDE.md|docs/04-workflow.md) return 0 ;;
```

---

## §6 — NOTE (не блокируют, но снимаются тем же кругом)

- **Н-1 — токен `FOUNDER-APPROVED` избыточен.** `docs/PENDING-SIGNATURE.md` вне зоны §11
  (`scripts/check_docs_freeze.sh:63`). Содержание строки правдиво, вреда нет, но токен
  замка и подпись founder'а — РАЗНЫЕ вещи, и их соседство приучает читать одно как другое.
  Рекомендация: перенести смысл в тело коммита обычной строкой без имени токена.
- **Н-2 — перечень несделанных задач НЕПОЛОН: пропущена задача 5.** Запись говорит «Задачи
  `M-74` 2/2b/3/4 не сделаны». Шапка самого гейта говорит иначе: «Задачи 2/2b/3/4/**5**
  открыты» (`verify_M-74.sh:3-4`, приведён в §4); задача 5 «Просроченность = ОТКАЗ» стоит
  `COMPILE-RED`. Перечисляя четыре, запись косвенно утверждает, что пятая закрыта. В записи,
  весь смысл которой — точность о НЕзакрытом, это осечка по существу. Условие: заменить на
  «2/2b/3/4/5».
- **Н-3 — заголовок «(ворота Ф0)» шире подписанного.** Ф0 несёт ЧЕТЫРЕ ворот
  (`DESIGN.md:318`): drill, доставленный вне машины алерт, ребут-дрилл, `venue-hl` suite.
  Тело записи сужает корректно, заголовок — нет; грепающий читатель видит заголовок первым.
  Условие: «(одни из ворот Ф0)» либо «(ворота Ф0: restore-drill)».
- **Н-4 — не назван УПРАВЛЕНЧЕСКИЙ запрет, только технические два.** `П-023` говорит:
  ретеншен «включается ОТДЕЛЬНЫМ решением — после `R1b`». То есть устранение двух
  технических запретов НЕ даёт права включить `apply` — нужна ещё одна подпись, граница C.
  Запись этого не произносит, и её «что разблокирует» читается как «останутся только две
  инженерные помехи». Роадмап это держит («Ждёт подписи founder'а», п.2), сама запись — нет.
  Условие: одна строка «включение `apply` остаётся отдельным решением founder'а (`П-023`)».
- **Н-5 — `П-023` без обратной ссылки** (`docs/PENDING-SIGNATURE.md:1701`, см. §3).
- **Н-6 — `docs/SESSION-HANDOFF.md:465`** несёт «`R1b` restore-drill НЕ проводился» —
  зона architect'а, чинится вместе с Б-3.
- **Н-7 — `docs/fa/ops.md:84`** «заблокирован Storage Box» — блокировка снята 29.08.
  Правка `docs/fa/*` — форма ⇒ по `gates.md` §9 потребует критика; предлагаю НЕ тащить её в
  этот круг, а завести отдельно.
- **Н-8 — reviewer-owned протухшее** (`TECH-DEBT.md:132` `TD-193`, `:1921`, `:3621`;
  `PROJECT-STATE.md:359/451/902/2126`). Architect в эти файлы не пишет. Обязано быть НАЗВАНО
  в §D handoff'а reviewer'у списком, иначе `TD-193` продолжит утверждать «не выполнялся ни
  разу» поверх подписанного приёма.
- **Н-9 — `docs/ROADMAP.md:75` колонка «Состояние»** («⏳ приём founder'а + перенос
  остатка») — reviewer-only по таблице владения самого роадмапа; приём состоялся, перенос
  остатка нет. Тоже в handoff reviewer'у.

---

## §7 — Чего я НЕ проверял исполнением (называю явно)

1. **`bash scripts/verify_M-74.sh` не запускался.** Скрипта нет ни в `main`, ни на дереве
   слияния (§2 Б-1); он живёт на `docs/M-73-closeout-architect`, а его прогон требует полной
   сборки того дерева. Проверено ДЕКЛАРАТИВНО и этого достаточно для утверждения записи
   «красен честно»: шапка скрипта объявляет краснотý заранее («ГЕЙТ НАПИСАН ДО РАБОТЫ И
   ОБЯЗАН БЫТЬ КРАСНЫМ. Задачи 2/2b/3/4/5 открыты»), агрегатор решает по коду возврата
   (`verify_M-74.sh:155-156`), §Tasks называет красными ровно две объявленные строки.
   То есть краснота ОБЪЯВЛЕНА, а не случайна — но что она сегодня именно такая, я не мерил.
2. **Сам ручной прогон не пересуживался** — граница C, не моя зона (мандат, `gates.md` §0.1).
   Я судил ЗАПИСЬ о решении, а не решение.
3. **Автоматический набор `M-74` и вердикты `C-214…C-219`** — вне диапазона, не судились.
4. **Числа `§0quater` взяты из документа, а не переизмерены** на настоящей копии: доступа к
   Storage Box у проверяющего нет, и такой замер был бы повторением прогона founder'а.

---

## §8 — Условия снятия REJECT (закрывающий круг короткий)

1. **Б-1:** влить `docs/M-73-closeout-architect` в `main` до/вместе с этой веткой — ЛИБО
   перенести сырой блок §0quater внутрь `П-028`.
2. **Б-2:** убрать ссылку на несуществующий раздел; назвать два запрета в тексте записи.
3. **Б-3:** `docs/DESIGN.md:297` — снять «НЕ ПРОВОДИЛСЯ НИ РАЗУ».
4. **Н-2, Н-3, Н-4** — три однострочные правки внутри `П-028` (перечень задач; сужение
   заголовка; отдельная подпись на `apply`).
5. **Н-8/Н-9** — списком в §D handoff'а reviewer'у, без правки файлов.

Н-1, Н-5, Н-6, Н-7 — рекомендации, снятие REJECT'а не блокируют.

После этого правка вливается в `main` СВОИМ PR (`gates.md` §9: автор приземляет сам,
`gh pr create` → зелёные чеки → `gh pr merge --merge --delete-branch`). Второго круга §9
предмет не требует, если правки ограничены §8: они не меняют ФОРМУ и не вводят новых
утверждений о коде.

---

## §9 — Предъявление FA

Диапазон `crates/**` НЕ трогает ⇒ `check_review_fa.sh` даст `SKIP`, предъявление не
требуется. Инварианты, на которые я ссылался (`OPS-I-3` `docs/fa/ops.md:405/471`,
`OPS-I-10` `:478`, `OPS-BKP` `:498`), проверены живыми на дереве слияния — см. §4.

---

## Done Block

Снимок ДО коммита вердикта (`branch-hygiene.md` п.9 — индекс проверяется до, диф после):

```text
$ git log --oneline -1
adf24ab docs(pending): П-028 — ПОДПИСАН приём restore-drill'а по ручному прогону [architect]

$ git status --porcelain
?? research/reviews/R-177-p028-recheck.md

$ ls -la research/reviews/R-177-p028-recheck.md
-rw-rw-r-- 1 nous nous 30781 Sep  8 23:43 research/reviews/R-177-p028-recheck.md
```
