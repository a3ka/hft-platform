<!-- GATE-META
milestone: A-015
audited_repo: a3ka/hft-platform
audited_base: 92f6e65a90382506eb3fcc4ab09d4744ffeaade4
audited_head: 848bfefe8a27566c99b6589120bbb1c2c2e87b86
verdict: REJECT
-->

# R-114 — перепроверка §9: запись решения founder'а по `A-015` §6 (`П-017` A2, барьер B)

**Предмет:** ветка `docs/founder-decision-a015-2026-08-23`, один коммит `848bfef`
(+35/−1, только `docs/PENDING-SIGNATURE.md` — подтверждено `git show 848bfef --numstat`).
**Проверяющий:** независимый Fable-агент со свежим контекстом (`gates.md` §9); автор записи —
сторона, себя не проверял.

**Вердикт: REJECT — одна блокирующая находка (Б-1).** Всё остальное в записи подтверждено
моими собственными прогонами: оба замера пересчитаны и сошлись, пять свидетелей найдены
открытием файлов, мутация свидетеля красит с заявленным исходом, выбор базы ветки
воспроизведён исполнением (`FAIL [4-МЁРТВЫЕ-ФАЙЛЫ]` на базе `main`, exit=1), полномочия не
превышены, граница C не задета, токен §11 не требуется. Но п.2 «следствий» утверждает
состояние харнесса, противоположное фактическому состоянию `main`, — ровно тот класс
(«утверждение о состоянии без открытия источника»), против которого заведена сама запись.

---

## Б-1 (БЛОКЕР) — «FACTS-маркер остаётся там, где стоял (PR #49 с вердиктом `C-116` REJECT)» — ложное утверждение о состоянии: PR #49 ВЛИТ, детектор ЖИВ в `main`

Запись (`docs/PENDING-SIGNATURE.md`, диф `848bfef`, п.2 следствий):

> вариант (б) НЕ выбран — FACTS-маркер остаётся там, где стоял (`A-010` §H, PR #49 с
> вердиктом `C-116` REJECT), и этой записью не воскрешается

Фактическое состояние, снятое командами в этой перепроверке:

```
$ gh pr view 49 --json mergedAt,mergeCommit --jq '.mergedAt, .mergeCommit.oid'
2026-08-21T15:09:50Z
3914ee67268b859a5f482e6a2dd42d0bb6e9f708

$ git show origin/main:scripts/verify_design_claims.sh | grep -c FACTS
30
```

PR #49 **влит в `main` 2026-08-21**, за двое суток до записи (запись датирована 23.08 18:40).
После `C-116` REJECT был **второй круг** — `research/critiques/C-117-facts-marker-rev2.md`
(шапка GATE-META: `verdict: NOTE`, обновлена последним кругом — `C-117:278`), находки
закрыты, ветка влита. Детектор FACTS в `origin/main:scripts/verify_design_claims.sh` — 30
вхождений; норма маркера описана в `docs/workflow/reading-map.md` (раздел «Документ,
называющий себя фактурой…») как ДЕЙСТВУЮЩАЯ. Более того, сам зависимый документ записи —
`docs/plans/two-barriers-step1-2026-08-23.md:1` — несёт живой маркер
`<!-- FACTS: audited_head=92f6e65… -->`, то есть автор ПОЛЬЗУЕТСЯ механизмом, который его
запись описывает как отклонённый.

«Остаётся там, где стоял … и не воскрешается» предполагает, что механизм мёртв. Он жив.
Читатель `П-017` (а это индекс яруса A — его прочтёт каждая будущая сессия) сделает вывод
«механизма FACTS в `main` нет» — ложный, с прямыми последствиями: механизм либо перестроят,
либо проигнорируют.

**Отягчение, не смягчение:** посылка была протухшей уже у арбитра — `A-015` закоммичен
2026-08-22 22:17 (`2c01368`, ветка `fix/M-65-battery-recalibration`), через сутки ПОСЛЕ
merge PR #49, и §6 формулирует вариант (б) как «PR #49 отклонён `C-116`». То есть founder
выбирал из вариантов, где (б) был описан ложно: реально (б) означал «доработку УЖЕ ЖИВОГО
механизма», а не «воскрешение отклонённого». Цитировать вопрос арбитра запись вправе; делать
его протухшую посылку СВОИМ утверждением п.2 — нет.

**Воспроизведение:** две команды выше; обе выполнены в этом сообщении перепроверки.

**Условие снятия:** переписать п.2 (и абзац пересказа вопроса) по фактическому состоянию:
PR #49 влит 21.08 (`3914ee6`), после `C-116` REJECT был круг `C-117` (закрыт NOTE), детектор
FACTS живёт в `main`; «(б) не выбран» тогда означает «живой механизм НЕ дорабатывается под
этот класс — класс закрывается барьером B». Отдельно: architect решает, менялся ли смысл
выбора founder'а от ложной посылки (б), и надо ли пере-предъявить вопрос — это не моё
решение; моё — назвать ложь. Смежное: профиль `.claude/agents/architect.md` в `main` несёт то
же протухшее «в `main` детектора нет» (раздел про `R-097`) — вне этого дифа, правится
отдельным заходом (зона §11: токен + §9).

---

## Проверено и ПОДТВЕРЖДЕНО — (а) утверждения о коде, каждое своим прогоном

Все прогоны — на дереве предмета `/tmp/hft-recheck-r114` (detached `848bfef`) и на копии
`origin/main` (`29c4640`); дерево слияния покрыто `--merge-preview` (ниже).

**П-1. «Разбор шести дал 5 ложных красных из 6».** Скрипт замера A (спека §4) прогнан мной:
на `origin/main` → `9 3 6`, те же шесть функций, что в step1; на дереве предмета → `9 4 5`
(шестой кандидат закрыт правкой в `3b5e5b0` — свидетель стал видим детектору). Все пять
свидетелей найдены ОТКРЫТИЕМ файлов на `origin/main`:
- `red_compaction.rs:388` — `assert!(restored > 0, "крах-окно не воспроизведено")` внутри
  `c8_…` (`:380`);
- `red_replay_digest_delivery.rs` `rd_6_…` (`:289`) — `assert_eq!(code, 0, …)`;
- `red_retention_compacted.rs` `rt_z_4_…` (`:384`) — три `>= 2` (`zst_before`, `pruned_zst`,
  `cold_zst`);
- `red_depth_lifetime_perlife.rs` `dv_i_14_…` — `assert_eq!(near.lives_born, 1, …)`;
- `red_catalog_equivalence.rs` — пара `claims()` (`:239-248`, зовётся из цикла тактов `:496`)
  ⊕ `assert_manifest_executed` (`:250`) образует равенство множеств в обе стороны.
Шестой (`assert_catalog_truthful`) — вакуум подтверждён структурой: на ПУСТОМ каталоге все
три сверки (`:764-803` на `main`) вакуумно истинны (обе стороны пусты).

**П-2. «6 строк из 340 = 1.8 %».** Скрипт замера B (спека §4) прогнан мной на дереве
предмета: `340 6` — те же четыре файла (M-09, M-38b ×3, M-58, M-62). Сошлось.

**П-3. «Единственный подтверждённый вакуум закрыт точечной правкой оракула» + мутация.**
Правка найдена: `3b5e5b0`, +12 строк в
`crates/gateway/tests/red_segment_meta_bound.rs:764-775` (свидетель
`assert!(!observed.is_empty(), "… SETUP НЕ СОСТОЯЛСЯ …")` с независимого обхода
`truth_indices`). Мутация «независимый обход выродился в пустой» (тело `truth_indices` →
`Vec::new()`) прогнана мной на дереве предмета:

```
test result: FAILED. 7 passed; 3 failed   (sm8, sm9, sm10)
panicked at red_segment_meta_bound.rs:771: SM-8 такт 1 …: SETUP НЕ СОСТОЯЛСЯ — независимый
обход каталога не нашёл НИ ОДНОГО сегмента …
```

— числа и текст совпали с заявленными в `3b5e5b0` дословно. Файл восстановлен
(`git checkout --`, porcelain пуст). Нюанс — N-2 ниже.

**П-4. «Класс воспроизвёлся на M-65 ЧЕТЫРЕЖДЫ, дважды после §10bis».** `A-015` §6 открыт
(с ветки `fix/M-65-battery-recalibration`): формулировка записи — точная цитата вопроса
арбитра; `§10bis` в `milestones/M-65-ws-session.md` на той ветке существует (grep → 1).

**П-5. Выбор базы ветки — воспроизведён исполнением.** На чистой копии `origin/main`
(`29c4640`) применён ТОЛЬКО диф записи, прогнан `verify_design_claims.sh`:

```
FAIL  [4-МЁРТВЫЕ-ФАЙЛЫ] docs/PENDING-SIGNATURE.md:907: ссылка на
      `docs/plans/two-barriers-step1-2026-08-23.md` — файл не существует
VERDICT: FAIL (1 нарушений); exit=1
```

— дословно как в теле коммита. База поверх `3b5e5b0` (носителя step1) выбрана ВЕРНО, и
объяснение автора подтверждено исполнением, а не поверено на слово. На дереве предмета
`bash scripts/verify_design_claims.sh --merge-preview origin/main` → `VERDICT: PASS
(0 нарушений)`, exit=0.

## (б) Полномочия

- **Три следствия из «A. Принимаю рекоммендацию (а)»:** №1 (барьер B обязателен как
  механизм класса) — законный вывод: founder принял (а) как ОТВЕТ на «чем закрывать класс»;
  №3 ((в) отвергнут, TD не заводится) — законный вывод из выбора (а) вместо (в). №2 —
  негативная часть («(б) не выбран») законна, фактура ложна (Б-1).
- **Граница C** (`gates.md` §0.1) не задета: процессно-инженерный предмет, ни денег, ни
  состава данных, ни фаз; `A-015` §4 констатирует то же для исходного спора.
- **Замок §11:** зона (`gates.md:447-448`) — `.claude/rules/**` · `.claude/agents/**` ·
  `.claude/wrappers/**` · `CLAUDE.md` · `docs/04-workflow.md`. `docs/PENDING-SIGNATURE.md`
  в ней НЕ значится; коммит трогает только его (`--numstat`: один файл). Токен
  `FOUNDER-APPROVED` не требуется — автор прав.
- **«Барьер A не строится»** — инженерное решение architect'а по замеру, в его праве:
  разморозка A2 была разрешением, не приказом; таблица в записи честно помечена «снято
  разбором, а не планом», решение атрибутировано (`step1` §4). Остаточный хвост — N-6.

## (в) Связность

Все пути записи существуют на дереве слияния (`--merge-preview` PASS выше):
`two-barriers-step1` (ветка), `two-barriers-spec` §2 = барьер B (`main`), `A-010` §H
(`research/arbitration/A-010-nine-disputes-2026-08-18.md:573`), `C-116`
(`research/critiques/C-116-facts-marker.md`, `verdict: REJECT`), PR #49. Исключение по
смыслу — N-4.

---

## NOTE (не блокируют; для передачи по принадлежности)

- **N-1 (зона PR #66, не этого дифа):** step1 §2 цитирует встречный сторож как
  `red_catalog_equivalence.rs:183-190`; фактически `claims()` — `:239-248` (проверено на
  `92f6e65` и `29c4640`). Битая строка-ссылка в разборе.
- **N-2 (зона PR #66):** мутация `truth_indices → пусто` НЕ дифференцирует правку по числам:
  на `main` (ДО правки) она даёт те же `7 passed; 3 failed` — но другими ассертами
  (`:777` СОСТАВ, `:959` дубль, `:1023` посторонний файл; прогнано мной на копии `main`).
  Фраза `3b5e5b0` «до правки те же три такта были ЗЕЛЕНЫ на пустоте» верна только в чтении
  «на пустом КАТАЛОГЕ» (изолирующая проба вакуума), не «при этой мутации». Свидетель при
  этом реально пиннит setup-вакуум: узкая мутация только его входа красит `:771` (прогнано,
  8/2).
- **N-3:** запись называет #66 «doc-часть», но `3b5e5b0` несёт +12 строк в sacred-оракуле
  (`crates/gateway/tests/`) под заголовком `docs(plans)` — состав коммита шире типа и шире
  слова «doc-часть». Судить это — кругу PR #66.
- **N-4:** `A-015` живёт только на ветке `fix/M-65-battery-recalibration`; для читателя
  `main` ссылка записи разрешится лишь после приземления той ветки. Порядок приземления в
  записи («#66 → эта запись») этого не оговаривает.
- **N-5:** заголовочные числа (5 из 6; 6/340 = 1.8 %) с этой записью живут уже в ТРЁХ
  документах (спека, step1, `П-017`) плюс два тела коммитов — при собственном заявлении
  «здесь они НЕ пересказываются». Конструкция и сценарии действительно не пересказаны, и
  таблица датирована («на 2026-08-23 (вечер)») — потому NOTE, не блокер; но это тот самый
  класс «расхождение носителей»: числа A уже разошлись однажды за сутки (9/3/6 → 9/4/5).
- **N-6:** класс «вакуумный ассерт» остаётся без механизма И без TD-записи; норма
  `binding-requires-mechanism` требует «механизируй либо признай остаточный риск записью».
  Проза в `П-017`/step1 названа честно, но канонический носитель остаточного риска —
  `TECH-DEBT.md` (reviewer-owned): нужен handoff reviewer'у на карточку.

## Done Block

```
$ git worktree add --detach /tmp/hft-recheck-r114 origin/docs/founder-decision-a015-2026-08-23
HEAD is now at 848bfef  (ветка занята /tmp/hft-arch-pending — запасной путь branch-hygiene п.2)

$ git merge-base HEAD origin/main
92f6e65a90382506eb3fcc4ab09d4744ffeaade4

$ python3 <скрипт A из спеки §4>      # origin/main 29c4640
9 3 6  (шесть функций — совпали со step1)
$ python3 <скрипт A>                  # дерево предмета 848bfef
9 4 5  (assert_catalog_truthful закрыт правкой 3b5e5b0)
$ python3 <скрипт B>                  # дерево предмета
340 6  (M-09, M-38b×3, M-58, M-62)

$ cargo test -p gateway --test red_segment_meta_bound   # мутация truth_indices→[]
test result: FAILED. 7 passed; 3 failed   (…:771 SETUP НЕ СОСТОЯЛСЯ)
$ cargo test … # та же мутация на копии origin/main
test result: FAILED. 7 passed; 3 failed   (…:777/:959/:1023 — другие ассерты)

$ bash scripts/verify_design_claims.sh   # копия main + только диф записи
FAIL [4-МЁРТВЫЕ-ФАЙЛЫ] docs/PENDING-SIGNATURE.md:907 …; exit=1
$ bash scripts/verify_design_claims.sh --merge-preview origin/main   # дерево предмета
VERDICT: PASS (0 нарушений); exit=0

$ gh pr view 49 --json mergedAt,mergeCommit --jq '.mergedAt, .mergeCommit.oid'
2026-08-21T15:09:50Z / 3914ee67268b859a5f482e6a2dd42d0bb6e9f708
$ git show origin/main:scripts/verify_design_claims.sh | grep -c FACTS
30

$ git status --porcelain            # после отката мутаций, до записи вердикта
(пусто)
```
