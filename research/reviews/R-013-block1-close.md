# R-013 — PR-гейт Блока 1 (закрывающий круг): эволюция `DESIGN.md` в редакцию AlphaQuant

- **Дата (UTC):** 2026-08-01
- **Роль:** reviewer
- **Ветка:** `docs/design-evolution`, HEAD `0a74e02`
- **Цель merge:** `origin/main` (HEAD `503b147`)
- **Предыдущие круги:** `R-004` (REJECT, 3 блокера — устранены) · `R-011` (REJECT, блокер Б-1 — устранён)
- **Подпись founder'а:** `docs/PENDING-SIGNATURE.md` П-001 — ✅ ПОДПИСАНО 2026-08-01 (`f5fe12e`)

## ВЕРДИКТ: ❌ REJECTED — 2 блокера (Б-2, Б-3)

Подпись П-001 разблокировала merge и **остаётся в силе** — она не отменяет гейт, о чём сама
и говорит: «reviewer по-прежнему обязан проверить … Если reviewer найдёт новый блокер — merge
не делается, подпись остаётся в силе и ждёт устранения». Найдены два новых блокера. Merge НЕ
сделан. `STATUS: PROPOSED` в шапке `docs/DESIGN.md` НЕ переведён в `ACTIVE`.

Оба блокера — один корень: **состояние документа замерено на ветке, а не на цели merge.**
Ветка отстаёт от `main` на 50 коммитов, и за эти 50 коммитов состояние кода изменилось
дважды в местах, о которых документ делает утверждения. На ветке документ говорит правду;
в дереве, куда он вливается, — нет.

---

## Block-scope — ✅ PASS

```
$ git diff --stat origin/main...HEAD
 docs/06-data-layer-and-storage.md                 |  34 +-
 docs/07-cockpit-backend-roadmap.md                |  19 +-
 docs/08-arch-improvement-roadmap.md               |  11 +-
 docs/09-roadmap-v2.md                             |  91 ++-
 docs/DESIGN-v2.md                                 | 787 ----------------------
 docs/DESIGN.md                                    | 770 +++++++++++++++++++--
 docs/fa/killswitch.md                             |   4 +
 docs/fa/oms.md                                    |   4 +
 docs/fa/risk.md                                   |   8 +
 docs/product/alphaquant-source.md                 | 455 +++++++++++++
 research/critiques/C-041-design-evolution.md      | 358 ++++++++++
 research/critiques/C-042-design-evolution-risk.md | 287 ++++++++
 research/reviews/R-004-block1-design-evolution.md | 329 +++++++++
 research/reviews/R-011-block1-final.md            | 332 +++++++++
 14 files changed, 2597 insertions(+), 892 deletions(-)

$ git diff --name-only origin/main...HEAD | grep -vE '^(docs/|research/critiques/|research/reviews/)'
(none outside docs/ + research/critiques + research/reviews)
```

Ни одного касания `crates/**`, `contracts/**`, `scripts/**`, `milestones/**`. Файлы
`research/critiques/**` и `research/reviews/**` — артефакты гейтов, коммитятся своей ролью
(`.claude/rules/branch-hygiene.md` §3), это не превышение scope. Block-C неприменим
(`crates/contracts/**` не тронут), RISK-BLOCK неприменим (`risk`/`killswitch`/`oms`/`venue-*`
не тронуты; правки `docs/fa/{risk,killswitch,oms}.md` — DEFERRED-шапки, не код).

## Block-Б1 (блокер `R-011`) — ✅ ЗАКРЫТ ПО СУЩЕСТВУ

`1e44807` переписал строку P2.5 в §10. Тройное самопротиворечие («✅ пройдена» / «хвост
докатывается» / ворота, ни одни из которых не выполнены) устранено: статус — `🚧 частично`,
заголовок таблицы получил оговорку «кроме строк, явно помеченных 🚧 частично», сделанное
и несделанное разведены поимённо, каждые ворота получили отдельный вердикт.

Проверил не формулировку, а факты, на которые она ссылается — все подтверждены:

| Утверждение §10 P2.5 | Проверка | Итог |
|---|---|---|
| M-09 tasks 1/2/4 MERGED | `milestones/M-09-data-safety-net.md` строки 101/102/104-106: task 1 ✅ `cf53e81`, task 2 ✅ `17b02a7`, task 4/4C/4D ✅ `9a352d6`/`ac645ac`/`83c340c` | ✅ |
| cold-copy offsite — НЕ СДЕЛАНО | `milestones/M-09…` task 3 = `⏳`; `TECH-DEBT.md:449` «`/mnt/*` пуст, Hetzner Storage Box не заведён/не смонтирован» | ✅ |
| TD-020 OPEN | `TECH-DEBT.md:507` «TD-020 остаётся OPEN до первого успешного `apply` со Storage Box» | ✅ |
| recon ε_test ловится — ЗЕЛЁНЫЙ | `crates/ops/tests/red_ops_recon.rs` существует; `scripts/verify_M-09.sh:29` `run "T2 OPS-I-1 recon (ε_test, деградированные входы)"` | ✅ |
| паритет алерт↔метрика — ЗЕЛЁНЫЙ | `scripts/verify_M-09.sh:49` `run "T4B OPS-I-5 правила алертов + rule-паритет"` | ✅ |
| restore-drill НЕ ПРОВОДИЛСЯ НИ РАЗУ | `milestones/M-09…` task 3 `⏳`, заблокирован Storage Box (founder ★) | ✅ |

**Б-1 закрыт.** Ни одно из добавленных утверждений не является новой необеспеченной
претензией.

## Block-каркас — ✅ PASS

- Разделов в `docs/DESIGN.md`: **25** (`§0`–`§23` + `§1.5`). База (merge-base `8c1890e`) —
  14 (`§0`–`§12` + `§1.5`). Эволюция аддитивная: добавлены `§13`–`§23`, нумерация каркаса
  сохранена — ровно то, что подписано в П-001.
- **`§1`–`§8` — байт-в-байт неизменны** относительно merge-base:
  ```
  $ diff -u <(awk '/^## §1\. /,/^## §9\. /' base) <(awk '/^## §1\. /,/^## §9\. /' new) | grep -cE '^[-+][^-+]'
  0
  ```
- Расширены только заявленные: `§0` (13→29 строк, продуктовый разворот), `§9` (16→27,
  уроки), `§10` (40→63, фазы), `§11` (11→21), `§12` (6→14). `§1.5` (36→36) не тронут.
- Ссылки `DESIGN.md §N` по репозиторию — 23 вхождения строго смежной формы (`DESIGN.md §N`
  подряд), 147 вхождений `DESIGN.md` с §-ссылкой в пределах 40 символов, 262 упоминания
  `DESIGN.md` всего. Цифра «~58» в шапке — оценка порядка, а не замер; занижена, но ничего
  ложного о коде не утверждает, находкой не считаю. Целевые разделы строгой формы —
  `§0 §1 §2 §4 §6 §7 §8 §9 §10 §11 §14 §22`, **все существуют**. Машинный гейт
  подтверждает независимо (проверка 3-ССЫЛКИ) и дополнительно: 98 ссылок `docs/*.md`
  ведут на существующие файлы (проверка 4-МЁРТВЫЕ-ФАЙЛЫ).

## Block-гейт — прогон `verify_design_claims.sh` (мой, сырой вывод)

### (а) На ветке `docs/design-evolution` (HEAD `0a74e02`) — PASS

```
$ git checkout origin/feat/verify-design-claims -- scripts/verify_design_claims.sh
$ bash scripts/verify_design_claims.sh; echo "exit=$?"
PASS  [1-ЕСТЬ] все 3 маркеров [ЕСТЬ] в таблицах статусов сопровождены существующим пруфом (15 вне таблиц — ASCII-схемы/проза, не проверялись)
PASS  [2-ПОКРЫТИЕ] §22: SM-I — заявлено=10, в оракулах=10 — подтверждено замером (loose=10)
PASS  [2-ПОКРЫТИЕ] §22: VB-I — заявлено=11, в оракулах=8 — подтверждено замером (loose=8)
PASS  [2-ПОКРЫТИЕ] §22: OPS-I — заявлено=10, в оракулах=8 — подтверждено замером (loose=8)
PASS  [2-ПОКРЫТИЕ] §22: ST-I — заявлено=10, в оракулах=8 — подтверждено замером (loose=8)
PASS  [2-ПОКРЫТИЕ] §22: CT-I — заявлено=6, в оракулах=5 — подтверждено замером (loose=5)
PASS  [2-ПОКРЫТИЕ] §22: GW-I — заявлено=0, в оракулах=12 — подтверждено замером (loose=12)
PASS  [2-ПОКРЫТИЕ] §22: JR-I — заявлено=7, в оракулах=3 — подтверждено замером (loose=3)
PASS  [2-ПОКРЫТИЕ] §22: RK-I — заявлено=10, в оракулах=0 — подтверждено замером (loose=3)
PASS  [2-ПОКРЫТИЕ] §22: INTG-I — заявлено=7, в оракулах=0 — подтверждено замером (loose=0)
PASS  [2-ПОКРЫТИЕ] §22: BK-I — заявлено=8, в оракулах=0 — подтверждено замером (loose=0)
PASS  [2-ПОКРЫТИЕ] §22: VN-I — заявлено=9, в оракулах=2 — подтверждено замером (loose=2)
PASS  [3-ССЫЛКИ] все 6 ссылок `DESIGN.md §N` указывают на существующие разделы
PASS  [4-МЁРТВЫЕ-ФАЙЛЫ] все 98 ссылок вида docs/*.md указывают на существующие файлы
INFO  [5-ФАЗЫ] фаза «**P0 Журнал** ✅» помечена пройденной, но строка не цитирует ни одного M-NN — не проверяется машинно
INFO  [5-ФАЗЫ] фаза «**P1 Data plane** ✅» помечена пройденной, но строка не цитирует ни одного M-NN — не проверяется машинно
INFO  [5-ФАЗЫ] фаза «**P2 Research core** ✅» помечена пройденной, но строка не цитирует ни одного M-NN — не проверяется машинно
PASS  [5-ФАЗЫ] 4 строк(и) фаз, помеченных пройденными, не противоречат цитируемым milestone'ам (грубая проверка)

VERDICT: PASS (0 нарушений)
exit=0
```

Прогон architect'а воспроизведён. Скрипт после прогона удалён из рабочего дерева
(`git status --porcelain` пуст) — он живёт в `feat/verify-design-claims`, в этот PR не
затаскивается.

### (б) На дереве merge-превью (`origin/main` + `0a74e02`, слияние чистое) — **FAIL**

```
$ git merge --no-ff --no-commit 0a74e02
Automatic merge went well; stopped before committing as requested
$ bash scripts/verify_design_claims.sh; echo "exit=$?"
PASS  [2-ПОКРЫТИЕ] §22: VB-I — заявлено=11, в оракулах=8 — подтверждено замером (loose=8)
FAIL  [2-ПОКРЫТИЕ] §22: семейство JR-I — документ заявляет 'в оракулах'=3, реальный замер (анти-плацебо) strict=4, loose (любое упоминание)=4
PASS  [2-ПОКРЫТИЕ] §22: RK-I — заявлено=10, в оракулах=0 — подтверждено замером (loose=3)
VERDICT: FAIL (1 нарушений)
exit=1
```

**Зелёный гейт на ветке ≠ зелёный гейт на main.** Это ровно тот класс, ради которого гейт
и строился, только на такте позже: цифра верна для дерева, где её мерили, и неверна для
дерева, куда она едет.

## Block-числа §22 — моя независимая перепроверка (не доверяя ни документу, ни гейту)

Свой греп, без использования скрипта, по обоим деревьям.
Команда: `grep -rhoE "\b<FAM>-I-[0-9]+\b" --include='*.rs' crates/*/tests/ | sort -uV`

| Семейство | Документ | Ветка `0a74e02` (мой замер) | `origin/main` (мой замер) | Итог |
|---|---|---|---|---|
| SM-I | 10 | 10 | 10 | ✅ |
| VB-I | 8 | 8 | 8 | ✅ |
| OPS-I | 8 | 8 | 8 | ✅ |
| ST-I | 8 | 8 | 8 | ✅ |
| CT-I | 5 | 5 | 5 | ✅ |
| GW-I | 12 | 12 | 12 | ✅ |
| **JR-I** | **3** | **3** (`JR-I-1,4,8`) | **4** (`JR-I-1,4,8,9`) | ❌ **Б-2** |
| RK-I | 0 | 0 в домашних крейтах | 0 в домашних крейтах | ✅ |
| INTG-I | 0 | 0 | 0 | ✅ |
| BK-I | 0 | 0 | 0 | ✅ |
| VN-I | 2 | 2 (`VN-I-5,7`) | 2 | ✅ |

Разобрал два места, где мой наивный греп сначала разошёлся с документом, — оба раза прав
документ:

- **RK-I.** Наивный греп даёт 3 (`RK-I-1` в `crates/portfolio/tests/red_portfolio.rs`,
  `RK-I-3` в `crates/alpha/…` и `crates/venue-hyperliquid/…`, `RK-I-8` в
  `crates/sim/tests/red_strategy_backtest.rs`). Все пять упоминаний — в ЧУЖИХ крейтах;
  домашние крейты семейства (`crates/risk`, `crates/killswitch`, `.claude/rules/testing.md`)
  не существуют. **0 — правда**, и это тот же вывод, что `C-042` F-1.
- **VB-I.** Наивный греп даёт 9. Девятый — `VB-I-9b` в
  `crates/gateway-serve/tests/red_jwt_verify.rs:1`: суффиксный под-ID, не `VB-I-9`.
  **8 — правда.**

**`VN-I` формат «2 (strict) / 2 (loose)»** — прежняя отсылка «см. сноску» заменена явными
числами; оба совпали с замером. Сноска-объяснение (суита M-41 из 40 тестов существует, долг
— маркировка) сохранена и по-прежнему корректна.

---

## Б-2 (БЛОКЕР) — §22, `JR-I`: «в оракулах = 3» ложно в дереве, куда документ вливается

**Где:** `docs/DESIGN.md:838`, таблица §22, строка `JR-I | event-store | 7 | 3 | [ЧАСТИЧНО]`.

**Симптом.** В merge-цели покрытие `JR-I` — **4**, а не 3. Четвёртый оракул — `JR-I-9`,
живой RED-набор в ДОМАШНЕМ крейте семейства: `crates/journal/tests/red_floor_scan.rs`,
`red_floor_scan_bounded.rs`, `crates/journal/tests/common/mod.rs` (ассерт
«`JR-I-9 НАРУШЕН`»). Это не упоминание-аналогия в чужом крейте — это покрытие, которого
документ себе не засчитывает.

**Откуда взялось.** `JR-I-9` пришёл в `main` с M-50 (`a188b3c` RED-оракулы →
merge `163939a`, вердикт `R-007` APPROVED) — уже ПОСЛЕ того, как ветка
`docs/design-evolution` отделилась. Замер §22 (2026-07-29) снят на ветке и на ветке верен.

**Воспроизведение:**
```
git worktree add /tmp/chk --detach origin/main && cd /tmp/chk
git merge --no-ff --no-commit 0a74e02
git checkout origin/feat/verify-design-claims -- scripts/verify_design_claims.sh
bash scripts/verify_design_claims.sh; echo "exit=$?"     # → VERDICT: FAIL (1 нарушений), exit=1
grep -rhoE "\bJR-I-[0-9]+\b" --include='*.rs' crates/*/tests/ | sort -uV
# JR-I-1 JR-I-4 JR-I-8 JR-I-9
```

**Почему блокер, а не NOTE.** (1) Merge публикует на `main` заведомо ложное число — восьмой
случай того же класса за трое суток, ради прекращения которого Блок 1 и делался.
(2) Машинный гейт документа становится КРАСНЫМ на `main` в момент merge — первый же его
прогон после слияния падает. Заводить машинную проверку и тем же движением уронить её —
хуже, чем не заводить: следующий агент научится, что её красный цвет ничего не значит.

## Б-3 (БЛОКЕР) — §23.1 и §9.1: «push-алертинга нет вовсе» / `[ПЛАН]` — неправда о коде в merge-цели

**Где:**
- `docs/DESIGN.md:878-879` — «…но **push-алертинга нет вовсе** (R10в): слепота между
  ручными ssh-проверками».
- `docs/DESIGN.md:883` — «1. **Push-алертинг вне машины** [ПЛАН, первым шагом —
  cron-watchdog → Telegram]».
- `docs/DESIGN.md:272` — §9.1, таблица «Уроки, которые мы ЕЩЁ НЕ выполнили (честный список,
  2026-07-29)», строка «алертинга НЕТ вовсе (R10в)».

**Симптом.** В merge-цели этот код существует, смержен и стоит в проде. `PROJECT-STATE.md`
на `origin/main` (файл, который веду я) строка 2120:

```
## Алертинг — `ops-watchdog` ✅ КОД В MAIN (merge `cc5197c`, R-010 APPROVED 2026-08-01)
```

Крейт `crates/ops` расширен (`watchdog.rs`, `watchdog_cycle.rs`, `TelegramTransport`,
бинарь `bin/ops-watchdog.rs`, обёртка `scripts/watchdog_cron.sh`), acceptance-гейт
`scripts/verify_alerting.sh` — 25 проверок, четыре круга ревью (`R-005`/`R-008`/`R-009`/
`R-010`). Ровно тот «cron-watchdog → Telegram», который §23.1 называет планом.
После merge мастер-документ будет прямо противоречить `PROJECT-STATE.md` и
`docs/PENDING-SIGNATURE.md` П-003 («алертинг ГОТОВ и в проде») в одном и том же дереве.

**Нюанс, который я обязан назвать, чтобы фикс не ушёл в противоположную ложь.** Алерты
сегодня действительно НЕ доставляются: нужны `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` в
окружении VPS и строка в cron — оба действия founder'а, П-003 открыт; до этого
`TelegramTransport` — штатный no-op. То есть верное состояние — **«построено и смержено,
не включено (ждёт секрета founder'а)»**, а не «нет вовсе» и не «✅ выполнено». Ребут-дрилл
(§9.1, строка 275) отдельно и по-прежнему не проводился — эту строку замечание не трогает.

**Почему блокер.** Это тот же дефект, что и семь предыдущих, только вывернутый: документ
утверждает состояние кода, которое не соответствует дереву. Машинный гейт его НЕ ловит —
он проверяет обеспеченность маркеров `[ЕСТЬ]`, а обратный дрейф (`[ПЛАН]` поверх
существующего кода) у него вне зоны. Здесь гейта нет, и бэкстоп — reviewer. Цена ошибки
конкретна: следующая сессия читает мастер-документ, видит «алертинга нет вовсе / [ПЛАН]» и
тратит цикл на постройку уже построенного — либо, что хуже, считает систему слепой и
принимает решения исходя из этого.

---

## Что НЕ является находкой (проверено и снято)

- **Ф0-строка §10** упоминает «push-алертинг вне машины + ребут-дрилл (M-43)» без ✅ —
  это недосказанность внутри фазы «🚧 в работе», а не ложное «сделано». Не блокирую;
  привести в порядок разумно тем же движением, что Б-3.
- **§9.1 «ретеншен-apply ни разу не выполнялся (R1)»** — правда: `TECH-DEBT.md:507`,
  TD-020 OPEN, `/mnt/*` пуст.
- **INFO-строки гейта [5-ФАЗЫ]** (P0/P1/P2 не цитируют M-NN) — фазы дореестровой эпохи,
  машинно непроверяемы по построению. Не дефект документа.
- **`research/critiques/**` и `research/reviews/**` в диффе** — артефакты гейтов,
  коммитятся своей ролью (`branch-hygiene.md` §3).

## Что требуется до следующего круга

Устранение Б-2 и Б-3 — зона **architect** (`docs/**` — не моя зона записи; я описываю
дефект, не проектирую фикс — `gates.md` §4, граница reviewer↔architect). После правок
гейт `verify_design_claims.sh` обязан быть зелёным **на дереве merge-превью**
(`origin/main` + ветка), а не только на ветке — иначе следующий круг упрётся в то же
место третий раз.

## Подпись П-001 — статус

**Остаётся в силе, merge ждёт.** Подпись снимает блокировку границы C (смена продуктовой
модели и приоритетов фаз) и НЕ является приёмкой фактов документа. Формулировка самой
записи П-001 это предусматривает дословно. Никакой пере-подписи после устранения Б-2/Б-3
не требуется — требуется только зелёный круг reviewer'а.

## Push-статус

⚠ NOT merged; ветка `docs/design-evolution` не влита в `main`. `docs/DESIGN.md` остаётся
`STATUS: PROPOSED`. `PROJECT-STATE.md` / `docs/PENDING-SIGNATURE.md` не обновлялись —
обновление наступает по APPROVED. Настоящий вердикт запушен на ветку milestone'а.
