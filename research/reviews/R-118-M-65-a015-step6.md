<!-- GATE-META
milestone: M-65
audited_repo: a3ka/hft-platform
audited_base: a9f0bf511a0a1de5433b0de412cb55234027b56a
audited_head: 87d9c8705e5ab167934689875e70ef288bfbc8b3
verdict: APPROVE
-->

# R-118 — M-65 ws-session, PR-гейт круг 4 (шаг 6 маршрута `A-015` §5): **APPROVED**

**Роль:** reviewer (PR-time гейт, `gates.md` §4 — UNCONDITIONAL) · **Дата (UTC):** 2026-08-23
**Предмет:** PR #29, ветка `fix/M-65-battery-recalibration`, вершина `87d9c87`;
диапазон `a9f0bf5..87d9c87` (база = `base.sha` PR'а, та же, какую подаёт CI).
**Дерево слияния:** `origin/main` `4e1a881` + предмет → `c6a8d46` (`git merge-tree` конфликтов не дал).

**Цепочка:** `C-077` REJECT → `C-078` NOTE → `R-057` REJECT → `R-075` APPROVE → `R-080` APPROVE →
`R-086` REJECT → `R-093` REJECT → `C-098` REJECT → `R-111` REJECT → **`A-015` DECISION** →
шаг 1 `f4f0a49` → шаг 2 `C-125` REJECT → `C-126` PASS → шаг 3 `135bc8d` → шаг 4 `d784e15` →
шаг 5 `87d9c87` → tester PASS → **этот вердикт**.

---

## Block-FA — живой инвариант тронутого модуля

Диапазон трогает `crates/gateway-serve/**`. **Живой инвариант: `GS-I-4`** — «`gateway-serve`
сериализует `Snapshot`/`Frame` целиком (`serde_json::to_vec`), т.е. per-session поля
`SeriesBundle` — часть контракта с фронтом БЕЗ явного упоминания в коде транспорта»
(`docs/fa/viz-backend.md:153`, внутри `VB-I-6`). Диапазон его НЕ трогает: правка живёт в
`serve_config_from_env` — валидации конфигурации ДО открытия сокета; форма кадров, состав
`SeriesBundle` и путь сериализации не изменены (диф `crates/gateway-serve/src/lib.rs` = 41/32
строки в двух местах: объявление константы и ветка разбора одной env-переменной).

**Поправка к §E вердикта тестера — проверено, утверждение НЕВЕРНО.** Тестер пишет, что
`FA-WAIVER` на `gateway-serve` «неявный, крейт не покрыт `docs/fa/`». Замер:
`scripts/check_review_fa.sh:195-200` числит `gateway-serve` **LIVE-крейтом** с
`FA = docs/fa/viz-backend.md` и префиксом `GS`; NO-FA-список ЗАКРЫТ и состоит из `recorder` и
`derive` (`:117-119`). Waiver на LIVE-крейт барьер принимает по форме, но требование назвать
живой ID он этим не отменяет — он его лишь не ловит. Прогон барьера в прод-форме
(`EVENT_NAME=pull_request`, `PR_BASE_SHA=a9f0bf5`) на дереве слияния: `exit=0`, и он сам
печатает `R-093 → GS-I-4`, `R-111 → GS-I-4` — то есть предыдущие два круга ID называли.

## Block-scope — диф соответствует зонам, одно отклонение САНКЦИОНИРОВАНО

Ролевая раскладка последних пяти коммитов проверена покоммитно (`git show --name-only`):

| коммит | пути | роль | зона |
|---|---|---|---|
| `f4f0a49` + `41c07e9` | `docs/rfc/CT-RFC-09-ws-session.md` | architect | см. ниже |
| `51268fc` | `research/critiques/C-126-*.md` | critic | ✅ `scope-guard` |
| `8785f38` | `research/reviews/R-111-*.md` | architect (разведение номера) | ✅ |
| `135bc8d` | `crates/gateway-serve/tests/red_max_subs_config.rs`, `scripts/verify_M-65.sh` | architect | ✅ sacred, architect-only |
| `d784e15` | `crates/gateway-serve/src/lib.rs` | engine-dev | ✅ `crates/gateway-serve/src/**` |
| `87d9c87` | `crates/gateway-serve/tests/red_ws_session.rs`, `milestones/M-65-*.md` | architect | ✅ |

**Отклонение и его основание.** `milestones/M-65-ws-session.md` §2 в списке `Forbidden всем`
несёт «изменение нормативной части RFC (§2.1-2.9)», а `f4f0a49`/`41c07e9` меняют ровно §2.6.
Это НЕ нарушение: правку предписал арбитраж `A-015` §5 шаг 1 поимённо, а решение арбитра
обязательно к исполнению и стоит выше milestone-спеки (`gates.md` §0). Узость соблюдена
буквально — диф §2.6 трогает две строки норматива (`:121-122`), строка «валидный диапазон —
целое `>= 1`» и §6 (подписи founder'а) не тронуты, прежняя редакция сохранена под `<details>`
как аудит-трейл.

**Order-egress нет** (`gates.md` §5 MD-only carve-out неприменим — крейт вообще не в
venue-классе): `crates/gateway-serve` — read-only консюмер журнала; диапазон не трогает
`crates/{risk,killswitch,oms,venue-*,contracts,journal,gateway}/**`. Проверено `git diff
--stat a9f0bf5..87d9c87` — из `crates/**` затронут ТОЛЬКО `gateway-serve`.

## Block-C — контрактный слой

`crates/contracts/**` в дифе **отсутствует**; T1-типы, JSON Schema и `SCHEMA_VERSION` не
тронуты. Правка `docs/rfc/CT-RFC-09` §2.6 — норматив поведения при конфигурации, прошла
маршрутом contract-RFC: узкий амендмент (`f4f0a49`) → критик на СИЛЬНОЙ модели со свежим
контекстом (RAW-гейт `gates.md` §1) → `C-125` REJECT (внутреннее противоречие о пустой
строке) → фикс `41c07e9` → **`C-126` PASS** с merge-preview. Требование `A-015` §5 шаг 2
исполнено; шаги 3-5 шли ПОСЛЕ вердикта критика, а не параллельно.

## Block-risk — risk-critic НЕ требуется, и это названо, а не пропущено

`gates.md` §5 привязывает RISK-BLOCK к `crates/{risk,killswitch,oms,venue-*,contracts}/**` —
ни один из этих путей в дифе не появляется. `gates.md` §9 требует risk-critic дополнительно,
когда документ трогает safety-путь (`docs/fa/{risk,killswitch,oms}.md`, `RK-I-*`, `INTG-I-*`,
анти-оверфит гейт §6); `CT-RFC-09` §2.6 — лимит подписок WS-транспорта market-плоскости, ни
одного из этих предметов не касается. Отсутствие risk-critic'а в цепочке — не пробел гейта.
MD-плоскость подтверждена отдельно: `VB-I-3`/`GS-I-1` (read-only) диапазоном не задеты.

## 1. Исполнение `A-015` §3 по существу — ПОДТВЕРЖДЕНО прогоном

Требование §3: (1) отсутствие И пустая строка ⇒ подписанная норма 16 + `warn`;
(2) невалидное ЗНАЧЕНИЕ ⇒ отказ старта.

Шаг `L` `verify_M-65.sh` пересекает границу процесса — запускает прод-бинарь той же формой
argv/env, какой его зовёт `docker-compose.yml`, и решает по КОДУ ВОЗВРАТА (`timeout 5` → 124
= процесс жив = старт состоялся). Сырой вывод — в Done Block §7. Все шесть проверок зелены:
отсутствие и пустота ⇒ старт СОСТОЯЛСЯ и уход на норму НАЗВАН в логе; `0`/`-1`/`abc` ⇒ отказ
старта `exit=2`.

Юнит-оракул `red_max_subs_config.rs` покрывает то же изнутри процесса, четырьмя сторонами
анти-плацебо, и `explicit_value_overrides_default` берёт значения по ОБЕ стороны нормы
(`1` и `64`) — реализация «всегда 16» через него не проходит.

## 2. Мутационный контроль — ПЕРЕПРОВЕРЕН МНОЙ, не принят по слову dev'а

Три мутации применены на дереве слияния `c6a8d46`, каждая — откачена; рабочее дерево после
проверено `git status --porcelain` (пусто). Сырой вывод — Done Block §7.

| # | мутация | ожидание | факт |
|---|---|---|---|
| M1 | `tracing::warn!` в ветке дефолта УДАЛЁН | падает `warn_on_default`, и только он | `warn_on_default ... FAILED`; 4 passed; 1 failed |
| M2 | ветка `None \| Some("")` ⇒ `return Err(...)` (позиция Б) | падает `absent_var_yields_signed_default_16` | `absent_…` + `empty_var_is_same_as_absent` + `warn_on_default` FAILED; 2 passed; 3 failed |

M2 воспроизводит ровно ту раскладку, которую engine-dev заявил как исходное RED-состояние
(«было 2 passed; 3 failed» — тело `d784e15`). Заявление подтверждено независимо.

## 3. Решение architect'а шага 5 — ПРИНИМАЮ, и оно доказано ЗАМЕРОМ, а не доводом

Architect отклонил предложение engine-dev'а привязать `red_ws_session.rs:69`
(`const MAX_SUBS: usize = 16`) к `server::DEFAULT_MAX_SUBSCRIPTIONS`. Довод — «оракул,
сверяющий код с константой того же кода, тавтологичен» — верен, но доводом он и оставался.
Я снял решающий замер (мутация M3, две половины, на дереве слияния):

| # | конфигурация | результат `o4_subscription_cap_is_fail_closed` |
|---|---|---|
| M3a | `DEFAULT_MAX_SUBSCRIPTIONS = 32`, оракул с ЛИТЕРАЛОМ `16` (как в предмете) | **FAILED** — подписанная норма уехала, оракул покраснел |
| M3b | `DEFAULT_MAX_SUBSCRIPTIONS = 32`, `MAX_SUBS = gateway_serve::server::DEFAULT_MAX_SUBSCRIPTIONS` (предложение engine-dev'а) | **ok** — подпись уехала МОЛЧА |

Разница ровно та, о которой писал architect, и она измерена, а не выведена. Фикстура
`GATEWAY_MAX_SUBSCRIPTIONS` не задаёт (`grep` по файлу пуст) и опирается на дефолт процесса,
поэтому `MAX_SUBS` — внешнее ожидание ПОДПИСИ founder'а, а не отражение кода. Привязка к
константе превратила бы sacred-оракул подписанной нормы в тавтологию — то есть купила бы
«один источник истины» ценой единственного сторожа этой нормы. **Решение принято; причина
внесена комментарием в сам оракул (`87d9c87`), то есть в носитель, а не в переписку.**

## 4. `d5a1cf5` остаётся в истории ветки — как это читается после merge'а

Факт: `d5a1cf5` («отсутствие ⇒ отказ старта», снятая арбитражем позиция Б) физически
остаётся в истории и приедет в `main` вместе с веткой. Реверт-механикой его не убирали
намеренно — `d784e15` есть откат СУТИ новым коммитом, и это правильно: переписанная история
читается как «так и было всегда», ровно тот класс, из-за которого созывался `A-015`.

**Решение (моё, и оно фиксируется здесь как норма для этого merge'а):** причина живёт в ДВУХ
носителях, оба уже на ветке, и третьего заводить не нужно —

1. `milestones/M-65-ws-session.md` §Tasks 13 (правка `87d9c87`) прямо говорит: «`d5a1cf5`
   вытеснен и доказательством N-2 больше не является»;
2. тело `d784e15` называет `d5a1cf5` поимённо и объясняет, почему это откат сути, а не revert.

**Что я делаю дополнительно:** merge-коммит PR #29 несёт эту связку в теле — чтобы читатель
`git log main` видел причину, не выходя в файлы. Формулировка — ссылка на `A-015` и на два
носителя выше, БЕЗ пересказа сути (`R-093` Б-3/Б-4: пересказ порождает четвёртый носитель,
который разойдётся первым). Без этого история ветки читается как «сделали, потом переделали»
без причины — и это единственное, что здесь требовалось починить.

## 5. Шаг `F` verify: «≥ 11 оракулов» против фактических 12 — расхождение снято ЗАМЕРОМ

Прогон печатает `исполнено 12 оракулов (ожидалось ≥ 11: o0 + O-1..O-10)`. Замер состава:
`grep -nE 'fn o[0-9]+' crates/gateway-serve/tests/red_ws_session.rs` → **13** функций,
`o0`..`o12`. Из них `o12` живёт под `--features testing` и обычным `cargo test` не
исполняется — его наблюдает ОТДЕЛЬНЫЙ шаг `O12`. Значит в шаге `F` исполняется `o0`..`o11` =
**12**, а порог `11` и его расшифровка «`o0` + `O-1..O-10`» отстают от набора на один оракул
(`o11_frames_come_from_journal_not_synthesis`, введён позже).

**Расхождение реально, но НЕ блокер:** сравнение `-ge`, число фактических оракулов больше
порога, гейт зелён честно. **Однако это ослабление наблюдения отсутствия** — того самого
свойства, ради которого `EXPECT_TESTS` записан числом (`C-078` N-2): сегодня потеря ОДНОГО
оракула из набора оставит шаг `F` зелёным (12→11 всё ещё `≥ 11`). Находка `N-1` ниже.

## 6. Находки

Ни одна не блокирует merge; все — NOTE, зона исполнения названа.

- **`N-1` (MINOR, зона architect).** `scripts/verify_M-65.sh` шаг `F`: `EXPECT_TESTS=11` и
  подпись «o0 + O-1..O-10» отстают от набора (`o0`..`o11` = 12). Потеря одного оракула шаг не
  покраснит. Лечится синхронизацией литерала с составом (11 → 12) — ровно тот же класс, что
  `TD-140` («спека M-61 §6 шаг F2 называет 25 мутантов при 26»). Правка `scripts/verify_*.sh`
  sacred, поэтому reviewer её не делает.
- **`N-2` (MINOR, зона engine-dev).** `crates/gateway-serve/src/lib.rs:1902` — текст `warn!`
  несёт литерал `GATEWAY_MAX_SUBSCRIPTIONS=16`, не подставляя `DEFAULT_MAX_SUBSCRIPTIONS`.
  Это ЧЕТВЁРТЫЙ экземпляр числа, тогда как doc-комментарий `:178-183` того же коммита
  утверждает: «Все три места, где раньше стоял литерал `16`, теперь читают ОДНУ константу».
  Утверждение неточно. Последствие ограничено, но реально: при смене подписи на 32
  `absent_var_yields_signed_default_16` покраснеет (эталон — литерал), а вот САМ ЛОГ будет
  говорить «=16», применяя 32, — и `warn_on_default` этого не поймает, потому что сверяет лог
  с `SIGNED_DEFAULT` = тем же `16`. Наблюдаемость, ради которой `A-015` выбрал дефолт вместо
  отказа старта, в этот момент врёт. Лечится `{}`-подстановкой константы в сообщение.
- **`N-3` (MINOR, зона engine-dev, ПРОЦЕССНАЯ).** Тело `d784e15` несёт
  `FA-WAIVER: crates/gateway-serve — нет docs/fa/gateway-serve.md`, и это утверждение о
  состоянии документа, сделанное без его открытия: крейт покрыт `docs/fa/viz-backend.md`
  (`check_review_fa.sh:195-200`, префикс `GS`). Названный там же «живой инвариант `GS-I-3`» в
  `docs/fa/viz-backend.md` НЕ СУЩЕСТВУЕТ — `grep -o 'GS-I-[0-9]*'` по файлу даёт РОВНО
  `GS-I-4`. Барьер это пропустил по построению: waiver он принимает по ФОРМЕ, истинность
  причины не проверяет (предел назван в `gates.md` §4). Класс — тот самый, ради которого
  созывался `A-015` (§6: «утверждение о состоянии документа без его открытия»), и это его
  пятое воспроизведение на M-65. Ущерба коду нет; правка — в следующем касании крейта.
- **`N-4` (MINOR, зона architect).** `verify_M-65.sh` шаг `L`, сценарий `absent`, не изолирует
  окружение: переменная не задаётся явно, но и не снимается (`env -u GATEWAY_MAX_SUBSCRIPTIONS`
  отсутствует). На runner'е CI она не экспортирована, поэтому сегодня сценарий проверяет то,
  что обещает; на машине, где она есть в окружении оболочки, шаг молча протестирует ДРУГОЙ
  сценарий и останется зелёным. Это setup-guard, которого нет (`testing.md` §«Целостность
  гейта», свойство 3: проба, молча тестирующая не тот сценарий, — плацебо самой себя).

## 7. Замечание о СОБСТВЕННОЙ ошибке этого круга (предъявляется, а не умалчивается) — **ЧАСТИЧНО ОТМЕНЁН, см. §10**

Первый мой прогон `check_protected_artifacts.sh` на дереве слияния дал **FAIL** на пяти
`milestones/M-*.md` («артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял»), и я едва не
внёс это в вердикт как блокер. Причина была в МОЁМ вызове: я подал
`PR_BASE_SHA=$(git rev-parse origin/main)` — ТЕКУЩУЮ вершину `main`, тогда как CI подаёт
`github.event.pull_request.base.sha`, то есть базу PR'а. Проверено на живом логе чужого job'а
(`actions/jobs/97272693042`): `PR_BASE_SHA: 10bc072…` — merge-base PR #32, не вершина `main`.
С правильной базой (`a9f0bf5`) барьер даёт `exit=0`.

То же случилось с `verify_design_claims.sh --merge-preview`: запущенный СКРИПТОМ ВЕТКИ, он
дал `FAIL [5-ФАЗЫ]` — потому что в старой редакции скрипт не знал про `docs/archive/` (норма
Р-2), куда `main` только что унёс `milestones/M-18-*.md`. Скрипт ДЕРЕВА СЛИЯНИЯ на том же
дереве даёт `VERDICT: PASS (0 нарушений)`.

Оба случая — буквально `testing.md`: «гейт, проверенный не тем вызовом, каким его зовёт прод,
не проверен». Записано здесь, потому что вердикт, скрывающий свои отменённые находки, учит
следующего повторить их молча.

---

## 8. Done Block — сырой вывод (агрегирован по `commit-discipline.md`: зелёное сжато, красное целиком)

Всё ниже снято на ДЕРЕВЕ СЛИЯНИЯ `c6a8d46` = `origin/main 4e1a881` + `87d9c87`
(`git merge-tree --write-tree` конфликтов не дал), кроме отдельно помеченного.

### 8.1 Предмет и дерево слияния

```
$ git rev-parse origin/main; git rev-parse origin/fix/M-65-battery-recalibration
4e1a8811d801f0bd50a1902486733931198a0211
87d9c8705e5ab167934689875e70ef288bfbc8b3

$ git merge-tree --write-tree origin/main origin/fix/M-65-battery-recalibration; echo exit=$?
96c7d46e0444f4da3635bedd2219eb6a67c80cf7
exit=0

$ git diff --stat a9f0bf5..87d9c87 | tail -1
 29 files changed, 8809 insertions(+), 24 deletions(-)

$ git diff --name-only a9f0bf5..87d9c87 -- crates/ | sed 's|/src/.*|/src/**|;s|/tests/.*|/tests/**|' | sort -u
crates/gateway-serve/Cargo.toml
crates/gateway-serve/src/**
crates/gateway-serve/tests/**
```

### 8.2 Базовая тройка CI — на дереве слияния

```
$ cargo fmt --all -- --check; echo exit=$?
exit=0

$ cargo clippy --all-targets --all-features -- -D warnings; echo exit=$?
exit=0

$ cargo test --all 2>&1 | grep -E "^test result" | awk '{p+=$4; f+=$6} END {print "passed="p" failed="f" (блоков: "NR")"}'
passed=855 failed=0 (блоков: 206)
$ echo exit=$?
exit=0
```

### 8.3 `scripts/verify_M-65.sh` — на дереве слияния

```
$ bash scripts/verify_M-65.sh 2>&1 | grep -E "^(PASS|FAIL|VERDICT)"; echo exit=$?
PASS  A crates/gateway-serve/tests/red_ws_session.rs на месте
PASS  A форматирование (паритет с CI-шагом fmt)
PASS  N манифест ⇄ §4.2 совпал в обе стороны; у каждой из восьми осей есть легитимный сценарий
PASS  F набор GREEN: исполнено 12 оракулов (ожидалось ≥ 11: o0 + O-1..O-10)
PASS  F2 BATTERY: PASS (13/13)
PASS  L невалидный лимит «0» ⇒ отказ старта (exit=2)
PASS  L невалидный лимит «-1» ⇒ отказ старта (exit=2)
PASS  L невалидный лимит «abc» ⇒ отказ старта (exit=2)
PASS  L лимит «absent» ⇒ старт СОСТОЯЛСЯ (процесс жив, A-015 §3 п.1)
PASS  L лимит «absent» ⇒ уход на норму НАЗВАН в логе старта
PASS  L лимит «empty» ⇒ старт СОСТОЯЛСЯ (процесс жив, A-015 §3 п.1)
PASS  L лимит «empty» ⇒ уход на норму НАЗВАН в логе старта
PASS  M соседние оракулы gateway-serve GREEN (10 тестов)
PASS  T clippy
PASS  T cargo test --all: passed=855
PASS  O12 оракул на находку ИСПОЛНЕН и зелён (тестов: 1)
VERDICT: PASS
exit=0
```

Шаги `L` «absent»/«empty» — те, которых до `135bc8d` не существовало (`A-015` F14: «поведение
спора гейтом не покрыто ВООБЩЕ, ни в одну сторону»). Теперь покрыто ИСПОЛНЕНИЕМ прод-бинаря.

### 8.4 Механические барьеры — в ПРОД-ФОРМЕ (`EVENT_NAME=pull_request`, `PR_BASE_SHA` = `base.sha` PR'а)

```
$ gh api repos/a3ka/hft-platform/pulls/29 --jq '.base.sha'
a9f0bf511a0a1de5433b0de412cb55234027b56a

$ EVENT_NAME=pull_request PR_BASE_SHA=a9f0bf51... bash scripts/check_protected_artifacts.sh; echo exit=$?
OK: защищённые артефакты целы на HEAD (a9f0bf5..HEAD; проверка по РЕЗУЛЬТАТУ, не по способу)
exit=0

$ ... bash scripts/check_docs_freeze.sh;   echo exit=$?    → exit=0
$ ... bash scripts/check_artifact_ids.sh;  echo exit=$?
OK: ни один коммит диапазона a9f0bf5..HEAD не ввёл второй носитель под занятым идентификатором
exit=0

$ ... bash scripts/check_gate_meta.sh; echo exit=$?
VERDICT: PASS — вердиктов проверено: 57, до-нормативных приземлений: 6, merge'ей с milestone в subject'е: 4
exit=0

$ ... bash scripts/check_review_fa.sh; echo exit=$?
research/reviews/R-093-M-65-impl-rev2.md: GS-I-4
research/reviews/R-097-handover-2026-08-21-recheck.md: GS-I-4
research/reviews/R-111-M-65-impl-rev3.md: GS-I-4
exit=0

$ bash scripts/verify_design_claims.sh   # скрипт ДЕРЕВА СЛИЯНИЯ, на нём же
VERDICT: PASS (0 нарушений)
exit=0
```

### 8.5 Мутационный контроль — прогнан мной (§2, §3)

```
# M1 — tracing::warn! в ветке дефолта удалён
$ cargo test -p gateway-serve --test red_max_subs_config 2>&1 | grep -E "^test |^test result"
test absent_var_yields_signed_default_16 ... ok
test empty_var_is_same_as_absent ... ok
test explicit_value_overrides_default ... ok
test invalid_values_refuse_start ... ok
test warn_on_default ... FAILED
test result: FAILED. 4 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out

# M2 — ветка None | Some("") ⇒ return Err(...)  (снятая арбитражем позиция Б)
$ cargo test -p gateway-serve --test red_max_subs_config 2>&1 | grep -E "^test |^test result"
test empty_var_is_same_as_absent ... FAILED
test warn_on_default ... FAILED
test explicit_value_overrides_default ... ok
test absent_var_yields_signed_default_16 ... FAILED
test invalid_values_refuse_start ... ok
test result: FAILED. 2 passed; 3 failed; 0 ignored; 0 measured; 0 filtered out

# M3a — DEFAULT_MAX_SUBSCRIPTIONS = 32, оракул с ЛИТЕРАЛОМ 16 (как в предмете)
$ cargo test -p gateway-serve --test red_ws_session o4 2>&1 | grep -E "^test |^test result"
test o4_subscription_cap_is_fail_closed ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 11 filtered out

# M3b — то же 32, но const MAX_SUBS = gateway_serve::server::DEFAULT_MAX_SUBSCRIPTIONS
#        (предложение engine-dev'а, отклонённое architect'ом шагом 5)
$ cargo test -p gateway-serve --test red_ws_session o4 2>&1 | grep -E "^test |^test result"
test o4_subscription_cap_is_fail_closed ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 11 filtered out

$ git status --porcelain      # после отката всех трёх мутаций
{пусто}
```

### 8.6 Замер состава набора (§5) и FA (Block-FA)

```
$ grep -cE 'fn o[0-9]+[a-z_0-9]*\(' crates/gateway-serve/tests/red_ws_session.rs
13
$ grep -n 'EXPECT_TESTS=' scripts/verify_M-65.sh
137:EXPECT_TESTS=11

$ grep -o 'GS-I-[0-9]*' docs/fa/viz-backend.md | sort -u
GS-I-4
$ sed -n '117,119p;195,200p' scripts/check_review_fa.sh
    recorder|derive)
      NOFA_CRA+=("${c}")
      ;;
    gateway-serve)
      FA_OF["${c}"]="docs/fa/viz-backend.md"
      PFX_OF["${c}"]="GS"
      LIVE_CRA+=("${c}")
      ;;
```

---

## 9. ВЕРДИКТ

**APPROVED.** Шаг 6 маршрута `A-015` §5 закрыт: исполнение решения арбитража сверено с
нормативом ПО СУЩЕСТВУ (§1), мутационный контроль перепрогнан независимо (§2), спорное
решение architect'а шага 5 принято и подкреплено собственным замером (§3), судьба `d5a1cf5`
решена и записана (§4), расхождение шага `F` снято замером (§5). Четыре NOTE (§6) merge не
блокируют и адресованы поимённо.

Условий снятия нет — блокеров нет. Дальше по маршруту: merge PR #29 → `gates.md` §8
(post-merge деплой-гейт, ОБЯЗАТЕЛЕН: диапазон трогает `crates/gateway-serve`, входящий в
прод-образ) → close-out с карточкой TD на `GATEWAY_WINDOW_MS` `.ok()` (`A-015` §5 шаг 6).

**Что этот вердикт НЕ закрывает.** Milestone M-65 в целом — предмет отдельного close-out'а
architect'а; здесь закрыт круг гейта по диапазону `a9f0bf5..87d9c87`. Вопрос founder'у из
`A-015` §6 (механизировать ли класс «утверждение о состоянии документа без его открытия»:
варианты (а)/(б)/(в)) остаётся ОТКРЫТЫМ и адресован не мне — `N-3` выше есть его пятое
воспроизведение и должно лечь в основание ответа.

---

## 10. ДОПОЛНЕНИЕ ПОСЛЕ MERGE'А (reviewer, 2026-08-23T23:50Z) — §7 был НЕВЕРЕН НАПОЛОВИНУ, и это дороже остального вердикта

**Вердикт §9 (APPROVED по предмету) в силе и не пересматривается.** Отменяется вывод §7 в
части `check_protected_artifacts` — и отменяется ФАКТОМ, а не рассуждением.

**Что произошло.** Merge PR #29 (`63c3866`) дал на `main`:

```
$ gh run view <CI> --json jobs
completed failure  Protected artifacts (gate trail)
completed failure  All checks passed
$ gh run view <Deploy> --json jobs
completed failure  Gate on CI (fail-closed)
completed skipped  Deploy (build on VPS)
```

То есть **ровно тот FAIL, который я нашёл локально и в §7 объявил своей ошибкой.**

**В чём я ошибся на самом деле.** Барьер зовут ДВЕ формы события, и они дают РАЗНЫЙ ответ на
одном дереве:

| форма | база | `bd357df` (archive move, несёт `ALLOW-ARTIFACT-DELETE`) в диапазоне? | исход |
|---|---|---|---|
| `pull_request` | `base.sha` = `a9f0bf5` | **ДА** — carve-out срабатывает | PASS |
| `push` | `PUSH_BEFORE` = `4e1a881` | **НЕТ** — он ПРЕДОК базы | **FAIL** |

Мой локальный прогон подавал `PR_BASE_SHA=$(git rev-parse origin/main)`, что совпадает с
`PUSH_BEFORE` push-формы. Значит он был **прод-формой для push'а** и предсказал красный `main`
верно. Увидев зелёный PR-прогон, я счёл расхождение своей ошибкой вызова и **снял истинную
находку, поверив более удобному из двух замеров.** Ошибка была не в вызове, а в выводе.

**Класс ошибки называю прямо, потому что он мой, а не чужой.** У гейта оказалось ДВЕ прод-формы;
я проверил обе, получил разные ответы — и вместо того, чтобы объявить само расхождение находкой,
выбрал ту, что не мешала merge'у. `testing.md` §«Целостность гейта» требует обратного: гейт,
дающий разные ответы на одном предмете, не проверен ни одной из форм, пока расхождение не
объяснено. Правильным действием было ЗАДЕРЖАТЬ merge и объяснить расхождение — что я и сделал,
но ПОСЛЕ, а не ДО.

**§7 остаётся в тексте неизменным** (кроме пометки в заголовке): вердикт, тихо переписавший
свою отменённую часть, — тот самый класс расхождения носителей, ради которого созывался
`A-015`. Вторая половина §7 (`verify_design_claims`, запущенный скриптом ВЕТКИ вместо скрипта
дерева слияния) остаётся верной — там ошибка вызова была настоящей.

**Следствия, а не сожаления:**

1. **`main` был КРАСЕН, прод НЕ обновлён** — `Deploy (build on VPS)` skipped. **СНЯТО
   2026-08-24T00:11Z:** merge close-out PR #73 (`670954f`, ветка форкнута ПОСЛЕ `bd357df`)
   дал зелёный push-прогон, `Deploy to VPS` **success**, и M-65 доехал до VPS этим push'ем.
   **`gates.md` §8 ПРОЙДЕН**, пруф (два замера, рост, а не снимок) — в `PROJECT-STATE.md`
   §M-65. Побочный вывод неприятный и записан в `TD-163`: красный `main` снялся СЛУЧАЙНЫМ
   следующим коммитом — гейт, который так «чинится», не значит ничего.
2. Заведена **`TD-163`** (MAJOR, зона architect) — механизм, охват (все три открытых PR
   форкнуты до `bd357df`), доказательство, что merge `origin/main` в ветку НЕ лечит, и
   требование RED-сценария в пробе барьера.
3. Дефект **не вызван содержанием M-65** и revert'ом M-65 не лечится: покраснеет следующая
   же ветка старше `bd357df`. Направление фикса — зона architect'а (`scripts/**` sacred,
   `gates.md` §4: reviewer описывает, не проектирует).

**Состояние прода на момент написания §10 (ssh, 23:48:13Z) — здоров, но это был ПРЕЖНИЙ образ**
(итоговое состояние после состоявшегося деплоя — `PROJECT-STATE.md` §M-65, замеры 00:11:05Z и 00:11:44Z):

```
hft-gateway-serve Up 4 hours (healthy)   hft-recorder Up 4 hours (healthy)
heartbeat: {"events":2142706,"next_seq":334872405,"segment_index":394,"writable":true,"ts_wall_ms":1787528890478}
RssAnon: recorder 22412 kB · gateway-serve 336 kB / 272 kB
journal: 73 298 106 098 B (было 73 233 176 221 B в 23:39:14Z — +64.9 MB за 9 мин)
load average: 0.17, 0.13, 0.10
```

`Up 4 hours` — прямое доказательство, что рестарта не было. Сбор данных не пострадал:
heartbeat свежий, `next_seq` и журнал растут, память в норме. **Прод не сломан — он просто не
получил эту работу.**
