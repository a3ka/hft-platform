# R-061 — перепроверка круга 5 M-61 (решение Б): APPROVED, 4 NOTE

**Предмет:** ветка `origin/docs/M-61-artifact-ids`, диапазон `3c11cd3..fcdf345` — четыре
коммита круга 5 (`4292ef9` спека · `31eabb4` RED-набор+генератор · `86ecbeb` verify G/S ·
`fcdf345` gates.md §12).
**Роль:** независимая Fable-перепроверка по `gates.md` §9, СВЕЖИЙ контекст · **Дата:** 2026-08-13
**Дерево:** свой worktree `/tmp/recheck-m61r5-*`, detached @ `fcdf345`; общий чекаут не тронут.
**Номер R-061 закреплён architect'ом** (заняты R-050…R-058, R-060); аллокатор не перезапускался;
занятость перепроверена перечислением: `grep -E '/(R-061|R-61)-'` по 700 путям объединения
69 ref'ов → пусто.

## ВЕРДИКТ: APPROVED

**Набор готов к диспетчеризации engine-dev'у.** По правилу круга 5 (`A-007` §6(1)-(3), §4.4
спеки): ложное ЗЕЛЁНОЕ на инварианте §4.1 исполнением НЕ предъявлено; ложных КРАСНЫХ на
замеренной форме прода нет. Четыре находки ниже — категория (iii), NOTE/TD, merge не блокируют;
Н-2 рекомендуется закрыть однострочной правкой спеки ДО диспетчеризации (дёшево сейчас, круг —
потом).

---

## (а) Утверждения о коде — проверены ИСПОЛНЕНИЕМ

### 1. Проба против сегодняшнего барьера: числа и имена совпали

```
$ bash scripts/tests/red_artifact_ids.sh            (worktree @ fcdf345)
…
PASS  МАНИФЕСТ ⇄ исполнение: 47 сценариев, состав совпал в обе стороны
PASS  СПЕКА⇄МАНИФЕСТ: 46 пар (ось,вид,значение) совпали в обе стороны
PASS  §4.3(2): у каждой оси есть легитимный сценарий
PASS  §4.3(4): у каждого из 4 мест сравнения §4.6 есть исполненный сценарий
VERDICT: FAIL (11)                                                     exit=1
```

Падения — РОВНО заявленные 11 сценариев предмета миграции, ни одним больше:
`B1DIR B2MULTI B2ORIG B2SLUGLESS B7CONT B7HDR B7REV L6FAM L6LETTERC L6LETTERM L6ONELETTER`
(сverено построчно по выводу). Остальные 36 зелены. RED-first состояние правильное: краснота —
та и только та, что обещана (барьер задачи 2 ещё не мигрирован — ожидаемо, не находка).

### 2. Четыре механические сверки — доказаны МУТАЦИЕЙ (6 мутаций, все покраснели куда надо)

| мутация | что сломано | какая сверка покраснела | сырой FAIL |
|---|---|---|---|
| A | из §4.2 оси 6 удалён атом `ведущие нули при букве` (SPEC=копия) | сверка 2 | `FAIL СПЕКА⇄МАНИФЕСТ: покрыто, НЕ объявлено` → `VERDICT: FAIL (12)` |
| B | из §4.2 удалена ВСЯ строка оси 7 | сверка 2 | то же, `FAIL (12)` |
| C | из пробы удалено исполнение `B6ZERO` (файл восстановлен) | сверки 1 И 4 | `FAIL МАНИФЕСТ: объявлены, НЕ исполнены: B6ZERO` + `FAIL §4.3(4): P2:B6ZERO-НЕ-ИСПОЛНЕН` → `FAIL (13)` |
| D | в §4.6 P4 `L5PRE`→`L5PREX` (несуществующий) | сверка 4 | `FAIL §4.3(4): P4:L5PREX-НЕТ-В-МАНИФЕСТЕ P4:НИ-ОДНОГО-СЦЕНАРИЯ` → `FAIL (12)` |
| E | в §4.2 оси 7 добавлен атом `фантомное значение` | сверка 2 (обратная сторона) | `FAIL СПЕКА⇄МАНИФЕСТ: объявлено в §4.2, НЕ покрыто: ось 7\|V\|фантомное значение` → `FAIL (12)` |
| F | в манифесте `L7QUOT\|7\|L`→`\|7\|V` (ось 7 без легитима; файл восстановлен) | сверка 3 (+2) | `FAIL §4.3(2): у осей 7 нет легитимного сценария — набор проходит «запретить всё»` → `FAIL (14)` |

Четвёртая сверка (места сравнения §4.6), добавленная этим кругом, НЕ декоративна: покраснела
двумя независимыми способами (C и D). Обе стороны сверки 2 живы (A/B и E). Worktree после C и F
восстановлен `git checkout --`, `git status --porcelain` чист.

### 3. Батарея

```
$ bash scripts/tests/red_artifact_ids.sh --battery
PASS  состав: §4.5 ⇄ генератор совпали, 25 мутантов
PASS  эталон → exit=0 VERDICT: PASS (47/47)
… (25 строк «уронил ровно объявленное») …
PASS  без барьера → exit=1, «SETUP НЕ СОСТОЯЛСЯ»
BATTERY: PASS (27/27)                                                  exit=0
```

27 = эталон + 25 мутантов + страж «нет барьера». Kill-set'ы сверены поимённо с таблицей §4.5 —
выборочно перепроверены глазами против спеки: `subjectreader → B7CONT B7HDR` ·
`revstrip → B7REV` · `letterexempt → B6LETTERDUP B6ZERO` ·
`showall → B5NOTANC B6ZERO L2CLOSEOUT L4MOD L5PRE` ·
`slugletter → 15 сценариев (B1A B1C B1DIR B1M B1R B3ORIG B4REN B5MID B5PR B5THIRD B6DASH
B6NOSLUG B7CONT B7HDR L6ONELETTER)` — совпадение посимвольное; страж равенства множеств
(объявленное = наблюдаемое) прочитан в коде и жив.

### 4. Три инверсии пиннят регресс

Против СЕГОДНЯШНЕГО барьера `B7HDR`, `B7CONT`, `B7REV` красны (строки «— ПРОШЛО» в прогоне
п.1): реализация, сохранившая чтение шапки / срезание `-rev`, набор НЕ пройдёт. Мутанты
`subjectreader` (роняет ровно B7CONT B7HDR) и `revstrip` (ровно B7REV) — красны в батарее.
Ключевое свойство круга предъявлено с обеих сторон.

### 5. Letter-aware ключ — независимый замер трёх форм хвоста на корпусе (208 `.md`-носителей)

```
строгий хвост  ([a-z])?(-.*)?$   : множественность 7 из 7 — C-18 C-24 C-58 M-46 R-1 R-35 R-38(×3)
dashoptional  -?([a-z])?(-.*)?$  : множественность 7 из 7 СОХРАНЕНА (fail-open НЕ даёт);
                                   но 'M-90-a' → id=M-90 letter=[a] — схлопывание с M-90a = ложное КРАСНОЕ
slugletter    -?([a-z])?.*$      : остаются 3 (C-24 R-35 R-38) — ПОТЕРЯНЫ 4 семьи: C-18, C-58, M-46, R-1
```

Подтверждаю замер приёмки, опровергший план миграции: fail-OPEN даёт именно СВОБОДНЫЙ хвост
(`slugletter`), и теряется в том числе флагманская `M-46`; `dashoptional` даёт только ложное
красное на `L6ONELETTER`. Обе формы ловятся: `B6DASH`+`slugletter`, `L6ONELETTER`+`dashoptional`
(батарея п.3). Развод одного мутанта плана на два — оправдан замером, воспроизведённым мной
независимо.

### 6. Шаги G и S `verify_M-61.sh` — обе половины исполнены

Половина 1 — против СЕГОДНЯШНЕГО барьера (шаги G+S извлечены в изолированный прогон без
правки verify):

```
PASS  S собственный диапазон проходит барьер (9 носителей введено)
PASS  S негативный контроль: второй носитель под идентификатором M-04-research-core.md заблокирован
FAIL  S негативный контроль TD: заводящая карточка TD-1 под номером, который держит
      НЕканоническая строка (прод-форма занятости), ПРОШЛА. Это fail-OPEN…
PASS  G §12 на месте, тело 8 строк (предел 8)
PASS  G §12 называет аллокатор поимённо
PASS  G §12 не обещает механизма чтения шапки (под Б его не существует)
FAIL  G SETUP НЕ СОСТОЯЛСЯ: из scripts/check_artifact_ids.sh не извлечён регексп идентификатора
VERDICT: FAIL (2 нарушений)                                            exit=1
```

Красно ПОИМЁННО по предмету миграции: TD-занятость fail-open + отсутствие `id_of` в коде.

Половина 2 — `check_artifact_ids.sh` временно замещён `ref-check.sh` (эталон Б из генератора),
затем восстановлен:

```
PASS  S собственный диапазон проходит барьер (9 носителей введено)
PASS  S негативный контроль: второй носитель под идентификатором M-04-research-core.md заблокирован
PASS  S негативный контроль TD: дубль карточки TD-1 заблокирован; номер держит НЕканоническая строка
PASS  G §12 на месте, тело 8 строк (предел 8)
PASS  G §12 называет аллокатор поимённо
PASS  G §12 не обещает механизма чтения шапки (под Б его не существует)
PASS  G форма §12 совпадает с кодом: 3 примера разобраны регекспом ^([MRCA])-([0-9]+)([a-z])?(-.*)?$;
      буква вплотную — часть ID, слаг из одной буквы — нет; перечислительная форма не разбиралась: M-60a/b/c
VERDICT: PASS                                                          exit=0
```

Шаг G требует ОТСУТСТВИЯ обещания шапки (G3, грепом `Предмет:|Контекст`) и сверяет форму
идентификатора с регекспом, извлечённым ИЗ КОДА (G4, с двумя стражами невакуумности:
`M-60a` разбирается / `README` нет; дубль строится вторым НОСИТЕЛЕМ, не расхождением
предметов) — оба требования мандата подтверждены и чтением, и исполнением. Синтетический
дубль в S — «второй `.md` под тем же ID» + отдельный TD-контроль на неканонической занятости.

### 7. §12 `gates.md`

Тело — РОВНО 8 строк (замер шага G: «тело 8 строк (предел 8)»); аллокатор назван поимённо
(`scripts/next_artifact_id.sh`); обещания читать шапку нет (`Предмет:`/`Контекст` в §12
отсутствуют — G3 PASS; слово «предмете» без двоеточия — не форма шапки). Коммит `fcdf345`
трогает только `.claude/rules/gates.md`.

## (б) Полномочия

```
$ EVENT_NAME=push PUSH_BEFORE=3c11cd3 bash scripts/check_docs_freeze.sh        → exit=0
$ EVENT_NAME=push PUSH_BEFORE=3c11cd3 bash scripts/check_protected_artifacts.sh
OK: защищённые артефакты целы на HEAD (3c11cd3..HEAD; …)                       → exit=0
$ git log -1 fcdf345 --format='%B' | grep -c 'FOUNDER-APPROVED'                → 1
```

Единственный коммит диапазона, трогающий замок §11 (`fcdf345`, `.claude/rules/gates.md`),
несёт `FOUNDER-APPROVED` в СВОЁМ теле — проверка покоммитная, диапазон из четырёх коммитов
просмотрен пофайлово (`git log --name-only`): спека / RED+генератор / verify / §12 — всё в
Allowed paths architect'а (§2 спеки), sacred-зоны engine-dev не тронуты. Граница C не
переступлена: перекрытие `A-007` §6(5) решением founder'а от 2026-08-12 ЗАПИСАНО в §10.1 спеки
явной строкой («Решение founder'а … ПЕРЕКРЫВАЕТ пункт арбитра `A-007` §6(5)»), с обоснованием
и таблицей судьбы остальных пунктов §5(1)-(5)/§6(1)-(4) — требование `A-007`-контекста
исполнено; сам выбор (буква вместо аудит-токена) — воля владельца по `gates.md` §0.1, что в
§10.1 названо дословно.

## (в) Связность

- **§10.1** — явная строка перекрытия присутствует, формулировка корректна (см. (б)).
- **Числа спеки против фактов, снятых мной заново:** ref'ов 69 ✓; носителей 209 = 208 `.md` +
  1 `.json` (`research/reports/R-001-obi-trackA.json`) ✓; уникальных letter-aware ID — 200 ✓;
  множественность letter-aware — 7 поимённо (`C-18 C-24 C-58 M-46 R-1 R-35 R-38×3`) ✓,
  letter-blind — 9 (те же + `M-38×3`, `M-60×4`) ✓; `TECH-DEBT.md` (origin/main): 124
  строки-записи ✓, занято любой строкой 120 ✓, прозаических 6 (`30 52 67 71 79 95`) ✓;
  сценариев 47 ✓ (сверка 1); мутантов 25 ✓ (состав §4.5⇄генератор); kill-set'ы пиннят 39 из 47,
  вне kill-set'ов ровно заявленные 8 (`B5BASE B5NOBASE L1X L2TXT L3OK L4DEL L5FIX L7QUOT`) ✓.
- **Висячих ссылок нет:** `C-069`/`C-070`/`C-071`, `A-006` (`A-006-m61-round-three.md`),
  `A-007`, `R-046`/`R-052`/`R-054`, `docs/plans/M-61-migration-to-decision-B.md` — все на ветке.
- **Нумерация секций спеки** идёт §7 → §10 (без §8/§9); внутренних ссылок на §8/§9 самой
  спеки нет (все «§9»/«§11» в тексте — про `gates.md`) — дыра нумерации безвредна, см. Н-4.
- **Механизмов, которых нет, спека не обещает** — с одним пограничным случаем (Н-2) и одним
  умолчанием (Н-3), оба ниже.

## Адверсарное расширение (`A-008` §3) — что дало

Исполнено против ЭТАЛОНА Б (`ref-check.sh` из генератора), фикстуры — одноразовые репозитории:

```
ADV-1  M-77ab-alpha.md в базе, M-77ab-beta.md в диапазоне (двухбуквенный суффикс) → exit=0  ← ПРОШЛО
ADV-1к M-77a-alpha.md / M-77a-beta.md (однобуквенный контроль)                    → exit=1  ← блок
ADV-2  M-78B-alpha.md / M-78B-beta.md (ВЕРХНИЙ регистр буквы)                     → exit=0  ← ПРОШЛО
ADV-3  R-057-M-65-ws-session.md в базе, R-057-second.md в диапазоне               → exit=1  ← блок (верно:
       имя с двумя ID занимает R-57; второй ID — упоминание в слаге)
ADV-4  занятость `- **TD-500** `долг-один` — …, см. TD-501 ниже.`; диапазон вводит
       карточку `- **TD-501** `новый`»                                            → exit=0  ← пропущено
       (эталон судит по ЖИРНОМУ ПОЛЮ, хвост строки номер не занимает)
```

ADV-1/ADV-2 → находка Н-3; ADV-4 → находка Н-2; ADV-3 — поведение верное, находки нет.
Проверены также кандидаты «ведущие нули без буквы» (закрыто `0*`/`10#`, пиннится `B6ZERO`),
«класс в чужом каталоге» (`B1DIR`/`L1REPORT` + ADV-3), неканонические формы `TECH-DEBT.md`
(`B2SLUGLESS`/`B2ORIG`/`B2MULTI` + S-контроль TD) — дыр не нашёл.

## Находки — все категории (iii) по §4.4, merge не блокируют

**Н-1 (NOTE, текст).** Шапка пробы (`scripts/tests/red_artifact_ids.sh`, блок «ПРАВИЛО
ОСТАНОВКИ (§4.4)») и хвост вердикта («опровержение обязано называть ОСЬ и ЗНАЧЕНИЕ (§4.4)»)
декларируют ПРЕЖНЕЕ правило остановки, которое §4.4 спеки этим же кругом переписал по
`A-007` §6 — и прямо назвал неисполнимым. Комментарий, не механизм; но проба — первое, что
читает engine-dev. Привести две строки к новому §4.4.

**Н-2 (NOTE, однострочная правка спеки рекомендована ДО диспетчеризации).** §3.1(в):
«номер занимает любая строка-запись — и занимает ВСЕ номера TD-NNN, упомянутые В ЭТОЙ СТРОКЕ»
противоречит комментарию пробы («ВСЕ номера ВЕДУЩЕГО ЖИРНОГО ПОЛЯ строки… Шире брать нельзя»)
и поведению эталона (ADV-4: TD-номер в хвосте строки-записи занятости не даёт). Замер обоих
прочтений на `origin/main:TECH-DEBT.md`: сегодня они дают ОДИНАКОВЫЕ 120 номеров (разница —
пустое множество), поэтому по §4.4(2) это НЕ блокер (расхождение недостижимо на снятой форме
прода). Но §3.1(в) — норматив для engine-dev, и оба прочтения проходят все 47 сценариев и
батарею: выбор направления останется за реализацией — ровно класс «пробел в спеке», из
которого выросли пять кругов. Правка: в §3.1(в) заменить «упомянутые В ЭТОЙ строке» на
«упомянутые в ВЕДУЩЕМ ЖИРНОМ ПОЛЕ строки» (форма, уже закреплённая `B2MULTI` и комментарием
пробы).

**Н-3 (NOTE/TD, адверсарное расширение).** Имя в каталоге-носителе, НЕ разбираемое регекспом
(`M-77ab` — двухбуквенный суффикс; `M-78B` — верхний регистр), МОЛЧА выпадает и из
`universe()`, и из `introduced()`: два файла под таким именем проходят зелёными (ADV-1/ADV-2,
exit=0 при контроле exit=1). По грамматике §4.1 такое имя идентификатором не является, поэтому
нарушение ИНВАРИАНТА формально не предъявлено — категория (iii), новое значение оси 6
(«нераспарсиваемое имя в каталоге-носителе»). Но направление падения (тихий игнор) нигде не
названо: §7 гарантирует «третьего варианта — молчания — нет» для классов `D/H/S`, а здесь
молчание ЕСТЬ; шаг G о классе знает (комментарий «пример `M-60ab` — код такого НЕ разбирает»),
набор — нет (`nameskip` пиннит только slugless-форму). Требуется либо строка в §7
(«нераспарсиваемое имя в каталоге-носителе игнорируется — названное ограничение»), либо
сценарий, пиннящий направление. Решение — architect'а.

**Н-4 (примечание).** Статус задачи 6 в §Tasks — «🚧 ПЕРЕОТКРЫТА (текст §12 заменяется
целиком)», при том что замена уже исполнена коммитом `fcdf345`. Оправдано тем, что оракул
задачи (шаг G) зеленеет целиком только после миграции барьера (задача 2), но при close-out
строку привести к факту. Туда же: нумерация секций спеки §7→§10 (пропуск §8/§9 после переписи)
— безвредно, ссылок нет.

Мелочь без номера: `id_of` в §3.1(б) несёт `0*` в регекспе, эталон — `10#`-нормализацию без
`0*`; поведенчески эквивалентно, запиннено `B6ZERO` — расхождением не считаю.

## Done Block

```
$ git log -1 --format='%h %s' (worktree, detached)
fcdf345 docs(rules): §12 — идентификатор уникален, продолжение берёт свой номер [architect]

$ bash scripts/tests/red_artifact_ids.sh | tail -1; echo exit=$?
VERDICT: FAIL (11)          exit=1     ← RED ровно на 11 заявленных сценариях (поимённо сверено)

$ bash scripts/tests/red_artifact_ids.sh --battery | tail -1; echo exit=$?
BATTERY: PASS (27/27)       exit=0     ← 25 мутантов, kill-set'ы = §4.5 посимвольно

$ мутации сверок: A→FAIL(12) B→FAIL(12) C→FAIL(13) D→FAIL(12) E→FAIL(12) F→FAIL(14)
  (все шесть — красны ИМЕННО той сверкой, на которую нацелены; worktree восстановлен)

$ G+S против сегодняшнего барьера → VERDICT: FAIL (2) exit=1 (S-TD fail-open; G4 setup умер)
$ G+S против эталона Б            → VERDICT: PASS     exit=0

$ EVENT_NAME=push PUSH_BEFORE=3c11cd3 bash scripts/check_docs_freeze.sh;           exit=0
$ EVENT_NAME=push PUSH_BEFORE=3c11cd3 bash scripts/check_protected_artifacts.sh;   exit=0

$ адверсарное: ADV-1 exit=0 / контроль exit=1 · ADV-2 exit=0 · ADV-3 exit=1 · ADV-4 exit=0

cargo не запускался (мандат: предмет markdown+shell); паритет с CI — вне предмета круга.
```

**Набор готов к диспетчеризации engine-dev'у** (задача 2 — миграция
`scripts/check_artifact_ids.sh` + `next_artifact_id.sh` под эталон Б, строго по RED).
Рекомендация до диспетчеризации: Н-2 (одна строка §3.1(в)), по желанию Н-1 (две строки шапки
пробы) — обе правки architect'а, нового круга по §4.4(3) не требуют.
