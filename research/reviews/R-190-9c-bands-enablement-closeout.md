<!-- GATE-META
milestone: M-70
audited_repo: a3ka/hft-platform
audited_base: 46cd25c4a1a82dc1984465989b5f147a06d0703d
audited_head: 4ddd392e14e1dbd4c0b2511e50ddc9e48b8707e0
verdict: APPROVE
-->

# R-190 — close-out включения полос глубины (`ROADMAP` 9-C, `П-014` п.4)

**Роль:** reviewer · **Дата:** 2026-09-20T20:45Z · **Предмет:** НЕ диф кода, а СОСТОЯНИЕ
ПРОДА: включение канонического набора `GATEWAY_BANDS` операторской строкой и фиксация
close-out'а в `PROJECT-STATE.md` / `TECH-DEBT.md` / `ROADMAP` (колонка «Состояние»).

**ВЕРДИКТ: APPROVED — включение состоялось, работает, цена названа.** Все числа мандата
founder'а перепроверены мной на живом проде; одно НЕ воспроизвелось буквально и названо ниже
(§2). Сверх проверки найдено три вещи, ни одна из которых не блокирует, и все три заведены
долгом, а не упомянуты вслух (§4).

---

## 0. Почему вердикт вообще пишется, если кода нет

Кода нет, но есть (а) смена ПОВЕДЕНИЯ прода по границе C, (б) запись в два reviewer-owned
файла и (в) утверждение «работает», которое кто-то будет цитировать. `gates.md` §4: этап
пройден, когда предъявлен ФАЙЛ. Вердикт в переписке аудит-трейлом не является.

**Риск-блок (`gates.md` §5) не применим:** `crates/**` не тронут (диф этой ветки — три
документных файла плюс этот вердикт), `crates/{risk,killswitch,oms,venue-*}` не существуют.
**Block-C (`05-contract-layer.md` §4) не применим:** `crates/contracts/**` не тронут.
**Замок §11** не задет: `.claude/**`, `CLAUDE.md`, `docs/04-workflow.md` вне дифа.

## 1. Предъявление FA (`gates.md` §4, M-66)

Диф `crates/**` не трогает ⇒ барьер `check_review_fa.sh` даст `SKIP`, и требование здесь
КОГНИТИВНОЕ. Исполняю его по существу, а не формально: предмет — выдача `gateway`, живой
инвариант **`VB-I-5`** (`docs/fa/viz-backend.md:203`, форма v10): «метка достоверности
принадлежит ТОЧКЕ, а не ряду; отсутствие метки у deep-точки → серия невалидна».

**Предъявлен ПРОДОМ, а не тестом:** во всех 14 рядах прод-кадра `series_provenance` имеет
длину 60 при 60 точках `series` (сторож `DB-I-4c` — равенство длин — держится на живых
данных), а словарь меток совпадает с таблицей `crates/gateway/src/lib.rs:2453-2485`.

## 2. Проверка утверждений мандата — прод, не отчёт

| утверждение founder'а | мой замер | исход |
|---|---|---|
| `GATEWAY_BANDS=0.015,0.03,0.05,0.08,0.15,0.3,0.6` | `docker inspect hft-gateway-serve` — ровно эта строка | ✅ |
| выдача даёт 14 строк `depth_series` | `wsprobe` в прод-контейнере: 14 рядов, полосы `1.5/3/5/8/15/30/60 %` × 2 стороны | ✅ |
| кадр 290 212 Б при пределе 2 000 000 | **289 896 Б** (20:19Z) и **277 149 Б** (20:35Z); предел 2 000 000 подтверждён в env | ⚠ **не воспроизведено буквально** — кадр плавает с составом окна; вывод (0.14× предела) тот же |
| старт тёплый, `ckpt_dir_present=true` | лог двух подключений: `events_decoded=8678` (19:44Z) и `25282` (20:19Z), оба `ckpt_dir_present=true` | ✅ |
| прогрев слепка заранее, простоя не было | `covered_through_seq.warm7` и `ckpt-*.bin` с mtime 19:43, новый слепок 20:15 | ✅ |
| `CHECKPOINT_BANDS` прописан в cron | `/etc/cron.d/hft-journal-retention:37` = тот же набор; `gateway-checkpoint-cron.sh:92` → `--bands "${CHECKPOINT_BANDS}"`, расписание `*/15` под `flock -n` | ✅ |

**Расхождение по кадру — не придирка.** Число 290 212 попало бы в `PROJECT-STATE` как «факт
прода», а кадр меняется от снятия к снятию на ±4 %. В записи стоят ОБА моих замера и
отношение к пределу, а не одно «ровное» число (`П-029`: выдуманная точность хуже честного
диапазона).

## 3. Состояние мира (`reading-map.md` ярус S) — проверено ДО выводов

```text
$ gh run list --branch main --limit 2
completed success Deploy to VPS  35532872608   2026-09-20T19:36:49Z
completed success CI (PR #205)   35532286938   2026-09-20T19:26:04Z

$ ssh … 'cd /root/hft-platform && git rev-parse --short HEAD'   → 46cd25c
$ git log --name-only 46cd25c..origin/main                      → 13 файлов, ВСЕ docs/research
$ bash scripts/check_branch_health.sh → VERDICT: PASS (веток 11, замечаний 0)
```

**Прод НЕ отстал.** `deploy.yml` `paths:` включает только `crates/**`, `Cargo.*`, `Dockerfile`,
`docker-compose.yml`, сам `deploy.yml` — docs-коммиты деплой не триггерят по конструкции,
поэтому `46cd25c` и есть последняя КОДОВАЯ ревизия. Это проверено списком файлов, а не
предположено по зелёному деплою.

**Liveness + содержательные пункты `gates.md` §8:** контейнеры `(healthy)`; heartbeat
`ts_wall_ms 1789935606046` против `date -u +%s 1789935613` — 7 с; `next_seq` 637 921 588 →
637 949 635 (журнал растёт); `RssAnon` gateway 13 324 kB, recorder 24 828 kB (`TD-021`: не
`docker stats`); отказов `exceeds limit` за 24 ч — **0**; диск 64 %.

**Сторож отложенного решения жив:** дефолт репозитория не тронут —
`env -u GATEWAY_BANDS docker compose config` → `GATEWAY_BANDS: "0.001"`, то есть
`verify_M-70.sh` task #7 продолжает судить дефолт, а включение живёт там, где ему положено —
в host `.env` (`П-014` п.4: «включение = строка оператора»).

## 4. Находки — три, все заведены долгом

**`F-1` (MINOR, `TD-215`) — 61 % веса депт-серии составляет повторяющийся литерал метки.**
Разбор прод-кадра: `depth_series` 58 047 Б = `series` 21 554 + `series_provenance` 35 534.
`П-027` считала депт-серию «≈25 КБ, то есть 0.05 % кадра»; факт — **2.3× больше и 21 %
кадра**. Вывод `П-027` («включать») этим не отменяется — кадр в 0.14× предела; отменяется
ЧИСЛО, на которое обопрутся в ёмкостном расчёте `DESIGN` §16.

**`F-2` (MINOR, `TD-216`) — полоса 60 % не наблюдена НИ РАЗУ.** Оба ряда `0.6` дают
`not-observed band=0.600000 reach=0.5932…/0.5972…` — 120 точек из 120 на двух снятиях с
интервалом 15 минут. Метка честна (`VB-I-5` работает), лжи на проводе нет; долг — в
расхождении FA с продом: `docs/fa/viz-backend.md:186` требует follow-up-замера 30–60 % «ДО
включения этих полос», а `research/data-quality/depth-verdict.md:22,218` этот замер уже
засчитывает (`M-33`). Зона правки — architect.

**`F-3` (MINOR, `TD-197` уточнён) — предпосылка карточки отпала сегодня.** Она стояла с
оговоркой «пока `GATEWAY_HEATMAP_WINDOW=0.001` равен `GATEWAY_BANDS=0.001`, долг
ненаблюдаем». Полосы ушли до `0.6`, окно карты осталось `0.001`: потребитель видит в одном
кадре карту и полосы в РАЗНЫХ охватах и не может различить это по кадру. Абзац «Когда болит»
переписан по факту.

**Принятая цена, а не находка:** словарь метки не различает «наблюдено полно внутри
REST-охвата» и «накоплено диффами» — до 1 мин 45 с недостоверной метки после ресинка, вне
ресинка 0 из 1153 точек (замер 9-B). Founder принял окно включением; починка — `TD-214`,
делается вместе со следующим бампом формы (цена бампа измерена: ≈23 мин недоступности).

## 5. Что я НЕ проверял — названо, чтобы не сошло за проверенное

1. **Суточной динамики охвата нет.** `not-observed` на `0.6` показан на 15 минутах.
2. **Ресинк в окне моих замеров не происходил** — окно `TD-214` я наблюдал по замеру 9-B
   (чужой прогон на прод-сегменте), а не своим.
3. **Клиента-кокпита не существует** — «доставка работает» означает, что кадр собирается и
   уходит зонду, а не что его кто-то рисует. Сторож ОТСУТСТВИЯ доставки по-прежнему не
   построен (`ROADMAP` 9-A-bis).
4. **`wsprobe --secret` против прод-секрета по-прежнему не работает** (`TD-207`,
   воспроизведено мной сегодня); гейт исполнен обходом — JWT, подписанный сырыми байтами.

## 6. Done Block

```text
$ ssh … 'docker exec hft-gateway-serve wsprobe --token <свой JWT> --frames 3 --seconds 8 --out /tmp/rev9c'; cat summary.json
{"schema_version":10,"cursor_upto_seq":637946870,"history_truncated":true,
 "latency_first_snapshot_ms":1781,"frames_received":3,
 "series_lengths":{"ohlcv":43,"depth_series":14,"heatmap":654,"cob":54,"volume_bubbles":144,…}}

$ wc -c snapshot.json        → 289896   (20:19Z)      277149   (20:35Z)
$ python3 — разбор полей второго кадра:
  volume_profile 137137 · heatmap 67795 · depth_series 58047 · bubbles 5284 · ohlcv 5115 · cob 1007
  depth_series: series 21554 Б · series_provenance 35534 Б (61.2 %)
  14/14 рядов: len(series)==len(series_provenance)==60
  bid → liveness=confirmed · ask → liveness=unconfirmed · 0.6 → not-observed (обе стороны, 120/120)

$ env -u GATEWAY_BANDS GATEWAY_JWT_SECRET=verify-dummy docker compose config | grep GATEWAY_BANDS
      GATEWAY_BANDS: "0.001"

$ bash scripts/check_branch_health.sh | tail -1
VERDICT: PASS — наблюдение состоялось
```

**Прогон `scripts/verify_M-70.sh` на дереве этой ветки — 41 `PASS` / 1 `FAIL`, exit=1.**
Печатаю красное целиком, а не прячу за агрегатом:

```text
── task #7 — доставка состава РАБОТАЕТ, а прод-дефолт УЗКИЙ (сторож отложенного включения)
PASS: task #7 доставка под оракулом DB-I-7, ОТРЕНДЕРЕННЫЙ прод-дефолт ровно 0.001,
      ручка переопределяема

── task #6 — bump GATEWAY_SCHEMA_VERSION: смена ФОРМЫ выдачи объявлена
FAIL: task #6 версия схемы НЕ поднята (база 10, HEAD 10): смена формы выдачи обязана
      объявляться бампом (VB-I-4)
VERDICT: FAIL (1)   exit=1
```

**Этот `FAIL` — не регресс и не находка по предмету.** Шаг `task #6` сравнивает
`GATEWAY_SCHEMA_VERSION` БАЗЫ и HEAD: он имел смысл, пока `M-70` был веткой против
пред-бампового `main`. После merge'а (`a54c7d5`, 18.09) база и HEAD несут одну и ту же
версию 10, и шаг обязан краснеть при любом состоянии дерева. Ключевой для сегодняшнего
предмета шаг — **`task #7` — `PASS`**: рендер даёт дефолт ровно `0.001`, ручка переопределяема,
то есть сторож отложенного включения жив и после включения на проде.

**`NOTE` для architect'а (не блокирует, в его зоне):** гейт закрытого milestone'а, оставшийся
в `scripts/`, — ловушка: любой, кто его запустит, получит красное и потратит время на
несуществующий дефект. `verify_M-86.sh` при close-out'е уехал в `docs/archive/` (`R-189` §5);
`verify_M-70.sh` этого шага не прошёл, потому что `M-70` не закрыт. Разумный порядок — либо
архивировать гейт вместе с close-out'ом `M-70`, либо сделать `task #6` самоотключающимся,
когда база уже несёт бампнутую версию.

## 7. Что записано (зона reviewer'а)

- `PROJECT-STATE.md` — новый раздел «9-C …» с замером; в разделах `M-70` и «`П-014` круг 2»
  проставлены ДАТИРОВАННЫЕ указатели (историю не переписывал — `PROJECT-STATE` летопись).
- `TECH-DEBT.md` — заведены `TD-214`, `TD-215`, `TD-216`; уточнены `TD-197` (предпосылка
  отпала) и `TD-207` (повторное воспроизведение). Отдельной записью назван недосчёт в
  заголовке «В ПЛАНЕ»: тела `TD-210`…`TD-216` лежат за границей, по которой считает команда.
- `docs/ROADMAP.md` — ТОЛЬКО колонка «Состояние» строк 9-B и 9-C (перечень и порядок —
  зона architect'а).
