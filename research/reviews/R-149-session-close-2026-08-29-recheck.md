<!-- GATE-META
milestone: P-023
audited_repo: a3ka/hft-platform
audited_base: d1221b1ca932d0b8e95403c2849308ed6e7b9ce2
audited_head: 956889d5a5f8a25a8da55a57fd4784a53232613c
verdict: NOTE
-->

# R-148 — перепроверка §9: close-out сессии 2026-08-29 (`956889d`)

> Поле `milestone` шапки — `P-023` (ведущая запись решения в этом коммите): барьер
> `check_gate_meta.sh:352` принимает только `КЛАСС-НОМЕР`, имя ветки в нём физически не
> проходит (проверено прогоном прод-формой); мандат предписывал имя ветки — расхождение
> решено в пользу механизма, предмет назван здесь: ветка `docs/session-close-2026-08-29`,
> один коммит `956889d`.

Независимый Fable-агент со свежим контекстом, НЕ автор правки. Предмет — один коммит
`956889d` на `docs/session-close-2026-08-29` (база `origin/main` = `d1221b1`), четыре файла:
`docs/PENDING-SIGNATURE.md` (+90, П-023/024/025) ·
`docs/workflow/oracle-blindness-class-2026-08-28.md` (+65/−8, правило Р-3 + М-3) ·
`docs/SESSION-HANDOFF.md` §0ter (+4/−1) · `docs/workflow/session-handover-2026-08-31.md` (+198).

**Вердикт: NOTE.** Существо правки воспроизведено и верно — включая самые дорогие
утверждения (офсайт-копия сверена мною независимо побайтово, прод замерен по ssh, счёт
П-024 сходится точно на ревизии, которую судил `C-178`). Полномочия чисты. Но в тексте
остались ЧЕТЫРЕ расхождения с фактом — три числа и один заголовок, — и все четыре лежат в
классе «число/утверждение, противоречащее соседнему или команде», который этот же коммит
объявляет закрытым («числа документа приведены к факту тем же коммитом»). Условия снятия —
одна короткая правка на ветке; нового круга не требуется, форма не тронута.

---

## Б — находки (каждая — с воспроизведением и условием снятия)

### Б-1. Заголовок §5 `oracle-blindness` противоречит собственному телу: «два правила» при «Правил стало ТРИ»

- Где: `docs/workflow/oracle-blindness-class-2026-08-28.md:109`.
- Воспроизведение (на `956889d`):
  ```
  $ grep -n '^## 5\.' docs/workflow/oracle-blindness-class-2026-08-28.md
  109:## 5. Что менять в конструкции набора — два правила, каждое проверено вопросом «что оно РАЗРЕШАЕТ»
  $ sed -n '111p' docs/workflow/oracle-blindness-class-2026-08-28.md
  Правил стало ТРИ. Первые два внесены 28.08 разложением §2: промахи распределены по
  ```
- Ожидалось: тело коммита `956889d` заявляет «Числа документа приведены к факту тем же
  коммитом: „Два кандидата“ → три, „оба правила“ → все три». Получено: §6 и хвост §8
  приведены, заголовок §5 — нет. Документ, который УЧИТ конструкции оракулов, сам несёт
  внутреннее противоречие числа — ровно класс, названный в мандате («числа, противоречащие
  соседним; корпус ловили многократно»).
- Условие снятия: заголовок §5 приведён к «три правила» (или к форме без числа).

### Б-2. `session-handover-2026-08-31.md` §0: «веток в origin 9 + main» — не сходится с командой ни в одном прочтении

- Где: `docs/workflow/session-handover-2026-08-31.md`, блок «0. Состояние мира — замер …,
  не память».
- Воспроизведение:
  ```
  $ git ls-remote --heads origin | wc -l
  9        # ВКЛЮЧАЯ main и ВКЛЮЧАЯ docs/session-close-2026-08-29 (ветку самого предмета)
  ```
  То есть сейчас — **8 + main**; на момент написания передачи (до push ветки предмета) —
  **7 + main**. «9 + main» (=10) не получается ни при каком счёте; вероятная механика —
  `wc -l` посчитал main и был записан как «9 + main». Арифметика приписки «было 11; снесены
  две, заведены две» (11−2+2=11) с итогом «9» также не сходится.
- Ожидалось: в разделе, озаглавленном «замер, не память», число воспроизводится командой.
  Получено: не воспроизводится.
- Условие снятия: строка приведена к факту с названной конвенцией счёта (например
  «8 веток + main, включая ветку этого close-out»), приписка «было …» согласована.

### Б-3. Замер git-личностей: объяснено 199 коммитов из 200 — пропущен `scout <318307@gmail.com>`

- Где: `docs/workflow/session-handover-2026-08-31.md` §4, карточка reviewer'у №1
  («78 engine-dev, 68 scout, 14 scout@test.local, 1 founder, и лишь 38 Alex K» — сумма 199).
- Воспроизведение:
  ```
  $ git log -200 --format='%an <%ae>' origin/main | sort | uniq -c | sort -rn
    78 engine-dev <t@t.local>
    68 scout <t@t.local>
    38 Alex K <96266972+a3ka@users.noreply.github.com>
    14 scout <scout@test.local>
     1 scout <318307@gmail.com>
     1 founder <t@t.local>
  ```
- Ожидалось: 200 из 200. Получено: одна личность (`scout <318307@gmail.com>` — как раз
  та, где в локальной конфигурации всплыл личный e-mail владельца) в карточке долга
  отсутствует, хотя для reviewer'а она едва ли не информативнее прочих.
- Условие снятия: строка дополнена шестой личностью (сумма = 200).

### Б-4. `oracle-blindness`: маркер `FACTS` не сдвинут при +65 строках фактов, собранных 2026-08-29

- Где: `docs/workflow/oracle-blindness-class-2026-08-28.md:1`.
- Воспроизведение:
  ```
  $ head -1 docs/workflow/oracle-blindness-class-2026-08-28.md
  <!-- FACTS: audited_head=c4cfb8564fb5549060762c7056485065557afee0 collected=2026-08-28 -->
  $ git diff --stat d1221b1..956889d -- docs/workflow/oracle-blindness-class-2026-08-28.md
   ... | 73 +++++++-
  ```
  `c4cfb85` существует и предок `956889d` (`git cat-file -e` → 0; `merge-base --is-ancestor`
  → 0) — `H-FACTS-SHA` формально чист. Но треть фактуры документа (Р-3, М-3: «PASS=36
  FAIL=0», круги на `feat/harness-milestone-shape`) собрана 2026-08-29 над ДРУГОЙ ревизией,
  а голова файла датирует весь сбор 2026-08-28/`c4cfb85`.
- Норма: `docs/workflow/reading-map.md` §Ярус S — «документ, называющий себя фактурой,
  несёт ревизию сбора»; смысл маркера — датировать, чтобы дрейф был ОБЪЯВЛЕН. Смягчение
  признаю́: сами секции Р-3 датированы в тексте («внесено 2026-08-29»).
- Условие снятия: маркер сдвинут на ревизию/дату этого дополнения, ЛИБО в голове названо,
  что Р-3/М-3 собраны 2026-08-29 (одна строка).

## Н — наблюдения (не находки; условий не несут)

1. **П-024 не несёт секции «что этим НЕ решено»** — П-023 и П-025 несут. Нормы, требующей
   секцию, нет; решение samodostatoчно (правило остановки). Фиксирую как асимметрию формы.
2. **Не воспроизведены мною** (датированные разовые замеры; правдоподобие подтверждено
   косвенно, ложность не показана): «канал 66 МБ/с», «BUILD_SECONDS=84.01», «43.85 ГБ кэша»
   (сейчас `docker buildx du` → `Total: 2.599GB` — кэш растёт заново, прошлый снесён),
   «32 минуты» копии.
3. **«каталог `journal-cold`» (П-023) изнутри субаккаунта не виден**: субаккаунт зачручен в
   свою base directory, копия лежит в `journal/` внутри неё; `ls journal-cold` с субаккаунта
   даёт «No such file». Формулировка не ложна (это имя base directory), но читатель ранбука
   повторит мой первый промах. На усмотрение автора.
4. Хвост передачи §5 «C-101 … (все на `feat/harness-milestone-shape`)» — верен буквально
   (файл достижим на ветке через `docs/archive/abandoned-gate-verdicts-2026-08-24/`,
   унаследованный от `main`), хотя живёт C-101 на `feat/harness-doc-integrity`, как сам же
   §3 и говорит.

## П — что проверено ИСПОЛНЕНИЕМ и сошлось

| # | утверждение правки | команда (сокр.) | результат |
|---|---|---|---|
| 1 | verify на дереве слияния | `bash scripts/verify_design_claims.sh --merge-preview origin/main` | `VERDICT: PASS (0 нарушений)` exit=0 |
| 2 | verify на ветке (расхождение прогонов?) | тот же без `--merge-preview` | `VERDICT: PASS` exit=0 — расхождения НЕТ |
| 3 | копия: 465 файлов / 82 017 629 323 Б, сверка без расхождений | с прода: `ssh -p23 -i ~/.ssh/storagebox u659392-sub1@… 'ls journal \| wc -l; du -sb journal'` | `465` · `82017629323` — **побайтово** |
| 4 | ёмкость коробки 5.0 ТБ, занято 77 ГБ | `df -h .` на коробке | `5.0T 77G … 2%` |
| 5 | диск прода 62 % | `ssh прод 'df -h /'` | `150G 90G 55G 62% /` |
| 6 | heartbeat: рост `free_bytes`, writable | `cat recorder.heartbeat` | `free_bytes: 58 648 432 640`, `writable: true`, `segment_index: 465` |
| 7 | прод = `eb1c20a`; отставание 7 файлов, кода нет | `git rev-parse` на VPS; `git diff --name-only eb1c20a..origin/main` (+2 фильтра) | `eb1c20a` · `7` · `0` · `0` |
| 8 | ретеншен в `dry-run` | `grep -i mode /etc/cron.d/hft-journal-retention` | `RETENTION_MODE=dry-run` |
| 9 | `check_milestone_shape.sh` в `main` отсутствует | `git cat-file -e origin/main:scripts/check_milestone_shape.sh` | `does not exist`, exit=128 |
| 10 | спас-реф `feat-M-10-rebased` несёт тот же SHA | `git for-each-ref \| grep M-10` | локальная ветка = salvage = `51c21dc` |
| 11 | `EXPECT_P` 7→8, `P7` приземлён | `git show origin/main:scripts/verify_M-71.sh`; то же на вершине `feat/M-72-…` + грep теста | `main`: `EXPECT_P=7`; ветка: `EXPECT_P=8`, `fn pl_i_5_p7_cap_terminal_flag…` есть |
| 12 | счёт П-024: 781 строка / 422 исполняемых | `git show dee67f1~2:<оба файла> \| wc -l` и грep не-комментариев | `781` · `422` — точно, на ревизии, которую судил `C-178` |
| 13 | «сценарии 95, батарея 42/42» | `bash scripts/tests/red_milestone_shape.sh` на `dee67f1` | `PASS=95 FAIL=0 (сценариев: 95)` · «поймано 42 из 42» · exit=0 |
| 14 | SHA в §0ter (`dee67f1`, `ad77df8`) | `cat-file -e` + `merge-base --is-ancestor` к текущим вершинам | оба существуют, оба предки — датированные замеры верны (ветки уехали в тот же день; врезка §0ter это оговаривает) |
| 15 | «открытых PR 0» | `gh pr list --state open` | пусто, exit=0 |
| 16 | `C-114` и цитата README архива | `git cat-file -e` + `git show … \| grep C-114` | файл есть; «ОДИН \`ESCALATE\` не получил арбитра — \`C-114\`» — дословно |
| 17 | вердикты кругов на ветке барьера | `git ls-tree -r dcab8fb9 research/critiques/` | `C-173`/`C-175`/`C-176`/`C-177`/`C-178` есть |
| 18 | «в `oracle-blindness` нет литеральной ссылки на скрипт барьера» | `grep -c check_milestone_shape <файл>` | `0` — подтверждено |
| 19 | `deploy/README.md` предписывает CIFS при выключенном SMB | `grep -n -i cifs deploy/README.md` | `:35`/`:50` — CIFS/fstab; долг reviewer'у назван верно |
| 20 | нумерация П плотная, аллокатор класс `П` не знает | `grep -n '^## П-'` · `sed -n '14,16p' scripts/next_artifact_id.sh` | …П-022→023→024→025 без дыр и дублей; допустимые классы `TD R C A M` — ручной номер оправдан |

## Полномочия (пункт б мандата)

- **Зона:** все четыре файла — `docs/**`, зона architect'а; `PROJECT-STATE.md`/`TECH-DEBT.md`
  (reviewer) и `crates/**/src` не тронуты (`git show --numstat` — ровно 4 пути).
- **Замок §11:** `.claude/**`, `CLAUDE.md`, `docs/04-workflow.md` не тронуты — токен
  `FOUNDER-APPROVED` не требуется, и его отсутствие корректно.
- **Граница C:** П-023/024/025 оформлены как ЗАПИСИ решений founder'а («РЕШЕНИЕ FOUNDER'А
  2026-08-29», «записано architect'ом»); «Рекомендация architect'а — (ii)» в П-023 помечена
  рекомендацией, выбор отнесён в отдельный милестоун со своим кругом. Разделы «что НЕ
  решено/НЕ закрывается» в П-023/П-025 присутствуют и телу не противоречат (П-024 — Н-1).
  Существо решений founder'а мною НЕ судится — вне предмета §9.
- Личность коммита `founder <t@t.local>` с меткой `[architect]` — сам предмет называет это
  долгом reviewer'у (карточка 1 §4), не скрывает.

## Done Block

```
$ git worktree add --detach /tmp/hft-recheck-close 956889d            # exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main    # VERDICT: PASS (0 нарушений), exit=0
$ bash scripts/verify_design_claims.sh                                # VERDICT: PASS, exit=0
$ ssh прод … 'ssh -p23 … u659392-sub1@… "ls journal | wc -l; du -sb journal"'
465
82017629323	journal                                                  # exit=0
$ bash scripts/tests/red_milestone_shape.sh   (на dee67f1)
PASS=95 FAIL=0 (сценариев: 95) · батарея ослаблений: поймано 42 из 42 · exit=0
$ git ls-remote origin refs/heads/docs/session-close-2026-08-29
956889d5a5f8a25a8da55a57fd4784a53232613c                              # вершина = audited_head
$ bash scripts/next_artifact_id.sh R
R-148
```
Красных прогонов не было; всё зелёное агрегировано выше (таблица П), расхождения вынесены
в Б-1…Б-4.

## Условие снятия NOTE

Один фикс-коммит на ветке `docs/session-close-2026-08-29` ДО merge: Б-1 (заголовок §5),
Б-2 (счёт веток + приписка «было 11»), Б-3 (шестая личность, сумма 200), Б-4 (маркер FACTS
либо строка о дате сбора Р-3). Все четыре — изложение, не форма: нового круга критика по
`gates.md` §9 не требуется; после фикса ветка вливается своим PR.
