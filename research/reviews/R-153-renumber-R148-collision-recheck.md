<!-- GATE-META
milestone: R-149
audited_repo: a3ka/hft-platform
audited_base: 37a6358adbb92879c081aea8db33e6c4bb910cbb
audited_head: 0b705be6bff258293ba9425c8adcbfe33b357744
verdict: APPROVE
-->

# R-153 — перепроверка §9: разводка коллизии `R-148` → `R-149` + фикс ложного замера в передаче

Независимый Fable-агент со свежим контекстом. Предмет — ветка `docs/renumber-R148-collision`,
два коммита (`9a511f6`, `0b705be`) над `37a6358` (текущий `origin/main`).

**Вердикт: APPROVE.** Разводка исполнена чисто и подтверждена барьерами прод-формой; сторона
расхождения выбрана ВЕРНО (прямой ответ ниже); одно точностное примечание к ОБОСНОВАНИЮ
выбора и одно — переносное из предмета A.

## (а) Утверждения о коде — исполнением на дереве слияния

1. **Один носитель `R-148` в `origin/main`, ноль на ветке:**
   `git ls-tree origin/main -- research/reviews/ | grep -c R-148` → **1**
   (`R-148-session-close-2026-08-29-recheck.md`); в дереве `0b705be` — только
   `R-149-session-close-…`; чужой `R-148-M-68-merge-resolution-round6.md` — на
   `origin/feat/M-68-rev4`, не тронут. После merge ветки в `main` останется ровно один
   носитель на репозиторий — чужой.
2. **`R-149` свободен и взят резервом:** перебор ВСЕХ рефов (`refs/remotes/origin` ∪
   `refs/heads`, свежий клон с `--prune`) — единственный носитель `R-149` = переименованный
   файл этой ветки; `git ls-remote origin 'refs/reserved/R-14*'` → **`refs/reserved/R-149`
   существует**. Резерв взят до приземления, как заявлено.
3. **Четыре ссылки переведены:** дифф `9a511f6` — `docs/PENDING-SIGNATURE.md` две строки
   (`:1673` `Н-3` в `П-023`, `:1734` `Н-1` в `П-024`), `oracle-blindness-class-2026-08-28.md:10`
   (`Б-4`), `session-handover-2026-08-31.md:188` (`Б-3`). Греп по `origin/main` вне двух
   файлов-носителей даёт РОВНО эти четыре вхождения — перевод полон, пятой ссылки нет.
4. **Таймлайн красного `main` подтверждён:** PR #118 влит `16:08:31Z` → ран `main` **failure**
   `16:08:34Z`; следующий merge (#119) `16:13:19Z` — **success** («сменил диапазон —
   позеленело», нарушение стало невидимым). `artifact-ids` прод-формой на диапазоне предмета:
   `EVENT_NAME=push PUSH_BEFORE=37a6358… bash scripts/check_artifact_ids.sh` → exit=0,
   «ни один коммит… не ввёл второй носитель» — коллизия разведена.
5. **Фикс передачи (`0b705be`) сверен с `TD-181`:** «непокрыт ОДИН барьер
   (`check_protected_artifacts.sh`), форма `pull_request`» — совпадает с карточкой в `main`
   и с моим мутационным воспроизведением (`R-152`, Done Block). Четыре карточки
   `TD-181`…`TD-184` в `main` есть (PR #119).

## (б) Полномочия

- **`ALLOW-ARTIFACT-DELETE` НУЖЕН и ЕСТЬ.** Барьер ловит переименование:
  `check_protected_artifacts.sh:318` — `git log --full-history --diff-filter=DR -M` (флаг `R`
  включён). Токен в теле `9a511f6` с причиной ≥12 символов. Прод-форма на диапазоне:
  `EVENT_NAME=push PUSH_BEFORE=37a6358… bash scripts/check_protected_artifacts.sh` → exit=0,
  `NOTE … переехал в R-149-… (остался под защитой)` — барьер увидел переезд и признал его.
- `check_gate_meta.sh` прод-формой на диапазоне → exit=0 (`вердиктов проверено: 0`).
- Замок §11 не тронут (диффы: PENDING, два `docs/workflow/*`, rename в `research/reviews/`);
  `FOUNDER-APPROVED` не требуется. Граница C: обе правки PENDING — замена идентификатора в
  ссылках, ни одно решение не изменено.
- `bash scripts/verify_design_claims.sh --merge-preview origin/main` → exit=0.

## (в) Связность

Ссылок на `R-148` в дереве ветки не осталось, кроме двух вхождений ВНУТРИ переименованного
файла (`:9` заголовок, `:179`) — объявленное исключение, прецедент `C-101`/`767ebae`
подтверждён (тело коммита несёт след). Новый текст карточек в передаче фактам `TD-181` не
противоречит. Конфликта с параллельной веткой предмета A по PENDING нет — ханки не
пересекаются.

## Прямой ответ на отдельный вопрос: сторона выбрана ВЕРНО. Блокера нет. Но обоснование «чужой занял номер РАНЬШЕ» git-записью НЕ подтверждается

Единственная проверяемая запись — коммиты создания носителей:

```
$ git log --diff-filter=A --format='%h %ci' origin/main -- research/reviews/R-148-session-close-2026-08-29-recheck.md
7ad870b 2026-08-29 15:54:42   ← ТВОЙ — раньше
$ git log --diff-filter=A --format='%h %ci' origin/feat/M-68-rev4 -- research/reviews/R-148-M-68-merge-resolution-round6.md
a8b12b0 2026-08-29 16:08:23   ← чужой — на 14 минут позже
```

Момент АЛЛОКАЦИИ номера git не фиксирует; по фиксируемому — порядок обратный заявленному.
«Кто раньше» здесь недоказуемо в обе стороны, и строить выбор на нём нельзя.

**Устойчивое основание — другое, и его достаточно; выбор от поправки не меняется:**

1. Чужой носитель — вердикт ДРУГОЙ РОЛИ на живой гейт-ветке (`feat/M-68-rev4`).
   Перенумеровать его значит переписать чужую рабочую ветку (`branch-hygiene` п.10) и
   сломать цепочку ссылок reviewer'а на его же вердикт.
2. Свой носитель — под собственным контролем, все четыре ссылки — в своих документах;
   стоимость расхождения минимальна и вся своя.
3. Прецедент `767ebae`: перенумеровывает ВЛАДЕЛЕЦ сталкивающегося артефакта, резервом.

Рекомендация (не условие): в следующей своей правке заменить основание «занял раньше» на
пункты 1–2 — временнáя посылка в теле коммита неисправима, след живёт здесь.

## Примечание (переносное из A)

`session-handover-2026-08-31.md` после `0b705be` печатает `red_gate_meta … FAIL (5)` — то же
число, что `R-152` П-1 не воспроизвёл (`TD-181` и мой мутант дают `FAIL (1)`). Править
вместе с П-1 предмета A, одним движением.

## Done Block

```
$ git ls-tree origin/main -- research/reviews/ | grep -c 'R-148'                     → 1
$ git ls-tree 0b705be -- research/reviews/ | grep -E 'R-14[89]'                      → только R-149-…
$ git ls-remote origin 'refs/reserved/R-14*' | grep R-149                            → есть
$ EVENT_NAME=push PUSH_BEFORE=$(git rev-parse 37a6358) bash scripts/check_protected_artifacts.sh; echo exit=$?
NOTE …R-148-… переехал в …R-149-… (остался под защитой)
OK: защищённые артефакты целы на HEAD    exit=0
$ EVENT_NAME=push PUSH_BEFORE=$(git rev-parse 37a6358) bash scripts/check_artifact_ids.sh; echo exit=$?
OK: ни один коммит диапазона… не ввёл второй носитель…    exit=0
$ EVENT_NAME=push PUSH_BEFORE=$(git rev-parse 37a6358) bash scripts/check_gate_meta.sh; echo exit=$?
VERDICT: PASS — вердиктов проверено: 0…    exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main >/dev/null 2>&1; echo exit=$?
exit=0
$ gh pr view 118 --json mergedAt → 16:08:31Z; gh run list --branch main: failure 16:08:34Z → success 16:13:19Z
```
