<!-- GATE-META
milestone: PR-59
audited_repo: a3ka/hft-platform
audited_base: 177b7ec8e50af1f6ec14a602172a1f35c2fb6efc
audited_head: febaba4d3c5bc778511b29276998c8a3155054d9
verdict: APPROVE
-->

# R-106 — перепроверка §9, круг 3: PR #59, исправление по `R-105` (норма Р-2 «архив гейтов»)

**Роль:** независимый Fable-клон architect'а со свежим контекстом (`gates.md` §9), круг 3.
**Предмет:** ветка `docs/r2-archive-norm`, вершина `febaba4` (диф круга — только
`docs/04-workflow.md`, +26/−13), база `origin/main` = `177b7ec` (сверено `git ls-remote`);
merge-base = базе ⇒ дерево ветки = дереву слияния (Done Block).

**Вердикт: APPROVED.** Все три пункта условия `R-105` закрыты — каждый перепроверен моим
замером, не сверкой с текстом вердикта. Нового воспроизводимого дефекта нет. Один NOTE,
merge не держит. Третий круг сходится чисто ⇒ триггер §0 п.2 (три круга по предмету)
арбитра НЕ требует: он существует для тупика, а тупика нет — причины кругов 1 и 2 разные
(зона/тег против пересчёта/сужения), закрытие оказалось конечным, как `R-105` и предсказал.

---

## (а) Перечень отложенных — ЗАКРЫТО; пересчёт мой, по всем 30 кандидатам

`R-105` считал восьмёрку; я взял ПОЛНЫЙ список ВЫНОСИТЬ из `R-098` §3.2 (30 номеров,
включая `M-38a`/`M-60a`) и прогнал каждый — паттерн `verify_M-<id>` без `.sh`, ловящий и
запись `M-17` без расширения:

```
$ for m in 04 07 17 18 20 22 23 24 29 30 32 33 34 35 37 38a 40 41 45 47 48 49 50 51 53 58 59 60a 61 62; do
    hits=$(grep -rl "verify_M-$m" crates/ deploy/ 2>/dev/null | wc -l); [ $hits -gt 0 ] && echo "M-$m: $hits"; done
M-17: 1  M-22: 1  M-35: 2  M-45: 3  M-48: 3  M-49: 3  M-50: 1  M-62: 3   — остальные 22: ноль
```

Ссылки несут РОВНО восемь — никакой девятый кандидат не всплыл. Раскладка по зонам,
пофайлово:

| M | файлы | зона |
|---|---|---|
| M-17 | `crates/research-cli/tests/red_footprint_bins.rs` | только `*/tests/**` |
| M-22 | `crates/gateway/tests/red_gateway_readonly.rs` | только `*/tests/**` |
| M-50 | `crates/journal/tests/red_floor_scan_prodscale.rs` | только `*/tests/**` |
| M-62 | `crates/gateway/tests/red_{catalog_equivalence,hint_pos_guard,segment_meta_bound}.rs` | только `*/tests/**` |
| M-35 | `crates/recorder/src/main.rs`, `crates/venue-binance/src/lib.rs` | `src` |
| M-45 | `venue-binance{,-futures}/src/lib.rs` + 1 тест | `src` (+ тест) |
| M-48 | `deploy/bin/{gateway-checkpoint,journal-retention}-cron.sh`, `deploy/cron.d/journal-retention` | `deploy` |
| M-49 | `crates/journal/src/segments.rs` + 2 теста | `src` (+ тесты) |

**Число автора верно: первый проход 26 из 30, отложены четыре — `M-35 M-45 M-48 M-49`.**
Оба вхождения «26 из 30» в тексте (`docs/04-workflow.md:118,129`) согласованы между собой
и с телом коммита; вилки «по списку одно, по критерию другое», уронившей круг 2, больше
нет. Пересчёт в теле `febaba4` заявлен авторским — числа совпали с моим независимым, что и
требовалось.

## (б) Фраза о местоположении — ЗАКРЫТО

«`M-17 M-22 M-50 M-62` — ссылки живут ТОЛЬКО в `*/tests/**`» — точно (таблица выше,
ни одного файла вне тестов). «`M-35 M-45 M-48 M-49` — ссылки есть в `crates/*/src/**` …
и `deploy/**`» — верно как групповое утверждение (src: M-35/45/49; deploy: M-48) и не
утверждает «у каждого обе зоны». Ложного «лежат в src и deploy» про тестовую четвёрку —
дефекта круга 2 — в тексте не осталось. То, что у `M-45`/`M-49` есть ЕЩЁ и тестовые
ссылки, классификацию не меняет: достаточно одной ссылки в чужой зоне, чтобы коммит не
собрался одной ролью, — критерий применён верно.

## Критерий применён последовательно — сверка со `scope-guard.md` и профилями

- `*/tests/**` — зона architect'а: `scope-guard.md` §SACRED («`*/tests/` (везде) —
  RED-спеки architect'а»), профиль §Writes («`*/tests/` … sacred, только architect»).
  «Он правит их сам, тем же коммитом» — в зоне. ✅
- `crates/*/src/**` — architect'у закрыт: профиль §NEVER («Impl-код внутри
  `crates/*/src/`»), таблица владения («ЛЮБОЙ impl-код»). ✅
- `deploy/**` — зона engine-dev (таблица владения, строка engine-dev). ✅

Круг 2 ловил класс «право отнимается у роли, которая им владеет» — в новой редакции обе
половины восьмёрки стоят по своим владельцам.

## (в) `COGNITIVE-ONLY` сужен до триггера; размен ЧЕСТЕН — `П-017` A2 открыт мной

Текст (`docs/04-workflow.md:53-66`): тег остаётся в заголовке, покрытие расписано по
частям — триггер когнитивен (`R-098` §3.1: три ревизии критерия, три ответа — проверено
по файлу), требования 2-3 механизируемы, механизм назван кандидатом дословно, инцидент
класса — `TD-138` (жив, `TECH-DEBT.md:85`, MAJOR).

**Обязателен ли кандидат ПРЯМО СЕЙЧАС — нет, и вот почему по первоисточнику.** `П-017` A2
дословно: «Согласен заморозить … а заморозку на новый наложим» ⇒ заморозка накрывает
НОВЫЙ харнесс. Кандидат в названной форме — стоячий инвариант репозитория («для каждого
`docs/archive/verify_M-NN.sh` не должно существовать упоминания … вне `docs/archive/**`»)
— это НОВЫЙ check-скрипт, не правка существующего: `check_gate_meta.sh` устроен как
проверка коммит-диапазона, стоячий скан в него не вешается без переделки. Форма при этом
выбрана ПРАВИЛЬНО против класса `TD-138`: диапазонная проверка коммита переезда (то, что
`R-105` Н-2 называл «правкой существующего») ловит только ссылки, ОСТАВЛЕННЫЕ при выносе,
и слепа к ссылкам, добавленным ПОЗЖЕ, — а `TD-138` ровно про второе. Стоячий инвариант
сильнее и честнее — и он заморожен A2. Момент «тем же кругом, что и первый вынос»
когерентен: первый проход — отдельная founder-санкционированная работа (Р-2 подписано),
уже несущая правку харнесса (белый список `id_of`, требование 1) — барьер встаёт туда,
где ему впервые есть что охранять. Дизъюнкция условия `R-105` («назван кандидатом честно
ЛИБО построен») выполнена первой ветвью, и ветвь выбрана обоснованно.

**Форма дома для этого паттерна существует и совпадает:** `branch-hygiene.md`
§Worktree-lifecycle п.3 — тег + «кандидат на настоящую механизацию назван и не
изображается сделанным». Новая секция ей следует.

## Нового обязывающего вне тега — НЕТ

Все три ханка дифа (`git diff d14f3f7..febaba4` открыт мной целиком) живут внутри секции
Close-out, чей заголовок несёт `` (`COGNITIVE-ONLY`) ``. «Правит их сам, тем же коммитом»
и «строить тем же кругом» — предусловия выноса и план внутри тегированной секции, не новые
обязанности третьих ролей вне её.

## Полномочия, связность — чисто (каждая строка — команда этого захода)

```
$ git ls-remote origin main → 177b7ec…; git merge-base origin/main HEAD → 177b7ec… (= база)
$ git show febaba4 --format='%B' | grep FOUNDER-APPROVED → «сокращение харнесса — 45 гейтов…» (≥12 симв.)
$ EVENT_NAME=pull_request PR_BASE_SHA=177b7ec… bash scripts/check_docs_freeze.sh → exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -2 → VERDICT: PASS (0 нарушений); exit=0
$ bash scripts/check_context_budgets.sh | tail -1 → PASS, 7 файлов, запас 2957 B; 04-workflow в скрипте не назван
$ sed -n '39p;78p' deploy/bin/gateway-checkpoint-cron.sh → обе строки про verify_M-48, дословно
$ sed -n '19p' scripts/check_context_budgets.sh → называет verify_M-60b.sh (требование 3 фактурно)
$ ls docs/archive/verify_*.sh | wc -l → 1 («сегодня в docs/archive/ один гейт» — верно)
$ номер: аллокатор next_artifact_id.sh не вернулся за 90 s (тот же симптом, что в R-105);
  универсум §12 воспроизведён вручную: ls-tree по refs/remotes/origin ∪ refs/heads,
  max = R-105 ⇒ R-106
```

Граница C не задета (ни составов данных, ни промоушенов, ни фаз). `П-017` A3 — носитель
вне впрыскиваемого ядра, бюджет не тронут по построению. Противоречий со `scope-guard.md`
нет; висячих ссылок нет — `R-105` (на ветке), `R-098` §3/§4, `TD-138`, `П-017` открыты.

FA-требование (`gates.md` §4): диф `crates/**` не трогает ⇒ `check_review_fa.sh` — SKIP по
построению; живой инвариант кода не требуется, что называю явно, а не молчанием.

## NOTE (не держит merge): скоуп кандидата придётся сузить при постройке

Фраза «до ПЕРВОГО переезда охранять ему нечего» верна для нормы (её переездов ещё не
было), но кандидат-инвариант, применённый БУКВАЛЬНО сегодня, сработал бы на единственном
архивном гейте: `verify_M-60` упомянут вне архива — `docs/plans/M-60-reconciliation-2026-08-13.md:59,163,173`,
`docs/SESSION-HANDOFF.md:27` (исторические планы и индекс веток, НЕ класс `TD-138`: никто
из них не объявляет мёртвый гейт живым сторожем). Плюс субстрочная ловушка: наивный греп
`verify_M-60` ловит и `verify_M-60b` — по `gates.md` §12 это РАЗНЫЕ идентификаторы.
Следствие для круга постройки: скоуп — несущие зоны (`crates/`, `deploy/`, `scripts/`,
`docs/fa/`), не историческая проза; сопоставление — по точному id с буквой. Это
build-time дизайн механизма, явно объявленного кандидатом, — потому NOTE, не находка
против текста нормы. Фикс не проектирую сверх сказанного.

## Итог

Условие `R-105` закрыто целиком: (а) 26/4 подтверждено независимым пересчётом по всем 30,
(б) местоположение ссылок соответствует замеру, (в) тег сужен до триггера, кандидат назван
честно и его отложение покрыто `П-017` A2, открытым мной лично. Ядро конструкции — роли,
токены, замер 45/6183/0-в-CI, требования 1-3 — перепроверено кругами 1-2 и не менялось.
**PR #59 к вливанию готов; арбитр §0 не требуется.**
