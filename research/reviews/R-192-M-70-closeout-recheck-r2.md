<!-- GATE-META
milestone: M-70
audited_repo: a3ka/hft-platform
audited_base: d179886b263963ea5c446b3d1b57f74c316346a5
audited_head: 8285ab9b4a0b378135fc3f3f3e6ff7d56cfaf21a
verdict: NOTE
-->

# R-192 — перепроверка `gates.md` §9, круг 2: close-out `M-70` по пути B (`docs/closeout-m70` @ `b5d3559`)

**Роль:** architect-клон, Fable, свежий контекст; тот же, что вынес `R-191` REJECT · **Дата:**
2026-09-20T22:40Z · **Предмет:** четыре коммита поверх `origin/main` = `d179886`: `3b24fca`
(мой `R-191`), `59e88a9` (переезд отменён, `SKIP` в `task #6`, §0closeout переписан),
`8bb3315` (`SKIP` сужен до «диапазон пуст ЛИБО не трогает `${LIB}`»), `b5d3559` (`V_HEAD`
поднят из развилки; регресс `task #6b` починен).

**ВЕРДИКТ: NOTE — блокеров нет; две поправки факта (стухшие `файл:строка` в спеке), два
замечания о пределах `SKIP`, одна опечатка.** Блокер круга 1 (`R-191` Б-1) снят ПО СУЩЕСТВУ:
гейт и спека на месте, требование 2 Close-out'а не задето, нового нарушения нормы не появилось.
`SKIP` ложного `PASS` не создаёт — воспроизведено МОЕЙ мутационной батареей из семи сценариев
(§3), а не таблицей автора. Сторож `task #7` жив и кусает на флипе дефолта (§5). Все
утверждения автора о коде подтвердились командой на дереве слияния, кроме двух номеров строк,
сместившихся от его же правки гейта (Б-1, Б-2 этого круга).

**Процедурное (`gates.md` §0):** вердикт НЕ REJECT ⇒ арбитр не созывается. Для ясности
маршрута: причина круга 1 (ссылки чужой зоны на вынесенный гейт) в этом круге ОТСУТСТВУЕТ —
гейт не вынесен. Ни одна находка ниже не повторяет причину `R-191`.

---

## 1. Дерево слияния, Ярус S, барьеры

```text
$ git rev-parse origin/main origin/docs/closeout-m70; git merge-base origin/main origin/docs/closeout-m70
d179886b263963ea5c446b3d1b57f74c316346a5
b5d355960c7a2a74e8821bed8fba2f897677225b
d179886b263963ea5c446b3d1b57f74c316346a5        # FF ⇒ дерево слияния == дерево ветки
$ git diff -M --stat origin/main HEAD
 docs/ROADMAP.md                                 |   2 +-
 milestones/M-70-depth-bands-enablement.md       | 161 +++++
 research/reviews/R-191-M-70-closeout-recheck.md | 285 +++++++++
 scripts/verify_M-70.sh                          |  36 ++-
$ git diff -M --word-diff=plain origin/main HEAD -- docs/ROADMAP.md | grep -oE '\[-[^]]*-\]|\{\+[^}]*\+\}'
{+`milestones/M-70-depth-bands-enablement.md` + `scripts/verify_M-70.sh` — **переезд в архив ОТЛОЖЕН** (`R-191` Б-1): … `04-workflow.md:138-151` … Ветка+}
{+влита и удалена+}                # только вставки в колонку «Где»; «Состояние» не тронута
```

Ярус S: `gh run list --branch main --limit 3` — три `success` (последний CI `#206` 20:57Z);
`check_branch_health.sh` → `VERDICT: PASS`. Барьеры CI в форме вызова CI
(`EVENT_NAME=pull_request PR_BASE_SHA=d179886…`), диапазон `d179886..b5d3559`:

```text
check_roadmap_sync         VERDICT: PASS                                                  exit=0
check_protected_artifacts  OK: защищённые артефакты целы на HEAD                          exit=0
check_docs_freeze                                                                         exit=0
check_artifact_ids         OK: … не ввёл второй носитель под занятым идентификатором     exit=0
check_gate_meta            VERDICT: PASS — вердиктов проверено: 1 (R-191)                 exit=0
check_review_fa            SKIP (диапазон не трогает crates/**)                           exit=0
check_context_budgets      VERDICT: PASS — 7 файлов, 112960 B из 114900 B (запас 1940 B)  exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -1
VERDICT: PASS (0 нарушений)                                                               exit=0
```

## 2. Снят ли Б-1 по существу (вопрос 1 мандата)

- **Требование 2 Close-out'а не задето:** `scripts/verify_M-70.sh` и `milestones/M-70-…md`
  на месте (`git diff --stat` выше — переименований нет); обе ссылки чужой зоны
  (`crates/gateway/src/lib.rs:111`, `docker-compose.yml:138`) указывают на существующий файл.
- **Нового нарушения от «ЗАКРЫТ, но живёт в `milestones/`» — нет.** Норма
  (`04-workflow.md:70-79`) описывает переезд как шаг close-out'а, но её же требование 2
  ОТКЛАДЫВАЕТ переезд ровно для этого случая; прецедент — `M-35 M-45 M-48 M-49`: все четыре
  закрыты, все четыре в `scripts/` и `milestones/` (`ls scripts/verify_M-{35,45,48,49}.sh` —
  на месте). Барьеров, выводящих «открыт» из расположения в `milestones/`, нет:
  `grep -l 'milestones/' scripts/check_*.sh` → `check_disk_budget`, `check_context_budgets`,
  `check_protected_artifacts`, `check_gate_meta`, `check_roadmap_sync` — ни один не судит
  закрытость; `check_roadmap_sync` триггерится на ПЕРЕЕЗД, которого теперь нет, и всё равно
  PASS. Reviewer закрытость делегировал явно — `PROJECT-STATE.md:2608` «сам milestone
  закрывает architect» (открыто мной: `sed -n 2608p | grep -o '…закрывает architect)'`).
- **Колонка «Где» строки 9** — верна по факту и по цитате: `04-workflow.md:138-151` —
  требование 2 (`:138` начало, `:152` — уже требование 3); владелец снятия назван
  (engine-dev). Н-1 круга 1 снялся сам: `PROJECT-STATE.md:2681` и `ROADMAP.md:99` («сторож
  осмыслен») ИСТИННЫ, пока гейт в `scripts/`.

## 3. Не создал ли `SKIP` ложного `PASS` (вопрос 2 — главный)

Проверял САМ, в изолированном worktree (`/tmp/hft-recheck-m70r2-mut`, detached `b5d3559`),
исполняя РЕАЛЬНЫЕ строки шагов `#6`+`#6b` (`sed -n '274,337p'` из `b5d3559:scripts/verify_M-70.sh`)
поверх `FAIL=0 SKIPPED=0 LIB=crates/gateway/src/lib.rs`:

| # | мир | ожидание | получено |
|---|---|---|---|
| M0 | ветка как есть: диапазон непуст, `${LIB}` не тронут | `SKIP` + `#6b` PASS | `SKIP: … диапазон НЕ ТРОГАЕТ crates/gateway/src/lib.rs` · `PASS: task #6b … (10)` · `FAIL=0 SKIPPED=1` ✅ |
| W0 | дерево на `d179886` (merge-base == HEAD) | `SKIP` (пуст) + `#6b` PASS | `SKIP: … диапазон ПУСТ (merge-base == HEAD == d179886…)` · `#6b` PASS · `FAIL=0 SKIPPED=1` ✅ |
| M1 | коммит: `${LIB}` тронут (комментарий), версия НЕ поднята | `FAIL` | `FAIL: task #6 версия схемы НЕ поднята (база 10, HEAD 10)` · `FAIL=1 SKIPPED=0` ✅ **сторож кусает** |
| M1b | коммит: бамп 10→11, пин не тронут | `#6` PASS, `#6b` FAIL | `PASS: task #6 … 10 → 11` · `FAIL: task #6b — EXPECTED_SCHEMA_VERSION=10 … константа=11` ✅ **сосед кусает** |
| M1c | + пин 10→11 | оба PASS | `PASS`/`PASS (11)` · `FAIL=0 SKIPPED=0` ✅ |
| M1d | `${LIB}` тронут и возвращён к идентичному (нетто-ноль) | `SKIP` (формы нет) | `git diff --name-only d179886..HEAD -- lib.rs` → пусто; `SKIP` ✅ корректно |
| **M5** | **НЕЗАКОММИЧЕННЫЙ бамп 10→11 в рабочем дереве** | ? | `SKIP: … форма выдачи не менялась` + `#6b FAIL (пин 10 ≠ 11)` → **Н-1 ниже** |

Итог: на ЗАКОММИЧЕННОМ диапазоне трёх исходов ровно три, ложного `PASS` нет, требование
бампа не отключено (M1), сосед не ослаблен (M1b). Пропуск виден в итоге: строка
`ПРОПУЩЕНО ШАГОВ: N` + `VERDICT: PASS (пропущено: N)` (`:454-458`).

### Н-1 (NOTE) — `SKIP` судит ДИАПАЗОН КОММИТОВ, а `V_HEAD` читает РАБОЧИЙ файл; сообщение `SKIP` это не оговаривает

M5: бамп сделан, но не закоммичен. Условие `git diff --name-only "${BASE}..HEAD" -- "${LIB}"`
(`:288`) рабочее дерево не видит ⇒ `SKIP` с текстом «форма выдачи не менялась» — в этом
мире ЛОЖНЫМ (форма сменилась, просто не закоммичена). Прежняя редакция шага на том же дереве
дала бы `PASS: версия поднята 10 → 11` (V_HEAD — из файла), а при смене формы БЕЗ бампа —
`FAIL`; новая даёт `SKIP` в обоих случаях, то есть до коммита сторож слеп.

Почему NOTE, а не блокер: (а) шаг C того же гейта судит границы тоже по `${BASE}..HEAD`
(`:449-450`) — конвенция гейта «судим коммиты», и `#6` ей теперь соответствует; (б) Done Block
и tester гоняют гейт на чистом дереве (`commit-discipline.md`: `git status --porcelain` пуст
перед push); (в) в M5 общий вердикт всё равно красный — но ТОЛЬКО потому, что пин не поднят;
подними dev оба незакоммиченно — итог `PASS (пропущено: 1)` при реально сменившейся форме.
Что сделать (одно из двух, зона architect'а, харнесс-трек): либо
`git diff --name-only "${BASE}" -- "${LIB}"` (без `..HEAD` — сравнивает базу с РАБОЧИМ
деревом и согласуется с чтением `V_HEAD` из файла), либо оставить конвенцию, но в тексте
`SKIP` написать «в диапазоне КОММИТОВ `${BASE}..HEAD`», чтобы сообщение не утверждало
больше, чем проверено.

### Н-2 (NOTE) — `${LIB}` как ЕДИНСТВЕННЫЙ носитель формы: предел назван мной, не гейтом

`crates/gateway/src/` содержит только `lib.rs` и `bin/`; все типы кадра там:
`pub struct Selector :282`, `OhlcvRow :353`, `DepthRow :427`, `SeriesBundle :441`,
`Snapshot :510` (`git grep -nE '^pub struct …' -- 'crates/**/src/**'`). Но WS-конверт
`ServeMsg` (`crates/gateway-serve/src/lib.rs:85-87`, `#[derive(Serialize, Deserialize)] pub
enum ServeMsg { Snapshot(Snapshot), … }`) версионируется «через `schema_version` внутри»
(`gateway-serve/src/lib.rs:56`) — тем же `GATEWAY_SCHEMA_VERSION`. Смена конверта (новый
вариант, переименование) меняет провод, не трогая `${LIB}` ⇒ `SKIP`. Это НЕ ложный PASS
(шаг честно говорит «не проверено»), и `task #6` — шаг acceptance `M-70`, а не общий сторож
формы; но комментарий к `SKIP` (`:289-297`) утверждает «форма выдачи не менялась», не
называя, что носителем считается ровно один файл. Что сделать: добавить
`crates/gateway-serve/src/lib.rs` во множество носителей ИЛИ назвать предел одной строкой
комментария рядом с `LIB=` (`:109`).

## 4. Не ослаблен ли ещё какой-то сосед (вопрос 3)

Переменные, присваиваемые внутри ветвей и читаемые позже (`grep -nE '^\s{2,}[A-Z_]+='`):
`ADAPTER_BODY :234`, `BANDS_LINE :376`, `RENDERED :428`, `RENDER_ERR :418`, `RENDER_RC :420`,
`SKIPPED :304`, `V_BASE :309`. Из них за пределы своего шага уходит только `V_HEAD` (читает
`#6b :328-334`) — теперь на `:284`, ВНЕ развилки; `V_BASE` читается только внутри `else`
`#6` (`:309-317`). `#6b` под `set -u` защищён формой `${V_HEAD:-}` (`:328`). Иных шагов,
зависящих от `#6`, нет: M0/W0 дают `#6b PASS`, полный прогон (§7) — без единого `FAIL`.

## 5. Б-2/Б-3/Б-4 приняты верно; Н-2 круга 1 закрыт на деле (вопросы 4–5)

- **Б-2.** Спека `:134-149`: «бэкап не содержит ключа `GATEWAY_BANDS` ВОВСЕ … Compose подставит
  `${GATEWAY_BANDS:-0.001}` (`docker-compose.yml:147`)». Прод, сейчас:
  `ls -la .env*` → `132 … .env`, `84 … .env.bak-1789933437`; `grep -cE '^GATEWAY_BANDS=' .env.bak-…`
  → `0`; в `.env` → `1`; `sed -n 147p docker-compose.yml` → `GATEWAY_BANDS: ${GATEWAY_BANDS:-0.001}`. ✅
- **Б-3.** Спека `:13-15` ссылается на `R-190` §6 NOTE `:167-172` и `PROJECT-STATE.md:2608`.
  `sed -n 167,172p R-190` — абзац «NOTE для architect'а … архивировать гейт вместе с
  close-out'ом M-70, либо сделать task #6 самоотключающимся»; `PROJECT-STATE.md:2608` несёт
  «сам milestone закрывает architect». ✅
- **Б-4.** В теле `59e88a9`: «26 — это ФАЙЛОВ, строк 94». ✅ (в спеке числа нет — и не нужно).
- **Н-2 круга 1 — сторож `task #7` жив.** Мутация на РЕАЛЬНЫХ строках шага (`:371-442`,
  `docker compose config` v2.39.1, `cargo`-часть шага не исполнялась):
  ```text
  compose как есть                    → PASS: task #7 … ОТРЕНДЕРЕННЫЙ прод-дефолт ровно 0.001   FAIL=0
  дефолт → 0.015,…,0.6 (флип)         → FAIL: task #7 — ОТРЕНДЕРЕННЫЙ прод-дефолт НЕ РОВНО узкий  FAIL=1
  GATEWAY_BANDS=0.015,0.03 в shell    → PASS (судит ДЕФОЛТ, env снят)                            FAIL=0
  ```
  Сторож краснеет ровно на варианте (а) развилки `§0closeout-bis` и не зависит от окружения
  запускающего. ✅

## 6. Поправки факта — стухшие `файл:строка` в спеке (от правки самого гейта)

### Б-1 (ФАКТ) — `milestones/M-70-…md:52` цитирует `:281-282` как «сравнивает версию в базе и на HEAD»

После `b5d3559` строки `281-282` гейта — комментарий («сверяет с ним sacred-пин…»);
извлечение теперь `V_HEAD :284`, `V_BASE :309`
(`grep -nE '^\s*V_BASE=|^V_HEAD=' scripts/verify_M-70.sh` → 284, 309). Поправить на `:284, :309`.

### Б-2 (ФАКТ) — `milestones/M-70-…md:89` цитирует `:334-335` («флип дефолта … включение известной лжи»)

Теперь это `:361-362` (`grep -n 'флип дефолта на канонический'` → 361; `КРАСНЕТЬ на таком
флипе` → 362); `:334-335` сегодня — тело `FAIL` шага `#6b`. Поправить на `:361-362`.
(`R-191` и тело `d376af1` цитируют старые номера — это датированные артефакты, их не трогать.)

### Н-3 (ОПЕЧАТКИ) — `:60` «Контроль тремя свидетелями» при пяти строках таблицы (W0, W3, W1, W2, W2b); `:47` «не был не просто не осуждён» — тройное отрицание, читается наоборот.

## 7. Полный прогон гейта на дереве ветки (закрывает п.1 моего `R-191` §6)

```text
$ bash scripts/verify_M-70.sh > m70full.out 2>&1; echo exit=$?      # дерево b5d3559, чистое, cargo не подменён
exit=0
$ grep -E '^(SKIP|FAIL|VERDICT|ПРОПУЩЕНО)' m70full.out; grep -c '^PASS' m70full.out
SKIP: task #6 — диапазон НЕ ТРОГАЕТ crates/gateway/src/lib.rs: форма выдачи не менялась, объявлять бампом нечего. НЕ проверено; …
ПРОПУЩЕНО ШАГОВ: 1 — см. строки SKIP выше; это НЕ проверенное.
VERDICT: PASS (пропущено: 1)
41
```

Заявление автора («`VERDICT: PASS (пропущено: 1)`, exit=0, ни одной строки `FAIL`») —
воспроизведено: 41 `PASS` / 0 `FAIL` / 1 `SKIP`, включая `ПАРИТЕТ С CI` (`fmt`, `clippy
--all-targets --all-features`, `test --all`), `task #6b` зелёный при `SKIP` соседа, `task #7`
зелёный на дефолте `0.001`. П.1 списка «не проверял» из `R-191` §6 этим закрыт.

## 8. Что я НЕ проверял — названо

1. Прод-гейтвей на предмет полос не опрашивал (инцидент 100 % CPU) — только `ls`/`grep`/`sed`
   по `.env*` и `docker-compose.yml` на проде.
2. `docs/plans/depth-liveness-window-measurement-2026-09-20.md` — не открывал (числа 9-B
   в спеке не менялись с круга 1, где наличие «1 мин 45 с» проверено грепом).
3. Временный репозиторий автора со «свидетелями» W0–W2b — не воспроизводил; вместо него
   моя батарея §3 на реальных строках шага.
4. `docs/fa/viz-backend.md` — открыт только `VB-I-4` (`:202`); живой FA-инвариант не
   называю как предъявление M-66: диапазон `crates/**` не трогает, `check_review_fa` → `SKIP`.
5. `check_context_budgets` — PASS с запасом 1940 B из 114900; какие 7 файлов в бюджете и
   входит ли в них растущая спека `M-70` — не смотрел.

## 9. Done Block

```text
$ pwd; git log -1 --oneline
/tmp/hft-recheck-m70r2
b5d3559 fix(M-70): регресс соседа — V_HEAD поднят из развилки; свидетели теперь включают task #6b [architect]
$ git grep -n 'verify_M-70' -- 'crates/*/src/**' docker-compose.yml 'deploy/**' | wc -l   # ссылки живы, гейт на месте
2
$ ls scripts/verify_M-70.sh milestones/M-70-depth-bands-enablement.md | wc -l
2
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -1; echo exit=$?
VERDICT: PASS (0 нарушений)
exit=0
$ bash scripts/next_artifact_id.sh R
R-192
$ bash scripts/check_branch_health.sh | tail -1
VERDICT: PASS — наблюдение состоялось (NOTE не блокируют: это наблюдатель, не барьер)
$ cd /tmp/hft-recheck-m70r2-mut && git status --porcelain | wc -l; git log -1 --oneline   # мутации откачены
0
b5d3559 fix(M-70): регресс соседа …
```

## 10. Сверка дельты `b5d3559..8285ab9` — замечания §3/§6 закрыты предписанным путём

**Дата:** 2026-09-20T23:20Z · **Предмет:** один коммит `8285ab9` (`scripts/verify_M-70.sh`
16/4, `milestones/M-70-…md` 16/4), поверх `730b80d`. Шапка `GATE-META` этого файла переведена
на `audited_head: 8285ab9…` — вердикт теперь судит эту вершину. `origin/main` = `d179886` без
изменений; ветка — fast-forward; `verify_design_claims.sh --merge-preview origin/main` →
`VERDICT: PASS (0 нарушений)`, exit=0.

**Итог: дельта ЧИСТАЯ — все пять замечаний закрыты тем, что предписано, и строгим путём.
Дефектов не найдено. Два наблюдения (§10.5), оба не о дельте, а о её пределах.**

### 10.1 Н-1 закрыт: сравнение база ↔ РАБОЧЕЕ дерево, три чтения согласованы

`scripts/verify_M-70.sh:300`: `git diff --name-only "${BASE}" -- ${FORM_CARRIERS}` — без
`..HEAD`. `V_HEAD :296` — из файла; `V_BASE :321` — `git show "${BASE}:${LIB}"`. Батарея на
РЕАЛЬНЫХ строках шагов `#6`+`#6b` (`sed -n '281,349p'` из `8285ab9:scripts/verify_M-70.sh`),
изолированный worktree `/tmp/hft-delta-m70-mut` (detached `8285ab9`, откачен до чистого):

| # | мир | получено |
|---|---|---|
| M0 | ветка как есть, носители не тронуты | `SKIP: … между базой … и РАБОЧИМ ДЕРЕВОМ не тронут ни один носитель формы` · `#6b PASS (10)` · `FAIL=0 SKIPPED=1` |
| W0 | дерево на `d179886` | `SKIP: … диапазон ПУСТ … и носители формы в рабочем дереве не тронуты` · `#6b PASS` |
| **M5** | бамп 10→11 НЕ закоммичен | `PASS: task #6 версия схемы поднята 10 → 11` · `FAIL: task #6b … EXPECTED=10, константа=11` — **слепого пятна нет**, было `SKIP` (`R-192` §3 M5) |
| M5b | бамп + пин, оба не закоммичены | `PASS`/`PASS (11)` · `FAIL=0 SKIPPED=0` |
| M9 | носитель тронут в рабочем дереве и ВОЗВРАЩЁН к идентичному | `SKIP` — нетто-ноль судится по содержимому, не по факту касания ✅ |
| M1 | коммит: комментарий в `${LIB}`, без бампа | `FAIL: … НЕ поднята (база 10, HEAD 10)` — требование не отключено |

Вопрос мандата «не появился ли обратный дефект — ложный `FAIL` на грязном дереве с посторонней
правкой носителя»: **M7** — незакоммиченный комментарий в `crates/gateway-serve/src/lib.rs`,
без бампа → `FAIL: task #6 версия схемы НЕ поднята`. Это НЕ новый дефект, а та же семантика, что
у закоммиченного M1: гейт с самого начала требует «тронул носитель — объяви бампом», и не
различает, важна ли правка (`:317-319` «ЧИСЛО, А НЕ ФАКТ ПРАВКИ СТРОКИ» — про обратное
направление). Раньше на грязном дереве шаг был слеп в ОБЕ стороны (SKIP), теперь видит в обе;
цена симметрична и названа. На чистом дереве (Done Block, tester) исход прежний — M0/W0.

### 10.2 Н-2 закрыт: носителей два, подстановка без кавычек — два pathspec'а

`:116` `FORM_CARRIERS="crates/gateway/src/lib.rs crates/gateway-serve/src/lib.rs"`, комментарий
`:110-115` называет предел явно. Форма подстановки проверена прямо (**M8**: оба файла тронуты
в рабочем дереве):

```text
$ git diff --name-only d179886 -- ${FORM_CARRIERS}          # без кавычек — как в гейте :300
crates/gateway-serve/src/lib.rs
crates/gateway/src/lib.rs
$ git diff --name-only d179886 -- "${FORM_CARRIERS}" | wc -l  # с кавычками — ОДИН pathspec с пробелом
0
```

Без кавычек — два аргумента, оба видны; с кавычками — ноль (несуществующий путь). Форма в гейте
верна; `set -u` не задет (переменная присвоена безусловно). **M6** (коммит в `gateway-serve`
без бампа) → `FAIL: … НЕ поднята`; **M6b** (+ бамп + пин) → оба `PASS`. Второй носитель судится.

**Третьего носителя под режимом `GATEWAY_SCHEMA_VERSION` нет** — проверено сканом, не
предположено: `git grep -lE 'GATEWAY_SCHEMA_VERSION|\bSnapshot\b|\bSeriesBundle\b|\bServeMsg\b'
-- 'crates/*/src/**'` → `gateway/src/lib.rs`, `gateway-serve/src/lib.rs` (оба — носители),
`gateway-serve/src/wire_v1.rs`, `gateway-serve/src/bin/wsprobe.rs`, `venue-binance/src/lib.rs`.
Разбор трёх остальных: `wsprobe.rs` — bin-ПОТРЕБИТЕЛЬ (типы `Summary`/`Render*` — отчёт зонда,
не провод); `venue-binance:325` — слово «Snapshot» в комментарии; `wire_v1.rs` — см. §10.5 (1).
`Frame` объявлен в `gateway/src/lib.rs:573`, `ServeMsg` — в `gateway-serve/src/lib.rs:86`: оба
внутри `FORM_CARRIERS`.

### 10.3 Б-1/Б-2 закрыты: номера строк совпадают с деревом `8285ab9`

```text
$ grep -nE '^V_HEAD=|^\s*V_BASE=|^FORM_CARRIERS=|флип дефолта на канонический|КРАСНЕТЬ на таком флипе' scripts/verify_M-70.sh
116:FORM_CARRIERS=…   296:V_HEAD=…   321:  V_BASE=…   373:# Пока это так, флип дефолта…   374:# КРАСНЕТЬ на таком флипе…
$ grep -nE ':296|:321|:373-374|:116' milestones/M-70-depth-bands-enablement.md | awk -F: '$1<=175'
52: … (`V_HEAD` — `:296`, `V_BASE` — `:321`)     82: … (`FORM_CARRIERS`, `:116`)     101: … (`:373-374`)
```

Все четыре ссылки указывают на названное. `crates/gateway-serve/src/lib.rs:86` → `pub enum
ServeMsg {`, `:56` → «Wire-конверт сообщений WS … версионированный через `schema_version`» ✅.

### 10.4 Н-3 закрыт: смысл сохранён

`:47` «`lib.rs:111` вообще не попал в поиск — его не просто не осудили, его не увидели» —
одно отрицание на утверждение, смысл прежний (греп не искал по `*.rs`). `:60` «Контроль
свидетелями на РЕАЛЬНЫХ строках обоих шагов (`#6` и соседа `#6b`)» — число снято, счёт не
разойдётся при следующем добавлении строки; таблица — восемь строк (W0, W3, W1, W2, W2b, M5,
M6, M6b), тело `8285ab9` говорит «восемь» ✅.

### 10.5 Наблюдения — не дефекты дельты, названы, чтобы не сошли за проверенное

1. **`wire_v1.rs` — носитель формы ПРОВОДА, но под ДРУГИМ версионным режимом.** Он несёт
   `ClientMessage` (`:37`, клиент → сервер) и описывает конверт `{"type","v":1,"sub","data"}`
   с собственным полем `v` (`CT-RFC-09` §2.2, `unknown_version` при чужом `v`); данные внутри
   конверта — `Snapshot`/`Frame` «как сегодня», то есть типы из `FORM_CARRIERS`. Смена
   протокола v1 объявляется полем `v`, а не `GATEWAY_SCHEMA_VERSION`, и `task #6` её судить не
   обязан: `VB-I-4` (`docs/fa/viz-backend.md:202`) говорит о `export_schema_version`. Предел
   `FORM_CARRIERS` = «то, что версионирует `GATEWAY_SCHEMA_VERSION`» — верный; в комментарии
   `:110-115` он подразумевается, но не назван словами. Одной строки «`wire_v1.rs` версионирует
   `v`, не входит» хватило бы; блокером это не является.
2. **`check_gate_meta` на диапазоне `d179886..8285ab9` БЕЗ этого коммита — `FAIL (1)`:**
   `subject-lock — после проходного вердикта (NOTE) тронут класс «гейт»: scripts/verify_M-70.sh`.
   Механика (`check_gate_meta.sh:518-535`): `own_touched "^${audited_head}"` — всё, что ветка
   сделала ПОСЛЕ `b5d3559`, содержит правку гейта, а токен `ALLOW-SUBJECT-CHANGE` ищется в
   телах коммитов того же диапазона — в `8285ab9` его нет (токен `59e88a9` стоит ДО вердикта и
   не считается). Это барьер, работающий как задуман: гейт изменён после проходного вердикта ⇒
   либо ре-аудит, либо токен. Ре-аудит — этот §10, `audited_head` → `8285ab9`; после моего
   коммита `own_touched` содержит только правку вердикта (не гейт-класс) ⇒ лок открыт
   ЗАКОННО, не токеном. **Следствие для маршрута:** `8285ab9` без коммита с этим §10 в `main`
   вливаться не должен — CI-джоб `gate-meta` его удержит; после коммита — проходит (§10.6).
   Любая следующая правка `scripts/verify_M-70.sh` на этой ветке снова потребует ре-аудита или
   токена — так и должно быть.

### 10.6 Полный прогон и барьеры на `8285ab9`

```text
$ bash scripts/verify_M-70.sh > m70full3.out 2>&1; echo exit=$?      # /tmp/hft-delta-m70, чистое дерево
exit=0
$ grep -E '^(SKIP|FAIL|VERDICT|ПРОПУЩЕНО)' m70full3.out; grep -c '^PASS' m70full3.out
SKIP: task #6 — между базой d179886… и РАБОЧИМ ДЕРЕВОМ не тронут ни один носитель формы (crates/gateway/src/lib.rs crates/gateway-serve/src/lib.rs): …
ПРОПУЩЕНО ШАГОВ: 1 — см. строки SKIP выше; это НЕ проверенное.
VERDICT: PASS (пропущено: 1)
41
```

Барьеры CI (`EVENT_NAME=pull_request PR_BASE_SHA=d179886…`), ДО моего коммита:
`check_roadmap_sync` PASS · `check_protected_artifacts` OK · `check_docs_freeze` exit=0 ·
`check_artifact_ids` OK · `check_review_fa` SKIP · `check_context_budgets` PASS (запас 1940 B) ·
**`check_gate_meta` FAIL (1)** — см. §10.5 (2); ПОСЛЕ коммита с §10 — `VERDICT: PASS — вердиктов проверено: 2, до-нормативных приземлений: 0, merge'ей с milestone в subject'е: 0`.

### 10.7 Суждение по `gates.md` §0 (спрошено прямо — отвечаю прямо)

**АРБИТР НЕ ТРЕБУЕТСЯ — предмет сходится, спора нет.** Триггер 2 («три круга по одному предмету
в любой комбинации вердиктов») по БУКВЕ счёта задет: круг 1 `R-191` REJECT, круг 2 `R-192`
NOTE, круг 3 — эта сверка. Но §0 написан против ТУПИКА («цикл architect↔critic может не
сойтись», «стороны не понимают друг друга»), и признаки тупика здесь отсутствуют все: ни одна
находка ни одного круга не оспорена; каждая закрыта тем способом, который назвал вердикт, и там,
где вердикт давал развилку, взят строгий путь; круг 3 не вносит новой конструкции — он
закрывает мои же замечания; вердикт круга 3 — без дефектов. Триггеры 1 (два REJECT по одной
причине), 3 (`ESCALATE`), 4 (прямое разногласие) и 5 (методологический тупик) не задеты.
Созывать арбитра, чтобы он подтвердил отсутствие спора, — трата ресурса, против которой §0 и
написан. Оговорка честности: это МОЁ суждение как перепроверяющего, и оно записано здесь, чтобы
маршрут не решался догадкой стороны; если founder читает триггер 2 буквально — его слово выше.

### 10.8 Что я НЕ проверял в этой сверке

1. Прод — не трогал вовсе (мандат; дельта его не касается).
2. Временный репозиторий автора с восемью свидетелями — не воспроизводил; моя батарея §10.1–10.2
   (M0, W0, M1, M5, M5b, M6, M6b, M7, M8, M9) на реальных строках шагов.
3. `wire_v1.rs` — прочитаны шапка (`:1-25`) и объявления типов; серверную сериализацию конверта
   v1 в `gateway-serve/src/lib.rs` (`:1092` и рядом) не читал построчно — достаточно, что она
   живёт в файле-носителе.
4. `check_gate_meta.sh` — прочитаны `:253-300`, `:340-400`, `:485-535`; остальное — нет.
