<!-- GATE-META
milestone: M-45
audited_repo: a3ka/hft-platform
audited_base: af2945284a728112c7aae7ef1a764f6a265c3dc0
audited_head: 092c02e2adc3b6933cf7c8478bab89c6a21343fd
verdict: APPROVE
-->

# R-166 — перепроверка §9, круг 3 (УЗКИЙ): остатки `R-165` Б-1а/Б-1б/Б-1в + Ф-1

Независимый Fable-агент со свежим контекстом; не автор правки, не автор `R-164` и не автор
`R-165`. Маршрут круга — решение founder'а: третий круг перепроверки вместо арбитража §0
(основание названо им: существа спора нет, все находки признаны и исправлены; арбитр решает
споры, а спора нет). Маршрут не пересуживается; судится ИСПОЛНЕНИЕ.

**Рамка сужена:** судятся РОВНО остатки `R-165` §6 — Б-1а, Б-1б, Б-1в на
`origin/docs/scope-guard-compose` (вершина `092c02e2adc3b6933cf7c8478bab89c6a21343fd`,
фикс поверх `bbbabd2`/`eac2287`/`bd86ec7`/`29c9823`, база `origin/main` `af29452` — дрейфа
нет) и Ф-1 на `origin/docs/M-45-rollout-signature` (вершина
`d9e015bdb9598bc6aba87bc212aa200be25715d6`). Установленное прошлыми кругами не пересуживалось:
правомерность нормативной строки (`R-164` §4), существо Б-1/Б-2 и истинность чисел
(`R-165` §1), чистота Ф-2 (`R-165` §3).

**Вердикт: APPROVE.** Все четыре остатка закрыты, каждый подтверждён моей командой в моём
worktree (`/tmp/hft-recheck-scope3`, detached на `092c02e`). Четвёртого вхождения класса
«показанный вывод не производится показанной командой» в диффах обеих веток НЕ найдено —
включая блоки, добавленные самими фиксами.

---

## 1. Б-1а — ЗАКРЫТ: каждый показанный вывод производится показанной командой дословно

Метод: для каждого блока документа команда прогнана в worktree, вывод сверен `diff`'ом
строка-в-строку с показанным (не глазами — механически).

1. **Блок лога правок** (`docs/workflow/compose-zone-2026-08-31.md:20-28`):
   `git log --format='%h %cs %s' --no-merges -- docker-compose.yml | grep -F '[engine-dev]'`
   → 8 строк; `diff` показанного (`:21-28`) против реального → **пусто, IDENTICAL**. Полные
   subject'ы, включая длинные хвосты `c2b926a`/`b69a791`, воспроизведены символ-в-символ.
2. **Счётчик** (`:30-31`): `git log --format='%s' --no-merges -- docker-compose.yml |
   grep -cF '[engine-dev]'` → `8` — дословно.
3. **Блок бюджета** (`:79-82`): фильтр теперь ПОКАЗАН
   (`| grep -E 'gates\.md|scope-guard|VERDICT'`), прогон → 3 строки; `diff` против `:80-82`
   → **пусто, IDENTICAL** (включая ширину колонок).
4. **Заявление автора про `%cs` проверено, а не принято:** `git log -1 --format='%cs / %ci'
   8e43cfb` → `2026-08-28 / 2026-08-28 12:18:27 +0000` — `%cs` действительно даёт ровно
   `YYYY-MM-DD`, `%ci` — нет. Плюс байт-идентичность п.1 доказывает это на всех 8 строках.

Условие `R-165` §6.1 («полный дословный вывод, либо отметка на каждом срезе; блоку бюджета —
показать фильтр») исполнено первой ветвью для лога и фильтром для бюджета.

## 2. Б-1б — ЗАКРЫТ: ложный указатель на `C-195` снят и заменён верным

- `compose-zone:56-68`: утверждение «живёт в `C-195`» названо НЕВЕРНЫМ, показана команда и
  вывод `0`. Мой прогон показанной команды:
  `git show origin/docs/M-45-rollout-signature:research/critiques/C-195-m45-rollout-signature.md
  | grep -cE '3419177|98ea630|head -4|четыре прецедента'` → `0` (exit=1; `grep -c` печатает
  `0` при нуле совпадений — показанный вывод производится, оговорка `R-097` N-7 о молчащем
  `git grep -c` здесь неприменима: это pipe в обычный `grep`).
- Команды мандата: `... | grep -cE '3419177|head -4|четыре'` → `0`;
  `git log -1 --format='%B' 29c9823 | grep -n 'четыре'` → `:8` («проходила ревью — четыре
  раза:»); то же для `ca20217` → `:10` («четыре прецедента»). Реальные носители — именно
  тела этих двух коммитов; документ теперь называет их и `M-45` §3ter (`:67-68`), файл
  `C-195` носителем не называет.
- Признание нарушения «правила цитаты» — в самом документе (`:65-67`) и в теле `092c02e`,
  без торга.

## 3. Б-1в — ЗАКРЫТ: неизменяемые носители названы прямо, со счётом

`compose-zone:49-54`: оба тела (`29c9823`, `ca20217`) названы поимённо, с цитатами-маркерами
(сверены с реальными телами: подстроки присутствуют), с прямой фразой «счёт „четыре" в обоих
телах неверен — правок восемь, и предъявленная в них четвёрка не производится командой,
которую они показывают». Это не намёк, а именование: читатель `git log` предупреждён этим
документом. Счёт «восемь» истинен (мой прогон счётчика — §1 п.2).

## 4. Ф-1 — ЗАКРЫТ на `d9e015b` по всем трём пунктам

1. **Вывод производится командой.** Блок `M-45:129-140` переснят той же командой с `%cs`;
   мой прогон `git log d9e015b --format='%h %cs %s' --no-merges -- docker-compose.yml |
   grep -F '[engine-dev]'` → `diff` против показанного (`:130-137`) → **пусто, IDENTICAL**;
   счётчик → `8`. Подстрока `[engine-dev]` теперь стоит в каждой строке вывода —
   самоопровержение прежней редакции устранено.
2. **Числа согласованы.** `M-45:126` теперь «**восемь правок**»; вхождение «четыре
   прецедента» в файле ровно ОДНО (`grep -c` → `1`, строка `:154`) — и это пометка об
   исправленной ошибке внутри таблицы следа, а не живое утверждение. `:143` «ВОСЕМЬ правок,
   семь милестоунов» — противоречия в разделе больше нет.
3. **Указатель поправлен.** `M-45:157-162`: «скопирован в `C-195`» назван НЕВЕРНЫМ, носители —
   тела `ca20217` и `29c9823`; совпадает с фактами §2.

След оставлен таблицей «круг → что стояло → почему неверно» на обеих ветках
(`compose-zone:40-43`, `M-45:148-151`); её фактические утверждения проверены отдельно:
`git log --format='%h %s' -- docker-compose.yml | head -8` — первой строкой действительно
merge-коммит `7ed83f1`, `98ea630` действительно седьмой. Истинно.

## 5. Четвёртое вхождение класса — НЕ НАЙДЕНО

Просмотрены ВСЕ `$`-блоки, добавленные фиксами (`git show <fix> | grep '^+.*\$ '`):
`092c02e` добавил три блока (лог, `C-195`-greп, бюджет) — все три прогнаны, §1-§2;
`d9e015b` добавил один блок и один inline-греп — прогнаны, §4. Состав диффов веток против
`origin/main` сверен (`git diff --name-only`): `scope-guard-compose` — 4 файла (норма,
компоуз-док, `R-164`, `R-165`; вердикты цитируют ошибку как ошибку — легитимный контекст);
`M-45-rollout-signature` — 12 файлов, из которых фикс-коммит `d9e015b` тронул один
(`milestones/M-45-persist-l2delta.md`, `git show --numstat` → `31/16` в одном файле),
остальные судились прошлыми кругами (`C-195`, `R-159..R-162`, `R-165` Ф-2/Ф-3) и в рамку
не входят. Непроизводимых блоков: ноль.

## 6. M-66 / §11 / гейты

- Диапазон обеих веток не трогает `crates/**` ⇒ `check_review_fa.sh` прод-формой →
  `SKIP (диапазон не трогает crates/**)`, exit=0. Живой FA-инвариант не называется — это
  соответствует норме (предел барьера назван, а не обойдён).
- Мой коммит трогает только `research/reviews/**` — вне зоны §11, токен не нужен
  (проверено составом диффа, не памятью).

## 7. Done Block (агрегированный; команда + exit-код/итог на каждое утверждение)

```text
$ git worktree add --detach /tmp/hft-recheck-scope3 origin/docs/scope-guard-compose → HEAD 092c02e
$ git log --oneline origin/main..HEAD → 092c02e/bbbabd2/eac2287/bd86ec7/29c9823 (ровно пять)
$ git rev-parse origin/main → af29452… (база не дрейфовала)
$ git log --format='%h %cs %s' --no-merges -- docker-compose.yml | grep -F '[engine-dev]' | diff <(sed -n '21,28p' compose-zone) - → пусто (IDENTICAL)
$ git log --format='%s' --no-merges -- docker-compose.yml | grep -cF '[engine-dev]' → 8
$ bash scripts/check_context_budgets.sh 2>&1 | grep -E 'gates\.md|scope-guard|VERDICT' | diff <(sed -n '80,82p' compose-zone) - → пусто (IDENTICAL)
$ git log -1 --format='%cs / %ci' 8e43cfb → 2026-08-28 / 2026-08-28 12:18:27 +0000 (%cs = ровно дата)
$ git show origin/docs/M-45-rollout-signature:research/critiques/C-195-*.md | grep -cE '3419177|98ea630|head -4|четыре прецедента' → 0, exit=1
$ git show origin/docs/M-45-rollout-signature:research/critiques/C-195-*.md | grep -cE '3419177|head -4|четыре' → 0, exit=1 (команда мандата)
$ git log -1 --format='%B' 29c9823 | grep -n 'четыре' → :8, exit=0 · то же ca20217 → :10, exit=0
$ git log d9e015b --format='%h %cs %s' --no-merges -- docker-compose.yml | grep -F '[engine-dev]' | diff <(показанное M-45:130-137) - → пусто (IDENTICAL); счётчик → 8
$ git show d9e015b:milestones/M-45-*.md | grep -c 'четыре прецедента' → 1 (строка :154, пометка об ошибке)
$ git log --format='%h %s' -- docker-compose.yml | head -8 → 7ed83f1 первый (merge), 98ea630 седьмой
$ EVENT_NAME=pull_request PR_BASE_SHA=$(git rev-parse origin/main) bash scripts/check_docs_freeze.sh; echo $? → 0
$ EVENT_NAME=pull_request PR_BASE_SHA=… bash scripts/check_review_fa.sh → SKIP (диапазон не трогает crates/**), exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main → VERDICT: PASS (0 нарушений), exit=0
$ bash scripts/check_branch_health.sh → VERDICT: PASS, замечаний 0, дублей предмета нет
$ bash scripts/next_artifact_id.sh R → R-166
```

**Маршрут дальше:** обе ветки готовы к следующему шагу цепочки — merge своим PR
(`gh pr create` → зелёные чеки по коду возврата → merge), по решению founder'а. Блокеров
этого гейта не осталось.
