<!-- GATE-META
milestone: M-87
audited_repo: a3ka/hft-platform
audited_base: 3a2e4607882756f660d664be5d2ba45780af0a4d
audited_head: 43223ad0af47c5dfd6dfb1461c0b42febeec5ab6
verdict: REJECT
-->

# R-204 — M-87 «предохранитель выдачи»: PR-гейт круга условий `R-202`

**Вердикт: REJECT. Один блокер, и он НЕ в предмете круга.** Оба условия `R-202` исполнены
и предъявлены прогонами: §13 приведена к факту, обоснования задач 15 и 16 переписаны по
НЫНЕШНЕМУ дереву и **правдивы по замеру** (я перепроверил их сам — барьер этого не умеет),
механизм построен, краснеет на `2a3495f`, зеленеет на вершине, и выдерживает мутацию.
Работа круга — принята.

Блокирует другое: **`docs/DESIGN.md` §22 становится ЛОЖЬЮ в момент слияния**, и это не
мнение, а красный джоб из агрегата `All checks passed`. Ветка физически не вливается.
Класс — ровно тот, ради которого `verify_design_claims.sh` и написан (`R-013` Б-2/Б-3:
«документ был правдив на ветке и стал ложью в момент слияния»), и ровно тот, который
`gates.md` §8 велит ловить прогоном на дереве слияния.

---

## §0 — Предмет: вершина взята КОМАНДОЙ, и она сдвинулась не там, где ждали

`.claude/agents/reviewer.md` Responsibility 0 + `docs/04-workflow.md` §2 «Предмет гейта —
ВЕТКА» (норма приземлилась в `main` ПОСРЕДИ этого круга, коммит `37e6c51`; исполняю её).

```
$ git fetch origin && git rev-parse origin/feat/M-87-serving-circuit-breaker
43223ad0af47c5dfd6dfb1461c0b42febeec5ab6
```

- SHA из мандата отсутствовал (мандат называл ветку) — брал вершину командой, как предписано.
- **Повторный `fetch` перед записью вердикта (п. 4): вершина ветки НЕ сдвинулась** —
  `43223ad` → `43223ad`.
- **Сдвинулся `main`, и существенно.** На момент начала работы `origin/main` = `29b0a93`
  (отставание ветки — 2 коммита, только `docs/SESSION-HANDOFF.md`, как и говорил мандат).
  На момент записи вердикта `origin/main` = `949a606` (merge PR #225): **отставание 7
  коммитов**, и они трогают `.claude/agents/{architect,critic,reviewer,risk-critic,tester}.md`,
  `docs/04-workflow.md`, `research/reviews/R-203-gm-gap-delegation-recheck.md`. Прогон
  merge-preview, снятый против `29b0a93`, я **отбросил и пересnял против `949a606`** — тот же
  единственный FAIL, `exit=1`.
- **Побочное следствие дрейфа: идентификатор `R-203` был занят, пока шёл этот круг.**
  `next_artifact_id.sh R` в начале работы дал `R-203`; к моменту записи в `main` уже жил
  `research/reviews/R-203-gm-gap-delegation-recheck.md`. Номер взят заново и **зарезервирован**
  (`gates.md` §12): `reserve_artifact_id.sh R` → `R-204` ← `bfee358`. Это не курьёз, а замер в
  пользу существующей нормы: без резерва аллокатор честно соврал бы вторично.
- Диапазон аудита: `3a2e460..43223ad` (**127** коммитов), `3a2e460` = `git merge-base
  origin/main 43223ad`. Ветка более НЕ содержит весь `main`, поэтому оговорка `strict:false`
  (`gates.md` §8) срабатывает — и именно она вскрыла блокер.

## Блок-scope — PASS

`git diff --name-only origin/main...43223ad` → **58 файлов**, все внутри §12 либо артефакты
гейтов:

- `crates/gateway-serve/src/{lib,admission,metrics,main,test_sync}.rs` ·
  `crates/gateway-serve/src/bin/wsprobe.rs` · `crates/gateway/src/lib.rs` — зона engine-dev, §12;
- `crates/gateway-serve/tests/**` (20 файлов) · `crates/gateway/tests/**` ·
  `scripts/verify_M-87.sh` · `milestones/M-87-*.md` · `docs/fa/viz-backend.md` — зона architect, §12;
- `docker-compose.yml` — объявлен §12; **в круге `R-202`→`R-204` НЕ тронут** (диф круга — два
  файла, см. ниже); состав записи (граница C, `П-008`) не затрагивался;
- `research/{critiques,reviews,arbitration}/**` (20 файлов) — артефакты гейтов;
  `docs/ROADMAP.md` — вне §12, но внутри глобальной зоны architect'а и требуется барьером
  `roadmap-sync`. NOTE, не нарушение (перенос `R-201`/`R-202`).

**Диф СОБСТВЕННО круга** — `git diff --numstat 2a3495f 43223ad`:

```
5	5	milestones/M-87-serving-circuit-breaker.md
377	0	research/reviews/R-202-M-87-r201-conditions-pr-gate.md
37	0	scripts/verify_M-87.sh
```

Два файла правки + мой предыдущий вердикт. Оба — зона architect'а, оба коммита с меткой
`[architect]`. Ровно то, что `R-202` адресовал: «Dev-работы в этом круге НЕТ».

`crates/contracts/**`, `crates/risk/**`, `crates/killswitch/**`, `crates/oms/**`,
`crates/venue-*/**` — не тронуты ни одним коммитом диапазона (проверено грепом по
`git diff --name-only`, не по памяти).

## Блок-C (контракты) — N/A

`crates/contracts/**` в дифе отсутствует; `GATEWAY_SCHEMA_VERSION` не бампается.
Contract-RFC не требуется.

## Блок-risk — N/A (risk-critic НЕ требуется)

`gates.md` §5 привязан к путям `risk|killswitch|oms|venue-*|contracts` — ни один не тронут.
`gateway-serve`/`gateway` — read-only консюмер журнала (`VB-I-3`, `docs/fa/viz-backend.md:206`),
order-egress отсутствует. §9 (safety-документы) не срабатывает: тронут
`docs/fa/viz-backend.md`, а не `fa/risk|killswitch|oms`.

## Блок-RED-first — PASS (тривиально, и это правильно)

`git diff --name-only 2a3495f 43223ad -- 'crates/*/tests/*' 'crates/*/src/*'` → **0**.
Ни одного теста, ни одной строки реализации. Круг был документально-харнессный, как
`R-202` и предписывал; переписывать код, закреплённый мутациями M1–M4 прошлого круга, было
бы вредом. За весь диапазон: тесты правили 37 коммитов, все `[architect]` (единственное
исключение — merge `0272260`) — перенос проверки `R-202`, пересчёт совпал.

## Блок-DoneBlock — PASS

Приёмка воспроизведена независимо на СВОЁМ чистом чекауте (`/tmp/hft-reviewer-m87-r203`,
detached `43223ad`): **32 PASS, 1 SKIP, `VERDICT: PASS`, exit=0**. Сырой вывод — в Done Block.
Ожидание мандата (31 + `task-status` = 32) подтверждено **фактическим счётом**
(`grep -cE '^PASS'` → 32), а не сверкой с обещанием.

## Блок «механизм на пути» (DoD, `gates.md` §4) — см. Н-2/Н-3

Задача 24 остаётся `🟡 ЧАСТИЧНО`: бюджет вызова с прод-пути снят и не подключён. Законный
выбор, разрешённый `R-201` условием 1 (вариант «б»); карточка «built-not-wired» MAJOR — на
close-out, merge не блокирует.

---

# УСЛОВИЯ `R-202` — ИСПОЛНЕНЫ ВСЕ ТРИ

## Условие 1 — §13 приведена к факту. ✅ ИСПОЛНЕНО, и обоснования ПРАВДИВЫ

Коммит `d80b947`, `5+/5-` в одном файле — ровно «один коммит architect'а», как требовалось.

| # | было на `2a3495f` | стало на `43223ad` | чем предъявлено |
|---|---|---|---|
| 10 | `⏳ OPEN` | `✅ DONE` + приписка | шаг `PASS task10: решение с токеном записано по каждому из 19 файлов и по двум тестам D-4` |
| 22 | `⏳ OPEN` | `✅ DONE` + `1606b61`, оракул `c1` | шаг `PASS task16: четыре условия R-196 (fail-closed без слепка · счётчик · бюджет · метаданные)` |
| 25 | `⏳ OPEN` | `✅ DONE` + `f1ac6a4`+`fc0fccd`, оракул `s1` | шаг `PASS task16: глобальный счётчик слотов не теряет декременты под гонкой` |
| 15 | `OPEN`, обоснование ЛОЖНО (`c2`, `snap_text.len()`) | `OPEN`, остаток назван по `R-202` Н-1 | **перепроверено мной, см. ниже** |
| 16 | `OPEN`, обоснование ЛОЖНО (`c3`, `read_to_end`) | `OPEN`, остаток «бюджет не подключён» | **перепроверено мной, см. ниже** |

**Я перепроверил ПРАВДИВОСТЬ новых обоснований замером, потому что барьер этого не делает**
(и честно об этом заявляет). Это и есть та часть работы гейта, которую механизм не забрал:

```
# задача 16 — «бюджет вызова НЕ ПОДКЛЮЧЁН»: ПРАВДА
$ grep -c 'feed_tail_within(' crates/gateway-serve/src/lib.rs
0
$ grep -rn 'feed_tail_within' crates/gateway-serve/src/
admission.rs:232:    // … комментарий
admission.rs:318:pub fn feed_tail_within<P: AsRef<Path>>(      # определение
admission.rs:401:    // … комментарий
lib.rs:2002:         // `feed_tail_within` СНЯТ с прод-пути выдачи как заглушка
lib.rs:2012:         // Сама функция `feed_tail_within` и `pump_one` сохранены …
# ни одного ВЫЗОВА из src/**; только определение и комментарии. Остаток назван верно.

# задача 15 — «read_dir и чтение заголовков остались на ГОРЯЧЕМ warm-пути»: ПРАВДА
$ grep -n 'payload_bytes_after_cursor' crates/gateway/src/lib.rs
3881:pub(crate) fn payload_bytes_after_cursor(dir: &Path, after_seq: u64) -> u64 {
5099:            let tail_bytes_read = payload_bytes_after_cursor(dir, cursor_after);   # warm-путь
$ sed -n '/fn payload_bytes_after_cursor/,/^}/p' crates/gateway/src/lib.rs | grep -nE 'list_segments|header|size_bytes'
2:    let segs = match journal::list_segments(dir) {
10:        if s.header.first_seq > after_seq {
11:            total = total.saturating_add(s.size_bytes);
$ grep -n 'fs::read_dir' crates/journal/src/segments.rs | sed -n '3p'
1191:    for entry in fs::read_dir(dir)? {        # dedup_indexed_paths, зовётся из list_segments
# цепочка `warm-путь → payload_bytes_after_cursor → list_segments → dedup_indexed_paths → read_dir`
# существует на проверяемой ревизии. Остаток назван верно, и назван ПО ПРИЧИНЕ, а не по инерции.
```

Обе мёртвые ссылки прошлой редакции сняты, и их смерть тоже проверена, а не принята на слово:
`grep -c 'fn c2' crates/gateway-serve/tests/red_m87_r196_conditions.rs` → **0**;
`snap_text.len()` на пути счётчика — **0** совпадений.

## Условие 2 — механизм построен. ✅ ИСПОЛНЕНО

Коммит `43223ad`, `+37` строк в `scripts/verify_M-87.sh`, шаг `task-status`. Требование
`R-202` формулировалось как «шаг, красящийся на *зелёный шаг приёмки при статусе `OPEN` без
названной причины*». Построенный шаг **шире**: он краснеет на статусе `OPEN` без названной
причины БЕЗУСЛОВНО, не сверяясь с зеленью соседнего шага. Надмножество требования, значит
условие покрыто; отсутствие перекрёстной сверки с собственным выводом приёмки — предел, а не
дыра в условии (Н-1).

## Условие 3 — анти-плацебо в обе стороны. ✅ ИСПОЛНЕНО, предъявлено прогонами

Заявление автора в теле `43223ad` («на `2a3495f` открытых 5, БЕЗ остатка — 10 15 16 22 25»)
я **не принял на слово, а воспроизвёл** — прод-формой вызова, на двух ревизиях. Совпало
дословно, включая перечень. См. §МУТАЦИОННЫЙ КОНТРОЛЬ, мутация A.

---

# БЛОКЕР

## Б-1 — `docs/DESIGN.md` §22 становится ЛОЖЬЮ при слиянии; джоб `design-claims` красит агрегат `All checks passed`

**Где:** `docs/DESIGN.md:920` — `| VB-I | viz-агрегаты | 11 | 8 | [ЧАСТИЧНО] |`.
**Причина:** `crates/gateway-serve/tests/red_m87_secret_form.rs:12` и `:99` — ДВА чистых
упоминания `` `VB-I-9` ``, введённые ЭТОЙ поставкой (задача 11, единая трактовка секрета,
`TD-207`). Они поднимают строгий счёт оракулов семейства `VB-I` с **8 до 9**.

**Замер, прод-формой вызова (та же, какой зовёт CI — без аргументов, ROOT = корень):**

```
# на ВЕТКЕ
$ bash scripts/verify_design_claims.sh
FAIL  [2-ПОКРЫТИЕ] §22: семейство VB-I — документ заявляет 'в оракулах'=8, реальный замер
      (анти-плацебо) strict=9, loose (любое упоминание)=9
VERDICT: FAIL (1 нарушений)
exit=1

# на ДЕРЕВЕ СЛИЯНИЯ против НЫНЕШНЕГО origin/main (949a606) — `gates.md` §8, strict:false
$ bash scripts/verify_design_claims.sh --merge-preview origin/main
FAIL  [2-ПОКРЫТИЕ] §22: семейство VB-I — документ заявляет 'в оракулах'=8, реальный замер
      (анти-плацебо) strict=9, loose (любое упоминание)=9
VERDICT: FAIL (1 нарушений)
exit=1

# на `main` БЕЗ ветки — гейт ЗЕЛЁН, то есть красноту вносит ветка, а не наследует
$ git rev-parse HEAD; bash scripts/verify_design_claims.sh
9f6d55a4129fd4a4b2e764dae97d2f39baa01575
VERDICT: PASS (0 нарушений)
exit=0
```

**Причинность предъявлена мутацией, а не выведена** (мутация D ниже): нейтрализация ровно
двух упоминаний `VB-I-9` в `red_m87_secret_form.rs` → `PASS  [2-ПОКРЫТИЕ] §22: VB-I —
заявлено=11, в оракулах=8 — подтверждено замером (loose=8)`, `VERDICT: PASS`.

**Почему это блокер, а не примечание — три довода, и первый механический:**

1. **Merge физически невозможен.** `design-claims` входит в `needs` агрегата
   `All checks passed` (`.github/workflows/ci.yml:615`, он же перечислен в условии на `:619`),
   джоб безусловный (триггер `pull_request: branches: [main]`, `.github/workflows/ci.yml:2-7`),
   и на `pull_request` checkout даёт МЕРЖ-коммит — то есть CI посчитает ровно то, что посчитал
   я. Защита `main` требует зелёный `All checks passed` (`gates.md` §8/§11). Вердикт APPROVED
   здесь означал бы обещание, которого я не могу исполнить.
2. **Это ИМЕННО тот класс, под который написан скрипт.** `verify_design_claims.sh` шапка,
   случай `R-013` Б-2/Б-3: «гейт прогнан на ветке (PASS), но `main` ушёл вперёд — на
   merge-цели ТЕ ЖЕ числа стали ложными». Здесь направление обратное и хуже: числа ложны уже
   на ветке, и ветка их вносит. `gates.md` §8: «расхождение прогонов — блокер, не примечание».
3. **Документ солгал бы в сторону ЗАНИЖЕНИЯ собственной защищённости** — и это не «неопасная»
   ложь. §22 — таблица, по которой судят, какие инварианты закрыты оракулами. Заниженное «8»
   при девяти реальных оракулах приглашает следующий круг «достроить покрытие `VB-I-9`»,
   которое уже построено. Ровно та же цена, что у `⏳ OPEN` при готовом коде (`R-202` Б-1
   довод 2) — приглашение переделать работающее, `scope-guard.md` §«Три жёстких правила» п. 2.

**Цена исправления — одна цифра:** `docs/DESIGN.md:920`, `8` → `9` (колонка `[ЧАСТИЧНО]`
остаётся верной: 9 < 11). Зона — architect (`docs/**`); reviewer в `docs/DESIGN.md` не пишет
(`scope-guard.md`). По `gates.md` §9 это синхронизация числа с замером, а не изменение формы
документа, — критик по триггерам не требуется; правка приземляется на ЭТУ ветку до PR.

**Почему это не нашли раньше — и это не упрёк, а дефект проводки.** `verify_M-87.sh` гоняет
паритет с CI по трём командам (`fmt`/`clippy`/`test --all`), но `design-claims` в него не
входит — как и прочие барьеры репозитория. Три круга гейта (`R-200`, `R-201`, `R-202`) прогоняли
приёмку и получали `exit=0`, не касаясь этого джоба. `gates.md` §3 говорит: «гейт, который
зеленее CI, — не гейт»; здесь приёмка милестоуна зеленее CI на целый джоб. Это переносимая
находка, не про M-87 — см. Н-6.

---

# ПРИМЕЧАНИЯ (не блокируют)

**Н-1 — три названных предела барьера `task-status`: ОЦЕНЕНЫ ЗАМЕРОМ, ни один не блокер.**
Мандат просил судить самому; сужу по прогонам, а не по впечатлению.

*(а) «проверяется наличие маркера остатка, а не его правдивость» — ДОЛГ, и он ровно тот
размер, что заявлен.* Измерено мутацией C: я вернул в §13 **дословно ту ложь, которую
заблокировал `R-202`** (ссылку на удалённый оракул `c2` и на `snap_text.len()` — обе мертвы,
`grep` → 0), добавив в неё слово `остаток`. Барьер: `PASS task-status: каждая из 2 открытых
задач НАЗЫВАЕТ свой остаток`; вся приёмка — `VERDICT: PASS`, `exit=0`. **То есть барьер
механизирует класс `R-200` §B3 (МОЛЧАЛИВАЯ пометка) и НЕ механизирует класс `R-202`
(протухшее обоснование).** Автор это заявил в теле коммита в точности так же — упрёка нет,
есть уточнение размера. Не блокер, потому что (1) `gates.md` §11 допускает «механизм ЛИБО
явная запись остаточного риска», и запись сделана; (2) правдивость обоснования — работа
гейта, и я её выполнил выше замером, а не пересказом. **Но остаточный риск обязан пережить
сообщение коммита:** карточка на close-out (Н-7), `TECH-DEBT.md` — моя зона.

*(б) «обратный класс `✅ DONE` при красном шаге не ловится» — ПОЧТИ ЗАКРЫТ АГРЕГАТОМ, остаток
назван.* Красный шаг сам валит приёмку: `FAIL` инкрементирует счётчик, финал —
`VERDICT: FAIL … exit=1` (`scripts/verify_M-87.sh`, хвост). Проверено исполнением трижды
(мутации A и B: `exit=1` при единственном `FAIL`). Значит «DONE при красном шаге» до merge
не доезжает — его держит защита ветки, а не пометка. **Остаток, который НЕ держится ничем:**
`DONE` на задаче, у которой шага приёмки НЕТ вовсе (задача 26 — «поведенческого оракула НЕТ
и он не изображается»). Это узкий и уже названный в спеке случай; блокером не делаю.

*(в) «регэксп `[0-9]+б?` не видит `2bis`» — ДЕФЕКТ ЕСТЬ, цена сегодня НУЛЕВАЯ, и это замер.*
Строк-задач в §13 — **28**; барьер видит **27**. Невидимая — `milestones/M-87-*.md:490`,
задача `2bis`:
```
$ grep -cE '^\| *[0-9A-Za-zбисbis]+ *\| *(⏳|✅|🟡|❌)' milestones/M-87-serving-circuit-breaker.md
28
$ grep -cE '^\| [0-9]+б? \| (⏳ OPEN|✅ DONE|🟡)' milestones/M-87-serving-circuit-breaker.md
27
```
Её статус — `✅ DONE`, а `DONE`-строки барьер не проверяет и так, поэтому пропуск сегодня не
стоит ничего. Стоил бы, если бы `2bis` когда-нибудь встала в `OPEN`: тогда молчаливая пометка
прошла бы молча — ровно класс, от которого барьер и заводился. Правка — один символ в двух
местах (`[0-9]+(б|bis)?`). Не блокер: сегодня не срабатывает; в долг (Н-7).

**Н-2 — дополнительный предел, мандатом не названный: барьер проверяет СЛОВАРЬ, а не смысл, и
в обратную сторону СЛИШКОМ СТРОГ.** Измерено мутацией B: я переписал остаток задачи 15
**другими словами**, сохранив его полностью и точно («Чего НЕ ХВАТАЕТ: на ГОРЯЧЕМ warm-пути
сохранились `read_dir` и чтение заголовков сегментов — …»). Барьер:
`FAIL task-status: открытые задачи БЕЗ названного остатка: 15`. То есть добросовестный автор,
назвавший остаток не тем синонимом, получает красный гейт, а недобросовестный, вписавший слово
`долг` в протухший текст, — зелёный. Направление ошибки **безопасное** (fail-closed), поэтому
не блокер; но список маркеров — открытое перечисление в скрипте, и его хрупкость стоит
записать, а не обнаружить третьим кругом.

**Н-3 — `🟡 ЧАСТИЧНО` барьером НЕ проверяется, хотя его `grep` эти строки выбирает.**
Предфильтр `grep -E '… (⏳ OPEN|✅ DONE|🟡)'` пропускает `🟡`-строки внутрь, но `case`-ветка
ловит только `⏳ OPEN` — `🟡` молча отбрасывается, и `TASKS_OPEN` их не считает. Задача 24
(`🟡 ЧАСТИЧНО`) остаток называет, так что цена и здесь нулевая; плюс слово `ЧАСТИЧНО` само
стоит в списке маркеров, то есть проверка была бы тавтологичной. Мёртвая ветка `grep`'а —
дефект формы, в долг вместе с (в).

**Н-4 — пустая строка `milestones/M-87-*.md:511` РАЗРЫВАЕТ таблицу §13** (перенос §E хендоффа
architect'а — подтверждаю замером: строка 511 пуста, задачи 17–27 и 18 рендерятся вне
таблицы). На барьер не влияет — он построчный, а не табличный, и все 27 строк видит через
`grep`. Дефект формы документа-источника; правка — зона architect'а, в тот же коммит, что Б-1.

**Н-5 — задача 24 «built-not-wired»: карточка, а не оракул** (перенос `R-202` Н-2, позиция
не изменилась). Бюджет вызова не подключён — предъявлено моим грепом выше (0 вызовов).
Что ловится: возврат холостого чтения (`q` краснеет по замеру ядра). Что НЕ ловится:
отсутствие самого предохранителя по байтам/событиям. Карточка severity **MAJOR** на close-out.

**Н-6 — приёмка милестоуна ЗЕЛЕНЕЕ CI на целый джоб; находка переносимая, не про M-87.**
`gates.md` §3 требует паритета verify с базовым CI-job'ом и проверки
`grep -E "run:" .github/workflows/ci.yml` — «каждая команда оттуда имеет пункт в verify».
Фактически `verify_M-87.sh` несёт три команды `build-test` и не несёт `design-claims`,
`gate-meta`, `artifact-ids`, `docs-freeze`, `review-fa`, `protected-artifacts`,
`roadmap-sync`, `archived-refs`, `context-budgets`, `resource-oracles`,
`rollout-composition`, `deploy-catchup`, `secret-material` — то есть 13 из 17 джобов
агрегата. Именно поэтому Б-1 дожил до четвёртого круга гейта при трёх зелёных приёмках.
Это дефект ШАБЛОНА verify, а не этого милестоуна, и лечить его внутри `verify_M-87.sh`
неправильно (будет четвёртая копия того же списка). Кандидат: один шаг «прогнать барьеры
репозитория прод-формой события», общий для всех `verify_M-NN.sh`, — предмет
harness-трека, не этого круга. В долг (Н-7); предъявляю как находку, но merge им не
блокирую — блокирует Б-1, который он объясняет.

**Н-7 — карточки долга на close-out (заводит reviewer ПОСЛЕ APPROVED, номера — механизмом).**
Пять, из них три перенесены и две новые:
1. «бюджет вызова не подключён» — MAJOR, built-not-wired (задачи 16/24; перенос `R-202` Н-2);
2. «экспорт счётчиков наружу процесса не построен» — MAJOR, built-not-wired (§11 п. 5,
   `A-037` У-7; перенос `R-202` Н-3);
3. «лимиты объявлены, не применены» — заводится ТОЛЬКО при нулях `docker inspect` на
   post-merge гейте (§8); замер 2026-09-21 дал `NanoCpus=0 Memory=0` (перенос `R-202` Н-4);
4. **НОВАЯ:** «барьер `task-status` проверяет наличие заявления, а не его правдивость;
   `2bis` вне регэкспа; `🟡` в мёртвой ветке» — остаточный риск из Н-1(а)/(в) и Н-3, чтобы он
   жил в `TECH-DEBT.md`, а не только в сообщении коммита `43223ad`;
5. **НОВАЯ:** «шаблон `verify_M-NN.sh` не держит паритет с агрегатом CI — 13 джобов из 17 вне
   приёмки» — Н-6.
**Аллокатор по-прежнему врёт на `TD`** (перенос `R-202` Н-5, проверено сегодня):
`next_artifact_id.sh TD` → `TD-218`, а `TD-218` занят строкой `TECH-DEBT.md:170` — у неё нет
заводящей карточки-слага, по которым аллокатор считает максимум. Номера беру `TD-219`+ и форму
записи `TD-218` достраиваю сам; `TECH-DEBT.md` — моя зона.

**Н-8 — §9 перепроверка: позиция та же, но зона круга РАСШИРИЛАСЬ.** Диф круга трогает
`scripts/verify_M-87.sh` — уставная зона `gates.md` §9 (харнесс). Отдельного артефакта §9 по
этому коммиту нет. Блокером не делаю (перенос позиции `R-201`/`R-202`): правка производна от
моего же вердикта `R-202`, инвариантов не ослабляет, и в цепочке есть вердикты сильной модели
со свежим контекстом. Но назвать обязан, и в close-out это идёт строкой. Зона §11 (замок
процессного слоя) НЕ тронута — `check_docs_freeze.sh` на дереве слияния молчит.

**Н-9 — барьеры репозитория прогнаны в ПРОД-ФОРМЕ вызова, на ЭМУЛЯЦИИ мерж-коммита.** Прямой
прогон с `PR_BASE_SHA=949a606` против вершины ветки даёт у всех четырёх барьеров честный
fail-closed («база НЕ предок HEAD») — потому что ветка теперь ОТСТАЁТ от `main` на 7 коммитов.
Это не дефект барьеров: на `pull_request` CI чекаутит мерж-коммит, у которого база — предок.
Собрал такой коммит локально (`git merge --no-ff origin/main` → `55b28ad`, выброшен после
замера) и прогнал по нему:
```
check_artifact_ids       exit=0  OK: ни один коммит диапазона 949a606..HEAD не ввёл второй носитель под занятым идентификатором
check_docs_freeze        exit=0  (молчание — зона §11 не тронута)
check_protected_artifacts exit=0 OK: защищённые артефакты целы на HEAD (949a606..HEAD)
check_gate_meta          exit=0  VERDICT: PASS — вердиктов проверено: 24, до-нормативных приземлений: 0
check_review_fa          exit=0  (R-201/R-202 → VB-I-10; R-196/R-200 → GS-I-1)
```
Единственный красный джоб агрегата — `design-claims` (Б-1).

---

# МУТАЦИОННЫЙ КОНТРОЛЬ — четыре мутации, и две из них меряют ПРЕДЕЛ, а не привязку

`testing.md` §«Мутационный контроль» — оба вопроса, и «к чему привязан», и «что ослаблено
рядом». Прогон прод-формой (`bash scripts/verify_M-87.sh`, без аргументов — так её зовёт
человек и tester), чекаут `/tmp/hft-reviewer-m87-r203` @ `43223ad`, после каждой — откат.

## A — спека с `2a3495f` против скрипта с вершины: шаг ОБЯЗАН покраснеть (условие 3 `R-202`)

```
$ git checkout 2a3495f -- milestones/M-87-serving-circuit-breaker.md
$ git diff --numstat -- milestones/
5	5	milestones/M-87-serving-circuit-breaker.md

$ bash scripts/verify_M-87.sh
FAIL  task-status: открытые задачи БЕЗ названного остатка: 10 15 16 22 25 — пометка
      неотличима от протухшей (R-200 §B3 · R-201 · R-202, три круга подряд)
VERDICT: FAIL (провалов: 1)
exit=1
```

**Пять найденных — РОВНО те пять, что я нашёл вручную в `R-202` Б-1, тем же перечнем и в том
же порядке.** Единственный `FAIL` во всей приёмке — этот шаг; остальные 31 зелёные, то есть
мутация покрасила ТО, ЧТО ДОЛЖНА, и ничего больше. Барьер воспроизводит находку человека —
это и есть проверка «механизм, а не третий абзац прозы».

## B — остаток назван ДРУГИМИ СЛОВАМИ: шаг краснеет (привязка есть; строгость чрезмерна)

```
$ sed -i '509s/ОСТАТОК НАЗВАН/НЕДОДЕЛКА НАЗВАНА/; 509s/Действительный остаток:/Чего НЕ ХВАТАЕТ:/' \
    milestones/M-87-serving-circuit-breaker.md
$ git diff --numstat -- milestones/
1	1	milestones/M-87-serving-circuit-breaker.md
# остаток СОХРАНЁН полностью: «Чего НЕ ХВАТАЕТ:** на ГОРЯЧЕМ warm-пути сохранились `read_dir`
# и чтение заголовков сегментов — …». Маркеров из списка скрипта: 0.

$ bash scripts/verify_M-87.sh
FAIL  task-status: открытые задачи БЕЗ названного остатка: 15 — пометка неотличима от
      протухшей (R-200 §B3 · R-201 · R-202, три круга подряд)
VERDICT: FAIL (провалов: 1)
exit=1
```

Первый вопрос мутационного контроля отвечён: шаг привязан к содержимому строки, а не
декоративен. Второй вопрос («что ослаблено рядом») отвечён честно и не в пользу барьера:
он привязан к СЛОВАРЮ. → Н-2.

## C — ложное, но СУЩЕСТВУЮЩЕЕ обоснование: шаг ПРОПУСКАЕТ (измерение предела (а))

```
$ python3 …   # примечание задачи 15 заменено дословной ложью, заблокированной R-202:
#   «**Действительный остаток:** оракул `red_m87_r196_conditions.rs::c2` КРАСЕН — счётчик
#    кормится размером ОТПРАВЛЕННОГО ответа (`snap_text.len()`, два места) …»
$ grep -c 'fn c2' crates/gateway-serve/tests/red_m87_r196_conditions.rs      # оракула НЕТ
0
$ grep -rc 'payload_bytes_read.*snap_text.len()' crates/gateway-serve/src/ crates/gateway/src/
0                                                                            # дефекта НЕТ

$ bash scripts/verify_M-87.sh
PASS  task-status: каждая из 2 открытых задач НАЗЫВАЕТ свой остаток
VERDICT: PASS
exit=0
```

**Приёмка зелёная при §13, содержащей ровно ту ложь, из-за которой вынесен `R-202`.** Это не
находка против автора — он объявил этот предел сам, теми же словами. Это измерение его
размера, которого в теле коммита не было: класс, повторившийся трижды, механизирован в СВОЕЙ
безобидной половине (молчание), а в опасной (протухший текст) по-прежнему держится на том,
что вердикт гейта перепроверяет ссылки замером. → Н-1(а) + карточка Н-7 п. 4.

## D — причинность блокера Б-1

```
$ sed -i 's/`VB-I-9`/`VB-I-НЕЙТР`/g' crates/gateway-serve/tests/red_m87_secret_form.rs
$ git diff --numstat
2	2	crates/gateway-serve/tests/red_m87_secret_form.rs

$ bash scripts/verify_design_claims.sh
PASS  [2-ПОКРЫТИЕ] §22: VB-I — заявлено=11, в оракулах=8 — подтверждено замером (loose=8)
VERDICT: PASS (0 нарушений)
```

Нейтрализация ОДНОГО из двух упоминаний оставляла `FAIL` (strict=9) — считается множество
уникальных ID, не строк; красит именно наличие `VB-I-9` как чистого токена. Правка требуется
в `DESIGN.md`, НЕ в оракуле: оракул `VB-I-9` построен законно и снимать его нельзя.

## Откат и базовая линия

```
$ git checkout 43223ad -- milestones/M-87-serving-circuit-breaker.md
$ git status --porcelain
{пусто}
$ git rev-parse HEAD
43223ad0af47c5dfd6dfb1461c0b42febeec5ab6
```
Мутации A/B/C откачены в рабочем чекауте; D жила в отдельном временном worktree
(`/tmp/hft-r203-dc`), снесённом после замера. Базовая линия — 32 PASS / 1 SKIP / exit=0.

---

# УСЛОВИЕ APPROVED

1. **Б-1:** `docs/DESIGN.md:920` — `в оракулах` `8` → `9`. Один символ; колонка
   `[ЧАСТИЧНО]` верна и остаётся. Предъявить прогоном НА ДЕРЕВЕ СЛИЯНИЯ, а не на ветке:
   `bash scripts/verify_design_claims.sh --merge-preview origin/main` → `VERDICT: PASS`,
   `exit=0`. **Оракул `VB-I-9` НЕ СНИМАТЬ** — он законен, лжёт документ, а не код.
2. **Н-4:** пустая строка `milestones/M-87-*.md:511` убрана, таблица §13 снова цельная —
   тем же коммитом, что п. 1 (обе правки — форма документов, зона architect'а).
3. Приёмка перепрогоняется целиком после правки (`verify_M-87.sh` → 32 PASS / 1 SKIP /
   exit=0), и **дополнительно** `verify_design_claims.sh --merge-preview origin/main` →
   exit=0. Второе — новый обязательный пункт этого предмета: без него мы четвёртый раз
   получим зелёную приёмку при красном CI (Н-6).
4. Ветка отстаёт от `main` на 7 коммитов (`strict:false`, `gates.md` §8). Перед PR —
   синхронизировать с `origin/main` либо предъявить merge-preview-прогон п. 3 на НЫНЕШНЕЙ
   вершине `main`; судить по коду возврата.

**Кому:** `architect`, один адресат, зона `docs/DESIGN.md` + `milestones/M-87-*.md`.
**Dev-работы в этом круге НЕТ.** Код закрыт и закреплён мутациями M1–M4 (`R-202`) и A–C
(здесь); трогать `crates/**` незачем и вредно.

Что НЕ требуется переделывать: условия 1–3 `R-202` (исполнены, предъявлено выше), задачи
10/22/23/25/26/27, барьер `task-status` (его пределы — долг Н-7, не переделка), обоснования
задач 15 и 16 (перепроверены замером, правдивы).

---

# Предъявление FA (M-66)

Диф трогает `crates/gateway-serve/**` и `crates/gateway/**`; FA зоны — `docs/fa/viz-backend.md`
(собственного `fa/gateway-serve.md` не существует — факт, сам по себе долг, перенос
`R-201`/`R-202`). Живые инварианты на проверяемой ревизии `43223ad`, использованные ПО
СУЩЕСТВУ, а не перечислением:

- **`VB-I-10`** (`docs/fa/viz-backend.md:213`) — «Bounded-window snapshot (M-37, `TD-039`).
  Память `snapshot`/`frames_since` ограничена ОКНОМ `[at−W, at]` …, иначе host-OOM (прод:
  RSS 7.3GB)». **Опора условия 1:** остаток задачи 15, который я перепроверил замером
  (`read_dir` + заголовки сегментов на горячем warm-пути через `payload_bytes_after_cursor`),
  есть цена, платимая НА КАЖДОЙ сессии внутри этой границы. Статус `OPEN` задачи 15 верен
  именно по этому инварианту, и обоснование в §13 теперь называет причину, а не протухшую
  ссылку.
- **`VB-I-11`** (`:214`) — «Провенанс ИСТОРИИ (M-48, `TD-048`). „All-time“ ≡ „от самого
  раннего seq, доступного под данным `EpochFilter`“ … система обязана НЕ ВЫДАВАТЬ ЭТО ЗА
  ДРУГОЕ». **Опора блокера Б-1 по КЛАССУ:** `DESIGN.md` §22, объявляющая восемь оракулов при
  девяти существующих, выдаёт одно за другое — тот же класс нечестности, перенесённый с
  данных на документ. И **опора Н-1(а):** обоснование, ссылающееся на удалённый оракул,
  выдаёт снятый дефект за живой; барьер этого не видит, вердикт — обязан.
- **`GW-I-9(б)`** (`:71`, уточнён этой поставкой по `A-037` D-4) — «слепок есть кэш, его
  потеря не останавливает исправную выдачу». **Опора Блока-DoneBlock:** шаги
  `task16: объём чтения и счётчик согласованы с замером ЯДРА` и `task12/task13` зелёные
  именно на этой семантике; WARM-путь читает слепок и хвост.
- `VB-I-3` (`:206`) — read-only Read Gateway; проверено, что диф его не нарушает
  (order-egress и journal-writer отсутствуют) — основание «Блок-risk: N/A».

**Ярус C — что искал грепом** (`reading-map.md` §2; файлы reviewer-owned, но 1 015 KB ≈ 226 k
токенов, целиком не читаются): `TECH-DEBT.md` по `TD-20[0-9]`, `TD-21[0-9]`, `TD-22[0-9]`,
`built-not-wired`, `gateway-serve`, `предохранител`; `PROJECT-STATE.md` по `M-87`,
`gateway-serve`, `предохранител`. Найденное и относящееся к предмету: `TD-207` (зонд `wsprobe`
против прод-формы секрета) — закрывается задачами 7/11, шаги `task7`/`task11` зелёные, закрытие
карточки на close-out ПОСЛЕ устранения Б-1; `TD-202` (built-not-wired на пути ретеншена) —
соседний класс, тот же `docker-compose.yml`, этой поставкой не затронут; `TD-217`
(built-not-wired в серверной библиотеке) — прямой предшественник карточек Н-7 п. 1–2, форму
записи беру оттуда; `TD-218` — дефект ФОРМЫ записи (нет заводящей карточки), из-за него врёт
аллокатор (Н-7).

---

# Done Block — приёмка на СВОЁМ чистом чекауте

```
$ pwd; git rev-parse HEAD
/tmp/hft-reviewer-m87-r203
43223ad0af47c5dfd6dfb1461c0b42febeec5ab6

$ git status --porcelain
{пусто}

$ git fetch origin && git rev-parse origin/feat/M-87-serving-circuit-breaker   # вершина — командой
43223ad0af47c5dfd6dfb1461c0b42febeec5ab6

$ git rev-parse origin/main; git rev-list --count 43223ad..origin/main
949a606f9b26d61a8b7973c70c43b2e86b212fc6
7                                          # ветка ОТСТАЁТ — strict:false применим

$ timeout 2400 bash scripts/verify_M-87.sh
… PASS × 32 …
SKIP  task9: ФАКТИЧЕСКИ применённые лимиты, запас для recorder'а и поведение НА лимите
      снимаются на проде (docker inspect) — шаг деплой-гейта §8
VERDICT: PASS
exit=0

$ grep -cE '^PASS' /tmp/r203-verify-tip.txt ; grep -cE '^FAIL' /tmp/r203-verify-tip.txt
32
0

# новый шаг круга — зелёный на вершине:
PASS  task-status: каждая из 2 открытых задач НАЗЫВАЕТ свой остаток

# CI-паритет внутри verify (три команды build-test):
PASS  CI-паритет: cargo fmt --all -- --check
PASS  CI-паритет: cargo clippy --all-targets --all-features -- -D warnings
PASS  CI-паритет: cargo test --all
PASS  D-1(б): библиотечный корпус зелен — passed=258 failed=0

# ГЕЙТ, КОТОРОГО В ПРИЁМКЕ НЕТ, — и он КРАСНЫЙ (Б-1):
$ bash scripts/verify_design_claims.sh --merge-preview origin/main
FAIL  [2-ПОКРЫТИЕ] §22: семейство VB-I — документ заявляет 'в оракулах'=8, реальный замер
      (анти-плацебо) strict=9, loose (любое упоминание)=9
VERDICT: FAIL (1 нарушений)
exit=1

# барьеры репозитория на эмуляции мерж-коммита (EVENT_NAME=pull_request, PR_BASE_SHA=949a606):
check_artifact_ids exit=0 · check_docs_freeze exit=0 · check_protected_artifacts exit=0
check_gate_meta exit=0 (вердиктов проверено: 24) · check_review_fa exit=0

# мутации: A покраснела перечнем 10 15 16 22 25 (= находка R-202 Б-1 дословно); B покраснела
# на 15 (привязка есть, словарь хрупок); C ПРОПУСТИЛА ложь (измерение предела); D доказала
# причинность Б-1. Все откачены, дерево чистое, базовая линия зелёная.
```

**Вердикт: REJECT.** Круг `R-202` исполнен полностью и честно — оба условия предъявлены
прогонами, не прозой, и правдивость обоих переписанных обоснований я перепроверил замером.
Блокирует то, что ни один из трёх прошлых кругов не мог увидеть, потому что приёмка
милестоуна зеленее CI на тринадцать джобов: после слияния `docs/DESIGN.md` §22 говорит
неправду о собственном покрытии, и агрегат `All checks passed` это красит. Цена
исправления — одна цифра; цена пропуска — невозможный merge и документ, приглашающий
достраивать уже построенное.
