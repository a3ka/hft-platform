<!-- GATE-META
milestone: M-87
audited_repo: a3ka/hft-platform
audited_base: 3a2e4607882756f660d664be5d2ba45780af0a4d
audited_head: 2a3495f1f0a33ab5c5d54a3d6b34d6de124f6a04
verdict: REJECT
-->

# R-202 — M-87 «предохранитель выдачи»: PR-гейт круга условий `R-201`

**Вердикт: REJECT. Один блокер, и он НЕ в коде.** Три блокера прошлого круга
(`R-201` Б-1/Б-2/Б-3) закрыты по существу, и закрытие предъявлено не словом, а
мутациями: четыре нейтрализации дали четыре КРАСНЫХ оракула на ожидаемых
утверждениях (§МУТАЦИОННЫЙ КОНТРОЛЬ). Переписывание оракулов состоялось — плацебо,
пойманное в `R-201`, снято в корне: эталон объёма чтения теперь ведёт ЯДРО
(`rchar` из `/proc/self/io`), а не наш же счётчик.

Блокер один: **пункт 4 условия APPROVED `R-201` («статусы §Tasks приведены к факту»)
не исполнен, и это ТРЕТИЙ круг одного класса** (`R-200` §B3 → `R-201` Н-1 → здесь).
Спека при этом не просто отстаёт — она ссылается на оракулы, УДАЛЁННЫЕ этой же
поставкой, и на дефекты, ею же снятые. Документ, по которому исполняют дальше, врёт
о предмете работы.

Предмет: `origin/feat/M-87-serving-circuit-breaker` @ `2a3495f`, 124 коммита над
`origin/main` @ `3a2e460`. `git rev-list --count 2a3495f..origin/main` → **0** — ветка
содержит весь `origin/main`, поэтому оговорка `strict:false` (`gates.md` §8) не
срабатывает: судится фактическое дерево слияния.

---

## Блок-scope — PASS

`git diff --name-only origin/main...2a3495f` → **57 файлов**, все внутри §12:

- `crates/gateway-serve/src/{lib,admission,metrics,main,test_sync}.rs` ·
  `crates/gateway-serve/src/bin/wsprobe.rs` · `crates/gateway/src/lib.rs` — зона
  engine-dev, объявлена §12;
- `crates/gateway-serve/tests/**` · `crates/gateway/tests/**` · `scripts/verify_M-87.sh` ·
  `milestones/M-87-*.md` · `docs/fa/viz-backend.md` — зона architect, объявлена §12;
- `docker-compose.yml` — объявлен §12; в круге `R-201`→`R-202` НЕ тронут (проверено:
  `git diff --name-only 1767bef..2a3495f` его не содержит), состав записи (граница C,
  `П-008`) не затрагивался;
- `research/{critiques,reviews,arbitration}/**` — артефакты гейтов; `docs/ROADMAP.md`
  (2 строки) — вне §12, но внутри глобальной зоны architect'а и требуется барьером
  `roadmap-sync`. NOTE, не нарушение (перенос из `R-201`).

`crates/contracts/**`, `crates/risk/**`, `crates/killswitch/**`, `crates/oms/**`,
`crates/venue-*/**` — не тронуты ни одним коммитом диапазона.

## Блок-C (контракты) — N/A

`crates/contracts/**` в дифе отсутствует; `GATEWAY_SCHEMA_VERSION` не бампается.
Contract-RFC не требуется.

## Блок-risk — N/A (risk-critic НЕ требуется)

`gates.md` §5 привязан к путям `risk|killswitch|oms|venue-*|contracts` — ни один не
тронут. `gateway-serve`/`gateway` — read-only консюмер журнала (`VB-I-3`,
`docs/fa/viz-backend.md:201`), order-egress отсутствует. §9 (safety-документы) не
срабатывает: тронут `docs/fa/viz-backend.md`, а не `fa/risk|killswitch|oms`.

## Блок-RED-first — PASS

`git log origin/main..2a3495f --format='%h %s' -- 'crates/*/tests/*'` → **37 коммитов**,
все с меткой `[architect]` (единственное исключение — merge-коммит `0272260`
синхронизации с `main`). В круге `R-201`→`R-202` тесты правили ровно два коммита, оба
`[architect]`: `f4576b3` (c2/c3 сняты, введён `red_m87_read_volume_truth.rs`) и
`abeae02` (оракул `c5` на второй путь записи). Пять dev-коммитов круга
(`993dc02`, `03a5b0a`, `86b7ab6`, `fc0fccd`, `a23b7c6`) трогают исключительно
`crates/*/src/**`. **Порядок соблюдён строго:** оракулы переписаны ДО фиксов —
`f4576b3` предшествует всем пяти. Это то, что `R-201` предписывал как
последовательный маршрут, и оно исполнено.

## Блок-DoneBlock — PASS

Приёмка воспроизведена независимо на СВОЁМ чистом чекауте (`/tmp/hft-reviewer-m87`,
detached `2a3495f`): **31 PASS, 1 SKIP, `VERDICT: PASS`, exit=0** — сырой вывод в
конце файла. Отчёт tester'а расхождений с моим прогоном не дал.

Отдельно о §E отчёта tester'а («восстановил обрезанный Python-скрипт мандата»):
файл `crates/gateway-serve/tests/m87_registry/mod.rs` в круге `R-201`→`R-202` НЕ
менялся, так что его обязанность 3 судила прошлое состояние; на мой вердикт она не
влияет, а восстановленная им форма даёт тот же наблюдаемый эффект (падение `r6`
статикой и `c1` фикстурой). Претензии нет.

## Блок «механизм на пути» (DoD, `gates.md` §4) — см. Н-2

Задача 24 остаётся `🟡 ЧАСТИЧНО`: бюджет вызова с прод-пути СНЯТ и не подключён
(`grep -c "feed_tail_within(" crates/gateway-serve/src/lib.rs` → 0 вызовов, только
комментарии). Это ЗАКОННЫЙ выбор — `R-201` условие 1 прямо разрешало вариант (б)
«вызов-заглушка снимается целиком, задача остаётся OPEN с TD-записью built-not-wired
severity MAJOR». Карточку завожу на close-out; merge она не блокирует.

---

# БЛОКЕР

## Б-1 — §Tasks расходится с фактом, и обоснования ссылаются на оракулы, УДАЛЁННЫЕ этой поставкой

**Где:** `milestones/M-87-serving-circuit-breaker.md:498, 509, 510, 516, 519`.

**Что требовалось:** `R-201` УСЛОВИЕ APPROVED п. 4 — «Н-1: статусы §Tasks приведены к
факту одним коммитом architect'а». Круг прошёл, статусы не приведены.

| # | статус на `2a3495f` | факт на `2a3495f` | чем факт предъявлен |
|---|---|---|---|
| 10 | `⏳ OPEN` | исполнена | `PASS task10: решение с токеном записано по каждому из 19 файлов и по двум тестам D-4` |
| 15 | `⏳ OPEN` | **остаток реален, обоснование ЛОЖНО** | обоснование ссылается на «`c2` КРАСЕН» и `snap_text.len()`; оракул `c2` УДАЛЁН коммитом `f4576b3`, `snap_text.len()` на пути счётчика отсутствует. Реальный остаток — другой (Н-1 ниже) |
| 16 | `⏳ OPEN` | статус верен, **обоснование ЛОЖНО** | ссылается на «`c3` КРАСЕН» (удалён тем же `f4576b3`) и «`read_to_end` читает сегмент целиком» (снят `86b7ab6`: streaming 64 КБ) |
| 22 | `⏳ OPEN` | **исполнена** | `1606b61`; `c1_missing_checkpoint_dir_refuses_instead_of_declaring_ready ... ok`; `R-201` Н-3 подтвердил по коду |
| 25 | `⏳ OPEN` | **исполнена** | `f1ac6a4` + `fc0fccd`; `PASS task16: глобальный счётчик слотов не теряет декременты под гонкой`; `R-201` Н-2 подтвердил |

**Почему это блокер, а не примечание.** Три довода, и ни один не про аккуратность:

1. **Спека — источник исполнения, а не отчёт.** Задачи 15 и 16 предписывают чинить
   `snap_text.len()` и `read_to_end`, которых в дереве НЕТ, ссылаясь на оракулы,
   которых НЕТ. Следующий dev, читающий §13 (а он обязан читать именно её), пойдёт
   искать снятый дефект. `CLAUDE.md`: «ошибка в спеке не падает в CI, а тихо
   тиражируется в код».
2. **Обратная ложь провоцирует повторный «фикс» верного кода.** Задачи 22 и 25
   закрыты и закреплены зелёными оракулами; `⏳ OPEN` против них есть приглашение
   переделать работающее — прямо против `scope-guard.md` §«Три жёстких правила» п. 2.
3. **Третье повторение одного класса.** `R-200` §B3 (статусы `DONE` при неготовом),
   `R-201` Н-1 (статусы `OPEN` при готовом), теперь снова. Спека САМА объявляет это
   дефектом (строка задачи 15: «Ложь в спеке в эту сторону хуже прежней»). Класс,
   воспроизводящийся третий круг при живом предупреждении, механизируется, а не
   переписывается прозой.

**Цена исправления — один коммит architect'а** (зона `milestones/**`; reviewer в неё
не пишет, `scope-guard.md`). Цена пропуска — исполнение по ложной спеке на круге,
который ГАРАНТИРОВАННО будет: задачи 10 и 16 остаются открытыми законно.

### Требуемый механизм (иначе четвёртый повтор гарантирован)

Шаг `verify_M-87.sh`, сверяющий §13 с ФАКТОМ: для каждой задачи, чей шаг приёмки
зелёный, статус `⏳ OPEN` запрещён БЕЗ явной приписки, называющей, какая ИМЕННО часть
условия не исполнена и почему зелёный шаг её не покрывает. Это ровно тот класс,
который `gates.md` §11 требует механизировать после повтора: «бэкстоп ревьюера
механизмом НЕ считается — ревью после факта, гейт до». Шаг проверяет ОДИН файл против
собственного вывода приёмки, то есть исполним без новых зависимостей.

---

# ПРИМЕЧАНИЯ (не блокируют)

**Н-1 — `read_dir` + чтение заголовков ОСТАЛИСЬ на горячем WARM-пути; дословная часть
задачи 15/23 не исполнена, и остаток задачи 15 — ЭТОТ, а не названный в спеке.**
`crates/gateway/src/lib.rs:5098` → `payload_bytes_after_cursor(dir, cursor_after)`
(`:3881`) → `journal::list_segments` → `dedup_indexed_paths` (`crates/journal/src/segments.rs:1191`:
`for entry in fs::read_dir(dir)?`) + `SegmentInfo.header`/`size_bytes` по каждому
сегменту. Задача 15 требует дословно: «`read_dir`+`metadata` уходят с горячего пути».
Не ушли. **Блокером не делаю по замеру цены:** на проде это 11 `open` + заголовки
(килобайты) против 92 ГБ описи каталога (`R-201` Б-2) и 88 МБ холостого чтения на
итерацию (`R-201` Б-1) — три порядка разницы, и величина СЧЁТЧИКА теперь честная,
что подтверждено независимым эталоном ядра. Но статус `OPEN` задачи 15 верен ПО ЭТОЙ
причине, а не по написанной, и приведение статусов (Б-1) обязано это назвать.

**Н-2 — задача 24 «built-not-wired»: карточка, а не оракул, и предел назван.**
Бюджет вызова с прод-пути снят; ОТСУТСТВИЕ бюджета не краснеет ничем — оракул `c3`,
который «проверял по имени», удалён, и сторожа у самого отсутствия нет. Что ловится:
ВОЗВРАТ холостого чтения (мутация M3 ниже — `q1` краснеет по замеру ядра). Что НЕ
ловится: то, что предохранитель по байтам/событиям так и не ограничивает дренаж.
Это не изображается механизмом: на close-out завожу карточку **severity MAJOR**
(`gates.md` §4 DoD). Обратный вопрос мутационного контроля (`testing.md`: «что
пришлось ослабить рядом») отвечен честно — ослаблен сам предохранитель, сторож у
ослабления — запись долга.

**Н-3 — §11 п. 5 милестоуна: экспорт счётчиков наружу процесса не строится** →
вторая карточка «built-not-wired» MAJOR на close-out. Сторож молчания построен,
наружу не выведен; внешняя тревога (`PL-I-8`) его не видит.

**Н-4 — SKIP `task9` остаётся неснятым и обязателен на post-merge гейте §8.**
`deploy.resources.limits` в `docker compose up` (не Swarm) ИГНОРИРУЕТСЯ; замер
2026-09-21 дал `NanoCpus=0 Memory=0` при живом описании сервисов. При нулях в
`docker inspect` — третья карточка «лимиты объявлены, не применены».

**Н-5 — аллокатор идентификаторов выдал ЗАНЯТЫЙ номер.** `scripts/next_artifact_id.sh TD`
→ `TD-218`, но `TD-218` уже занят строкой таблицы `TECH-DEBT.md:170`. Причина: у этой
записи нет заводящей карточки-слага ``- **TD-218** `слаг` ``, а аллокатор считает
максимум именно по ним. `gates.md` §12 требует брать номер механизмом — механизм
здесь соврал бы. Карточки close-out'а беру `TD-219`/`TD-220`, а форму записи
`TD-218` (слаг-карточку) достраиваю сам: `TECH-DEBT.md` — моя зона.

**Н-6 — §9 перепроверка.** Диф трогает `docs/fa/viz-backend.md` и
`scripts/verify_M-87.sh` — уставную зону. В цепочке есть вердикты сильной модели со
свежим контекстом (`C-245`…`C-254`, `A-037`…`A-040`); отдельного артефакта §9 по
FA-правке нет, правка производна от `A-037` D-4 и инвариантов не ослабляет — блокером
не делаю (перенос позиции `R-201` Н-6), но в close-out назвать обязательно.

**Н-7 — барьеры прогнаны в ПРОД-ФОРМЕ вызова** (`EVENT_NAME=pull_request`,
`PR_BASE_SHA=3a2e460`), а не в удобной: `check_artifact_ids.sh` → `OK: ни один коммит
диапазона не ввёл второй носитель под занятым идентификатором`;
`check_docs_freeze.sh` → молчание (зона §11 не тронута).

---

# МУТАЦИОННЫЙ КОНТРОЛЬ — переписывание оракулов СОСТОЯЛОСЬ

`testing.md` §«Мутационный контроль». Четыре мутации на чистом чекауте
`/tmp/hft-reviewer-m87` @ `2a3495f`, каждая откачена; после откатов дерево чистое и
оба набора зелёные. Именно эти мутации `R-201` предъявил как ПРОХОДЯЩИЕ прежние
оракулы — теперь они краснеют.

## M1 — счётчик кормится размером КАТАЛОГА (регресс `R-201` Б-2)

```
$ sed -i 's|payload_bytes_read: ckpt_bytes_read.saturating_add(tail_bytes_read),|payload_bytes_read: payload_bytes_for_dir(dir).unwrap_or(0),|' crates/gateway/src/lib.rs
$ git diff --numstat crates/gateway/src/lib.rs
1	1	crates/gateway/src/lib.rs

$ cargo test -p gateway-serve --test red_m87_read_volume_truth
test q_read_volume_and_counter_tell_the_truth ... FAILED
panicked at crates/gateway-serve/tests/red_m87_read_volume_truth.rs:281:5:
счётчик выдачи объявил 194526 Б прочитанными, а ядро видело только 57711 Б. Учесть
больше, чем реально прочитано, невозможно — значит величина не измеряется, а СОЧИНЯЕТСЯ
test result: FAILED. 0 passed; 1 failed
rc=101
```

## M2 — счётчик = литерал `1` (мутация, ПРОХОДИВШАЯ прежний оракул `R-201`)

```
$ sed -i 's|payload_bytes_read: ckpt_bytes_read.saturating_add(tail_bytes_read),|payload_bytes_read: 1,|' crates/gateway/src/lib.rs
$ cargo test -p gateway-serve --test red_m87_read_volume_truth
panicked at red_m87_read_volume_truth.rs:287:5:
счётчик выдачи объявил 1 Б, а один только хвост весит 466 Б.
test result: FAILED. 0 passed; 1 failed
rc=101
```

**Это главный результат круга.** В `R-201` та же мутация не покраснила НИ ОДНОГО
оракула («главная цифра милестоуна заменена литералом `1` — ни один оракул не
покраснел»). Теперь она ловится нижней границей, а M1 — верхней. Эталон независимый:
`rchar` ядра, не наш счётчик (`testing.md` §«Зависимый эталон мутация ловит плохо»).

## M3 — возврат снятого вызова-заглушки бюджета (регресс `R-201` Б-1)

```
$ python3 …  # восстановлен блок `let _budget_guard = { … feed_tail_within(…) }` в drain-цикле
$ git diff --numstat crates/gateway-serve/src/lib.rs
19	0	crates/gateway-serve/src/lib.rs

$ cargo test -p gateway-serve --test red_m87_read_volume_truth
panicked at red_m87_read_volume_truth.rs:265:5:
ФАКТИЧЕСКИ прочитано 448846 Б при каталоге 194498 Б (порог 97249). Тёплый слепок
покрывает тело журнала — обслуживание обязано читать слепок и ХВОСТ (466 Б), а не
обходить все 24 сегментов. Замер независим от наших счётчиков: это `rchar` из /proc/self/io
test result: FAILED. 0 passed; 1 failed
rc=101
```

Условие 1 `R-201` требовало переписать `c3` «на проверку ПО РАБОТЕ». Исполнено
сильнее буквы: `c3` удалён, а холостое чтение теперь ловится не счётом строк, а
замером ЯДРА — то есть проверкой работы, а не имени.

## M4 — третий путь записи в счётчик слотов (регресс задачи 27)

```
$ python3 …  # возвращена `pub fn set_slots_in_flight_pub(v) { SLOTS_IN_FLIGHT_GLOBAL.store(v, SeqCst) }`
$ git diff --numstat crates/gateway-serve/src/metrics.rs
4	0	crates/gateway-serve/src/metrics.rs

$ cargo test -p gateway-serve --test red_m87_r196_conditions
test c5_slot_counter_has_exactly_two_writers ... FAILED
panicked at red_m87_r196_conditions.rs:180:5: путей записи в счётчик занятых слотов
должно быть РОВНО ДВА …, найдено 3
test result: FAILED. 2 passed; 1 failed
rc=101
```

## Откат и базовая линия

```
$ git checkout -- crates/gateway/src/lib.rs crates/gateway-serve/src/{lib,metrics}.rs
$ git status --porcelain
{пусто}
$ cargo test -p gateway-serve --test red_m87_read_volume_truth --test red_m87_r196_conditions
test result: ok. 3 passed; 0 failed   (c1, c4, c5)
test result: ok. 1 passed; 0 failed   (q)
rc=0
```

---

# УСЛОВИЕ APPROVED

1. **Б-1:** §13 приведена к факту ОДНИМ коммитом architect'а — статусы задач 10/22/25
   на `✅ DONE` (либо явная приписка, почему зелёный шаг не покрывает условие),
   обоснования задач 15 и 16 переписаны по НЫНЕШНЕМУ дереву: ссылки на `c2`/`c3`
   снять (оракулов нет), `snap_text.len()`/`read_to_end` снять (дефектов нет),
   остаток задачи 15 назвать по Н-1 (`read_dir` + заголовки на WARM-пути через
   `payload_bytes_after_cursor`), остаток задачи 16 — «бюджет не подключён, долг MAJOR».
2. **Механизм к Б-1:** шаг `verify_M-87.sh`, красящийся на «зелёный шаг приёмки при
   статусе `OPEN` без названной причины». Третий повтор класса прозой не закрывается
   (`gates.md` §11).
3. Приёмка перепрогоняется целиком после правки; новый шаг обязан ПОКРАСНЕТЬ на
   `2a3495f` (до правки статусов) — иначе он ничего не пиннит.

**Кому:** `architect`, один адресат, зона `milestones/M-87-*.md` + `scripts/verify_M-87.sh`.
Dev-работы в этом круге НЕТ — код закрыт, и трогать его незачем.

Что НЕ требуется переделывать (предъявлено мутациями, повторный круг по коду был бы
вредом): Б-1/Б-2/Б-3 `R-201`, задачи 22/23/25/26/27.

---

# Предъявление FA (M-66)

Диф трогает `crates/gateway-serve/**` и `crates/gateway/**`; FA зоны —
`docs/fa/viz-backend.md` (собственного `fa/gateway-serve.md` не существует — факт,
сам по себе долг, перенос `R-201`). Живые инварианты на проверяемой ревизии,
использованные по существу:

- **`GW-I-9(б)`** (`docs/fa/viz-backend.md:71`) — «слепок есть кэш, его потеря не
  останавливает исправную выдачу» (уточнён этой поставкой по `A-037` D-4). **Опора
  Н-1 и M1/M3:** WARM-путь обязан читать слепок и хвост, а не каталог; оракул `q`
  меряет именно это границей ядра.
- **`VB-I-10`** (`:213`) — «Bounded-window snapshot (M-37, `TD-039`). Память
  `snapshot`/`frames_since` ограничена ОКНОМ `[at−W, at]` …, иначе host-OOM (прод:
  RSS 7.3GB)». **Опора M3:** возврат холостого обхода сегментов на путь инициализации
  каждой сессии есть нарушение этой границы, и он краснеет.
- **`VB-I-11`** (`:214`) — «Провенанс ИСТОРИИ (M-48, `TD-048`) … система обязана НЕ
  ВЫДАВАТЬ ЭТО ЗА ДРУГОЕ». **Опора Б-1 по классу:** спека, объявляющая исполненное
  неисполненным и снятый дефект живым, выдаёт одно за другое — тот же класс
  нечестности, перенесённый с данных на документ.
- `VB-I-3` (`:201`) — read-only Read Gateway; проверено, что диф его не нарушает
  (order-egress и journal-writer отсутствуют) — основание «Блок-risk: N/A».

**Ярус C — что искал грепом** (`reading-map.md` §2): `TECH-DEBT.md` по `TD-207`,
`TD-011`, `TD-202`, `TD-217`, `TD-21[5-9]`, `built-not-wired`, `gateway-serve`;
`PROJECT-STATE.md` по `M-87`, `gateway-serve`, `предохранител`. Найденное и
относящееся к предмету: `TD-207` (зонд `wsprobe` против прод-формы секрета) —
закрывается задачами 7/11, шаг `task11` зелёный, закрытие карточки на close-out ПОСЛЕ
устранения Б-1; `TD-218` — дефект ФОРМЫ записи (нет заводящей карточки, Н-5).

---

# Done Block — приёмка на СВОЁМ чистом чекауте

```
$ pwd; git log -1 --oneline
/tmp/hft-reviewer-m87
2a3495f docs(M-87): задача 27 закрыта — дверь заложена, а не сторожится [architect]

$ git status --porcelain
{пусто}

$ git rev-list --count 2a3495f..origin/main        # ветка содержит весь main
0

$ timeout 2400 bash scripts/verify_M-87.sh
… PASS × 31 …
SKIP  task9: ФАКТИЧЕСКИ применённые лимиты, запас для recorder'а и поведение НА лимите
      снимаются на проде (docker inspect) — шаг деплой-гейта §8
VERDICT: PASS
exit=0

$ grep -cE "^PASS" /tmp/r202-verify.txt
31

# CI-паритет внутри verify:
PASS  CI-паритет: cargo fmt --all -- --check
PASS  CI-паритет: cargo clippy --all-targets --all-features -- -D warnings
PASS  CI-паритет: cargo test --all
PASS  D-1(б): библиотечный корпус зелен — passed=258 failed=0

# Барьеры в прод-форме вызова (EVENT_NAME=pull_request, PR_BASE_SHA=3a2e460):
$ bash scripts/check_artifact_ids.sh
OK: ни один коммит диапазона 3a2e460..HEAD не ввёл второй носитель под занятым идентификатором

# Мутации: 4 из 4 покраснели на ожидаемых оракулах (§МУТАЦИОННЫЙ КОНТРОЛЬ), откачены,
# дерево чистое, базовая линия зелёная.
```

**Вердикт: REJECT.** Код круга принят по существу и закреплён мутациями; блокирует
единственное — §Tasks обязана говорить правду о себе, потому что по ней исполняют
следующий круг.
