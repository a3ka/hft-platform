<!-- GATE-META
milestone: R-092
audited_repo: a3ka/hft-platform
audited_base: 147fc5afa8abd1cd19f534a34a6eebfd13ff89ef
audited_head: 7e7329cb44dabfe02d6b570e3c9398edf06a706e
verdict: APPROVE
-->

# R-092 — перепроверка `gates.md` §9: `review-fa` объявлен работающим — снято (`7e7329c`)

- **Роль:** независимый перепроверяющий по `gates.md` §9, СВЕЖИЙ контекст. Автор правки —
  architect — в этом предмете СТОРОНА и себя не проверял.
- **Предмет:** `origin/docs/TD-155-review-fa-cognitive-only`, коммит `7e7329c`; десять файлов
  зоны §11 (`.claude/rules/gates.md` +3/−3, девять профилей `.claude/agents/*.md` по +14/−3).
- **База аудита:** merge-base `147fc5a`; проверка выполнена И на ветке, И на **дереве
  слияния** с `origin/main` (`127e768`) — `gates.md` §8.
- **Вердикт: APPROVE.** Проверено **22** утверждения о коде, **неверных — 0**. Непокрытых
  мест, всё ещё объявляющих барьер действующим, **не найдено**. Три замечания ниже — NOTE,
  ни одно не блокирует.

---

## §1 — (а) Утверждения о коде: 22 проверено, 0 неверных

Каждое утверждение предъявлено командой и её кодом возврата. Прогон на дереве слияния
(`git merge-tree --write-tree origin/main HEAD` → `d7874d7`, материализовано в `/tmp/mtree`).

### Ядро правки — барьера в `main` нет

| # | утверждение | команда | итог |
|---|---|---|---|
| 1 | `scripts/check_review_fa.sh` в `main` отсутствует | `git cat-file -e origin/main:scripts/check_review_fa.sh` | `fatal: path ... does not exist`, exit=128 — **ВЕРНО** |
| 2 | джоб `review-fa` в `main`-`ci.yml` отсутствует | `git show origin/main:.github/workflows/ci.yml \| grep -n review-fa` | пусто, exit=1 — **ВЕРНО** |
| 3 | в `main`-`ci.yml` 12 джобов | `grep -cE '^  [a-zA-Z_][a-zA-Z0-9_-]*:$'` → 14 | 14 − 2 (`pull_request:`, `push:` — триггеры под `on:`) = **12, ВЕРНО** |

Перечень джобов `main`: `build-test security delivery protected-artifacts docs-freeze
contracts artifact-ids reserve-ids design-claims context-budgets gate-meta status-check` —
`review-fa` в нём нет.

### Барьер существует на `feat/M-66-fixture` — и ровно в заявленном объёме

| # | утверждение | итог |
|---|---|---|
| 4 | `check_review_fa.sh` — **311** строк | `git show origin/feat/M-66-fixture:… \| wc -l` → `311` — **ВЕРНО** |
| 5 | проба `red_review_fa.sh` — **1325** строк | → `1325` — **ВЕРНО** |
| 6 | джоб в `ci.yml:231` | `231:  review-fa:` — **ВЕРНО** |
| 7 | вшит в `needs` агрегата (`:274`) | `274:    needs: [… design-claims, review-fa]` — **ВЕРНО** |
| 8 | вшит в fail-closed `if` агрегата (`:278`) | `278: … \|\| "${{ needs.review-fa.result }}" != "success" …` — **ВЕРНО** |

Это точное попадание по всем пяти числам, включая обе точки проводки. Заявление автора, что
барьер «написан ЦЕЛИКОМ», а не «не написан», подтверждается: джоб не просто объявлен, но и
включён в `needs`, и в условие агрегата — то есть не страдает дырой `C-082` B-3.

### Статус PR #6 и происхождение расхождения

| # | утверждение | итог |
|---|---|---|
| 9 | PR #6 открыт, head = `feat/M-66-fixture` | `gh pr view 6` → `"state":"OPEN"`, `"headRefName":"feat/M-66-fixture"` — **ВЕРНО** |
| 10 | `C-089` — REJECT | `research/critiques/C-089-M-66-rev4.md` на ветке, `verdict: REJECT` / `## Verdict: REJECT` — **ВЕРНО** |
| 11 | у PR #6 конфликты | `"mergeable":"CONFLICTING"`, `"mergeStateStatus":"DIRTY"`; независимо `git merge-tree --write-tree origin/main origin/feat/M-66-fixture` → exit=1 — **ВЕРНО** |
| 12 | задача 5 M-66 влита в `main` отдельно (`965a1f5`) | `git merge-base --is-ancestor 965a1f5 origin/main` → exit=0 — **ВЕРНО** |
| 13 | тело `965a1f5` утверждает «Механизм существовал и работал» | `git log -1 --format=%B 965a1f5` → строка 5 — **ВЕРНО** |

Корень назван автором точно и подтверждается: правка ДЕВЯТИ профилей (задача 5) уехала в
`main`, а механизм остался на ветке. Это ровно класс `gates.md` §8 «документ проверяется на
ДЕРЕВЕ СЛИЯНИЯ» — тело `965a1f5` правдиво про ветку и ложно про `main`.

### Полнота: «десять мест», а не два

| # | утверждение | итог |
|---|---|---|
| 14 | мест было ДЕСЯТЬ, не два | `git grep -l 'Проверяется машинно джобом \`review-fa\`' 7e7329c^ -- .claude/` → **9** профилей + `gates.md:183` (`Барьер — \`scripts/check_review_fa.sh\`, джоб \`review-fa\`.`) = **10, ВЕРНО** |
| 15 | блок в девяти профилях идентичен | md5 блока по девяти файлам на `7e7329c^` → **одна** сумма `495f5085`, count=9 — **ВЕРНО** |

Замечу отдельно в пользу автора: карточка `TD-155` (`R-088`) сузила предмет до ДВУХ мест;
автор правил по замеру, а не по карточке, и замер верен. Это ровно та дисциплина, которой
`CLAUDE.md` требует «правило цитаты».

### Бюджет впрыскиваемого ядра

| # | утверждение | итог |
|---|---|---|
| 16 | `gates.md` ДО правки — 47891 B | `git cat-file -s 7e7329c^:.claude/rules/gates.md` → `47891` — **ВЕРНО** |
| 17 | `gates.md` ПОСЛЕ — 47883 B, лимит 47900 | прогон → `OK .claude/rules/gates.md 47883 B / 47900 B (запас 17 B)` — **ВЕРНО** |
| 18 | общий бюджет PASS, запас 2926 B | `VERDICT: PASS — 7 файлов, 111974 B из 114900 B (запас 2926 B)`, exit=0 — **ВЕРНО** |
| 19 | уложился ВЫЧИСТКОЙ, а не поднятием лимита | `git diff --stat origin/main HEAD -- scripts/check_context_budgets.sh` → **пусто**; в numstat коммита скрипта нет — **ВЕРНО** |
| 20 | тот же результат на дереве слияния | прогон в `/tmp/mtree` → идентичный `VERDICT: PASS … запас 2926 B`, exit=0 — **ВЕРНО** |

Пункт 19 — существенный: автор утверждал соблюдение бюджета вычисткой, и это проверяемо
именно отсутствием правки лимита. Диф коммита трогает ровно 10 файлов, `check_context_budgets.sh`
среди них нет. Убранная строка («Механизм не заставляет читать — он делает НЕЧТЕНИЕ
наблюдаемым») описывала несуществующий механизм — её удаление не теряет норму, а снимает
ложь; нормативное предложение и основание («FA названа в 0 из 3 вердиктов; `TD-138`») на
месте, проверено чтением `gates.md:180-186` на дереве слияния.

## §2 — (б) Полномочия: зона §11, токен `FOUNDER-APPROVED`

| # | утверждение | итог |
|---|---|---|
| 21 | токен есть и в требуемой ФОРМЕ | чтением `scripts/check_docs_freeze.sh:61-77` — **ВЕРНО** |
| 22 | барьер `docs-freeze` проходит | `bash scripts/check_docs_freeze.sh 147fc5a` → **exit=0** — **ВЕРНО** |

Проверено ЧТЕНИЕМ, а не догадкой. `in_zone()` (`:61-67`) покрывает `.claude/rules/*` и
`.claude/agents/*` — обе тронутые подзоны в зоне замка, токен обязателен. `tok()` (`:69-77`)
требует `^FOUNDER-APPROVED: .{12,}` **в ТЕЛЕ** коммита (`git log -1 --format='%b'`), намеренно
не по `git show` — иначе токен внутри диффа авторизовал бы сам себя (`A-005` §4 #1). Тело
`7e7329c` несёт с начала строки: `FOUNDER-APPROVED: founder 2026-08-17 поручил дешёвую
половину TD-155 — снять утверждение о машинной проверке, барьер не писать` — причина много
длиннее 12 символов. Семантика барьера ПОКОММИТНАЯ; коммит в диапазоне один и токен несёт он
сам, так что реверт-пары здесь нет.

**Про `bash scripts/check_docs_freeze.sh origin/main` — отдельно, потому что он КРАСНЫЙ:**
```
FAIL  база 'origin/main' НЕ предок HEAD — история переписана (force-push); …
exit=1
```
Это НЕ дефект токена и не находка против правки: ветка просто отстала от `origin/main`
(merge-base `147fc5a` ≠ `origin/main` `127e768`), поэтому `origin/main` не предок её HEAD.
Против настоящей базы барьер зелёный (exit=0, строка выше). В CI ложного красного не будет:
джоб берёт базу ИЗ СОБЫТИЯ (`ci.yml:98-105`, `PR_BASE_SHA`), а `actions/checkout@v4` на
`pull_request` выкладывает merge-ref, где база — предок HEAD. Проверено, а не предположено:
`git merge-tree --write-tree origin/main HEAD` → exit=0, конфликтов нет.

## §3 — (в) Связность: непокрытых мест НЕТ

`grep -rn 'review-fa\|check_review_fa'` по ВСЕМУ дереву слияния. Разбор каждого попадания:

| место | что говорит | вердикт |
|---|---|---|
| `.claude/rules/gates.md:183` + девять `.claude/agents/*.md` | «барьера в `main` НЕТ», `COGNITIVE-ONLY` | **исправлено, все 10** |
| `docs/workflow/reading-map.md:103` | «построен на `feat/M-66-fixture`: проба 40/40, четыре круга критика» | **уже верно** — про ветку, не про `main`; не объявляет барьер действующим |
| `docs/SESSION-HANDOFF.md:20` | строка о ветке `docs/M-66-protocol-attestation`: «НЕ сделана задача 3 — джоб `review-fa` в `ci.yml`» | **не в классе дефекта** — прямо говорит, что джоба нет |
| `research/reviews/R-087…:42,209-227,489` | F-6: «`review-fa` объявлен механизированным, но его НЕТ» | **исторический вердикт**, содержит верное отрицание; аудит-трейл не правится |
| `research/reviews/R-088…:212` | заводит `TD-155` | исторический вердикт |
| `TECH-DEBT.md:32,778-811` | карточка `TD-155` | см. **N-2** ниже |

Ни одно место в корпусе больше не объявляет барьер действующим машинным. Заявленная полнота
(«десять мест») подтверждена и в обратную сторону: одиннадцатого не существует.

## §4 — (5) Оценка по существу: не создаёт ли новая редакция ЛОЖНОЙ уверенности

Спрошено прямо, отвечаю прямо. **По существу — нет, и в девяти профилях сделано образцово.**

Текст профилей называет статус без прикрас: «Барьер НАПИСАН и живёт на ветке
`feat/M-66-fixture` (PR #6, **открыт, `C-089` REJECT, конфликты**)». Все три неудобных факта
названы, а не обойдены — проверено выше пунктами 9-11, все три ВЕРНЫ. Никакого «вот-вот
будет» текст не обещает: слова о сроке, плане или скорой посадке отсутствуют. Более того,
раздел прямо формулирует, ПОЧЕМУ ложная механизация хуже честного отсутствия («отсутствующий
барьер, названный отсутствующим, не создаёт ложной уверенности») — то есть правка лечит класс
дефекта, а не только его экземпляр.

**N-1 (NOTE, `gates.md`).** В `gates.md:183` осталось только «(PR #6)» — без «REJECT» и
«конфликты», которые есть в профилях. Читатель одного `gates.md` может прочесть «лежит в PR»
как «на подходе». Не блокирую по трём причинам: несущее утверждение («барьера в `main` НЕТ»,
«держится на дисциплине») верно и стоит первым; запас файла — **17 B**, расширять physически
нечем без встречной вычистки; девять профилей, которые роли читают по startup-протоколу,
несут полную формулировку. Если правка `gates.md` случится по другому поводу — стоит
дописать два слова.

**N-2 (NOTE, вне зоны автора — адресовать reviewer'у).** Карточка `TD-155`
(`TECH-DEBT.md:809-811`) по-прежнему гласит: «Дорогая половина (**написать**
`check_review_fa.sh` + джоб) — по общему условию разморозки». Автор сам установил, что это
ФАКТИЧЕСКИ НЕВЕРНО («писать нечего — вопрос в доведении PR #6»), и это подтверждено моими
пунктами 4-8. Риск конкретный и ровно тот, против которого введено «правило предшественника»:
следующий агент прочтёт карточку и начнёт писать 311+1325 строк заново. **Автор не виноват и
поступил верно:** `TECH-DEBT.md` reviewer-owned (`CLAUDE.md` cross-refs; `scope-guard.md` —
строка reviewer'а), architect в него не пишет — тот же прецедент, что `gates.md` §11 о
`TD-124`. Но названо это ТОЛЬКО в теле коммита, которое следующий агент не читает. Поэтому:
reviewer при ближайшем close-out обязан переформулировать «дорогую половину» `TD-155` в
«довести PR #6 до зелёного и влить» и снять «написать».

**N-3 (NOTE, гигиена).** Ветка отстала от `origin/main` (`147fc5a` vs `127e768`). Здесь это
безвредно и проверено: `origin/main` с merge-base НЕ трогал ни один файл ядра
(`git diff --numstat 147fc5a origin/main -- .claude/rules/ .claude/agents/ CLAUDE.md
docs/04-workflow.md scripts/check_context_budgets.sh` → **пусто**), merge чистый, бюджет на
дереве слияния совпал побайтно. Но при `strict: false` (`gates.md` §8) зелёный чек снимается
на СТАРОЙ базе — а запас `gates.md` равен 17 B. Если до merge'а в `main` приедет чужая правка
`gates.md`, бюджет может лопнуть уже ПОСЛЕ слияния. Перед merge'ем стоит перепрогнать
`check_context_budgets.sh` на дереве слияния (я это сделал — сейчас PASS).

## §5 — Что я НЕ проверял

- Содержательное качество самого барьера на `feat/M-66-fixture` (311/1325 строк) — предмет
  `C-089`, не мой; я проверял только ФАКТ его существования и объём.
- `FA-WAIVER`: диф не трогает `crates/**` (numstat — только `.claude/**`), предъявление
  FA-инварианта неприменимо. `FA-WAIVER: N/A — диф не трогает crates/**`.

## Done Block

```
$ git merge-base origin/main origin/docs/TD-155-review-fa-cognitive-only
147fc5afa8abd1cd19f534a34a6eebfd13ff89ef

$ git show --numstat --format='' 7e7329c | awk '{a+=$1;d+=$2;n++} END{print n" файлов +"a"/-"d}'
10 файлов +129/-30

$ git cat-file -e origin/main:scripts/check_review_fa.sh; echo exit=$?
fatal: path 'scripts/check_review_fa.sh' does not exist in 'origin/main'
exit=128

$ git show origin/main:.github/workflows/ci.yml | grep -c 'review-fa'; echo exit=$?
0
exit=1

$ git show origin/feat/M-66-fixture:scripts/check_review_fa.sh | wc -l
311
$ git show origin/feat/M-66-fixture:scripts/tests/red_review_fa.sh | wc -l
1325
$ git show origin/feat/M-66-fixture:.github/workflows/ci.yml | grep -n 'review-fa'
231:  review-fa:
274:    needs: [build-test, security, delivery, protected-artifacts, contracts, docs-freeze, artifact-ids, design-claims, review-fa]
278:          if [[ ... || "${{ needs.review-fa.result }}" != "success" ]]; then

$ gh pr view 6 --json state,mergeable,mergeStateStatus,headRefName
{"headRefName":"feat/M-66-fixture","mergeStateStatus":"DIRTY","mergeable":"CONFLICTING","state":"OPEN"}

$ git merge-base --is-ancestor 965a1f5 origin/main; echo exit=$?
exit=0

$ git grep -l 'Проверяется машинно джобом `review-fa`' 7e7329c^ -- .claude/ | wc -l
9

$ bash scripts/check_docs_freeze.sh 147fc5a; echo exit=$?
exit=0

$ bash scripts/check_context_budgets.sh | tail -3; echo exit=$?
OK    .claude/rules/gates.md                     47883 B /   47900 B (запас 17 B)
VERDICT: PASS — 7 файлов, 111974 B из 114900 B бюджета (запас 2926 B)
exit=0

$ cd /tmp/mtree && bash scripts/check_context_budgets.sh | tail -1; echo exit=$?
VERDICT: PASS — 7 файлов, 111974 B из 114900 B бюджета (запас 2926 B)
exit=0

$ git merge-tree --write-tree origin/main HEAD >/dev/null; echo merge_exit=$?
merge_exit=0

$ bash scripts/next_artifact_id.sh R
R-092
```

## Итог

**APPROVE.** Правка снимает ложное утверждение о машинном барьере во всех ДЕСЯТИ местах, где
оно жило, оставляет норму в силе и честно маркирует её `COGNITIVE-ONLY`. 22 утверждения о
коде проверены командами на дереве слияния — **неверных нет**, включая пять точных чисел про
барьер на `feat/M-66-fixture`. Полномочия в порядке: зона §11, токен `FOUNDER-APPROVED` в
требуемой форме, барьер `docs-freeze` зелёный против настоящей базы. Бюджет ядра соблюдён
вычисткой, лимит не тронут. Непокрытых мест нет. N-1 и N-3 — замечания к форме, N-2
адресована reviewer'у как владельцу `TECH-DEBT.md`.
