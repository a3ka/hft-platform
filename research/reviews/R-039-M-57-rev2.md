# R-039 — PR-гейт M-57, круг 2 (`feat/M-57-task5` @ `c11a610`): **REJECT**

**Дата:** 2026-08-06 · **Роль:** reviewer (PR-гейт, `gates.md` §4 UNCONDITIONAL)
**Предмет:** `milestones/M-57-tail-follower.md` §7–§8 (круг 2 после `R-035`) — курсор хвоста
в память сессии, файловый sidecar удалён.
**Проверено на:** свой worktree `/tmp/hft-rev-m57t5`, detached `c11a610`, дерево чистое.
**Предыдущие вердикты в цепочке:** `C-059` (REJECT) · `C-060` (REJECT) · `C-063` ·
`A-004` (арбитр) · `R-035` (REJECT, два блокера) · tester round 2 (PASS).

> **Номер вердикта.** `R-036`/`R-037` заняты в `main` (аудит воркфлоу), `R-038` занят
> ДВАЖДЫ на чужих ветках (`R-038-M-59.md`, `R-038-M-60a-arbiter-trigger.md`) — обе не в
> `main`. Беру `R-039`, чтобы не плодить третью коллизию. См. `NOTE-1`.

---

## §A — Вердикт

**REJECT.** Механизм круга 2 РАБОТАЕТ (доказано мутацией, §C.1), но **acceptance-гейт
milestone'а зелёный против возврата P0-дефекта** — проверено прогоном, не рассуждением:
композитная мутация из двух строк возвращает полное пересканирование активного сегмента
на каждом тике И оставляет `verify_M-57.sh` **VERDICT: PASS (11/11, exit=0)**, а оба
новых прод-форменных оракула `f035_1`/`f035_2` — **зелёными**.

Это ровно то, о чём предупреждал `R-035` §H п.3 (`F-035-3`), и что осталось незакрытым:
задача 8 milestone'а — `⏳ BLOCKED`. После круга 2 аргумент «гейт держится и без этого»
(`R-035` §F) **перестал быть верным**: T3 живёт в крейте `journal` и pump'а не касается,
поэтому единственным зубом гейта против P0 остался `td083`, чьи зубы заёмные.

Merge заблокирован до устранения F-1..F-4 (F-5..F-7 — обязательны к исполнению, но сами
по себе merge не блокируют).

---

## §B — Прогоны на предмете (сырой вывод)

```
$ pwd; git log -1 --format='%h %s'; git status --porcelain
/tmp/hft-rev-m57t5
c11a610 feat(M-57): task #9 — sidecar удалён целиком; три prod-вызова stream_from явно не покрыты [engine-dev]
(пусто)

$ cargo fmt --all -- --check;                                            exit=0
$ cargo clippy --all-targets --all-features -- -D warnings;              exit=0
$ bash scripts/verify_M-57.sh                                            → VERDICT: PASS, exit=0
    PASS T0 · T1 build --workspace · T2 clippy · T2b fmt · T3 O-1..O-4 · T4 ×2 · T5 ×3 · T6   (11/11)
$ cargo test --workspace | grep "^test result" | awk …
passed=797 failed=0 (блоков: 194)                                        exit=0
$ cargo test -p gateway --test red_tail_cursor_prod_form
test f035_1_tail_cursor_survives_readonly_journal_dir ... ok
test f035_2_two_sessions_do_not_share_one_cursor ... ok
test result: ok. 2 passed; 0 failed                                      exit=0
```

Числа tester'а воспроизведены полностью (797/797, verify 11/11, f035 2/2) — расхождений нет.
Done Block'и dev'а и tester'а — сырой stdout с exit-кодами; `commit-discipline.md` соблюдён.

---

## §C — Мутационный контроль (замер, не рассуждение)

### C.1 — Мутация 1: механизм круга 2 откачен (`pump` → `stream_from`)

`crates/gateway/src/lib.rs:3095` — `journal::stream_from_at(dir, filter, after, self.tail_hint)`
заменено на `journal::stream_from(dir, filter, after)`. Смысл: P0-дефект возвращается
ЦЕЛИКОМ (после удаления sidecar `stream_from` всегда сканирует активный сегмент с начала).

```
$ bash scripts/verify_M-57.sh
PASS  T3 O-1..O-4 GREEN                    ← журнальные оракулы НЕ ВИДЯТ дефект
FAIL  T5 РЕГРЕСС в M-53/M-54/M-56
  test td083_tick_wallclock_does_not_grow_with_history ... FAILED
VERDICT: FAIL (1 нарушений)                exit=1

$ cargo test -p gateway --test red_tail_cursor_prod_form
f035_1 ... FAILED  «тик ПРОЧИТАЛ 8003 событий при приращении 3»
f035_2 ... FAILED  «худший тик прочитал A=8009, B=8009 при бюджете 12»
test result: FAILED. 0 passed; 2 failed    exit=101
```

**Что доказано:** (а) механизм круга 2 реально несёт эффект — числа совпали с замером прода
из `R-035` §D (8003 / 8009) до единиц; (б) оракулы `f035_1`/`f035_2` имеют зубы; (в) **гейт
их не гоняет** — красным он стал ТОЛЬКО через `td083`, а `f035_*` в `verify_M-57.sh` не
упоминаются вовсе (`grep -c red_tail_cursor_prod_form scripts/verify_M-57.sh` → **0**).

### C.2 — Мутация 1+2: композит (тот же откат + подмена проброса)

Дополнительно `crates/gateway/src/lib.rs:1973`: `events_scanned: stream.events_scanned()`
→ `stream.events_decoded()` — ровно `F-035-3` из `R-035` §F, одна строка.

```
$ bash scripts/verify_M-57.sh
PASS  T0 · T1 · T2 · T2b · T3 · T4 ×2 · T5 ×3 · T6
VERDICT: PASS                              exit=0      ← 11/11 ПРОТИВ ВЕРНУВШЕГОСЯ P0-ДЕФЕКТА

$ cargo test -p gateway --test red_tail_cursor_prod_form
f035_1 ... ok
f035_2 ... ok
test result: ok. 2 passed; 0 failed        exit=0      ← прод-форменные оракулы ТОЖЕ зелёные
```

**Что доказано:** ВСЕ оракулы M-57 уровня gateway — включая два новых, написанных именно
против прод-формы, — читают ОДНО И ТО ЖЕ поле `gateway::ReadStats.events_scanned`, а само
это поле не пиннит ни один оракул. Две строки правки возвращают дефект, ради которого
существует milestone, и оставляют весь гейт зелёным. `testing.md`: «зависимый эталон мутация
ловит плохо» и «оракул обязан мерить ТО, ЧТО обещает».

Обе мутации откачены; после отката `git status --porcelain` пуст, `git diff` пуст.

---

## §D — Блокеры

### F-1 (BLOCKER, целостность гейта) — оракулы круга 2 не подключены к acceptance-гейту

`scripts/verify_M-57.sh` не исполняет `crates/gateway/tests/red_tail_cursor_prod_form.rs`
ни одним шагом: T3 гоняет `-p journal --test red_tail_scan_bounded`; T5 — `-p journal`,
`-p gateway --test red_frames_seek_bound --test red_push_seek_bounded --test
red_connect_cost_single --test red_snapshot_noclone`, `-p gateway-serve`. Целиком
`cargo test -p gateway` (который подхватил бы `f035_*`) не запускается нигде, `cargo test
--workspace/--all` в скрипте отсутствует.

Следствия:
1. `gates.md` §3 «минимум 1 проверка на задачу из §Tasks» нарушен: задача 6 в §8 milestone'а
   названа оракулами `f035_1`/`f035_2` — гейт их не исполняет.
2. `gates.md` §3 «паритет с CI: гейт, который зеленее CI, — не гейт»: CI гоняет
   `cargo test --all`, verify — четыре именованных таргета крейта `gateway`.
3. Эмпирически (§C.1): гейт краснеет против отката механизма ТОЛЬКО через `td083`, а его
   зубы заёмные (§C.2).

**Зона правки:** architect (`scripts/verify_*.sh` — sacred).

### F-2 (BLOCKER) — условие APPROVED `R-035` §H п.3 не выполнено; после круга 2 оно стало несущим

`F-035-3` открыт: задача 8 в §8 milestone'а — `⏳ BLOCKED`, и сама спека пишет: «закрывать
milestone с ней в подвешенном виде нельзя». В `R-035` §F это было названо «не блокером сам
по себе» с явным обоснованием: «`O-2` живёт в крейте `journal` и ассертит счётчик НАПРЯМУЮ,
поэтому мутация 1 всё равно валит `verify_M-57` (T3)». **После круга 2 это обоснование
недействительно:** механизм переехал на уровень gateway (`pump` → `stream_from_at`), T3 его
не касается и остаётся зелёным (§C.1), а композит (§C.2) зеленит гейт целиком.

Наивный ассерт `scanned > decoded` действительно негоден (§8.1 milestone'а — принято), но
из этого следует, что нужен ДРУГОЙ оракул, а не что его можно не иметь. Дизайн оракула —
зона architect (`gates.md` §4: reviewer описывает дефект, architect проектирует);
reviewer фиксирует лишь ФАКТ: измеритель, на котором стоит весь гейт milestone'а,
подделывается одной строкой, и это проверено прогоном.

### F-3 (BLOCKER, scope + порча записи + единственный конфликт мержа) — правка `docs/ORCHESTRATION-STATE.md`

Коммит `2e48409` (architect, «TD-098 → TD-109») задел `docs/ORCHESTRATION-STATE.md` — файла
НЕТ в `Allowed paths` M-57 (§4 спеки: `crates/{journal,gateway}/**`, `scripts/verify_M-57.sh`,
`research/reports/M-57-*.md`). Три отдельных дефекта в одной правке:

1. **Испорчена ЧУЖАЯ запись.** Строка 25 говорила о ретро-аудите: «коллизия номеров
   **TD-098/099/100** с `main` — перенумерацию делает reviewer при merge». Здесь `TD-098` —
   номер ИЗ ДРУГОГО контекста (ветка `docs/retro-audit`), и массовая замена превратила её в
   «**TD-109**/099/100». Запись теперь ложна и вводит в заблуждение того reviewer'а, которому
   предстоит перенумеровывать `docs/retro-audit`, — тем более что `TD-109` этой же веткой
   занят под M-57.
2. **Файл в `main` УДАЛЁН** (`0bd8b45`, «ORCHESTRATION-STATE убран из startup-протокола,
   журналы в архив» — решение founder'а, зафиксированное в `CLAUDE.md`); содержимое переехало
   в `docs/archive/orchestration-log-2026-07-08.md`.
3. **Это ЕДИНСТВЕННЫЙ конфликт мержа всей ветки** — проверено:
   ```
   $ git merge-tree --write-tree --name-only origin/main origin/feat/M-57-task5
   docs/archive/orchestration-log-2026-07-08.md
   CONFLICT (content): Merge conflict in docs/archive/orchestration-log-2026-07-08.md
   ```
   Неаккуратное разрешение = воскрешение документа, который `main` осознанно вынес из
   startup-пути, либо занос ложной строки в архив.

**Требуется:** правку `docs/ORCHESTRATION-STATE.md` с ветки убрать (в архиве `main` строка
про коллизию `TD-098/099/100` обязана остаться в исходном виде).

### F-4 (BLOCKER, документация sacred-оракула) — перенумерация `TD-098 → TD-109` не доведена

Acceptance-критерий задачи 7 (§8 milestone'а) — «грепом: старый номер на ветке = 0». Не
выполнен в самом оракуле M-57:

```
$ grep -n TD-098 crates/journal/tests/red_tail_scan_bounded.rs
1:  //! M-57 (`TD-098`) — **активный сегмент не смеет пересканироваться на каждом тике**,
209:  "O-1 (TD-098): events_scanned ведёт себя как АЛИАС events_decoded. …
257:  "O-2 (TD-098): сегмент вырос в 8 раз — работа тика выросла …
```

В `main` `TD-098` — чужой долг (`оракул-O-1-M-56-меряет-глобальный-счётчик-в-параллельном-
тест-раннере`, `TECH-DEBT.md:312`, заведён `R-030`). После merge панические сообщения
sacred-оракула будут указывать на посторонний долг — ровно то, что `R-035` F-035-4 и просил
устранить. Соседние файлы уже переразмечены (`segments.rs` — 15 упоминаний `TD-109`,
`gateway/src/lib.rs` — 7, `verify_M-57.sh` — 2), то есть расхождение внутри одного набора.

Отдельно: **записи `TD-109` не существует ни в `main`, ни на ветке** (`grep -c TD-109
TECH-DEBT.md` → 0 в обоих). Сейчас ~25 ссылок в исходниках указывают в пустоту. Заведение
записи — моя зона и делается при close-out (после APPROVED), но до тех пор номер обязан быть
зарезервирован непротиворечиво: либо TD заводится этим кругом (мной, по факту APPROVED),
либо ссылки согласованы между собой.

---

## §E — Находки, не блокирующие merge сами по себе

### F-5 (MAJOR, дисциплина) — §Tasks milestone'а лжёт о собственном состоянии

- Задачи **6 и 7** стоят `⏳ OPEN`, хотя закоммичены (`71ab66d`, `2e48409`).
- **Задачи 9 в §Tasks НЕТ ВООБЩЕ**, при том что на неё ссылаются коммит `c11a610`
  («task #9») и 7 комментариев в прод-коде (`gateway/src/lib.rs:1877`, `:2012`, `:2544`,
  `journal/src/segments.rs:119`, `:883` и др. — `grep -rn "задача 9" crates/*/src` → 7).
  Трассируемость «коммит → задача спеки» разорвана в обе стороны.

Класс известен и в этом же milestone'е уже чинился самим architect'ом (`7934cbd` — «таблица
оракулов приведена к коду; статусы задач 1/2 — к факту»). Правка колонки Status — carve-out
dev'а, остальное — architect.

### F-6 (MINOR, комментарии на P0-пути) — документ врёт о коде в самом горячем месте

1. `crates/gateway/src/lib.rs:3141-3152` — **дубль комментария с ПРОТИВОПОЛОЖНЫМ смыслом**:
   первый абзац объясняет, почему `None` от `tail_hint()` — это «корректно, следующий тик
   заполнит», второй — почему старый hint при `None` **НЕ сбрасывается**. Код (`:3153`,
   `.or(self.tail_hint)`) реализует второе; первый абзац описывает поведение, которого нет.
2. `:3088-3091` — «`stream_from` читает hint из файлового sidecar'а `journal.tail-offset`».
   После задачи 9 sidecar удалён целиком, и `segments.rs:1301` говорит прямо обратное
   («`stream_from` больше не читает hint из sidecar»). Два комментария в одном коммите
   противоречат друг другу.

Обоснование механизма — часть механизма: следующий, кто будет чинить hot-путь, прочтёт
неверное описание. Зона — engine-dev (impl-комментарии).

### F-7 (MINOR, дисциплина) — трейлер `Co-authored-by` вернулся

`71ab66d` несёт `Co-authored-by: Alex K <alex.kurz@protonmail.com>` вопреки BINDING-запрету
(`CLAUDE.md`, `commit-discipline.md`) и прямому предупреждению `R-035` F-035-5 («круг
исправления обязан не тиражировать трейлер»). Переписывание истории не предлагаю (на ветке
сидит рабочий worktree `/tmp/hft-dev-m57-task5`); фиксирую как повтор.

### NOTE-1 — номера вердиктов раздаются без механизма

`main` содержит `R-035-M-58-rev2.md`, ветка M-57 — `R-035-M-57.md` (коммит `d4fd45e`):
один номер, два разных предмета, конфликта файлов нет — коллизия невидима до сверки.
`R-038` занят дважды на ветках (`R-038-M-59`, `R-038-M-60a-arbiter-trigger`). Прецедент —
`TD-090` («коллизия номеров два разных milestone M-46»). Кандидат в долг при close-out.

---

## §F — Что проверено и НЕ является находкой

1. **Механизм НА ПУТИ (`gates.md` §4, DoD).** WS-бин действительно ходит через новый путь:
   `crates/gateway-serve/src/lib.rs:380` `LiveReducer::resume`, `:400` догон `live.pump`,
   `:524` периодический push-тик `live.pump` — то есть `stream_from_at` + `tail_hint`
   в горячем цикле. Проверено чтением бина, а не по отчёту.
2. **Три непокрытых вызова `stream_from` — заявление dev'а верно.** `frames_since_with_stats`
   доступен наружу только через `serve::frames_msgs`, который WS-бин не зовёт (ссылки —
   тесты `red_serve_passthrough.rs` и канарейка `:775`); `snapshot_from_checkpoint` — только
   через `serve::snapshot_msg` (бин после M-54 берёт снапшот из прогретого `live`);
   `checkpoint::advance_to` — cron-бин `gateway-checkpoint` раз в 5–15 мин. Поведение
   идентично `main` pre-M-57 — не регресс. **Но цена названа честно быть должна:** долг
   `A-004` §C.2 + «три stateless-вызова» заводится при close-out (`R-035` §H п.6).
3. **Корректность seek'а по данным.** Гарды `resolve_active_start_offset` (`seg_idx` совпал ·
   `pos >= header_end` · `after >= hint.last_seq` · `after_seq.is_some()`) в сумме
   пропускают только события, которые фильтр `after_seq` всё равно отбросил бы; любое
   нарушение — откат к полному скану (fail-open, безопасная деградация). `.or(self.tail_hint)`
   в `pump` не создаёт дыры: устаревший hint отбрасывается по `seg_idx` при ротации, а
   «отставший» курсор (`after < last_seq`) — по четвёртому гарду. Оборванный (torn) хвостовой
   фрейм позицию не двигает: `active_tail_hint` обновляется только после УСПЕШНО
   декодированного события. `parse_segment_index_any(...).unwrap_or(0)` недостижим:
   `dedup_indexed_paths` отбрасывает файлы с непарсящимся индексом.
4. **Block-C чист.** `crates/contracts/**` не тронут (T6 гейта + `git diff --name-only`);
   `ReadStats` живёт в `crates/gateway`, T1-формой не является. Contract-RFC не требуется.
5. **RED-first соблюдён.** Оракулы круга 2 (`5cda5df`, architect) закоммичены ДО impl
   (`71ab66d`, engine-dev); dev файлов в `*/tests/**` не трогал — проверено пофайлово по
   `git log --name-only`. `2e48409` (правка тестов) — architect, зона его.
6. **Атомарность коммитов** — задачи 5/6/9 разнесены по коммитам с ссылкой на milestone.
7. **RISK-BLOCK не применяется:** ветка не трогает `crates/{risk,killswitch,oms,venue-*}`
   и `contracts` — risk-critic не требуется (`gates.md` §5).

---

## §G — Условия APPROVED (круг 3)

1. **F-1:** `verify_M-57.sh` исполняет оракулы круга 2. Предъявить не текстом скрипта, а
   прогоном: мутация 1 (§C.1) обязана валить гейт ИМЕННО через `f035_*`, а не только
   через `td083`. Заодно — паритет с CI (`cargo test --all` либо явное перечисление всех
   таргетов `gateway`).
2. **F-2:** `F-035-3` закрыт оракулом, пиннящим сам проброс `ReadStats.events_scanned`.
   Критерий приёмки — **композитная мутация §C.2 обязана краснеть**; сегодня она зелёная
   на 11/11. Дизайн — architect (задача 8 снимается с BLOCKED либо переносится в отдельный
   milestone ЯВНЫМ решением, но тогда M-57 не может ссылаться на гейт как на доказательство).
3. **F-3:** правка `docs/ORCHESTRATION-STATE.md` снята с ветки; `git merge-tree` против
   `origin/main` даёт 0 конфликтов.
4. **F-4:** `grep -rn TD-098 crates/` на ветке = 0; номер `TD-109` непротиворечив.
5. **F-5/F-6/F-7** отработаны (статусы §Tasks приведены к факту, задача 9 внесена в спеку,
   дубль-комментарий и ложное утверждение про sidecar убраны, трейлер не тиражируется).
6. Повтор PR-гейта на ветке, **синхронизированной с `main`** (сейчас отставание 45 коммитов;
   `gates.md` §8 — утверждения проверяются на ДЕРЕВЕ СЛИЯНИЯ, там уже M-58/M-59-наборы).
7. После APPROVED — merge, close-out по `M-57` §6: деплой-гейт `gates.md` §8 + прогон против
   прода по `R-029` §C (CPU < 5 % перед замером, предсказание ДО мержа, три точки backlog'а)
   + CPU `gateway-serve` под 5 и 20 клиентами. При close-out reviewer заводит `TD-109`
   (слепые меры работы), долг `A-004` §C.2 (метаданные-путь `O(#сегментов)` на тик) и долг
   по трём непокрытым stateless-вызовам.

---

## §H — Done Block

```
$ pwd; git log -1 --format='%h %s'
/tmp/hft-rev-m57t5
c11a610 feat(M-57): task #9 — sidecar удалён целиком; три prod-вызова stream_from явно не покрыты [engine-dev]

$ cargo fmt --all -- --check;                                   exit=0
$ cargo clippy --all-targets --all-features -- -D warnings;     exit=0
$ bash scripts/verify_M-57.sh;                                  exit=0   (VERDICT: PASS, 11/11)
$ cargo test --workspace | grep "^test result" | awk …
passed=797 failed=0 (блоков: 194);                              exit=0
$ cargo test -p gateway --test red_tail_cursor_prod_form;       exit=0   (2 passed)

# мутационный контроль (§C):
#  мутация 1  (lib.rs:3095  stream_from_at → stream_from)
#     verify → VERDICT: FAIL (1), exit=1 — красный ТОЛЬКО td083 (T5); T3 зелёный
#     f035_1 = 8003 событий/тик, f035_2 = A 8009 / B 8009 при бюджете 12  → FAILED, exit=101
#  мутация 1+2 (+ lib.rs:1973  events_scanned → events_decoded)
#     verify → VERDICT: PASS, 11/11, exit=0   ← ПРОТИВ ВЕРНУВШЕГОСЯ P0-ДЕФЕКТА
#     f035_1, f035_2 → ok, exit=0             ← прод-форменные оракулы тоже слепнут
#  обе мутации откачены; git status --porcelain пуст, git diff пуст

$ git merge-tree --write-tree --name-only origin/main origin/feat/M-57-task5
docs/archive/orchestration-log-2026-07-08.md
CONFLICT (content): Merge conflict in docs/archive/orchestration-log-2026-07-08.md

$ grep -c red_tail_cursor_prod_form scripts/verify_M-57.sh
0
$ grep -c TD-098 crates/journal/tests/red_tail_scan_bounded.rs
3
$ git rev-list --count HEAD..origin/main
45
```

**Push-статус:** ⛔ NOT merged, NOT pushed to `main` — блокируется F-1..F-4 (`gates.md` §4).
Вердикт закоммичен и запушен на `feat/M-57-task5`.
`PROJECT-STATE.md`/`TECH-DEBT.md` НЕ трогаю: они отражают состояние `main`, а M-57 в `main`
не входит.
