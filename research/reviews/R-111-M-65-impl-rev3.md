<!-- GATE-META
milestone: M-65
audited_repo: a3ka/hft-platform
audited_base: a9f0bf511a0a1de5433b0de412cb55234027b56a
audited_head: 7886f6aa8a42e0a4a1d176620eaabb02f4aca9e1
verdict: REJECT
-->

# R-111 — M-65 ws-session, PR-гейт круг 3 (после `R-093`): **REJECT**

**Роль:** reviewer (PR-time гейт, `gates.md` §4 — UNCONDITIONAL) · **Дата (UTC):** 2026-08-22
**Предмет:** ветка `fix/M-65-battery-recalibration`, вершина `7886f6a`; диапазон
`a9f0bf5..7886f6a` (66 коммитов, из них после `R-093` — девятнадцать; три последних
`[architect]`: `c6642ee`, `01c4923`, `7886f6a`).
**Дерево:** `/tmp/hft-rev-m65-final`, detached на `7886f6a`, чистое. Общий чекаут не тронут.
**Спека:** `milestones/M-65-ws-session.md` (808 строк, прочитана целиком) ·
**Норматив:** `CT-RFC-09` §2.1-2.9
**Цепочка:** `C-077` REJECT → `C-078` NOTE → `R-057` REJECT → `R-075` APPROVE →
`R-080` APPROVE → `R-086` REJECT → `R-093` REJECT → `C-098` REJECT → **этот вердикт**.

**Вердикт: REJECT.** Четыре из пяти условий снятия `R-093` §5 выполнены, и выполнены ПО
СУЩЕСТВУ — я это подтверждаю замером, а не чтением (§2). Блокирует другое:

1. **Б-1** — вторая половина `O-12` (детерминизм повтора, `§10.3`) **ВАКУУМНА**: она
   различает инструменты по имени символа в сериализованном кадре, а `frame` селектора не
   несёт. Доказано изолирующей мутацией: при снятых развязках А+Б — то есть при
   воспроизведённой находке `R-086` §2 — эта половина остаётся ЗЕЛЁНОЙ. Условие `C-098` B-1
   («observe every post-release outcome named in milestone §10.3») по четвёртому исходу не
   выполнено. Это ТОТ ЖЕ класс вакуумности, который на этой же ветке уже чинили дважды
   (`5121f1d` — первая половина того же теста; `7886f6a` — шаг гейта). Третье повторение.
2. **Б-2** — `§11` N-2, действующий разбор, предписывает ПРОТИВОПОЛОЖНОЕ тому, что
   реализовано и слито: «правится НОРМАТИВ через УЗКИЙ contract-RFC … отсутствие ⇒ 16», тогда
   как код (`d5a1cf5`, `c6642ee`) даёт «отсутствие ⇒ отказ старта». Реализация ВЕРНА, спека —
   нет. `§Tasks` 13 при этом говорит «Исполнить решения §11».
3. **Б-3** — `§Tasks` снова лжёт о состоянии: задачи 12, 12в, 13, 14 стоят `⏳ OPEN` при
   выполненной работе, номер «13» занят ДВАЖДЫ. Это прямое невыполнение стоящего условия
   `R-057` §7 п.5 («§Tasks приведён к факту»), второе воспроизведение `R-057` М-4.

Код я не оспариваю: он верен, гейт у меня зелёный целиком (§6). Блокируют оракул, который
обещает больше, чем меряет, и документ, который предписывает не то, что слито.

---

## 1. Block-scope — ✅ ПРОЙДЕН

| проверка | результат |
|---|---|
| крейты, тронутые диффом | РОВНО ОДИН — `crates/gateway-serve/**` |
| `crates/{risk,killswitch,oms,venue-*}/**` | не тронуты |
| `crates/contracts/**` | **не тронут** → Block-C не применяется (см. §3) |
| `crates/gateway/src/**`, `crates/journal/**` (forbidden §2) | не тронуты |
| `ports:` в `docker-compose.yml` (forbidden §2) | не появились; диф добавляет РОВНО одну переменную `GATEWAY_MAX_SUBSCRIPTIONS` |
| `CT-RFC-09` нормативная часть §2.1-2.9 | **не изменена**; диф трогает ТОЛЬКО шапку и §6 (внесение подписей founder'а) — §2 спеки это architect'у прямо разрешает |
| `crates/gateway-serve/Cargo.toml` — правки architect'а | **ТОЛЬКО `[features] testing = []`** (`180f78e`, 12 строк, из них 10 — комментарий). Секцию `[dependencies]` трогал engine-dev (`f3c9668`, `futures-util`) — его зона по §2 |
| три коммита architect'а поверх работы dev'а | `c6642ee` → `crates/gateway-serve/tests/**` (sacred RED, зона architect'а); `01c4923` → `scripts/tests/red_ws_session_battery.sh`; `7886f6a` → `scripts/verify_M-65.sh`. Все три внутри `Allowed paths` §2 |
| RED-first | **ни один коммит `[engine-dev]` не трогает `*/tests/`, `scripts/verify_*`, `crates/contracts/`** — проверено покоммитным прогоном по всему диапазону; sacred-зона цела |
| порядок RED→GREEN на задаче 13 | `755863c` (RED, architect) предшествует `d5a1cf5`/`3c57f24`/`365d62e` (impl, engine-dev) |
| атомарность | задачи 13 разнесены: N-2, N-3, N-4, §12 — четыре отдельных коммита |

```
$ git diff --name-only a9f0bf5..7886f6a | cut -d/ -f1-2 | sort -u
crates/gateway-serve
docker-compose.yml
docs/rfc
milestones
research/critiques
research/reviews
scripts/tests
scripts/verify_M-65.sh

$ for c in $(git log a9f0bf5..7886f6a --format='%h %s' | grep -F '[engine-dev]' | cut -d' ' -f1); do
    git show --name-only --format='' $c | grep -E 'tests/|scripts/verify|contracts/'; done
{пусто}
```

**Block-scope: ЧИСТО.**

## 2. Block-risk — триггер НЕ СРАБАТЫВАЕТ, проверено МНОЙ по дифу

`gates.md` §5 привязан к путям `crates/risk|killswitch|oms|venue-*|contracts` — ни один не
тронут. Это несрабатывание самого триггера, а не MD-only carve-out. Дополнительно проверен
ДУХ правила — есть ли на пути деньги:

```
$ git diff a9f0bf5..7886f6a -- crates/gateway-serve/src/ \
    | grep -nE "^\+" | grep -iE "submit|place_order|new_order|cancel_order|order_egress|RiskApproved|hmac|api_key|secret_key|\.post\("
{ни одного попадания по торговому пути; единственные совпадения на подстроку —
 `pump_signal_and_wait` (точка встречи оракула) и слово «egress» в комментарии про байты}
```

`gateway-serve` остаётся тем, чем был: read-only WS-транспорт над журналом. Ордерного
egress'а, подписи торговых действий и записи в журнал в дифе нет.

**Block-risk: PASS. risk-critic по этому диапазону НЕ требуется** — подтверждаю сам, по
дифу, а не по чужому слову.

## 3. Block-C (контракты T1) — N/A, проверено дифом

`crates/contracts/**` в дифе отсутствует полностью. Спека §2 объявляет `Contract impact: нет`
— соответствует факту. Правка `docs/rfc/CT-RFC-09-ws-session.md` — 28 строк, ВСЕ в шапке и
§6 (запись полученных подписей founder'а). §2.6 и вся нормативная часть §2.1-2.9 побайтово
целы; это и делает Б-2 ниже дефектом СПЕКИ, а не контракта.

## 4. Предъявление FA (`gates.md` §4, M-66)

Диф трогает `crates/gateway-serve/**`. Собственной FA у крейта **не существует** —
`ls docs/fa/` даёт 20 файлов, `gateway-serve.md` среди них нет; `reading-map.md` §2 (ярус B)
называет этот пробел прямо: «`derive`/`recorder`/`gateway-serve` — FA не существует, сам этот
факт есть долг».

FA-WAIVER: crates/gateway-serve — собственной FA у крейта нет (docs/fa/gateway-serve.md отсутствует), пробел назван в reading-map.md §2 как открытый долг

Опора — FA слоя. Живой инвариант, применимый к диффу, проверен мной на `7886f6a`:

> **`VB-I-3`** (`docs/fa/viz-backend.md:150`): «Read Gateway read-only: grep-канарейка —
> gateway не импортирует journal-writer/recorder-write; recorder не зависит от gateway».

```
$ grep -rnE "journal::(Writer|WriterConfig)|journal_writer|recorder::" crates/gateway-serve/src/
{пусто — writer не импортируется}
```

**Барьер прогнан, а не предположен.** `scripts/check_review_fa.sh` на этой ветке
ОТСУТСТВУЕТ (введён в `main` позже базы ветки — см. Н-3), поэтому я взял его версию из
`origin/main` и прогнал на дереве ветки:

```
$ git show origin/main:scripts/check_review_fa.sh > /tmp/cra.sh
$ EVENT_NAME=push PUSH_BEFORE=a9f0bf5 bash /tmp/cra.sh
research/reviews/R-093-M-65-impl-rev2.md: GS-I-4
research/reviews/R-106-M-65-impl-rev3.md: GS-I-4
exit=0
```

То есть барьер разрешает `crates/gateway-serve` в `docs/fa/viz-backend.md` (префикс `GS`),
и NO-FA-крейтом его НЕ считает: waiver выше — предъявление документного пробела, а не
обход барьера. Оба развязки закрыты одновременно, и это названо, а не смешано.

`VB-I-3` цел. Семейство `GS-I-*` в FA по-прежнему заведено неполно (`grep -oE "GS-I-[0-9]+"
docs/fa/viz-backend.md` → только `GS-I-4`, при том что `scope-guard.md` ссылается на
`GS-I-1`/`GS-I-3`/`GS-I-5`) — долг, названный ещё `R-093` §1ter; завожу его карточкой при
close-out, файл reviewer-owned.

## 5. Ярус C — что я искал грепом

`TECH-DEBT.md` + `PROJECT-STATE.md` — 1 015 KB ≈ 226 k токенов, читаются запросом
(`reading-map.md` §2, профиль reviewer'а). Искал: `gateway-serve`, `M-65`, `ws-session`,
`CT-RFC-09`, `GS-I-`, `TD-124`, `TD-155`, `TD-151`, `TD-039`.

- `PROJECT-STATE.md`: `M-65` — **0 вхождений**; состояние «реализовано» ещё не заявлено, и
  это верно, заявлять его — мой шаг после APPROVED;
- `TECH-DEBT.md`: `M-65` — 2 вхождения, обе внутри `TD-151`
  (`:992` `disk-guard-оракул-меряет-ОКРУЖЕНИЕ…`, заведена reviewer'ом 14.08 по `R-075` Н-4:
  шаг гейта краснел от состояния хоста). **OPEN**; у меня шаг `T` зелёный (§6) —
  воспроизведение зависит от хоста, карточка не закрывается этим прогоном;
- `ws-session` — 2 вхождения, обе документные и НЕ про предмет M-65: `TD-084` (`:446`,
  висячая ссылка `CT-RFC-09` §4 на несуществующий `TD-095`) и `TD-096` (`:1551`, висячие
  ссылки и занятый номер RFC). Ни одна не трогает нормативную часть §2;
- `TD-124` (блокировка merge токеном `docs-freeze`) — 1 вхождение, закрывается мной при
  ближайшем close-out; сегодня close-out'а нет.

---

## 6. Прогон гейта — МОЙ, не пересказ tester'а

`commit-discipline.md`: пересказ = «NOT REVIEWED». Отчёт tester'а («verify exit=0, 12/12,
батарея 13/13, 845 passed») в моём распоряжении был только в виде СВОДКИ, поэтому я прогнал
гейт сам, на своём дереве.

```
$ cd /tmp/hft-rev-m65-final && git log -1 --oneline
7886f6a fix(M-65): шаг O12 гейта был ВАКУУМЕН — паттерн не мог совпасть никогда [architect]

$ git status --porcelain
{пусто}

$ bash scripts/verify_M-65.sh
--- A: RED-набор и батарея на месте, парсятся, форматированы ---
PASS  A crates/gateway-serve/tests/red_ws_session.rs на месте
PASS  A форматирование (паритет с CI-шагом fmt)
--- N: манифест набора ⇄ таблица осей §4.2, в ОБЕ стороны ---
PASS  N манифест ⇄ §4.2 совпал в обе стороны; у каждой из восьми осей есть легитимный сценарий
--- F: RED-набор O-1..O-10 GREEN ---
PASS  F набор GREEN: исполнено 12 оракулов (ожидалось ≥ 11: o0 + O-1..O-10)
--- F2: батарея мутантов §4.5 — FAIL-CLOSED до задачи 9 ---
PASS  F2 BATTERY: PASS (13/13)
--- L: лимит подписок fail-closed В ОБЕ СТОРОНЫ ---
PASS  L невалидный лимит «0» ⇒ отказ старта (exit=2)
PASS  L невалидный лимит «-1» ⇒ отказ старта (exit=2)
PASS  L невалидный лимит «abc» ⇒ отказ старта (exit=2)
--- M: регресс — цена M-65 не уплачена соседним инвариантом ---
PASS  M соседние оракулы gateway-serve GREEN (10 тестов)
--- T: паритет с CI + НЕНУЛЕВОЕ число исполненных тестов ---
PASS  T clippy
PASS  T cargo test --all: passed=845

── O12: оракул на гонку switch × in-flight pump (задача 12) ──
PASS  O12 оракул на находку ИСПОЛНЕН и зелён (тестов: 1)

VERDICT: PASS
exit=0
```

**Гейт зелёный целиком, и я его воспроизвёл.** Ровно поэтому вердикт отрицательный: зелёный
гейт — не доказательство. `R-057` §0 и `R-093` §3 в этой цепочке уже расходились с зелёным
гейтом, и оба раза были правы.

### 6.1 Что ещё я подтверждаю ЗАМЕРОМ (условия `R-093` §5 — четыре из пяти закрыты)

| условие | статус | чем проверено |
|---|---|---|
| **Б-2** — механизм синхронизации исполняется либо удалён | **ЗАКРЫТО** | все ЧЕТЫРЕ границы `C-098` B-1 сняты: `lib.rs:77` (`pub mod test_sync`), `lib.rs:1135` и `:1522` (обе точки вызова), `test_sync.rs:48` (`pub mod rendezvous`) — везде `#[cfg(any(test, feature = "testing"))]`. `O-12` под флагом СОБИРАЕТСЯ и ИСПОЛНЯЕТСЯ (1 passed) |
| **Б-3** — N-2/N-3/N-4 приведены к `CT-RFC-09` §2.1-2.9 | **ЗАКРЫТО по существу** (спека — нет, см. Б-2 ниже) | N-2: отсутствие/пусто/`0`/мусор ⇒ `Err` (`lib.rs:1900-1914`), оракул `red_max_subs_config.rs` с парным vantage'ем; N-3: `GATEWAY_INITIAL_SUBSCRIBE_GRACE_MS` читается, дефолт 250 (`lib.rs:1922-1948`); N-4: `error_msg` отдаёт `Value::Null`, оракул `red_wire_null_sub.rs` пиннит ТИП узла и несёт парный vantage против «всегда null» |
| **Б-4** — мёртвый код удалён | **ЗАКРЫТО** | `NotTextPayload`, `MalformedSelector`, `version()`, `id()` — 0 вхождений в `wire_v1.rs` на `7886f6a` |
| **Н-1** — шаг гейта наблюдает ОТСУТСТВИЕ | **ЗАКРЫТО** | см. §6.2 |
| **Б-1** — мутационная приёмка `O-12` | **НЕ ЗАКРЫТО целиком** | §7 |

### 6.2 Шаг O12 — проверен по прямому поручению; дефект `7886f6a` устранён верно

Имя теста на `7886f6a` — `o12_switch_during_inflight_pump_does_not_restore_old_sub`
(`red_ws_session.rs:1315`); прежний паттерн требовал пробела сразу после `pump` и не мог
совпасть ни при каком исходе. Новый паттерн — `^test o12_switch_during_inflight_pump[A-Za-z0-9_]* \.\.\. ok$`.
Замер по четырём входам, мой:

```
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... ok      -> 1
test o12_switch_during_inflight_pump_bogus_stub ... ok                    -> 1
test o12_switch_during_inflight_pumpkin ... ok                            -> 1
test o11_frames_come_from_journal_not_synthesis ... ok                    -> 0
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... FAILED  -> 0
```

**Шаг различает «есть/нет», а не только «зелёный», и различает их РАЗНЫМИ механизмами:**
падение теста ловится КОДОМ ВОЗВРАТА `cargo test` (внешняя ветка `else`), отсутствие —
СЧЁТОМ строк (нулевой счёт при exit=0). Это правильная конструкция: «вернул 0, но исполнил 0»
— отдельный исход, и он FAIL. Требование `R-093` Н-1 выполнено.

**Н-1 (NOTE, точность заявленного).** Тело `7886f6a` заявляет четвёртым входом «чужой тест с
похожим именем -> 0». Замер выше показывает: суффиксная часть `[A-Za-z0-9_]*` принимает ЛЮБОЕ
продолжение имени, поэтому заглушка `..._bogus_stub` шаг удовлетворит. Отсекается только имя
с ДРУГИМ префиксом. Не блокер — предъявление отсутствия работает; но формулировка в теле
коммита шире своего замера.

---

## 7. БЛОКЕРЫ

### Б-1 (BLOCKER) — вторая половина `O-12` вакуумна: пиннит НИЧЕГО

**Где.** `crates/gateway-serve/tests/red_ws_session.rs:1436-1453` — блок «§10.3: детерминизм
повтора».

**Что.** Различение инструментов построено на поиске подстроки `"BTCUSDT"` в сериализованном
сообщении:

```rust
let stale_repeat = repeat.iter()
    .filter(|v| serde_json::to_string(v).unwrap_or_default().contains("BTCUSDT"))
    .count();
assert_eq!(stale_repeat, 0, "O-12 НАРУШЕН (детерминизм повтора, §10.3): …");
```

**Почему это дефект.** `wire_v1.rs:170` — кадр есть
`{"type":"frame","v":1,"sub":<id>,"data":{schema_version, from, to, delta}}`; `delta` есть
`SeriesBundle`, и селектора он НЕ несёт (символ живёт только в `Snapshot.selector`,
`crates/gateway/src/lib.rs:310`). В окне `repeat` снапшотов нет по построению — после второй
докормки журнала приходят только кадры. Значит `stale_repeat` тождественно `0`, и ассерт
истинен против ЛЮБОЙ реализации, включая ту, которую он обязан ловить.

**Это ровно тот дефект, который тот же файл диагностирует сорока строками выше** — и который
`5121f1d` уже чинил в ПЕРВОЙ половине этого же теста:

> «Прежняя редакция искала имя символа в сериализованном кадре — а `Frame` селектора не
> несёт, поэтому вектор был ПУСТ ВСЕГДА и ассерт истинен при любой реализации, включая ту,
> которую он обязан ловить» (`red_ws_session.rs:1383-1386`).

Первая половина переведена на различение по ЦЕНЕ (`append_more_priced` + `max_price_in`) и
работает; вторая осталась на опровергнутой технике. Вдобавок повторная докормка идёт
`append_more`, а не `append_more_priced` — то есть ценового признака в окне `repeat` нет ни у
одной стороны.

**ВОСПРОИЗВЕДЕНИЕ — изолирующая мутация, три прогона, дерево восстановлено (`git status
--porcelain` пусто).** Дерево `/tmp/hft-rev-m65-probe`, detached на `7886f6a`.

*Прогон 1 — форма выдачи (инструментация, `--nocapture`):*
```
PROBE tail_len=1 tail_types=["frame"] tail_btc=0 repeat_len=1 repeat_types=["frame"] repeat_btc=0
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... ok
```
В окне `repeat` — только кадры, ни одного вхождения `BTCUSDT`. Совпадение подстроки в этом
окне недостижимо.

*Прогон 2 — КОНТРОЛЬ (мутация есть, тест не тронут):* сняты ОБЕ развязки —
`let live_keeps = !drained && current_gen == Some(gen_at_pump);` → `let live_keeps = true;`
(4 вхождения, `lib.rs:1216`, `:1272`, `:1572`, `:1612`):
```
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... FAILED
test result: FAILED. 0 passed; 1 failed
exit=101
---- panicked at red_ws_session.rs:1357: «смена селектора обязана начинаться с НОВОГО
     snapshot (§2.4), пришло: {"type":"frame",…}»
```
Мутация РЕАЛЬНО ловится — но ПЕРВОЙ половиной, на строке 1357. До второй половины прогон не
доходит.

*Прогон 3 — ИЗОЛЯЦИЯ (мутация та же; нейтрализованы пять ассертов первой половины —
`type_of(&after)`, `stale.is_empty()`, `!priced.is_empty()`, `!fresh.is_empty()`,
`ids == vec!["w1"]`; активен ТОЛЬКО `stale_repeat`):*
```
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... ok
test result: ok. 1 passed; 0 failed
exit=0
```

**Вывод, снятый прогоном, а не рассуждением: при воспроизведённой находке `R-086` §2 половина
«детерминизм повтора» остаётся ЗЕЛЁНОЙ.** Она не пиннит ничего.

**Чего это стоит по нормам.**
- `C-098` B-1, условие снятия: «observe every post-release outcome named in milestone §10.3».
  §10.3 называет ЧЕТЫРЕ исхода; три из них (`snapshot` нового селектора, отсутствие старого,
  один `sub` в потоке) наблюдаются честно, четвёртый — «повторный прогон даёт тот же
  результат побитово» — только ЗАЯВЛЕН. Условие не выполнено.
- `testing.md` §«Оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ» и §«Мутационный контроль»: симптом
  назван дословно — «гейт зелёный, но ты не можешь предъявить, ЧТО он покраснеет против
  конкретного слома». Здесь предъявить нечего.
- §10.3 сам про `o1` пишет: «Заявка есть, исполнения нет — ось покрыта НОМИНАЛЬНО». Вторая
  половина `O-12` воспроизводит ровно то, ради устранения чего `O-12` и писался.

**Границы находки, названные честно.** Защита от гонки в КОДЕ есть и она ВЕРНА — прогон 2 это
доказывает, и я это не оспариваю. `R-093` §5 п.1 в формулировке «снятие А ЛИБО Б обязано
ронять оракул» удовлетворён первой половиной. Блокирует не дыра в защите, а ложная гарантия,
въезжающая в `main`: следующий круг прочтёт «детерминизм повтора проверен» и будет неправ.

**Зона фикса — architect** (`gates.md` §4, граница reviewer↔architect: я описываю дефект, фикс
и его оракул проектирует architect). Техника, которой можно воспользоваться, в этом же файле
уже есть — но выбирать её не мне.

### Б-2 (BLOCKER) — `§11` N-2 предписывает ПРОТИВОПОЛОЖНОЕ тому, что слито

**Где.** `milestones/M-65-ws-session.md:656-709`.

**Что.** Действующий текст §11 N-2:

> «**Решение — ТРЕТИЙ ВАРИАНТ:** правится НОРМАТИВ через УЗКИЙ contract-RFC, реализация в
> главном остаётся» · таблица «что править», строка 1: «`CT-RFC-09` §2.6 (`:121-122`) — УЗКИЙ
> contract-RFC: **отсутствие И пустая строка ⇒ подписанная норма 16** с наблюдаемой записью
> при старте».

Реализовано и слито — ОБРАТНОЕ: `d5a1cf5` («отсутствие ⇒ отказ старта»), `c6642ee` (вердикт
по блокеру engine-dev'а: вариант (a)). `CT-RFC-09` §2.6 не менялся ни на байт.

**Кто прав по существу — реализация.** Проверено мной первоисточником, а не пересказом:

```
$ git show 7886f6a:docs/rfc/CT-RFC-09-ws-session.md | sed -n '119,122p'
### 2.6 Лимиты (fail-closed)

- `max_subscriptions_per_connection` — конфиг, отсутствие/невалидное значение ⇒ **отказ
  старта** (`gates.md`: «parse-error → unbounded — запрещено», урок R7);
```

Норматив покрывает ОТСУТСТВИЕ прямым текстом, а §5 спеки менять §2.1-2.9 запрещает. Решение
`c6642ee` верное, и я его поддерживаю. **Дефект — в документе, а не в коде.**

**Почему это блокирует merge, а не идёт NOTE'ом.**
1. `§10bis`, заведённый НА ЭТОЙ ЖЕ СПЕКЕ, устанавливает: «§Tasks ССЫЛАЕТСЯ, разбор РЕШАЕТ».
   Разбор — §11 — и есть действующий носитель решения. Сегодня он говорит «заведи
   contract-RFC и сделай дефолт 16». `§Tasks` 13 говорит «Исполнить решения §11». Исполнитель,
   открывший спеку после merge'а, построит противоположное слитому.
2. Ложь уже протекла в sacred-оракул: `crates/gateway-serve/tests/red_max_subs_config.rs:3-4`
   ссылается на «§11 (contract-RFC НЕ требуется; правится реализация)» — на редакцию, которую
   §11 сегодня НЕ содержит; а обоснование в его шапке (строки 12-17) дословно воспроизводит
   ту редакцию §11, которую сама спека убрала под `<details>` с пометкой `[снято]` как
   ошибочную. Живой оракул слитого поведения обоснован текстом, помеченным «ошибочен».
3. Это ЧЕТВЁРТОЕ расхождение носителей на одной спеке. Три предыдущих спека перечисляет сама
   (`§10bis`: `C-098` B-2, §Tasks 12б против §10.3.1, §Tasks 13 против §11). Механизма против
   класса в `main` нет и он не изображается — но и молча вливать четвёртый экземпляр,
   вместе с правилом, написанным против него, я не могу.

**Как это получилось (для следующего круга, не в упрёк).** `25c323d` приводил §Tasks к §11 по
редакции `027e864` («правится реализация»), но к тому моменту §11 уже был заменён `0fd7380`
на третий вариант. Затем `860c717` свернул §Tasks в голую ссылку — и расхождение перестало
быть видимым в таблице задач. Правка носителя по памяти о его прошлой редакции; ровно класс
`reading-map.md` §3.

### Б-3 (BLOCKER, дешёвый) — `§Tasks` снова лжёт о состоянии; `R-057` §7 п.5 не выполнен

```
$ awk -F'|' 'NR>=91 && NR<=97 {printf "%-5s %-32s %s\n", $2, substr($3,1,30), $5}' \
      milestones/M-65-ws-session.md
 12    Оракул на гонку `switch × in-      ⏳ OPEN
 12а   `#[cfg(any(test, feature = "t      ✅ DONE
 12б   Оракул `o12` в `tests/red_ws_      ✅ DONE
 12в   Шаг `verify_M-65.sh`, наблюда      ⏳ OPEN
 13    Исполнить решения §11 (N-2/N-      ⏳ OPEN
 14    Исполнить решение §12 (N-5…N-      ⏳ OPEN
 13    Механические находки `R-080`       ✅ DONE
```

Фактически: 12 закрыта (`O-12` исполняется), 12в закрыта (`180f78e` + `7886f6a`, шаг у меня
зелёный), 13 закрыта (`755863c`, `d5a1cf5`, `3c57f24`, `365d62e`, `c2cdaf9`, `c6642ee`), 14
закрыта (`c2cdaf9`). Четыре строки `⏳ OPEN` при выполненной работе.

Дополнительно: **номер «13» занят ДВАЖДЫ** — задача §11/§12 и задача «механические находки
`R-080` о самой батарее». Один идентификатор на два разных предмета — тот же класс, что
`gates.md` §12 запрещает для артефактов; здесь он внутри таблицы задач и барьером не покрыт.

`R-057` §7 п.5 — стоящее условие APPROVED — говорит: «М-3/М-4 — задачи разнесены по коммитам
при доработке; **§Tasks приведён к факту**». `R-057` М-4 формулировал цену: «Индекс
milestone'а — то, по чему следующий агент понимает, что сделано; расхождение уже дважды в
этом проекте приводило к повторной работе». Второе воспроизведение того же М-4.

Фикс — минуты; блокирую именно потому, что цена фикса ничтожна, а цена лжи в индексе уже
дважды оплачена.

---

## 8. Находки ниже блокера (merge не блокируют)

**Н-1** — точность заявленного в теле `7886f6a` про «чужой тест с похожим именем»; замер в
§6.2. NOTE.

**Н-2** — семейство `GS-I-*` в `docs/fa/viz-backend.md` заведено неполно (жив только
`GS-I-4`, ссылаются на `GS-I-1`/`GS-I-3`/`GS-I-5`); собственной FA у `gateway-serve` нет.
Назван ещё `R-093` §1ter, не закрыт. Завожу TD-карточкой при close-out (файл reviewer-owned).

**Н-3** — ветка отстаёт от `main` на **129 коммитов** (`git rev-list --count a9f0bf5..origin/main`),
при `strict: false` в защите ветки (`gates.md` §8). Слияние конфликтов не даёт
(`git merge-tree --write-tree origin/main 7886f6a` → exit=0, `CONFLICT` = 0), но зелёный чек
будет снят на СТАРОЙ базе. Перед merge'ем следующего круга обязателен прогон базовой тройки
CI и `verify_M-65.sh` на ДЕРЕВЕ СЛИЯНИЯ, а не на ветке.

**Н-4** — авторы коммитов диапазона (`git log --format='%an' | sort | uniq -c`): `t` — 32,
`scout` (владелец) — 14, `tester` — 12, `test` — 8. Наследие ролевых `user.name` в
worktree'ах — ровно тот замер, из-за которого
`branch-hygiene.md` п.6 отменил git-личность как признак роли. Все три коммита ЭТОГО круга
авторства владельца (`scout`), ролевая метка в subject'е на месте. Историческое, не чиню.

---

## 9. Условие снятия REJECT

1. **Б-1.** Половина «детерминизм повтора» `O-12` МЕРИТ то, что обещает. Критерий приёмки —
   не зелёный прогон, а изолирующая мутация: при снятой развязке (А либо Б) и нейтрализованной
   ПЕРВОЙ половине теста эта половина обязана КРАСНЕТЬ. Предъявление — прогоном в Done Block,
   а не утверждением. Проектирование — architect.
2. **Б-2.** §11 N-2 приведён в соответствие со слитым решением: действующий текст называет
   вариант (a) («отсутствие ⇒ отказ старта», основание — `CT-RFC-09` §2.6 дословно), прежние
   редакции уходят под `[снято]` как аудит-трейл, а не остаются действующими. Шапка
   `red_max_subs_config.rs` перестаёт ссылаться на редакцию, которой в §11 нет.
3. **Б-3.** §Tasks приведён к факту: 12, 12в, 13, 14 → `✅ DONE`; дублирующийся номер «13»
   разведён.
4. Повторный прогон `verify_M-65.sh` + новый круг reviewer'а. Прогон базовой тройки CI на
   дереве слияния с `origin/main` (Н-3) — до merge'а, не после.

Условия `R-093` §5 п.2 (Б-2), п.3 (Б-3), п.4 (Б-4) и п.5 (Н-1) — **закрыты**, переподтверждать
их не нужно; замеры в §6.1.

**Merge в `main` до выполнения п.1-3 запрещён.** PR не открываю; вердикт возвращается
founder'у на диспетч — предмет п.1-3 в зоне architect'а, не engine-dev'а.

---

## 10. Done Block

```
$ pwd
/tmp/hft-rev-m65-final

$ git log -1 --oneline
7886f6a fix(M-65): шаг O12 гейта был ВАКУУМЕН — паттерн не мог совпасть никогда [architect]

$ git status --porcelain      # до создания этого файла
{пусто}

$ wc -l crates/gateway-serve/tests/red_ws_session.rs scripts/verify_M-65.sh \
        scripts/tests/red_ws_session_battery.sh
  1456 crates/gateway-serve/tests/red_ws_session.rs
   189 scripts/verify_M-65.sh
   571 scripts/tests/red_ws_session_battery.sh
  2216 total

$ git rev-list --count a9f0bf5..7886f6a
66

$ bash scripts/verify_M-65.sh; echo exit=$?
… (полный вывод — §6)
VERDICT: PASS
exit=0

$ bash scripts/next_artifact_id.sh R
R-106
exit=0

$ git merge-tree --write-tree origin/main 7886f6a >/dev/null; echo exit=$?
exit=0                                   # конфликтов нет

$ git rev-list --count a9f0bf5..origin/main
129

# ── мутационный контроль (дерево /tmp/hft-rev-m65-probe, detached 7886f6a) ──
$ grep -c "let live_keeps = true;" crates/gateway-serve/src/lib.rs
4
$ cargo test -p gateway-serve --features testing --test red_ws_session o12_switch_during_inflight_pump
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... FAILED
test result: FAILED. 0 passed; 1 failed; 0 ignored; 0 measured; 12 filtered out
exit=101                                 # КОНТРОЛЬ: мутация ловится (первой половиной, :1357)

# ── та же мутация + нейтрализованы 5 ассертов первой половины ──
$ cargo test -p gateway-serve --features testing --test red_ws_session o12_switch_during_inflight_pump
test o12_switch_during_inflight_pump_does_not_restore_old_sub ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 12 filtered out
exit=0                                   # Б-1: вторая половина ЗЕЛЕНА против слома

$ git -C /tmp/hft-rev-m65-probe status --porcelain
{пусто — дерево восстановлено}

$ df -h / | tail -1
/dev/md2  437G  326G  90G  79% /
```

**Вердикт: REJECT.** Возврат — founder'у на диспетч `architect`'а (все три блокера в его
зоне: sacred-оракул, спека milestone'а, §Tasks).

FA-WAIVER: crates/gateway-serve — собственной FA у крейта нет (docs/fa/gateway-serve.md отсутствует), пробел назван в reading-map.md §2 как открытый долг
