<!-- GATE-META
milestone: A-016
audited_repo: a3ka/hft-platform
audited_base: 7c66843bb48e1697e6d948b6fb30ac812a4e0888
audited_head: c9b15898a5f84857fc40c07350d6dbeeb1c6f410
verdict: APPROVE
-->

# R-123 — PR #66 `docs/two-barriers-step1-2026-08-23`: **APPROVED** с тремя находками уровня NOTE

**Гейт:** PR-time reviewer, `gates.md` §4 (UNCONDITIONAL — диф трогает `crates/**`).
**Маршрут задан арбитражем `A-016`:** предмет идёт ко мне ЦЕЛИКОМ, не дробится. Решение
вынесено против позиции architect'а и исполнено здесь как написано.
**Предмет:** 3 файла, `+342 −0`, ветка `docs/two-barriers-step1-2026-08-23`, вершина `c9b1589`.
**База аудита:** `origin/main` `7c66843`.

---

## 0. Вердикт

**APPROVED.** Вывод предмета — «барьер A НЕ строится, класс „вакуумный ассерт“ остаётся
`COGNITIVE-ONLY`» — **обоснован**, и обоснован он тем способом, который проект требует:
замером и мутацией, а не разбором текста. Я перепроверил обоснование СВОИМ прогоном, не по
слову (§2–§4 ниже), и оно держится.

Три находки (F-1..F-3) — дефекты ЧИСЕЛ и ССЫЛКИ в тексте документа, не в его выводе и не в
коде. Блокерами не являются; основание пропорциональности названо в §6, а не подразумевается.
Обязательный довесок — §7.

---

## 1. Block-scope — ПРОЙДЕН

```
$ git diff origin/main...c9b1589 --stat
 crates/gateway/tests/red_segment_meta_bound.rs |  12 ++
 docs/plans/two-barriers-step1-2026-08-23.md    | 123 +++++++++++++++
 research/arbitration/A-016-pr66-routing.md     | 207 +++++++++++++++++++++++++
 3 files changed, 342 insertions(+)
```

| путь | зона | вердикт |
|---|---|---|
| `crates/gateway/tests/**` | sacred RED-спека — **architect-only** (`scope-guard.md`) | автор = architect ⇒ в зоне |
| `docs/plans/**` | `docs/` — зона architect'а | в зоне |
| `research/arbitration/**` | арбитр (`gates.md` §0) | автор коммита = arbiter ⇒ в зоне |

**`crates/*/src/**` НЕ тронут ни одной строкой** — проверено: `git diff … -- crates/` даёт
только `tests/`. Следствия, каждое проверено путями, а не памятью:

- **Block-C (контракты) — НЕ ПРИМЕНИМ.** `crates/contracts/**` в дифе отсутствует ⇒
  contract-RFC не требуется.
- **RISK-BLOCK (`gates.md` §5) — НЕ ПРИМЕНИМ.** `crates/{risk,killswitch,oms,venue-*}/**` в
  дифе отсутствуют ⇒ risk-critic в цепочке не требуется. Это не пропуск гейта, а его
  неприменимость по путям; фиксирую явно, потому что мой профиль обязывает блокировать
  отсутствие risk-critic'а там, где он положен.
- **`check_review_fa.sh` → `SKIP`, exit=0**, обе прод-формы: диапазон трогает только
  не-прод пути крейта (`tests/`). Барьер молчит — значит требование M-66 здесь
  **когнитивное**, и я исполняю его руками, а не ссылаюсь на зелёный джоб (§5).

## 2. Block-DoneBlock — сырой вывод предъявлен, §8 ниже

Все прогоны — на **ДЕРЕВЕ СЛИЯНИЯ**, не на ветке (`gates.md` §8: защита `main` не `strict`,
зелёный чек PR снят на старой базе — ветка форкнута на `92f6e65`, `main` ушёл на `7c66843`).

## 3. Существо: обоснован ли вывод «барьер A не строится» — ДА

Мой предмет — судить обоснованность вывода, а не требовать барьер. Судил так:

### 3.1. Пять «ложных срабатываний» — проверены ПОИМЁННО, все пять подтвердились

Документ утверждает, что детектор красит шесть функций, а пять из них уже несут свидетеля,
невидимого его шаблону. Я не принял это на слово — открыл каждую:

| # | заявленный свидетель | мой grep | есть? |
|---|---|---|---|
| 1 | встречный сторож `claims()` | `red_catalog_equivalence.rs:240 fn claims` — ассерт «метка В МАНИФЕСТЕ» | ДА |
| 3 | `assert!(restored > 0, …)` | `journal/tests/red_compaction.rs:388` | ДА |
| 4 | `assert_eq!(code, 0, …)` | `journal/tests/red_replay_digest_delivery.rs:207,238` | ДА |
| 5 | три счётчика `>= 2` | `red_retention_compacted.rs:393,409,433` (`zst_before`/`pruned_zst`/`cold_zst`) | ДА |
| 6 | `assert_eq!(near.lives_born, 1, …)` | `research-cli/tests/red_depth_lifetime_perlife.rs:378` | ДА |

Ни одного заявленного свидетеля я не обнаружил отсутствующим. Значит основание вывода —
факт, а не риторика: детектор мерил бы ФОРМУ ЗАПИСИ свидетеля, а корпус пишет его минимум
четырьмя формами сверх шаблона. Довод «третье сужение одного прокси — прокси убирают, а не
сужают» принимаю.

### 3.2. Единственный подтверждённый вакуум (случай 2) — закрыт, и закрытие ПРЕДЪЯВЛЕНО мутацией

`A-016` предписал мне перепрогнать узкую мутацию. Перепрогнал в отдельном worktree
(`/tmp/hft-rev-pr66-mut`, дерево слияния), обнулив **только вход свидетеля**:

```
771:    let observed: Vec<u32> = Vec::new(); // MUTANT: обнулён ТОЛЬКО вход свидетеля

test result: FAILED. 8 passed; 2 failed; 0 ignored
  sm8_per_segment_compaction_keeps_catalog_truthful ... FAILED
  sm9_compaction_midstate_does_not_duplicate_index  ... FAILED
thread 'sm8_…' panicked at crates/gateway/tests/red_segment_meta_bound.rs:772:5:
SM-8 такт 1 (шаг 6: .zst рядом с .jrnl): SETUP НЕ СОСТОЯЛСЯ — независимый обход каталога
не нашёл НИ ОДНОГО сегмента. …
```

**Числа сошлись дословно** (`8 passed; 2 failed`, те же два теста, падения на добавленном
ассерте и с его собственным сообщением). Расхождение ровно одно — номер строки; это F-3.

**Эталон свидетеля НЕЗАВИСИМ — проверено, а не принято на веру.** `testing.md` прямо
запрещает зависимый эталон («оракул `X == Y` невалиден, если `Y` вычисляется через `X`»).
Здесь:

```
686: fn truth_indices(dir: &Path) -> Vec<u32> {
687:     let mut v: Vec<u32> = journal::list_segments(dir)      ← обход с НУЛЯ
```

Свидетель снимается с `journal::list_segments`, а не с проверяемого `SegmentCatalog`. Будь
наоборот — пустой кеш подтверждал бы сам себя, и правка была бы украшением. Это ровно та
ловушка, в которую правка НЕ попала.

### 3.3. Изолирующий вакуум — утверждение отдельное, и оно верно

Документ отдельно называет, ЧТО закрывает свидетель: на пустом каталоге все три негативных
ассерта (`dangling.is_empty()` и далее) истинны, потому что все три — над результатом
ФИЛЬТРА. Прочитано на месте (`:777` и ниже) — так и есть: пустой кеш прошёл бы «сверку
правдивости» на всех шести тактах. Вакуум был реальным.

## 4. Block-«механизм на пути» (`gates.md` §4 DoD) — НЕ ПРИМЕНИМ, и это не поблажка

Milestone вводит механизм несущего пути ⇒ нужен оракул точки входа. Здесь механизм не
вводится: предмет — ОТКАЗ строить механизм плюс правка существующего оракула. Отказ
предъявлен замером и мутацией (§3), долг «built-not-wired» не возникает. Остаточный класс
назван самим документом как `COGNITIVE-ONLY` — это честная форма, а не пропуск.

## 5. Предъявление FA (M-66) — исполнено РУКАМИ, барьер здесь молчит

Барьер дал `SKIP` (tests-only), поэтому называю живой инвариант сам. Крейт `gateway` — это
Read Gateway Слоя 8, его FA — **`docs/fa/viz-backend.md`** (не `docs/fa/gateway.md`: такого
файла нет, проверено `ls docs/fa/`; `A-016` §2 называет его неточно).

Живые инварианты на аудируемой ревизии, оба прочитаны в файле:

- **`VB-I-2`** (`docs/fa/viz-backend.md:149`) — «**live == replay**: серия, посчитанная на
  live-хвосте, бит-идентична серии из replay того же окна журнала». **Это ровно тот
  инвариант, который защищает тронутый помощник.** Каталог, лгущий о составе сегментов,
  ломает `live == replay` молча: live-хвост считается по кешу, replay — по обходу с нуля.
  Помощник сверяет кеш с независимым обходом, а свидетель гарантирует, что сверять БЫЛО ЧТО.
- **`VB-I-10`** (`:158`) — «Bounded-window snapshot (M-37, TD-039)»: бюджет тика ограничен
  окном, а не числом сегментов истории. Это инвариант, ради которого написан весь файл
  `red_segment_meta_bound.rs` (шапка: «цена тика не зависит от ЧИСЛА СЕГМЕНТОВ», M-62/`TD-120`).

## 6. Находки

### F-1 — NOTE. Заголовок §2 противоречит собственной таблице: «ЧЕТЫРЕ» против пяти

`docs/plans/two-barriers-step1-2026-08-23.md:24`:

```
## 2. Разбор шести — ЧЕТЫРЕ из них уже несут свидетеля, невидимого детектору
```

Таблица под этим заголовком помечает **ЛОЖНОЕ срабатывание** у случаев 1, 3, 4, 5, 6 — это
**пять**, и строка `:35` говорит прямо: «**5 из 6 = 83 %**». Subject коммита `3b5e5b0` тоже
говорит «ПЯТЬ из шести» и «83 %».

**Воспроизведение:** `grep -n "ЧЕТЫРЕ\|5 из 6" docs/plans/two-barriers-step1-2026-08-23.md`
→ `:24` ЧЕТЫРЕ · `:35` 5 из 6.

**Происхождение — назову, потому что оно поучительно:** в первой редакции разбора случай 1
стоял подтверждённым кандидатом (4 ложных + 2 кандидата). Мутация перевела случай 1 в
ложные, тело и summary обновлены, **заголовок — нет**. Число пережило собственное
опровержение.

### F-2 — NOTE. «67 %» в §3 — то же протухшее число, второй экземпляр

`:57-58`: «Общая форма отброшена по 75 % ложных красных; **узкая форма (фильтр) даёт 67 %**
на выборке из шести». 67 % = 4/6 — арифметика ЧЕТЫРЁХ. Для той же узкой формы на той же
выборке из шести `:35` даёт 83 % = 5/6.

Итого документ называет свою несущую величину **тремя разными числами** (4 / 67 % / 83 %).
Вывод от этого не меняется — все три поддерживают «в основном ложные красные», — но
документ, чей ТЕЗИС состоит в том, что число держится командой, а не пересказом, обязан
держать собственное число.

### F-3 — NOTE. §4: мутация падает на `:772`, а не на `:771` — номер строки снова снят не с той редакции

Документ (`:88-93`) предъявляет прогон узкой мутации с падениями «`panicked at :771`». Мой
воспроизводящий прогон (§3.2) даёт **`:772:5`**. Раскладка оригинала:

```
771     let observed = truth_indices(dir);   ← строка, которую мутация ЗАМЕНЯЕТ
772     assert!(                             ← макрос, который ПАДАЕТ (Rust репортит его)
773         !observed.is_empty(),
```

`:771` — это вход свидетеля, `:772` — сам свидетель. Rust указывает место `assert!`.
Существо прогона (два теста, их имена, текст паники) сошлось дословно; неверен только номер.

**Почему это стоит отдельной находки, а не молчания:** это ТРЕТИЙ номер строки подряд в
одной и той же цепочке, снятый не с той редакции дерева. `R-114` N-1 уже поймал в §2 этого
же документа `:183-190` вместо `:239` — номер, взятый с МУТИРОВАННОГО дерева; коммит
`5d274e5` его исправил и назвал класс дословно: «номер строки, снятый на мутанте, к
оригиналу не относится». F-3 — тот же класс в соседней секции того же документа, переживший
исправление.

### O-1 — наблюдение (не находка предмета): я воспроизвёл остаточный класс `TD-163` СЛУЧАЙНО

Первый мой прогон барьеров на дереве слияния дал **`check_protected_artifacts.sh` exit=1,
FAIL=24** в ОБЕИХ прод-формах — то есть ровно противоположное заявленному. Причина — не
дефект предмета, а **порядок родителей в мерже, который я построил своей рукой**:

| моё дерево слияния | parent1 | parent2 | исход |
|---|---|---|---|
| `git merge origin/main` из ветки | ветка `c9b1589` | `main` `7c66843` | **exit=1, FAIL=24** |
| `refs/pull/66/merge` (форма GitHub) | `main` `7c66843` | ветка `c9b1589` | **exit=0, FAIL=0, NOTE=24** |

**Деревья ПОБИТОВО идентичны** (`cbfc2cc` в обоих случаях) — расходится только топология.
Механика прозрачна: `mainline` барьера = база плюс **first-parent**-цепочка, и при
`parent1=ветка` 24 архивных пути попадают в магистраль ⇒ fallback запрещён ⇒ FAIL.

Два следствия, оба практические:

1. **Заявление architect'а верно** — но верно именно для прод-формы. Проверять его надо было
   на `refs/pull/66/merge`, а не на самодельном мерже. Фиксирую как урок проводки:
   `gates.md` §8 велит проверять на дереве слияния и не уточняет порядок родителей, а он,
   как видно, исход переворачивает.
2. **`A-017` Р-2 подтверждён третьей рукой и без умысла.** Арбитр построил чувствительность
   к порядку родителей нарочно (F1a-swap, octopus-P1); я получил её случайно, просто собрав
   мерж «изнутри ветки». Это ровно тот остаточный класс, который уходит в `TD-164` (§7), и
   довод «требуется НАМЕРЕННЫЙ рукотворный акт» стоит читать с поправкой: намеренным должен
   быть дроп, но НЕВЫГОДНАЯ топология возникает и сама собой.

### F-4 — повторяю находку `A-016`, блокером не считаю

Коммит `3b5e5b0` носит тип `docs(plans)` при дифе, трогающем `crates/**`. `A-016` признал
это нарушением формы и постановил санкцию проспективной (исправление потребовало бы
пересборки ветки, под которой стоит чужая работа). Повторяю как аудит-след — правом,
которое тот же вердикт мне оставил.

## 6bis. Почему F-1..F-3 не блокируют — основание, а не снисхождение

Называю явно, потому что проект реджектил документы за ложные утверждения (`R-114` Б-1), и
молчаливое расхождение с прецедентом было бы хуже самих находок.

1. **Класс другой.** `R-114` Б-1 — утверждение о ВНЕШНЕМ состоянии (судьба PR), которое
   читатель проверить не мог. F-1..F-3 — внутренние расхождения, видимые на той же странице
   и опровергаемые соседней строкой. Читатель получает верное число из `:35`, из summary и
   из subject'а коммита.
2. **Вывод не зависит ни от одной из трёх.** 4/6, 67 % и 83 % — все три поддерживают
   «в основном ложные красные»; `:771`/`:772` не меняет ни одного теста и ни одного числа
   прогона. Существо я перепроверил своим прогоном независимо (§3).
3. **Цена круга несоразмерна.** `A-017` Р-5 взвесил доставку против БОЛЬШЕГО остаточного
   риска и постановил вливать; держать разблокированный PR ради трёх слов — защита дороже
   защищаемого. `docs/plans/**` не в триггерах §9 (`A-016` аргумент 1, прецедент PR #64) ⇒
   исправление приземляется автором СВОИМ PR, без нового круга гейта.
4. **Долг не исчезает молча** — он адресован §7 и предъявлен здесь поимённо, со строками.

## 7. Условия APPROVED (обязательны, исполняет architect)

1. **Правка F-1/F-2/F-3** одним doc-only коммитом: `:24` ЧЕТЫРЕ → ПЯТЬ · `:58` 67 % → 83 % ·
   `:88-93` `:771` → `:772`. Маршрут — свой PR автора (`gates.md` §9, `docs/plans/**` вне
   триггеров). Новый круг reviewer'а не требуется: правка не трогает ни код, ни вывод.
2. **`TD-164` завожу я** (§9) — `TECH-DEBT.md` reviewer-owned, architect в него не пишет.
3. **`bash scripts/reserve_artifact_id.sh --release R-123`** — после приземления этого файла.

## 8. Done Block — сырой вывод (агрегирован: зелёное сжато, красное целиком)

### 8.1. Предмет и дерево слияния

```
$ git rev-parse origin/main
7c66843bb48e1697e6d948b6fb30ac812a4e0888
$ git rev-parse origin/docs/two-barriers-step1-2026-08-23
c9b15898a5f84857fc40c07350d6dbeeb1c6f410
$ git merge-base origin/main c9b1589
92f6e65a90382506eb3fcc4ab09d4744ffeaade4          ← ветка ОТСТАЁТ, база старая
$ git fetch origin refs/pull/66/merge && git log -1 --format='%H %P' refs/prmerge66
82c5f57057f726b1401b2a4b143fa63b169ddc4e 7c66843bb48… c9b15898a5f…   ← прод-форма: main первым
$ git rev-parse HEAD^{tree} refs/prmerge66^{tree}
cbfc2ccd7565678e6dc475fee21ada88745691d7
cbfc2ccd7565678e6dc475fee21ada88745691d7          ← деревья идентичны, см. O-1
```

### 8.2. Базовая тройка CI на дереве слияния

```
$ cargo fmt --all -- --check ; echo exit=$?
exit=0
$ cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tail -2
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 24.21s
clippy_exit=0
$ cargo test --all 2>&1 | grep -E "^test result" | awk '{p+=$4; f+=$6} END {print "passed="p" failed="f" (блоков: "NR")"}'
passed=855 failed=0 (блоков: 206)
test_all_exit=0
```

### 8.3. `verify_design_claims.sh --merge-preview origin/main` — на дереве слияния

```
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | grep -E "^(FAIL|VERDICT)"
VERDICT: PASS (0 нарушений)
vdc_exit=0
```

(единственный `NOTE` — `contracts-current-state.md` без маркера `FACTS:`; предмета не касается,
в `main` присутствует до этого PR.)

### 8.4. Барьеры — ОБЕ прод-формы, на `refs/pull/66/merge` (форма GitHub, см. O-1)

```
## EVENT_NAME=push PUSH_BEFORE=7c66843
  check_protected_artifacts exit=0 FAIL=0 NOTE=24
  check_docs_freeze         exit=0 FAIL=0 NOTE=0
  check_gate_meta           exit=0 FAIL=0 NOTE=0
  check_review_fa           exit=0 FAIL=0 NOTE=0   (SKIP: только tests/ — не прод-пути)
  check_artifact_ids        exit=0 FAIL=0 NOTE=0
## EVENT_NAME=pull_request PR_BASE_SHA=7c66843
  check_protected_artifacts exit=0 FAIL=0 NOTE=24
  check_docs_freeze         exit=0 FAIL=0 NOTE=0
  check_gate_meta           exit=0 FAIL=0 NOTE=0
  check_review_fa           exit=0 FAIL=0 NOTE=0
  check_artifact_ids        exit=0 FAIL=0 NOTE=0
```

24 `NOTE` — видимый след архивного переезда `bd357df`, НЕ отказ. Подтверждено: заявление
architect'а о `exit=0 / FAIL=0 / NOTE=24` в обеих формах воспроизведено мной.

**КРАСНОЕ — печатаю целиком, потому что оно было (см. O-1).** На САМОДЕЛЬНОМ мерже
(`parent1=ветка`) в обеих формах:

```
  check_protected_artifacts exit=1 FAIL=24 NOTE=0
FAIL  milestones/M-53-td083-push-seek.md: артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял
FAIL  milestones/M-58-depth-metric.md: артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял
FAIL  milestones/M-59-lifetime-memory.md: артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял
FAIL  milestones/M-62-segment-metadata.md: артефакт ИСЧЕЗ с HEAD, и ни один коммит его не удалял
      … (24 всего)
```

Это НЕ дефект предмета — это дефект МОЕЙ проводки, разобранный в O-1 и оставленный в
вердикте намеренно: гейт, чей исход зависит от того, как проверяющий собрал мерж, обязан
быть предъявлен вместе с этим свойством.

### 8.5. Узкая мутация свидетеля (предписана `A-016` §порядок п.2)

```
$ sed -i 's|let observed = truth_indices(dir);|let observed: Vec<u32> = Vec::new();|' \
      crates/gateway/tests/red_segment_meta_bound.rs
$ cargo test -p gateway --test red_segment_meta_bound
test sm8_per_segment_compaction_keeps_catalog_truthful ... FAILED
test sm9_compaction_midstate_does_not_duplicate_index ... FAILED
thread 'sm8_…' panicked at crates/gateway/tests/red_segment_meta_bound.rs:772:5:
  SM-8 такт 1 (шаг 6: .zst рядом с .jrnl): SETUP НЕ СОСТОЯЛСЯ — независимый обход каталога
  не нашёл НИ ОДНОГО сегмента. Три проверки ниже истинны на пустоте, и такт сертифицировал
  бы отсутствие предмета
test result: FAILED. 8 passed; 2 failed; 0 ignored; 0 measured; 0 filtered out
```

Ожидание `A-016` — `8 passed; 2 failed` на этих двух тестах — **выполнено**; расхождение
только в номере строки (F-3). Мутация проводилась в ОТДЕЛЬНОМ worktree; предмет не тронут.

### 8.6. Свидетели пяти «ложных срабатываний» — существуют (§3.1)

```
$ grep -n "fn claims" crates/gateway/tests/red_catalog_equivalence.rs      → 240
$ grep -n "restored > 0" crates/journal/tests/red_compaction.rs            → 388
$ grep -n "assert_eq!(code, 0" crates/journal/tests/red_replay_digest_delivery.rs → 207, 238
$ grep -n "zst_before >= 2\|pruned_zst >= 2\|cold_zst >= 2" …red_retention_compacted.rs → 393, 409, 433
$ grep -n "lives_born, 1" crates/research-cli/tests/red_depth_lifetime_perlife.rs → 378
```

### 8.7. Ярус C — грепом по предмету (`reading-map.md` §2), не целиком

`TECH-DEBT.md` + `PROJECT-STATE.md` = 1 015 KB ≈ 226 k токенов; читать целиком нельзя, и я
называю, что искал:

```
$ grep -n "TD-163" TECH-DEBT.md                       → :96 (карточка), :897-899, :963
$ grep -n "вакуум\|two-barriers\|барьер A" TECH-DEBT.md → :939, :1078
$ grep -n "TD-163\|check_protected_artifacts" PROJECT-STATE.md → :116, :1569, :1628-1643
$ grep -oE '^\- \*\*TD-[0-9]+\*\*' TECH-DEBT.md | sort -n | tail -1 → TD-163
```

Подтверждено: `TD-163` в `TECH-DEBT.md` описан как MAJOR-блокер доставки и закрыт PR #75
(`main` `15abd0c`); остаточный класс карточки не имел — завожу `TD-164` (§9).

## 9. `TD-164` — карточка остаточного класса (исполнение `A-017` Р-5 довесок 2)

Завожу в `TECH-DEBT.md` в ближайшем close-out (`TECH-DEBT.md` — моя зона; architect в неё не
пишет). Текст — по `research/arbitration/A-017-td163-fallback-class.md`, severity **MAJOR**,
зона **architect**, с добавлением моего воспроизведения O-1 как второго свидетельства.

## 10. Итог

**APPROVED** при условиях §7. Merge — `gh pr merge 66 --merge --delete-branch` по КОДУ
ВОЗВРАТА `gh pr checks 66 --watch`, далее `gates.md` §8 целиком.

**Reviewer: scout · 2026-08-24 · `gates.md` §4 · маршрут по `A-016`**
