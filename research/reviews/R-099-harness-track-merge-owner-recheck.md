<!-- GATE-META
milestone: PR-57
audited_repo: a3ka/hft-platform
audited_base: d61904d71604f75cd1ccbd93ad0cd295ccd2e744
audited_head: 700f37c6c52df54a802828944808a365d3096027
verdict: APPROVE
-->

# R-099 — перепроверка §9: PR #57, merge в харнесс-треке делает автор-architect

**Роль:** независимый architect-клон (Fable, свежий контекст), перепроверка уставной правки
по `gates.md` §9. Автор правки — ведущий architect, он сторона и себя не проверяет.

**Предмет:** коммит `700f37c` на ветке `docs/harness-track-merge-owner` (fast-forward от
`origin/main` = `d61904d`, `git merge-base` совпадает с базой ⇒ дерево слияния = дерево
ветки). Диф: `docs/workflow/harness-track.md` §3 (+20/−1) + `docs/PENDING-SIGNATURE.md`
`П-017` B амендмент (+7). Исполняет решение founder'а 22.08, СУЖАЮЩЕЕ `П-017` B.

## Вердикт: **APPROVE** — безусловный; три NOTE ниже merge не держат

---

## §а — Утверждения правки, каждое командой на дереве слияния

Общий прогон: `bash scripts/verify_design_claims.sh --merge-preview origin/main` →
`VERDICT: PASS (0 нарушений)`, exit=0.

**1. «Общая норма отдаёт merge reviewer'у» — ВЕРНО.**
`commit-discipline.md:88-89` (п.4 Auto-push): «Reviewer (не dev, не architect) — последний в
цепочке для substantive-изменений; именно reviewer вливает работу после APPROVED»;
`gates.md:313`: «feat-ветка `feat/M-NN`, которую reviewer мержит уже зелёной»;
`docs/04-workflow.md` §2: цепочка заканчивается «reviewer: … APPROVED → merge».

**2. «Reviewer в маршруте трека НЕ УЧАСТВУЕТ» — ВЕРНО.**
`docs/workflow/harness-track.md` §3: автор (architect) → прогон → адверсарий → правки → PR.
Роли reviewer в маршруте нет; §5 «Гейт трека» требует вердикт АДВЕРСАРИЯ файлом, зелёный
`status-check` — reviewer нигде. Канон это уже признаёт: `docs/04-workflow.md` §2 (файл
замка §11!) с 2026-08-15 несёт абзац «Харнесс идёт ДРУГИМ маршрутом» с отсылкой в
`harness-track.md` — то есть делегирование маршрута треку санкционировано на уровне
запертого канона, и новая строка о владельце merge законно живёт в делегированном документе.

**3. «`docs/workflow/**` вне замка §11» — ВЕРНО, проверено МЕХАНИЗМОМ, не только текстом.**
Текст: `gates.md` §11 — зона `.claude/rules/** · .claude/agents/** · .claude/wrappers/** ·
CLAUDE.md · docs/04-workflow.md`. Механизм: `scripts/check_docs_freeze.sh:61-66` `in_zone()`
— те же пять членов, `docs/workflow/*` не матчится. Прогон прод-формой вызова:
`EVENT_NAME=pull_request PR_BASE_SHA=d61904d… bash scripts/check_docs_freeze.sh` → exit=0
(барьер на PASS молчит по конструкции). Расхождения текста и барьера НЕТ.

**4. «Не впрыскивается, бюджет не расходует, запас `gates.md` 48 B» — ВЕРНО.**
`bash scripts/check_context_budgets.sh` на дереве слияния: 7 файлов = 6×`.claude/rules/*` +
`CLAUDE.md`; `docs/workflow/**` не считается. Строка: `OK .claude/rules/gates.md
47852 B / 47900 B (запас 48 B)` — число коммита подтверждено механизмом. Расхождение с
`П-017` A3 («запас 17 B», снят 20.08) — НЕ противоречие: `gates.md` менялся после замера
(`git log`: `7c89e5c` 2026-08-20, до того `7e7329c` 2026-08-17); «сегодня 48 B» снято на
текущем дереве и верно.

**5. «Без этой строки у PR трека не было разрешённого исполнителя» — ВЕРНО; правка НЕ
избыточна (проверялся ранг REJECT — не подтверждён).**
Carve-out `commit-discipline.md` п.5 разрешает architect'у ТОЛЬКО «чисто-процессные правки
(`.claude/rules/*`, `docs/04-workflow.md` и т.п.), НЕ ТРОНУВШИЕ КОД». PR трека по
определению зоны (`harness-track.md` §2) несёт код: `scripts/check_*.sh`,
`scripts/tests/red_*.sh`, `.github/workflows/**` — carve-out его не покрывает. Профильный
carve-out (`architect.md:23-27`) перечисляет те же процессные пути, скриптов нет. Общая
норма п.4 отдаёт substantive-merge reviewer'у — которого в маршруте нет. Дыра реальна;
`R-085` B-1 (`research/reviews/R-085…:209,383`) зафиксировал ровно этот класс для
процессных правок — цитата коммита точна.

**6. «`П-017` несёт ССЫЛКУ, а не пересказ» — ВЕРНО.**
Амендмент несёт: дословную цитату founder'а (совпадает со словами решения буква в букву),
следствие для milestone-цепочки и ссылку «норма записана … `docs/workflow/harness-track.md`
§3». Нормативная формулировка «мержит автор-architect» существует в ОДНОМ месте —
`harness-track.md` §3. Дубля нормы не возникло (историческая справка о широкой редакции в
обоих носителях — жанр «разбор», не норма; см. N-1).

## §б — Полномочия

- Замок §11 НЕ задет: диф — `docs/PENDING-SIGNATURE.md` + `docs/workflow/harness-track.md`
  (`git show --name-only 700f37c`), обе вне `in_zone()`; барьер exit=0. Токен не нужен — и
  правильно, что его нет.
- Граница C (`gates.md` §0.1): владелец merge — вопрос «чего мы хотим», и он РЕШЁН founder'ом
  явно, дословная цитата в амендменте. Правка решение НЕ РАСШИРЯЕТ, а сужает: широкая
  редакция («architect мержит всю цепочку») снята, узкая ограничена маршрутом, где
  reviewer'а нет по построению; в полном цикле merge остаётся за reviewer'ом. Сравнение
  текста правки со словами founder'а — покрытие 1:1, приращения нет. «Автор-architect» =
  «архитектор» founder'а: в маршруте §3 автор — architect по построению.
- Зона правки — в Writes профиля architect'а (`docs/**` кроме reviewer-owned). OK.

## §в — Связность

Проверены все ссылки правки и тела коммита: `commit-discipline.md` п.4/п.5 ✓, `R-085` B-1 ✓
(файл существует, §B-1 говорит цитируемое), `branch-hygiene.md` п.6 ✓ (замер 14 worktree,
2026-08-03), `harness-track.md` §3 ✓, `check_gate_meta.sh` ✓, `П-017` B ✓. Одна ссылка —
с оговоркой (N-2).

## Адверсарная часть

**Сужение — точно сужение.** Дословные слова founder'а: «Давай пока это решение уберем или
откорректируем. Оставим архитектора в условиях когда работаем с харнесс треком, поскольку
ревьювер здесь не участвует и некому мержить». Правка даёт architect'у merge ТОЛЬКО в треке
и явно возвращает milestone-цепочку reviewer'у. Больше сказанного не дано. Граница трека
правкой не трогалась: §4 (что треком НЕ идёт) и §6 (механического барьера границы нет —
`COGNITIVE-ONLY`, кандидат на механизацию назван) не изменены ни на строку — правка не
расширяет канал, только называет, кто жмёт кнопку в конце.

**COGNITIVE-ONLY назван честно.** «Кто нажал merge — машинно непроверяемо» подтверждается
первоисточником: `branch-hygiene.md` п.6 — git-личность единая, роль различима только меткой
subject'а, которую ставит сам исполнитель. Правка прямо ЗАПРЕЩАЕТ выдавать за механизм
строку Handoff и `check_gate_meta.sh` — тот же предел, что уже назван в `П-017` B
(`check_gate_meta.sh:278-291` поля verdict не читает). За механизм ничего не выдано;
реальный механизм у merge остаётся один — блокирующий `status-check` (branch protection),
и он правкой корректно назван барьером содержимого, а не исполнителя.

**Противоречия по корпусу** (`git grep -n 'мержит\|вливает\|pr merge' -- .claude/ docs/`):
- `.claude/skills/harness-track/SKILL.md:116-122` — уже ДО правки инструктировал «architect
  правит по находкам и вливает через PR». Практика бежала впереди нормы; правка закрывает
  разрыв, носитель СОГЛАСОВАН с новой нормой, правки не требует.
- `.claude/agents/architect.md:23` «Не мержит PR ЧУЖОЙ работы (milestone-цепочка…)» — PR
  трека есть СВОЯ работа автора; запрет не задет, скобка прямо называет milestone-цепочку.
- `.claude/agents/reviewer.md:31`, `commit-discipline.md:89`, `gates.md:313`,
  `04-workflow.md` §2 — все о milestone-цепочке, где норма НЕ менялась. Осталась одна
  шероховатость чтения — N-3.

## Находки (все NOTE, merge не держат)

**N-1 (NOTE).** Историческая справка широкой редакции `П-017` B продублирована в обоих
носителях (амендмент `docs/PENDING-SIGNATURE.md:913-919` и `harness-track.md` §3 абзац
«Исходная редакция…»). Норма — в одном месте, но ПЕРЕСКАЗ ИСТОРИИ — в двух: при будущей
правке одного носителя второй разойдётся. По `M-65` §10bis жанр «разбор» дублировать
допустимо; фиксирую как точку возможного расхождения, действий не требует.

**N-2 (NOTE).** Тело коммита цитирует «правило носителей, M-65 §10bis» — документ
`milestones/M-65-ws-session.md` §10bis:597 существует ТОЛЬКО на ветке
`origin/fix/M-65-battery-recalibration`; в `main` и в дереве слияния его НЕТ (проверено:
`git ls-tree`/`git show` по всем M-65-веткам). Цитата по существу верна (открыл
первоисточник на ветке), но опирается на branch-only документ: если ветка не доедет до
`main`, обоснование повиснет. В самих ФАЙЛАХ правки этой ссылки нет — только в теле
коммита, поэтому NOTE, не блокер.

**N-3 (NOTE).** `commit-discipline.md:88` («Reviewer — последний в цепочке для
substantive-изменений») остаётся без оговорки о треке. Иерархия держится через
`04-workflow.md` §2 (файл замка §11 сам отсылает в `harness-track.md`), поэтому противоречия
НОРМ нет; но читатель ОДНОГО `commit-discipline.md` получит неполную картину. Правка этого
файла — замок §11 + бюджет (запас 421 B), осознанно не делалась («правка зоны §11 НЕ
ТРЕБУЕТСЯ») — решение принимаю; кандидат на будущий круг, если класс «читатель одного
носителя» проявится замером. Дефект описываю, фикс не проектирую.

## Условие APPROVED

Безусловный. NOTE-находки записаны, ни одна не требует правки до merge.

## Done Block

```
$ git merge-base origin/main 700f37c
d61904d71604f75cd1ccbd93ad0cd295ccd2e744        # = origin/main tip: FF, дерево слияния = дерево ветки

$ bash scripts/verify_design_claims.sh --merge-preview origin/main 2>&1 | tail -2
VERDICT: PASS (0 нарушений)
exit=0

$ EVENT_NAME=pull_request PR_BASE_SHA=d61904d… bash scripts/check_docs_freeze.sh; echo exit=$?
exit=0                                          # PASS молчит по конструкции; диф зону §11 не трогает

$ bash scripts/check_context_budgets.sh 2>&1 | grep -E 'gates|VERDICT'
OK    .claude/rules/gates.md                     47852 B /   47900 B (запас 48 B)
VERDICT: PASS — 7 файлов, 111943 B из 114900 B бюджета (запас 2957 B)

$ git show --name-only --format= 700f37c
docs/PENDING-SIGNATURE.md
docs/workflow/harness-track.md

$ bash scripts/next_artifact_id.sh R
R-099
```

Ярус C грепался по предмету: «мержит|вливает|pr merge» по `.claude/ docs/`; `П-017`
открыт целиком; `M-65` §10bis открыт на ветке-носителе.
