<!-- GATE-META
milestone: TD-156
audited_repo: a3ka/hft-platform
audited_base: 92f6e65a90382506eb3fcc4ab09d4744ffeaade4
audited_head: 752502d
verdict: ESCALATE
-->

# R-115 — TD-156: разведение пяти пар заблокировано запретным списком M-61 §5

**Роль:** reviewer · **Дата:** 2026-08-23 · **Класс:** триаж долга, не PR-гейт милестоуна.
**Предмет:** диспетч founder'а «закрыть TD-156 — развести пять пар „один идентификатор —
два предмета“, номера брать `scripts/reserve_artifact_id.sh`».
**База:** `origin/main` `92f6e65` · **Носитель записи:** `752502d` (влит PR #67, `29c4640`).

## ВЕРДИКТ: ⚠️ ESCALATE — диспетч НЕ исполнен, требуется решение founder'а

Исполнение поручения нарушило бы коммитнутую спеку. Это находка, а не недоделка.

---

## F-1 — `M-61` §5 запрещает ровно эти пять переименований

`milestones/M-61-artifact-ids.md` §5 «Запретный список», строка 1:

> Переименовывать СУЩЕСТВУЮЩИЕ множественности (`C-018`, `C-024`, `C-058`, `M-46`,
> `R-001`, `R-035`, `R-038`) | на них ссылаются вердикты, милестоуны и шапки
> sacred-оракулов; переименование рвёт цепочку доказательств ровно так, как её рвали
> висячие ссылки `TD-110`

Перечень запрета совпадает с перечнем поручения. `R-001` и `R-038` на `origin/main`
сегодня одноносительные (`git ls-tree --name-only HEAD research/reviews/`), потому в
таблицу сверки `TD-156` они и не попали.

**Воспроизведение:**
```
awk '/^## 5\./,/^## 6\./' milestones/M-61-artifact-ids.md | grep -o \
  'Переименовывать СУЩЕСТВУЮЩИЕ множественности ([^)]*)'
```

## F-2 — основание запрета истинно: ссылки живут в SACRED-файлах

Замер на дереве слияния (`92f6e65` + `752502d`), не на отставшей ветке:

```
C-018:  93 / 23     C-024:  78 / 23     C-058: 211 / 29
M-46:  413 / 62     R-035:  94 / 30     ─── 889 упоминаний, 111 файлов
```

Из них шапки sacred-оракулов, недоступных ни reviewer'у, ни dev'у:

| файл:строка | текст |
|---|---|
| `crates/gateway-serve/tests/red_ws_series_vs_replay.rs:1` | `M-46 O-1/O-2 (architect, sacred)` |
| `crates/gateway-serve/tests/red_ws_protocol.rs:1` | `M-46 O-3/O-4/O-5 (architect, sacred)` |
| `crates/gateway-serve/tests/red_ws_honesty_sessions.rs:1` | `M-46 O-6/O-7 (architect, sacred)` |
| `crates/gateway/tests/red_tail_cursor_prod_form.rs:1` | `M-57, вердикт `R-035`` |
| `crates/journal/tests/red_tail_scan_bounded.rs:103` | `` `R-035` F-035-1: 8003 события вместо 3 `` |
| `crates/journal/tests/red_stitch_monotonic.rs:9` | `probe risk-critic C-018 rev2` |

Переименование носителя БЕЗ правки всех 889 ссылок превращает ссылку ДВУСМЫСЛЕННУЮ в
ВИСЯЧУЮ — меняет дефект MINOR на дефект класса `TD-110`, ради недопущения которого
запрет и написан.

## F-3 — зона: носители не принадлежат reviewer'у

Носители живут в `milestones/**`, `research/**`, ссылки — ещё и в `crates/**/tests/**`
(sacred) и `scripts/tests/**` (харнесс). Зона reviewer'а — `PROJECT-STATE.md`,
`TECH-DEBT.md`, PR-комменты (`scope-guard.md`). Сама карточка `TD-156` закрепляет
носителей за architect'ом. Исполнить поручение своими руками означало бы совершить
нарушение, которое я же обязан реджектить в Block-scope.

## F-4 — по `M-46` выбор объявлен НЕ инженерным

`TD-156` (а): «выбор, какой из двух артефактов теряет свой адрес, — не инженерный».
Младшая спека несёт founder-подпись 27.07; старшая владеет `scripts/verify_M-46.sh` и
всеми sacred-оракулами M-46. Граница C (`gates.md` §0.1) — не делегируется даже арбитру.

---

## Условие снятия ESCALATE — одно из трёх, исполнитель во всех architect

1. **Снять строку `M-61` §5** — тогда переименование носителей И правка всех 889 ссылок
   идут ОДНОЙ цепочкой коммитов; иначе получаем `TD-110`.
2. **Признать пять пар ПОСТОЯННЫМ легаси** — `TD-156` закрывается как «принятый
   остаточный риск», в `gates.md` §12 идёт строка о предсуществующих множественностях.
3. **Узкий путь только для `C-058`** — единственная пара «один предмет»: форвард-путь
   БУКВОЙ (`C-058b`) прямо предписан founder'ом и запиннен пробой `L6LETTERC`
   (`scripts/tests/red_artifact_ids.sh:757-766`); буква сохраняет текстовую цепочку.

## Что сделано вместо исполнения

Врезка в `TD-156` (`TECH-DEBT.md`, reviewer-owned) с замером и отменой прежней строки
«сперва развести пять пар». `TD-156` остаётся **OPEN**, severity MINOR не повышена.
`reserve_artifact_id.sh` под переномерацию НЕ вызывался — резерв под неисполняемый план
оставил бы висячие `refs/reserved/*` на origin. Номер этого вердикта взят механизмом:
`R-112` оказался занят параллельной сессией, резерв перепрыгнул на `R-115`.

## Done Block

```
$ EVENT_NAME=pull_request PR_BASE_SHA=92f6e65 bash scripts/check_artifact_ids.sh
OK: ни один новый артефакт не введён (диапазон 92f6e65..HEAD)          exit=0
$ EVENT_NAME=push PUSH_BEFORE=92f6e65 bash scripts/check_artifact_ids.sh
OK: ни один новый артефакт не введён (диапазон 92f6e65..HEAD)          exit=0
$ EVENT_NAME=pull_request PR_BASE_SHA=92f6e65 bash scripts/check_protected_artifacts.sh
OK: защищённые артефакты целы на HEAD                                  exit=0
$ EVENT_NAME=pull_request PR_BASE_SHA=92f6e65 bash scripts/check_docs_freeze.sh
                                                                       exit=0
$ gh pr checks 67 --watch >/dev/null 2>&1; echo $?
0                                                                   (17/17 pass)
$ git show --numstat --format='' 752502d
59      1       TECH-DEBT.md
```

**Форма проверки (носители не тронуты):**
```
$ git status --porcelain research/ milestones/ crates/ scripts/
{пусто}
$ ls research/critiques research/reviews milestones \
    | grep -oE '^[MRC]-[0-9]+[a-z]?' | sort | uniq -c | awk '$1>1'
  C-018 → 2   C-024 → 2   C-058 → 2   M-46 → 2   R-035 → 2
```
Пять множественностей на месте — как и должно быть при блоке; ничего не скопировано и
не перенесено.

## Предел этого вердикта, названный явно

Диапазон не трогает `crates/**`, поэтому джоб `review-fa` даёт **SKIP**
(`check_review_fa.sh:57`) — зелёный джоб здесь прочтения FA не доказывает. Живой
инвариант тронутого модуля не называется за отсутствием тронутого модуля.

`FA-WAIVER: N/A — диапазон вне crates/**`
