<!-- GATE-META
milestone: M-58
audited_repo: a3ka/hft-platform
audited_base: cf24aac7ac52814df2c01226e50705e32f7070ae
audited_head: 7d19c5cdf7a70e836e5aebc37ed0a4a6aa537b13
verdict: APPROVED
-->

# R-035 — PR-гейт M-58, круг 2: метрика жизненного цикла уровня (учёт НА ЖИЗНЬ)

**Роль:** reviewer. **Дата:** 2026-08-05T22:03Z. **Круг:** 2 (после `R-033` REJECT).
**Дерево:** `/tmp/hft-rev-m58b`, detached от `origin/feat/M-58-depth-metric`.
**merge-base:** `e4c45c7`. **Дерево слияния:** `/tmp/hft-rev-m58-merge` от `origin/main`.

**ШАГ 0 пройден:** `origin` = `https://github.com/a3ka/hft-platform.git`, toplevel
`/tmp/hft-rev-m58b`, HEAD `7d19c5c`.

**Ветка занята чужим worktree** (`/tmp/hft-research-dev-1785794966`, там `feat/M-58-depth-metric`
на `d7318aa` — два дня без движения). Вместо перехвата ветки взят detached-чекаут
(`branch-hygiene.md` п.2, запасной путь). Побочно замечено: локальный ref
`feat/M-58-depth-metric` = `d7318aa` отстал от `origin/` на 7 коммитов — судится `origin`,
то есть то, что реально вливается.

---

## ВЕРДИКТ: **APPROVED**

Все три блокера круга 1 закрыты и **перепроверены прогоном, а не по сообщениям коммитов**.
Мутационный контроль прогнан мной независимо и воспроизвёл заявленные спекой числа
до последнего разряда. Остаются два MINOR и один NOTE — заводятся долгом, merge не блокируют.

| находка `R-033` | severity | статус | чем предъявлено |
|---|---|---|---|
| F-1 транскрипция выборочна | BLOCKER | ✅ ЗАКРЫТА | 14/14 строк + сырой артефакт; мой независимый прогон численно ИДЕНТИЧЕН |
| F-2 замок A-002 снят вопреки числам | BLOCKER | ✅ ЗАКРЫТА | вердикт переписан пополосно; замок ОСТАЁТСЯ; проверено ПО ЧИСЛАМ |
| F-3 fmt красит CI | BLOCKER | ✅ ЗАКРЫТА | `cargo fmt --all -- --check` exit=0 |
| F-4 гейт зеленее CI | MAJOR | ✅ ЗАКРЫТА (остаток → N-3) | fmt+`test --all` в гейте; DV-I-7/8 гоняются |
| F-5 `finished: Vec` без границы | MAJOR→TD | ✅ вынесена | спека `M-59-lifetime-memory.md`; долг завожу при close-out (TD-107) |
| F-6 §Tasks все OPEN | MINOR | ⚠️ ОСТАТОК | задача 3 всё ещё `🚧 IN_PROGRESS` при выполненной работе → N-2 |
| F-7 ссылка на занятый TD-098 | MINOR | ✅ ЗАКРЫТА | ложные ссылки сняты; канонический номер теперь TD-103 (см. N-4) |

---

## Block-scope — ✅ PASS

Sacred-зоны тронуты ТОЛЬКО architect'ом. Проверено пофайлово по всем 20 коммитам ветки:

```
$ for c in $(git log origin/main..HEAD --format=%h); do ... grep -E 'tests/|verify_.*\.sh' ...
51d8450 [architect] :: scripts/verify_M-58.sh
c2e52e5 [architect] :: scripts/verify_M-58.sh
c5b4648 [architect] :: scripts/verify_M-58.sh
bb68545 [architect] :: tests/red_depth_band_3060.rs tests/red_depth_lifetime.rs tests/red_depth_lifetime_perlife.rs
28bba4d [architect] :: tests/red_depth_band_3060.rs tests/red_depth_lifetime.rs
69fe751 [architect] :: tests/red_depth_lifetime_perlife.rs scripts/verify_M-58.sh
```

Ни одного касания `crates/research-cli/tests/**` или `scripts/verify_*.sh` не-architect'ом.
research-dev остался в `src/`, `examples/`, `research/data-quality/**` — своей зоне по
§Allowed paths милестоуна.

**Contract Block-C — N/A.** `crates/contracts/**` не тронут (диф пуст), `BandReport`/
`LifetimeReport` живут в `research-cli` (T2/T3) ⇒ contract-RFC не требуется, `CT-I-*` не
затронуты.

**RISK-BLOCK — N/A.** `crates/{risk,killswitch,oms,venue-*}` не тронуты. Проверено:
`git diff origin/main...HEAD --stat -- crates/risk crates/killswitch crates/oms 'crates/venue-*'`
→ пусто.

---

## F-1 — ЗАКРЫТА. Проверена не чтением файла, а повторным замером

Круг 1 нашёл, что из 14 строк прогона в файл перенесли 6, и среди выпавших оказались два
худших числа. Теперь:

- `research/data-quality/depth-lifetime-results.md` §M-58 — **14 строк** (7 полос × 2 стороны);
- `research/data-quality/m58-rerun-segment78.txt` — сырой вывод **файлом в репозитории**
  (закрывает «сохранён в stdout прогона», `DESIGN.md` §16.3).

**Независимая пересъёмка reviewer'ом** на том же сегменте (`/tmp/m33-journal`, segment 78, 1.0 GB):

```
$ cargo run --release -p research-cli --example depth_lifetime -- /tmp/m33-journal > /tmp/r035-rerun.txt
$ diff /tmp/r035-rerun.txt research/data-quality/m58-rerun-segment78.txt
0a1,2   > [M-32] reading journal dir=... epochs=["own-2026-07"]
0a1,2   > [M-32] window: first_ts_ms=... l2delta=121241 trades=291623
22a25   > [M-32] running consistency over 412864 events...
```

Расходятся ровно **три строки, и все три — stderr-баннер** (закоммиченный артефакт снят с
`2>&1`, мой прогон разделил потоки). **Всё численное содержимое — 14 строк таблицы, сводка
NEAR/FAR, `consistency_rate`, сводный JSON — идентично.** Замер воспроизводим и честен.

Отдельно проверено, что величина ДЕЙСТВИТЕЛЬНО сменилась, а не переименовалась: против M-32
на том же сегменте `bid [0,150)` даёт `64 781 → 315 200` рождений (жизней впятеро больше цен),
`NEAR 0.981 → 0.991`, `FAR 0.805 → 0.965`.

---

## F-2 — ЗАКРЫТА. Замок проверен ПО ЧИСЛАМ, как требует мандат

**Механический замок З-1 держится:**
```
$ grep -rn GATEWAY_BANDS docker-compose.yml
docker-compose.yml:134:      GATEWAY_BANDS: ${GATEWAY_BANDS:-0.001}
docker-compose.yml:197:      - --bands=${GATEWAY_BANDS:-0.001}
$ git diff origin/main...HEAD --stat -- docker-compose.yml
(пусто — ветка файл не трогает)
```
`0.001 ≤ 0.013` — дефолт внутри валидированной зоны в ОБОИХ местах. Тихого включения полос
не произошло.

**Текстовый вердикт переписан по полным числам** (`depth-verdict.md:64` — «**замок A-002
ОСТАЁТСЯ**»), пополосно и посторонне: bid подтверждает живость на всех семи полосах
(0.713–0.992); ask опровергает на трёх из шести глубже 1.3 % — `[300,500)=0.419`,
`[800,1500)=0.247`, `[3000,6000)=0.403`. Исход назван **третьим — смешанным**, которого
`A-002` не предусматривал, и по конструкции арбитража milestone не снимает замок сам.
Вопрос оформлен founder'у в `PENDING-SIGNATURE.md` П-011 (граница C — состав прод-выдачи).
Противоречие круга 1 («live-verified 1.5–60 %» против «`[150,300)` включать нельзя») снято.

**Отмечаю отдельно, потому что это редкость.** `depth-verdict.md:129-136` фиксирует, что
**собственное предсказание авторов ОПРОВЕРГНУТО замером**: ждали смещения ВВЕРХ (фантом
TD-016 засчитывается как cancelled), а старая метрика на глубине живость **занижала**
(bid `[3000,6000)`: 0.622 → 0.873). Дефект метрики реален (зависимость от окна и плотности
сетки), но вывод о живости он не завышал. Записано без подгонки задним числом — ровно то
поведение, которое требует `DESIGN.md` §16.3 и `testing.md`.

---

## F-3 / F-4 — ЗАКРЫТЫ прогоном

```
$ cargo fmt --all -- --check; echo exit=$?
exit=0                                    # круг 1: exit=1, 8 расхождений

$ grep -n 'cargo (fmt|clippy|test)' scripts/verify_M-58.sh
86: cargo fmt --all -- --check            # добавлено по F-4
93: cargo test --all                      # добавлено по F-4
54: for t in red_depth_lifetime red_depth_band_3060 red_depth_scale   # DV-I-7/8 добавлены по F-4 п.2
```

Гейт на ветке: **22 PASS / 0 FAIL, `VERDICT: PASS`, exit=0**, в т.ч. `cargo test --all` —
193 блока зелёных.

---

## Мутационный контроль — прогнан reviewer'ом независимо (мандат, п.2)

Спека (`M-58-depth-metric.md:89-98`) заявляет: реализация была подменена фиктивной
(переименование полей БЕЗ смены семантики), и «все пять оракулов провалились с ожидаемыми
числами». Проверил сам, двумя разными мутациями.

### Мутация 1 (узкая) — возврат предиката рождения

```rust
- let new_birth = self.states.get(&l.price).is_none_or(|s| s.fate != Fate::Alive);
+ let new_birth = !self.states.contains_key(&l.price);        // семантика M-32
```
Результат: **упал только DV-I-13** (`left (1,1) / right (2,2)`), четыре оракула зелёные.

**Это не дефект оракулов, и я проверил почему.** Предикат — не единственный носитель
семантики: в текущем коде отмена УДАЛЯЕТ запись из `states` (`depth_lifetime.rs:164`), поэтому
`!contains_key` после отмены снова истинно и перерождение всё равно засчитывается. Мутация
меняет поведение лишь там, где состояние остаётся неатрибутированным — две полные жизни
одной цены ВНУТРИ одного тика. Ровно этот случай и держит DV-I-13. Гранулярность верная.

### Мутация 2 (заявленная спекой) — фиктивная реализация M-32 + переименование полей

Взял `crates/research-cli/src/depth_lifetime.rs` из `origin/main` (учёт НА ЦЕНУ) и переименовал
`born/cancelled/frozen/censored → lives_*`. Публичный API совпадает (проверено дифом
сигнатур), сборка проходит по типам — то есть это именно «фиктивная реализация», описанная
в спеке.

```
$ cargo build -p research-cli --tests            → Finished, exit=0
$ cargo test -p research-cli --test red_depth_lifetime_perlife
test dv_i_10_rebirth_on_same_price_is_a_new_life ................. FAILED   left: 1        right: 2
test dv_i_11_metric_does_not_saturate_with_window_length ......... FAILED   left: (1,1,0)  right: (2,1,1)
test dv_i_12_censored_life_does_not_poison_later_lives ........... FAILED   left: 1        right: 2
test dv_i_13_lives_balance_on_degraded_input ..................... FAILED   left: (1,1)    right: (2,2)
test dv_i_14_band_is_attributed_per_life_and_output_is_deterministic  FAILED left: 0        right: 1
test result: FAILED. 0 passed; 5 failed
```

**Все пять чисел совпали со спекой построчно** (таблица `M-58-depth-metric.md:82-87`).
Задокументированный мутационный контроль **воспроизведён независимо** — это не самоаттестация.

Вторая половина требования мандата — регресс не должен ложно краснеть:

```
$ cargo test -p research-cli --test red_depth_lifetime   → ok. 6 passed; 0 failed
$ cargo test -p research-cli --test red_depth_band_3060  → ok. 2 passed; 0 failed
$ cargo test -p research-cli --test red_depth_scale      → ok. 2 passed; 0 failed
```

**Разделение чистое: DV-I-10..14 — 5/5 красных, DV-I-1..9 — 10/10 зелёных.** Новые оракулы
пиннят именно новую семантику, старые — инварианты, общие обеим схемам учёта (gap ≠ отмена,
отсутствие ≠ удаление, детерминизм, границы ресурса).

Код восстановлен, дерево чистое:
```
$ cp /tmp/dl_backup.rs crates/research-cli/src/depth_lifetime.rs && git status --porcelain
(пусто)
$ cargo test -p research-cli --test red_depth_lifetime_perlife → ok. 5 passed; 0 failed
```

---

## Полнота транскрипции: сторож проверен пробой, а не чтением (мандат, п.3)

Круг 1 показал, что первая редакция проверки грепала по ВСЕМУ файлу и давала ложный PASS.
`51d8450` это чинит и — что важно — **честно записывает провал в теле коммита и в комментарии
гейта** (`verify_M-58.sh:135-142`). Проверил, что чинит по существу:

```
$ RESULTS=research/data-quality/depth-lifetime-results.md
$ # ТА ЖЕ проводка, что в гейте (строки 143-144), на таблице, усечённой мной до 6 строк:
$ SECTION="$(awk '/УСЛОВИЯ ПРОГОНА M-58/,0' "$RESULTS")"
$ printf '%s\n' "$SECTION" | grep -cE '^\| *(bid|ask) *\|'
6                                    ⇒ FAIL (порог >= 14) — сторож ловит усечение
$ grep -cE '^\| *(bid|ask) *\|' "$RESULTS"      # ПРЕЖНЯЯ, ложная редакция
32                                   ⇒ PASS — сторож был слеп
```

И то же самое **через прод-проводку целиком**, а не изолированным сниппетом:
```
$ bash scripts/verify_M-58.sh   # на усечённой таблице
FAIL  задача 3 — таблица §M-58 НЕПОЛНА: строк bid/ask внутри секции = 6, ожидалось >= 14 ...
VERDICT: FAIL (1 проверок не прошло)
exit=1
```
Файл восстановлен после пробы (`git status --porcelain` пуст).

Сторож удовлетворяет всем четырём свойствам `testing.md`: гоняется прод-формой, меряет свой
инвариант (секцию, а не файл), падает против слома И против несостоявшегося setup (ветка
`if [ -z "$SECTION" ]` — «считать нечего»), наблюдает отсутствие строк, а не только сбой.

---

## Новые находки круга 2 (merge не блокируют)

### N-1 (MINOR) — прибор переименован, а его ПОКАЗАНИЯ — нет

`crates/research-cli/examples/depth_lifetime.rs` печатает заголовки колонок и ключи JSON
старыми именами, а баннер — старым милестоуном:

- `:114` — `println!("\n=== M-32 Q2а — depth_lifetime::analyze (DV-I-1..5) ===")`;
- `:118` — заголовки `"born", "cancelled", "frozen", "censored"`;
- `:252` — ручной JSON: ключи `\"born\":`, `\"cancelled\":` при значениях `b.lives_born`,
  `b.lives_cancelled`;
- `:30`, `:43`, `:106` — префикс `[M-32]` в stderr.

Из-за этого закоммиченный артефакт `research/data-quality/m58-rerun-segment78.txt` —
доказательная база вердикта и будущего решения founder'а — озаглавлен `[M-32]` и несёт
колонку `born`. Это ровно тот класс, ради которого milestone и затевался: спека
(`M-58-depth-metric.md:58-61`) прямо пишет, что переименование сделано, потому что «старое имя
означало другую величину, и молчаливая смена смысла под тем же именем — тот самый класс
ошибки». `examples/` — call-site, чей ВЫВОД пересмотреть забыли.

**Почему не блокер:** программных потребителей старых ключей нет (проверено грепом по
`crates/ scripts/ research/` — единственное вхождение это сама строка заголовка), поля в коде
переименованы полностью (`grep '\.born\b' --include=*.rs` → пусто), а сам вердикт
`depth-verdict.md` предъявляет числа под именами `lives_*`. Риск — в чтении артефакта человеком
через месяц. Завожу **TD-108**.

### N-2 (MINOR) — `F-6` закрыт не полностью: задача 3 осталась `IN_PROGRESS`

`milestones/M-58-depth-metric.md:73` — задача 3 (пересъёмка + §M-58) числится `🚧 IN_PROGRESS`,
хотя выполнена: 14 строк, условия прогона и сырой артефакт на месте, гейт по задаче 3 даёт
три PASS. `c78cfd0` перевёл 1/2/4/5 в `✅ DONE`, а 3 оставил честно — на тот момент F-1 был
открыт; `7d19c5c` его закрыл, но статус не переставил (carve-out `scope-guard.md` это
research-dev'у разрешает). Документ утверждает незавершённость завершённой работы.

### N-3 (NOTE) — паритет с CI по clippy остался неполным

`verify_M-58.sh:111` — `cargo clippy -p research-cli --all-targets -- -D warnings`;
CI (`ci.yml`) — `cargo clippy --all-targets --all-features -- -D warnings` по всему workspace.
`gates.md` §3 называет clippy поимённо и отдельно предупреждает: «узкий скоуп `-p <крейт>`
слеп к поломке соседей». В `R-033` F-4 я назвал fmt и `test --all` — они добавлены; clippy
я тогда не назвал, поэтому это остаток, а не невыполненное условие.

Проверено прогоном, что merge CI не покрасит:
```
$ cargo clippy --all-targets --all-features -- -D warnings; echo exit=$?
exit=0
```
Форма гейта — предмет rev2 M-60 (там же чинится класс «гейт зеленее CI»), не заплатка здесь.

### N-4 (INFO) — у дефекта метрики появился канонический номер: **TD-103**

`F-7` требовал убрать ссылки на занятый `TD-098`; `e1505ee` заменил их на «дефект метрики
(`R-031` §A.1)» — на тот момент верно, номера ещё не существовало. С merge'а `f90a5a8`
(`R-034`, 2026-08-05) дефект живёт в `TECH-DEBT.md` как **TD-103**. Ссылки в
`A-002`/`depth-verdict.md`/`viz-backend.md` стоит привести к номеру при следующем касании
(зона architect'а). Механическая замена оставила два шва: `A-002` — «поражено (дефект метрики
(`R-031` §A.1)/R-031 §A.1/A-002)» (ссылка задвоена) и самореферентная строка в таблице сверки.

---

## Merge: конфликт разрешён в пользу архива

Ветка правит `docs/ORCHESTRATION-STATE.md`, который в `main` переименован в
`docs/archive/orchestration-log-2026-07-08.md` (`0bd8b45`, решение founder'а). Git следует за
переименованием и даёт content-конфликт.

Взята сторона `main`. Причина не механическая: сторона ветки — таблица пайплайна эпохи
M-54/M-56 (2026-08-03), то есть **устаревший журнал, вписываемый в архив**. Файл выведен из
startup-пути именно как журнал закрытого эксперимента; живой индекс — `SESSION-HANDOFF` §0.
Merge архив не меняет:
```
$ git checkout --ours docs/archive/orchestration-log-2026-07-08.md
$ git diff origin/main -- docs/archive/orchestration-log-2026-07-08.md
(пусто)
```
Статус M-58 записываю в `PROJECT-STATE.md` (моя зона) при close-out.

---

## Done Block

```
$ git remote get-url origin && git rev-parse --show-toplevel && git log -1 --format='%h %s'
https://github.com/a3ka/hft-platform.git
/tmp/hft-rev-m58b
7d19c5c docs(M-58): F-1 R-033 — полная транскрипция 14/14 строк замера, сырой вывод файлом [research-dev]

# ── гейт на ВЕТКЕ ──
$ bash scripts/verify_M-58.sh
VERDICT: PASS            verify_exit=0        # 22 PASS / 0 FAIL
  в т.ч.: паритет CI — cargo fmt --all -- --check чист
          паритет CI — cargo test --all зелёный (193 блоков)
          задача 3 — таблица §M-58 полна: строк bid/ask внутри секции = 14 (7 полос × 2 стороны)
          замок A-002 З-1 держится: дефолт полос 0.001 внутри валидированной зоны (<=0.013)

$ cargo fmt --all -- --check; echo exit=$?
exit=0

$ cargo clippy --all-targets --all-features -- -D warnings; echo exit=$?    # форма CI, шире гейта
exit=0

# ── мутационный контроль (reviewer, независимо) ──
$ # мутация 2: реализация M-32 из origin/main + переименование полей
$ cargo test -p research-cli --test red_depth_lifetime_perlife
test result: FAILED. 0 passed; 5 failed        # DV-I-10..14, числа совпали со спекой
$ cargo test -p research-cli --test red_depth_lifetime   → ok. 6 passed; 0 failed
$ cargo test -p research-cli --test red_depth_band_3060  → ok. 2 passed; 0 failed
$ cargo test -p research-cli --test red_depth_scale      → ok. 2 passed; 0 failed
$ git status --porcelain                       # после восстановления
(пусто)

# ── проба сторожа полноты (анти-плацебо), прод-проводкой ──
$ bash scripts/verify_M-58.sh                  # таблица усечена мной до 6 строк
FAIL  задача 3 — таблица §M-58 НЕПОЛНА: строк bid/ask внутри секции = 6, ожидалось >= 14
VERDICT: FAIL (1 проверок не прошло)           probe_exit=1
$ git status --porcelain                       # после восстановления файла
(пусто)

# ── независимая пересъёмка замера ──
$ cargo run --release -p research-cli --example depth_lifetime -- /tmp/m33-journal
rerun_exit=0
$ diff <мой прогон> research/data-quality/m58-rerun-segment78.txt
# расходятся только 3 строки stderr-баннера; всё численное — идентично

# ── барьер артефактов, ПРОД-ФОРМА (как зовёт ci.yml) ──
$ EVENT_NAME=pull_request PR_BASE_SHA=e4c45c7 bash scripts/check_protected_artifacts.sh
OK: защищённые артефакты целы на HEAD (e4c45c7..HEAD; проверка по РЕЗУЛЬТАТУ, не по способу)
exit=0

# ── дерево слияния ──
$ git merge --no-ff origin/feat/M-58-depth-metric        # конфликт архива разрешён в пользу main
$ git diff --cached --numstat | awk '$2>0'               # где вообще есть удаления
28  28  crates/research-cli/examples/depth_lifetime.rs
63  27  crates/research-cli/src/depth_lifetime.rs
8   5   crates/research-cli/tests/red_depth_band_3060.rs
26  14  crates/research-cli/tests/red_depth_lifetime.rs
# защищённых артефактов среди удалений нет

$ git diff origin/main...HEAD --stat -- crates/risk crates/killswitch crates/oms 'crates/venue-*'
(пусто)                                        # RISK-BLOCK не применим
```

Гейт на дереве слияния и пост-merge §8 — в §Close-out ниже.

---

## Условие APPROVED — выполнено

1. F-1 — полная таблица 14 строк ✅ + сырой вывод артефактом ✅ + воспроизведён мной ✅;
2. F-2 — вердикт по фактическим числам пополосно/посторонне ✅, исход смешанный назван,
   замок ОСТАЁТСЯ ✅, вопрос founder'у в П-011 ✅;
3. F-3 — `cargo fmt --all -- --check` зелёный ✅;
4. F-4 — паритет по fmt/`test --all` ✅, DV-I-7/8 гоняются ✅ (clippy — остаток N-3);
5. F-6/F-7 — ссылка на TD исправлена ✅; статус задачи 3 — остаток N-2.

**Долги при close-out:** TD-107 (F-5, память per-life — спека `M-59` готова),
TD-108 (N-1, показания прибора под старыми именами), N-2/N-3 — записями в тех же долгах.

**Что сделано хорошо и подлежит закреплению.** Круг 2 — редкий случай, когда ремонт не
ограничился симптомом: гейт получил сторожа именно на тот класс, через который прошёл F-1
(отбор данных), а `51d8450` честно предъявил, что ПЕРВАЯ редакция этого сторожа сама дала
ложный PASS, и назвал причину (мерил окружение, а не свой инвариант). Вердикт при этом
зафиксировал опровержение собственного предсказания о направлении смещения. Это поведение
дороже, чем зелёный прогон.

---

## §Close-out — merge, деплой-гейт §8, долги

**Merge:** `a633333` (`--no-ff`) + close-out `dca889a`, запушены в `main`
(`cf24aac..dca889a`). Вердикт положен и на ветку предмета (`0761a20`) — чтобы следующий круг
читал находки там, где работает, а не искал их в переписке (`gates.md` §4).

```
# ── гейты на ДЕРЕВЕ СЛИЯНИЯ (не на ветке) ──
$ bash scripts/verify_M-58.sh
VERDICT: PASS       verify_exit=0        # 22 PASS / 0 FAIL
  паритет CI — cargo fmt --all -- --check чист
  паритет CI — cargo test --all зелёный (193 блоков)

$ EVENT_NAME=push PUSH_BEFORE=cf24aac bash scripts/check_protected_artifacts.sh
OK: защищённые артефакты целы на HEAD (cf24aac..HEAD)      exit=0

$ git diff --cached --numstat | awk '$2>0'      # где вообще есть удаления
28 28 crates/research-cli/examples/depth_lifetime.rs
63 27 crates/research-cli/src/depth_lifetime.rs
8  5  crates/research-cli/tests/red_depth_band_3060.rs
26 14 crates/research-cli/tests/red_depth_lifetime.rs
# ни одного защищённого артефакта среди удалений

# ── §8 пост-merge ──
$ gh run list --limit 2
completed  success  docs(state): close-out M-58 …  Deploy to VPS  main  push  31052175517  13m1s  2026-08-05T22:17:31Z
completed  success  docs(state): close-out M-58 …  CI             main  push  31052175476  10m46s 2026-08-05T22:17:31Z

$ ssh … 'docker ps; cat …/recorder.heartbeat; date -u +%s; grep RssAnon /proc/<pid>/status'
hft-gateway-serve Up 34 seconds (healthy)
hft-recorder      Up 39 seconds (healthy)
{"events":4406,"free_bytes":67453120512,"min_free_bytes":10737418240,
 "next_seq":176279403,"segment_index":189,"ts_wall_ms":1785969047500,"writable":true}
1785969057
/usr/local/bin/recorder      RssAnon:  8440 kB
/usr/local/bin/gateway-serve RssAnon:   328 kB
```

**Чтение пруфа.** Deploy на этот раз **сработал** (path-фильтр `crates/**`), контейнеры
пересобраны и перезапущены — `Up 34/39 seconds`, healthcheck прошёл, rollback не понадобился.
Heartbeat свежий на **10 с** (`1785969057 − 1785969047`). Журнал НЕ обнулился и продолжает
расти: `next_seq` 175 768 396 → **176 279 403**, сегмент 188 → **189** (замер полутора часами
ранее, `R-034` §E). `writable: true`, свободно 67.4 GB при пороге 10 GB. Память меряна
`RssAnon`, а не `docker stats` (TD-021): 8.4 MB / 328 kB — свежий старт, роста нет.

**Sanity свежих событий не требуется, и это проверено, а не предположено.** `gates.md` §8 п.2
требует его, когда деплой менял поведение данных (парсеры/форматы). Диф merge'а по коду —
исключительно `crates/research-cli/**` (офлайн-анализатор):
```
$ git diff cf24aac..HEAD --name-only -- crates/recorder crates/gateway crates/gateway-serve 'crates/venue-*' crates/journal
(пусто)
```
Ни recorder, ни gateway, ни venue-адаптеры, ни journal не тронуты ⇒ путь записи и форматы
событий не менялись; рестарт — следствие пересборки образа, а не смены поведения.

**Долги заведены** (`TECH-DEBT.md`, reviewer-owned):
- **TD-107** (MAJOR) — `finished: Vec` растёт на каждую жизнь (650 372 жизни за 3.4 ч одного
  символа); `DV-I-7` не ловит, потому что меряет время, а его фикстура уровни не отменяет.
  Спека фикса готова — `milestones/M-59-lifetime-memory.md`.
- **TD-108** (MINOR) — N-1: вывод примера под старыми именами (`born`, `[M-32]`) при полях
  `lives_*`; артефакт замера, лежащий под founder-решением, озаглавлен старым милестоуном.
- **TD-103 → CLOSED** merge'ем `a633333`, с явной записью, чего закрытие НЕ означает: вывод о
  живости дальних полос не восстановлен, ask опровергает на трёх полосах, замок `A-002`
  остаётся, решение о составе выдачи — за founder'ом (П-011).
- N-2 (статус задачи 3) и N-3 (clippy в гейте уже CI) — остатки без отдельных номеров:
  N-2 правится одной строкой при следующем касании милестоуна, N-3 — предмет rev2 M-60,
  где чинится класс «гейт зеленее CI» целиком.

**Worktree-GC** выполнен (`branch-hygiene.md` — рекультивация часть close-out).
