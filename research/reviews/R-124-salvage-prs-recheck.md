<!-- GATE-META
milestone: M-60a
audited_repo: a3ka/hft-platform
audited_base: 4b974a4039fe834a27d0866de4f05d85d1bdbb5e
audited_head: 19ac431d9a5db45ed402b29ecf8e4c29ae46acbc
verdict: REJECT
-->

# R-124 — перепроверка §9 ДВУХ PR раскопок 2026-08-24: REJECT

**Проверяющий:** независимый Fable-агент со свежим контекстом (`gates.md` §9); НЕ автор правок.
**Предмет — ОБА PR одним вердиктом** (по мандату founder'а):
- **PR #80** `docs/lost-work-salvage-2026-08-24` (`21da1a8` → `c398f13` → `dc24d64`): `П-018`,
  строка §0ter о барьере `milestone-shape`;
- **PR #81** `docs/audit-trail-salvage-2026-08-24` (`7ae7528` → `19ac431`): десять вердиктов в
  `docs/archive/abandoned-gate-verdicts-2026-08-24/`, очередь в `milestones/BACKLOG.md`.

Все проверки — на ДЕРЕВЕ СЛИЯНИЯ (`gates.md` §8): worktree detached `4b974a4` + merge обеих
веток (`--no-ff`, оба без конфликтов). Воспроизведения барьера — в отдельном мутационном
worktree, убран после снятия данных. Во время круга `origin/main` уехал на `0536505`
(PR #82, только `PROJECT-STATE.md`+`TECH-DEBT.md` — предмет не задет; `4b974a4` — предок,
история не переписана).

**Вердикт: REJECT.** PR #80 по пунктам (а) чист (одна NOTE); блокеры — в PR #81
(`README.md` и `BACKLOG.md`) плюс классовая находка (в) в файле, который правит PR #80.
Все находки чинятся правками двух-трёх файлов на тех же ветках; конструкция предметов верна.

---

## (а) Утверждения автора — команда → вывод → совпало

| # | утверждение автора | моя команда (дерево слияния) | вывод | совпало |
|---|---|---|---|---|
| 1 | `П-018`: вердикт:код за 14 сут = **131:34** ≈ 3.9:1 | `git rev-list --count --since='14 days ago' <rev> -- ':(glob)crates/*/src/**'` и `git log --since=… --name-only … research/{critiques,reviews,arbitration} \| sort -u \| wc -l` | на `fcbbcb7` (main в момент замера автора): **34 / 131 — точно**; на `4b974a4`: 34 / **133** | ✅ (дрейф — merge PR #66 после замера; вывод «мораторий действует» инвариантен, см. NOTE-1) |
| 2 | методика счёта | `--diff-filter=A` (только ДОБАВЛЕННЫЕ файлы) → **125**; `--no-merges` по коду → **31** | 125:34 ≈ 3.7:1 — вывод тот же при любой из методик; сама метрика «файлы : коммиты» — ТА ЖЕ, что в подписанном `П-017` A3 (80:14) | ✅ методика консистентна подписи, вывода не искажает |
| 3 | запасы бюджета: `commit-discipline.md` **421 B**, `gates.md` **48 B** | `bash scripts/check_context_budgets.sh` | `8579/9000 (запас 421 B)` · `47852/47900 (запас 48 B)` | ✅ точно |
| 4 | `DESIGN.md:1037` — «Сейчас (блокер): offsite COLD (R1) + алертинг — до любого роста нагрузки» | `sed -n '1033,1040p' docs/DESIGN.md` | строка 1037 дословно | ✅ |
| 5 | спека M-68 строки 44-45 и 117 | `git show origin/feat/M-68-depth-from-book:milestones/M-68-depth-from-book.md \| sed -n '40,50p;113,120p'` | 44-45: «Состав записываемых данных не меняется (`П-011` амендмент 2026-08-17), contract-RFC не требуется»; 117: «выдача глубже 1.3 % остаётся закрытой до решения по `П-011`» | ✅ |
| 6 | `A-011` §8 п.5 — два решения founder'у | `grep -n '§8'` + чтение §8 файла в архиве | п.5 дословно: «**founder** — два решения, арбитру не переданные (`gates.md` §0.1): (а)… (б) `C-094` B-6 — фазовое решение Ф0/Ф1» | ✅ |
| 7 | `C-094` B6 — смешение фазового решения | `git show origin/feat/M-68-depth-from-book:research/critiques/C-094-M-68.md` :107-113 | «M-68 не может служить контейнером для несвязанного founder decision»; `1b9e0c9` подтверждён телом коммита («ТРИ РЕШЕНИЯ FOUNDER'А», «Ф1 больше не имеет Ф0 предусловием») | ✅ |
| 8 | `П-017` A3 — мораторий с условием снятия | чтение `docs/PENDING-SIGNATURE.md` §П-017 | «новых нормативных единиц во впрыскиваемом ядре не заводить, ПОКА отношение не станет лучше 1:1» — условие снятия названо в самой записи | ✅ |
| 9 | **ключевой вывод: «(б) не требуется для M-68»** | судил по СУЩЕСТВУ: спека (п.5); `docs/09-roadmap-v2.md:89` — предусловие Ф1 само мотивировано ростом данных («больше данных → выше цена»); M-68 в Ф1 `main`-роадмапа НЕ значится (`grep M-68` → 0); зона M-68 — `crates/gateway/src/**` (чтение уже пишущейся книги), RED `red_depth_from_book.rs` лежит в `crates/gateway/tests/` | M-68 данных не растит и Ф1 не открывает; сам `C-094` B6 требовал ровно этого отделения | ✅ по существу |
| 10 | десять вердиктов до PR #81 отсутствовали в `main` | `git ls-tree -r --name-only 4b974a4 \| grep -E '(A-011\|C-10[1679]\|C-11[01234])-'` | none — 0 попаданий по всем десяти ID | ✅ |
| 11 | `check_gate_meta.sh` отбил бы размещение в `research/**` | воспроизведено: копия дерева на `4b974a4`, десять файлов в `research/{critiques,arbitration}/`, коммит, `EVENT_NAME=push PUSH_BEFORE=4b974a4… bash scripts/check_gate_meta.sh` | **10× FAIL «audited_head … НЕ предок HEAD — аудировалась другая линия истории»** (в т.ч. дословно строка `A-011`/`0b0cef23…` из README), `VERDICT: FAIL (10)`, exit=1 | ✅ механизм и формулировка — точно; ЧИСЛО — нет, см. **Б-2** |
| 12 | `ARCHIVED-VERDICT` эту проверку не открывает | чтение кода: токен читается ТОЛЬКО в ветке «нет шапки GATE-META» и только при статусе `A` (`check_gate_meta.sh:314-322`); проверка предка — `:371`, безусловна для файлов с шапкой | ✅; ссылка README на `:371` верна |
| 13 | `docs/archive/**` вне предметных путей `gate_meta` и вне защиты | `check_gate_meta.sh:300` (только `research/{critiques,reviews,arbitration}`); `check_protected_artifacts.sh:90` (`PROTECTED_DIRS` без `docs/archive`) | ✅; сверх того `check_artifact_ids` на диапазоне PR #81: «ни один новый артефакт не введён», exit=0 |
| 14 | `OhlcvRow` в двух крейтах | `grep -n 'pub struct OhlcvRow' crates/gateway/src/lib.rs crates/research-cli/src/export_io.rs` | `lib.rs:181` · `export_io.rs:84` | ✅ строки точны |
| 15 | три версионные шкалы | `grep -n` по трём файлам | `GATEWAY_SCHEMA_VERSION: u32 = 8` (`gateway/src/lib.rs:65`) · `export_schema_version: 1` (`research/exports/format.md:3`) · `SCHEMA_VERSION: u32 = 4` (`contracts/src/lib.rs:26`) | ✅ |
| 16 | `CT-RFC-06…09` заняты; `verify_contracts.sh` существует | `ls docs/rfc/`, `ls scripts/verify_contracts.sh` | 06-l2delta · 07-journal-sharding · 08-time-axis · 09-ws-session; скрипт есть (8702 B) | ✅ |
| 17 | шапки: `C-101` REJECT · `C-109` ESCALATE · `C-114` ESCALATE · `C-112` CONCERNS | `grep -m1 'verdict:'` по всем десяти файлам архива | все совпали (плюс `C-106`/`C-107`/`C-110`/`C-111`/`C-113` REJECT, `A-011` DECISION) | ✅ |
| 18 | «`C-114` ESCALATE = §0 триггер 3» | буква §0 т.3 + `ls research/arbitration/` в `main` (`A-012`…`A-017` — все о других предметах) | триггер 3 применим, арбитра по plan-abc действительно нет | ✅ |
| 19 | «`C-110`→`C-111`→`C-112` = ТРИ круга, триггер 2 сработал» | шапки всех трёх: **ОДИН** `audited_head 2dcad48…` у всех; `C-111:73-74`: «Этот трек сам заводит ОБЩИЙ ключ `C-110` для ТРЁХ АДВЕРСАРИЕВ» | три ПАРАЛЛЕЛЬНЫЕ оси одного круга, исправительных кругов между ними НЕ БЫЛО | ❌ **Б-3** |
| 20 | README: «`C-109` и `C-114` повисли» без арбитра | `A-011:11` «**Созыв:** `gates.md` §0, триггеры 2 …, **3 (`ESCALATE` от critic)**»; `:14` «**Круги:** … `C-109` ESCALATE — прочитаны целиком» | `C-109` арбитра ПОЛУЧИЛ — им и был `A-011`, лежащий строкой выше в таблице того же README | ❌ **Б-1** |

## (б) Полномочия

- **Замок §11:** оба диапазона не трогают `.claude/**`, `CLAUDE.md`, `docs/04-workflow.md`.
  Фактом: `EVENT_NAME=push PUSH_BEFORE=<merge-base fcbbcb7> bash scripts/check_docs_freeze.sh`
  на вершине каждой ветки → **exit=0 оба**. Токен `FOUNDER-APPROVED` не требуется.
- **Граница C — очередь:** `BACKLOG.md` записывает решения founder'а 2026-08-24 ДОСЛОВНЫМИ
  цитатами («M-68 поднимай выше…», «не заводи новые задачи…»); четыре довода при M-68 — фактура
  замером, не подмена воли. Заморозка трёх предметов опирается на подписанный `П-017` A2.
  `П-018` — запись в очередь подписи со статусом «🔴 ждёт вас»: (а) выводится из УЖЕ
  подписанного `П-017` A3, (б) отдаётся founder'у с явным «рекомендации нет». Решений,
  принадлежащих founder'у, авторская правка не принимает. Одно НО — производное от Б-3:
  «возобновление требует АРБИТРА» подано как следствие §0, которого нет (см. Б-3).
- **Новых обязывающих норм** во впрыскиваемом ядре нет; носители — вне бюджета ядра.
- **Предел, названный честно:** удаление девяти ref'ов — действие ВНЕ диффов обоих PR;
  проверить состав «девяти» постфактум нечем. Что проверяемо — проверено: шесть удалённых
  ref'ов, чьи локальные копии остались (`M-60-rebuild`, `M-65-ws-session`,
  `M-59-lifetime-memory`, `TD-141-fa-sync`, `fix/M-65-battery-recalibration` — предки `main`,
  0 уникальных коммитов; `feat/M-63-ci-cost` — 3 неэквивалентных коммита, но оба файла
  перенесены в `main`, диф остатка = только шапка закрытия в `milestones/M-63-ci-cost.md`).
  Потерь среди проверяемых НЕТ. Напряжение с `A-011` §8 п.4 («ни один ref не удалять до
  этого») называю в §E Handoff'а, вердиктом этих PR оно не является.

## (в) Связность и класс

- `bash scripts/verify_design_claims.sh` на дереве слияния → **PASS (0 нарушений)**;
  `H-FACTS-SHA`: 7/7 маркеров валидны, включая новый `README.md` архива
  (`audited_head=fcbbcb7` — существует, предок).
- Ссылки предметов разрешаются: `A-011` §8 п.5 ✅ · `C-094` B6 (ветка) ✅ · `1b9e0c9` ✅ ·
  `09-roadmap-v2.md` Ф0/Ф1 ✅ · `check_gate_meta.sh:371` ✅ · `TD-158`/`TD-159` в
  `TECH-DEBT.md` ✅ · `П-014` п.4 ✅ (состав выдачи `GATEWAY_BANDS` → канонический набор) ·
  `red_depth_from_book.rs` на ветке ✅ · `plan-design-migration.md` §4 ✅.
- **КЛАСС найден живым — Б-4.** `c398f13` правит ОДНУ строку живой таблицы §0ter
  (`SESSION-HANDOFF.md:443`), класс — «живой индекс лжёт о состоянии» — остаётся в ТРЁХ
  соседних строках ТОЙ ЖЕ таблицы (замер ниже). §0bis протух ДЕКЛАРИРОВАННО (шапка
  «⚠️ ПРОТУХЛО… См. §0ter» на `:16`) — он находкой не является.

## Находки

### Б-1 (БЛОКЕР, PR #81) — README утверждает ложь о `C-109`

`docs/archive/abandoned-gate-verdicts-2026-08-24/README.md:49-50`: «Два `ESCALATE` не
получили арбитра… `C-109` и `C-114` повисли». Опровергается файлом, лежащим строкой выше в
таблице ТОГО ЖЕ README: `A-011-process-package-dispute.md:11` называет созыв «триггеры 2, …,
**3 (`ESCALATE` от critic)**», `:14` перечисляет `C-109` ESCALATE среди прочитанных кругов.
`C-109` получил арбитра — `A-011`; повис только `C-114`. Воспроизведение:
`sed -n '11p;14p' docs/archive/…/A-011-process-package-dispute.md`. Класс — «утверждение о
состоянии без открытия источника», в документе, заводимом как аудит-трейл.

### Б-2 (БЛОКЕР, PR #81) — цитата `VERDICT: FAIL (11)` не соответствует предъявляемому составу

`README.md:17` цитирует `FAIL (11)`, документируя архив из **десяти** файлов. Воспроизведение
на составе архива даёт `VERDICT: FAIL (10)` (каждая строка — «НЕ предок HEAD», exit=1).
`FAIL (11)` воспроизводится, только если 11-м файлом добавить `C-094-M-68.md` — который сам
README (§«Чего здесь НЕТ») объявляет неархивируемым. Правка: перецитировать прогон состава
архива либо явно назвать состав прогона, давшего 11. Существо claim'а (барьер отбивает,
обхода нет) — подтверждено полностью.

### Б-3 (БЛОКЕР, PR #81) — «триггер 2 сработал» не подтверждается артефактами

`README.md:51` и `BACKLOG.md:323`: «ТРИ круга (`C-110`→`C-111`→`C-112`) — §0 триггер 2
сработал». Все три шапки несут **один** `audited_head` (`2dcad48…`) — предмет не менялся
между вердиктами, исправительных кругов не было; `C-111:73-74` сам называет их «тремя
адверсариями» с общим ключом. §0 триггер 2 («ТРИ КРУГА по одному предмету») — про
несходимость итераций; здесь — один круг из трёх осей. Установившееся значение «круга»
подтверждает `A-011:14` (круги = последовательные `C-106`→`C-107`→`C-109`). Следствие
(«возобновление — арбитр, а не четвёртый круг») выведено из несработавшего триггера и подано
фактом в ИСПОЛНЯЕМОМ документе. Заморозка сама по себе стоит на `П-017` A2 и в этом триггере
не нуждается. Правка: убрать/переформулировать основание в обоих носителях (что писать —
решает автор; я дефект описываю, фикс не проектирую).

### Б-4 (БЛОКЕР, PR #80, класс по (в)) — три соседних строки живой §0ter лгут о состоянии

Тот же класс, что чинил `c398f13`, в той же живой таблице (`SESSION-HANDOFF.md`):
- `:446` «решения founder'а 17.08 (П-013…П-016) **НЕ в `main`**» — ложно:
  `git show origin/main:docs/PENDING-SIGNATURE.md | grep -c '^## П-01[3-6]'` → **4**;
- `:445` «Барьер `review-fa` НАПИСАН, но в `main` его нет (`TD-155`)» — ложно:
  `scripts/check_review_fa.sh` в `main` (гонялся этим кругом, `SKIP` exit=0),
  `grep -c review-fa ci.yml` → **4**;
- `:444` «M-65 — круг 4 → `C-098` REJECT | PR #29» — протухло: close-out M-65 влит
  (`fcbbcb7` = merge PR #77, `e78da16` «статус PROPOSED → ЗАКРЫТ»).
Замер 18.08 (мандат перепроверки): правка экземпляра без грепа класса уже дважды тиражировала
ложь в `main`. Требуется тем же PR #80: датировать/поправить три строки той же таблицы.

### NOTE-1 (PR #80) — окно годности чисел 131:34

Числа сняты на `fcbbcb7` и были точны в момент замера; к моменту merge-превью `origin/main`
даёт 133:34 (в `П-018` ревизия замера не запинована). Вывод («мораторий действует, до порога
вчетверо») инвариантен к дрейфу — не блокер.

### NOTE-2 (PR #81) — цитата условия `C-094`, помеченная «дословно»

`BACKLOG.md` склеивает «…каждого task. Затем push…» в «…каждого task; push…». Смысл сохранён
полностью; при метке «дословно» лучше точная цитата.

## Условие снятия REJECT

Новыми коммитами на тех же ветках: (1) `README.md` — исправить Б-1, Б-2; (2) `BACKLOG.md` +
`README.md` — Б-3; (3) `SESSION-HANDOFF.md` §0ter — Б-4 (три строки). Каждое новое
утверждение о состоянии — с командой в теле коммита. Круг 2 — точечная перепроверка §9
(свежий контекст) исправленных мест; конструкцию предметов заново судить не требуется —
по (а) она подтверждена.

## Done Block (агрегированный; зелёное сжато, красное целиком)

```
$ git rev-parse origin/main                        # на старте круга
4b974a4039fe834a27d0866de4f05d85d1bdbb5e
$ git merge --no-ff origin/docs/lost-work-salvage-2026-08-24   → clean (2 files, +99/−1)
$ git merge --no-ff origin/docs/audit-trail-salvage-2026-08-24 → clean (12 files, +2791)
$ git rev-list --count --since='14 days ago' fcbbcb7 -- ':(glob)crates/*/src/**' → 34
$ git log --since='14 days ago' --name-only --format='' fcbbcb7 -- research/critiques \
    research/reviews research/arbitration | grep -E '^research/' | sort -u | wc -l → 131
  (то же на 4b974a4 → 133; --diff-filter=A → 125)
$ bash scripts/check_context_budgets.sh → PASS; commit-discipline запас 421 B, gates 48 B; exit=0
$ ls-tree 4b974a4 | grep -E '(A-011|C-10[1679]|C-11[01234])-' → none (десяти вердиктов в main нет)
$ [репродукция] десять файлов → research/{critiques,arbitration}/, коммит, затем:
  EVENT_NAME=push PUSH_BEFORE=4b974a4… bash scripts/check_gate_meta.sh
  → 10 × «FAIL … audited_head … НЕ предок HEAD»; VERDICT: FAIL (10); exit=1
  [репродукция-2] + C-094 одиннадцатым → VERDICT: FAIL (11); exit=1
$ check_docs_freeze (обе ветки, base=merge-base fcbbcb7) → exit=0, exit=0
$ на дереве слияния: check_docs_freeze exit=0 · check_gate_meta exit=0 (PASS, вердиктов 0)
  · check_protected_artifacts exit=0 (OK) · check_artifact_ids exit=0 (OK)
  · check_review_fa exit=0 «SKIP (диапазон не трогает crates/**)»
$ bash scripts/verify_design_claims.sh → VERDICT: PASS (0 нарушений); H-FACTS-SHA 7/7; exit=0
$ gh pr view 80/81 --json state,statusCheckRollup → оба OPEN, non-green чеков: 0
$ gh run list --branch main --limit 3 → CI success · Deploy success
$ git rev-parse --short origin/main                # на закрытии круга
0536505   (уехал легитимно: merge PR #82, только PROJECT-STATE/TECH-DEBT; 4b974a4 — предок)
```

**Уборка:** мутационный и merge-worktree сняты (`git worktree remove --force`): каталогов
`/tmp/recheck-*` было 3 → остался 1 (worktree этого вердикта; снимается после push, см.
Handoff §D). `target/` не собирался — cargo не запускался. Резерв номера: `R-124` взят
CAS-механизмом (`reserve_artifact_id.sh`), снять после merge носителя:
`bash scripts/reserve_artifact_id.sh --release R-124`.

FA-инвариант не называется: диапазон не трогает `crates/**`, барьер даёт `SKIP` — waiver не
требуется и не выписывается.
