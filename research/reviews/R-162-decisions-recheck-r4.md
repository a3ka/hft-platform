<!-- GATE-META
milestone: M-45
audited_repo: a3ka/hft-platform
audited_base: d77398d7b22396c452d2651e90498033186055dd
audited_head: d66996420dd8acff5c72e3656c2ad31c7520ff8d
verdict: NOTE
-->

# R-162 — перепроверка §9, круг 4 (СУЖЕННЫЙ): фиксы `d669964` по `R-161` Б-1/Б-2/Н-2 + метод

**Роль:** независимый Fable-агент со свежим контекстом (`gates.md` §9). Автор — ведущий
architect — сторона; себя не проверял. Диапазон `d77398d..d669964` (12 коммитов, docs-only).
Рамка круга СУЖЕНА диспетчером: судятся РОВНО четыре вещи (`R-161` Б-1 полностью, Б-2 по
существу, Н-2, метод); принятое прошлыми кругами не пересуживается (`gates.md` §0 — решение
обязательно обеим сторонам).

**Вердикт: NOTE.** Все четыре судимых предмета — ЗАКРЫТЫ, впервые за четыре круга ни одно
поименованное условие не исполнено наполовину. Класс «объём падает» проверен грепом по всему
диапазону, а не по названным местам, — живых вхождений ноль (см. §1). Софизм границы C
перевёрнут по существу, третьей формы в диапазоне нет (§2). NOTE, а не APPROVE, потому что
`R-161` усл.3 (Н-1/Н-3/Н-4: исполнить или ПИСЬМЕННО отклонить) остаётся исполненным лишь в
форме честного объявления «не закрыты», а Н-5/Н-6 в теле `d669964` не упомянуты вовсе —
это состояние названо в §5 и обязано закрыться ДО merge в `main`, но по рамке круга и по
факту честного объявления блокером круга не является.

---

## §1. (1) `R-161` Б-1 — ЗАКРЫТ ПОЛНОСТЬЮ. Греп КЛАССА, не названных мест

Класс: любая форма «объём падает / прирост снижается / диффы дешевле» без оговорки об
инверсии. Проверены ФИНАЛЬНЫЕ состояния всех пяти не-вердиктных файлов диапазона плюс
добавленные строки самого диффа диапазона; морфология — широкая (`пад|упал|дешевл|сниж|
сжим|эконом`), после того как первый снимок (`пада`) не поймал бы «упадёт»/«Падение»:

```
$ git diff --stat origin/main..HEAD | tail -1
 8 files changed, 1673 insertions(+), 8 deletions(-)
$ for f in docs/data-epochs.md docs/plans/depth-delivery-architecture-2026-08-31.md \
    docs/plans/scope-check-m45-m70-2026-08-31.md milestones/M-45-persist-l2delta.md; do
    grep -n -iE 'пад|упал|дешевл|сниж|сжим|эконом' "$f"; done
$ awk 'NR>=1769 && NR<=1924' docs/PENDING-SIGNATURE.md | grep -n -iE 'пад|упал|дешевл|сниж|сжим|эконом'
```

Разбор каждого попадания (полный вывод — Done Block):

- `П-026` `rationale` — «объём ВЫРАСТЕТ… а не упадёт»; «Падение объёма — следствие ИНВЕРСИИ
  ролей… которой в `M-45` НЕТ» — знак верный, оговорка на месте;
- `П-026` §Порядок п.4 (`:1868` у `R-161`) — исправлен `d669964`: «**ожидание РОСТ на
  ~5-7 % на поток**… Оракул, ждущий падения, объявил бы исправную работу дефектом»;
- `П-026` §Побочные следствия (`:1920` у `R-161`) — исправлен `d669964`: «включение диффов
  АДДИТИВНО, суммарный прирост РАСТЁТ»;
- `data-epochs.md` `E-002` — «Прежняя редакция ждала ПАДЕНИЯ и была неверна»; «инверсия…
  снижает объём… `[verify-at-impl]`» — оба с оговоркой об инверсии;
- `M-45:313` — «§17 п.3 („объём ПАДАЕТ") — следствие п.2, а не самостоятельное обещание» —
  это разбор оговорки, не её отсутствие; `M-45:354` «максимум экономии» — таблица вариантов
  инверсии, контекст и есть инверсия;
- `scope-check` §1.3 об.3 — «суточный объём скорее вырастет на ~5-7 %… чем упадёт»; §4 п.2 —
  «направление („аддитивно, не падение") доказано кодом» — знак верный;
- `depth-delivery:226` «исполняется ДЕШЕВЛЕ (депт-серия +25 КБ)» — другой предмет (стоимость
  доставки глубины по `П-014`, не объём журнала L2Delta) — в класс не входит;
- `R-159`/`R-160`/`R-161` — вердикты гейтов цитируют старый знак как НАХОДКУ; носителями
  класса не являются по функции.

**Живых вхождений класса в диапазоне: НОЛЬ.** Подписная запись `П-026` больше не спорит
сама с собой: все три носителя ожидания (`rationale`, §Порядок п.4, §Побочные следствия)
несут один знак — РОСТ. Гейт раскатки (колонка «Оракул / гейт» задачи 7 → «`П-026`
§Порядок») теперь ведёт исполнителя к шагу с ВЕРНЫМ знаком.

## §2. (2) `R-161` Б-2 — ЗАКРЫТ ПО СУЩЕСТВУ, не переименован

**(а) Вывод ПЕРЕВЁРНУТ, а не смягчён.** §4bis.5 (`M-45:387-395`), дословно: «**инверсия —
РЕШЕНИЕ ГРАНИЦЫ C и требует подписи founder'а.**» Это не «открытый вопрос» (минимум, который
требовал `R-161` усл.2), а прямая противоположная классификация — сильнее требуемого.
Прежняя редакция не удалена молча: она процитирована и названа «зеркальной формой софизма»
со ссылкой на общий приём («назвать изменение записываемого свойством второго порядка и тем
вывести его из-под границы») — след оставлен, класс назван.

**(б) Новый довод ВЕРЕН.** «Разрежение якоря 1 с → 10-30 с означает, что состояний книги в
журнале станет в 10-30 раз МЕНЬШЕ; полнота держится диффами, но это ДРУГАЯ гарантия, а не
та же.» Проверено по первоисточникам: `DESIGN` §17 (снимок 1 Гц сегодня — «раз в секунду на
каждый поток», якорь 10-30 с по п.2 — арифметика 10-30× прямая) и `fa/book.md` §6/§I —
полнота через диффы держится на цепочке gap-детекции и честного ресинка (`BK-I-2`: `Stale`
СИНХРОННО на гэпе; `BK-I-4`: детерминизм редьюсера) — то есть на гарантии ДРУГОГО механизма
с другими условиями отказа, чем прямая запись состояния. Формулировка точна.

**Механизм E-записи срабатывает теперь АВТОМАТИЧЕСКИ.** `R-161` усл.2 требовал ещё
«обязательство E-записи на границе каденции». Отдельной строки в §4bis.5 нет, но она стала
ИЗБЫТОЧНОЙ конструкцией самой правки: правило 2(б) `data-epochs.md` заводит эпоху при
«осознанной смене СОСТАВА записи по подписи founder'а — граница C», а §4bis.5 теперь
классифицирует инверсию именно так ⇒ подпись инверсии триггерит 2(б) без дополнительного
текста. Жалоба `R-161` Б-2 была ровно в том, что классификация «не граница C» ВЫКЛЮЧАЛА
правило 2(б); обратная классификация его включает. Существо закрыто.

**(в) ТРЕТЬЕЙ формы софизма в диапазоне НЕТ.** Греп по финальным состояниям всех пяти
не-вердиктных файлов:

```
$ grep -n -iE 'границ[аыу] C не|не границ|не задета|не пересекается|а не состав|состав не (мен|трога)|второго порядка' <файлы диапазона>
```

Все попадания — мета-уровень: софизм, НАЗВАННЫЙ софизмом (`scope-check` §1.1/§3.244,
`M-45:388-391`, `П-026` §СНЯТО КАК СОФИЗМ). Ни одно место диапазона больше не выводит
изменение записываемого из-под границы C переописанием. «Что НЕ затронуто» в `E-002`
(`SCHEMA_VERSION` тот же, `BINANCE_*SYMBOLS` не менялись) — фактические утверждения о
неизменённом, а не переклассификация изменённого; `П-011` «граница C не пересекается» — на
`main` до диапазона, предметом не является.

## §3. (3) `R-161` Н-2 — ЗАКРЫТ

Шапка `docs/data-epochs.md:1-17` приведена к двум классам записей: «1. дефект семантики —
записанному нельзя доверять (`E-001`); 2. осознанная смена СОСТАВА записи по подписи
founder'а — дефекта нет, но до границы данных ФИЗИЧЕСКИ НЕТ (`E-002`)», с обоснованием
(«отсутствие НАБЛЮДЕНИЯ ≠ отсутствие СОБЫТИЯ»). Противоречие «шапка: только дефекты» против
«`E-002`: ЭТО НЕ ДЕФЕКТ» снято: открыватель `E-002` теперь попадает во 2-й класс шапки
дословно. Заголовок файла сменён согласованно («где записанное МЕНЯЕТ СМЫСЛ»), строка
«Отсутствие записи здесь означает…» расширена на оба класса. Правило 1 («перед выводом
проверь, нет ли эпохи, затрагивающей используемые поля») осталось исполнимым — процедура
чтения не изменилась, расширился лишь класс поводов для записи.

## §4. (4) МЕТОД — смена подтверждается РЕЗУЛЬТАТОМ; предъявление — с оговоркой

- **Дифф `d669964` соответствует телу коммита строка в строку** (впервые за четыре круга):
  заявлены три правки (§Порядок п.4, §Побочные следствия, шапка data-epochs + §4bis.5) —
  `git show --stat` даёт ровно три файла, 27+/8−, и каждый заявленный фрагмент найден в
  диффе. Ложных утверждений о собственном диффе — класса, пойманного `R-161` Б-1 п.4, —
  в теле НЕТ. Тело честно перечисляет и НЕзакрытое (Н-1/Н-3/Н-4).
- **Результат — тот, который производит греп класса:** мой независимый греп по всему
  диапазону (§1) даёт ноль живых вхождений. Правка по названным местам этого не даёт —
  три круга это доказали от противного.
- **Оговорка:** тело коммита говорит «с предъявлением нуля совпадений», но самого вывода
  грепа в теле нет — предъявление живёт вне git. Проверяемое ядро утверждения (ноль
  совпадений) воспроизведено мной независимо и ИСТИННО, поэтому это дефект формы, не
  ложность. На будущее: предъявление — это вывод команды в артефакте, не слово о нём.

## §5. Состояние объявленно-открытого остатка (НЕ блокеры круга; блокеры MERGE)

Проверено открытием файлов на `d669964` — без пересуживания, состояние как объявлено:

- **Н-1** — правило 2(б) `data-epochs.md:142` по-прежнему якорится «граница C, `П-011`»
  (канон — `DESIGN` §6 / `gates.md` §0.1); открыт, объявлен.
- **Н-3** — задача 6 (`M-45:107`) по-прежнему противоречит себе в одной строке: «ТОЛЬКО при
  раскатке… не при merge» против «✅ DONE» при раскатке, которой не было; открыт, объявлен.
- **Н-4** — маршрут нормативных правок спеки не назван; `C-*`, называющего M-45, в диапазоне
  нет. Замечание к следующему шагу: перепроверка §9 критика НЕ заменяет (асимметрия §9), и
  правка §4bis.5, хоть и исполняет условие вердикта, этот вопрос не снимает — его надо
  исполнить или письменно отклонить ДО merge.
- **Н-5/Н-6** (`R-161` усл.4: уточнение «ресинк-путь эмиссии надо СОЗДАТЬ»; носитель
  обещания инверсии в BACKLOG — `grep -n 'инверси|якор' milestones/BACKLOG.md` → пусто) —
  НЕ исполнены и, в отличие от Н-1/Н-3/Н-4, в теле `d669964` НЕ упомянуты. Единственное
  умолчание круга; называю его здесь, чтобы оно перестало быть умолчанием.

**Новых фактов, поднимающих Н-1/Н-3/Н-4 до блокеров, не найдено.**

## §6. Рекомендация по `A-028` §4 — НЕ требуется

Четвёртый круг НЕ вернул REJECT: судимые классы закрыты, метод сменён проверяемо.
Пересборка предмета другой конструкцией / смена исполнителя не показаны. Остаток —
перечислимый список из пяти именованных строк (§5), каждая дешевле круга гейта.

## Полномочия и связность (рамкой круга — только сверка, что диапазон их не тронул)

`d669964` не меняет ни `to`, ни охват, ни цитату воли founder'а в `П-026`; форма
`{what, from, to, rationale, report_ref}` на месте (`docs/03-integration-contract.md` §3
открыт). `verify_design_claims --merge-preview origin/main` → PASS, exit=0.

## FA (M-66)

Диапазон не трогает `crates/**` — `check_review_fa.sh` даст `SKIP`; требование когнитивное.
Живые инварианты зоны предмета, проверены открытием `docs/fa/book.md` на `d669964`:
**`BK-I-2`** (§I п.2 — `Stale{SeqGap}` синхронно, до следующего события) и **`BK-I-4`**
(§I п.4 — детерминизм редьюсера, зеркало `DET-I-1`) — оба несут довод §2(б) о том, что
полнота через диффы есть ДРУГАЯ гарантия, чем прямая запись состояний.

## Done Block (агрегировано, `commit-discipline.md`)

```
$ git ls-remote --heads origin docs/M-45-rollout-signature
d66996420dd8acff5c72e3656c2ad31c7520ff8d
$ git rev-parse origin/main HEAD
d77398d7b22396c452d2651e90498033186055dd
d66996420dd8acff5c72e3656c2ad31c7520ff8d
$ git show d669964 --stat --format='' | tail -4
 docs/PENDING-SIGNATURE.md          |  7 ++++---
 docs/data-epochs.md                | 17 ++++++++++++++---
 milestones/M-45-persist-l2delta.md | 11 +++++++++--
 3 files changed, 27 insertions(+), 8 deletions(-)
$ awk 'NR>=1828 && NR<=1924' docs/PENDING-SIGNATURE.md | grep -c -iE 'упал|скорее падает'
0        # «упадёт» в rationale формой «упал» не ловится; носителей старого знака в П-026 нет
$ grep -n -iE 'объём (упал|падает)|скорее падает' docs/PENDING-SIGNATURE.md docs/data-epochs.md \
    milestones/M-45-persist-l2delta.md docs/plans/scope-check-m45-m70-2026-08-31.md \
    docs/plans/depth-delivery-architecture-2026-08-31.md; echo exit=$?
docs/plans/scope-check-m45-m70-2026-08-31.md:117:3. Довод «объём ПАДАЕТ» на фьючерсы **не переносить и на споте пометить оценкой под
milestones/M-45-persist-l2delta.md:313:строке. И §17 п.3 («объём ПАДАЕТ») — следствие п.2, а не самостоятельное обещание: без
exit=0   # оба хита — мета: запрет переноса довода и разбор оговорки §17 п.3; живого носителя нет
$ sed -n '387p' milestones/M-45-persist-l2delta.md
- **инверсия — РЕШЕНИЕ ГРАНИЦЫ C и требует подписи founder'а.** Прежняя редакция этого пункта
$ sed -n '6,14p' docs/data-epochs.md | head -3
**Файл несёт ДВА класса записей** (уточнено 2026-08-31 по `R-160` Н-2 — прежняя шапка
определяла его ИСКЛЮЧИТЕЛЬНО как реестр дефектов, и `E-002` ей противоречила):
$ grep -n 'инверси\|якор' milestones/BACKLOG.md; echo exit=$?
exit=1   # Н-6: носителя обещания инверсии в BACKLOG по-прежнему нет
$ bash scripts/verify_design_claims.sh --merge-preview origin/main 2>&1 | tail -2
VERDICT: PASS (0 нарушений)
exit=0
```
