# R-027 — PR-гейт: ветка `feat/arch-audit` (независимый арх-аудит + CT-RFC-08 + C-056 + правки правил)

**Гейт:** doc-гейт класс A (`gates.md` §9 п.3 — reviewer: scope, cross-references,
`PROPOSED → ACTIVE`, merge и push) + PR-time §4.
**Предмет:** `origin/feat/arch-audit` @ `e1c0853` (9 коммитов).
**Merge-цель:** `origin/main` @ `cb07126` (НЕ `0b4a398`, на котором снят аудит, — см. Б-1).
**Замеры:** worktree `/tmp/hft-rev-audit` (detached @ `e1c0853`) и `/tmp/hft-rev-merge2`
(detached @ `cb07126` + merge-превью ветки). Общий чекаут не трогался.
**Дата:** 2026-08-03. **Ревьюер:** reviewer.

---

## ВЕРДИКТ

| Блок | Итог |
|---|---|
| Block-scope (пути дифа) | ✅ **PASS** |
| Block-аудит-трейл (вердикт критика как артефакт) | ✅ **PASS** |
| Block-C (контракты T1) | ✅ **PASS** (не применимо — `crates/**` не тронут; гейт атомарности прогнан) |
| Block-risk | ✅ **PASS** (не применимо — кода нет) |
| Block-правила (`.claude/rules/*` по существу) | ✅ **PASS** + N-1 |
| Block-честность (аудит врёт или нет) | ❌ **REJECT — Б-1** |
| Block-гейты (verify/fmt/clippy/тесты) | ✅ **PASS** + N-2 (находка вне периметра ветки) |

# ❌ REJECTED — один узкий блокер (Б-1). Передизайна не требуется, правка — одна секция.

Инженерное содержание ветки я подтверждаю: RFC прошёл три круга RAW-гейта и держится на
замерах (перепроверил выборочно — §3), правки правил исполнимы и не противоречат
действующим (§4), scope чист, аудит-трейл на месте. Блокирует ровно одно: документ,
правдивый на своей базе, становится ложью в дереве, куда он вливается.

---

## Б-1 (БЛОКЕР) — `docs/plans/arch-audit-2026-08.md` лжёт о состоянии кода в merge-цели

**Класс — не новый.** Это в точности `R-013` Б-2/Б-3 («документ был правдив на ветке и стал
ложью в момент слияния; прогон на ветке необходим, но не достаточен») — тот самый инцидент,
ради которого построен `scripts/verify_design_claims.sh` (его шапка называет этот класс
пятым и отдельным). Машинный гейт здесь не спасает: он покрывает `docs/DESIGN.md` и **не
покрывает `docs/plans/**`** (`docs/ORCHESTRATION-STATE.md:213`) — я последний барьер.

**Что произошло.** База аудита — `origin/main` @ `0b4a398`. За время трёх кругов критика
founder подписал `П-008` и `main` уехал на `cb07126`, исполнив часть того, что документ
предлагает сделать:

```
$ git log --oneline 0b4a398..origin/main
cb07126 ops(TD-086): concurrency deploy-main без отмены + push тестов не деплоит прод (П-008 п.3)
4aca3f6 ops(П-008 п.1): состав записи пробрасывается в recorder явно, дефолты = дефолтам кода
14017a4 ops(TD-085): чекпоинт gateway каждые 15 мин под flock (П-008 п.2, санкция founder'а)
5bc3b9a docs(process): П-008 — итог арх-аудита, пять решений founder'а
...
```

**Восемь утверждений документа, ложных на `cb07126`** (каждое — замером на merge-превью):

| # | Место в документе | Что сказано | Факт на merge-цели |
|---|---|---|---|
| 1 | §0 п.7 | «`concurrency` нет ни в одном workflow → **ПОДТВЕРЖДЕНО**» | `.github/workflows/deploy.yml:26-28` — `concurrency:` / `group: deploy-main` / `cancel-in-progress: false` |
| 2 | §1 п.4 | «deploy без `concurrency` и с триггером по тестам — каждый push в `crates/**` рвёт forward-only запись» | обе половины закрыты: `deploy.yml:26-28` + `deploy.yml:42` — `- '!crates/*/tests/**'` |
| 3 | §5 P0-3 | «TD-086: `concurrency` + `'!crates/*/tests/**'` в `deploy.yml`» — как задача «делать сейчас» | сделано в `cb07126` |
| 4 | §5 P0-1 | «чекпоинт каждые 10–15 мин (cron-правка)» — как задача «делать сейчас» | сделано: `deploy/cron.d/journal-retention:79` — `*/15 * * * * root flock -n …` |
| 5 | §0 п.12 | «на VPS env состава не выставлен» | `docker-compose.yml` — `BINANCE_SYMBOLS`/`BINANCE_FUTURES_SYMBOLS`/`HL_COINS` пробрасываются явно (`4aca3f6`) |
| 6 | §C-3 (б) | «операторский шаг — выставить env явно на VPS» — как открытое предложение | исполнено там же |
| 7 | §6 п.1 | «требует подписи founder'а: выставить явные env состава» | подписано и исполнено (`П-008` п.1) |
| 8 | §6 п.2 | «требует подписи founder'а: участить cron чекпоинта» | подписано и исполнено (`П-008` п.2) |

Дополнительно (НЕ блокирует, для полноты): §0 п.9 числа тест-файлов `journal 36`/`ops 15`
и «10718 строк тестов» на `cb07126` дают `37`/`16`/`11287` — часть расхождения критик уже
объяснил методикой подсчёта (`C-056` §1 п.9), часть — движение `main`. Вывод пункта не
меняется, поэтому это NOTE, а не блокер.

**Почему это блокер, а не косметика.** Тест класса из `gates.md` §9 — «изменит ли этот текст
то, что сделает следующий агент?» — здесь отвечает ДА дважды:
- агент, взявший §5 «P0 — делать сейчас», пойдёт делать пункты 3 и 4 заново (правка cron на
  живом сервере и правка `deploy.yml` — оба на проде);
- агент, поверивший §0 п.7 / §1 п.4, будет считать, что деплой до сих пор без `concurrency`,
  и может «починить» уже починенное или неверно диагностировать следующий инцидент деплоя.

**Почему одной шапки «База замеров: … @ `0b4a398`» недостаточно.** Она уже стоит в документе
(строка 5) — и именно в этой конфигурации проект уже получал `R-013`. Заявление базы
объясняет происхождение чисел, но не отменяет того, что §5/§6 — не датированная фактура, а
ЖИВОЙ список действий, и часть его исполнена.

### Условие снятия REJECT (зона architect — я описываю дефект, не проектирую документ)

Отдельная датированная секция (например `§8. Состояние на момент merge`), в которой:
1. назван merge-SHA (`cb07126`) и то, что §0/§1/§3 — фактура на `0b4a398` и не переписывается
   (аудит — датированный артефакт, ретро-правка находок НЕ требуется и нежелательна);
2. поимённо перечислены исполненные пункты §5/§6 со ссылкой на коммит `main` (строки 3-8
   таблицы выше), с явной пометкой «закрыто `П-008`»;
3. §5 P0-1 помечен как исполненный НАПОЛОВИНУ: cron сделан, milestone «единая стоимость
   коннекта» (`TD-093(б)`) — нет; §5 P0-3 — исполнен полностью, решение по деплой-модели
   (C-4 / §6 п.3) остаётся открытым.

Иных правок документа я не требую. `docs/rfc/CT-RFC-08-time-axis.md`,
`research/critiques/C-056-arch-audit.md` и обе правки `.claude/rules/*` — без замечаний,
переработке не подлежат.

**Про повторный круг критика.** Формально `gates.md` §9 «Петля самоправки» требует
RE-critic + reviewer. По существу: правка вносится ПО ТЕКСТУ находки reviewer'а и целиком
проверяема замером (`git log 0b4a398..origin/main` + шесть файлов), а инженерное содержание
уже получило `C-056` rev3 PASS. Считаю достаточным reviewer-круг с перепроверкой замером;
решение о созыве критика — за диспетчером. Арбитр (`gates.md` §0) НЕ созывается: спора по
существу нет, находка фактическая.

---

## §1. Block-scope — PASS

```
$ git diff --name-only origin/main...origin/feat/arch-audit
.claude/rules/gates.md
.claude/rules/testing.md
docs/plans/arch-audit-2026-08.md
docs/rfc/CT-RFC-08-time-axis.md
research/critiques/C-056-arch-audit.md
$ git diff --stat origin/main...origin/feat/arch-audit | tail -1
 5 files changed, 1211 insertions(+)
```

Файлов в `crates/**` — НОЛЬ (проверено именно этой командой, не доверием). Ни одной строки
удалений вообще. Правки `.claude/rules/*` — только добавления (+6 / +17). Зоны соблюдены:
`docs/**`, `research/critiques/**`, `.claude/rules/**`.

## §2. Block-аудит-трейл — PASS

```
$ git log --format='%h %an <%ae> %s' origin/main..origin/feat/arch-audit
e1c0853 architect <architect@noreply.local> docs(audit): итог гейта C-056 — rev3 PASS; N18 …
10917ee critic    <critic@noreply.local>    docs(critic): C-056 rev3 — PASS (B7/B8 закрыты …
5e0506b architect <architect@noreply.local> docs(rfc): CT-RFC-08 rev3 по C-056 rev2 …
134607f critic    <critic@noreply.local>    docs(critic): C-056 rev2 — REJECT (узкий) …
f39b322 architect <architect@noreply.local> docs(audit): rev2 по C-056 …
b5dbfed critic    <critic@noreply.local>    docs(critic): C-056 — вердикт по arch-audit …
2640cb3 architect <architect@noreply.local> docs(rules): testing.md …
5dcd38a architect <architect@noreply.local> docs(rfc): CT-RFC-08 …
27dcdb2 architect <architect@noreply.local> docs(audit): независимый арх-аудит 2026-08 …
```

Вердикт критика — ФАЙЛОМ в ветке (`research/critiques/C-056-arch-audit.md`), тремя коммитами
под `%an = critic` (`branch-hygiene.md` п.3 выполнен). Untracked-вердиктов нет. Коммиты
атомарные, каждый несёт предмет в subject, `Co-Authored-By` ни в одном теле нет.

Все три круга гейта прослеживаются по файлу: rev1 REJECT (B1–B6) → rev2 узкий REJECT
(B7/B8 + N12–N17) → rev3 **PASS** с обязательным переносом N18 в RED-набор будущего
milestone'а. Процессная отметка `gates.md` §0 п.2 (три круга = точка созыва арбитра) в
вердикте сделана явно, отказ от созыва обоснован — принимаю.

## §3. Block-честность: выборочная перепроверка сильных утверждений — держатся

Мандат: «за сутки документы этого проекта врали неоднократно». Перепроверял независимо от
критика, своим замером, самые нагруженные утверждения (те, на которых стоит вывод):

| Утверждение | Моя проверка | Итог |
|---|---|---|
| §0 п.1 — ось времени от wall-clock, обнуляется на open | `crates/journal/src/lib.rs:110`,`:167` — `epoch: SystemTime::now()`; `:205-213` — `now.duration_since(self.epoch)` | ✅ |
| §0 п.3 (главная СОБСТВЕННАЯ находка аудита — sim) | `crates/sim/src/exchange.rs:108` — `effective_ts_mono_ns: self.last_ts_mono_ns + draw.delta_submit_ns`; `:141` — `if self.pending[i].effective_ts_mono_ns <= ts_mono_ns` | ✅ находка реальна |
| §0 п.4 — прод-read-path ось НЕ использует (на этом стоит калибр «не redesign») | `grep -rn ts_mono_ns crates/gateway/src crates/gateway-serve/src` → пусто | ✅ |
| §0 п.10 — консюмеры `alpha` только вне прод-процессов | `grep -rln "alpha::" crates/*/src` → portfolio, strategy(×2), research-cli/grid, sim/strategy_backtest | ✅ (recorder/gateway-serve отсутствуют) |
| §0 п.13 — «34 MAJOR» гипотезы неверно | `grep -c 'Severity: \*\*MAJOR\*\*' TECH-DEBT.md` → **32**; `TECH-DEBT.md:96` — TD-084 = wsprobe hex, не healthcheck | ✅ опровержение верно |
| §3 Н-1 — продуктовая аналитика в `gateway`, а не в `alpha` | `find`+`wc`: gateway src 3349, tests 21 файл/6197 строк; alpha src 238 | ✅ (числа gateway совпали в точности) |
| C-056 §13 N18 — «тем же проходом» неверно для крупных фреймов | `crates/journal/src/segments.rs:1866` — `const SEQ_PREFIX_CAP: usize = 10`; `:1930` — `postcard::take_from_bytes::<u64>(&prefix)` возвращает ТОЛЬКО `seq` | ✅ N18 обоснован кодом; перенос в RED-набор обязателен |
| CT-RFC-08 §4 п.6 — tripwire, который покраснеет от бампа | `crates/contracts/tests/red_rfc04.rs:33` — `assert_eq!(SCHEMA_VERSION, 4, …)` | ✅ |
| CT-RFC-08 §4 п.7 — `verify_M-35.sh` пиннит версию | `scripts/verify_M-35.sh:44` — `grep -qE 'SCHEMA_VERSION: u32 = 4'` | ✅ |
| CT-RFC-08 §2.2 п.3 — `HeartbeatSample` без `deny_unknown_fields` (аддитивное поле безопасно) | `crates/ops/src/watchdog.rs:125` — атрибута нет; единственный `deny_unknown_fields` в дереве — `research-cli/src/strategy_cell.rs:32` | ✅ |
| CT-RFC-08 §2.2 п.2 / N15 — `write_meta` в `lib.rs`, не `segments.rs` | `crates/journal/src/lib.rs:349` — `fn write_meta` (tmp+rename); `:316-326` — `read_meta` через `[u8;8]` `read_exact` | ✅ ссылка исправлена верно |
| CT-RFC-08 §2.2 — формула-прецедент | `crates/journal/src/segments.rs:1466-1476` — `resolve_next_seq_with` = `meta_seq.max(seg_last)` | ✅ |

Расхождений по существу не нашёл ни одного. Единственное расхождение — временнóе (Б-1).

**Отдельно отмечаю в пользу аудита:** §3 (что в гипотезе НЕВЕРНО) — сильнейшая часть
документа и она подтверждается замером: Н-1 (диагноз «инфраструктура вместо продукта»
смещён), Н-3 (числа гипотезы), Н-4 («система ловит через раз», а не «не ловит» — allow-list
M-45 действительно был пойман `C-048` ДО мержа). Опровержение собственного заказчика,
подкреплённое командами, — то, ради чего аудит и заказывался.

## §4. Block-правила — PASS + N-1

Правки `.claude/rules/*` — конституция; проверял по трём критериям мандата.

**(1) `gates.md` §4, блок «Механизм на пути (DoD)» (+6 строк).**
- *Проверяемо?* Да, и адресат верный — блок стоит в списке проверок **reviewer'а** (по
  `C-056` N8: «адресат правила — тот, кто мержит»). Проверка бинарна: есть оракул точки
  входа ИЛИ есть TD-запись `built-not-wired` MAJOR. Обе половины наблюдаемы на PR-гейте.
- *Не противоречит?* Соседние блоки §4 (Scope / Done Block / Contract Block-C /
  Риск-инварианты) не пересекаются с ним ни предметом, ни адресатом. Дублирования нет.
- *Исполнимо?* Да. Формулировка «мержится ТОЛЬКО с подключением» смягчена явным клапаном
  «подключение осознанно отложено → TD MAJOR» — правило не создаёт тупика, при этом долг
  становится видимым, а не подразумеваемым. Клапан лежит в зоне reviewer'а (`TECH-DEBT.md`
  — reviewer-owned), то есть исполнить его есть кому.
- *Cross-reference жив?* `testing.md` §«Канарейка ops-пути…» существует —
  `.claude/rules/testing.md:317`.
- *Фактура случая верна?* «`LiveReducer` M-38b→M-53» — да: механизм ныне подключён
  (`crates/gateway-serve/src/lib.rs:358,373,380,434,456` — `resume`/`pump` в push-цикле),
  то есть правило написано по закрытому, а не по выдуманному инциденту.

**(2) `testing.md` — два дополнения В СУЩЕСТВУЮЩИЕ разделы (+17 строк).**
Отдельного раздела НЕ заведено — ровно как требовал `C-056` N7 («~на 2/3 дублирует два
действующих раздела»). Проверил врезку по месту:
- «ЗАВИСИМЫЙ ЭТАЛОН» — внутри `## Мутационный контроль …` (`:272`), врезка на `:295`.
  Критерий операционален и однозначен: «сломай X так, чтобы Y сломался ТЕМ ЖЕ образом, —
  покраснеет ли оракул? Нет → тавтология». Опоры существуют: `TD-083` (`TECH-DEBT.md:18`),
  `research/critiques/C-055-M-46.md`.
- «МЕХАНИЗМЫ несущего пути» — внутри `## Канарейка ops-пути ИСПОЛНЯЕТ вызывателя …`
  (`:317`), врезка на `:336`. Даёт определение несущего пути перечислением
  (recorder / gateway-serve / ops-cron / venue-ingest) — проверяемо, не «по духу»; называет
  ЧЕТЫРЕ дешёвые санкционированные формы, то есть не создаёт неисполнимого требования
  «поднимать полный прод» на каждый milestone; и явно запрещает grep-по-имени со ссылкой
  на живой долг `TD-088` (`TECH-DEBT.md:195`) — то есть закрывает известную дыру, а не
  повторяет общее место.

**N-1 (NOTE, не блокер) — правило про рост правил не выполнено самим набором.** §C-5
документа предлагает «мораторий на новые ПРОЗАИЧЕСКИЕ правила: новый урок фиксируется ЛИБО
скриптом-гейтом, ЛИБО одной строкой в чек-листе», а сам набор добавляет 23 строки прозы в
`gates.md`+`testing.md`. Внутреннего противоречия формально нет (C-5 — предложение, не
принятое правило; §5 держит его в P2 за подписью founder'а), и обе врезки — по существу
верны и адресны. Но если C-5 будет подписан, эти же 23 строки — первый кандидат на
превращение в машинную проверку (DoD «механизм на пути» проверяем скриптом: milestone,
тронувший `crates/{recorder,gateway-serve,ops}/src/**`, обязан иметь тест, исполняющий
точку входа, ИЛИ запись `built-not-wired`). Фиксирую как направление, не как требование.

## §5. Block-C и Block-risk

- **Block-C — не применим и это доказано машинно:** `crates/contracts/**` не тронут, гейт
  атомарности CT-RFC прогнан на merge-превью и сказал это сам (`VERDICT: PASS`, §6).
  `CT-RFC-08` — документ-предложение; он САМ объявляет (`:9`), что реализация в dev не
  диспетчеризуется, а `§4` перечисляет полный атомарный пакет для будущего milestone'а.
  То есть Block-C сработает на ТОМ milestone'е, а не здесь. Верно.
- **Block-risk — не применим:** `risk`/`killswitch`/`oms`/`venue-*` не тронуты (нет кода
  вообще). risk-critic не требуется. Отмечаю на будущее: milestone по CT-RFC-08 тронет
  `crates/contracts/**` ⇒ RISK-BLOCK (`gates.md` §5) + plan-time critic (`§1` п.1 и п.3) —
  ДВА гейта, как и записал критик в `C-056` N17.

## §6. Done Block (агрегированный — `commit-discipline.md`)

Все команды — на merge-превью (`origin/main` @ `cb07126` + `origin/feat/arch-audit`),
worktree `/tmp/hft-rev-merge2`. Merge-превью прошёл чисто (`Auto-merging
.claude/rules/gates.md`, без конфликтов).

```
$ git diff --name-only origin/main...origin/feat/arch-audit | grep -c '^crates/'
0

$ cargo fmt --all --check;                              echo exit=$?
exit=0

$ cargo clippy --workspace --all-targets -- -D warnings; echo exit=$?
exit=0

$ bash scripts/verify_M-46.sh;                           echo exit=$?
VERDICT: PASS
exit=0

$ bash scripts/verify_M-53.sh;                           echo exit=$?
VERDICT: PASS
exit=0

$ bash scripts/verify_design_claims.sh;                  echo exit=$?
VERDICT: PASS (0 нарушений)
exit=0

$ bash scripts/verify_ct_rfc_atomic.sh cb07126;          echo exit=$?
PASS  crates/contracts/src/** не тронут — атомарность CT-RFC пакета не применима
VERDICT: PASS
exit=0

$ bash scripts/verify_contracts.sh;                      echo exit=$?
VERDICT: PASS
exit=0

$ EVENT_NAME=push PUSH_BEFORE=cb07126 bash scripts/check_protected_artifacts.sh; echo exit=$?
OK: защищённые артефакты целы на HEAD (cb07126..HEAD; проверка по РЕЗУЛЬТАТУ, не по способу)
exit=0
```

`verify_design_claims.sh` зелёный, но это НЕ опровергает Б-1: скрипт проверяет
`docs/DESIGN.md`, а `docs/plans/**` вне его периметра (`docs/ORCHESTRATION-STATE.md:213` —
известный пробел). Зелёный машинный гейт здесь ничего не говорит о плановом документе.

### N-2 (NOTE, находка ВНЕ периметра ветки) — флапающий sacred-оракул красит `cargo test --all`

```
$ cargo test --workspace 2>&1 | grep -E "^test result|FAILED"
...
test td083_tick_wallclock_does_not_grow_with_history ... FAILED
test result: FAILED. 5 passed; 1 failed; …
error: test failed, to rerun pass `-p gateway --test red_frames_seek_bound`
test_exit=101

$ cargo test -p gateway --test red_frames_seek_bound; echo exit=$?
test result: ok. 6 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
exit=0
```

Причина — оракул меряет ОТНОШЕНИЕ настенных длительностей:
`crates/gateway/tests/red_frames_seek_bound.rs:368-380` — два `Instant::now()/elapsed()` и
`assert!(ratio < 4.0)`. Под параллельной нагрузкой `--workspace` шум планировщика на КОРОТКОМ
замере ломает отношение; в одиночку тест зелёный. CI гоняет ровно `cargo test --all`
(`.github/workflows/ci.yml:24`) ⇒ класс «случайно красный `main`».

Воспроизводимость: интермиттент — из трёх полных прогонов `--workspace` красный был один;
в изоляции 6/6 зелёных. То есть это не «сломано», а «случайно красит `main`», что хуже:
такой FAIL легко списать на инфраструктуру и обойти руками (формулировка `TD-023`).

**Это НЕ новый класс, а новая живая инстанция уже известного и однажды закрытого:**
`TD-023` (`book-memory-oracle-flaky-under-parallel-tests`, ✅ CLOSED 2026-07-15,
`TECH-DEBT.md:1250`) — «метрика, не изолированная от параллельного прогона», родня `TD-021`.
Там источником была глобальная аллокаторная статистика, здесь — настенное время; лечение
там оказалось тем же, что напрашивается здесь: не «поднять порог», а сменить измеряемую
величину на нагрузочно-независимую (счётчик прочитанных байт/событий вместо секунд — как
в `crates/journal/tests/red_open_bounded.rs`). Тест sacred (шапка файла: «RED M-38b (sacred,
architect-only)»), поэтому фикс — зона architect, не моя и не dev'а.

**К этой ветке отношения не имеет** (в дифе нет ни строки кода — §1), поэтому не блокер и
не основание держать merge. Запись `TD` в `TECH-DEBT.md` заведу в merge-круге (моя зона;
сейчас `main` не трогаю — вердикт REJECT, а `TECH-DEBT.md` живёт в `main`).

## §7. Чего я НЕ делал

- **НЕ мержил** и не пушил в `main` — вердикт REJECT.
- **НЕ менял статус** `PROPOSED → ACTIVE` (`gates.md` §9 п.3): статус переводится в момент
  merge, а merge не состоялся. Отдельно отмечаю на будущий круг: §5 документа — это
  ПРИОРИТЕТЫ, то есть `gates.md` §9 п.4 (founder ★). Документ это признаёт сам (врезка
  перед §5 и §6 п.0). Поэтому при снятии Б-1 в `ACTIVE` переводится СТАТУС ДОКУМЕНТА как
  принятого артефакта аудита; принятой очередью работ §5 становится только подписью
  founder'а — я это в `PROJECT-STATE.md` запишу именно так.
- **НЕ правил** ни один файл ветки: `docs/**`, `.claude/rules/**`,
  `research/critiques/**` — не моя зона записи (`scope-guard.md`). Я описываю дефект;
  документ правит architect (`gates.md` §4, граница reviewer↔architect).
- **НЕ трогал** общий чекаут `/home/nous/hft-platform` (остался на `docs/06-volume-truth`),
  ветки `feat/M-54-connect-cost` и `feat/ws-session-contract`, чужие worktree и `git stash`.
- **НЕ созывал** арбитра (`gates.md` §0): спора по существу нет, находка фактическая и
  проверяется одной командой.

## §8. Маршрут

`architect` (автор аудита) → секция «Состояние на момент merge» по условию снятия Б-1 →
`reviewer` (я) перепроверяю замером и мержу. `C-056` rev3 PASS остаётся в силе — его предмет
Б-1 не затрагивает.

*Reviewer, 2026-08-03. Read-only на код и на документы ветки; тронут только этот файл.*
