<!-- GATE-META
milestone: M-88
audited_repo: a3ka/hft-platform
audited_base: 3d2a2ac14332a54f5981886df2ed9a1bc347e804
audited_head: 9f482a29ffc749de71313f6459667c2cf3648c40
verdict: APPROVE
-->

# R-198 — M-88 close-out: приёмка архивной половины (PR #218) и половина reviewer'а

**Роль:** reviewer. **Дата (UTC):** 2026-09-23.
**Предмет:** (1) PR #218 `docs/m88-archive` — вынос гейта и спеки `M-88` в `docs/archive/`
по норме `Р-2`; (2) два шага close-out'а, закреплённые за reviewer'ом таблицей `Р-2`
(`docs/04-workflow.md` §2): объявление милестоуна закрытым в `PROJECT-STATE.md` и колонка
«Состояние» строки `P0-CORR` в `docs/ROADMAP.md`.

**ВЕРДИКТ: APPROVED** — с одной находкой `N-1` (не блокер, заведена как `TD-218` MINOR) и
двумя решениями, которые ниже предъявлены основанием, а не объявлением.

---

## 1. Предъявление FA (M-66)

Диф предмета трогает `crates/gateway/**` (перенос ссылок и история милестоуна), поэтому
вердикт называет ЖИВОЙ инвариант модуля. На судимой ревизии `docs/fa/viz-backend.md` несёт
**`VB-I-12`** — «Контракт ОБНОВЛЕНИЯ книго-зависимых серий (M-88, форма v11): кадр обязан быть
ОДНОЗНАЧЕН — "наблюдали, и там пусто" отличимо от "не наблюдали"» (`:210`). Инвариант заведён
PR #217 (`3e9c95f`/`4460269`), влитым в `main` перед этой приёмкой, — то есть он живой именно
на той ревизии, которую я сужу, а не в памяти прошлой сессии.

Связанный инвариант, который `M-88` и чинил: **`VB-I-2`** — «live == replay: серия, посчитанная
на live-хвосте, бит-идентична серии из replay того же окна журнала» (`:200`). Дефект
`P0-CORR` был его прямым нарушением.

```
$ grep -oE "VB-I-[0-9]+" docs/fa/viz-backend.md | sort -uV | tr '\n' ' '
VB-I-1 VB-I-2 VB-I-3 VB-I-4 VB-I-5 VB-I-6 VB-I-7 VB-I-8 VB-I-9 VB-I-10 VB-I-11 VB-I-12
```

---

## 2. Block-scope — PR #218

Диф соответствует заявленному ТОЧНО: пять переименований со 100 % идентичностью содержимого
плюс одна строка роадмапа. Ни одного содержательного изменения перееханных файлов.

```
$ git diff --name-status -M origin/main...origin/docs/m88-archive
M	docs/ROADMAP.md
R100	milestones/M-88-liquidity-removal-contract.md	docs/archive/M-88-liquidity-removal-contract.md
R100	scripts/lib/m88_predicates.sh	docs/archive/m88_predicates.sh
R100	scripts/tests/red_m88_mutants.sh	docs/archive/red_m88_mutants.sh
R100	scripts/tests/red_verify_M-88.sh	docs/archive/red_verify_M-88.sh
R100	scripts/verify_M-88.sh	docs/archive/verify_M-88.sh

$ git diff --numstat -M origin/main...origin/docs/m88-archive | head -1
1	1	docs/ROADMAP.md
```

**Разведение зон `Р-2` соблюдено — проверено машинно, а не прочтением.** Колонка «Состояние»
(зона reviewer'а) в коммите architect'а НЕ тронута; число колонок строки сохранено:

```
$ old=$(git show origin/main:docs/ROADMAP.md | sed -n '107p'); new=$(git show origin/docs/m88-archive:docs/ROADMAP.md | sed -n '107p')
$ [ "$(echo "$old" | awk -F'|' '{print $(NF-1)}')" = "$(echo "$new" | awk -F'|' '{print $(NF-1)}')" ] && echo ИДЕНТИЧНА
ИДЕНТИЧНА
$ echo "колонок: old=$(echo "$old"|awk -F'|' '{print NF}') new=$(echo "$new"|awk -F'|' '{print NF}')"
колонок: old=8 new=8
```

Вставка architect'а легла в колонку ОПИСАНИЯ (его зона — «перечень и ПОРЯДОК»), и это ровно
то разведение, ради которого норма `Р-2` переписывалась после `R-104` Б-1.

## 3. Block-C (контракты) и Block-risk

- `crates/contracts/**` диф НЕ трогает ⇒ contract-RFC не требуется, Block-C неприменим.
- `crates/risk/**`, `crates/killswitch/**`, `crates/oms/**`, `crates/venue-*/**` диф НЕ трогает
  ⇒ RISK-BLOCK (`gates.md` §5) не взводится, вердикт risk-critic в цепочке не требуется.
- Диф вообще не содержит изменений кода: только пути и одна строка документа.

## 4. Токены и барьеры

Оба токена, которых требует норма `Р-2` для пары «гейт + спека», в теле коммита `9f482a2`:

```
$ git log origin/main..origin/docs/m88-archive --format='%B' | grep -nE "ALLOW-SUBJECT-CHANGE|ALLOW-ARTIFACT-DELETE"
31:ALLOW-SUBJECT-CHANGE: переезд гейта M-88 в архив по норме Р-2 после проходного вердикта
33:ALLOW-ARTIFACT-DELETE: спека M-88 переезжает в docs/archive/ по норме Р-2; файл не
```

**Требование 1 `Р-2` (универсум номеров не сужается) — проверено в самих барьерах, а не по
заявлению коммита:** `next_artifact_id.sh:91` читает класс `M` из ДВУХ каталогов
(`^(milestones|docs/archive)/M-...`), `check_artifact_ids.sh:66` держит `docs/archive/M-*.md` в
белом списке. Номер `M-88` остаётся занятым после переезда.

**Требование 3 (живые барьеры на гейт не опираются) — подтверждено:** исполняемых ссылок на
`verify_M-88` / `m88_predicates` / `red_m88_mutants` вне `docs/archive/` нет.

```
$ git grep -nE "verify_M-88|m88_predicates|red_m88_mutants|red_verify_M-88" origin/docs/m88-archive -- scripts .github deploy 'crates/*/src'
(пусто)
```

**Свежесть базы (`gates.md` §8, предел `strict: false`) — проверена, риск не реализуется:**

```
$ git merge-base origin/main origin/docs/m88-archive
3d2a2ac14332a54f5981886df2ed9a1bc347e804   # == origin/main, ветка НЕ отстаёт
```

## 5. НАХОДКА N-1 — восемь висячих ссылок на перееханную спеку (`TD-218`, MINOR, НЕ блокер)

**Что.** Тело коммита `9f482a2` утверждает: «ссылок из ЧУЖИХ зон нет — grep по crates/\*/src,
deploy, .github пуст, значит коммит собирается одной ролью и SCOPE VIOLATION не порождает».
**Утверждение ложно по факту.** На базе переезда:

```
$ git grep -nE "milestones/M-88" 3d2a2ac -- 'crates/**/src/**' | wc -l
6                                    # crates/gateway/src/lib.rs:247,361,536,2793,2979,3028 — зона engine-dev
$ git grep -lE "milestones/M-88" 3d2a2ac -- crates
crates/gateway/src/lib.rs
crates/gateway/tests/red_m88_contract_form.rs        # sacred-зона architect'а
crates/gateway/tests/red_m88_update_contract.rs      # sacred-зона architect'а
```

Спека переезжала тем же коммитом, что и гейт, а путь `milestones/M-88-*.md` после переезда не
существует (`ls milestones/M-88*` → `No such file or directory`). Требование 2 нормы `Р-2`
запрещает выносить артефакт, на который ссылается ЧУЖАЯ зона, до снятия ссылки её владельцем —
здесь чужая зона есть (`crates/*/src` — engine-dev).

**Почему ложная пустота получилась — ДВЕ независимые причины, обе воспроизведены:**

1. **паттерн.** Греп искал имя ГЕЙТА (`verify_M-88`), а переезжала ещё и СПЕКА:
   ```
   $ git grep -nE "verify_M-88" 3d2a2ac -- 'crates/**/src/**'
   (пусто — гейт из src действительно не зовут)
   ```
2. **pathspec.** Форма `crates/*/src` без `**` молча возвращает НОЛЬ:
   ```
   $ git grep -lE "milestones/M-88" 3d2a2ac -- 'crates/*/src' | wc -l
   0
   $ git grep -lE "milestones/M-88" 3d2a2ac -- 'crates/**/src/**' | wc -l
   1
   ```
   Вторая причина опаснее первой: форма выглядит правильной и отвечает «чисто» молча.

**ПРЕДЕЛ МОЕЙ СОБСТВЕННОЙ ПРОВЕРКИ, названный, а не умолчанный:** мой первый греп по этой
ветке был неполон ТЕМ ЖЕ способом — я искал литерал
`M-88-liquidity-removal-contract\.md` и не видел формы со звёздочкой `milestones/M-88-*.md`,
которой написаны ссылки в `src/lib.rs`. Находка появилась только на третьем заходе. Записываю
это здесь, потому что вердикт, скрывающий слабость своей же проверки, — тот же класс, что
разбирает находка.

**Почему НЕ блокер и почему merge не отозван.** Ссылки не исполняемые — доккомментарии;
сборка, тесты и все барьеры зелены (`fmt+clippy+test` pass 12m13s, 19/19 чеков). Ничего не
сломано; откат стоил бы дороже дефекта. Но класс реален — «документ указывает на путь,
которого нет», родня `TD-138`.

**Механизма против этого НЕТ, и он здесь не изображается.** Кандидат, названный в
`04-workflow.md` §2 («для каждого `docs/archive/verify_M-NN.sh` не должно существовать
упоминания `verify_M-NN` вне `docs/archive/**`»), покрывает ГЕЙТ и НЕ покрывает СПЕКУ — то
есть ровно этот случай прошёл бы и мимо него. Барьер обязан брать ОБА имени и грепать
pathspec'ом с `**`. Заведено как `TD-218`.

**Кому передано:** шесть ссылок в `crates/gateway/src/lib.rs` — **engine-dev** (impl-код мне и
architect'у закрыт); две в `crates/gateway/tests/**` — **architect** (sacred). Свои две
(`PROJECT-STATE.md`, `TECH-DEBT.md`) я снял этим же коммитом — это моя зона.

---

## 6. ШАГ 1 REVIEWER'А — `M-88` объявлен ЗАКРЫТЫМ милестоуном

`PROJECT-STATE.md` §M-88: заголовок «✅ КОД В MAIN» заменён на **«✅ МИЛЕСТОУН ЗАКРЫТ
2026-09-23 (`R-198`)»**, с явным основанием прямо в разделе.

**Основание.** Таблица `Р-2`, строка 1: «назвать, какие милестоуны ЗАКРЫТЫ» — зона reviewer'а,
потому что закрытость есть СУЖДЕНИЕ, а не факт после merge'а (`R-098` §3.1: три ревизии
машинного критерия дали три разных ответа). Суждение опирается на: все задачи §Tasks
исполнены; приёмка прогнана (17 PASS / 0 FAIL / exit=0, `R-197` §4); прод проверен глазами;
обе половины close-out'а состоялись — архивная (PR #218) и эта.

**Что при этом НЕ объявлено закрытым, и почему различение существенно.** Закрыт МИЛЕСТОУН, а
не ДЕФЕКТ. `M-88` чинит серверную половину и заканчивается ею; строка `P0-CORR` описывает то,
что видит ПОЛЬЗОВАТЕЛЬ. Объявить закрытым милестоун — можно; объявить вылеченным дефект,
предъявить который не на чем, — нельзя.

## 7. ШАГ 2 REVIEWER'А — решение по колонке «Состояние» строки `P0-CORR`

**РЕШЕНИЕ: строка ОСТАЁТСЯ 🟡 открытой; условие закрытия ЗАМЕНЕНО.**

**Почему не ✅.** Предмет строки — пользовательский дефект («снятая ликвидность остаётся на
карте у клиента НАВСЕГДА»). Пока потребителя нет, лечение предъявить не на чем. Закрыть строку
суждением «здесь всё сделано» значило бы подменить предмет строки предметом милестоуна — ровно
то, от чего ограждает класс «built-not-wired» (`gates.md` §4 DoD).

**Почему прежнее условие снято.** `A-036` §4.3 п.4 требовал «прогнать 7+4 эталонных вектора
против фронта». Условие стояло на допущении, что потребитель чужой, но СУЩЕСТВУЕТ и достижим.
Founder снял допущение 2026-09-23: фронт — не предмет этого репозитория, он появится позже и в
другом месте. Здесь у условия нет ни исполнителя, ни предмета; невыполнимое требование не
исполняется — оно заменяется догадкой (`CLAUDE.md`). Держать 🟡 на неисполнимом условии значит
превратить единственный источник ПОРЯДКА РАБОТ в вечно-жёлтую строку, которая порядка не
образует.

**Замок арбитра НЕ СНЯТ — сменён НОСИТЕЛЬ.** `A-036` §4.3 п.3 («строка `P0-CORR` после merge
не закрывается; merge сопровождается TD-записью built-not-wired MAJOR») остаётся в силе:
reviewer не вправе отменять решение арбитра и не отменяет. Удерживает замок карточка
`TD-217` — реестр открытого долга и есть правильное место для обязательства, которое здесь не
закрывается; роадмап же перестаёт изображать несуществующую задачу.

**ДЕЙСТВУЮЩЕЕ УСЛОВИЕ ЗАКРЫТИЯ (записано в самой строке):** появление ПЕРВОГО потребителя,
гейтящего на `schema_version == 11`, и его сходимость на 7+4 эталонных векторах
(`docs/archive/M-88-liquidity-removal-contract.md` §5.1-5.2). Владелец — **founder**. Ревизия —
**ПО СОБЫТИЮ** (появление потребителя), не по календарю. Задач для architect/dev строка не
порождает, и это сказано в ней прямым текстом, чтобы её не читали как невыполненную работу.

## 8. `TD-217` переформулирован тем же суждением

«Механизм есть, потребителя нет — и ЗДЕСЬ его не будет.» Переписано «чем закрывается»
(появление первого v11-потребителя и сходимость на векторах; владелец — founder; ревизия по
событию).

**Severity оставлена MAJOR, и это решение, а не инерция.** Цена долга не уменьшилась — она
ОТЛОЖЕНА вместе с потребителем и вернётся целиком в день его появления. Понижать severity за
то, что продукта ещё нет, значило бы награждать отсутствие продукта. Отдельно сохранено
условие корректности: потребитель ОБЯЗАН гейтить на `schema_version == 11`, иначе v10-клиент
прочитает `cob_observed` как `false` ⇒ `NoChange` ⇒ стакан замрёт МОЛЧА.

---

## 9. Post-merge деплой-гейт (`gates.md` §8)

Прод проверен глазами ПОСЛЕ merge'а PR #218:

```
$ ssh -i /home/nous/.ssh/hft_deploy root@167.233.192.131 'docker ps --format "{{.Names}} {{.Status}}"; cat .../recorder.heartbeat'
hft-gateway-serve Up 2 hours (healthy)
hft-recorder Up 2 hours (healthy)
{"events":2346848,"free_bytes":43295604736,"min_free_bytes":10737418240,"next_seq":675582214,
 "segment_index":797,"ts_wall_ms":1790180263735,"writable":true}
Wed Sep 23 04:17:46 PM UTC 2026
```

Контейнеры healthy, heartbeat свежий (`ts_wall_ms` совпадает с часами хоста), журнал растёт,
`writable: true`, свободно 43 ГБ при пороге 10 ГБ. **Предел названия честно:** диф PR #218 не
менял ни строки кода — переименования файлов вне `crates/**` и одна строка документа, — поэтому
поведенческой деградации этот деплой вызвать не мог; проверка сделана как обязательная, а не
потому, что была под подозрением.

## 10. Названные пределы этого вердикта

1. **Закрытость милестоуна — суждение, а не замер.** Машинного критерия нет (`R-098` §3.1), и
   я его не изображаю.
2. **Новое условие закрытия `P0-CORR` наблюдаемо, но не механизировано.** Появление
   потребителя здесь ничем не детектируется; сторожа нет, и кандидат я не выдумываю — предмет
   находится вне репозитория.
3. **Находка `N-1` закрыта записью, а не механизмом.** Барьер висячих ссылок остаётся
   КАНДИДАТОМ (`TD-218`); до его постройки требование держится на внимательности того, кто
   выносит артефакт, — и один раз уже не удержалось.
5. **Барьер поймал ЭТОТ вердикт, и это записано, а не замолчано.** Первая редакция шапки
   несла `verdict: APPROVED`, тогда как машинный перечень `check_gate_meta.sh` знает форму
   `APPROVE`; джоб `GATE-META` дал FAIL за 7 секунд и удержал merge. Ошибка моя, поймана не
   мной — механизмом. Записываю потому, что вердикт, умолчавший о собственном красном гейте,
   ровно так же недостоверен, как отчёт без Done Block.

4. **Ссылки в `crates/**` я не правлю** — impl-код и sacred-тесты мне закрыты
   (`scope-guard.md`); передано владельцам зон, исполнение не проверено этим вердиктом.
