<!-- GATE-META
milestone: M-60a
audited_repo: a3ka/hft-platform
audited_base: fcbbcb7225e8ab80ab5b2528f7f621717afa8617
audited_head: 8caa5d92c6bf3899e2702cf2f169e4690481c8c3
verdict: REJECT
-->

# R-125 — круг 2 перепроверки §9 (PR #80 `2077dc8`, PR #81 `8caa5d9`): четыре блокера сняты, класс ЖИВ строкой выше

**Проверяющий:** независимый Fable-агент со свежим контекстом (`gates.md` §9); тот же, что
выносил `R-124` круга 1. Рамка круга 2 — ТОЧЕЧНАЯ: судятся только исправленные места
(README — три абзаца, `BACKLOG.md` — две строки, §0ter — три строки) плюс повторный греп
класса. Конструкция предметов заново не судится — по (а) круга 1 она подтверждена.

Все проверки — на ДЕРЕВЕ СЛИЯНИЯ: worktree detached `0536505` (текущий `origin/main`,
PR #82) + merge `2077dc8` и `8caa5d9` (`--no-ff`, оба чисты). `audited_base` шапки —
merge-base `fcbbcb7` (обе ветки от него; барьеры per-branch зелены только при этой базе —
подтверждаю наблюдение автора).

**Вердикт: REJECT — узкий.** Все четыре блокера круга 1 исправлены верно, каждая
встроенная команда воспроизводится байт-в-байт. Блокер один и новый — **Б-5**: повторный
греп класса (пункт (ii) рамки, заказанный самим автором) нашёл ЧЕТВЁРТУЮ строку той же
живой таблицы §0ter с тем же дефектом, СТРОКОЙ ВЫШЕ трёх исправленных, — при том что
коммит `2077dc8` озаглавлен «правлю КЛАСС, а не экземпляр». Цена снятия — одна строка.

---

## Проверка исправлений круга 1 — команда → вывод → совпало

| блокер | что проверял | моя команда (дерево слияния) | совпало |
|---|---|---|---|
| **Б-1** | README §«Что говорят о процессе»: «`C-109` арбитра ПОЛУЧИЛ, им был `A-011`; без арбитра — ОДИН `C-114`» + цитата `sed -n '11p;14p'` | `sed -n '11p;14p' A-011-process-package-dispute.md` → обе строки **байт-в-байт** как процитировано; `ls research/arbitration/` в `main` (`A-015`/`A-016`/`A-017` — другие предметы, plan-abc нет) | ✅ снят |
| **Б-2** | `VERDICT: FAIL (10) ← состав ЭТОГО архива»; происхождение `11` названо (`C-094` первой попытки), не затёрто | моя репродукция круга 1: состав архива → `FAIL (10)` exit=1; `+C-094` одиннадцатым → `FAIL (11)` exit=1 — обе цифры теперь стоят на своих местах | ✅ снят |
| **Б-3** | README + `BACKLOG.md:323`: «ОДИН круг из ТРЁХ параллельных осей… Триггера §0 здесь НЕТ»; «возобновление — круг 2 по условиям трёх вердиктов; арбитр НЕ требуется»; у plan-abc арбитр ОСТАВЛЕН с проверкой | `grep -m1 'audited_head' C-11{0,1,2}-*.md \| awk '{print substr($2,1,12)}'` → `2dcad4882ac7` ×3 (тремя строками); формулировки обоих носителей соответствуют артефактам и букве §0 | ✅ снят (см. NOTE-3 о форме показа) |
| **Б-4** | три строки §0ter (`:444`-`:446`), каждая с командой в таблице | `grep -c '^## П-01[3-6]'` → **4** · `git cat-file -e origin/main:scripts/check_review_fa.sh` → **ЕСТЬ** и `grep -c review-fa ci.yml` → **4** · `grep -m1 -o 'ЗАКРЫТ [0-9-]*'` по M-65 → **`ЗАКРЫТ 2026-08-24`** · сверх таблицы: `63c3866` = merge PR #29 ✓, `research/reviews/R-118-M-65-a015-step6.md` `verdict: APPROVE` ✓ | ✅ снят |

Новых ложных утверждений в исправленных местах — пункт (i) рамки — **не найдено**: каждое
новое утверждение о состоянии несёт команду, и каждая команда воспроизведена мной с тем же
выводом.

## Барьеры на дереве слияния (`0536505` + оба PR)

```
check_docs_freeze exit=0 · check_gate_meta exit=0 (PASS, вердиктов проверено: 1 — R-124)
check_protected_artifacts exit=0 · check_artifact_ids exit=0 · check_review_fa exit=0 (SKIP)
```

## Б-5 (БЛОКЕР, PR #80) — класс жив: строка M-69 той же таблицы §0ter

Пункт (ii) рамки («грепни класс ещё раз»). Живая таблица §0ter, строка НАД тремя
исправленными: «**M-69** … | **круг 2 у критика. Круг 1 — `C-099` REJECT** … |
`feat/M-69-window-guard`». Опровергается командой:

```
$ git ls-tree -r --name-only origin/feat/M-69-window-guard -- research/critiques research/arbitration \
    | grep -vFf <(git ls-tree -r --name-only origin/main -- research/critiques research/arbitration)
research/arbitration/A-014-m69-window-guard.md        (verdict: DECISION)
research/critiques/C-099-M-69-window-guard.md         (REJECT, круг 1)
research/critiques/C-100-M-69-window-guard.md         (REJECT, круг 2)
research/critiques/C-104-M-69-r3.md                   (REJECT, круг 3)
research/critiques/C-132-M-69-window-guard-r4.md      (NOTE — проходной, круг 4)
```

Предмет прошёл ЧЕТЫРЕ круга критика и арбитраж `A-014`, круг 4 — проходной `NOTE`; PR #32
OPEN и диспетчирован «tester → reviewer» решением founder'а — так говорит `BACKLOG.md`
строка 0.1 ТОГО ЖЕ дерева слияния, прямо противореча строке §0ter в двух шагах от
исправленных. Это ровно тот дефект, что `:444` у M-65 («круг 4 → REJECT» при закрытом
предмете), и ровно то, от чего заголовок `2077dc8` объявил класс вылеченным. Симметрично
кругу 1: строку чинит PR #80, команда — в таблицу.

Почему блокер, а не NOTE: строка отправляет следующую сессию «на круг 2 критика» предмета,
который уже прошёл круг 4 проходным вердиктом и ждёт tester/reviewer — сессия уйдёт на
созыв гейта, который давно состоялся. Тот же аргумент, которым `2077dc8` сам обосновал
цену трёх строк («обе тратят сессию целиком»).

## NOTE (не блокеры)

- **NOTE-3 (README, Б-3):** показанный вывод `2dcad4882ac7 · 2dcad4882ac7 · 2dcad4882ac7`
  командой не производится — она печатает ТРИ СТРОКИ (класс `R-097` N-7 «показанный вывод
  обязан производиться командой»). Значения верны; при правке Б-5 можно заодно развести
  строки как есть.
- **NOTE-4 (§0ter, строка M-66):** указатель «где лежит: PR #6» протух — `gh pr view 6` →
  **CLOSED**. Этапное утверждение строки («`C-089` REJECT, висит») командой не опровергнуто
  (уникальных M-66-вердиктов новее на четырёх ветках предмета нет), поэтому не блокер.
- **NOTE-1/NOTE-2 круга 1** приняты автором как NOTE — согласен, обязательств не порождают;
  метка «дословно» у цитаты `C-094` на усмотрение автора.

## Условие снятия REJECT

Одна правка PR #80 (`docs/SESSION-HANDOFF.md` §0ter, строка M-69): привести этап к факту с
командой в таблице (форму выбирает автор; факт: круги 1–4 = `C-099`/`C-100`/`C-104` REJECT,
`A-014` DECISION, `C-132` NOTE; PR #32 OPEN, диспетчирован tester → reviewer — `BACKLOG` 0.1).
Круг 3 — точечно эта строка; исправления Б-1…Б-4 повторной проверки не требуют — они
приняты настоящим вердиктом.

## Done Block (агрегированный)

```
$ git rev-parse origin/main → 053650533222945b6b680fb997680c1ac79d3473
$ git merge-base origin/main origin/docs/audit-trail-salvage-2026-08-24 → fcbbcb72…
$ merge 2077dc8 → clean · merge 8caa5d9 → clean (worktree /tmp/recheck2-merge)
$ sed -n '11p;14p' A-011-…md → байт-в-байт с цитатой README
$ grep -m1 audited_head C-11{0,1,2}-*.md | awk … → 2dcad4882ac7 (×3, тремя строками)
$ grep -c '^## П-01[3-6]' ← origin/main:PENDING-SIGNATURE → 4
$ git cat-file -e origin/main:scripts/check_review_fa.sh → ЕСТЬ; grep -c review-fa ci.yml → 4
$ git show origin/main:milestones/M-65-ws-session.md | grep -m1 -o 'ЗАКРЫТ [0-9-]*' → ЗАКРЫТ 2026-08-24
$ git log -1 63c3866 → Merge pull request #29 …; R-118 → verdict: APPROVE
$ пять барьеров на дереве слияния (PUSH_BEFORE=0536505…) → все exit=0
$ gh pr view 6/28/32/80/81 → CLOSED / MERGED / OPEN / OPEN / OPEN
$ ls-tree diff веток M-69/M-66 против main → Б-5 (5 файлов у M-69), у M-66 новых вердиктов нет
```

**Уборка:** merge-worktree `/tmp/recheck2-merge` снят `worktree remove --force` (число — в
Handoff); `target/` не собирался. Резерв: `R-125` взят CAS-механизмом; снять после merge
носителя: `bash scripts/reserve_artifact_id.sh --release R-125` (резерв `R-124` снимается
той же командой после merge PR #81).

FA-инвариант не называется: диапазон не трогает `crates/**` (`check_review_fa` → `SKIP`).
