<!-- GATE-META
milestone: P-022
audited_repo: a3ka/hft-platform
audited_base: f68331cecb5922bbfad0540a3807dedb117a6e8f
audited_head: b9b834c1b51f49850c2a2eb998d979f988375449
verdict: REJECT
-->

# R-137 — перепроверка §9: запись `П-022` (разрезать предмет: готовое вливать, спорное — долгом)

**Вердикт: REJECT.** Решение founder'а, его форма (выбор из четырёх вариантов architect'а),
границы разреза и согласованность с независимыми арбитражами `A-025`/`A-026` сомнению НЕ
подвергаются — граница C соблюдена, воля владельца не расширена и не сужена. REJECT — о
ТЕКСТЕ записи: одно ложное числовое утверждение в таблице замера, предъявленного founder'у
(Б-1), и неслияемость предмета с текущим `main` (Б-2, расхождение прогонов ветка/слияние —
блокер по `gates.md` §8). Плюс пять точностных находок (Н-1…Н-5). Это третья запись класса
«решение founder'а» подряд, снятая за фактическую точность (`R-114`, `R-132`, теперь эта), —
штатная работа гейта, не провал предмета.

**База аудита:** `audited_base` = `f68331c` — родитель единственного коммита предмета;
`origin/main` на момент прогона уехал на `0bb6c50` (PR #103, `П-021`), и это расхождение —
предмет Б-2, а не смена базы. Предмет — ровно один коммит `b9b834c`
(`docs/PENDING-SIGNATURE.md` +101/−0, `git show --numstat` воспроизведён).

Перепроверяющий — независимый Fable-агент со свежим контекстом; автор записи — сторона.
Прогоны — в собственном дереве `/tmp/hft-recheck-r137`, detached `b9b834c`; общий чекаут
не тронут.

**Ярус C читан ГРЕПОМ, что искал:** `docs/PENDING-SIGNATURE.md` по `^## П-` (индекс);
`R-132` по `Н-4` (позиция находки); `session-handover-2026-08-27.md` по `арбитраж` (§5:114);
`A-024` по `стоимость гейтов|Круга 9` (строки §8); `A-026` по `разрез|долг` (применение
разреза); вердикт-шапки всех 17 критик по `^verdict:|^##|^###` (классы находок). Целиком
открыты: `П-022` (весь дифф), `П-019`, `П-020`, `П-021`, `П-017` (шапка+A2), `R-132`,
`C-170`, `C-169`, `C-168` (находки), `A-025` (целиком до §6), `A-026` (структура + §1/§3bis/§4/§5
по грепу с чтением окрестностей), `R-133`/`R-134` (шапки и вердикт-секции), `A-024` §8.

---

## Б-1 (блокер) — «M-71: … пять коммитов» — ЛОЖНО: коммитов реализации ЧЕТЫРЕ

Запись (таблица «Замер, предъявленный founder'у ДО ответа»): «`M-71` | … | пять коммитов,
ревью `R-133` REJECT».

Воспроизведение:

```
$ git rev-list --count 0adc4e1..ca482e4        # диапазон, судимый R-133
4
$ git log --format='%h %s' 0adc4e1..ca482e4
ca482e4 feat(M-71): tasks #2,#6 — wire-путь enforce'ит предел, эхо venue усечено [engine-dev]
4b9dfab feat(M-71): task #4 — GATEWAY_MAX_RESPONSE_BYTES env parsing, fail-closed [engine-dev]
3419177 feat(M-71): task #4 — ручка GATEWAY_MAX_RESPONSE_BYTES в docker-compose [engine-dev]
de85577 feat(M-71): task #1,2,3 — предел объёма в шести строителях gateway [engine-dev]
$ git show origin/feat/M-71-rev6:research/reviews/R-133-M-71-egress-cap-impl.md | sed -n '12p'
**Предмет:** `0adc4e1..ca482e4` на `origin/feat/M-71-egress-cap-dev` (4 коммита engine-dev)
$ git log --all --format='%h %s' --grep='feat(M-71)' | grep -c 'engine-dev'
4
$ git log --format='%h %s' ca482e4..origin/feat/M-71-egress-cap-dev   # после ревью — только вердикт
fcbcd4a docs(M-71): R-133 — impl REJECT: … [reviewer]
```

Сам `R-133` — первоисточник, на который строка ссылается, — называет «4 коммита engine-dev»
в собственной шапке. Пятого impl-коммита нет ни на одной ветке (`--all` по subject-классу).
Вероятный источник ошибки — включение базы диапазона в счёт.

Существо довода записи НЕ рушится — соотношение «17 кругов против четырёх коммитов» даже
острее, чем против пяти. Но это число стоит в таблице, объявленной «замером, предъявленным
founder'у ДО ответа», в записи, чей предмет — стоимость гейтов против стоимости реализации.
Прецедент квалификации — `R-132` Б-1 (существо абзаца выживало, ложная опора — всё равно
блокер) и `R-114` Б-1 (ложное утверждение о состоянии в записи решения founder'а). Строка
«Воспроизводится двумя командами» покрывает только числа M-68 — счёт M-71 в записи не
подкреплён командой вовсе, и вот чем это кончилось.

## Б-2 (блокер) — предмет неслияем: конфликт с текущим `main`; прогоны ветка/слияние расходятся

`main` с момента отреза ветки принял `П-021` (PR #103, merge `0bb6c50`) — 75 строк в хвост
того же `docs/PENDING-SIGNATURE.md` от той же базы `f68331c`. Обе правки дописывают хвост
файла → текстовый конфликт:

```
$ git merge-tree $(git merge-base HEAD origin/main) HEAD origin/main | grep -c '<<<<<<<'
1
$ bash scripts/verify_design_claims.sh . 2>&1 | tail -1
VERDICT: PASS (0 нарушений)                                    # дерево ветки
$ bash scripts/verify_design_claims.sh --merge-preview origin/main 2>&1 | tail -3
FAIL  [SETUP] --merge-preview: слияние base-ref 'origin/main' + HEAD (b9b834c…) КОНФЛИКТУЕТ —
merge-цель не собирается автоматически …
VERDICT: FAIL (1 нарушений)
```

`gates.md` §8: «расхождение прогонов — блокер, не примечание». Автор в этом не виноват
хронологически (оба предмета шли параллельно в один вечер), но влить запись в таком виде
нельзя, и разрешение конфликта — содержательное действие: `П-022` обязан встать ПОСЛЕ
`П-021` (порядок индекса — ярус A стартового протокола). Взаимной зависимости текстов нет
(`П-022` на `П-021` не ссылается) — правка механическая, но обязана пройти этим же кругом
с повторным `--merge-preview` → PASS.

## Н-1 — «`R-132` Н-4 … снял предыдущую запись именно за это» — снятие несли Б-1/Б-2

Шапка `R-132` (открыта): «REJECT — о ТЕКСТЕ записи: **два ложных фактических утверждения
(Б-1, Б-2)** … Плюс четыре точностных находки (Н-3…Н-6)». Снятие (REJECT) несли блокеры;
`Н-4` — точностная находка, чья правка была условием APPROVED (п.4 списка условий). Запись
противоречит сама себе через предложение: сначала «Н-4 … снял … именно за это», затем
«Блокеры Б-1/Б-2 того же вердикта были о другом». Верная форма: «`Н-4` того же вердикта
поймал ровно этот класс (выбор, поданный как вывод); снятие записи несли Б-1/Б-2 — о
другом». Позиция `:84` и купированная цитата — точны (проверено `grep -n`, `sed -n '82,86p'`).

## Н-2 — воспроизводящая команда счёта M-68 меряет НЕ ТО, что заявляет

`git ls-tree -r --name-only <ref1> <ref2> <ref3> -- research/critiques/` — `ls-tree`
принимает ОДИН tree-ish; `origin/feat/M-68-rev3` и `origin/feat/M-68-depth-from-book`
съедены как pathspec и молча проигнорированы (несуществующий путь в дереве ошибки не даёт).
Команда фактически считает только `rev4`:

```
$ git ls-tree -r --name-only origin/feat/M-68-rev4 -- research/critiques/ | grep -ic 'M-68'
9        # то же число, что у «трёхветочной» формы
$ git ls-tree -r --name-only origin/feat/M-68-rev3 -- research/critiques/ | grep -ic 'M-68'
6        # ⊂ rev4
$ git ls-tree -r --name-only origin/feat/M-68-depth-from-book -- research/critiques/ | grep -ic 'M-68'
2        # ⊂ rev4
```

Число 9 ВЕРНО — но только потому, что `rev4` оказался надмножеством; при непересекающихся
ветках форма недосчитала бы молча. `testing.md`: «оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ»;
команда, предъявленная founder'у как воспроизведение замера, — тот же класс. Правка:
одна ветка-надмножество названа явно, либо цикл по ref'ам с `sort -u`.

## Н-3 — «все находки касались наборов оракулов» — минимум три круга нашли противоречие СПЕКИ

Классы находок всех 17 кругов сняты по файлам вердиктов (шапки + тела находок): дефекта
РЕАЛИЗАЦИИ не назвал ни один — сильная половина утверждения ПОДТВЕРЖДЕНА. Но «все находки —
наборы оракулов» неточно: `C-162` F1 («взаимоисключающая нормативная спецификация»),
`C-168` R1 и `C-169` R1 (политика отсутствия: «two normative statements … prescribe the
opposite outcomes») — находки о НОРМАТИВНОЙ СПЕКЕ, и запись сама говорит это ниже
(«случай `C-169` R1, где два нормативных носителя … предписывают противоположные исходы»).
Собственная граница применимости записи опровергает её же обобщение. Правка: «все находки
касались проверяющей оснастки — наборов оракулов и нормативной спеки; ни одна — реализации».
Заодно честно назвать предел: большинство кругов АУДИРОВАЛИ артефакт-сет ДО существования
реализации — «не нашли дефекта в продукте» для них конструктивно, а не эмпирически.

## Н-4 — асимметрия таблицы: вердикт ревью назван для M-71 и скрыт для M-68

Строка M-71 несёт «ревью `R-133` REJECT»; строка M-68 — «2 [ревью]» без вердиктов, тогда как
`R-134` (impl-ревью M-68) — тоже **REJECT**, четыре блокера («три из четырёх — утверждения
кода О СЕБЕ, четвёртый живёт на пути `LiveReducer::pump`…»; при этом «работа по существу
сделана и сделана хорошо»). Читатель таблицы делает вывод, что ревью M-68 прошло. Симметрия
обязательна: либо вердикты обоих ревью, либо ни одного. (На довод записи это работает В ПОЛЬЗУ:
оба импла отревьёваны REJECT'ами — счёт кругов ещё не конец.)

## Н-5 — шапка «Статус» открывает чтение решения как нормы класса

«Предмет — … маршрут двух застрявших милестоунов … **и, следом, конструкция plan-time гейта
для предметов этого класса**» — эта половина фразы читается как включение конструкции гейта
для КЛАССА в предмет решённого, тогда как «Чего это решение НЕ говорит» п.2 прямо запрещает
такое чтение («Обобщение до нормы потребует своего круга и своей записи; здесь оно НЕ
сделано»). Конструкция гейта для класса — предмет ВОПРОСА, поставленного `A-024` §8.4, а не
предмет ОТВЕТА founder'а. Уточнить шапку, чтобы п.2 не приходилось вычитывать против неё.

---

## Что ПОДТВЕРЖДЕНО (существо записи держится)

1. **Счёт кругов и ревью — верен пофайлово.** M-68: 9 уникальных критик на `rev4`
   (`C-094`, `C-138`, `C-156`, `C-160`, `C-162`, `C-164`, `C-165`, `C-167`, `C-170`);
   2 ревью (`R-130`, `R-134`). M-71: 8 критик на `rev6` (`C-157`, `C-158`, `C-159`,
   `C-161`, `C-163`, `C-166`, `C-168`, `C-169` — грепом `m-?68`/`m-?71`, регистронезависимо,
   с учётом имён без дефиса); 1 ревью (`R-133`). Итого 17 кругов ✓.
2. **Арбитражи на момент вопроса: 3 и 2 — верно.** `A-025` закоммичен 23:16:40, `A-026` —
   23:14:15, запись — 23:13:44 (все 26.08 UTC): оба ИСПОЛНЯЮТ разрез и в замер до ответа
   не входят. `A-018`/`A-023`/`A-024` и `A-021`/`A-022` существуют на ветках предметов ✓.
3. **`44d6aac`: `234 80 crates/gateway/src/lib.rs`** — один файл, дословно ✓.
4. **Поправка к передаче верна:** `session-handover-2026-08-27.md:114` — «девять кругов
   критика, два ревьюера, **два арбитража**»; их три ✓.
5. **`A-024` §8 п.4 — строки 273-275 дословно; п.5 «Круга 9 не существует ни при каком
   исходе» — строка 277 ✓;** `C-170` адресует «Per `A-024` §8.4–§8.5» ✓.
6. **`gates.md` §0.1 процитирован верно** («арбитр решает „как ПРАВИЛЬНО", founder — „чего
   мы ХОТИМ"», файл открыт) ✓.
7. **`TECH-DEBT.md` — зона reviewer'а** (`scope-guard.md`, таблица владения; профиль
   architect'а «NEVER writes») ✓; операционная механика записи «арбитр называет текст
   карточки → reviewer заводит» ДОСЛОВНО совпадает с тем, что независимо предписали
   `A-025` §5.3(б)+§6 («тексты в §6, зона reviewer'а, ни арбитр, ни architect в
   `TECH-DEBT.md` не пишут») и `A-026` §3bis ✓.
8. **Граница применимости разреза (Блок 3.1 мандата) — ВЕРНА и подтверждена арбитрами
   независимо.** `A-026` §1: «Разрез founder'а … к R1 НЕПРИМЕНИМ … это спека, предписывающая
   dev'у два противоположных исхода одновременно»; §5: «разрез применён несимметрично — R1
   сделан единственной политикой …, R2(в) вынесен долгом». `A-025` §4: три непокрытия —
   долгом, готовое — в работу. Расхождения записи с арбитражами НЕТ; помета «утверждение
   architect'а, не founder'а» — честная, и `A-026` §5 несёт то же уведомление founder'у ✓.
9. **Kill-фраза (Блок 3.2) — не декоративна:** называет класс невалидной ссылки (пропуск
   проверки БЕЗ карточки долга), последствие (отклонение) и двойное ограничение (объявленное
   непокрытие; только два предмета). Конструкция — прецедент `П-021`/`R-136` Б-3 ✓.
10. **Не-обобщение (Блок 3.3) держится** четырьмя пунктами «НЕ говорит» — с оговоркой Н-5
    по шапке. **Предмет не подменён (Блок 3.4):** запись отвечает на вопрос о стоимости
    гейтов; смежных решений (что готово; лимиты; risk/killswitch/contracts; граница C) не
    присваивает — п.1/п.3 прямо возвращают их арбитрам и марш­рутам ✓.
11. **Форма ответа founder'а раскрыта честно** (выбор из четырёх вариантов architect'а,
    рекомендованный пункт; «формулировки — мои, воля — его») — ровно урок `R-132` Н-4,
    применённый к себе ✓. «Доводи оба» отменён с основанием: ответ дан до `C-170`
    (22:20:44) — передача §5 это attestует; `C-169` лёг 22:18:58, двумя минутами раньше, —
    «до C-170 и C-169» согласовано ✓.
12. **Полномочия и замок §11:** зона `docs/PENDING-SIGNATURE.md` — architect; в зону
    docs-freeze НЕ входит — проверено ПО КОДУ барьера, не по аналогии
    (`check_docs_freeze.sh:63`: `.claude/rules/*|.claude/agents/*|.claude/wrappers/*|CLAUDE.md|docs/04-workflow.md`);
    `bd20428` (`П-020`) токена действительно не нёс (`git log -1 --format='%B' bd20428 |
    grep -c FOUNDER-APPROVED` → молчание, exit=1) и прошёл ✓. `b9b834c` токена не несёт и
    не обязан ✓.
13. **Барьеры в прод-форме CI** (`EVENT_NAME=pull_request`, `PR_BASE_SHA=f68331c…`), решение
    по коду возврата: `check_docs_freeze`=0 · `check_protected_artifacts`=0 ·
    `check_artifact_ids`=0 · `check_gate_meta`=0 · `check_review_fa`=0 (`SKIP` — диапазон не
    трогает `crates/**`; предел барьера M-66, живой FA-инвариант не требуется, waiver не
    нужен) · `check_context_budgets`=0. `verify_design_claims.sh .` на дереве ветки —
    `VERDICT: PASS (0 нарушений)`, exit=0 — заявка тела коммита воспроизведена ✓.
14. **Висячих ссылок нет:** `R-132:84`, передача §5:114, `A-024:273-275/:277`, `C-169` R1,
    `C-170`, все `A-*`/`C-*`/`R-*` идентификаторы — существуют и открыты этим прогоном ✓.

## Условие APPROVED (rev2 записи, один коммит на этой же ветке после влития origin/main)

1. **Б-1:** «пять коммитов» → «четыре коммита» (рядом — команда:
   `git rev-list --count 0adc4e1..ca482e4` → 4; `R-133` шапка говорит то же).
2. **Б-2:** влить `origin/main` в ветку (или rebase), `П-022` — ПОСЛЕ `П-021`;
   `bash scripts/verify_design_claims.sh --merge-preview origin/main` → `VERDICT: PASS`.
3. **Н-1:** «снятие несли Б-1/Б-2; Н-4 поймал ровно этот класс».
4. **Н-2:** команда счёта — одна ветка-надмножество, названная явно, либо честный цикл.
5. **Н-3:** «касались проверяющей оснастки — оракулов и нормативной спеки; ни одна —
   реализации» + предел (большинство кругов судили артефакт-сет до существования кода).
6. **Н-4:** вердикт `R-134` REJECT назвать в строке M-68 (симметрия с M-71).
7. **Н-5:** шапка «Статус» — конструкция гейта для класса есть предмет вопроса `A-024`,
   не предмет ответа; убрать чтение «решено для класса».

Дельта-перепроверка по названным пунктам, не новый полный круг.

## Done Block

```text
$ git -C /tmp/hft-recheck-r137 rev-parse --short HEAD
b9b834c
$ git show --numstat --format='' b9b834c
101	0	docs/PENDING-SIGNATURE.md
$ git rev-list --count 0adc4e1..ca482e4
4
$ git ls-tree -r --name-only origin/feat/M-68-rev4 -- research/critiques/ | grep -ic 'M-68'
9
$ git ls-tree -r --name-only origin/feat/M-71-rev6 -- research/critiques/ | grep -icE 'm-?71'
8
$ git show --numstat --format='' 44d6aac
234	80	crates/gateway/src/lib.rs
$ sed -n '114p' docs/workflow/session-handover-2026-08-27.md
владелец». Счёт по M-68: девять кругов критика, два ревьюера, два арбитража — против одного
$ git merge-tree $(git merge-base HEAD origin/main) HEAD origin/main | grep -c '<<<<<<<'
1
$ for s in docs_freeze protected_artifacts artifact_ids gate_meta review_fa context_budgets; do
    EVENT_NAME=pull_request PR_BASE_SHA=f68331cecb5922bbfad0540a3807dedb117a6e8f \
    bash scripts/check_$s.sh >/dev/null 2>&1; echo "check_$s=$?"; done
check_docs_freeze=0
check_protected_artifacts=0
check_artifact_ids=0
check_gate_meta=0
check_review_fa=0
check_context_budgets=0
$ bash scripts/verify_design_claims.sh . >/dev/null 2>&1; echo "branch=$?"
branch=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main >/dev/null 2>&1; echo "merge=$?"
merge=1
```
