<!-- GATE-META
milestone: M-70
audited_repo: a3ka/hft-platform
audited_base: d179886b263963ea5c446b3d1b57f74c316346a5
audited_head: d376af1d5752d3edaed137d0ed52788cb0c326fd
verdict: REJECT
-->

# R-191 — перепроверка `gates.md` §9: close-out `M-70` (ветка `docs/closeout-m70`, `d376af1`)

**Роль:** architect-клон, Fable, свежий контекст (`gates.md` §9: автор правки — сторона, себя
не проверяет) · **Дата:** 2026-09-20T21:46Z · **Предмет:** один коммит `d376af1` поверх
`origin/main` = `d179886`: `git mv` спеки и гейта `M-70` в `docs/archive/`, колонка «Где»
строки 9 роадмапа, `§0closeout` + `§0closeout-bis` в архивной спеке.

**ВЕРДИКТ: REJECT — один блокер, три поправки факта, два замечания.** Блокер не в том, ЧТО
сделано, а в том, что норма, по которой это сделано, содержит требование, которое коммит
нарушает и одновременно объявляет проверенным: `04-workflow.md` §2 Close-out, требование 2 —
«гейт, на который ссылается ЧУЖАЯ зона, НЕ выносится, пока ссылку не снял её владелец».
Ссылок в чужой зоне ДВЕ (`crates/gateway/src/lib.rs:111`, `docker-compose.yml:138`), обе
живы на дереве слияния, ни одна не названа ни в теле коммита, ни в `§0closeout`. Из 31 уже
архивного гейта ни один не оставил за собой ссылку в `crates/*/src/**`, `docker-compose.yml`
или `deploy/**`; четыре гейта (`M-35 M-45 M-48 M-49`) держатся в `scripts/` ровно из-за
таких же ссылок-комментариев. `M-70` стал бы первым исключением, и не по решению, а по
недосмотру. Всё остальное, что автор утверждает о коде, подтвердилось командой (§2).

---

## 1. Дерево слияния и барьеры — проверено командой, не принято

```text
$ git rev-parse origin/main origin/docs/closeout-m70
d179886b263963ea5c446b3d1b57f74c316346a5
d376af1d5752d3edaed137d0ed52788cb0c326fd
$ [ "$(git merge-base origin/main HEAD)" = "$(git rev-parse origin/main)" ] && echo FF
FF                      # ветка — fast-forward от origin/main ⇒ дерево слияния == дерево ветки
$ git show --stat --format='' -M HEAD
 docs/ROADMAP.md                                    |  2 +-
 {milestones => docs/archive}/M-70-depth-bands-enablement.md | 93 +++
 {scripts => docs/archive}/verify_M-70.sh           |  0
$ git show -M --word-diff=plain --format='' HEAD -- docs/ROADMAP.md | grep -oE '\[-[^]]*-\]|\{\+[^}]*\+\}'
{+`docs/archive/M-70-depth-bands-enablement.md` + `docs/archive/verify_M-70.sh` (переезд 2026-09-20 …+}
{+влита и удалена)+}      # удалений нет ⇒ колонка «Состояние» не тронута — верно
```

Барьеры CI, в форме вызова CI (`EVENT_NAME=pull_request PR_BASE_SHA=d179886…`):

```text
check_roadmap_sync         OK: close-out (1) сопровождён правкой docs/ROADMAP.md   VERDICT: PASS   exit=0
check_protected_artifacts  NOTE  milestones/M-70-…: ALLOW-ARTIFACT-DELETE в d376af1  OK           exit=0
check_docs_freeze                                                                                  exit=0
check_artifact_ids         OK: … не ввёл второй носитель под занятым идентификатором             exit=0
check_gate_meta            VERDICT: PASS — вердиктов проверено: 0                                  exit=0
check_review_fa            SKIP (диапазон не трогает crates/**)                                    exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -2
PASS  [H-FACTS-SHA] маркеров `FACTS:` проверено 19 — все ревизии сбора существуют …
VERDICT: PASS (0 нарушений)                                                                        exit=0
```

Все зелёные — и это ничего не говорит о блокере ниже: требование 2 Close-out'а барьера не
имеет (`04-workflow.md:61-68` называет его кандидатом, «строить тем же кругом, что и первый
вынос»). Первый вынос состоялся (31 гейт в архиве), барьера нет — нарушение ловится только
глазами. Здесь и поймано.

Ярус S (`reading-map.md` §2): `gh run list --branch main --limit 5` — пять `success`,
последний CI `#206` 20:57Z; `deploy.yml` — три `success`; прод `git rev-parse --short HEAD`
→ `46cd25c` (последняя КОДОВАЯ ревизия, docs-коммиты деплой не триггерят — `R-190` §3);
`check_branch_health.sh` → `VERDICT: PASS` (веток 13, замечаний 0).

## 2. (а) Утверждения о коде — каждое командой на дереве слияния

| утверждение автора | команда / замер | исход |
|---|---|---|
| `verify_M-70.sh` — гейт диапазона, task #6 в `:270-291`, извлечение в `:281-282` | `grep -nE '^step "task #6'` → 270; `V_BASE=`/`V_HEAD=` → 281/282; внешний `fi` → 292 | ✅ (диапазон 270-292, `:291` — внутренний `fi`; не дефект) |
| после вливания `V_BASE == V_HEAD == 10` ⇒ FAIL при любом дереве | `eval "$(sed -n '270,292p' docs/archive/verify_M-70.sh)"` на дереве ветки → `FAIL: task #6 версия схемы НЕ поднята (база 10, HEAD 10)`; `git show d179886:crates/gateway/src/lib.rs \| sed -n 's/^pub const GATEWAY_SCHEMA_VERSION: u32 = \([0-9]\+\);.*/\1/p'` → 10 | ✅ исполнением, не арифметикой на бумаге |
| бамп 9→10 состоялся коммитом `09a7fdf` | `git show 09a7fdf -- crates/gateway/src/lib.rs \| grep -E '^[-+]pub const GATEWAY_SCHEMA_VERSION'` → `-… = 9;` / `+… = 10;`; subject `feat(M-70): task #6 — GATEWAY_SCHEMA_VERSION 9→10 [engine-dev]` | ✅ |
| `crates/gateway/src/lib.rs:117` → `10` | `sed -n 117p` → `pub const GATEWAY_SCHEMA_VERSION: u32 = 10;` | ✅ |
| task #7 — `:311`, обоснование `:334-335` | `grep -n` → 311 `step "task #7 …"`; 334 «Пока это так, флип дефолта на канонический = включение известной лжи…»; 335 «КРАСНЕТЬ на таком флипе. Это страж РЕШЕНИЯ…» | ✅ |
| CI не зовёт: 0/0/0 | `grep -c verify_M-70` по `ci.yml`/`deploy.yml`/`branch-build.yml` → 0/0/0 | ✅ |
| «26 упоминаний … ни одного исполнения» | `git grep -l verify_M-70 -- . ':!docs/archive/**' \| wc -l` → **26 файлов**; `git grep -c … \| awk` → **94 строки**; исполняемых вызовов вне `*.md` — 0 | ⚠ 26 — это ФАЙЛЫ, не упоминания (Б-4); «ни одного исполнения» — верно, но это НЕ критерий нормы (Б-1) |
| универсум номеров не сужается: `next_artifact_id.sh:91`, `check_artifact_ids.sh:66` | `:91` → `M) _sed_re='^(milestones\|docs/archive)/M-…'`; `:66` → `docs/archive/M-*.md) ;;` | ✅ |
| `.gitignore:4` | локально `sed -n 4p .gitignore` → `.env`; на проде `git check-ignore -v .env` → `.gitignore:4:.env	.env` | ✅ |
| прод: `.env` 132 Б, бэкап 84 Б, оба mtime 19:43, `docker inspect` несёт семь полос | `ssh … 'ls -la .env*'` → `132 Sep 20 19:43 .env`, `84 Sep 20 19:43 .env.bak-1789933437`; `docker inspect hft-gateway-serve … \| grep GATEWAY_BANDS` → `0.015,0.03,0.05,0.08,0.15,0.3,0.6` | ✅ размеры и значение |
| бэкап «содержит ЗНАЧЕНИЕ ДО включения» | `grep -E '^GATEWAY_BANDS=' .env.bak-1789933437` → **ни одной строки**; ключи бэкапа: только `GATEWAY_JWT_SECRET` | ❌ **Б-2** — бэкап значения НЕ содержит вовсе |
| 9-B: PR #205, «1 мин 45 с»; 9-C: PR #206, `R-190`; merge `M-70` PR #193 `a54c7d5` | `gh pr view 205/206/193 --json mergeCommit` → `4ddd392` / `d179886` / `a54c7d5`; `grep -c '1 мин 45' docs/plans/depth-liveness-window-measurement-2026-09-20.md` → 4 | ✅ |
| ветка `docs/M-70-rev2` «влита и удалена» | `git ls-remote --heads origin docs/M-70-rev2 \| wc -l` → 0 | ✅ |
| ≈23 мин холодной пересборки «16:30:19 → 16:53:05» | `PROJECT-STATE.md:2649-2650` несёт обе метки | ✅ |
| `TD-206` — класс «никто не наблюдает отсутствие» | `TECH-DEBT.md:158` — `TD-206` «прод-выдача НЕ ДОХОДИТ до клиента», закрыт `R-189` | ✅ по смыслу |
| «по передаче `R-190` §D» (тело коммита) | `grep -inc 'handoff' R-190` → 0; `grep -n '§D'` → 0; файл 182 строки, кончается §7 | ❌ **Б-3** — секции §D в `R-190` нет |

## 3. (б) Полномочия

- **Зона.** `milestones/*.md`, `scripts/verify_*.sh`, `docs/**` — architect (`scope-guard.md`).
  Роадмап: тронута ТОЛЬКО колонка «Где» (`git show -M --word-diff` выше — вставки без
  удалений); «Состояние» — reviewer-owned — не тронута. ✅
- **Замок §11.** `.claude/**`, `CLAUDE.md`, `docs/04-workflow.md` в дифе отсутствуют
  (`git show --stat` — три файла); `check_docs_freeze` exit=0. `FOUNDER-APPROVED` не требуется. ✅
- **Токены переезда** (`04-workflow.md:105-108`): `ALLOW-ARTIFACT-DELETE` и
  `ALLOW-SUBJECT-CHANGE` оба в теле коммита; `check_protected_artifacts` прочёл первый
  (`NOTE … ALLOW-ARTIFACT-DELETE в d376af1`), второй лок не взводился (вердиктов в диапазоне
  0) — норма сама называет это латентным. ✅
- **Право на СВОЙ PR (§9).** Переезд гейта и спеки на close-out — предписанный нормой шаг
  architect'а (`04-workflow.md:74-79`, таблица ролей), close-out-пруфы автор приземляет сам.
  Это НЕ форма: инварианты, границы, фазы не меняются. Маршрут верен — но маршрут не спасает
  предмет, нарушающий требование той же нормы (§4, Б-1).
- **Граница C — снятие сторожа `task #7`.** Сужу самостоятельно, как требует мандат.
  Коммит НЕ меняет ни одного развёрнутого состояния: дефолт репозитория остаётся
  `${GATEWAY_BANDS:-0.001}` (`docker-compose.yml:147`), прод несёт семь полос по host `.env`.
  Гейт из CI не вызывается (0/0/0), то есть как БАРЬЕР на `main` он не работал никогда.
  Следовательно, само снятие — не решение границы C: оно ничего не двигает и ничего не
  разблокирует машинно. **Но довод автора «основание отпало» — неполон.** У сторожа было
  ДВА основания, и автор разобрал одно. Первое (`:334-335`) — «пока метка врёт, флип
  дефолта = включение лжи» — действительно отпало замером 9-B и решением founder'а.
  Второе названо reviewer'ом ПОСЛЕ включения и ТЕМ ЖЕ ДНЁМ: `R-190` §3 «сторож отложенного
  решения жив … продолжает судить дефолт», `PROJECT-STATE.md:2681` «остаётся осмысленным»,
  `ROADMAP.md:99` «сторож `verify_M-70` task #7 осмыслен» — дефолт репозитория есть
  параметр границы C, и сторож — единственное, что краснеет на его несанкционированном
  флипе. Автор сам подтверждает это в `§0closeout-bis`: развилка (а) «дефолт репозитория
  поднять до семи» вынесена founder'у как граница C. Пока эта развилка открыта, снимать
  единственный сторож флипа — преждевременно; не нарушение границы, а потеря наблюдателя
  за ней (Н-2). Reviewer в `R-190` §6 предложил ДВА выхода — архив ИЛИ самоотключающийся
  `task #6`; второй сохраняет сторож и не упирается в Б-1.

## 4. (в) Связность и висячие ссылки — здесь блокер

`scripts/verify_M-70.sh` после переезда не существует. Все ссылки вне архива — 26 файлов /
94 строки — разделены по ЗОНЕ и по РОЛИ, как требует норма, а не по «исполняется / не
исполняется»:

| класс | где | суждение |
|---|---|---|
| История/вердикты (`research/critiques/C-193…C-228`, `research/reviews/R-170…R-190`, `docs/workflow/session-handover-*`, `docs/plans/*`, `TECH-DEBT.md:917`) | датированные снимки | допустимо; `verify_design_claims` выводит архивный класс из проверок ссылок (`[V-АРХИВ]`) |
| `TECH-DEBT.md:100,119,120` | тела закрытых карточек, «закрыт merge'ем M-70» | допустимо, история |
| **`crates/gateway/src/lib.rs:111`** | doc-comment: «verify_M-70 честно краснеет task #6b как индикатор того, что правка пина ещё не пришла» | **ЧУЖАЯ ЗОНА (engine-dev), ЖИВОЙ указатель** — объявляет гейт действующим индикатором |
| **`docker-compose.yml:138`** | «Записью env (не комментарием) — verify_M-70 task #7 честно проверяет каждое значение в строке» | **ЧУЖАЯ ЗОНА (engine-dev, `scope-guard.md`: корневой compose), ЖИВОЙ указатель** — объявляет гейт машинной проверкой прод-конфига; тот же класс, что `verify_M-48` в `gateway-checkpoint-cron.sh:39,78`, названный нормой ХУДШИМ |
| `PROJECT-STATE.md:2681`, `docs/ROADMAP.md:99` (колонка «Состояние») | «сторож … продолжает судить дефолт и остаётся осмысленным» | reviewer-owned; после переезда — ложь в летописи и в роадмапе, владелец не уведомлён артефактом (Н-1) |

### Б-1 (БЛОКЕР) — нарушено требование 2 Close-out'а, и оно объявлено проверенным не тем критерием

`04-workflow.md:138-151`: «Гейт, на который ссылается ЧУЖАЯ зона, НЕ выносится, пока ссылку
не снял её владелец. … `M-35 M-45 M-48 M-49` — ссылки есть в `crates/*/src/**` … и
`deploy/**` … Коммит не собирается одной ролью ⇒ отложены». `R-098` §4.3 (`:330-334`)
прямо говорит, что речь о КОММЕНТАРИЯХ: «это не исполняемые зависимости (комментарии) …
но каждая ссылка ОБЪЯВЛЯЕТ гейт названным сторожем инварианта, и после переезда инвариант
остаётся без названного сторожа».

Воспроизведение на дереве слияния:

```text
$ git grep -n 'verify_M-70' -- 'crates/*/src/**' docker-compose.yml 'deploy/**'
crates/gateway/src/lib.rs:111:///    (`scope-guard.md`, `*/tests/**` sacred); verify_M-70 честно краснеет task #6b как
docker-compose.yml:138:      # `GATEWAY_BANDS` на проде. Записью env (не комментарием) — verify_M-70 task #7 честно

$ for f in docs/archive/verify_M-*.sh; do n=$(basename $f .sh); \
    git grep -l "$n" -- 'crates/*/src/**' docker-compose.yml 'deploy/**'; done | sort -u
crates/gateway/src/lib.rs        # ← только M-70
docker-compose.yml               # ← только M-70; остальные 30 архивных гейтов — 0 ссылок

$ for n in verify_M-35 verify_M-45 verify_M-48 verify_M-49; do printf '%s: ' $n; \
    git grep -l "$n" -- 'crates/*/src/**' docker-compose.yml 'deploy/**' | tr '\n' ' '; echo; done
verify_M-35: crates/recorder/src/main.rs crates/venue-binance/src/lib.rs
verify_M-45: crates/venue-binance-futures/src/lib.rs crates/venue-binance/src/lib.rs
verify_M-49: crates/journal/src/segments.rs
verify_M-48: deploy/bin/gateway-checkpoint-cron.sh deploy/bin/journal-retention-cron.sh deploy/cron.d/journal-retention
$ ls scripts/verify_M-35.sh scripts/verify_M-45.sh scripts/verify_M-48.sh scripts/verify_M-49.sh   # все четыре на месте
$ sed -n 805p crates/venue-binance/src/lib.rs; sed -n 2508p crates/journal/src/segments.rs
/// … T5/T5b-канарейки `verify_M-45.sh` проверяют      # комментарий — и он держит гейт в scripts/
/// стоит канарейка `verify_M-49.sh` и оракулы …        # комментарий — и он держит гейт в scripts/
```

Практика нормы однозначна: ссылка-комментарий в чужой зоне ДЕРЖИТ гейт. У `M-70` таких две.
Тело коммита проверяет другое — «ни одного ИСПОЛНЕНИЯ» — и этим критерием требование 2
пройти нельзя: у четырёх отложенных гейтов исполнений тоже ноль. `§0closeout` спеки ни одну
из двух ссылок не называет (`grep -nE 'docker-compose|lib\.rs:111|чуж' docs/archive/M-70-…md`
на строках новой секции — пусто). Мандат перепроверки называет `docker-compose.yml:138`, но
мандат — переписка, не артефакт; `lib.rs:111` не назван и там.

**Что сделать (два законных пути, выбор — architect'а):**

- **(A) путь нормы.** Handoff founder'у → `engine-dev` снимает обе ссылки: `lib.rs:111`
  переписать без указания на гейт (индикатор `task #6b` — согласованность
  `EXPECTED_SCHEMA_VERSION` с константой — остаётся в sacred-тесте, его и назвать);
  `docker-compose.yml:136-146` — убрать «verify_M-70 task #7 честно проверяет», оставив
  утверждение о `П-014` п.4 и об операторской строке. После merge'а их коммита — повторить
  переезд новым коммитом, тело которого проверяет требование 2 ТЕМ критерием, что в норме
  (`git grep verify_M-70 -- 'crates/*/src/**' docker-compose.yml 'deploy/**'` → пусто), и
  `§0closeout` называет обе снятые ссылки с их коммитом.
- **(B) путь `R-190` §6, вариант 2.** Гейт остаётся в `scripts/`, `task #6` становится
  самоотключающимся (`V_BASE == V_HEAD` при `merge-base == HEAD` ⇒ `SKIP` с явной причиной,
  не `PASS`); спека — в архив или нет по решению architect'а (роадмап-барьер требует правки
  роадмапа только при переезде спеки). Это харнесс-правка в зоне architect'а, идёт
  харнесс-треком; она же закрывает Н-2 до решения founder'а по развилке.

### Б-2 (ФАКТ) — бэкап `.env` НЕ содержит «значение ДО включения»; он не содержит ключа вовсе

`docs/archive/M-70-depth-bands-enablement.md:82-84`: «Единственная его резервная копия
(`.env.bak-…`, 84 байта) содержит ЗНАЧЕНИЕ ДО включения». Замер:

```text
$ ssh … 'cd /root/hft-platform && grep -E "^GATEWAY_BANDS=" .env.bak-1789933437 || echo NONE; sed -E "s/=.*/=<…>/" .env.bak-1789933437'
NONE
GATEWAY_JWT_SECRET=<…>
```

Вывод абзаца («восстановление из бэкапа ТИХО откатывает к одной узкой полосе») ВЕРЕН — но
по другому механизму: при отсутствии ключа Compose берёт дефолт `${GATEWAY_BANDS:-0.001}`
(`docker-compose.yml:147`). Это даже сильнее, чем написано: откат произойдёт при ЛЮБОМ
восстановлении, где `.env` собран без этого ключа, и сравнивать значения будет не с чем.
Поправить одну фразу: «не содержит ключа `GATEWAY_BANDS` вовсе ⇒ Compose подставит
`0.001`». Тело коммита («84 Б со СТАРЫМ значением» — в мандате) — та же ошибка.

### Б-3 (ЦИТАТА) — «по передаче `R-190` §D» ссылается на секцию, которой нет

`grep -inc handoff research/reviews/R-190-…md` → 0; `grep -n '§D'` → 0; файл 182 строки,
последняя секция — §7. Основание close-out'а реально есть, и оно другое: `R-190` §6 `NOTE`
для architect'а (`:167-172`, «архивировать гейт вместе с close-out'ом M-70, либо сделать
task #6 самоотключающимся») и `PROJECT-STATE.md:2608` «(сам milestone закрывает
architect)». Правило цитаты (`CLAUDE.md` п.3): «документ X говорит Y» требует открытия X.
При переработке по Б-1 тело нового коммита называет реальное основание.

### Б-4 (ТОЧНОСТЬ) — «26 упоминаний» = 26 файлов, 94 строки

`git grep -l … | wc -l` → 26; `git grep -c … | awk '{s+=$2}'` → 94. Не влияет на вывод,
влияет на доверие к числам в теле коммита, которое дальше цитируют.

### Н-1 — два reviewer-owned утверждения становятся ложью в момент merge'а, владелец не уведомлён артефактом

`PROJECT-STATE.md:2681` и `docs/ROADMAP.md:99` (колонка «Состояние» 9-C) говорят «сторож
`verify_M-70` task #7 … остаётся осмысленным / осмыслен». После переезда гейта в архив
обе фразы ложны. Зона — reviewer (верно, что автор их не тронул). Но норма Close-out'а
разводит роли и предполагает ПОРЯДОК: reviewer называет закрытым → architect переезжает.
Здесь reviewer тем же днём записал противоположное суждение о сторожe, а architect закрыл
сторож без артефакта, адресованного reviewer'у. Минимум — Handoff §D reviewer'у с двумя
`файл:строка`; при пути (B) из Б-1 обе фразы остаются истинными и правка не нужна.

### Н-2 — сторож снят ДО решения founder'а по развилке, которую сам же ему вынес

См. §3 «Граница C». `§0closeout-bis` даёт founder'у три варианта; вариант (а) — флип
дефолта репозитория — есть ровно то, на чём `task #7` краснеет. Снимать единственный
наблюдатель параметра границы C, пока решение о нём открыто, — не нарушение границы, а
потеря наблюдения за ней; класс тот же, что автор сам называет в `§0closeout-bis` («никто
не наблюдает ОТСУТСТВИЕ»). Путь (B) из Б-1 закрывает и это.

## 5. Что верно и не нуждается в правке

Маршрут (свой PR architect'а) — верен. Зона — соблюдена, «Состояние» не тронута. Оба
токена переезда — на месте. Все `файл:строка` в `§0closeout` — указывают на названное.
Арифметика `task #6` — подтверждена ИСПОЛНЕНИЕМ шага на дереве. Универсум номеров —
не сужается. Прод-факты (`132 Б`, `84 Б`, `.gitignore:4`, `docker inspect`) — совпали,
кроме одной фразы (Б-2). Находка `§0closeout-bis` — реальная, полезная, классифицирована
верно и корректно вынесена founder'у как граница C; рекомендация (б) обоснована.

## 6. Что я НЕ проверял — названо, чтобы не сошло за проверенное

1. Полный прогон `docs/archive/verify_M-70.sh` (cargo-тесты `DB-I-*`, рендер compose) — не
   гонял; исполнен ТОЛЬКО извлечённый шаг `task #6` (`:270-292`) на дереве ветки.
2. Прод-гейтвей на предмет полос НЕ опрашивал (инцидент 2026-09-20, 100 % CPU при рескане);
   ограничился `ls`, `git check-ignore`, `grep` по `.env*`, `docker inspect`.
3. Содержимое `docs/plans/depth-liveness-window-measurement-2026-09-20.md` сверх наличия
   строки «1 мин 45 с» (4 совпадения) — не читал.
4. `R-105` целиком — только §Б-1 (таблица ролей переезда `M-04`); `R-098` — только §4.3.
5. FA `docs/fa/viz-backend.md` — не открывал: диапазон `crates/**` не трогает,
   `check_review_fa` → `SKIP`; живой инвариант здесь не называю, чтобы не цитировать
   непрочитанное.

## 7. Done Block

```text
$ pwd; git log -1 --oneline
/tmp/hft-recheck-m70
d376af1 docs(M-70): close-out — спека и гейт в архив; гейт диапазона снят, сторож разобран [architect]
$ git grep -n 'verify_M-70' -- 'crates/*/src/**' docker-compose.yml 'deploy/**' | wc -l
2
$ EVENT_NAME=pull_request PR_BASE_SHA=d179886b263963ea5c446b3d1b57f74c316346a5 \
  bash scripts/check_roadmap_sync.sh | tail -1;  echo exit=$?
VERDICT: PASS
exit=0
$ bash scripts/verify_design_claims.sh --merge-preview origin/main | tail -1; echo exit=$?
VERDICT: PASS (0 нарушений)
exit=0
$ bash scripts/next_artifact_id.sh R
R-191
$ bash scripts/check_branch_health.sh | tail -1
VERDICT: PASS — наблюдение состоялось (NOTE не блокируют: это наблюдатель, не барьер)
```
