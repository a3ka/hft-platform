# SESSION-HANDOFF — АРХИВ закрытых разделов (снято 2026-08-17)

> **Architect-owned, append-only архив.** Выделен из `docs/SESSION-HANDOFF.md` 2026-08-17:
> живой файл вырос до 67 256 B при живом индексе (§0bis + §0) в 28 233 B — 42 % файла
> (`docs/workflow/reading-map.md` §5, третья из трёх правок вычистки; первые две —
> `TECH-DEBT.md` и `PROJECT-STATE.md` — сделаны reviewer'ом 2026-08-16). Разделы перенесены
> **ДОСЛОВНО**, в исходном порядке, без единой правки текста. Живой файл —
> `docs/SESSION-HANDOFF.md`. Обращение к обоим — грепом по предмету
> (`docs/workflow/reading-map.md` ярус C).

> **Критерий переноса — fail-closed, по СОБСТВЕННОМУ статусу раздела, а НЕ по дате.**
> Раздел переезжает, только если каждый его пункт закрыт ИЛИ имеет живой носитель, и это
> предъявлено командой (таблица ниже). Раздел, где хоть одно действие не закрыто или хоть
> одно утверждение действует, остаётся в живом файле ЦЕЛИКОМ. Проверка была поимённой:
> из одиннадцати разделов §1–§9 переехали **пять**, остались **шесть** — при том, что все
> одиннадцать датированы 2026-07-31 / 2026-08-03 и наивный фильтр по дате унёс бы их все.

## Почему переехал каждый из пяти — проверено командой, а не датой

| раздел | что проверено | команда и результат |
|---|---|---|
| **§1. Точное состояние** | ни одной живой строки: `origin/main` назван `f5907e2` (сейчас `2c56a34`), все девять перечисленных веток удалены из `origin`, состояние журнала/латентности снято 2026-08-03 | `git ls-remote --heads origin <ветка>` → пусто для `docs/scale-consolidation`, `feat/M-56-snapshot-noclone`, `feat/M-54-connect-cost`, `feat/scale-architecture`, `feat/M-49-rev4`, `docs/design-v2`, `docs/plan-c040-fixed`, `docs/critique-C-040`, `docs/design-evolution`. Устаревание §1 зафиксировано ещё reviewer'ом: `docs/archive/PROJECT-STATE-closed-2026-08-16.md:2347` |
| **§3. M-49 — ЗАКРЫТ** | заголовок объявляет закрытие без оговорок; единственный незакрытый хвост раздела — `TD-052` и `TD-053` (в т.ч. пометка «⚠️ требует замера нормальным инструментом») — закрыт обеими карточками | `grep -c '\*\*TD-052\*\*\|\*\*TD-053\*\*' TECH-DEBT.md` → 0 в живом файле; обе карточки лежат в `docs/archive/TECH-DEBT-closed-2026-08-16.md` со статусом CLOSED 2026-08-02 / 2026-08-01. Уроки раздела приземлены в `.claude/rules/{gates,testing,branch-hygiene}.md` |
| **§4. Закрыто в сессии 2026-08-03** | последняя незакрытая строка таблицы — `M-56` со статусом «🚧 ждёт reviewer» — смержена (`c714d0f`); остальные семь строк уже несли ✅ | `docs/archive/PROJECT-STATE-closed-2026-08-16.md:2347` называет merge обеих веток; пять уроков раздела сверены с первоисточниками reviewer'ом (`research/reviews/R-030` §«Проверено отдельно») и приземлены в правила |
| **§4bis. Закрыто ранее (2026-07-31)** | таблица целиком ✅; единственное ДЕЙСТВУЮЩЕЕ утверждение раздела — находка `E-001` (инвертированные стороны трейдов HL) — имеет собственный живой носитель с тем же решением | `grep -n 'E-001' docs/data-epochs.md` → `:13` заголовок эпохи, `:51` решение architect'а. Три правила раздела — в `.claude/rules/gates.md` §4 и `.claude/rules/testing.md` |
| **§8. Долг и инварианты** | обе половины раздела имеют более полные живые носители: счётчик «OPEN 33 / CLOSED 18» протух, а сверка «заявлено / в оракулах» перекрыта `DESIGN` §22 — там она ПОДРОБНЕЕ (`JR-I` 12/7 против 7/2 здесь, `VN-I` с разбором маркировки) | `sed -n '912,930p' docs/DESIGN.md` — таблица семейств целиком. Из двенадцати названных `TD-*` шесть ещё открыты (`TD-006`, `TD-020`, `TD-016`, `TD-021`, `TD-002`, `TD-015`), но каждый — своей карточкой в живом `TECH-DEBT.md`: раздел был указателем, не носителем |

## Что НЕ переехало и почему (шесть разделов остались в живом файле)

Перечислено здесь же, чтобы у критерия была предъявляемая обратная сторона: архив,
не называющий, что он ОТКАЗАЛСЯ забрать, недоказуем.

| раздел | что держит его живым |
|---|---|
| **§2. Решения founder'а (BINDING)** | восемь действующих решений founder'а без отметки о снятии; заголовок прямо говорит «не переобсуждать» |
| **§5bis. Следующие шаги — ПРИОРИТЕТ** | пункты 3–7 не сделаны (многопоточность, RFC разделяемой проекции, сжатие на проводе, cursor-resume, нагрузочный стенд); на п.3 живьём ссылается `PROJECT-STATE.md:40` и `research/reviews/R-044` |
| **§5. Следующие шаги (Блок 1/Блок 2)** | 5.2 несёт дыру `Д1` (смена семантики при неизменной форме) со словами «механизма защиты пока НЕТ — проектирование за architect'ом»; в `TECH-DEBT.md` карточки на `Д1` нет — живой носитель только `docs/plans/contracts-current-state.md` §Д1 |
| **§6. Требует решения founder'а** | три из четырёх вопросов (Telegram-токен, бамп `SCHEMA_VERSION` ради `E-001`, юрисдикция) НЕ представлены в `docs/PENDING-SIGNATURE.md` — `grep -c` даёт 0; этот раздел и есть их единственная очередь |
| **§7. Инфраструктура и практики** | действующая топология прода; на раздел поимённо ссылается КОД: `crates/ops/src/bin/ops-watchdog.rs:18` («значения по умолчанию соответствуют прод-топологии VPS, `docs/SESSION-HANDOFF.md §7`») |
| **§9. Гигиена репозитория** | `TD-110` в живом `TECH-DEBT.md` **OPEN** и называет предметом правки ровно этот раздел: «явное списание, где §9 называет судьбу самих ФАЙЛОВ… **Зона — architect** (`docs/SESSION-HANDOFF.md` + merge)» |

---

## §1. Точное состояние

| Что | Значение (2026-08-03T19:5xZ) |
|---|---|
| `origin/main` | `f5907e2` (после merge M-54 + R-029) |
| Прод (VPS `167.233.192.131`) | M-54 задеплоен (`aeb409b`), оба контейнера healthy, `restarts=0` |
| Журнал | `next_seq` **159 121 674** · сегмент **167** · свободно **77 GB** · `writable: true` |
| Cron | компакция 03:50 ✅ · **чекпоинт `*/15` мин** (было 04:00 — изменено `14017a4`, TD-085) · ретеншен 04:07 ✅ |
| Латентность подключения | **≈300–770 ms** при свободном сервере (было 28–142 s до этой сессии) |

**Ветки НЕ в `main` на конец сессии:**

| Ветка | Что там | Что делать |
|---|---|---|
| `docs/scale-consolidation` | сведённые архитектурные решения: `DESIGN.md` §16.1-16.3, роадмап Ф2, оба `plans/scale-*.md`, критики `C-058*`, П-005/007/009/010, правила процесса | **→ reviewer → merge**. Это носитель решений сессии |
| `feat/M-56-snapshot-noclone` | `bcb47c8`, `51fd7f3` — снапшот без клонирования (закрывает регрессию +404 ms от M-54) | **→ reviewer → merge → §8 + замер на проде** |
| `feat/M-54-connect-cost` | уже сведена в `docs/scale-consolidation` | после merge консолидации — удалить |
| `feat/scale-architecture` | уже сведена в `docs/scale-consolidation` | после merge консолидации — удалить |

**Ветки:**

| Ветка | Состояние |
|---|---|
| `feat/M-49-rev4` | **АКТИВНАЯ** — engine-dev делает задачи 4a/4b/4c |
| `docs/design-v2`, `docs/plan-c040-fixed`, `docs/critique-C-040` | ✅ СВЕДЕНЫ в `main` 31.07. `DESIGN-v2.md`, `09-roadmap-v2.md`, `plans/plan-design-migration.md`, `critiques/C-040-*.md` лежат в main как артефакты. **`DESIGN-v2.md` пока КАНДИДАТ рядом с `DESIGN.md`** — сама замена делается отдельным заходом с planner+critic (см. §5.2) |

---|---|---|
| `feat/M-49-rev4` | `3af77ba` | **АКТИВНАЯ РАБОТА** — ждёт engine-dev, 3 задачи |
| `docs/design-v2` | `db2f9ab` | `DESIGN-v2.md` (787 строк) + `09-roadmap-v2.md`; **подпись founder'а на замену ДАНА** |
| `docs/plan-c040-fixed` | `cd2a161` | план миграции + ревизии контрактов (в `docs/plans/`) |
| `docs/critique-C-040` | `31d72ee` | вердикт критика на план |

---

## §3. M-49 — ЗАКРЫТ 2026-07-31 (шесть кругов)

**Merge `091ece1` в `main`, §8 GREEN проверен фактами.** TD-049/TD-050/TD-051 → CLOSED.
Вердикты: `research/reviews/R-001` (REJECT), `R-002` (REJECT), `R-003` (**APPROVED**).

**Прод после merge (замер 2026-07-31T14:26Z):** recorder перезапустился на ужесточённом коде
поверх живого каталога и **поднялся** — Up 13 мин, healthy; heartbeat отставание 4.4 с;
`next_seq` 137 579 079 (растёт без разрыва); сегменты 140/141 закрыты по ~1 GiB, 142 активный;
свободно 86.7 GB. Это был главный риск merge'а — ложное срабатывание ужесточения не дало бы
recorder'у стартовать.

**Итоговый контракт JR-I-8.** `open_with` не начинает запись с `seq` ≤ максимального уже
присутствующего. Нечитаемый хвост ⇒ `Err` с диагностикой (файл + причина + действие), не старт
с `meta_seq`. Страж держится и когда окно скана (4 MiB) не достаёт до начала файла. Операторский
выход — файловая декларация `journal.force-next-seq.json`, одноразовая, с аудит-следом.
Пол защиты — три состояния: `Known` (учитывает читаемый ПРЕФИКС повреждённого сегмента,
вычисляется потоково при ограниченной памяти), `NoSegments` (пол 0 законен), `Unknown`
(**отказ** — незнание не есть разрешение). `readable_floor` зовётся ТОЛЬКО в ветке нечитаемого
хвоста при наличии декларации — горячий путь старта не тронут.

**Уроки, закреплённые правилами (все в `main`):**
- вердикт reviewer'а — артефакт в `research/reviews/R-NNN.md`, коммитится (иначе пропадает);
- запретный список в спеке правки существующего кода;
- мутационный контроль — ДВА вопроса, и вопрос 2 нельзя закрывать первым найденным объяснением;
- **форма прода снимается ЗАМЕРОМ по ssh** и называется в спеке (3 из 6 кругов — один дефект:
  фикстура 8 KiB против 1 GiB; эталон, исключающий читаемый префикс; `.zst`-жертва там, где
  активный сегмент прода СЫРОЙ);
- RN-19: приземление работы субагента проверяет оркестратор фактами git (отчёт = гипотеза,
  git = факт); при повторном провале пушит сам — `git push` не меняет авторство.

**Новый долг от R-003 (не блокеры, приземлены осознанно):**
- **TD-052** — пол ограничен по ПАМЯТИ, но не по ВРЕМЕНИ: в состоянии `Unknown` прочитывается
  и декодируется весь каталог.
- **TD-053** — `READABLE_SCAN_MAX_CARRY = 64 KiB` против санити-капа ридера 64 **MiB**: событие
  крупнее 64 KiB скан пола трактует как порчу ⇒ ресинк на 1 байт ⇒ **пол может быть занижен**.
  ⚠️ **Требует замера нормальным инструментом.** Грубый скан architect'а по сырому сегменту дал
  ложные срабатывания (нашёл «фрейм 62 MB») — вывод по нему делать НЕЛЬЗЯ. Замерить максимальный
  реальный размер события через сам крейт (`read_all`/`stream` на копии сегмента) и, если события
  > 64 KiB достижимы (кандидат — L2-снапшоты книги, особенно после M-45), поднять severity:
  занижение пола открывает тот же seq-reuse, который M-49 закрывал.

## §4. Закрыто в сессии 2026-08-03 (масштаб read-path)

| Что | Итог |
|---|---|
| **TD-083** (P0) | ✅ merge `af2ee7b`. Любое WS-подключение заклинивало `gateway-serve` намертво (CPU 100 %, accept-loop мёртв, `docker ps` показывал `healthy`). Причины: `frames_since` читал журнал с головы на КАЖДОМ тике + блокирующее чтение в однопоточном рантайме |
| **TD-085** — чекпоинт `*/15` | ✅ `14017a4`. Backlog при подключении 3.3M → ≈50k событий |
| **M-54** — хвост читается один раз | ✅ merge `aeb409b`. Догон стал **×3.9 дешевле** |
| **M-56** — снапшот без клонирования | 🚧 `bcb47c8`/`51fd7f3`, ждёт reviewer. Закрывает регрессию +404 ms, внесённую M-54 |
| **CT-RFC-09** — контракт WS-сессии | ✅ merge `76e57f9`. Форма зафиксирована, против неё пишется фронт |
| **Арх-аудит + CT-RFC-08** | ✅ merge `7b8756b` |
| **Фикс барьера артефактов** | ✅ `24f9201`. Ложно валил `main` на легитимном переименовании внутри влитой ветки (`git log` без `--full-history` не видит side-ветку) |
| **Решение по 10k сессий** | ✅ см. §00 |

### Уроки сессии, которые стоили дороже всего (не повторять)

1. **Замер без записанных условий недействителен.** Один и тот же код давал **234 ms и
   12 000 ms** в зависимости от того, освободился ли сервер. На грязных данных я дважды сделал
   противоположные выводы — сперва «backlog решён», затем «backlog не влияет». Оба неверны.
   Протокол — `DESIGN.md` §16.3, инструмент — `scripts/measure_M-54_connect.sh`.
2. **Предсказание регистрируется ДО изменения.** Именно так reviewer нашёл дефект M-54
   (`R-029` §C): предсказал 476 ms, получил 768 ms, и разбор вскрыл выросшую константу. Мой
   замер «улучшение в 4 раза» её пропустил.
3. **Оракул, зелёный с первого запуска, ничего не доказывает.** O-1 в M-56 сперва ПРОШЁЛ на
   коде с дефектом (абсолютный порог зависел от ширины фикстуры). Помогло сравнительное
   свойство: две проекции, отличающиеся только глубиной книги ×8, при одном окне обязаны дать
   одинаковую работу. До фикса — ×6.01, после — **×1.00**.
4. **Успешный коммит ≠ закоммичено то, что думал.** `git mv` занял индекс, `commit -m` без
   путей забрал ровно индекс — коммит с описанием семи правок нёс **ноль вставок**. Правило:
   после коммита `git show --numstat --format='' HEAD`.
5. **Зелёный прогон не того кода выглядит как зелёный прогон того кода.** Tester прогнал M-54
   в общем чекауте на посторонней ветке, где оракула нет вообще. Правило: ШАГ 0 в профиле
   tester'а — `pwd` + `git log -1` + `ls` предмета.

## §4bis. Закрыто ранее (2026-07-31)

| Что | Итог |
|---|---|
| **M-40** (R2/R2b) | ✅ merge `984f6f9`; §8 прод-замером: dry-run назвал 122 упоминания `.jrnl.zst` (до фикса план был ПУСТ) |
| **M-41** (R4) | ✅ merge `51fc3cc`, в проде; 40 оракулов, 4 дефекта парсера HL |
| Гигиена долга | ✅ 52→51 (снят дубль TD-043), OPEN 33 / CLOSED 18, статусы M-38b/M-47/M-48 |
| `docs/data-epochs.md` | ✅ реестр эпох, запись **E-001** |
| Замер объёмов | ✅ домержен в main (`docs/06` §2/§5.1) |
| Процессные правила | ✅ три штуки (см. ниже) |

**E-001 — главная находка.** Парсер HL маппил `"A" => Buy`, `"B" => Sell`; официальная нотация
(проверено по первоисточнику): «B = Bid = Buy, A = Ask = Short». **Все трейды Hyperliquid с
`seq` ниже ~123 205 544 записаны с перевёрнутым направлением** — непригодны для CVD, delta,
footprint, анализа агрессии. Автокоррекция при чтении НЕ вводится сознательно (риск двойной
инверсии; `provenance` в контейнере — константа, эпохи машинно не различить).

**Три правила, добавленные в `main`:**
1. `gates.md` §4 — **вердикт reviewer'а есть АРТЕФАКТ**: пишется в `research/reviews/R-NNN.md`
   и коммитится ДО завершения. Инцидент: два вердикта вернулись словом «REJECTED», ~300k
   токенов работы гейта пропали.
2. `testing.md` — **запретный список**: спека правки существующего кода обязана содержать
   таблицу «Запрещено / Почему».
3. `testing.md` — **мутационный контроль отвечает на ДВА вопроса**: (а) падает ли оракул при
   откате фикса; (б) какой смежный инвариант пришлось ослабить, чтобы пройти оракул.

**Практика, себя оправдавшая:** канал возврата от субагентов НЕ доставляет результаты.
Проверять их работу через `git` и прогоны, а не по отчётам; требовать артефакт в репозитории.

---

## §8. Долг и инварианты

После гигиены: **OPEN 33 / CLOSED 18**. Ключевое: **TD-049** (CRITICAL, закрывается M-49) ·
TD-050/051 (прицепом) · TD-006/TD-020 (=R1) · TD-016/TD-021 (память и метрика) ·
TD-029/TD-030 · **TD-007** (`DET-I-1` реализован ЧАСТИЧНО — при том что это sacred P0-ворота) ·
TD-002 (скомпрометированный ключ) · TD-015 (эпохи ledger'а).

**Сверка «заявлено / в оракулах»** (замер, R9): `SM-I` 10/10 · `VB-I` 11/9 · `OPS-I` 10/8 ·
`ST-I` 10/8 · `CT-I` 6/5 · `JR-I` 7/2 · `RK-I` 10/3 · **`INTG-I` 7/0** · **`BK-I` 8/0** ·
**`VN-I` 9/0** · `GW-I` 0/12 (обратный дрейф: оракулы есть, семейство в доках не заведено).

---

