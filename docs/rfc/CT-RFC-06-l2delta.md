# CT-RFC-06 — `MdPayload::L2Delta`: инкрементальные обновления стакана в T1

**STATUS: PROPOSED**
**Дата:** 2026-08-02
**Автор:** architect
**Гейты впереди:** critic → risk-critic → founder (contract-RFC, `docs/05-contract-layer.md` §4)

---

## 0. Мотивация и проверка посылки — что этот RFC решает НА САМОМ ДЕЛЕ

### 0.1 Продуктовая мотивация (`docs/DESIGN.md` §17)

Первый wedge продукта — AI Tutor + Scalper Workspace на order-flow ядре. Несущие виджеты
(heatmap, footprint, DOM-replay, spoofing radar, liquidity tracker —
`docs/07-cockpit-backend-roadmap.md` §14) определены
**внутри секунды**: absorption — то, что происходит между двумя соседними 100 мс диффами.
Секундный `L2Snapshot` — финальное состояние книги на конец секунды; всё, что появилось и
исчезло внутри секунды (классический spoofing-паттерн: крупная заявка жила 300 мс), в снапшот
не попадает НИКОГДА — это не вопрос разрешения, а отсутствие данных. На снапшот-only журнале
недостижимы оба Workspace первого wedge (`docs/DESIGN.md` §17, строка 564+). Forward-only:
не записанную сегодня суб-секундную историю не восстановить никогда.

### 0.2 Проверка посылки мандата — вариант УЖЕ существует

Мандат этой задачи сформулирован как «contract-RFC на **новый** вариант T1-формы `MdPayload`».
**Посылка разошлась с кодом** (проверено командами, не по памяти; тот же вывод — в разведке
`research/measurements/m-45-l2delta-impact.md` §0, приземлённой в дерево этого RFC
merge-коммитом ради достижимости пруфа из `main` — R-019 F1):

```
$ grep -n "L2Delta {" crates/contracts/src/lib.rs
293:    L2Delta {
$ git cat-file -e f635bd2^{commit} && git log -1 --format=%s f635bd2
merge(M-18): CT-RFC-04 L2Delta — сырые book-дельты (BTC-only spot+perp) [reviewer APPROVED]
$ git cat-file -e 7a237f7^{commit} && git log -1 --format=%s 7a237f7
merge(M-18): TD-031 schema-epoch isolation — reviewer APPROVED (...)
```

`MdPayload::L2Delta` (postcard-дискриминант 6) введён **CT-RFC-04** и реализован
**M-18** (STATUS: ✅ CLOSED 2026-07-21, `milestones/M-18-l2delta-capture.md`): venue-адаптеры
эмитят его в прод для BTC (allow-list `L2DELTA_CAPTURE_SYMBOLS = &["BTCUSDT"]` —
`crates/venue-binance/src/lib.rs:485`, `crates/venue-binance-futures/src/lib.rs:460`),
recorder пишет, `book`/`sim`/`gateway`/`journal` читают. `docs/DESIGN.md` §17 п.1 и
`milestones/M-45-persist-l2delta.md` (founder-подпись 2026-07-27) прямо фиксируют:
«Механизм есть (M-18), **CT-RFC не нужен**» — расширение allow-list не меняет T1-форму.

### 0.3 Что тогда закрывает CT-RFC-06 (предмет документа)

Написать «RFC на введение L2Delta» значило бы произвести ДЕВЯТЫЙ случай лжи документа о коде
(счёт за сутки — `docs/ORCHESTRATION-STATE.md`, C-044). Вместо этого CT-RFC-06 закрывает
T1-смежные вопросы, которые **действительно остаются открытыми** на пути к дельта-первичному
режиму (`DESIGN.md` §17 п.2 «инверсия ролей») и которые никакой другой документ не решил:

1. **Ратификация формы** варианта 6 — достаточна ли она для дельта-первичного режима,
   поле за полем (§1), и класс изменения для M-45 и для режимной инверсии (§2).
2. **Эпохи**: чем машинно различимы снапшот-первичные и дельта-первичные сегменты —
   чтобы не повторить E-001, где эпохи неразличимы на 123 млн событий (§3).
3. **Восстановимость**: инвариант якорного снапшота при ретеншене/компакции (§4).
4. **Детерминизм** `DET-I-1` на смешанном журнале — по существу, не декларативно (§5).
5. **Размер события / TD-053** — карта рисков ПОСЛЕ M-50 (§6), **объёмы** по замерам (§7),
   **план приземления** с полной (не заниженной) картой exhaustive-`match` (§8),
   **решения founder'а** (§9).

Статус PROPOSED: документ идёт на critic → risk-critic (`contracts`-тематика = RISK-BLOCK,
`.claude/rules/gates.md` §5) → founder. Реализация — отдельные milestone'ы после вердиктов.

## 1. Форма `L2Delta` — поля и обоснование каждого

Форма (T1, `crates/contracts/src/lib.rs:293-300`, введена CT-RFC-04) — **ратифицируется
без изменений**. Каждое поле, и почему без него нельзя / почему лишних нет:

```rust
L2Delta {
    bids: Vec<Level>,
    asks: Vec<Level>,
    first_update_id: u64,
    final_update_id: u64,
    prev_final_update_id: Option<u64>,
    ts_exch_ms: i64,
}
```

| Поле | Обоснование |
|---|---|
| `bids` / `asks: Vec<Level>` | ТОЛЬКО изменившиеся уровни, раздельно по сторонам. Разделение сторон **устраняет** потребность в per-level поле `side` (экономия на каждом уровне каждого события — самый горячий байт-путь журнала, §7). `Level {price, size}` fixed-point ×1e8 — та же дисциплина, что весь `MdPayload`. |
| — признак удаления | **Отдельного поля НЕТ намеренно**: `size == 0` = явный remove от биржи (wire-семантика Binance `@depth`: qty="0"). Doc-comment варианта (lib.rs:281-286) закрепляет и «отсутствие»: уровень, которого в дельте нет, НЕ изменился — дельта не источник истины о неупомянутом (`.claude/rules/testing.md` чек-лист «отсутствие»). Флаг дублировал бы `size==0` и жил бы в T1 вечно. |
| `first_update_id: u64` | Binance `U` — нижняя граница update-id дельты. Вместе с `final_update_id` даёт **gap-детекцию при реплее без обращения к бирже**: для спота непрерывность = `U == prev.u + 1`. Без него пропуск дельт невидим ⇒ тихо испорченная реконструкция книги. |
| `final_update_id: u64` | Binance `u` — верхняя граница; якорение к REST-снапшоту (`lastUpdateId`) при бутстрапе реконструкции. |
| `prev_final_update_id: Option<u64>` | Futures `pu` (чейнится `pu == prev.u`); у спота такого поля в wire НЕТ ⇒ `Option`, `None` для спота. Единый вариант на оба рынка вместо двух вариантов-близнецов — меньше exhaustive-`match`-поверхности (§8). |
| `ts_exch_ms: i64` | Биржевое время события — единая ось `event_data_ts` для ВСЕХ md-вариантов (`crates/journal/src/segments.rs:2566-2580` требует его от каждого варианта); латентность/ретеншен/выравнивание реплея. |

**Чего в форме НЕТ и не будет** (каждое отклонено осознанно — лишнее T1-поле живёт вечно):
`venue`/`symbol` (несёт конверт `MdEvent`), локальный `seq`/`ts_mono_ns` (несёт `Event`),
флаг «это якорь» (якорь — это `L2Snapshot`, отдельный вариант; роль различима по типу),
уровневый кап/метаданные бакетирования (дельта персистится **без потерь**, в этом её смысл —
кап см. §6), номер эпохи (эпоха — свойство сегмента, не события; §3).

**Вывод §1:** форма достаточна и для сегодняшнего BTC-only захвата, и для дельта-первичного
режима. Новых полей НЕ требуется. Открытым остаётся только Hyperliquid (§9.3): HL шлёт
агрегированный `l2Book`-снапшот, не diff — Binance-семантика `U`/`u`/`pu` на него не ложится.

## 2. Класс изменения и `SCHEMA_VERSION`

По правилам `docs/05-contract-layer.md` §4 (не по аналогии):

- **Аддитивный вариант enum в конце = minor bump.** Это изменение **уже совершено**:
  CT-RFC-04 rev2 + TD-031 бампнули `SCHEMA_VERSION` 2→3 (машинная изоляция эпох:
  `decide_open_segment` не переиспользует сегмент чужой schema-эпохи — doc-comment
  `crates/contracts/src/lib.rs:22-23`). Текущее значение — `SCHEMA_VERSION = 4`
  (lib.rs:26; 3→4 — CT-RFC-05 `MarginInventory`).
- **M-45 (расширение allow-list)**: константы `L2DELTA_CAPTURE_SYMBOLS` в venue-крейтах —
  **не T1-изменение**. Форма, дискриминанты, wire-байты не меняются ⇒ **бамп
  `SCHEMA_VERSION` НЕ нужен**, contract-пакет по §4 (schema/CHANGELOG/фикстуры) НЕ нужен.
  Сегменты schema-4 с расширенным набором символов читаются тем же кодом байт-в-байт (CT-I-3).
- **Инверсия ролей** (`DESIGN.md` §17 п.2: якорь 10–30 с вместо 1 Гц) — тоже НЕ меняет
  бинарную форму, но меняет **семантику потока** (консюмер больше не может предполагать
  «свежий снапшот ≤1–2 с»). `SCHEMA_VERSION` защищает бинарную совместимость и для
  семантического сдвига НЕ предназначен; машинный маркер режимного сдвига — `epoch_id`
  сегмента, см. §3. Это ровно урок E-001: семантический сдвиг без изменения формы — тот
  класс, который обязан получить ЯВНЫЙ маркер эпохи, а не бамп схемы «на всякий случай».
- **Если** founder выберет отдельный вариант для HL (§9.3) — это будет НОВЫЙ аддитивный
  вариант ⇒ отдельный атомарный contract-RFC (CT-RFC-07) с полным пакетом §4 и minor bump
  4→5; карта правок для него — готова в §8.2.

## 3. Эпохи: как читатель отличает снапшот-эпоху от дельта-эпохи

**Урок E-001 (`docs/data-epochs.md`), который нельзя повторить:** семантический сдвиг без
машинного маркера. Инверсия сторон HL не меняла форму — и границу эпохи пришлось
ВОССТАНАВЛИВАТЬ постфактум по heartbeat'ам («seq ≈ 123 205 544, ±несколько тысяч событий»).
123 млн событий с машинно-неразличимой эпохой — признанное ограничение. Режимный сдвиг M-45
(какие символы несут дельты; позже — инверсия ролей якоря) — тот же класс: форма не меняется,
семантика потока меняется.

**Механизм уже существует end-to-end — проверено кодом, не по памяти:**

1. `SegmentHeader` (T1, `crates/contracts/src/lib.rs:97-110`, CT-RFC-02) несёт
   `schema_version` И `epoch_id` («стабильный ключ эпохи, по которому research фильтрует
   данные. Смешение эпох — ЯВНОЕ решение»).
2. `decide_open_segment` (`crates/journal/src/segments.rs:1950+`, TD-031/L2D-I-8): reuse
   сегмента требует совпадения **`source && provenance && epoch_id && schema_version`** —
   т.е. смена `epoch_id` конфигурации МАШИННО открывает новый сегмент; смешанный сегмент
   двух эпох невозможен по конструкции.
3. Recorder берёт `epoch_id` из env `EPOCH_ID` (`crates/recorder/src/main.rs:479-482`,
   дефолт `own-<UTC-YYYY-MM>`) — операторский рычаг уже развёрнут в прод.

**Решение (нормативное, новых T1-полей НЕ требует):**

- **Каждое изменение состава/роли L2Delta-эмиссии = новый `epoch_id`, выставляемый ДО
  рестарта с новым allow-list'ом**, плюс запись в `docs/data-epochs.md` (класс «режимный
  сдвиг», не «дефект») **ДО раскатки** — зеркальная противоположность E-001, где запись
  делалась постфактум по памяти рестартов. Конвенция значения: `own-YYYY-MM-m45-<slug>`
  (напр. `own-2026-08-m45-alist2`); деплой-чеклист milestone'а M-45 обязан включать шаг
  «выставить `EPOCH_ID`» и §8-eyes-on проверку «первое L2Delta-событие нового символа лежит
  в сегменте с новым `epoch_id`».
- Читатель различает эпохи ЧТЕНИЕМ `SegmentHeader.epoch_id` + реестром `docs/data-epochs.md`.
  Почему одного факта «в сегменте есть/нет L2Delta по символу X» недостаточно: **отсутствие
  неотличимо от «не было изменений/обрыв потока»** — это то же правило «отсутствие — не
  сигнал» из семантики самой дельты (§1). Отсутствие данных не кодирует намерение;
  намерение кодирует эпоха.
- `schema_version` для режимного сдвига НЕ бампается (§2): он защищает бинарную
  совместимость (TD-031-сценарий), а не семантику потока. Два разных механизма — два
  разных ключа; смешивать их значило бы потерять оба смысла.

**Чего механизм эпох НЕ решает — остаточные классы, названные явно (R-019 F4).**
Формулировка «зеркальная противоположность E-001» относится к ЗАМЫСЛУ, а не к покрытию:
маркер эпохи закрывает **намеренный** сдвиг (о нём знают заранее и выставляют `EPOCH_ID` до
раскатки). Открытыми остаются два класса, и ни один из них этим RFC не закрывается:

1. **Незамеченный семантический сдвиг** — собственно класс E-001. Инверсия сторон HL была
   ДЕФЕКТОМ: о нём не знали в момент возникновения, поэтому выставить маркер заранее было
   некому и нечем. Против этого класса работает не `epoch_id`, а обнаружение (recon-сверка,
   §5; sanity свежих событий на §8-eyes-on). Ни одна конвенция именования эпох не защищает
   от сдвига, которого никто не заметил.
2. **Забытый операторский шаг** — `EPOCH_ID` выставляет человек. Забыли выставить перед
   рестартом с новым allow-list'ом → `decide_open_segment` увидит совпадение всех четырёх
   ключей, ПЕРЕИСПОЛЬЗУЕТ сегмент, и две эпохи молча смешаются в одном файле. Машинного
   fail-closed на «состав эмиссии изменился, а `epoch_id` — нет» **не существует**: код
   сравнивает `epoch_id` со своим же конфигом, а не с фактическим составом потока. §8.1
   ловит это только post-hoc глазами. Закрытие класса (декларация ожидаемого состава
   эмиссии в конфиге + машинная сверка с фактом на открытии сегмента) — кандидат в отдельный
   milestone, СОЗНАТЕЛЬНО вне объёма M-45.

## 4. Восстановимость: якорный снапшот vs ретеншен/компакция

Дельта без якоря — мусор: реконструкция книги требует `L2Snapshot` (якорь) + непрерывную
цепочку дельт (`U`/`u`/`pu`, §1) от него. Вопрос: может ли якорь исчезнуть из-под зависящих
от него дельт? Проверено по коду и docs/06 §4:

- **Компакция НЕ удаляет события.** `compact_segment`/`compact_closed_segments`
  (`crates/journal/src/segments.rs:2791+, 2962+`) — zstd-сжатие ЗАКРЫТОГО сегмента целиком
  с sha256 round-trip-проверкой до удаления оригинала. Прунинга событий внутри сегмента не
  существует как операции.
- **Ретеншен — не удаление, а перенос.** `RetentionPlan.offload_and_prune`
  (`segments.rs:2155+`): сегмент СНАЧАЛА выгружается в COLD (Storage Box), потом удаляется
  горячая копия. «Журнал бессмертен» (JR-I-6/CT-I-3, docs/06 §4) — данные меняют tier, не
  существование.
- **Локальный prune — fail-closed за чекпоинтом.** `RetentionPolicy
  .checkpoint_covered_through_seq: Option<u64>` (M-38b/C-030 R1, `segments.rs:2103+`):
  дефолт `None` = никаких prune; prune разрешён только для сегментов, полностью покрытых
  чекпоинтом (`last_seq(seg) <= covered`); escape-hatch `allow_prune_without_checkpoint`
  дефолтно `false`, каждый обход — поимённо в отчёте.

**Следствие: якорь структурно не может быть потерян** — он либо в HOT/WARM, либо в COLD.
Но «не потерян» ≠ «дёшево достижим». Остаточные риски и их закрепление:

**Инвариант (предлагается к закреплению как `JR-I-10`, нумерация: JR-I-8 — M-49,
JR-I-9 — M-50/TD-053):**

> **JR-I-10 (якорная достаточность).** Для любого `seq` S, читаемого из журнала, и любого
> символа, несущего `L2Delta` в эпохе S, существует достижимый якорь: `L2Snapshot` этого
> символа с `seq ≤ S` в читаемом хранилище (HOT/WARM/COLD), либо checkpoint, покрывающий S.
> Ни одна операция ретеншена/компакции не имеет права инвалидировать это свойство.

**Условие невакуумности — «читаемое хранилище» определяется фактом, не тиром (R-019 F5).**
В формулировке выше `COLD` засчитывается читаемым **только если COLD фактически смонтирован
и чтение из него проверено**, а не потому, что сегмент помечен как выгруженный. Иначе
инвариант выполняется ВАКУУМНО: любой якорь объявляется достижимым «в COLD», которого нет.
Сегодня это ровно так и есть — Storage Box не заведён (§8.3 R1 стоит первым именно поэтому),
поэтому **на сегодня COLD читаемым НЕ считается, и JR-I-10 держится на HOT/WARM**. Оракул из
п.2 ниже этот аспект не покрывает: он проверяет `retention_plan` (какое решение принято), а
не доступность тира (можно ли фактически прочитать). Пока R1 не сделан, всякая операция,
опирающаяся на «якорь остался в COLD», обязана считаться потерей якоря; проверка
доступности тира — отдельный оракул, вводится вместе с R1.

Чем закрепляется — три слоя, два уже существуют:
1. **Уже в коде:** offload-до-prune (COLD-копия остаётся читаемой) + fail-closed
   checkpoint-gate (сегмент без покрытия не прунится локально). Это делает JR-I-10
   выполнимым сегодня без единой правки.
2. **RED-оракул (architect, milestone инверсии ролей, ДО кода):** тест в
   `crates/journal/tests/` — построить журнал «якорь в сегменте N, дельты в N+1..N+2»,
   прогнать `retention_plan` — план, отправляющий сегмент N в prune при непокрытых
   чекпоинтом дельтах N+1.., обязан не существовать; плюс тест реконструкции с
   произвольного seek: книга достигает `synced` без обращения за пределы читаемого набора.
   Анти-плацебо: оракул падает на реализации, прунящей по одному лишь возрасту.
3. **Каданс якоря — измерение, потом решение (§9.2):** латентность бутстрапа/seek при
   якоре раз в 10–30 с — до 10–30 с дельт реплея вместо ≤1 с — НЕ измерена
   (`m-45-l2delta-impact.md` §4.3). Число фиксируется только после замера; при выборе
   кадансов рассматривается опция «сегмент самодостаточен» (≥1 якорь на символ в каждом
   сегменте с его дельтами) — она сводит seek к одному сегменту, но требует связки
   эмиссии якоря с ротацией сегмента (связывание venue↔journal — цена, которую надо
   взвесить, не платить по умолчанию).

## 5. Детерминизм: `DET-I-1` на смешанном журнале

`DET-I-1/2/3` закреплены M-51 (merge `d896b98`, TD-007 CLOSED — проверено:
`git cat-file -e d896b98^{commit}` OK, `TECH-DEBT.md`). Ломает ли их смешанный журнал
(снапшоты + дельты в одном потоке)? **Нет — по конструкции, и это уже 12 дней прод-факт,
а не прогноз:**

- `DET-I-1` — свойство редьюсера («один и тот же поток событий → бит-идентичный результат»),
  не свойство однородности входа. Смешение типов событий не вводит недетерминизма, если
  редьюсер детерминирован на каждом типе.
- `book::Books::apply` (`crates/book/src/lib.rs:322-350`) обрабатывает ОБА варианта в одном
  `match`: `L2Snapshot → apply_snapshot`, `L2Delta → apply_l2delta` (с sequencing-гейтом
  `U`/`u`/`pu`; на `Gap` книга остаётся `stale` — fail-closed, не «додумать»). BTC получает
  оба типа событий в одном потоке с 2026-07-21 (M-18) — смешанный журнал уже штатный вход
  реплея.

**Что из DET-I-* фактически покрыто смешанным входом, а что нет (R-019 F6).** Утверждение
«M-51 закрепил DET-I-* поверх именно такого журнала» верно ПО СУЩЕСТВУ (digest —
тип-агностичная свёртка над фреймами), но как заявление о покрытии оно шире факта. Замер
фикстур, а не пересказ:

```
$ for f in $(grep -rln "DET-I-" crates/*/tests/); do echo "$f L2Delta=$(grep -c L2Delta $f)"; done
crates/book/tests/red_det_projection.rs         L2Delta=1   <- DET-I-2, вход СМЕШАННЫЙ ✅
crates/gateway/tests/red_checkpoint_byte_identity.rs L2Delta=4   <- M-38b, вход СМЕШАННЫЙ ✅
crates/journal/tests/red_det_replay_digest.rs   L2Delta=0   <- DET-I-1, фикстур с дельтой НЕТ
crates/journal/tests/red_det_prodscale.rs       L2Delta=0
crates/journal/tests/red_det_sources.rs         L2Delta=0
crates/journal/tests/red_det_restart.rs         L2Delta=0
```

Смешанный вход есть у **DET-I-2** (проекция) и у byte-identity чекпоинта. У оракула
**DET-I-1** (`replay_digest`) фикстур с `L2Delta` — **ноль**: все его входы снапшот-/
sys-только. Аргумент §5 от этого не рушится (свёртка не смотрит на тип payload'а), но
покрытие DET-I-1 на смешанном журнале сегодня **аргументировано, а не проверено оракулом**
— ровно класс «фикстура счастливого пути» (`.claude/rules/testing.md`). **Следствие для
M-45:** milestone обязан включить задачу architect'а «добавить `L2Delta` в фикстуры
`red_det_replay_digest.rs`» (смешанный поток снапшот+дельта, реплей ×3 бит-идентичен),
иначе расширение эмиссии уедет в прод под оракулом, который расширенного потока не видел.
- `sim` (`crates/sim/src/exchange.rs:283-288`) **намеренно игнорирует** `L2Delta`: честный
  fill-путь ведёт книгу из `L2Snapshot`+`Trade`; учёт дельты поверх снапшота был бы двойным
  счётом. Детерминизм бэктеста не затронут.
- Гэп в цепочке дельт (обрыв WS, потерянный diff) — событие ВХОДА, записанное в журнал;
  реплей воспроизводит и гэп, и `stale`-состояние книги бит-идентично. Недетерминизма нет —
  есть честно записанная неполнота.

**Реальный риск — не детерминизм, а консюмеры, предполагающие snapshot-cadence** (проверено
кодом): `recorder/src/recon_loop.rs:55-61` сверяет книги ТОЛЬКО по `L2Snapshot`
(`_ => return` на всё остальное, включая `L2Delta`). Сегодня (якорь 1 Гц) это верно; при
инверсии ролей (якорь раз в 10–30 с) recon-частота падает на порядок — окно слепоты
recon-класса (родственно RK-I-8 «recon-расхождение → halt») растёт с ~1–2 с до 10–30 с.
**Решение:** инверсия ролей НЕ раскатывается до апдейта recon-петли (сверка по
реконструированной из дельт книге либо явно принятый founder'ом компромисс «recon по
редкому якорю» — §9.2). Расширение allow-list БЕЗ инверсии ролей (M-45 как заскоуплен:
якорь остаётся 1 Гц) recon не деградирует — дельты идут ПОВЕРХ прежнего снапшот-потока.

## 6. Размер события: TD-053 и `READABLE_SCAN_MAX_CARRY`

Мандат этой задачи ссылался на TD-053 как на ОТКРЫТЫЙ долг. **Состояние изменилось —
проверено:** TD-053 ✅ **CLOSED 2026-08-01** (M-50, merge `163939a`, `TECH-DEBT.md:1113`;
`git cat-file -e 163939a^{commit}` — OK). Актуальная картина:

- `READABLE_SCAN_MAX_CARRY = 64 KiB` (`crates/journal/src/segments.rs:1568`) остался, но
  больше НЕ является порогом невидимости: по **JR-I-9** (M-50) кандидат крупнее carry, но в
  пределах `FRAME_LEN_SANITY_CAP = 64 MiB` (`segments.rs:431` — ЕДИНАЯ константа со штатным
  ридером), side-верифицируется вторым независимым источником потоково, без буферизации
  тела; успех → `seq` входит в пол. «Known с заниженным полом» запрещён контрактом.
- Числа замера (`research/measurements/td-053-event-size.md`, 4 614 851 прод-событие):
  реальный максимум `L2Delta` — 32 799 B (50.1% от 64 KiB); синтетика — пересечение порога
  на 3236 уровней/сторону (71 237 B); `L2Snapshot` упирается в bucket-cap 3000 уровней =
  66 032 B (100.8%). `L2Delta` уровневого капа **не имеет** — и не получит (§1: дельта
  персистится без потерь; кап превратил бы её в тот же лосси-снапшот).

**Меняет ли `L2Delta`-экспансия картину рисков M-50? По существу — нет, по вероятности —
да, и это покрыто:** больше символов × нет капа = чаще фреймы >64 KiB (добавляемые символы
тоньше BTC — патологический дифф вероятнее). Но после M-50 такой фрейм идёт по
верифицируемому slow-path, а не в тихий seq-reuse. Потолок риска — `FRAME_LEN_SANITY_CAP`
(64 MiB): недостижим для L2-диффа (потребовал бы ~3 млн уровней в одном 100 мс сообщении).
Остаток: RED-оракулы M-50 гоняются на прод-масштабе (testing.md TD-011) — фикстура «дельта
71 KiB» уже в классе покрытия `fs_*`-оракулов M-50; отдельного нового оракула CT-RFC-06 не
требует.

## 7. Объёмы: события/сек, байт/сутки, ретеншен

Все числа — из замеров, не из оценок (docs/06 §2 — боевой прод 2026-07-14;
`milestones/M-45-persist-l2delta.md` — живой Binance WS 2026-07-27; там, где вывод мой, —
помечено):

| Величина | Снапшот-поток (сегодня) | Дельта-поток |
|---|---|---|
| Событий/с на ликвидный символ×venue | ~1 (1 Гц) | ~7 (диффы @100 мс, ~14 уровней) |
| Байт/с на ликвидный символ | 20–31 KB/с | ≈1.5 KB/с (~0.1 KB/с средний символ) |
| Доля журнала | `L2Snapshot` = **96% байт**, 8.83 GB/сут сырых, ~1 GB/сут zstd | — |

- **Дельты компактнее, но их больше:** ~7× больше СОБЫТИЙ/с, каждое в среднем ~13–20×
  дешевле по байтам ⇒ чистый эффект по байтам — отрицательный (дешевле). Рост event-rate
  бьёт не в диск, а в event-rate-консюмеров (метрики `md_events_total{kind="l2delta"}` —
  лейбл уже существует, `recorder/src/lib.rs:77`; gateway-фильтры) — умеренно (~7×/символ).
- **M-45 (allow-list, якорь остаётся 1 Гц):** аддитивно **+0.1–0.3 GB/сут на символ**
  (сырых) — проценты от текущего ~1 GB/сут сжатого. 80-дневный runway практически не
  меняется (docs/08; порядок «после R1/Storage Box» закреплён founder-подписью M-45).
- **Инверсия ролей (якорь 10–30 с):** объём ПАДАЕТ — снапшот-поток (96% байт) сжимается в
  10–30×, дельта-поток добавляет ~5–7% от прежних снапшотных байт (мой вывод из строк
  выше: 1.5 vs 20–31 KB/с). Оценка docs/06 §5.1: итог **2.5–3× меньше сверх zstd**
  `[verify-at-impl]` — при росте fidelity. Давление на ретеншен/горизонт диска ослабевает,
  не усиливается; fidelity и экономия здесь на одной стороне.

## 8. План приземления

### 8.1 M-45 как заскоуплен (расширение allow-list) — БЕЗ contract-пакета

T1-форма не меняется (§2) ⇒ пакет docs/05 §4 не собирается. Milestone-набор (RED-first,
architect → venue-dev/engine-dev):
1. Констант-правка `L2DELTA_CAPTURE_SYMBOLS` (`venue-binance/src/lib.rs:485`,
   `venue-binance-futures/src/lib.rs:460`) — RED: тест эмиссии для нового символа падает
   до правки (анти-плацебо: и негативный — символ ВНЕ списка не эмитит).
2. Смена `EPOCH_ID` + запись эпохи в `docs/data-epochs.md` ДО раскатки (§3) — verify-гейт
   проверяет наличие записи.
3. §8-eyes-on: первое L2Delta нового символа — в сегменте с новым `epoch_id` (gates.md §8).
4. `verify_M-45.sh` включает **`cargo build --workspace` и `cargo clippy --workspace
   --all-targets -- -D warnings`** — урок RN-8/RN-18: узкий `-p` слеп к E0004 и не
   компилирует `examples/**`/`src/bin/**`.

### 8.2 Если вводится НОВЫЙ вариант (HL-дельта, §9.3) — полный пакет + карта из ПЯТИ `match`

Атомарный CT-RFC-07 по docs/05 §4: Rust-тип → JSON Schema → minor bump 4→5 → миграционная
заметка → фикстуры valid/invalid → CHANGELOG → ≥1 тест. Плюс правка **всех exhaustive
`match` без `_ =>`**. Мандат называл **три** — фактически их **ПЯТЬ** (карта
`m-45-l2delta-impact.md` §1.1, каждая позиция перепроверена мной чтением файла; строки —
на HEAD `4ccefbc`, мандатные номера строк `segments.rs` устарели):

| Файл:строки (проверено) | Что | Владелец |
|---|---|---|
| `crates/recorder/src/lib.rs:69-79` (`md_kind_label`) | все 8 вариантов явно | engine-dev |
| `crates/journal/src/segments.rs:2566-2580` (`event_data_ts`) | 8 вариантов через `\|` (мандат говорил 2312-2323 — строки съехали) | engine-dev |
| `crates/sim/src/exchange.rs:222-288` (`on_event`) | 8 вариантов; `L2Delta => {}` намеренно | engine-dev |
| `crates/research-cli/src/bin/latency_probe.rs:120-134` | 8 вариантов; бинарь — ловится только `--workspace`/`--all-targets` | research-dev |
| `crates/journal/examples/dump.rs:18-40` | 8 вариантов; example — тот файл, что был пропущен в C-044 F2 | engine-dev |

Занизить список с 5 до 3 — повторить дефект C-044 F2 (перечисление по памяти вместо грепа).
Компилятор (E0004) поймает все пять при `cargo build --workspace`, но план обязан называть
их заранее — иначе оценка коммитов/владельцев в milestone врёт.

### 8.3 Порядок

R1 (Storage Box) → M-45 §8.1 → замер бутстрап-латентности при редком якоре (§4.3) →
recon-апдейт (§5) → инверсия ролей (§9.2). TD-053 больше НЕ блокер (CLOSED, §6).

## 9. Требует решения founder'а

Ничего из нижеследующего architect не решает сам (Граница C, gates.md §7):

1. **Ратификация CT-RFC-06 как whole:** форма варианта 6 достаточна, новый вариант НЕ
   вводится, M-45 идёт без contract-пакета (§1, §2, §8.1). Это подтверждение уже
   подписанного (DESIGN §17 п.1, M-45 2026-07-27), но после гейта critic/risk-critic —
   подпись нужна явная.
2. **Инверсия ролей и каданс якоря (10–30 с)** — принять ТОЛЬКО после (а) замера
   бутстрап/seek-латентности при редком якоре, (б) решения по recon-петле: апдейт recon на
   дельта-реконструированную книгу ИЛИ явно принятый компромисс «recon раз в 10–30 с» (§5).
   Цена ошибки асимметрична (recon — RK-класс): дефолт — НЕ инвертировать, пока оба пункта
   не закрыты.
3. **Hyperliquid:** HL шлёт агрегированный `l2Book`-снапшот; сырого диффа с
   Binance-семантикой `U`/`u`/`pu` нет. Выбор: (а) отдельный T1-вариант под HL-семантику
   (CT-RFC-07, полный пакет §8.2), (б) «HL остаётся snapshot-only» — явно и навсегда, с
   записью в DESIGN §17. Вопрос стоимость/ценность данных — не инженерный.
4. **Состав расширенного allow-list** (какие символы кокпита) и конвенция `epoch_id` для
   режимных сдвигов (§3) — операторское решение с продуктовой ценой (байт/сутки, §7).

---

## Источники и пруфы

Каждое утверждение о коде в этом документе подкреплено командой по worktree на
`origin/main` @ `4ccefbc` (совпадает с HEAD ветки этого RFC минус docs-коммиты). Цитируемые
SHA проверены `git cat-file -e <sha>^{commit}`: `f635bd2`, `7a237f7`, `d896b98`, `163939a`,
`6122fce` (ветка `origin/research/m-45-impact`). Строчные номера — на состоянии `4ccefbc`;
при чтении после будущих merge — перепроверять грепом, не доверять номерам (урок этого же
документа: мандатные `segments.rs:2312-2323` устарели за двое суток).
