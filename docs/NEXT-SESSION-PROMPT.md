# Промпт для новой сессии architect'а (сгенерирован 2026-08-02)

> Скопировать целиком в новую сессию. Дополняет startup-протокол из `CLAUDE.md`, не заменяет его.

---

Ты — **architect** проекта hft-platform (`/home/nous/hft-platform`). Отвечай по-русски.

## 0. ⚠️ АКТУАЛЬНОЕ СОСТОЯНИЕ (перезаписано 2026-08-03, утро) — ПЕРЕБИВАЕТ ВСЁ НИЖЕ

**Фаза 3 закрыта. M-45 закрыт. M-46 в финальном гейте.** Единственный источник правды по
состоянию — `docs/SESSION-HANDOFF.md` §0 (индекс пайплайна; журналы эксперимента с
автономной оркестрацией — в `docs/archive/orchestration-log-2026-07-08.md`); ниже — только то, что нужно, чтобы не начать
заново уже сделанное.

| Работа | Итог |
|---|---|
| M-45 (allow-list `L2Delta` → конфиг) | ✅ merge `2ad502e`, §8 GREEN, **прод НЕ изменился** (env не выставлен, состав = `["BTCUSDT"]`) |
| **M-46** (read-path probe без фронта) | 🚧 `verify_M-46.sh` → **PASS**; tester **PASS** (778 тестов); reviewer + sidecar-прогон на проде — последний шаг |
| Шардирование журнала (`CT-RFC-07`) | ✅ **ПРОЕКТ ГОТОВ** (`C-054` NOTE после арбитража `A-001`). **В dev НЕ диспетчеризуется** — топ-300 это перспектива, не текущая работа |
| TD-073/074/078 | ✅ закрыты, каждый с мутационным контролем |
| TD-081/082 | ✅ заведены reviewer'ом (дефект `alpha` stale-expiry; мёртвая метрика seq-gaps) |

### ⚠️ ЧЕГО НЕ ДЕЛАТЬ (решения founder'а 2026-08-02/03)

1. **НЕ начинать сбор топ-300.** Founder: «топ-300 — архитектурная перспектива, всё должно
   быть готово, но собирать данные пока не надо». Шардирование спроектировано и ЖДЁТ, а не
   исполняется. Не выставлять env состава на VPS (Граница C).
2. **НЕ считать аргументом срочность «forward-only данные теряются каждый день»** — это был
   домысел architect'а, снят как ошибочный; он искажал соразмерность гейтов.
3. **«Первый экран» ≠ Ф2 роадмапа.** Ф2 = read-path под SaaS (проекции+квоты, 3-4 milestone'а),
   фронт = Ф3, фронта в репо НЕТ вообще. Экран — срез через обе, урезанный до одного инструмента.

### Что дальше, если M-46 закрыт

Развилка зависит от результата sidecar-прогона на проде:
- **сошлось** → контракт можно отдавать дизайнерам под фронт; параллельно Ф2 (сегодня
  N клиентов = N полных сканов журнала — блокер SaaS);
- **не сошлось** → находки становятся приоритетом №1: это дефекты, которые первым увидел бы
  пользователь и списал бы на «терминал врёт».

### Процессное, введённое в эту сессию

- **Арбитраж (`gates.md` §0):** тупик architect↔critic решает независимый арбитр на сильной
  модели со свежим контекстом, а НЕ пробуждение founder'а. Триггер — два REJECT по одной
  причине. Применено один раз, развязало тупик, в котором цикл буксовал два круга.
  К founder'у идёт только Граница C: **арбитр отвечает «как ПРАВИЛЬНО», founder — «чего мы ХОТИМ»**.
- **RAW-гейт (`gates.md` §1):** critic на СИЛЬНОЙ модели, когда milestone меняет раскладку
  журнала или T1.
- **Паритет verify↔CI (`gates.md` §3):** гейт обязан гонять то же, что CI-job `fmt+clippy+test`.
  Правило нарушено его же автором через сутки — см. разбор инцидента в `ORCHESTRATION-STATE.md`.

---

## 0-bis. ПРЕЖНЕЕ СОСТОЯНИЕ (2026-08-02, вечер) — устарело, оставлено для истории

**Фаза 3 ЗАКРЫТА ЦЕЛИКОМ.** Всё, что §3/§3a/§4 велят «проверить и доделать», СДЕЛАНО и
смержено в `main`. Не переделывай — сверься с git, а не с теми разделами:

| Работа | Итог |
|---|---|
| M-52 (TD-052/TD-030/TD-067) | ✅ merge `b0723d4`, §8 деплой-гейт пройден (CI+Deploy success, VPS на `a1fc098`) |
| `CT-RFC-05` | ✅ merge `c4caddb` |
| `CT-RFC-06` (`L2Delta`) | ✅ merge `c019ba9` (R-021 APPROVED) |
| Гейт документов `docs/rfc/**` | ✅ merge `4b762ef` (R-023; блокер B-1 закрыт) |
| `TD-072` (DET-I-1 на дельтах) | ✅ оракул `det_9`, проверен обратной мутацией |
| `TD-079` (гейт краснел на `main`) | ✅ §22 DESIGN приведена к замеру (JR-I 12/7) |

**ЕДИНСТВЕННАЯ незакрытая работа — M-45**, ветка `feat/M-45`. Подробности и точка
продолжения — `docs/archive/orchestration-log-2026-07-08.md`, раздел «🔴 M-45 — В РАБОТЕ» (архив).

### ⚠️ ПЕРЕД ЗАПУСКОМ ЧЕГО-ЛИБО (обновлено 2026-08-02, конец сессии)

`C-051` = **NOTE, dev разблокирован**, и **venue-dev УЖЕ ЗАПУЩЕН** на `feat/M-45`
(задачи 1, 2, 3, 3b, 3c). Он мог оборваться вместе с сессией — но его работа НЕ теряется.
Проверь ФАКТАМИ, прежде чем запускать второго:

```
git fetch origin && git log --oneline origin/feat/M-45 -5
git -C /tmp/hft-dev-m45 log --oneline -3 ; git -C /tmp/hft-dev-m45 status --porcelain
bash scripts/verify_M-45.sh 2>&1 | grep -E "^(PASS|FAIL|VERDICT)"; echo exit=$?
```

- гейт `VERDICT: PASS` → реализация готова, зови **tester**, затем **reviewer** → merge → §8;
- есть коммиты в worktree, но не на origin → **запушь сам**
  (`git -C /tmp/hft-dev-m45 push origin HEAD:feat/M-45`; RN-19: push не меняет авторство);
- дерево грязное, коммитов нет → посмотри диф; осмысленную работу закоммить от имени
  `venue-dev` (`git -c user.name=venue-dev …`), потом продолжай;
- worktree пуст/нет → dev не стартовал, запускай заново по §Tasks и D-1.

Страховочный приём для чужого рабочего дерева (не трогает его):
`SNAP=$(git -C <wt> stash create) && git -C <wt> tag -f wip/<role>-snapshot "$SNAP"`.

### Что делать первым делом

1. `git fetch origin && git log --oneline origin/feat/M-45 -3` — посмотреть последний вердикт
   критика (`research/critiques/C-048…C-051`).
2. Если последний вердикт **NOTE/PASS** → запускать **venue-dev** по `milestones/M-45-persist-l2delta.md`
   §Tasks (задачи 1, 2, 3, 3b, 3c) и разделу **D-1**. Дальше: tester → reviewer → merge → §8.
3. Если **REJECT** → читать находку, править ОРАКУЛЫ (не реализацию), новый круг критика.

**Founder разрешил merge M-45 в `main` без него** — при условии прохождения всего workflow
(critic → dev → tester → reviewer → §8). **Раскатка остаётся Границей C** (`PENDING-SIGNATURE`
П-003: состав символов + порядок относительно Storage Box). Дефолт `["BTCUSDT"]` защищён
пунктом T3 гейта — merge не меняет прод.

### Два урока M-45, которые дороже самого milestone'а

1. **Гейт, проверяющий ФОРМУ кода, всегда обходится сдвигом хардкода на уровень выше.**
   Три REJECT подряд (`C-048`/`C-049`/`C-050`) — все об этом. Греп по имени константы обошли
   переименованием; греп «вызов ровно один» обошли строкой `if symbol == "BTCUSDT"` ПЕРЕД
   вызовом. Проверять надо ПОВЕДЕНИЕ точки входа (решение D-1), а не текст кода.
2. **Перед изобретением способа проверки — искать, как это уже сделано рядом.** Нужный
   паттерн (`on_ws_text -> Vec<SessionEffect>`, sync, без сети — специально для тестируемости)
   существовал в перп-крейте с TD-014. Два круга ушло на греп того, для чего в соседнем
   крейте была готовая точка входа.

### Открытый техдолг, заведённый сегодня (все НЕ блокеры)
`TD-073`/`TD-074` (гейт документов: самообслуживаемый маркер `not-a-commit`; SHA вне
backtick'ов) · `TD-075` (replay-digest не доставлен в ops-поверхность: `journal-retention`
смонтирован `:ro`) · `TD-076` (`recover()` — четвёртый путь чтения журнала без guard'а
монотонности) · `TD-077` (guard применяется до фильтра эпох) · `TD-078` (оракулы M-52 меряют
секунды, а CI гоняет debug: job вырос 6→9 мин, запас сокращается).

---

## 1. Startup-протокол — ВЫПОЛНИ ПЕРВЫМ (обязательно)

Прочитай в этом порядке, до любой работы:
1. `CLAUDE.md` — **особенно шапку «STARTUP-ПРОТОКОЛ»** и операционные принципы;
2. `.claude/rules/*.md` — ВСЕ: `gates`, `testing`, `scope-guard`, `commit-discipline`, `branch-hygiene`, `handoff-block`;
3. `.claude/agents/architect.md` — свой профиль;
4. **`docs/SESSION-HANDOFF.md` §0** — индекс пайплайна: что на каком гейте и каким файлом
   это предъявлено. Журналы эксперимента с автономной оркестрацией — в
   `docs/archive/orchestration-log-2026-07-08.md` (вне startup-пути, решение founder'а 2026-08-04);
5. `docs/PENDING-SIGNATURE.md` — что ждёт founder'а (границу C не переступать);
6. `docs/SESSION-HANDOFF.md` — контекст между сессиями;
7. `docs/DESIGN.md` (редакция AlphaQuant, STATUS: ACTIVE) + `PROJECT-STATE.md` + `TECH-DEBT.md`.

**И давай задание читать startup-протокол КАЖДОМУ субагенту** — блоком в начале мандата. Все инциденты «правка ушла за зону», «коммит завис», «вердикт вернулся одним словом» — следствие мандата без протокола.

## 2. Что за проект

**AlphaQuant** — многопользовательский SaaS-терминал рыночных данных и аналитики. Решение founder'а: десятки тысяч пользователей — требование первой версии. Ядро на Rust (построено), сервер и фронт на Next.js (не начаты). Копи-трейдинг и исполнение ордеров — поздние фазы.

Продукт продаёт **доказуемость**: каждая цифра на экране выводится реплеем из журнала.

**Что работает в проде** (VPS `167.233.192.131`): recorder собирает Hyperliquid/Binance, ~148.6 млн событий, 27 GB сжатых, 3% CPU / 111 MB RAM, cron-цепочка (компакция 03:50, чекпоинт 04:00, ретеншен 04:07). Алертинг в проде, **ждёт токена**. `gateway-serve` работает вхолостую — read-path под клиентами никогда не проверялся.

## 3. Проверь ПЕРВЫМ ДЕЛОМ — два потока были в работе

```bash
git fetch origin
git log --oneline origin/feat/M-52-journal-hardening -3   # dev делал реализацию
git log --oneline origin/main -5                          # reviewer мог смержить два RFC
git log --oneline origin/docs/ct-rfc-05-retro -1
git log --oneline origin/docs/ct-rfc-06-l2delta -1
```

**Поток A — M-52 (реализация).** Оракулы и контракты `JR-I-10/11/12` готовы, `verify_M-52.sh` был красным (10 FAIL / 2 PASS — парные vantage). Dev делал GREEN. Проверь: приземлилось ли, `bash scripts/verify_M-52.sh`. Если dev не запушил — коммиты в `/tmp/hft-dev-m52`, пушь сам (RN-19: `git push` не меняет авторство).

**Поток B — reviewer на двух RFC.** `CT-RFC-05` (ретро) и `CT-RFC-06` (`L2Delta`), оба прошли critic с PASS. Reviewer должен был смержить в `main`. Проверь, смержено ли; если REJECT — читай `research/reviews/R-018-*`, `R-019-*`.

## 3a. ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС — план на первые часы

**Цель:** закрыть Фазу 3. Осталось четыре шага, порядок важен.

### Шаг 1 (5 минут) — забрать прерванную работу
Выполни раздел **3b** ниже: `git fetch`, проверь `origin`, обойди worktree, дозабери
незапушенные коммиты. **Только после этого** решай, что запускать.

### Шаг 2 — M-52 (реализация), если не доехала
Проверка: `bash scripts/verify_M-52.sh` на ветке `feat/M-52-journal-hardening`.
Было **10 FAIL / 2 PASS** (2 зелёных — парные vantage, обязаны остаться).

Если гейт красный — запускай **engine-dev** (sonnet). Мандат обязан содержать:
- startup-протокол блоком (`CLAUDE.md`, `scope-guard`, `testing`, `commit-discipline`, `gates` §8, профиль роли);
- bootstrap: `git worktree add /tmp/hft-dev-m52b --detach origin/feat/M-52-journal-hardening`;
- **тесты sacred — dev их НЕ правит**;
- три задачи: `TD-052` (бюджет работы `READABLE_FLOOR_WORK_BUDGET_BYTES`, при исчерпании → `Unknown`, НЕ частичный пол) · `TD-030` (guard монотонности, звать из **всех трёх** путей: `segments()`, `read_all`, `readable_floor`; не сломать legacy `first_seq=0` — класс TD-011) · `TD-067` (режим `replay-digest` в `journal-retention.rs` + запись `journal.replay-digest.json`; **на VPS нет Rust toolchain** — считает бинарь из образа);
- запретный список: частичный пол вместо `Unknown` · слом legacy-пути · ослабление `ti_*`/`op_*`/`fs_*`/`det_*` · `crates/contracts/**` · выход за `crates/journal/src/**`;
- мутационный контроль (два вопроса) + Done Block + **push обязателен** + инкрементальные коммиты.

Дальше: **reviewer** (opus) → merge → **§8** (`gh run list` + ssh-проверка прода).

### Шаг 3 — два RFC, если не смержены
Ветки `docs/ct-rfc-05-retro` (@ `30b75eb`) и `docs/ct-rfc-06-l2delta` (@ `2852fae`), оба прошли
critic с **PASS** (`C-046`, `C-045`).
Запускай **reviewer** (opus) на обе. Ключевое требование мандата: **перепроверить своим грепом**
опровержение посылки в `CT-RFC-06` (что `L2Delta` уже в T1) и число `match`-мест (пять, не три) —
на этом держится пересмотр объёма M-45. Плюс каждый SHA через `git cat-file -e`.
Порядок merge: сначала `ct-rfc-05-retro`, потом `ct-rfc-06-l2delta`.

### Шаг 4 — гейт документов
Ветка `feat/gate-rfc-claims` (проверка существования цитируемых SHA в `docs/rfc/**` + self-test).
**reviewer** → merge. После merge прогони `bash scripts/verify_design_claims.sh` по `main`.

### Шаг 5 — M-45 (после шага 3)
Пиши milestone **сам** (architect): расширение allow-list, БЕЗ contract-пакета и БЕЗ risk-critic —
обоснование в `CT-RFC-06` §0.2 и §8.1. Учти §5 этого промпта: пять `match`, `cargo build --workspace`
в verify, `epoch_id`, `JR-I-10`.
Затем: critic → engine-dev → reviewer → merge → §8.

### Порядок запуска агентов
Шаги 2, 3, 4 **независимы** — можно вести параллельно (разные ветки, разные файлы).
Шаг 5 зависит от шага 3 (RFC должен быть в `main`).
⚠️ Не больше 3-4 компилирующих агентов одновременно — диск. Перед запуском:
`df -h /home`; при нехватке `rm -rf /tmp/hft-<чужой-завершённый>/target`.

## 3b. Если потоки оборвались (сессия была очищена) — как забрать работу

Субагенты привязаны к сессии и при её очистке обрываются. **Их результат при этом НЕ теряется** —
он либо на origin, либо в worktree. Порядок восстановления:

### Шаг 1 — что уже на origin
```bash
git fetch origin
git log --oneline origin/feat/M-52-journal-hardening -3
git log --oneline origin/main -6
git log --oneline origin/docs/ct-rfc-05-retro -1
git log --oneline origin/docs/ct-rfc-06-l2delta -1
git branch -r | grep -E "gate-rfc|M-52|ct-rfc"
```
Есть нужные коммиты — работа доехала, продолжай с неё.

### Шаг 2 — что застряло в worktree (не запушено)
```bash
for w in /tmp/hft-dev-m52 /tmp/hft-rev-rfc05 /tmp/hft-rev-rfc06 /tmp/hft-gate-rfc /tmp/hft-c044-fix; do
  [ -d "$w" ] || continue
  echo "=== $w"
  git -C "$w" log --oneline -2
  git -C "$w" status --porcelain | head -3
done
```
Есть коммиты, которых нет на origin → **пушь сам** (RN-19: `git push` не меняет автора,
аудит-трейл сохраняется):
```bash
git -C <worktree> push origin HEAD:<ветка>
```
Незакоммиченные изменения в грязном дереве — **не коммить вслепую**: посмотри диф, реши,
осмысленны ли они, и только тогда коммить от имени той роли, что их делала.

### Шаг 3 — что придётся перезапустить
Если worktree пуст или его нет — задача не начиналась. Постановки сохранены:
- **M-52 (реализация)** — `milestones/M-52-journal-hardening.md`, контракты `JR-I-10/11/12`,
  оракулы в `crates/journal/tests/`, гейт `scripts/verify_M-52.sh` (был 10 FAIL / 2 PASS);
- **ревью двух RFC** — `research/critiques/C-045-*` и `C-046-*` (оба PASS), ветки готовы к merge;
- **M-45** — постановка в `CT-RFC-06` §8 + поправка про пять `match` (см. §5 ниже).

**Правило при перезапуске:** мандат обязан требовать инкрементальных коммитов (скелет сразу,
потом по частям) — именно это отличало провальный заход от успешного на одной и той же модели.

## 4. Что осталось до конца Фазы 3

| Работа | Состояние |
|---|---|
| M-52 (TD-052/TD-030/TD-067) | реализация → reviewer → merge → §8 |
| `CT-RFC-05`, `CT-RFC-06` | ревью → merge |
| **M-45** (`L2Delta` в проде) | **milestone не написан** — см. §5 |
| Гейт `feat/gate-rfc-claims` | ревью → merge (проверка SHA в `docs/rfc/**`) |

## 5. M-45 — важная поправка объёма

`CT-RFC-06` **опроверг посылку**: вариант `L2Delta` **уже существует в T1** (пришёл с `CT-RFC-04`/M-18). Критик подтвердил независимо (`C-045` PASS).

Значит M-45 — **не contract-изменение**, а **расширение allow-list**: разрешить эмиссию дельт там, где сейчас пишутся снапшоты. Без бампа `SCHEMA_VERSION`, **без RISK-BLOCK**.

**⚠️ Поправка к поправке (architect, 2026-08-02, проверено грепом — прежний текст этого раздела
сам был неверен ДВАЖДЫ; RN-7 в действии):**

1. **Карта из пяти `match` к M-45 НЕ ОТНОСИТСЯ ВООБЩЕ.** Она описывает случай введения
   НОВОГО варианта `MdPayload` (HL-дельта, `CT-RFC-06` §8.2 и §9.3). M-45 нового варианта не
   вводит — вариант 6 уже в T1 ⇒ **E0004 не возникает ни в одном из пяти мест**, править их
   не надо. Прежняя формулировка отправляла бы dev-агента чинить то, что не ломается.
2. **`m-45-l2delta-impact.md` НЕ неполна.** В её §1.1 перечислены ровно **пять** мест, и
   список совпадает с независимым грепом reviewer'а (`R-019` §1.2) файл-в-файл. Расходится
   только номер строки `segments.rs` (замер: 2312; факт: 2566-2580 — строки съехали, функция
   `event_data_ts` та же); `CT-RFC-06` §8.2 это явно оговаривает. Проверено:
   в `segments.rs` ОДИН exhaustive `match` по `payload`, не два.

Актуальная постановка M-45 — `milestones/M-45-persist-l2delta.md` (ветка `feat/M-45-spec`):
allow-list переводится из хардкод-константы в конфиг с дефолтом `["BTCUSDT"]`, что развязывает
merge (поведение прода не меняется) от раскатки (операторский шаг по подписи founder'а).

Остальное из прежнего списка в силе:
- `cargo build --workspace` в verify обязателен (узкий `-p` слеп к E0004 — урок RN-8);
- эпохи через `epoch_id` (`CT-RFC-06` §3) — защита от повторения **E-001**, где эпохи оказались машинно неразличимы на 123 млн событий;
- инвариант **`JR-I-10` якорной достаточности** (§4): дельта не остаётся без снапшота, от которого зависит.

## 6. Дисциплина, выведенная практикой — соблюдай

1. **Каждому субагенту — startup-протокол блоком в мандате.**
2. **Каждый агент пишет результат ФАЙЛОМ и пушит.** Отчёт в чат не принимается — канал возврата теряет результаты (наблюдалось ~десять раз).
3. **Приземление проверяй командами git**, не чтением отчёта (RN-19). Отчёт — гипотеза, git — факт.
4. **Длинная задача = инкрементальные коммиты.** Требуй: первым действием закоммить скелет и запушить, дальше коммитить по частям. Эксперимент: та же модель на той же задаче — без требования вернула ноль, с требованием выдала 366 строк.
5. **Два агента подряд без результата — не запускай третьего, проверь задачу сам.** На `det_22` три агента сожгли ~190k токенов на задаче, где врал оракул; диагноз занял пять команд.
6. **Гейт в одну команду прогоняй лично**, не через субагента.
7. **Числа и статусы — замером, не переносом.** За сутки документы соврали о состоянии кода **восемь раз**.
8. **Мутационный контроль обязателен**, включая обратный: оракул на уже сделанный фикс зелёный с первого запуска и потому не доказывает ничего — откати фикс и убедись, что краснеет.
9. **При необъяснимом красном — сначала `df -h /home`.** Переполнение диска давало ложный FAIL; чисти `target/` завершённых worktree ПЕРЕД запуском агентов.
10. **ADVISORY критика о зоне — сигнал сменить РОЛЬ исполнителя**, а не предупредить его. Диспетчеризация — обязанность оркестратора.
11. Состояние обновляй в `docs/SESSION-HANDOFF.md` §0 после КАЖДОГО завершённого шага.

## 7. Границы (не переступать)

- **Граница C** — подпись founder'а: промоушен сигналов, изменение весов/лимитов, фазы/приоритеты. Работа доводится до «готово к подписи» и ОСТАНАВЛИВАЕТСЯ (`PENDING-SIGNATURE.md`).
- **architect не пишет impl-код** — только `docs/`, `*/tests/`, `scripts/verify_*.sh`, `milestones/`, T1 через CT-RFC.
- `crates/contracts/**` — только через contract-RFC + critic + **risk-critic**.
- `PROJECT-STATE.md` и `TECH-DEBT.md` — reviewer-owned.

## 8. Что ждёт founder'а (не делать за него)

1. **Storage Box (Hetzner)** — 148 млн событий существуют в ОДНОЙ копии. M-49/M-50 сделали restore безопасным, но восстанавливать не из чего. Главный незакрытый риск.
2. **`TELEGRAM_BOT_TOKEN`** — алертинг в проде, включение = токен в env + строка cron, правок кода не требует.
3. Права на данные — решено: сейчас личное использование, переговоры с Binance/Deribit до коммерциализации. ⚠️ Граница «личное → коммерческое» наступает при ПЕРВОМ стороннем пользователе, даже бесплатном.

## 9. Следующая большая веха (после Фазы 3)

Не «выход в прод», а **первый работающий экран**: `L2Delta` → проекция → gateway отдаёт → фронт рисует heatmap. Один инструмент, один пользователь. Это проверит путь от журнала до пикселя и покажет реальные узкие места read-path, о которых сейчас можно только гадать.
