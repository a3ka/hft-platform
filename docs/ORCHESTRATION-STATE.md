# Состояние оркестрации — передача незавершённой работы между сессиями

Ведёт architect. Обновляется при КАЖДОМ запуске/возврате агента.
Назначение: сессия architect'а обрывается по исчерпанию контекста, а работа продолжается.
Этот файл + `SESSION-HANDOFF.md` + `PENDING-SIGNATURE.md` — всё, что нужно новой сессии.

**Главный принцип, делающий перенос возможным: каждый агент ПИШЕТ и ПУШИТ результат в git.**
Отчёт в чат не принимается. Проверено дорогой ценой: за 31.07 канал возврата субагентов вернул
пустоту семь раз подряд (включая разведку на 189k токенов и два вердикта гейта). Всё, что было
записано в ветку, — уцелело; всё, что осталось «в отчёте», — исчезло.
Отсюда правило RN-19 (`gates.md` §8): **отчёт агента — гипотеза, состояние git — факт.**

---

## 🕐 МАНДАТ: автономная работа 6 часов (founder, 2026-07-31T23:25Z → 2026-08-01T05:25Z)

**Указание founder'а:** работать непрерывно; закончив запланированное, БРАТЬ СЛЕДУЮЩУЮ по
важности задачу самостоятельно, не дожидаясь указаний. Founder спит и утвердит результаты утром.

**Единственная жёсткая граница:** всё, что требует подписи founder'а (граница C —
`docs/PENDING-SIGNATURE.md`), доводится до состояния «готово к подписи» и ОСТАНАВЛИВАЕТСЯ.
Никакой агент не подставляет автоматическое approve.

### ⚠️ Новая задача, порождённая разбором (вставить в очередь после п.3)

**Механическая проверка маркировки `[ЕСТЬ]`/`[ЧАСТИЧНО]`/`[ПЛАН]` в `DESIGN.md`.**

За ночь 31.07 документ ТРИЖДЫ солгал о состоянии кода, каждый раз по-разному:
- `C-042` F-1 — покрытие `RK-I` заявлено 3 оракула, фактически **0**;
- `C-041` Ф1 — покрытие `VN-I` заявлено 0, фактически суита M-41 из **40 тестов**;
- `R-004` Б3 — §18 описывал механизм изоляции как СУЩЕСТВУЮЩИЙ, тогда как в коде это место
  помечено как отсутствующее.

Ошибки разнонаправленные (завышение, занижение, ложное существование) — значит дело не в
невнимательности к конкретной цифре, а в отсутствии проверки как класса. Правило «форма
снимается замером» (`testing.md`) записано утром того же дня и нарушено трижды за сутки
**самим автором правила**. Вывод: правило, исполнение которого держится на добросовестности
автора, не работает — нужен гейт.

**Что построить:** `scripts/verify_design_claims.sh` — по образцу уже построенных контрактных
гейтов (`feat/contract-gates`). Проверяет машинно:
- каждое `[ЕСТЬ]` в `DESIGN.md` сопровождается путём к файлу/тесту, и этот путь СУЩЕСТВУЕТ;
- числа покрытия в таблице §22 совпадают с `grep`-замером по репозиторию;
- ссылки `DESIGN.md §N` указывают на существующие разделы;
- нет ссылок на удалённые файлы.
Setup-guard обязателен: проверка, которая не смогла выполниться, — FAIL, не молчаливый PASS.

**Почему это важнее, чем кажется:** документ — источник, из которого исполняют агенты. Ложное
`[ЕСТЬ]` означает, что dev не построит механизм, считая его готовым; ложное покрытие означает,
что sacred-инвариант уйдёт в реализацию без оракула. Это тот же класс, что «зелёный тест против
заглушки», только этажом выше.

### ⚠️ Алертинг — REJECTED (`research/reviews/R-005-alerting.md`, ветка `feat/alerting`)

Семь находок, две блокирующие. **НЕ мержено.**

- **F-1 (BLOCKER)** — `WD-SEQ-STALLED` выключается интервалом cron: детектор сравнивает `next_seq`
  между запусками, и при окне меньше интервала тихая деградация НЕ ЛОВИТСЯ. Это главный класс
  отказа (контейнер жив, healthcheck зелёный, данные не идут) — ровно тот, ради которого сторож
  и строился.
- **F-2 (BLOCKER)** — `TELEGRAM_BOT_TOKEN` попадает в лог cron при сетевой ошибке (утечка секрета
  в файл на VPS).
- **F-3** — ложная тревога по диску во время ночной компакции/ретеншена (временный расход места).
- **F-4** — **RED-first вывернут наизнанку: реализация раньше тестов, тесты писал dev.**
- **F-5** — «не смог оценить» стирает дедуп-память (`push_or_clear` смешивает два разных `None`).
- **F-6** — маркеры `*.alert` («прогон УПАЛ») не читаются вовсе.
- **F-7** — рестарт-петля подавляется после первого сообщения.

**F-4 — ошибка architect'а в МАНДАТЕ, не dev'а.** В мандате было написано «тесты пиши в своём
крейте» — то есть я поручил одному агенту и спецификацию, и реализацию. `crates/ops` формально
не sacred, но RED-first — правило не про ЗОНЫ, а про ПОРЯДОК: тест есть спецификация и пишется
ДО кода, иначе описывает то, что получилось, а не то, что требовалось.

Следствие видно в F-1: тест проверял поведение ФУНКЦИИ и был зелёным, тогда как СЦЕНАРИЙ
(watchdog под реальным cron-интервалом) не работал. Мутационный контроль этого не поймал —
он подтверждает связь теста с кодом, но не то, что тест проверяет ПРАВИЛЬНОЕ.

**Правило на будущее (внести в `testing.md`):** мандат dev-агенту НИКОГДА не включает написание
тестов на собственную реализацию — даже вне sacred-зон. Оракул пишет architect ДО мандата; dev
получает падающий тест как спецификацию. Исключений нет: «крейт не sacred» — не основание,
потому что защищается не зона, а порядок.

**Что делать дальше:** architect пишет RED-оракулы на все семь находок (особенно СЦЕНАРНЫЙ тест
F-1: watchdog под реальным интервалом cron, а не вызов функции), затем dev делает GREEN.

### Состояние на конец ночной смены (обновлено architect'ом)

| Поток | Ветка | Статус |
|---|---|---|
| **M-50** (защита TD-053) | `feat/M-50-floor-scan` @ `a2d2e28` | реализовано, `verify_M-50.sh` **PASS** (16 проверок), мутация подтвердила фикс (6 тестов краснеют при откате капа). **На PR-гейте у reviewer'а** |
| **Контрактные гейты** | `feat/contract-gates` @ `ac718e4` | обе блокирующие находки `R-006` закрыты и **проверены прогоном** (см. ниже). Нужен повторный ревью |
| **Алертинг** | `feat/alerting` | оракулы architect'а на месте (`red_ops_watchdog_cycle.rs` — сценарный, `red_ops_transport_redaction.rs`); **dev делает GREEN** |
| **Блок 1** | `docs/design-evolution` | все находки `C-041`/`C-042`/`R-004` закрыты; нужен повторный ревью → **подпись founder'а (П-001)** |

### Проверено прогоном лично (не по отчётам агентов)

**M-50.** Гейт `VERDICT: PASS`, ключевое — `fs_8`: пол видит событие 16 MiB И пик памяти не растёт
с размером события (обе стороны конфликта решены, не разменяны). Регресс M-49 цел
(`op_5`/`op_8`/`ti_7`/`ti_8` зелёные). **Мутация:** возврат `FRAME_LEN_SANITY_CAP` к 64 KiB → 6
тестов FAILED. Неожиданное: упал и `fs_7` (штатный рестарт) — значит TD-053 задевал не только
операторский путь с декларацией, но и обычный старт recorder'а. Область поражения была ШИРЕ
исходной находки.

**Контрактные гейты.** `verify_contracts.sh` **PASS** (5 проверок). Мутация: поле в схему, которого
нет в типах → `S1` и `S4` FAIL, exit 1 — гейт умеет краснеть.
`R-006` F-2 закрыт: запуск из подкаталога больше не даёт молчаливый PASS, а объясняет результат.
`R-006` F-1/F-3 закрыты: `scripts/tests/red_diff_contract_schema.sh` — **12 сценариев PASS**,
включая два регрессия-гварда на `$ref` (в свойстве и внутри `items`) → `breaking`, не `none`;
плюс setup-guard кейсы (несуществующий base-ref, отсутствующий классификатор → FAIL).

### Уроки смены (внести в правила при первой возможности)

1. **RED-first — правило про ПОРЯДОК, а не про зоны.** Мандат dev-агенту НИКОГДА не включает
   написание тестов на собственную реализацию, даже вне sacred. Нарушение стоило круга на
   алертинге (`R-005` F-4): тест функции зелёный, сценарий не работает.
2. **Мутационный контроль имеет границу.** Он доказывает связь теста с кодом, но НЕ то, что тест
   проверяет правильное. На алертинге мутация прошла (2 детектора → 2 падения), а сценарий был
   сломан.
3. **Кривая мутация даёт пустой вывод, который легко принять за успех.** На M-50 первая попытка
   сломала компиляцию (заменил определение константы) — вывод пуст, выглядит как «тесты не упали».
   Проверять, что мутация вообще собралась.
4. **Гейты сами нуждаются в анти-плацебо.** Оба контрактных гейта содержали ложный PASS —
   классификатор врал «схема не изменилась» на `$ref`-breaking, атомарный гейт проходил из
   подкаталога. Это третий слой, где за сутки всплыл один класс дефекта: фикстуры тестов → числа
   в документации → сами проверки.

### Очередь приоритетов — брать сверху вниз

| # | Задача | Почему этот приоритет | Готовность |
|---|---|---|---|
| 1 | Закрыть `C-041` Ф3 (§11/§12 устарели) и Ф4 (форма цитат) → **reviewer** → merge Блока 1 | документ уже изменён; незавершённая правка хуже обоих законченных состояний | находки описаны |
| 2 | Прогнать `feat/contract-gates` **самому**, проверить анти-плацебо гейтов → reviewer → merge | гейт, не проверенный на срабатывание, — заглушка (`testing.md`) | ветка готова |
| 3 | **TD-007 (`DET-I-1`)** — прочитать аудит, спроектировать недостающие оракулы | стержневой инвариант; продукт продаёт ДОКАЗУЕМОСТЬ, а она гарантирована частично | аудит готов |
| 4 | **M-50** (защита TD-053) — принять проект Fable, отдать dev → tester → reviewer | дефект достижим УЖЕ СЕЙЧАС, класс seq-reuse (необратимая порча) | Fable проектирует |
| 5 | Алертинг → reviewer → merge | 14 дней тишины и 335 млн тиков в прошлом проекте | в работе |
| 6 | **CT-RFC на `L2Delta`** (M-45) — форму проектирует architect, делегированию не подлежит | предусловие продукта: без дельт нет heatmap/footprint/DOM-replay | карта влияния готова |
| 7 | Ретро-документ `CT-RFC-05` | governance-дыра: 49 упоминаний в коде без RFC | — |
| 8 | Защита **Д1** (смена СЕМАНТИКИ при неизменной ФОРМЕ) | schema-валидатор не ловит ПО ОПРЕДЕЛЕНИЮ; готового решения нет — чистое проектирование | — |
| 9 | TD-030 (guard монотонности), TD-052 (пол не ограничен по времени) | долг журнала, всплывший на M-49 | — |

### ⚠️ Инцидент 01.08: переполнение диска дало ЛОЖНЫЙ красный гейт

**Что было.** `verify_alerting.sh` показал 14 PASS и один FAIL — `cargo test --workspace`,
exit=101. Выглядело как реальный дефект: «оракулы алертинга зелёные, но что-то сломано в другом
крейте». Следующая команда упала с ENOSPC: временная ФС харнесса заполнена, `df` показал
**437G / 100% занято**.

**Причина.** За ночь создано ~20 worktree, в каждом `cargo build` создаёт `target/` по **6–9 GB**.
Суммарно они съели диск. Транскрипты агентов оказались ни при чём (первая гипотеза была неверна —
удаление `*.output` места не дало).

**После освобождения 39 GB тот же гейт дал `VERDICT: PASS`, exit=0.** То есть FAIL был ложным,
и корректная работа dev'а едва не ушла на переделку.

**Правило (усиление существующего).** Гигиена worktree — часть оркестрации, не уборка:
1. При КАЖДОМ необъяснимом падении гейта — **сначала `df -h /home`**, потом выводы. Особенно если
   падает то, что не связано с правкой (чужой крейт, workspace-прогон).
2. После завершения работы агента — удалять `target/` его worktree:
   `rm -rf /tmp/hft-<роль>-<тема>/target`. Сам worktree можно оставить (он мал), `target` — нет.
3. Не запускать больше 3–4 агентов с компиляцией одновременно: каждый строит свой `target`.
4. Ложный красный опаснее ложного зелёного в одном отношении: он заставляет ЧИНИТЬ исправное,
   тратя цикл и внося правки в работающий код.

### Правила этих шести часов

1. **Startup-протокол — первым делом** (`CLAUDE.md`, шапка), включая после каждого сжатия контекста.
2. **Каждому субагенту — блок «Startup-протокол» в мандате.** Без него агент не знает ни зон, ни
   формата Done Block, ни правил push.
3. **Каждый агент пишет результат ФАЙЛОМ и пушит.** Отчёт в чат не принимается — канал возврата
   за 31.07 потерял результат девять раз подряд.
4. **Приземление проверять командами git** (RN-19), не чтением отчёта.
5. **Гейт в одну команду прогонять лично**, не через субагента (два агента сожгли 350k токенов
   на прогон одного скрипта).
6. **Числа в документах — замером**, не переносом из старых отчётов. За 31.07 это ошибка №1:
   покрытие `RK-I` завышено, `VN-I` занижено, форма фикстур M-49 не совпадала с продом.
7. Состояние обновлять в этом файле ПОСЛЕ каждого завершённого потока — он единственное, что
   переживает сжатие контекста.

## 📍 СОСТОЯНИЕ ФАЗЫ 3 (2026-08-02, конец сессии architect'а)

### ✅ M-51 закрыт — TD-007 CLOSED впервые за проект
Merge `d896b98`, §8 GREEN (прод `32a44f4`), проверено architect'ом лично: контейнеры healthy,
heartbeat 8 с, `next_seq` 148.6 млн растёт, сегмент 157. Порядок обхода книги изменился
(`iter_sorted`) — проекция не поехала.
Новый долг от reviewer'а: **TD-065**, **TD-066**, **TD-067** (`replay_digest` не доставляется в
прод — детерминизм доказан на тестах, но в рантайме дайджест не считается; класс TD-011:
«зелёные тесты при непроверенном проде»).

### 🔄 CT-RFC-05 (ретро-документ) — на гейте
Ветка `docs/ct-rfc-05-retro` @ `2b0cbc2`, 204 строки, `STATUS: RETRO-DOCUMENTED`.
Закрывает governance-дыру: изменение T1 (`MarginInventory`, `SCHEMA_VERSION` 3→4) жило в 49
упоминаниях кода без RFC. Документ честно фиксирует, что восстановлен постфактум, не выдумывает
мотивацию, называет связь с `M-35` и причину сбоя (машинного гейта не существовало; теперь есть
`verify_ct_rfc_atomic.sh`).
**Critic вернул REJECT** (`C-044`, на ветке). Две блокирующие находки — ТОТ ЖЕ класс, что весь день:

- **F1** — §4 цитирует **3 из 4 коммитов, которых НЕТ** в смёрженной истории `main`. Несуществующие
  SHA в нормативном документе хуже отсутствия документа: это ложный аудит-трейл, на который сошлются.
- **F2** — список мест правки занижен: **4 вместо 5**, пропущен `dump.rs`. Повторение M-35, где
  перечисляли по памяти вместо грепа и пропустили `md_kind_label`. `dump.rs` лежит в `examples/`,
  которые `clippy --all-targets` компилирует (урок RN-18).

**Что делать:** перепроверить SHA **грепом по реальной истории** (`git log -S "CT-RFC-05" --oneline
--all`, `git log --oneline --all -- crates/contracts/`) и дополнить список мест правки полным
грепом, включая `examples/**` и `src/bin/**`. Затем повторный critic → reviewer → merge.

### 🔍 ВОСЬМОЙ случай лжи документа о коде — и пробел в защите

Счёт за сутки: `RK-I` завышено · `VN-I` занижено · §18 ложное «есть» · §10 ложное «пройдено» ·
`VB-I` 9→8 · `JR-I` 2→4 · «push-алертинга нет» при смёрженном алертинге · теперь **несуществующие
SHA в CT-RFC-05**.

**Пробел:** `verify_design_claims.sh` проверяет `docs/DESIGN.md`, но **НЕ покрывает `docs/rfc/**`**.
Ретро-документ прошёл бы любой автоматический гейт — его ловил только человек.

**Задача (высокий приоритет, дешёвая):** расширить `verify_design_claims.sh` на `docs/rfc/**` с
новой проверкой класса «**цитируемый SHA существует в истории**» (`git cat-file -e <sha>^{commit}`).
Это ловит F1 машинно и навсегда. Плюс проверка «перечисленные пути существуют» — ловит F2.
Обязателен self-test: документ с выдуманным SHA → FAIL; с реальным → PASS.

### ✅ CT-RFC-06 (`L2Delta`) — НАПИСАН, и он ОПРОВЕРГ посылку задачи

Ветка `docs/ct-rfc-06-l2delta` @ `cdd2dd6`, 366 строк, три инкрементальных коммита.

**ГЛАВНОЕ (§0.2): вариант `L2Delta` УЖЕ СУЩЕСТВУЕТ в T1** — пришёл с `CT-RFC-04` / M-18.
Мандат architect'а исходил из «спроектировать новый вариант `MdPayload`» — вводить нечего.

**Следствие: M-45 радикально проще, чем планировалось.** Вместо полного contract-пакета
(RFC + типы + схема + CHANGELOG + фикстуры + тесты + critic + **risk-critic**) остаётся
**расширение allow-list** — разрешить эмиссию дельт там, где сейчас пишутся снапшоты.
Без бампа `SCHEMA_VERSION`, **без RISK-BLOCK** (`crates/contracts/**` не меняется).

**Что документ дал сверх опровержения:**
- **Эпохи** решены через `epoch_id` — конкретный механизм против повторения **E-001**, где эпохи
  оказались машинно неразличимы на 123 млн событий;
- предложен инвариант **`JR-I-10` — якорная достаточность**: дельта не остаётся без снапшота,
  от которого зависит (закрывает риск «якорь съеден ретеншеном»);
- **`match`-ей ПЯТЬ, а не три** — карта влияния `research/measurements/m-45-l2delta-impact.md`
  была НЕПОЛНОЙ (собиралась грепом, но упустила два места);
- `TD-053` в контексте дельт закрыт тем же `JR-I-9` из M-50 — проверено, не предположено;
- §9 — что вынесено на решение founder'а.

**Дальше:** critic на документ → milestone на расширение allow-list с картой из ПЯТИ `match` +
`cargo build --workspace` в verify (узкий `-p` слеп к E0004, урок RN-8).

### 📌 Практика, подтверждённая экспериментом: инкрементальная фиксация
Первый заход Fable на этот же документ: 88k токенов, 11 действий, **ноль коммитов**, причина не
названа. Второй заход с требованием «первым действием закоммить скелет и пушить, дальше коммитить
каждые 2-3 раздела»: **три коммита, 366 строк, задача выполнена**.
**Для любой длинной задачи мандат обязан требовать промежуточной фиксации.** Ноль коммитов
объявляется провалом независимо от объёма размышлений.

### ⏳ Остаток Фазы 3
`TD-052` (пол не ограничен по времени) · `TD-030` (guard монотонности) · `TD-067` (доставка
`replay_digest` в прод).

---

## 📍 СЕССИЯ 2026-08-02 (вторая половина) — закрытие Фазы 3

Стартовое состояние: два агента прошлой сессии осиротели (сессия очищена). Подтверждено:
**осиротевший агент недостижим по `SendMessage`, но его работа не теряется** — она либо на
origin, либо в worktree. Reviewer'а забирать было нечего (`R-019` уже был на origin);
engine-dev продолжал работать в `/tmp/hft-dev-m52` ещё ~15 минут после осиротения и довёл
M-52 до конца. Приём, который стоит повторять: `git stash create` + тег
(`wip/<role>-<ms>-snapshot`) делает страховочный снимок чужого рабочего дерева **не трогая его**.

### ✅ CT-RFC-06 — APPROVED и закрыт (R-019 → правки → R-021)

`R-019` подтвердил суть (вариант в T1, пять `match`, RISK-BLOCK не нужен), но заблокировал по
пруф-гигиене: пруф-якоря жили на несмёрженных ветках. Закрыто: `research/measurements/**`
приземлены merge'ем в дерево RFC (авторство замеров сохранено), названы остаточные классы
(F4 эпохи, F5 невакуумность `JR-I-10`, F6 покрытие `DET-I-1`). Гейт документов —
`VERDICT: PASS` на **merge-цели**, не только на ветке.

### 🔍 ДЕВЯТЫЙ и ДЕСЯТЫЙ случаи лжи документа о коде — оба в СВОИХ ЖЕ процессных документах

1. **`NEXT-SESSION-PROMPT.md` §5 врал дважды в одном абзаце**: (а) «карта из пяти `match`
   применима к M-45» — не применима вовсе, M-45 не вводит нового варианта, E0004 не возникает;
   (б) «замер `m-45-l2delta-impact.md` НЕПОЛОН (три места)» — полон, пять мест, совпадает с
   грепом reviewer'а файл-в-файл. Расходился только номер строки `segments.rs` (2312 → 2566).
   Цена, если бы не проверили: dev-агент чинил бы пять мест, которые не ломаются.
2. **Гейт документов сам был fail-open** (`R-020` B-1): проверял SHA только рядом со словами
   «коммит/merge/мёрж/sha», поэтому «подтверждено **исправлением** `b3a5a95`» проходило молча —
   3 из 20 токенов реального корпуса, а на документе из выдуманных SHA печаталось «проверка
   неприменима». Гейт, построенный против `C-044`, воспроизводил `C-044`.

**Вывод, сильнее прежнего:** проверять замером надо не только `DESIGN.md`, но и промпты, планы
и сами гейты. Документ, описывающий проверку, — такой же кандидат на ложь, как документ,
описывающий код.

### 📌 M-45 — объём пересмотрен, раскатка вынесена на Границу C

Спека `milestones/M-45-persist-l2delta.md` (ветка `feat/M-45-spec`). Ключевое решение:
allow-list из хардкод-константы → конфиг с дефолтом `["BTCUSDT"]`. Это **развязывает merge и
раскатку**: код мержится, прод не меняется; включение — операторский шаг по подписи founder'а
(`PENDING-SIGNATURE` П-003: порядок vs Storage Box + состав символов). Прежняя постановка
требовала бы менять состав записываемых данных коммитом агента — то есть совершать решение
Границы C кодом.

Найдено замером при написании спеки: символ из wire нормализуется `to_uppercase()`
(`venue-binance/src/lib.rs:303`), а allow-list сравнивается точно ⇒ env в нижнем регистре
**молча выключил бы запись BTC**. Forward-only ⇒ невосстановимо. Закрыто оракулом O-4.

### 🟡 M-45 — РЕАЛИЗАЦИЯ ЗАКРЫТА, ГЕЙТ ЗЕЛЁНЫЙ, идёт tester (обновлено 2026-08-02T16:35Z)

**Ветка `feat/M-45` @ `6b3515c`.** `C-051` = NOTE. venue-dev отработал ПОЛНОСТЬЮ и завершился
(вернув пустой отчёт «-» — работа уцелела только потому, что была запушена; RN-19 в действии).

| коммит | автор | что |
|---|---|---|
| `23d921b` | venue-dev | задачи 2,3,3b — перп, `new_with_l2delta` |
| `c3b997c` | venue-dev | задачи 1,3,3c — спот, `SpotSession`/`SessionEffect`/`on_ws_text` (+420/−250) |
| `7a292f4` | venue-dev | clippy cleanup |
| `6b3515c` | architect | §Tasks ✅ DONE + запись мутационного контроля |

**Гейт прогнан architect'ом ЛИЧНО** (дисциплина «гейт в одну команду — сам»):
`bash scripts/verify_M-45.sh` → **`VERDICT: PASS`, exit=0, 21 PASS, ни одного FAIL**.
Переход RED→GREEN зафиксирован на реальных коммитах: `d75a1b7` → FAIL (10 нарушений),
`7a292f4` → PASS.

**Обратная мутация на ФИНАЛЬНОЙ реализации** (не на заглушке, в отличие от проб критика):
- `PROD_DEFAULT = "BTCUSDT,ETHUSDT"` ⇒ **T3 красный** (`0 passed; 1 failed`) — гейт ловит
  тихое расширение состава = совершение решения Границы C кодом;
- `l2delta_allow` принят и проигнорирован в конструкторе ⇒ **O-8 красный**
  (`2 passed; 4 failed`) — D-1 держится против правдоподобной реализации, не только пустой.
Обе откачены, дерево чистое.

**Структурная проверка грепом (то, ради чего было три REJECT):** `l2delta_event` вызывается
ровно один раз в каждом крейте — внутри `l2delta_emission_for`; `on_ws_text` делегирует туда
с `&self.l2delta_allow`; `if symbol == "BTCUSDT"` перед вызовом НЕТ; env читается ровно в
одном месте на крейт; литерал `"BTCUSDT"` остался только как `PROD_DEFAULT` внутри
`parse_capture_symbols`.

**Сейчас:** tester на чистом чекауте (`/tmp/hft-tester-m45`). Дальше — reviewer → merge в
main → §8 деплой-гейт.

### 🎯 СЛЕДУЮЩАЯ РАБОТА ПОСЛЕ M-45 — шардирование журнала (решение founder'а 2026-08-02)

**Выбрано founder'ом из трёх вариантов** (против «первого экрана Ф2» и техдолга TD-073..078).

**Что делать:** CT-RFC + RED-оракулы на раскладку журнала по инструменту (шард на инструмент
или группу) вместо единого хронологического потока. Обоснование целиком — `PENDING-SIGNATURE`
П-005/П-006.

**Три причины, почему именно это и почему сейчас:**
1. **Предусловие топ-300.** Founder хочет управлять составом динамически: входящие в топ
   добавлять, выпавших убирать, скамкоины удалять. «Удалить уже записанное» на монолитном
   журнале ломает `DET-I-1` и обещание продукта; на шардированном — это `rm` одного шарда
   плюс запись об удалении в мета-журнал (доказуемость сохраняется).
2. **Дешевеет только сейчас.** Журнал 27 ГБ. После полугода записи 300 инструментов та же
   операция = переписать терабайты.
3. **Замер (02.08): gateway читает журнал НАПРЯМУЮ** (`journal::list_segments`,
   `crates/gateway/src/lib.rs:2041`). Значит «первый экран», построенный до шардирования,
   пришлось бы переделывать — read-path зависит от раскладки. Порядок Ф2-после-шардирования
   экономит одну полную переделку.

**Гейт для этой работы — RAW (`gates.md` §1): critic на СИЛЬНОЙ модели**, мандат обязан
называть повышение явно. Правило закреплено `94d70c9` (`CLAUDE.md` + `gates.md`).

**Не блокировано Storage Box:** проектирование ведётся параллельно ожиданию (~09.08).

**Хронология гейта — самое ценное в этой работе:**

**Хронология гейта — самое ценное в этой работе:**
- `C-048` REJECT — набор доказывал СУЩЕСТВОВАНИЕ чистых функций, не их вызов; реализация
  могла оставить их осиротевшими и пройти всё зелёным.
- `C-049` REJECT — исправление тоже было грепом (вызов `l2delta_event` внутри
  `l2delta_emission_for`) и обошлось сдвигом хардкода на уровень выше:
  `if symbol == "BTCUSDT" { … }` ПЕРЕД вызовом.
- `C-050` REJECT — **класс дефекта закрыт, D-1 валидирован**; осталась узкая недоделка
  (O-8 был только для перпов, T5b для спота структурно невыполним) — устранено в `0804101`.

**Урок, который дороже самого milestone'а:** гейт, проверяющий ФОРМУ кода, всегда обходится
сдвигом гейта на уровень выше. Сколько уровней ни перечисли грепом — следующий останется
непокрытым. Проверять надо ПОВЕДЕНИЕ точки входа (решение D-1 в milestone-файле). Это то же,
что `RK-I-1` («компилятор — часть теста») и `INTG-I-*` («тест подтверждает ОТСУТСТВИЕ
альтернативного пути»), просто я два круга применял не тот инструмент.

**Второй урок:** нужный паттерн УЖЕ БЫЛ В ПРОЕКТЕ. `FuturesSession::on_ws_text ->
Vec<SessionEffect>` (sync, без сети/каналов) введён TD-014 специально для тестируемости.
Два круга я грепал то, для чего в соседнем крейте существовала готовая точка входа.
Перед изобретением способа проверки — искать, как это уже сделано рядом.

**Третий урок — RN-20: «git — факт» верно, но факт про ЖИВОГО агента — движущаяся мишень.**
Начало сессии 02.08 (продолжение): `git -C /tmp/hft-dev-m45 status` дал чистое дерево на
`d75a1b7`, и я заключил «venue-dev ничего не приземлил, запускаю заново». Через 15 минут в
том же worktree стоял коммит `23d921b` — dev прошлой сессии **работал всё это время**,
просто на момент снимка ещё не коммитил. Запущенный дубликат успел создать worktree и
перехватить локальную ветку `feat/M-45` (`git worktree add -B`) — убито и вычищено до того,
как он тронул файлы, но это была прямая заявка на два агента в одной зоне.

Признаки, которые НЕ работают как детектор живости:
- одиночный снимок `git status`/`git log` worktree — только «на этот миг», не «мёртв»;
- скан `/proc/*/cwd` по worktree — cwd процесса агента лежит в базовом чекауте, его
  Bash-вызовы транзиентны; отсутствие совпадения ничего не доказывает (то же, что урок про
  `pgrep` в дисциплине §9).

Что работает: **`stat -c %y` по файлам зоны агента + ПОВТОРНЫЙ снимок git через несколько
минут.** Свежий mtime или сдвинувшийся HEAD = агент жив, второго не запускать. Правило:
перед запуском агента на milestone, который прошлая сессия уже кому-то поручила, — сначала
доказать МОЛЧАНИЕ (два снимка с интервалом), а не однократную пустоту.

**Следующий шаг:** дождаться venue-dev (спот, задачи 1 и 3c) → `bash scripts/verify_M-45.sh`
лично → tester → reviewer → merge → §8 деплой-гейт.
**Merge разрешён founder'ом** при условии прохождения всего workflow; раскатка — по-прежнему
Граница C (`PENDING-SIGNATURE` П-003).

### ✅ Закрыто в этой сессии сверх Фазы 3
`TD-072` (оракул `det_9` — DET-I-1 на смешанном журнале, проверен обратной мутацией:
падает он, 8 старых остаются зелёными) · `TD-079` (гейт документов краснел на `main` с
merge M-52 — §22 заявляла JR-I 7/4, замер дал 12/7).

### ⚠️ Системная проблема сессии: агенты завершаются МОЛЧА
Пять случаев: три dev-агента на `det_22` (~190k токенов — задача была невыполнима, оракул врал,
и никто не сказал), Fable на `CT-RFC-06` (88k, ноль), плюс ранние chain-break'и без push.
**Ни один не вернул причину.** Мандаты прямо разрешали `SCOPE VIOLATION REQUEST` и требовали «не
завершаться молча» — не помогло.

Практика, которая работает: **если два агента подряд не дали результата — не запускать третьего, а
проверить задачу самому**. На `det_22` пять команд architect'а дали диагноз там, где три агента
не справились.

## 🩺 M-51 / `det_22` — ДИАГНОЗ ПОСТАВЛЕН (2026-08-02, architect лично)

**Три dev-агента подряд не справились, потратив ~190k токенов, и ни один не сообщил причину.**
Разбор architect'ом занял пять команд. Причина оказалась двойной.

### Причина 1 — оракул даёт ЛОЖНОЕ срабатывание

Оракул указывает на `crates/research-cli/src/export_io.rs:283`:
```rust
let mut keys: Vec<&(Venue, String)> = buckets.keys().collect();
keys.sort_by(|a, b| venue_sort_key(&a.0).cmp(...).then_with(|| a.1.cmp(&b.1)));
for key in keys { ... }
```
**Код КОРРЕКТЕН**: ключи собираются и СОРТИРУЮТСЯ до обхода; там же комментарий автора про
детерминизм (`RC-I-5`). Канарейка `det_22` видит первую строку (`.keys()`) и не смотрит, что
следом идёт `.sort_by()`.

### Причина 2 — задача трижды уходила НЕ ТОЙ РОЛИ

`crates/research-cli/**` — зона **research-dev**, а не engine-dev (`scope-guard.md`). Критик
предупреждал об этом ещё в `C-043` ([ADVISORY] «`export_io.rs` вне зоны engine-dev»). Architect
передавал предупреждение агентам («упрёшься — выдай SCOPE VIOLATION REQUEST»), но **не сделал
очевидного: не сменил роль исполнителя**. Все три агента упёрлись в scope-guard и **завершились
молча**, вместо SCOPE VIOLATION REQUEST, — хотя мандат это прямо разрешал.

### Попытка быстрого фикса оракула — ОТКАЧЕНА (важный урок)

Architect добавил в `det_22` распознавание паттерна «собрать + отсортировать»
(`collected_then_sorted`). Все 4 канарейки позеленели. **Мутационная проверка показала, что фикс
ОСЛАБИЛ оракул:** внесённое настоящее нарушение (`for (k, v) in m.iter()` без сортировки)
исходный оракул ЛОВИЛ (FAILED), а «исправленный» пропускал (ok). Фикс откачен, ветка чиста.

Это ровно та ошибка, от которой architect предостерегал каждого dev-агента в запретных списках:
**подгонка проверки под зелёный результат**. Поймана только потому, что мутация была обязательной
частью процедуры, а не «если останется время».

### Что делать дальше (для следующей сессии)

Развилка, решение — за architect'ом:
1. **Уточнить оракул аккуратно** — распознавать `collect()`+`sort` так, чтобы `for ... in m.iter()`
   ПО-ПРЕЖНЕМУ ловился. Обязательна мутационная проверка обоих направлений: грязная проба краснеет,
   чистая проходит. Быстрый вариант уже провалился — нужна точная реализация, не эвристика на глаз.
2. **Либо объявить источник** через существующий механизм (`waived`, см. `det_23`) — законно, так
   как порядок здесь доказуемо не виден (ключи сортируются), с ссылкой на строку сортировки.

⚠️ **Вариант 3 — «пусть research-dev перепишет корректный код» — НЕВЕРЕН**: код уже правильный,
менять его ради зелёного теста значит чинить исправное.

### Процессные выводы

- **Агент, упёршийся в scope-guard, ОБЯЗАН выдать SCOPE VIOLATION REQUEST, а не завершиться молча.**
  Трижды подряд этого не произошло. Мандат разрешал — не помогло; формулировку надо усилить.
- **ADVISORY критика о зоне — сигнал сменить РОЛЬ, а не предупредить исполнителя.** Диспетчеризация
  — обязанность оркестратора, а не исполнителя.
- **Оракул, на котором застряли три агента подряд, — подозреваемый №1.** Первым делом проверять
  оракул, а не наращивать попытки. Правило дешёвое: пятая команда дала диагноз, три цикла — нет.

## 🔬 M-51 (DET-I-1) — состояние на 2026-08-02, ОДНА точка до GREEN

Ветка `feat/M-51-determinism` @ `ff46c5c` (авторы: architect — оракулы, engine-dev — реализация).

**Сделано и запушено:** `journal::replay_digest` + `ReplayDigest` · `Books::iter_sorted`
(детерминированный обход вместо порядка `HashMap`) · `BacktestExchange.active` → `BTreeMap` ·
детерминизм retention-отчёта. Sacred-файлы (`crates/*/tests/**`, `scripts/verify_M-51.sh`) целы.

**Гейты пройдены:** critic `C-043` — **NOTE** (блокеров нет; независимо перепроверил compile-RED,
поведенческий RED `sim` 4/4, анти-плацебо, и главное — что канарейка источников **ловит реальные
находки, а не шум**). Две не-блокирующие находки: `research-cli/src/export_io.rs` в Allowed paths
вне зоны engine-dev (ADVISORY); канарейка не покрывает `ops`/`venue-*`/`gateway-serve`
(информационно, оракул трогать нельзя).

**ОСТАЛОСЬ РОВНО ОДНО:** `bash scripts/verify_M-51.sh` → `VERDICT: FAIL`, одна проверка:
```
cargo test -p journal --test red_det_sources
det_23 ok · det_24 ok · det_25 ok
det_22_no_unordered_hash_iteration_in_domain_code  FAILED
```
Текст оракула: «обход хэш-контейнера в доменном коде без объявленной причины — порядок задаётся
хэш-сидом процесса, то есть реплей журнала дважды даёт разный результат». **Конкретное место
(файл:строка) оракул печатает в своём stdout** — прогнать и посмотреть.

**Как чинить:** `BTreeMap`/`BTreeSet`, либо сортировка ключей перед обходом, либо объявление
через механизм, который признаёт оракул (см. `det_23_directory_iteration_is_declared`).
⚠️ **Объявлять источник безопасным ради зелёного теста ЗАПРЕЩЕНО** — законно только если порядок
доказуемо не виден ни в журнале, ни в проекциях, ни в выдаче. `DET-I-1` — P0-ворота продукта.

**Два инцидента процесса на этом milestone:**
1. **Chain-break №3 за две ночи** — dev сделал три корректных коммита и завершился без `push`;
   выложил architect по RN-19 (авторство `engine-dev` сохранилось, `git push` его не меняет).
2. **Второй dev-агент забутстрапился и не сделал НИЧЕГО** (9 действий, 0 коммитов, чистое дерево).
   Вероятная причина — диск был на 95%. Перед перезапуском чистить `target/` завершённых worktree.

**Дальше:** dev закрывает `det_22` → `verify_M-51.sh` PASS → reviewer → merge → §8 eyes-on.
После merge: TD-007 → CLOSED (стержневой инвариант закреплён впервые).

## ✅ ФАЗА 1 ЗАВЕРШЕНА (2026-08-01)

| Что | Итог |
|---|---|
| **Блок 1** — `docs/DESIGN.md` в редакции AlphaQuant | **STATUS: ACTIVE**, 25 разделов (§13–§23: модель данных, CQRS-проекции, multi-tenancy, read-path, L2Delta, Source Adapter, AI Runtime, права на данные, шов execution, инварианты платформы, SLA). П-001 ✅ ЗАКРЫТО |
| **`verify_design_claims.sh`** | в `main`, отдельным CI-джобом, с режимом `--merge-preview` |
| **Контрактные гейты** | в `main` (`verify_contracts.sh`, `verify_ct_rfc_atomic.sh`, `diff_contract_schema.sh` + self-test'ы) |
| §8 | CI зелёный с новым Contracts-джобом; deploy не триггерился (path-filter); прод healthy |
| Новый долг | TD-060…TD-064 (заведены reviewer'ом по ходу) |

**Цена Блока 1: восемь кругов гейтов.** `C-041` (critic) · `C-042` (risk-critic) · `R-004` · `R-011`
· `R-013` · `R-014` · машинный гейт дважды. Ни один не был холостым — каждый нашёл расхождение
документа с кодом.

### Семь лжей документа за сутки — и кто их поймал

| Заявлялось | Как есть | Кто |
|---|---|---|
| `RK-I` — 3 оракула | 0 (комментарии-аналогии в чужих крейтах) | risk-critic |
| `VN-I` — 0 тестов | суита M-41 из 40 тестов | critic |
| §18 — механизм существует | в коде пусто | reviewer |
| §10 — фаза P2.5 пройдена | не пройдена | reviewer |
| `VB-I` — 9 | 8 | **машина** |
| `JR-I` — 2, затем 3 | 4 (`JR-I-9` пришёл с M-50) | **машина + reviewer** |
| «push-алертинга нет вовсе» | `ops-watchdog` уже в `main` | reviewer (merge-превью) |

Первые четыре стоили по полному циклу ревью. Последние три машина ловит за прогон.

### Главный процессный урок Фазы 1

**Медленный гейт сам создаёт дефект, который потом ловит.** Пока документ проходил восемь кругов,
в `main` въехали M-50 и алертинг — и текст, правдивый на своей ветке, стал ложью в точке
приземления. Отсюда правило (`gates.md` §9): документ класса A проверяется на ДЕРЕВЕ СЛИЯНИЯ;
прогон на ветке необходим, но не достаточен; расхождение между прогонами — блокер.

## 🔬 ФАЗА 2 — В РАБОТЕ: оракулы `DET-I-1` (TD-007)

Ветка `feat/M-51-determinism`. Architect пишет RED-набор по аудиту
`research/measurements/td-007-determinism-coverage.md`.

**Почему это ценнее остального в очереди:** продукт продаёт ДОКАЗУЕМОСТЬ («каждая цифра на экране
выводится реплеем из журнала», `DESIGN.md` §0/§14 — проекции объявлены производными). Аудит
показал: совпадение инкрементального расчёта проекции с пересборкой реплеем **не проверено вовсе**.
Расхождение = цифра на экране не воспроизводится = торговое предложение неверно.

Главный оракул: инкремент в рантайме против пересборки с нуля, бит в бит. Плюс канарейка на
источники недетерминизма в доменном коде и прод-масштабная фикстура (форма прода снимается
командой, не по памяти).

Дальше: dev делает GREEN → reviewer → merge → §8.

## 🌙 ПЛАН НОЧИ 2 (утверждён founder'ом 2026-08-01: «да, выполни все это»)

Всё ниже проходит гейтами и НЕ требует участия founder'а: подпись на Блок 1 получена (П-001),
остальное подписи не требует. Founder спит; результаты утверждает утром.

### Фаза 1 — закрыть начатое (в работе)

| Работа | Ветка | Цепочка | Критерий готовности |
|---|---|---|---|
| **`verify_design_claims.sh`** | `feat/verify-design-claims` | self-test → reviewer → merge | ложь документа о состоянии кода ловится машинно |
| **Блок 1** (фикс `R-011` Б-1: §10 объявляет фазу P2.5 пройденной — не верно) | `docs/design-evolution` | фикс → **прогон нового гейта** → reviewer → merge + `PROPOSED`→`ACTIVE` | документ-канон в `main`, П-001 ЗАКРЫТ |
| **Контрактные гейты** (фикс `R-012` F-4) | `feat/contract-gates` | фикс → reviewer → merge | три гейта в CI |

**Порядок обязателен:** гейт документа мержится ДО Блока 1 и прогоняется по нему — чтобы поймать
ПЯТУЮ ложь до `main`, а не после. За сутки документ солгал о состоянии кода четырежды
(`RK-I` завышено · `VN-I` занижено · §18 ложное «есть» · §10 ложное «пройдено»).

### Фаза 2 — стержневой инвариант

**TD-007 (`DET-I-1`) — оракулы детерминизма.** Аудит собран
(`research/measurements/td-007-determinism-coverage.md`): реплей проверяется частично, проекции
на бит-идентичность не проверены вовсе. **Продукт продаёт доказуемость** («каждая цифра выводится
реплеем из журнала») — сейчас это утверждение не подкреплено оракулом.

Цикл: architect пишет RED → engine-dev GREEN → reviewer → merge → §8 eyes-on.

### Фаза 3 — если останется время, по убыванию ценности

1. **Ретро-документ `CT-RFC-05`** — governance-дыра: 49 упоминаний в коде, документа нет.
   Класс A ⇒ critic → reviewer.
2. **TD-052** — пол защиты ограничен по ПАМЯТИ, но не по ВРЕМЕНИ: в состоянии `Unknown`
   прочитывается весь каталог.
3. **TD-030** — машинный guard монотонности сегментов (условие, принятое reviewer'ом при
   закрытии M-49; покрыть и `readable_floor`).

### Чего в плане НЕТ намеренно

- **M-45 (`L2Delta`)** — CT-RFC довожу до «готово к подписи» и ОСТАНАВЛИВАЮСЬ: T1-изменение
  формата данных, решение founder'а.
- **Storage Box, `TELEGRAM_BOT_TOKEN`** — действия founder'а, кодом не закрываются.

### Дисциплина этих часов (напоминание себе после сжатия контекста)

1. Startup-протокол — первым делом, и себе, и КАЖДОМУ субагенту в мандате.
2. Каждый агент пишет результат ФАЙЛОМ и пушит; приземление проверяю командами git (RN-19).
3. Гейт в одну команду прогоняю ЛИЧНО, не через субагента.
4. Числа и статусы — замером, не переносом (4 нарушения за сутки, все мои).
5. Исправление по вердикту получает оракул ДО отправки в dev.
6. При необъяснимом красном — сначала `df -h /home` (ложный FAIL от переполнения уже был).
7. Состояние — в этот файл после КАЖДОГО завершённого шага.

## Как новая сессия поднимает работу (порядок действий)

1. Прочитать: `CLAUDE.md` → `.claude/rules/*` → `docs/SESSION-HANDOFF.md` → **этот файл** →
   `docs/PENDING-SIGNATURE.md`.
2. `git fetch origin && git branch -r` — посмотреть, какие ветки из таблицы ниже появились.
3. По каждой строке таблицы: если ветка есть — работа приземлилась, проверить и выполнить
   «что делать с результатом». Если ветки нет — агент не доехал, перезапустить по мандату.
4. **Ничего не мержить в `main`, если запись в `PENDING-SIGNATURE.md` требует подписи founder'а.**

---

## Запущено в ночь 31.07 → 01.08

| # | Задача | Куда пушит | Что делать с результатом |
|---|---|---|---|
| 1 | **critic**: аудит эволюции `DESIGN.md` | `docs/design-evolution`, файл `research/critiques/C-041-design-evolution.md` | PASS/NOTE → к reviewer. REJECT → architect правит находки, гейт повторяется |
| 2 | **risk-critic** (opus): не ослаблен ли риск-контур | `docs/design-evolution`, файл `research/critiques/C-042-design-evolution-risk.md` | KILL/CONCERNS блокируют merge. PASS → к reviewer |
| 3 | **контрактные гейты**: `verify_contracts.sh`, атомарный RFC-гейт, классификатор схем | `feat/contract-gates` | Прогнать самому, проверить анти-плацебо, затем reviewer → merge. Подписи founder'а НЕ требует (инструменты, не фазы) |
| 4 | **TD-053**: замер размеров событий | `research/td-053-event-size`, файл `research/measurements/td-053-event-size.md` | Если события > 64 KiB достижимы — severity в CRITICAL, milestone на фикс скана пола. Правку `TECH-DEBT.md` делает reviewer |
| 5 | **TD-007**: аудит покрытия `DET-I-1` | `research/td-007-determinism-coverage`, файл `research/measurements/td-007-determinism-coverage.md` | Architect проектирует недостающие оракулы (sacred-зона). Приоритет высокий: продукт продаёт доказуемость |

| 6 | **Алертинг** (engine-dev): детектор + форматирование + транспорт за трейтом | `feat/alerting` | Прогнать тесты, reviewer → merge. **Боевое включение = токен в env + строка в cron**, правки кода не потребует |
| 7 | **M-45 подготовка**: карта поломки `match`, объёмы, риски миграции | `research/m-45-impact` | Фактура для CT-RFC. **Сам CT-RFC и форму `L2Delta` проектирует architect** — делегированию не подлежит (T1) |

## Статус на 01:1x UTC (последнее, что успел зафиксировать architect)

| Поток | Ветка | Состояние |
|---|---|---|
| Блок 1 + гейты | `docs/design-evolution` @ `f329a54` | **critic и risk-critic отработали.** `C-042` = **CONCERNS**, обе BLOCKING-находки УСТРАНЕНЫ architect'ом (см. ниже). Осталось: вердикт critic'а `C-041` проверить, затем **reviewer** |
| Контрактные гейты | `feat/contract-gates` @ `b3b42d2` | приземлились, включая подключение к CI. **Прогнать самому и проверить анти-плацебо** |
| TD-007 | `research/td-007-determinism-coverage` @ `b5fc0ca` | приземлился — **прочитать первым: это стержневой инвариант продукта** |
| TD-053 | `research/td-053-event-size` | в работе |
| Алертинг | `feat/alerting` | в работе |
| M-45 | `research/m-45-impact` | в работе |

### Что устранено по вердикту risk-critic (`C-042`, CONCERNS)

**F-1 — ложное покрытие риск-семейства.** §22 заявлял «`RK-I`: 10 заявлено, 3 в оракулах».
Оракулов **ноль**: семь упоминаний `RK-I-*` в коде — комментарии-аналогии в чужих крейтах, два
из них ПРЯМО отрицают, что являются риск-гейтом, два лежат в `src/`, не в тестах. Цифру 3
architect перенёс из старого замера R9, не перепроверив. Контрольная проверка risk-critic'а:
таблица §22 точна на всех прочих семействах (`GW-I` 12/12, `SM-I` 10/10, `INTG-I` 0/0) —
единственное неверное число оказалось на риск-семействе, и оно было завышено.
Исправлено на **0 из 10** в §21 и §22.

**F-2 — исполняемые ворота входа в execution исчезли.** При вливании фаз AlphaQuant в §10
пропала строка с acceptance-критериями (RED-suite `RK-I-1..10` + `INTG-I-*` GREEN, падавшие на
заглушках; 48 ч testnet-MM; disconnect-drill без остаточных заявок) — осталась общая фраза «шов
предусмотрен». Восстановлено как **§21.1** таблицей из 6 проверяемых ворот, с честной пометкой
«текущее состояние: 0 из 6». Помещено в §21, а не в §10, намеренно: роадмап отвечает «когда»,
ворота — «при каком условии»; условие обязано переживать перестановку фаз, что уже едва не
стоило их потери.

## ⚠️ TD-053 — замер завершён, дефект ДОСТИЖИМ (приоритет повышен)

Ветка `research/td-053-event-size` @ `3efaabc`, файл `research/measurements/td-053-event-size.md`.

**Вывод замера (мерено через сам крейт `journal::read_all`, не наивным сканом байт):**
дефект не достижим на *наблюдаемой* нагрузке (макс. реальное событие — 68.8% порога 64 KiB),
**но архитектурно достижим уже сейчас, без всякого M-45**: собственный верхний предел
`L2Snapshot` (bucket-cap venue-binance) даёт кадр **66 032 B = 100.8% порога** без единой правки
кода; `L2Delta` не ограничен вообще и **уже эмитируется в проде** с M-18 (BTCUSDT, CLOSED 21.07).
M-45 не меняет верхнюю границу размера события, но увеличивает частоту попаданий в хвост
распределения.

**Последствие:** событие крупнее капа невидимо для скана пола ⇒ `readable_floor` занижен ⇒
операторская декларация проходит с `next_seq` внутри занятого диапазона ⇒ **seq-reuse**, то есть
ровно тот класс необратимой порчи, ради которого M-49 прошёл шесть кругов.

**Рекомендация замера:** поднять severity TD-053 с MINOR. Правку `TECH-DEBT.md` делает
**reviewer** (файл reviewer-owned) — передать при первом же ревью.

**Запущено немедленно (поток 8):** проектирование защиты, **модель Fable** — founder разрешил
дорогую модель на важные моменты проектирования этой ночью. Ветка `feat/M-50-floor-scan`:
контракт + RED-оракулы + `milestones/M-50-floor-scan-large-events.md` + `scripts/verify_M-50.sh`.
Ключевое требование мандата: **размен «терпимость против памяти» запрещён** — кап 64 KiB
существует как граница ПАМЯТИ (carry между чанками), простое поднятие до 64 MiB вернёт класс
TD-011. Оба свойства обязательны одновременно, как уже зафиксировано в M-49 rev5.
Реализация — dev по готовым оракулам; Fable только проектирует.

## Итоги ночи 31.07 → 01.08 (обновлено architect'ом перед исчерпанием контекста)

### Приземлилось и проверено

| Поток | Ветка | Итог |
|---|---|---|
| Блок 1 + оба гейта | `docs/design-evolution` | `C-041` **REJECT** (critic) и `C-042` **CONCERNS** (risk-critic). **Все находки HIGH/BLOCKING устранены** — см. ниже. Осталось: Ф3, Ф4 (не блокирующие) → затем **reviewer** |
| Контрактные гейты | `feat/contract-gates` | приземлились + подключены к CI. **Прогнать самому, проверить анти-плацебо** |
| TD-007 (`DET-I-1`) | `research/td-007-determinism-coverage` | **прочитать первым** — стержневой инвариант продукта |
| TD-053 | `research/td-053-event-size` | дефект **достижим уже сейчас** (`L2Snapshot` 66 032 B = 100.8% порога) |
| M-45 | `research/m-45-impact` | карта готова |

### Устранённые находки гейтов

**`C-042` F-1 (risk-critic)** — покрытие `RK-I` заявлено 3, фактически **0**. Исправлено.
**`C-042` F-2** — исполняемые ворота входа в execution исчезли при вливании фаз. Восстановлены
как §21.1, шесть проверяемых пунктов, честная пометка «состояние: 0 из 6».
**`C-041` Ф1 (critic)** — `VN-I` заявлен «0 тестов», фактически RED-суита M-41 из **40 тестов**
в 5 файлах, но лишь **2** помечены `VN-I-N`. Переписано как долг МАРКИРОВКИ, а не отсутствие
покрытия.
**`C-041` Ф2** — §14–§20 опирались на `/tmp/alphaquant.txt`, файл ВНЕ репозитория. Успели внести:
`docs/product/alphaquant-source.md` (454 строки). После очистки `/tmp` источник был бы утрачен
безвозвратно.

**Урок, зафиксировать в правилах при первой возможности:** F-1 и Ф1 — ЗЕРКАЛЬНЫЕ случаи одной
ошибки (покрытие завышено / занижено), обе цифры перенесены из старого замера R9 без
перепроверки. Таблица §22, вводящая правило «не выдумывать покрытие», нарушила его дважды в
разные стороны. Правило «форма снимается замером» (`testing.md`, закреплено утром 31.07)
относится и к ЧИСЛАМ в документах, не только к фикстурам тестов.

### Открытые находки `C-041` (не блокирующие merge, но закрыть до ACTIVE)
- **Ф3 (MEDIUM):** §11/§12 не тронуты веткой, но внутренне устарели рядом с новыми §9.1/§10.
- **Ф4 (LOW):** непоследовательная форма цитаты `AlphaQuant §N` внутри одного абзаца.

### M-45: карта поломки `match` (`research/m-45-impact` @ `6122fce`)
Три исчерпывающих `match` без `_ =>` сломаются на новом варианте `MdPayload` (E0004):
`recorder/src/lib.rs:69-79` (`md_kind_label`), `journal/src/segments.rs:2312-2323`
(`event_data_ts`), `sim/src/exchange.rs:222-283` (`on_event`).
⚠️ `md_kind_label` — **ровно тот файл, который M-35 пропустил**, перечисляя по памяти вместо
грепа. Метод аудита верен: проверено чтением каждого из ~90 файлов, включая `examples/**` и
`src/bin/**` (RN-18 — узкий `cargo test -p` их не компилирует).
CT-RFC и форму `L2Delta` проектирует **architect**, делегированию не подлежит.

### Ещё в работе на момент фиксации
`feat/alerting` (детектор + транспорт за трейтом) · `feat/M-50-floor-scan` (**Fable**, защита
от TD-053: контракт + RED-оракулы + milestone + verify).

## Готово и ждёт гейта

| Что | Ветка | Состояние |
|---|---|---|
| Блок 1 — эволюция `DESIGN.md` | `docs/design-evolution` (HEAD `3a5e473`) | 25 разделов, 888 строк, каркас §1–§12 цел, `RK-I-1..10` на месте, фазы AlphaQuant в §10, `DEFERRED`-шапки. **Ждёт: critic + risk-critic → reviewer → подпись founder'а (П-001)** |

## Следующее по очереди (после ночных)

1. Ретро-документ `CT-RFC-05` — нормативный текст, пишет architect (класс A → critic).
2. **Защита от Д1** — смена СЕМАНТИКИ при неизменной ФОРМЕ. Готового решения НЕТ, schema-валидатор
   не ловит по определению. Задача на проектирование, не на исполнение.
3. M-45 — подготовка запущена (поток 7); по её результату architect пишет CT-RFC.
4. Алертинг — запущен (поток 6); после merge остаётся токен и строка cron.

---

## Правила оркестрации, выведенные практикой (соблюдать)

1. **Мандат агенту обязан требовать: записать файл в репозиторий и запушить.** Read-only агенту
   нельзя поручать результат — ему нечего терять, теряет оркестратор (сгорело 189k токенов).
2. **Проверять приземление командами git, а не чтением отчёта** (RN-19). Одна попытка вернуть
   агента через `SendMessage`, при повторном провале — пушить самому из его worktree:
   `git push` не меняет ни автора, ни коммиттера, аудит-трейл цел.
3. **Гейты прогонять самому**, если это одна команда с коротким выводом. Два субагента подряд
   сожгли 350k токенов на прогон `verify_M-49.sh`, который выполняется одной строкой.
4. **Ветка на задачу, worktree на роль.** Общий чекаут не переключать.
5. **Стоп перед границей C.** Работа доводится до «готово к подписи» и останавливается.
