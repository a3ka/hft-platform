# Состояние оркестрации — передача незавершённой работы между сессиями

Ведёт architect. Обновляется при КАЖДОМ запуске/возврате агента.
Назначение: сессия architect'а обрывается по исчерпанию контекста, а работа продолжается.
Этот файл + `SESSION-HANDOFF.md` + `PENDING-SIGNATURE.md` — всё, что нужно новой сессии.

**Главный принцип, делающий перенос возможным: каждый агент ПИШЕТ и ПУШИТ результат в git.**
Отчёт в чат не принимается. Проверено дорогой ценой: за 31.07 канал возврата субагентов вернул
пустоту семь раз подряд (включая разведку на 189k токенов и два вердикта гейта). Всё, что было
записано в ветку, — уцелело; всё, что осталось «в отчёте», — исчезло.
Отсюда правило RN-19 (`gates.md` §8): **отчёт агента — гипотеза, состояние git — факт.**

---

## Как новая сессия поднимает работу (порядок действий)

1. Прочитать: `CLAUDE.md` → `.claude/rules/*` → `docs/SESSION-HANDOFF.md` → **этот файл** →
   `docs/PENDING-SIGNATURE.md`.
2. `git fetch origin && git branch -r` — посмотреть, какие ветки из таблицы ниже появились.
3. По каждой строке таблицы: если ветка есть — работа приземлилась, проверить и выполнить
   «что делать с результатом». Если ветки нет — агент не доехал, перезапустить по мандату.
4. **Ничего не мержить в `main`, если запись в `PENDING-SIGNATURE.md` требует подписи founder'а.**

---

## Запущено в ночь 31.07 → 01.08

| # | Задача | Куда пушит | Что делать с результатом |
|---|---|---|---|
| 1 | **critic**: аудит эволюции `DESIGN.md` | `docs/design-evolution`, файл `research/critiques/C-041-design-evolution.md` | PASS/NOTE → к reviewer. REJECT → architect правит находки, гейт повторяется |
| 2 | **risk-critic** (opus): не ослаблен ли риск-контур | `docs/design-evolution`, файл `research/critiques/C-042-design-evolution-risk.md` | KILL/CONCERNS блокируют merge. PASS → к reviewer |
| 3 | **контрактные гейты**: `verify_contracts.sh`, атомарный RFC-гейт, классификатор схем | `feat/contract-gates` | Прогнать самому, проверить анти-плацебо, затем reviewer → merge. Подписи founder'а НЕ требует (инструменты, не фазы) |
| 4 | **TD-053**: замер размеров событий | `research/td-053-event-size`, файл `research/measurements/td-053-event-size.md` | Если события > 64 KiB достижимы — severity в CRITICAL, milestone на фикс скана пола. Правку `TECH-DEBT.md` делает reviewer |
| 5 | **TD-007**: аудит покрытия `DET-I-1` | `research/td-007-determinism-coverage`, файл `research/measurements/td-007-determinism-coverage.md` | Architect проектирует недостающие оракулы (sacred-зона). Приоритет высокий: продукт продаёт доказуемость |

| 6 | **Алертинг** (engine-dev): детектор + форматирование + транспорт за трейтом | `feat/alerting` | Прогнать тесты, reviewer → merge. **Боевое включение = токен в env + строка в cron**, правки кода не потребует |
| 7 | **M-45 подготовка**: карта поломки `match`, объёмы, риски миграции | `research/m-45-impact` | Фактура для CT-RFC. **Сам CT-RFC и форму `L2Delta` проектирует architect** — делегированию не подлежит (T1) |

## Статус на 01:1x UTC (последнее, что успел зафиксировать architect)

| Поток | Ветка | Состояние |
|---|---|---|
| Блок 1 + гейты | `docs/design-evolution` @ `f329a54` | **critic и risk-critic отработали.** `C-042` = **CONCERNS**, обе BLOCKING-находки УСТРАНЕНЫ architect'ом (см. ниже). Осталось: вердикт critic'а `C-041` проверить, затем **reviewer** |
| Контрактные гейты | `feat/contract-gates` @ `b3b42d2` | приземлились, включая подключение к CI. **Прогнать самому и проверить анти-плацебо** |
| TD-007 | `research/td-007-determinism-coverage` @ `b5fc0ca` | приземлился — **прочитать первым: это стержневой инвариант продукта** |
| TD-053 | `research/td-053-event-size` | в работе |
| Алертинг | `feat/alerting` | в работе |
| M-45 | `research/m-45-impact` | в работе |

### Что устранено по вердикту risk-critic (`C-042`, CONCERNS)

**F-1 — ложное покрытие риск-семейства.** §22 заявлял «`RK-I`: 10 заявлено, 3 в оракулах».
Оракулов **ноль**: семь упоминаний `RK-I-*` в коде — комментарии-аналогии в чужих крейтах, два
из них ПРЯМО отрицают, что являются риск-гейтом, два лежат в `src/`, не в тестах. Цифру 3
architect перенёс из старого замера R9, не перепроверив. Контрольная проверка risk-critic'а:
таблица §22 точна на всех прочих семействах (`GW-I` 12/12, `SM-I` 10/10, `INTG-I` 0/0) —
единственное неверное число оказалось на риск-семействе, и оно было завышено.
Исправлено на **0 из 10** в §21 и §22.

**F-2 — исполняемые ворота входа в execution исчезли.** При вливании фаз AlphaQuant в §10
пропала строка с acceptance-критериями (RED-suite `RK-I-1..10` + `INTG-I-*` GREEN, падавшие на
заглушках; 48 ч testnet-MM; disconnect-drill без остаточных заявок) — осталась общая фраза «шов
предусмотрен». Восстановлено как **§21.1** таблицей из 6 проверяемых ворот, с честной пометкой
«текущее состояние: 0 из 6». Помещено в §21, а не в §10, намеренно: роадмап отвечает «когда»,
ворота — «при каком условии»; условие обязано переживать перестановку фаз, что уже едва не
стоило их потери.

## ⚠️ TD-053 — замер завершён, дефект ДОСТИЖИМ (приоритет повышен)

Ветка `research/td-053-event-size` @ `3efaabc`, файл `research/measurements/td-053-event-size.md`.

**Вывод замера (мерено через сам крейт `journal::read_all`, не наивным сканом байт):**
дефект не достижим на *наблюдаемой* нагрузке (макс. реальное событие — 68.8% порога 64 KiB),
**но архитектурно достижим уже сейчас, без всякого M-45**: собственный верхний предел
`L2Snapshot` (bucket-cap venue-binance) даёт кадр **66 032 B = 100.8% порога** без единой правки
кода; `L2Delta` не ограничен вообще и **уже эмитируется в проде** с M-18 (BTCUSDT, CLOSED 21.07).
M-45 не меняет верхнюю границу размера события, но увеличивает частоту попаданий в хвост
распределения.

**Последствие:** событие крупнее капа невидимо для скана пола ⇒ `readable_floor` занижен ⇒
операторская декларация проходит с `next_seq` внутри занятого диапазона ⇒ **seq-reuse**, то есть
ровно тот класс необратимой порчи, ради которого M-49 прошёл шесть кругов.

**Рекомендация замера:** поднять severity TD-053 с MINOR. Правку `TECH-DEBT.md` делает
**reviewer** (файл reviewer-owned) — передать при первом же ревью.

**Запущено немедленно (поток 8):** проектирование защиты, **модель Fable** — founder разрешил
дорогую модель на важные моменты проектирования этой ночью. Ветка `feat/M-50-floor-scan`:
контракт + RED-оракулы + `milestones/M-50-floor-scan-large-events.md` + `scripts/verify_M-50.sh`.
Ключевое требование мандата: **размен «терпимость против памяти» запрещён** — кап 64 KiB
существует как граница ПАМЯТИ (carry между чанками), простое поднятие до 64 MiB вернёт класс
TD-011. Оба свойства обязательны одновременно, как уже зафиксировано в M-49 rev5.
Реализация — dev по готовым оракулам; Fable только проектирует.

## Итоги ночи 31.07 → 01.08 (обновлено architect'ом перед исчерпанием контекста)

### Приземлилось и проверено

| Поток | Ветка | Итог |
|---|---|---|
| Блок 1 + оба гейта | `docs/design-evolution` | `C-041` **REJECT** (critic) и `C-042` **CONCERNS** (risk-critic). **Все находки HIGH/BLOCKING устранены** — см. ниже. Осталось: Ф3, Ф4 (не блокирующие) → затем **reviewer** |
| Контрактные гейты | `feat/contract-gates` | приземлились + подключены к CI. **Прогнать самому, проверить анти-плацебо** |
| TD-007 (`DET-I-1`) | `research/td-007-determinism-coverage` | **прочитать первым** — стержневой инвариант продукта |
| TD-053 | `research/td-053-event-size` | дефект **достижим уже сейчас** (`L2Snapshot` 66 032 B = 100.8% порога) |
| M-45 | `research/m-45-impact` | карта готова |

### Устранённые находки гейтов

**`C-042` F-1 (risk-critic)** — покрытие `RK-I` заявлено 3, фактически **0**. Исправлено.
**`C-042` F-2** — исполняемые ворота входа в execution исчезли при вливании фаз. Восстановлены
как §21.1, шесть проверяемых пунктов, честная пометка «состояние: 0 из 6».
**`C-041` Ф1 (critic)** — `VN-I` заявлен «0 тестов», фактически RED-суита M-41 из **40 тестов**
в 5 файлах, но лишь **2** помечены `VN-I-N`. Переписано как долг МАРКИРОВКИ, а не отсутствие
покрытия.
**`C-041` Ф2** — §14–§20 опирались на `/tmp/alphaquant.txt`, файл ВНЕ репозитория. Успели внести:
`docs/product/alphaquant-source.md` (454 строки). После очистки `/tmp` источник был бы утрачен
безвозвратно.

**Урок, зафиксировать в правилах при первой возможности:** F-1 и Ф1 — ЗЕРКАЛЬНЫЕ случаи одной
ошибки (покрытие завышено / занижено), обе цифры перенесены из старого замера R9 без
перепроверки. Таблица §22, вводящая правило «не выдумывать покрытие», нарушила его дважды в
разные стороны. Правило «форма снимается замером» (`testing.md`, закреплено утром 31.07)
относится и к ЧИСЛАМ в документах, не только к фикстурам тестов.

### Открытые находки `C-041` (не блокирующие merge, но закрыть до ACTIVE)
- **Ф3 (MEDIUM):** §11/§12 не тронуты веткой, но внутренне устарели рядом с новыми §9.1/§10.
- **Ф4 (LOW):** непоследовательная форма цитаты `AlphaQuant §N` внутри одного абзаца.

### M-45: карта поломки `match` (`research/m-45-impact` @ `6122fce`)
Три исчерпывающих `match` без `_ =>` сломаются на новом варианте `MdPayload` (E0004):
`recorder/src/lib.rs:69-79` (`md_kind_label`), `journal/src/segments.rs:2312-2323`
(`event_data_ts`), `sim/src/exchange.rs:222-283` (`on_event`).
⚠️ `md_kind_label` — **ровно тот файл, который M-35 пропустил**, перечисляя по памяти вместо
грепа. Метод аудита верен: проверено чтением каждого из ~90 файлов, включая `examples/**` и
`src/bin/**` (RN-18 — узкий `cargo test -p` их не компилирует).
CT-RFC и форму `L2Delta` проектирует **architect**, делегированию не подлежит.

### Ещё в работе на момент фиксации
`feat/alerting` (детектор + транспорт за трейтом) · `feat/M-50-floor-scan` (**Fable**, защита
от TD-053: контракт + RED-оракулы + milestone + verify).

## Готово и ждёт гейта

| Что | Ветка | Состояние |
|---|---|---|
| Блок 1 — эволюция `DESIGN.md` | `docs/design-evolution` (HEAD `3a5e473`) | 25 разделов, 888 строк, каркас §1–§12 цел, `RK-I-1..10` на месте, фазы AlphaQuant в §10, `DEFERRED`-шапки. **Ждёт: critic + risk-critic → reviewer → подпись founder'а (П-001)** |

## Следующее по очереди (после ночных)

1. Ретро-документ `CT-RFC-05` — нормативный текст, пишет architect (класс A → critic).
2. **Защита от Д1** — смена СЕМАНТИКИ при неизменной ФОРМЕ. Готового решения НЕТ, schema-валидатор
   не ловит по определению. Задача на проектирование, не на исполнение.
3. M-45 — подготовка запущена (поток 7); по её результату architect пишет CT-RFC.
4. Алертинг — запущен (поток 6); после merge остаётся токен и строка cron.

---

## Правила оркестрации, выведенные практикой (соблюдать)

1. **Мандат агенту обязан требовать: записать файл в репозиторий и запушить.** Read-only агенту
   нельзя поручать результат — ему нечего терять, теряет оркестратор (сгорело 189k токенов).
2. **Проверять приземление командами git, а не чтением отчёта** (RN-19). Одна попытка вернуть
   агента через `SendMessage`, при повторном провале — пушить самому из его worktree:
   `git push` не меняет ни автора, ни коммиттера, аудит-трейл цел.
3. **Гейты прогонять самому**, если это одна команда с коротким выводом. Два субагента подряд
   сожгли 350k токенов на прогон `verify_M-49.sh`, который выполняется одной строкой.
4. **Ветка на задачу, worktree на роль.** Общий чекаут не переключать.
5. **Стоп перед границей C.** Работа доводится до «готово к подписи» и останавливается.
