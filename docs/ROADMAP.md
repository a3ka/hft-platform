# ROADMAP — порядок работ и точка продолжения

**Это единственный источник ПОРЯДКА работ.** Читается первым в стартовом протоколе
(`CLAUDE.md`). Отвечает на один вопрос: **с чего продолжать и почему именно с этого.**

Чем он НЕ является: не состояние (`PROJECT-STATE.md`), не долг (`TECH-DEBT.md`), не фазы
продукта (`DESIGN.md` §10), не журнал сессий (`docs/workflow/session-handover-*.md`).
Те отвечают «что есть», «что сломано», «куда идём», «что произошло». Здесь — «что дальше».

---

## Владение — ДВА хозяина, граница узкая

| что | кто | основание |
|---|---|---|
| перечень, ПОРЯДОК, «почему здесь», заведение и снятие пунктов | **architect** | порядок есть проектное суждение |
| **ТОЛЬКО колонка «Состояние»** | **reviewer** | закрытость — его суждение, он один пишет `PROJECT-STATE.md` |

Это перенос заведённого, а не новое правило: `04-workflow.md` §Close-out уже разводит роли
ровно так («закрытость называет reviewer, переезд делает architect»), а `scope-guard.md`
§«Milestone-файлы» уже даёт dev'у carve-out РОВНО на колонку статуса и ни на что больше.

**Сделано сверх плана 07-08.09 (механизмы, уже в `main`):** барьер «ключи не живут в
репозитории» (`check_secret_material.sh` + проба 16 сценариев + джоб в агрегате) —
исполнение требования founder'а; этот роадмап и его барьер синхронизации.

**Барьер:** `scripts/check_roadmap_sync.sh` — коммит, уносящий спеку милестоуна в
`docs/archive/`, обязан в ТОМ ЖЕ диапазоне тронуть этот файл. Иначе закрытие предмета
проходит молча, и роадмап протухает — ровно то, что случилось с пятью предыдущими
документами (замер ниже).

**Предел барьера назван, а не изображён:** он ловит ОТСУТСТВИЕ правки, а не её
осмысленность. Строка, обновлённая неверно, пройдёт. Против этого работает круг гейта.

---

## Замер, из-за которого файл заведён (2026-09-07)

На вопрос «что дальше» отвечали ШЕСТЬ документов, ни один не главный:

```
docs/DESIGN.md            1048 строк   правка 2026-08-30
docs/09-roadmap-v2.md      328 строк   правка 2026-08-19
milestones/BACKLOG.md      585 строк   правка 2026-08-31
docs/SESSION-HANDOFF.md    557 строк   правка 2026-08-29   ← зовётся «живым индексом»
PROJECT-STATE.md          2352 строки  правка 2026-09-07
TECH-DEBT.md              3956 строк   правка 2026-09-07
$ ls docs/workflow/session-handover-*.md | wc -l
23
```

Преемственность фактически жила в 23 записках сессий, каждая — пересказ памяти предыдущей.
Цена: план из 11 пунктов от 2026-09-05 содержал **пять ошибок**, вскрытых аудитом
2026-09-07 (`docs/plans/audit-2026-09-07-*.md`): первый пункт не готов, оповещения ждали не
founder'а, крупный долг отсутствовал, фазы шли не в объявленном порядке, многопоточность
описана преувеличенно. То есть отсутствие роадмапа стоило **одного аудита на сессию**.

---

## ПЛАН

Порядок — от того, без чего остальное теряет смысл, к тому, что даёт продукту ценность.
Состояние снимается КОМАНДОЙ, а не памятью: колонка «Состояние» — единственное, что вправе
править reviewer, и только после merge и деплой-гейта (`gates.md` §8).

### Блок 1 — снять часы с диска

Локальный диск заполняется, потому что удаление старого выключено; выключено правильно —
по `П-023` его включает только доказанная читаемость копии. Ёмкости при этом достаточно с
запасом: холодное хранилище 5 ТБ, занято 207 ГБ, копия свежая и полная (замер 2026-09-07).
**Проблема не в месте, а в недоказанной читаемости.**

| № | Предмет | Что сделать | Где | Владелец | Состояние |
|---|---|---|---|---|---|
| 1 | **M-74** restore-drill | ✅ **ЦЕЛЬ ДОСТИГНУТА 2026-09-08 РУЧНЫМ ПРОГОНОМ** на настоящей копии (`milestones/M-74-restore-drill.md` §0quater). Отбор по правилу спеки: 593 индекса → первый/средний/последний (15 июля / 17 августа / 8 сентября). Предъявлены ТРИ свойства, а не одно: прочитано 2 170 780 событий; повторное чтение дало ТОТ ЖЕ отпечаток; порча в 32 байта поймана отказом `rc=4`. **Остаётся ДОЛГОМ автоматизация** — повторяемость (проверена сегодняшняя копия, завтрашняя не проверена ничем) и наблюдаемость (метрика `backup_restore_drill_ok` не эмитится). Задачи 2/2b/3/4 НЕ СДЕЛАНЫ, `verify_M-74.sh` красен честно: милестоун ДОСТИГ ЦЕЛИ, но не выполнен как набор задач. Остаток выносится отдельным предметом ДРУГОМУ исполнителю вместе с двумя открытыми находками `C-219` (пропуск sidecar'ов; гонка при переименовании источника). Кругов гейта по этому набору больше не назначается — их было шесть | `docs/M-73-closeout-architect` | architect → передача | 🚧 **приём СОСТОЯЛСЯ** (`П-028`, 08.09) — цель принята. **НЕ ЗАКРЫТ:** задачи 2/2b/3/4/5 не сделаны, `verify_M-74.sh` красен честно и в `main` его нет. Остаток — `TD-193` (**ЧАСТИЧНО**, не закрыта) + находки `C-219` `B-1`/`B-2`; ждёт переноса другому исполнителю |
| 2 | включение удаления журнала — **КОНСТРУКЦИЯ РЕШЕНА ЗАМЕРОМ 2026-09-08** | **Механизм ИСПРАВЕН, не хватало ДВУХ МОНТИРОВАНИЙ.** Прогон вхолостую с подключённым томом чекпоинтов (побочных эффектов ноль — гарантия `RetentionMode::DryRun`, оракул `r2_dry_run_touches_nothing`) дал ОСМЫСЛЕННЫЙ план: 187 сегментов в `offload_and_prune`, отказ «checkpoint coverage artifact absent» ИСЧЕЗ. Значит правки кода НЕ ТРЕБУЕТСЯ — требуется топология.<br>**Что делать (по замеру, а не по рассуждению):** (1) смонтировать `gateway-ckpt:/ckpt:ro` в сервис `journal-retention` — **механической защиты у этого класса в `main` НЕТ**: барьер `scripts/check_retention_composition.sh` ПОСТРОЕН, но лежит на ветке `harness/retention-composition`, ни одним джобом не зовётся и краснеет на текущем компоузе; класс остаётся `COGNITIVE-ONLY` до вливания барьера ВМЕСТЕ с монтированием (`gates.md` §11: правило, которое механизировать нечем, объявляет себя `COGNITIVE-ONLY` вместо того, чтобы изображать гейт). Прежняя редакция говорила «барьер держит этот класс» — настоящее время о защите, которой в `main` нет (`R-177` `К3.5`); (2) смонтировать холодное хранилище в `JOURNAL_COLD_DIR` **ТОЛЬКО НА ЧТЕНИЕ**.<br>**Почему именно RO — проверено исполнением:** `verify_cold_copy` зовёт `create_dir_all(cold)`, и на СУЩЕСТВУЮЩЕМ каталоге это no-op даже при RO-монтировании; копирование случается ТОЛЬКО если файла в холодном корне нет. Отсюда семантика ровно та, что нужна: **файл есть ⇒ сверка `sha256` обеих сторон ⇒ локальный prune; файла нет ⇒ запись отклонена ⇒ fail-closed, локальная копия остаётся.** Единственным писателем холодной копии остаётся `journal-offsite-cron.sh`, и уборщик физически не может её повредить. Прежний план (RW-монтирование) этого свойства не давал.<br>**Мина, которая была:** `/mnt/journal-cold` — пустой ЛОКАЛЬНЫЙ каталог, поэтому выгрузка копировала бы сегменты на ТОТ ЖЕ диск перед удалением. RO-монтирование настоящего хранилища снимает и её | прод, `docker-compose.yml`, `deploy/**` | engine-dev → **founder** (режим) | ⏳ два монтирования; дефект заведён в реестр — `TD-202` (MAJOR, built-not-wired), воспроизведён на проде 09.09 |
| 2bis | ✅ **МЕСТО НА ДИСКЕ — ВОПРОС СНЯТ 2026-09-08** | Освобождено 57 ГиБ разовым ручным удалением: 27 → 84 ГБ, занято 42 %; запас из ~4 суток стал ~5 месяцев. **Покупка железа не требуется.** Предусловия предъявлены поимённо: 392 сегмента старше 14 суток; **пофайловая** сверка `sha256` с хранилищем 392 из 392 без расхождений (первая, по множествам сумм, забракована самим автором — при перепутанных именах прошла бы); покрытие чекпоинтом выше диапазона; активный сегмент не тронут; осталось 203 при `keep-min=4`. После удаления предъявлено ИСПОЛНЕНИЕМ, что журнал читается: индексы 393..595 без дыр, чекпоинтер прошёл насквозь `exit=0` и сдвинул покрытие. Аудит-след — `/var/lib/hft/prune-audit/pruned-2026-09-08.{list,sha256}`. **Удаление было РАЗОВЫМ:** без п.2 вопрос вернётся примерно через две недели | прод | architect (исполнено) | ✅ снято |
| 3 | **M-78** порог тревоги ≠ порог отказа | сегодня алерт срабатывает В МОМЕНТ остановки записи (`TD-176`) | новый | architect → dev | ⏳ OPEN |
| 4 | **M-79** оповещения на живой путь | код есть, запускателя нет — «построено-не-подключено» | новый | architect → dev | ⏳ OPEN |
| 5 | токены канала на сервер | только после п.4 | прод | **founder** | ⏳ ждёт п.4 |

### Блок 2 — перестать врать самим себе (можно параллельно блоку 1)

| № | Предмет | Что сделать | Где | Владелец | Состояние |
|---|---|---|---|---|---|
| 6 | close-out `M-73` + уборка веток | влить 2 готовых коммита; снести 2 ветки, не несущие ничего сверх `main` | ветка + origin | architect | ⏳ OPEN |
| 7 | **M-80** реестр долга к одному источнику | 127 карточек, 21 протухшая, ДВА несогласованных источника статуса | новый | architect → reviewer | ⏳ OPEN |
| 8 | **M-81** описания модулей к коду | §T называет сторожей, которых нет: у `journal` **8 из 8**. Это ярус B, обязательное чтение ДО работы | новый | architect | ⏳ OPEN |

### Блок 3 — вернуть продукт в движение

| № | Предмет | Что сделать | Где | Владелец | Состояние |
|---|---|---|---|---|---|
| 8bis | **M-84** фиксированные семь полос глубины | **РЕШЕНИЕ FOUNDER'А 2026-09-10 — `П-029`.** Сервер ВСЕГДА считает продуктовый набор `1.5/3/5/8/15/30/60 %`, обе стороны; клиент НЕ задаёт сетку, а выбирает, ЧТО ОТОБРАЖАТЬ. UX не меняется: «15 % BID» — галочка отображения, а не заказ вычисления. **Зачем:** набор полос входит в отпечаток селектора (`crates/gateway/src/lib.rs:3449`), поэтому разные наборы = разные расчёты одного и того же стакана ⇒ стоимость растёт с числом ПОЛЬЗОВАТЕЛЕЙ. Фиксированный набор убирает ОДНУ из ШЕСТИ осей отпечатка (шестая — `depth_cadence_ms`, с `M-68`) (`timeframe_ms` и `window_ms` клиент присылает сам) и немедленно даёт ОБЩИЙ ЧЕКПОИНТ — имя слепка детерминировано отпечатком. Это НЕОБХОДИМОЕ условие `П-010`, а не его исполнение: одного расчёта на многих не будет до разделяемой проекции (п.13). Закрывает `TD-198` исчезновением предмета: число полос больше не задаётся клиентом, предел не нужен. **Цена названа заранее:** считаем и то, что не смотрят (выгодно примерно с двух зрителей); отпечаток меняется ⇒ старые чекпоинты недействительны. **ДВА ЖИВЫХ ПРЕДУСЛОВИЯ ВКЛЮЧЕНИЯ:** `TD-159` (OPEN, MAJOR — метка достоверности одна на точки разного качества; дыра достижима ровно в момент смены `GATEWAY_BANDS`) и **ОТСУТСТВИЕ БАРЬЕРА, удерживающего эмиссию до восстановления глубины** (`docs/fa/viz-backend.md:158-162`): после старта и любого разрыва книга пересобирается REST-снимком с жёстким пределом (~1.3 % spot BTCUSDT), расширенные полосы в эти окна отдают ЗАНИЖЕННЫЕ числа, а метка молчит — тихая деградация в опасную сторону. Каденция (`M-68`) — ИСПОЛНЕННЫЙ контекст, а не живое предусловие. Прод сегодня считает ОДНУ полосу. **Цена перехода НЕ ИЗМЕРЕНА** — замер на прод-форме стакана обязателен ДО спеки. Контракт выдачи меняется ⇒ критик обязателен | новый | architect → dev | ⏳ спека |
| 9 | **M-70** полосы глубины | зелёные `protected-artifacts` и `gate-meta` на ДЕРЕВЕ СЛИЯНИЯ (оба — зона architect'а) | `docs/M-70-rev2` | architect → reviewer | 🟢 **КРУГ ГЕЙТА 6 — `R-187` APPROVED, СЛИТО 2026-09-18** (PR #193, `a54c7d5`). **Оба блокера `R-186` сняты, и один из них — МОЙ СОБСТВЕННЫЙ ДЕФЕКТ ВОСПРОИЗВЕДЕНИЯ:** дерево слияния, собранное базой-ВЕТКОЙ, и дерево, собранное базой-`main` (как собирает GitHub), **бит-идентичны по содержимому** и дают противоположные вердикты `check_protected_artifacts` — `exit=1` против `exit=0`. Механика — `mainline` барьера есть база плюс цепочка ПЕРВЫХ родителей (`:150-154`): при обратном порядке в магистраль попадают 63 коммита ветки, 23 из них написаны ДО переезда `M-75` в архив ⇒ fallback запрещён ⇒ FAIL. Порядок GitHub проверен на шести мержах: у ВСЕХ первый родитель — прежняя вершина `main`. **Инструмент проекта делает это правильно, а ручная сборка — нет:** `verify_design_claims.sh --merge-preview` (`:178-186`) создаёт worktree ИЗ base-ref и вливает HEAD. `gate-meta` снят токеном `ALLOW-SUBJECT-CHANGE` — подтверждено МУТАЦИЕЙ (тело без строки токена → `exit=1`). **Поправка, записанная в вердикт:** `ALLOW-ARTIFACT-DELETE` барьер (1) НЕ гасит — токен читается только в цикле по коммитам, которые путь УДАЛЯЛИ; в верном порядке fallback находит настоящих удаляющих `70ca5c8`/`8562a06` с их СОБСТВЕННЫМИ токенами, а снятие токена мутацией оставляет барьер зелёным. Развязку не там искал `R-186`. **ЦЕЛЬ МИЛЕСТОУНА НЕ ДОСТИГНУТА:** полосы НЕ включены — прод-дефолт остался узким (`0.001`, вариант B), задача 7 переопределена в СТОРОЖА отложенного включения, подпись `П-014` п.4 за founder'ом. Закрыты `TD-159`, `TD-161`, `TD-198`. **Деплой-гейт §8 в форме §5 (пять пунктов) — четыре из пяти.** П.4 дал число, которого не было ни у кого, включая бамп `M-68`: холодная пересборка слепка после бампа схемы 9→10 — **≈22 мин 46 с** (16:30:19 → 16:53:05, журнал 67 ГБ / 321 сегмент; инкрементальные прогоны — 1-2 с), со стороны пользователя ≈23 минуты недоступности кокпита. **П.2 нашёл MAJOR сверх предмета — `TD-206`:** прод-кадр 5 052 685 Б против предела 2 000 000 Б, выдача отвергается fail-closed, клиент не получает НИЧЕГО; доминирует volume profile (194 641 бакет), `depth_rows=2`; мержем НЕ вызвано (диф не трогает ни прод-код VP, ни константу). П.3 (sanity метки) поэтому физически недостижим. Три liveness-проверки при этом ЗЕЛЕНЫ — класс `TD-011`/`TD-031`. Заведены `TD-206` (MAJOR), `TD-207` (MAJOR — штатный зонд §8 не аутентифицируется против прод-формы секрета), `TD-208` (MINOR), `TD-209` (замер). **Дальше: close-out милестоуна за architect'ом; `TD-206` — блокер кокпита, вперёд предмета** |
| **9-A** | **`TD-206` (MAJOR) — кадр перерос подписанный предел; кокпит не получает НИЧЕГО** | **ПЕРВЫЙ ПО ПОРЯДКУ ВО ВСЁМ БЛОКЕ.** Замер на проде 18.09, подтверждён в логе: `response exceeds limit: limit=2000000 bytes, observed=5052685 bytes (heatmap=5256 cob=16 vp_bins=194641 bubbles=407 ohlcv=59 depth_rows=2)`. Подключение и авторизация проходят, КАДР ОТВЕРГАЕТСЯ — клиент не получает ни одного сообщения. **Причина НЕ полосы** (их вклад `depth_rows=2`), а профиль объёма: 194 641 корзина, копится all-time, окно ему никто не ставил. Предел перейдён САМ, без единой правки кода. **Худшее — незаметность:** все три признака жизни `gates.md` §8 зелены (контейнер `healthy`, heartbeat свежий, журнал растёт). Класс `TD-011`/`TD-031`. Развилка закрывается ЗАМЕРОМ, не вкусом: окно профиля / огрубление корзин по цене / постраничная выдача / поднятие предела с ПЕРЕСЧЁТОМ `DESIGN` §16 (10 000 сессий × размер кадра). Оракул обязан мерить РАЗМЕР СОБРАННОГО КАДРА на прод-форме и падать ДО предела: признак «вернулся `Err`» НЕГОДЕН — он возвращается и сегодня, впустую. Плюс отдельный сторож ОТСУТСТВИЯ доставки — сегодня «кокпит молчит третьи сутки» не видит никто. **ДОМ ПРЕДМЕТА — `M-86`** (спека + RED + гейт закоммичены 2026-09-18, ветка `feat/M-86-vp-bin-width`, базовая линия гейта снята КРАСНОЙ до работы dev-а). Форма решена ЗАМЕРОМ (`docs/plans/vp-frame-size-measurement-2026-09-18.md`) и решением founder-а: фиксированная ценовая сетка, шаг 0.25 USD. Сторож отсутствия доставки ВЫДЕЛЕН отдельной строкой 9-A-bis — он про другую подсистему | `M-86` в `main` | architect → dev | ✅ **ЗАКРЫТО 2026-09-20.** Код в `main` (PR #198, `46cd25c`, `R-188` APPROVED) + на проде; close-out architect'а влит (PR #200, `63854d4`); финальный гейт и решение по долгу — `R-189` APPROVED. **`TD-206` ЗАКРЫТ ПОЛНОСТЬЮ** по ВТОРОМУ замеру — зрелая 16.5-часовая UTC-сессия, прод-форма вызова, перемерена reviewer'ом независимо: кадр **227 579 Б = 0.114× предела**, **4 810** корзин, **0** не на сетке, `poc`/`vah`/`val` — ключи сетки, отказов `exceeds limit` за 24 ч — **0**. Зонд у 23:59 UTC признан НЕ нужным: плотность заполнения сетки 0.999 ⇒ корзины ограничены ХОДОМ ЦЕНЫ, а не временем (`корзин ≤ ход/0.25 + 1`; до предела нужно ≈18 500 USD хода при пройденных 1 204). Остаточный класс — отдельная живая карточка `TD-210` (её запас исправлен находкой `R-189` `F-1`: ≈3.6×, а не 6.8×), сторож отсутствия доставки — строка 9-A-bis. Долг merge'а: `TD-210`…`TD-213`. **Остался один шаг, зона architect'а:** переезд спеки и гейта в `docs/archive/` — разрешение выдано `R-189` §5 |
| **9-A-bis** | **сторож ОТСУТСТВИЯ доставки — счётчики попыток/успехов/отказов выдачи и правило «попытки есть, успехов ноль за окно»** | **Заведён отдельно от `M-86` осознанно, и причина названа, чтобы предмет не растворился:** у `gateway-serve` нет ни метрик, ни heartbeat (замер 2026-09-18: `grep -niE "metrics\|heartbeat\|prometheus" crates/gateway-serve/src/lib.rs` даёт только комментарии об удалённом WS-кадре), то есть это другая подсистема (`crates/ops`, реестр метрик, правила алертов) и другой гейт; держать её внутри `M-86` значит задержать оживление кокпита на круги, предмету не нужные. **Цена отсутствия измерена:** авария `TD-206` прожила незамеченной около месяца при всех трёх зелёных признаках жизни `gates.md` §8. Пассивных счётчиков НЕДОСТАТОЧНО, если спроса нет вовсе — правило обязано различать «никто не просил» и «просили, не получили»; активная проба (cron-подключение) — кандидат, упирающийся в `TD-207`. Класс `OPS-I-8` («жив, но не работает») уже объявлен для recorder-а и на выдачу НЕ распространён | новый | architect → dev | ⏳ OPEN |
| **9-B** | **замер окна недостоверной метки** | дёшево (порядок часа) и снимает последнюю неопределённость перед включением полос. Метка считается от `book::max_reach_pct` (`crates/book/src/lib.rs:287-297`) — расстояния до КРАЙНЕГО уровня, а не от полноты книги; ветка `not-observed` только при `band > reach` (`crates/gateway/src/lib.rs:2248`). После ресинка книга плотна лишь до REST-охвата (~1.3 % spot BTCUSDT), но ОДНО дальнее обновление поднимает охват, и промежуточные полосы получают `liveness=confirmed` на суммах по пустой середине (`R-185` §1, зонд 1). **Мерить:** долю времени после перезапуска recorder'а, когда сумма дальней полосы ниже установившейся более чем на X %. По числу — либо чинить СЛОВАРЬ метки (различать «наблюдено полно внутри REST-охвата» и «накоплено диффами с момента ресинка»), либо принять окно как цену | новый | architect | ⏳ OPEN |
| **9-C** | **включение семи полос** — одна строка | `П-014` п.4 ПОДПИСАНА и НЕ ИСПОЛНЕНА: прод-дефолт `GATEWAY_BANDS` остаётся `0.001` (проверено по всей истории `main` — канонического набора там не было НИ РАЗУ; `docker inspect` на проде → `0.001`). Включение = строка оператора в host `.env`; шаг гейта `task #7` архивной спеки `M-70` — СТОРОЖ отложенного решения, краснеет на расширении дефолта. **Бессмысленно до 9-A:** кадр не доезжает целиком, семь полос добавят ~20 КБ к пяти мегабайтам. **После 9-B** — решение принимает founder С ЧИСЛОМ на руках (граница C, `gates.md` §0.1); без числа вопрос задавать запрещено `П-029` («выдуманное число хуже отсутствующего») | прод, `deploy/**` | **founder** → engine-dev | ⏳ ждёт 9-A и 9-B |
| 9bis | **M-85** кадр `frames_since` строится ОТ КНИГИ | ✅ **ЗАКРЫТ 2026-09-17.** Снял внешний блокер п.9. Код в `main` (`3f399ec`, `R-183` APPROVE), прод проверен. Спека и гейт переехали в `docs/archive/` по `04-workflow.md` §2 — приёмка состоялась, после merge гейт не исполняет никто. Путь гейта прошёл ТРИ круга критика и арбитраж `A-034`; предмет был здоров с первого дня, круги ушли на согласование описаний с кодом | `docs/archive/` | — | ✅ закрыт |
| 9ter | **`TD-205`** судьба пути `frames_since`: он линеен по ДЛИНЕ ИСТОРИИ | решение ПРОЕКТНОЕ и моё: (а) бутстрап книги из чекпоинта, (б) объявить путь offline-only со своим оракулом, (в) снять вместе с `frames_msgs`. Замер ОБЯЗАН предшествовать выбору и обязан мерить РАБОТУ детерминированным счётчиком (посещённые события), **не wall-clock**: `testing.md` §«Целостность гейта» св-во 2 — исход, зависящий от хоста, есть флак; `red_m77_pump_cost.rs` уже отверг время по той же причине (`TD-078`). Формулировка карточки `TD-205` («оракул обязан мерить ВРЕМЯ») этому противоречит — поправка передана reviewer'у, карточка его зона. Прод не задет: прод-вызовов у пути ноль | новый | architect | ⏳ OPEN |
| 10 | **TD-201** сторож буфера `book_series` | хвост `M-77`; тот же код, что п.9 | там же | architect | ⏳ OPEN |
| 11 | **M-82** цена такта ≠ f(размер книги) | `TD-200`, ДОРОЖАЕТ: чем реже якорь, тем длиннее дельта-хвосты | новый | architect → dev | ⏳ OPEN |
| 12 | ~~**M-83** предел числа полос в запросе~~ | **ОТКЛОНЁН founder'ом 2026-09-10 (`П-029`) в пользу `M-84`.** Предмет исчезает вместе с клиентской сеткой: при фиксированном наборе клиент число полос не задаёт, и верхний предел сторожить нечего. `TD-198` закрывается не заплаткой, а исчезновением предмета. Прежняя формулировка: `TD-198` — клиент задаёт число полос без верхней границы | новый | — | ⏳ OPEN |
| **12bis** | **отдача и забор данных через сервер** (клиент заказывает НУЖНЫЕ серии и ПЕРИОД; тяжёлое отдаётся отдельным запросом и кешируется; история доступна не только растягиванием окна) | **РЕШЕНИЕ FOUNDER'А 2026-09-19: СТРОИТСЯ ПОЗЖЕ.** Дословно: «мне важно, чтобы они накапливались. как отдавать/забирать данные построим позже через сервер». Предмет поднят founder'ом же 18.09 вопросами «какому клиенту сервер что отдаёт» и «как мы передаём историю». **НАКОПЛЕНИЕ — ПРИОРИТЕТ И ОНО РАБОТАЕТ** (замер 2026-09-19: холодная копия 733 сегмента / 388 ГБ, все индексы целы, rsync без `--delete`; удаление по расписанию — только пробный прогон, «по cron'у НИКОГДА apply»; самый ранний сегмент — 15 июля 2026). **Что НЕ построено:** в протоколе подписки нельзя попросить прошлое (сервер принимает только `subscribe`/`unsubscribe`); среднего яруса хранения «дни-месяцы» не существует; `gateway::replay` не имеет НИ ОДНОГО вызывателя в прод-коде; доступ к архивным сегментам ручной. **Глубина истории — 2 месяца 4 дня**, и «годы» недостижимы правкой кода: только ожиданием либо покупкой у поставщика (шов замены источника предусмотрен `DESIGN` §18, упирается в права на данные §20 и в деньги) | новый | **founder** → architect | ⏳ отложено решением founder'а |
| 13 | read-path шаги 3–5 | пересканирование, многопоточность, разделяемая проекция (`DESIGN` §16.2). Условие цели «десятки тысяч» | по документу | цепочка | ⏳ OPEN |

### Блок 4 — остальное, порядок внутри блока свободный

| № | Предмет | Состояние предмета | Где | Состояние |
|---|---|---|---|---|
| 14 | **M-72** терминальность подписки | круг 4 — ПОСЛЕДНИЙ по `A-029` §2 | `feat/M-72-subscription-terminality` | ⏳ ведётся |
| 15 | **M-76** барьер подлинности выписок | RED-набор не написан | `feat/M-76-raw-output-barrier` | ⏳ OPEN |
| 16 | перенос семантики TOTAL | `П-013` подписала текст, живущий только на невлитой ветке | из `docs/M-67-rev2` | ⏳ OPEN |
| 17 | **M-64** контракт экспорта | ждёт спеки миграции `К1`/`К6b` с 11.08 | `feat/M-64-export-contract` | ⏳ OPEN |
| 18 | **M-67** рыночный слой | два оракула не компилируются, `MD-I-7` против снятого замка `П-014` | `docs/M-67-rev2` | ⏳ OPEN |
| 19 | барьер формы milestone-спеки | **8 кругов REJECT без созыва арбитра** — нарушение `gates.md` §0 само по себе | `feat/harness-milestone-shape` | ⏳ OPEN |
| 20 | **M-60c** чистка корпуса | фоновая гигиена | спека в `main` | ⏳ OPEN |

---

## Ждёт подписи founder'а

| что | когда | основание |
|---|---|---|
| включение удаления журнала (п.2) | после ОБОИХ запретов, не только п.1 | `П-023` |
| ~~место на диске (п.2bis)~~ | ✅ снято 2026-09-08 | вопрос закрыт разовым удалением 57 ГиБ; покупка не потребовалась |
| токены канала оповещений (п.5) | после п.4 | `П-003` |
| снапшоты холодного хранилища | вне очереди | `П-023`: один ключ способен снести и оригинал, и копию. Стоит денег ⇒ решение владельца |
| порядок фаз: подтвердить «Ф0 первой» либо снять | вне очереди | `П-018(б)` против фактического хода работ (`audit-2026-09-07-phases.md`) |

## Чего здесь нет намеренно

**Отложенный торговый путь** (`risk`/`killswitch`/`oms`, исполнение ордеров, live) —
решение founder'а 2026-09-07 «реализуем позже». Шов предусмотрен `DESIGN` §21, ворота
входа — §21.1; в порядок работ не входит.

**Сроков нет.** Замеров собственной скорости у нас не существует, а придуманные числа хуже
отсутствующих: они становятся обязательством, которое никто не проверял.

---

## Cross-references

- `docs/plans/audit-2026-09-07-debt.md` — триаж 127 карточек долга
- `docs/plans/audit-2026-09-07-branches.md` — судьба девяти невлитых веток
- `docs/plans/audit-2026-09-07-claims.md` — где документы врут о коде
- `docs/plans/audit-2026-09-07-phases.md` — где мы РЕАЛЬНО по фазам
- `docs/DESIGN.md` §10 (фазы — «куда идём»), `PROJECT-STATE.md` («что есть»),
  `TECH-DEBT.md` («что сломано»), `milestones/BACKLOG.md` (очередь предметов)
