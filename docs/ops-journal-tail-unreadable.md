# Runbook — `journal tail unreadable` (recorder не поднялся)

Источник: M-49 (JR-I-8, TD-049). Этот документ — то, на что указывает диагностика
`open_with`, когда хвост журнала физически повреждён и recorder отказался стартовать.
Аудитория — оператор, который увидел в логах/алертах строку с маркером
`journal tail unreadable`.

## Почему recorder отказал (а не стартовал «как есть»)

Журнал append-only. `next_seq` обязан продолжать историю, уже лежащую на диске — иначе
новая запись ляжет ПОВЕРХ уже занятого диапазона `seq`, и реплей воспроизведёт эту порчу
бит-в-бит: откатить нечем. Раньше recorder в этой ситуации молча стартовал с
`journal.meta` (а при восстановлении из холодного хранилища `journal.meta` ОТСУТСТВУЕТ —
ретеншен выгружает сегменты, не мету — так что `meta_seq = 0`), что означало запись с нуля
поверх существующей истории. Замер architect'а: до фикса M-49 усечённый `.zst`-хвост или
битый v2-заголовок давали `open_with = Ok` и перекрывали до нескольких сотен уже
записанных событий без единой ошибки в логе.

Поэтому сейчас: сегмент с максимальным индексом существует и не пуст, но его последний
`seq` установить не удалось → `open_with` возвращает `Err`, сбор данных останавливается
ЯВНО (минуты простоя), а не портится МОЛЧА (необратимо).

Сообщение об ошибке всегда называет конкретный файл, например:

```text
journal tail unreadable: segment-00000007.jrnl.zst — last seq could not be established
(compressed segment header unreadable: ...). Recorder refuses to start: starting from
journal.meta would REUSE seq already present in the log. Action: re-fetch this segment
from cold storage, or declare next_seq explicitly (runbook: docs/ops-journal-tail-unreadable)
```

## Шаг 1 — прочитать ошибку

Имя проблемного файла — в самом сообщении (первое слово после `journal tail unreadable:`).
Это либо `segment-NNNNNNNN.jrnl` (сырой сегмент), либо `segment-NNNNNNNN.jrnl.zst`
(сжатый). Причина отказа — в скобках сразу после имени: усечение (`truncat`), несовпадение
CRC заголовка (`crc mismatch`), обрыв zstd-потока (`unreadable`) и т.п.

## Шаг 2 — проверить целостность файла

Для сжатого сегмента (`.jrnl.zst`):

```bash
zstd -t /path/to/journal/segment-NNNNNNNN.jrnl.zst
echo "exit=$?"
```

`exit=0` и «is compressed» — поток декомпрессируется целиком (порча тогда, скорее всего,
в v2-заголовке ПОСЛЕ распаковки, не в самом zstd-потоке — см. Шаг 3, вариант «частичная
порча»). Ненулевой exit / `Decoding error` — поток физически повреждён или усечён
(обрыв выкачки/копирования).

Для сырого сегмента (`.jrnl`) сравните размер файла с ожидаемым (если есть лог/метрика
прежнего размера) — резкое уменьшение указывает на усечение при копировании/крахе диска.

## Шаг 3 — перекачать сегмент из холодного хранилища

Штатный путь восстановления: у выгруженного (`retention`) сегмента ДОЛЖНА быть холодная
копия (Storage Box / смонтированный путь — `RetentionPolicy.cold_root`). Перекачайте
именно этот файл заново поверх повреждённого:

```bash
cp /cold/root/segment-NNNNNNNN.jrnl.zst /path/to/journal/segment-NNNNNNNN.jrnl.zst
zstd -t /path/to/journal/segment-NNNNNNNN.jrnl.zst && echo OK
```

После успешной перекачки перезапустите recorder — если целостность восстановлена,
`open_with` продолжит историю штатно (`seq` без разрыва), декларация из Шага 4 не нужна.

Если холодной копии нет (сегмент никогда не выгружался, либо сама холодная копия тоже
повреждена) — переходите к Шагу 4.

## Шаг 4 — крайний случай: сегмент невосстановим

Если перекачать нечего (холодной копии нет) или перекачанная копия ТОЖЕ не проходит
`zstd -t` — сегмент признаётся невосстановимым. Это осознанный разрыв в истории `seq`,
а не рутинная операция; выполняется только после того, как вариант из Шага 3 исчерпан.

1. **Карантин.** Уберите повреждённый файл из активного каталога журнала (не удаляйте —
   переместите, он может пригодиться для судебного разбора):
   ```bash
   mkdir -p /path/to/journal/quarantine
   mv /path/to/journal/segment-NNNNNNNN.jrnl.zst /path/to/journal/quarantine/
   ```
   Карантин — ТОЛЬКО ручное действие оператора. `open_with` файлы данных не перемещает,
   не переименовывает и не удаляет (rev3 M-49): горячий путь старта recorder'а не трогает
   состояние каталога, кроме самой декларации из следующего пункта. Выполните этот шаг
   ДО перезапуска recorder'а с декларацией.

   **Последствие пропуска этого шага (R-001 NOTE 3).** «Recorder поднялся» ≠ «инцидент
   закрыт». С валидной декларацией recorder стартует и пишет НЕЗАВИСИМО от того, убрали вы
   повреждённый сегмент или нет — но пока он остаётся в каталоге, происходит два разных
   отказа деградации:
   - строгий путь чтения (`stream`/`list_segments` — то, чем пользуются кокпит, research,
     бэктест) продолжает отказывать на этом каталоге целиком (контракт `ti_6`: строгий путь
     не терпит порчу молча);
   - если повреждён СЫРОЙ сегмент (`.jrnl`, не `.zst`) с целым заголовком — новые события
     дописываются В ЭТОТ ЖЕ файл (`decide_open_segment` продолжает его как reuse-кандидат),
     то есть здоровые новые данные оказываются в одном файле с повреждённым хвостом.
   Карантин обязателен именно поэтому, а не только «для порядка».

2. **Декларация.** Определите, с какого `seq` продолжать (строго больше максимального
   ЧИТАЕМОГО `seq` в оставшейся истории — recorder откажет, если это не так), и положите
   в каталог журнала файл `journal.force-next-seq.json`:
   ```json
   {
     "next_seq": 512,
     "reason": "segment-00000007.jrnl.zst невосстановим: bit-rot, холодной копии нет",
     "declared_at_ms": 1785362203969
   }
   ```
   `reason` — обязателен и осмыслен: он сохранится в аудит-следе
   (`journal.force-next-seq.applied.json`) после применения.

3. **Понимайте последствие.** Эта декларация — явно названный разрыв в `seq`: события
   между максимальным читаемым `seq` и объявленным `next_seq` потеряны безвозвратно (тот
   же класс решения, что `pruned_without_checkpoint_coverage` в ретеншене, M-38b — обход
   разрешён, но обязан быть НАЗВАН, не тихим).

4. Перезапустите recorder. При валидной декларации `open_with` стартует, применяет
   декларацию ОДИН РАЗ (файл переименовывается в `journal.force-next-seq.applied.json`,
   `reason` сохранён), и запись продолжается с объявленного `next_seq`. При невалидной
   декларации (`next_seq` ≤ максимального читаемого `seq`) recorder откажет снова — это
   защита от того, чтобы сама декларация случайно стала каналом seq-reuse под давлением
   инцидента.

## Шаг 5 (rev5) — декларация ТОЖЕ отклонена: «readable seq floor is UNKNOWN»

Шаг 4 предполагает, что recorder МОЖЕТ установить, до какого `seq` история читается (пол
защиты), и просто проверяет вашу декларацию против этого пола. Но возможен более тяжёлый
случай: **пол защиты сам неизвестен** — ни в одном сегменте каталога (сжатом или сыром) не
нашлось ни одного валидного события. Тогда recorder откажет в декларации с сообщением вида:

```text
journal.force-next-seq.json rejected: readable seq floor is UNKNOWN — no valid event frame
could be read anywhere in this journal directory, so declaration next_seq=... cannot be
verified against anything. Restore the affected segment(s) from a cold copy, or move them
to manual quarantine, then retry — see docs/ops-journal-tail-unreadable.md.
```

**Почему это ОТДЕЛЬНЫЙ отказ, а не то же самое, что Шаг 4.** Декларация — это заявление
оператора «я знаю, что дальше `seq X`», а recorder ОБЯЗАН это заявление ПРОВЕРИТЬ (не
просто поверить на слово: под давлением инцидента легко ошибиться цифрой). Проверка — это
сравнение `next_seq` с фактически читаемым максимумом. Если ЭТОТ максимум сам неизвестен
(каталог целиком нечитаем — например, весь `.jrnl`/`.jrnl.zst` состоит из мусора, или
собраны фрагменты из разных инцидентов), проверить нечем. **Отсутствие знания о поле — это
не разрешение.** Раньше (до rev5) такой каталог трактовался как «пол = 0» и ЛЮБАЯ
декларация с `next_seq >= 1` проходила — это и есть неявный seq-reuse с формальным
одобрением оператора, который выглядел как «сработало», но не проверял ничего. Сейчас
recorder честно говорит «я не могу это подтвердить» вместо того, чтобы тихо согласиться.

**Что делать.** Цель — сделать пол снова ИЗВЕСТНЫМ (`Known`) или обнулить его легитимно
(`NoSegments`), и тогда декларация из Шага 4 станет проверяемой:

1. **Восстановите из холодной копии БОЛЬШЕ, чем один сегмент.** Если «unknown» получился
   потому, что повреждён не только последний (активный) сегмент, а и предыдущие — Шаг 3
   применяется к КАЖДОМУ нечитаемому сегменту, не только к тому, что назван в первом
   сообщении об ошибке. Проверяйте каждый: `zstd -t` для `.jrnl.zst`, сверка размера для
   `.jrnl`.
2. **Если восстановить нечего** (холодных копий нет вообще ни для одного сегмента, или все
   они тоже испорчены) — карантините ВСЕ нечитаемые сегменты (Шаг 4, п. 1), пока в каталоге
   не останется либо (а) хотя бы один сегмент с читаемым содержимым (пол становится
   `Known`), либо (б) вообще ни одного сегмента (пол становится `NoSegments` — легитимный
   старт с нуля, вся история потеряна и это ОСОЗНАННОЕ решение, а не тихий побочный эффект).
3. Только теперь кладите декларацию (Шаг 4, п. 2) — она либо будет успешно проверена против
   новообразовавшегося `Known`-пола, либо (если карантинили всё) не нужна вовсе:
   `NoSegments` разрешает старт с `next_seq=0` без декларации.

Как и в Шаге 4 — это осознанная потеря истории (потенциально БОЛЬШЕЙ, чем в обычном случае:
неизвестно, сколько событий лежало в сегментах, которые пришлось карантинить целиком, раз
их нельзя прочитать даже частично). `reason` в декларации обязан это отразить.

## Шаг 6 (M-52) — декларация отклонена «readable seq floor is UNKNOWN» под ИНЦИДЕНТОМ:
## теперь это может быть бюджет РАБОТЫ, не только «валидных фреймов нет вообще»

С M-52 (JR-I-10, TD-052+TD-054) скан пола (Шаг 5) несёт ЕЩЁ ОДНУ границу — не только по
памяти (как в rev5), но и по объёму РАБОТЫ (прочитанные/раздекодированные байты по ВСЕМУ
каталогу, включая side-верификацию крупных кандидатов, JR-I-9). Раньше скан честно (но
неограниченно по времени) дочитывал ВЕСЬ каталог, прежде чем ответить `Unknown` — на проде
(158 сегментов, ≈140 GiB сырых) это могло занять от единиц до десятков минут РОВНО тогда,
когда оператор поднимает recorder под инцидентом. Теперь скан останавливается по бюджету и
отвечает `Unknown` быстро — но сообщение об ошибке ("readable seq floor is UNKNOWN...")
ВНЕШНЕ выглядит так же, как исходный Шаг 5 (валидных фреймов действительно нет НИГДЕ). Оба
случая требуют ОДНОГО и того же действия оператора — расширять диагностику различением не
нужно, но важно понимать: **`Unknown` не значит «каталог точно пуст/испорчен целиком»** —
он означает «пол ЭТИМ проходом установить не удалось» (по любой из двух причин), и решение
то же самое, что в Шаге 5:

1. Восстановите из холодной копии как можно БОЛЬШЕ сегментов (Шаг 3) — это одновременно и
   чинит «валидных фреймов нет», и УМЕНЬШАЕТ объём мусора, который скану придётся пройти
   (снижает шанс повторного исчерпания бюджета).
2. Если восстанавливать нечего — карантините нечитаемые/подозрительные сегменты (Шаг 4,
   п. 1), пока пол не станет `Known` (хотя бы один сегмент даёт валидный читаемый префикс в
   пределах бюджета) либо `NoSegments` (карантинили всё).
3. Кладите декларацию (Шаг 4, п. 2) заново — она будет проверена против нового пола.

**Что НЕ пытаться сделать.** Бюджет — именованная константа крейта
(`READABLE_FLOOR_WORK_BUDGET_BYTES`, `crates/journal/src/segments.rs`), не операторский
конфиг: поднять его «чтобы декларация прошла с первого раза» нельзя без пересборки бинаря
(и не нужно — карантин мусора почти всегда дешевле, чем ждать более длинного скана).

## Шаг 7 (M-52) — как проверить детерминизм БОЕВОГО журнала (JR-I-12, TD-067)

До M-52 единственной проверкой боевого журнала был sha256 ФАЙЛА (`journal.legacy.json`
fingerprint и подобные) — это доказывает неизменность БАЙТ на диске, но НЕ то, что реплей
(декодер) воспроизводит один и тот же поток событий. `DET-I-1` (бит-идентичный реплей,
`docs/DESIGN.md` §1) был доказан оракулами в лаборатории (M-51) и до сих пор ни разу не
наблюдался в поле — на VPS нет Rust toolchain, а функция `journal::replay_digest` не
позвана НИКЕМ из образа. С M-52 это доступно через уже доставляемый бинарь
`journal-retention`.

### Прогон дайджеста

```bash
docker compose run --rm --entrypoint journal-retention recorder \
  --dir=/journal --mode=replay-digest
```

Вывод:

```text
events=<N> first_seq=<seq|none> last_seq=<seq|none> state_hash=<64 hex>
```

и машинная запись `journal.replay-digest.json` (то же самое — для cron/скриптов, не для
человеческого глаза) рядом с журналом.

### Честность окна — ВАЖНО

Без `--from`/`--to` окно ОТКРЫТОЕ: пока recorder дописывает журнал, повторный прогон БЕЗ
явного окна законно даст ДРУГОЙ `state_hash` (в поток добавились новые события) — это НЕ
регрессия детерминизма, а рост живого журнала. **Сравнение двух прогонов имеет смысл
ТОЛЬКО на ЗАКРЫТОМ окне** (оба флага заданы):

```bash
docker compose run --rm --entrypoint journal-retention recorder \
  --dir=/journal --mode=replay-digest --from=1000000 --to=1999999

docker compose run --rm --entrypoint journal-retention recorder \
  --dir=/journal --mode=replay-digest --from=1000000 --to=1999999
```

Два прогона на ОДНОМ закрытом окне обязаны дать ОДИН `state_hash` — это и есть первый
реальный прод-прогон, обещанный TD-067 (§8 деплой-гейта: два запуска подряд с предъявлением
совпавшего `state_hash` и пика RSS).

### Автоматическая сверка (`--expect`)

```bash
docker compose run --rm --entrypoint journal-retention recorder \
  --dir=/journal --mode=replay-digest --from=1000000 --to=1999999 \
  --expect=<ранее записанный state_hash>
echo "exit=$?"
```

`exit=0` — совпало. `exit=4` — РАСХОЖДЕНИЕ (`DET-I-1` под вопросом на боевом журнале),
вывод называет ОБЕ величины (`expect=...` и `actual=...`). Коды `1`/`2`/`3` уже заняты
(аргументы/сбой сверки холодной копии/`disk_pressure`) — `4` зарезервирован ИМЕННО за
дайджестом, cron обязан отличать «прогон не удался» от «детерминизм нарушен».

### Что это НЕ делает

- **Не трогает данные.** `replay-digest` read-only по журналу: не открывает `Journal` на
  запись, не пишет события, не меняет `journal.meta`/`journal.legacy.json`. Единственный
  новый файл — `journal.replay-digest.json`, атомарная запись рядом.
- **Recorder дайджест не считает НИКОГДА** — это отдельный операторский/cron-прогон вне
  горячего пути сбора (recorder держит поток на ~3% CPU, самопроверка внутри него была бы
  и риском для сбора, и самореференцией — событие о журнале в журнале меняло бы сам
  журнал).
- **Не читает через `read_all`/`recover`.** Прод — 26 GB / 148 млн событий; загрузка всего
  журнала в RAM — тот же класс дефекта, что уже ронял recorder (TD-011). Дайджест —
  потоковый, память не растёт с размером журнала.

## Что НЕ делать

- Не удаляйте каталог журнала целиком — это гарантированная потеря ВСЕЙ истории, а не
  только повреждённого сегмента.
- Не редактируйте повреждённый сегмент вручную (обрезка/дозапись байт) в надежде
  «починить» его — журнал читается по формату (magic + length-prefixed frames + crc32),
  ручная правка почти всегда даёт файл, неотличимый от порчи для парсера.
- Не понижайте `next_seq` в декларации ниже читаемого максимума «чтобы recorder точно
  стартовал» — это ровно тот seq-reuse, от которого защищает весь этот механизм; recorder
  такую декларацию отклонит с объяснением.
