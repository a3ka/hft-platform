<!-- FACTS: audited_head=57a7d4b collected=2026-08-23 -->

# Передача сессии architect'а — 2026-08-22 (вечер)

**Главное в одной строке:** сессия прошла под девять REJECT'ов на четырёх предметах, и
**ни один не был придиркой** — каждый нашёл дефект, которого автор не видел; харнесс
сократился впервые (0.74 → 0.71, в пике было 0.80), M-65 развернулся арбитражем против
architect'а.

---

## 0. Состояние мира

```
origin/main   40c7cce   CI success
харнесс       18 427 строк / прод-код 26 015 = 0.71:1   (старт сессии 0.74, пик 0.80)
диск          76 %
висяков       нет; веток 30; NOTE: предмет M-66 на 4 ветках (аудит-трейл, не трогать)
```

## 1. Влито (шесть PR)

| PR | предмет |
|---|---|
| #53 | передача 22.08 с §0bis/§3bis |
| #54 | замеры Р-1 и Р-2 |
| #55 | `П-014` предусловие (б): FA приведена к подписи founder'а (критик PASS `C-120`) |
| #57 | merge в харнесс-треке делает автор-architect — сужение `П-017` B (`R-099` APPROVE) |
| #58 | заход reviewer'а: `R-098`, `TD-156`, `TD-157` |
| #59 | **норма Р-2** «гейт закрытого милестоуна уезжает в архив» (`R-106` APPROVE, 3 круга) |

## 2. Открытые PR — что с каждым

| PR | вердикт | что осталось |
|---|---|---|
| **#60** вынос 26→24 гейтов | `R-109` REJECT ЗАКРЫТ | чеки устарели (от прогонов ДО правок); локально все барьеры зелёные. **Перепрогнать CI и влить** |
| **#56** архивный класс | `R-108` **PASS** (круг 6) | чеки устарели по той же причине. **Перепрогнать и влить** |
| **#28** барьер ресурсных оракулов | `C-123` **PASS** (круг 3) | то же. **Перепрогнать и влить** |
| **#29** M-65 | `A-015`, `C-125`→`C-126` **PASS** | вершина `51268fc`; следующий шаг — мой, см. §3 |
| **#32** M-69 | `A-014` шаг 1 исполнен (`f3102f0`) | ждёт critic круга 4, ОГРАНИЧЕННОГО по `A-014` §5 п.2 |

**Важно про красные чеки #56/#28/#60:** три PR показывают FAIL по `Artifact IDs`/`GATE-META`,
но это прогоны, предшествовавшие двум фиксам — переномерации `R-106`→`R-110` и правке
`check_artifact_ids` (переезд перестал быть коллизией). Локальный прогон барьера на ветке
#60: `OK`. Первое действие новой сессии — перезапустить чеки, а не чинить.

## 3. M-65 — арбитраж развернул решение architect'а

`A-015` решил ПРОТИВ позиции, слитой в этой сессии. Действует §11:
отсутствие переменной И пустая строка ⇒ подписанные **16 + warn**; невалидное значение ⇒
отказ старта. Маршрут `A-015` §5:

1. ✅ **architect** — узкий contract-RFC на `CT-RFC-09` §2.6 (`f4f0a49`), исправлен по
   `C-125` (`41c07e9`: я написал «пустая строка отвергается» в том же коммите, где
   постановил обратное);
2. ✅ **critic круг 2 — PASS** (`C-126`, `51268fc`). Гейт contract-RFC пройден: «пусто» и
   «отсутствует» имеют ОДИН контрактный исход — 16 + `warn`. Круг 1 (`C-125`) нашёл
   внутреннее противоречие в моём же тексте, исправлено `41c07e9`;
3. ⏳ **architect — СЛЕДУЮЩИЙ ШАГ, разблокирован.** Переписать sacred-оракул
   `crates/gateway-serve/tests/red_max_subs_config.rs` по составу §11:
   `absent_var_yields_signed_default_16` (эталон — ЛИТЕРАЛ 16, не константа из кода),
   `empty_var_is_same_as_absent`, `invalid_values_refuse_start`,
   `explicit_value_overrides_default`, `warn_on_default`. Шапка чинится: снять мёртвую
   цитату и ссылку на несуществующую редакцию §11. Шаг L `verify_M-65.sh` дополняется
   ЗАПУСКОМ бинаря: отсутствие ⇒ стартует, пусто ⇒ стартует (решение по exit-коду).
   **RED обязан быть честным: против текущего Б-кода первые два ПАДАЮТ.** Реализацию НЕ
   трогать — это шаг 4, engine-dev;
4. ⏳ engine-dev — `None` и пустая ⇒ 16 + warn (откат СУТИ `d5a1cf5` новым коммитом);
5. ⏳ architect — §Tasks 13 снять `DONE` в части N-2;
6. ⏳ reviewer — круг; в close-out завести TD на `GATEWAY_WINDOW_MS` `.ok()` → unbounded.

**Закрыто в этой сессии:** `R-106` Б-1 (вторая половина `O-12` была вакуумна — тот же
дефект, что чинился в первой; изолирующая мутация предъявлена) и Б-3 (§Tasks к факту,
дубль номера разведён).

## 4. П-014 — reviewer REJECT, RED переписан

`R-110` Б-1: метка ЗАЛИПАЕТ на merge-пути (`Snapshot::apply`, «первый непустой побеждает»).
При старой семантике безвредно, при новой — клиент до переподключения читает
«liveness=confirmed» о полосе, которой в книге нет.

Оракул написан и был честно красным (`1b53307`). **Круг 2 закрыт engine-dev'ом**
(`481403a`, `6447b2a`, вершина `6447b2a`): merge-путь больше не залипает, охват снимается
там, где считаются числа полосы (ветка `L2Snapshot`), `CKPT_SCHEMA_VERSION` 1→2.
Прогон dev'а: набор 6/6, `gateway` 124/0, workspace 829/0, сторож `O-1` ×1.00, clippy и
fmt чисты. **Дальше — tester**, промпт у founder'а.

**ПРОБЕЛ ПОКРЫТИЯ, названный самим dev'ом и НЕ закрытый — зона architect.** Коммит
`6447b2a` не пиннится НИ ОДНИМ оракулом: мутация «вернуть чтение живой книги
(`self.book.max_reach_pct`) вместо снятого охвата» оставляет набор 6/6 ЗЕЛЁНЫМ, при том
что `GW-I-4` ломается на дельта-хвосте. Нужен RED на фикстуру «`L2Snapshot`, затем
`L2Delta`, снимающая дальние уровни»; временная проба dev'а давала
`full = not-observed reach=0.025000` против `merge = liveness=confirmed`, воспроизведение
записано в теле `6447b2a`. **Это третий случай за два дня, когда правка проходит без
оракула — не откладывать.**

**Семантика, уточнённая dev'ом сознательно** (принять или оспорить — решение architect'а):
метка описывает охват НАБЛЮДЕНИЯ, из которого взяты числа полосы (последний `L2Snapshot`),
а не охват живой книги — иначе метка утверждала бы о состоянии, не вошедшем в серию.
`П-017` предусловие (а) при этом закрыто: REST-ресинк приходит `L2Snapshot`-ом и двигает
и числа, и охват.

**`CKPT_SCHEMA_VERSION` 1→2:** на проде существующие `ckpt-*.bin` будут один раз
пересобраны (тихий rebuild, `GW-I-9б`) — первое подключение после деплоя дороже обычного.
Назвать это в close-out.

**`П-014` НЕ ЗАКРЫТ и после этого:** пункт 2 подписи (каденция, депт-серия snapshot-only
1 Гц против дельт 100 мс) остаётся открытым. Любой close-out, объявляющий `П-014`
исполненным, — класс `TD-155`.

## 5. Что осталось, поимённо

1. перезапустить чеки и влить **#60, #56, #28** (три PASS-вердикта на руках);
2. **M-65: шаг 3 `A-015` — переписать sacred-оракул, разблокирован `C-126` PASS.** Дальше
   шаги 4–6 (engine-dev → §Tasks → reviewer);
3. **П-014: (а) RED на пробел покрытия `6447b2a` — зона architect, срочно;** (б) tester по
   кругу 2, промпт у founder'а; (в) затем reviewer;
4. M-69: critic круга 4 по `A-014` §5 п.2;
5. **два барьера по разморозке founder'а 22.08** — не построены, разморозка выдана:
   - негативный ассерт обязан иметь позитивного свидетеля (класс «вакуумный ассерт»,
     три срабатывания: `R-086` §2, `R-106` Б-1, и дважды у меня на П-014);
   - строка §Tasks не смеет нести нормативного содержания сверх ссылки `§N` (класс
     «расхождение носителей», четыре срабатывания; предложен и арбитром в `A-015` §6 как
     вариант (а));
6. `TD-156` (пять пар «один идентификатор — два предмета») — reviewer;
7. вопрос founder'у из `A-015` §6: (а)/(б)/(в) — **рекомендация architect'а (а)**.

## 6. Уроки, дороже предметов

1. **Проверка, которая скрывает результат, хуже отсутствующей.** Трижды за сессию:
   `git show --diff-filter=D` молчит при renames; `ls <glob> | tail -1` прячет всё, кроме
   последней строки; `--stat` показывает числа, но «сколько удалено» в них не читается.
   Форма, которая не лжёт: перечисление по дереву `git ls-tree` для КАЖДОГО каталога плюс
   явная сверка «идентификатор не встречается дважды».
2. **Ассерт на сужающемся ПРОКСИ порождает спираль.** #56 прошёл пять кругов: счётчик →
   перечень грепов → образ регулярного извлечения. Каждый следующий круг находил, чего не
   видит предыдущий прокси. Сошлось только когда прокси убрали — сравнение СТРОКИ целиком.
3. **Оракул, зелёный против заведомо сломанного кода, — вакуум.** На П-014 я написал такой
   дважды подряд: фикстура возвращалась к исходному охвату; дренаж одним батчем не
   задействовал merge-путь.
4. **Утверждение о состоянии документа без его открытия.** `25c323d` написал «§11
   пересмотрен» — §11 никто не пересматривал. Отсюда весь арбитраж `A-015`.
5. **Гонка номеров реальна при параллельных клонах.** Два клона взяли `R-106` через
   `next_artifact_id.sh` и уронили CI. `reserve_artifact_id.sh` существует ровно против
   этого и не был ни в одном мандате. **Резерв — обязательная строка мандата.**

## 7. ГДЕ ЛЕЖИТ КАКОЙ ВЕРДИКТ — ни один не в `main`

Проверено командой: все восемь вердиктов этой сессии живут на ветках своих предметов, а не
в `main`. Ссылка по одному идентификатору следующую сессию НЕ приведёт к файлу — отсюда
таблица. Читать так: `git show <ветка>:<путь>`.

| ID | ветка | путь |
|---|---|---|
| `A-014` | `feat/M-69-window-guard` | `research/arbitration/A-014-m69-window-guard.md` |
| `A-015` | `fix/M-65-battery-recalibration` | `research/arbitration/A-015-m65-n2-absent-config.md` |
| `C-121` `C-122` `C-123` | `fix/resource-oracle-barrier` | `research/critiques/C-12{1,2,3}-*.md` |
| `C-125` `C-126` | `fix/M-65-battery-recalibration` | `research/critiques/C-12{5,6}-*.md` |
| `R-100`…`R-103`, `R-107`, `R-108` | `harness/verdict-archive-class` | `research/reviews/R-1*.md` |
| `R-109` | `harness/archive-id-universe` | `research/reviews/R-109-pr60-archive-recheck.md` |
| `R-110` (M-65, вердикт reviewer'а) | `fix/M-65-battery-recalibration` | `research/reviews/R-110-M-65-impl-rev3.md` |
| `R-110` (П-014, вердикт reviewer'а) | `feat/p014-depth-provenance` | `research/reviews/R-110-p014-depth-provenance.md` |

**Осторожно: номер `R-110` занят ДВАЖДЫ** — это остаток гонки аллокатора 22.08. Один занят
переномерованным вердиктом M-65, второй выдан клоном на ветке `П-014`. Пара разойдётся при
merge'е и уронит `artifact-ids`; развести ДО вливания любой из двух веток. Это восьмая пара
класса `TD-156`, и она наша собственная, свежая.

## Cross-references
- `research/arbitration/A-014` (M-69), `A-015` (M-65 N-2)
- `research/reviews/R-098` `R-099` `R-103` `R-106` `R-107` `R-108` `R-109` `R-110`
- `research/critiques/C-120` `C-121` `C-122` `C-123` `C-125`
- `docs/plans/tasks-form-barrier-measurement-2026-08-22.md` (Р-1), `verify-archive-measurement-2026-08-22.md` (Р-2)
