<!-- FACTS: audited_head=217992743f3c569c272d47914047459c3cef0d00 collected=2026-08-28 -->

# Передача сессии architect'а — 2026-08-28 → 2026-08-29

**Главное в одной строке:** оба предмета отклонены PR-гейтом и ждут работы; founder решил
СНАЧАЛА разобрать КЛАСС (пять кругов подряд зелёный гейт при живом дефекте), и только потом
писать шестой точечный оракул.

Продолжение `session-handover-2026-08-27.md` (в `main`).

## 0. Состояние мира — замер, не память

```
main            2179927   зелёный, CI и Deploy success
прод            bba42cd   отставание от main — 0 КОДОВЫХ файлов
                          (git diff --name-only bba42cd..origin/main -- ':(glob)crates/*/src/**' | wc -l → 0)
feat/M-71-rev6  f50aac7   вердикт R-143 REJECT (круг 4), три блокера
feat/M-68-rev4  06f6a02   спека и оракулы готовы, ждёт engine-dev (круг 4)
открытых PR     0
диск локально   88 %      47 деревьев, 37 висящих резервов
```

### ⚠ ДВЕ НАХОДКИ ЯРУСА S, ОБЕ БЕЗ ВЛАДЕЛЬЦА

**1. `hft-recorder` перезапускался 55 раз, и ни один алерт не сработал.**

```
docker inspect hft-recorder --format '{{.RestartCount}} {{.State.StartedAt}} {{.State.ExitCode}} {{.HostConfig.RestartPolicy.Name}}'
  → 55  2026-08-28T03:50:39Z  0  unless-stopped
heartbeat: events=3169320 next_seq=391941773 segment_index=454 writable=true  (свежий)
docker logs --tail 8 → журнал растёт, венью живы
```

Контейнер `healthy`, heartbeat свежий, журнал растёт — все три liveness-сигнала зелены, и
именно поэтому 55 рестартов НИКЕМ не наблюдаются. Это дословно класс `DESIGN` §23.1 / `PL-I-8`:
«алертим полезную работу, не liveness». Запись **forward-only**: каждый рестарт — невосполнимый
гэп. Причина рестартов НЕ установлена (не выяснял — вне зоны и вне остатка контекста).
Владельца нет; `crates/ops` ops-watchdog построен, но канал молчит (`П-003`, ждёт токена).

**2. Диск VPS 88 %.** `df -h /` на проде → `126G/150G`, свободно 19.6 ГБ при
`min_free_bytes = 10.7 ГБ` в heartbeat. До fail-closed порога recorder'а ≈9 ГБ.

## 1. ДВА ПРЕДМЕТА

| предмет | ветка | вершина | состояние |
|---|---|---|---|
| **M-71** предел объёма выдачи | `feat/M-71-rev6` | `f50aac7` | `R-143` REJECT, круг 4, ТРИ блокера |
| **M-68** каденция депт-серии | `feat/M-68-rev4` | `06f6a02` | спека+оракулы готовы, ждёт `engine-dev` |

### M-71: что нашёл круг 4 (`R-143`), всё проверено architect'ом лично

- **`B-1`** — `journal::stream_from` попал на ТИКОВЫЙ путь: `crates/gateway/src/lib.rs:3495`
  внутри `commit_batch`, который зовётся при каждом закрытии батча. Это возврат перескана
  активного сегмента, ради устранения которого существовали `M-57` и `M-62`. Замер ревьюера
  (`rchar` из `/proc/self/io`): при 32 000 событий в сегменте — 1 703 434 Б на тик из ТРЁХ
  событий против 65 880 Б у родителя, ×25.9. **Живёт на ЧЕСТНОМ пути, отказов не требует.**
  Предсказан поимённо `TD-117`: «если один из них окажется на тиковом пути, гейт смолчит».
- **`B-2`** — реализована конструкция, которую спека ЭТОГО ЖЕ круга вычеркнула. Таблица
  §4bis.5 (`milestones/M-71-egress-cap.md:705-709`) помечает транзакционность `ИСКЛЮЧЁН` с
  причиной «livelock»; тело коммита `59b7052` — «pump pure transactional». Livelock предъявлен
  исполнением: восемь тиков подряд, курсор не двигается, кадров ноль, подписка мертва.
- **`B-3`** — условие `R-139` §4 п.3 не исполнено ТРЕТИЙ круг, плюс различение причин неверно:
  `matches!(kind, io::ErrorKind::Other)` объявлен признаком «нашей» ошибки, тогда как журнал
  использует `Other` в четырёх местах (`crates/journal/src/segments.rs:654,657,660,663`).
  Один ложный диагноз заменён симметричным ложным.

**Гейт при всём этом ЗЕЛЁН:** `verify_M-71.sh` → `PASS`, 44/0, `exit=0`; workspace 917 passed.

### M-68: готово к разработчику

Промпт разработчику на задачи 13/23/24 + `N-2` выдан в переписке предыдущей сессии; вершина
`06f6a02`, базовая линия `FAIL (3)`, `exit=1`, 25 PASS. Ничего от architect'а не ждёт.

## 2. РЕШЕНИЕ FOUNDER'А 2026-08-28 — что делать ДАЛЬШЕ

**Сначала разобрать КЛАСС, потом писать оракулы.** Основание — наблюдение `R-143` §E,
подтверждённое счётом:

> Пятая подряд щель одного семейства: `R-133` N-3 → `R-139` B-1 → `R-140` §2 п.6 → `B-1` здесь
> → `B-2` здесь. Набор раз за разом судит не то, что ломается. Это уже не случайность
> милестоуна, а свойство конструкции оракулов.

**Пять кругов подряд гейт был ЗЕЛЁНЫЙ, а дефект — настоящий.** Каждый раз чинили экземпляр и
писали новый точечный оракул; следующий круг ломался в соседнем месте, куда набор не смотрит.

Счёт по M-71: 9 кругов критика, 4 круга ревьюера, 3 арбитража, 5 зелёных гейтов при живых
дефектах — против одного милестоуна, добавляющего предел объёма ответа.

**Задача следующей сессии, в этом порядке:**

1. **Разбор класса** (не арбитраж — спора нет; разбор). Почему набор из 44 зелёных проверок
   пять раз подряд не видел настоящего дефекта, и что менять в КОНСТРУКЦИИ набора, а не в
   шестом экземпляре. Материал: `TD-148` (MAJOR, уже заведён — «`ReadStats` снимается только с
   первичного стрима, второй не инструментируется вовсе»), `TD-117`, `R-143` §2 п.5,
   `R-140` §2, `R-139` §2.
2. **Затем** — оракулы на `B-1` и `B-2` M-71.

### Что уже известно про конструкцию набора (не начинать с нуля)

- **`B-1` слеп ПО ПОСТРОЕНИЮ:** `ReadStats` снимается ТОЛЬКО с первичного стрима
  (`crates/gateway/src/lib.rs:3551`, `read_stats_from_stream(&stream)`); добавленный второй
  стрим не инструментируется, `stream_from` кладёт `segment_meta_ops: 0`
  (`crates/journal/src/segments.rs:1903`). Поэтому `f035_1` и `SM-2` зелены при 1.7 МБ
  реального чтения. Заведено `TD-148`.
- **`B-2` слеп ПО ПОСТРОЕНИЮ:** `P5` делает три отказа, затем СНИМАЕТ предел
  (`red_egress_cap_paths.rs:539-550`) и судит сходимость ПОСЛЕ снятия. Поведение, ПОКА предел
  стоит, он не судит. На проде предел не снимает никто.
- **Общая форма обеих слепот:** оракул мерит ПРОКСИ (счётчик, который ведёт только один из
  путей) либо судит режим, в котором дефект не проявляется. Оракул на `B-1` обязан мерить
  ФАКТИЧЕСКОЕ ЧТЕНИЕ (`rchar` из `/proc/self/io` — мера, которой пользовался ревьюер), а не
  `ReadStats`.

## 3. ВОПРОС FOUNDER'У, ОСТАВШИЙСЯ БЕЗ ОТВЕТА

**Возвращать ли правленую спеку критику.** Задан 2026-08-28, ответа нет. Суть — коллизия
двух обязывающих норм:

| норма | текст |
|---|---|
| `.claude/agents/architect.md:65-66` | «правка нормативных секций спеки милестоуна (инвариант достаточности, оси, acceptance) — **всегда форма, всегда критик**» |
| `A-025` §5.5 (M-68) | «предмет уходит **founder'у** … без пятого арбитража и **без plan-time кругов любого имени**» |
| `A-026` §4 п.5 (M-71) | «**Круга 10 не существует ни при каком исходе**» |

Правились именно нормативные секции: инварианты §4bis.5 и §0sexies.2ter, `Allowed paths`
(carve-out зоны), пять новых задач в §Tasks. По профилю критик обязателен; арбитражи его
запрещают. Разрешает это только founder — по тексту самого `A-025` §5.5.

**Рекомендация architect'а (против собственного удобства): один УЗКИЙ круг стоит того.**
Три круга подряд работа отклонялась из-за неполноты спеки, а круг 4 — из-за того, что
разработчик взял ЯВНО вычеркнутый вариант, и между спекой и реализацией это не поймал никто.
Критик — ровно тот, кто читает спеку против реализации ДО того, как круг сгорит. Предлагаемая
формулировка: не общий plan-time круг, а «самосогласованность изменённых нормативных секций с
оракулами и с выбранной реализацией».

## 4. ЧТО ЭТА СЕССИЯ СДЕЛАЛА НЕВЕРНО — класс, а не список

**Три круга подряд дефект был следствием МОЕЙ спеки, и класс один:**
**я называю проблему целиком, а требование пишу на её ЧАСТЬ.**

| круг | что назвал | что предписал |
|---|---|---|
| M-71 круг 3 | вердикт требовал «серия LIVE бит-идентична реплею» | написал «персистентный аккумулятор не свернёт повторно» — про сервер, не про провод |
| M-68 `Б-1` | §0sexies.2quinquies САМА перечислила ДВА прод-инициализатора | задача 22 предписала проводку в ОДИН |
| M-68 `Б-2` | требовался сброс незакрытого интервала | назвал точку `finish`, которой у живого пути НЕТ (`finish` берёт `self`) |

**Отдельно — круг 4 M-71 сломался НЕ на моей ошибке:** разработчик реализовал кандидата,
которого спека помечала `ИСКЛЮЧЁН`, сославшись на ту же секцию. Моя доля — я не показал
достижимого хода и сам это записал («⚠ ДОСТИЖИМОСТЬ GREEN НЕ ПРЕДЪЯВЛЕНА»); его доля —
взял вычеркнутый вместо того, чтобы вернуть «недостижимо».

**Проба достижимости GREEN ловила меня ЧЕТЫРЕЖДЫ за сессию:**
1. проба шла на системном тулчейне 1.94.1 вместо пиннутого 1.97.0 (класс `TD-035`) — и тот же
   дефект оказался вмонтирован в сам гейт `verify_M-71.sh` (найдено `C-171`, закрыто сторожем);
2. барьер дверей давал ложное КРАСНОЕ против ПРАВИЛЬНОЙ реализации;
3. оракул `P4` сперва мерил неполноту вместо порчи, потом ронял соседа глобальным состоянием;
4. оракул `d19` оказался ТАВТОЛОГИЕЙ — строил селектор писателя сам и остался бы красным при
   любом исправлении. Удалён, заменён лексическим инвентарём с названным пределом.

**Вывод для следующей сессии:** проба достижимости обязательна, и её собственные результаты
надо перепроверять — она четыре раза была права и один раз (её первая редакция) сама врала.

## 5. ЧТО СДЕЛАНО И ВЛИТО

- **`П-022`** — решение founder'а «разрезать предмет: готовое вливать, спорное — долгом».
  Влито в `main` (`2179927`, PR #105) после `R-137` REJECT и rev2, закрывшего семь условий.
- **`П-021`** (Fastify) и передача 27.08 — влиты (PR #103, #104).
- **Коллизия `R-138`** — два PR-гейта параллельно взяли один номер, ни один не резервировал.
  Барьер её НЕ ловит (судит push-диапазон, на каждой ветке видит свой файл). Разведено:
  M-71-вердикт переименован в `R-139`.
- **M-71:** оракулы `P4` (состояние сервера) и `P5` (серия потребителя) — пара, порознь ни один
  не достаточен; сторож расхождения тулчейнов в шаге C гейта; инвариант §4bis.5 переписан.
- **M-68:** `d12` перестроен под close-семантику, `d17` (НОК каденции и таймфрейма), `d18`
  (ручка есть конфиг), `d20` (live == replay при заданной каденции), инвентарь `C3ter`.

## 6. ХВОСТЫ, У КОТОРЫХ ЕСТЬ ВЛАДЕЛЕЦ

**Reviewer, на close-out** — карточки долга, тексты готовы, не заведены ни одна:
`A-025` §6 (три, MAJOR) · `A-026` §3bis (одна, MINOR) · `R-143` N-3
(`SNAPSHOT_ENVELOPE_BYTES = 300`, `LEGACY_DRAIN_BATCH = 256` — оценки, не замеры) ·
«свойство стоимости каденции не пиннит ни один оракул» (`M-68` §0sexies.2ter).
**Карточка, названная в вердикте и не заведённая, есть непокрытие, ставшее невидимым — ровно
то, что `П-022` запрещает.**

**Без владельца:** 55 рестартов recorder'а · диск VPS 88 % · локальный диск 88 % при 47
деревьях · 37 висящих резервов номеров (`reserve_artifact_id.sh --sweep 3` показывает
кандидатов и НИЧЕГО не удаляет — «брошенность доказывается молчанием во времени, не снимком»).

**Протухшая строка в документе стартового протокола:** `docs/workflow/reading-map.md:82`
говорит «`GW-I` — 12 оракулов», замер даёт **13**
(`grep -rhoE '\bGW-I-[0-9]+\b' crates/ | sort -uV` → `GW-I-1..12` и `GW-I-14`). Это ложный
факт в документе, который каждая роль читает на старте; правка требует своего круга по §9.

## 7. Cross-references

- Вердикты M-71: `R-133`, `R-139`, `R-140`, `R-143` (все на `feat/M-71-rev6`)
- Вердикты M-68: `R-138`, `R-141` (на `feat/M-68-rev4`)
- Арбитражи: `A-021`, `A-022`, `A-026` (M-71) · `A-018`, `A-023`, `A-024`, `A-025` (M-68)
- Решения founder'а: `docs/PENDING-SIGNATURE.md` `П-020`, `П-021`, `П-022`
- Долг: `TD-117`, `TD-148` (обе про слепоту набора — материал разбора класса), `TD-167`

---

## 8. АДДЕНДУМ 2026-08-28T13:20Z — два места, где §0 и §1 уже НЕВЕРНЫ

Дописано ТЕМ ЖЕ architect'ом через полчаса после §0. Причина — не аккуратность, а класс:
таблица живого состояния протухает быстрее, чем закрывается круг гейта (`SESSION-HANDOFF`
§0ter, четыре срабатывания). Ниже — замер, а не правка по памяти.

### 8.1. M-68 НЕ «ждёт dev» — dev УЖЕ РАБОТАЕТ, и его работа НЕ В `origin`

§1 говорит «ждёт `engine-dev`». На момент аддендума это ложь: разработчик запущен
founder'ом, **сделал все четыре задачи** и ещё собирает.

```
git ls-remote origin refs/heads/feat/M-68-rev4        → 06f6a02   (без его работы)
git -C /tmp/hft-dev-m68-r7 log HEAD --not --remotes=origin --oneline | wc -l → 4
живой процесс в дереве                                 → 1
```

**Четыре коммита живут ТОЛЬКО локально**, ветка `engine-dev/m68-r4`, дерево
`/tmp/hft-dev-m68-r7`:

```
32c88aa docs(M-68): N-2 — статус-колонка §Tasks приведена к правде   [engine-dev]
01da186 feat(M-68): task #24 — старт-гейт зеркалит гвард отношения    [engine-dev]
8e43cfb feat(M-68): task #23 — gateway-checkpoint читает GATEWAY_DEPTH_CADENCE_MS [engine-dev]
6c9ed41 feat(M-68): task #13 — VB-I-2 live==replay при каденции       [engine-dev]
```

**Для следующего агента этой работы НЕ СУЩЕСТВУЕТ** (`gates.md` §8: коммит, живущий только в
worktree, для следующего агента не существует). Пуш — шаг мандата самого dev'а; architect его
коммиты не пушит (push-scope: чужие → СТОП).

**Восстановление, если сессия оборвётся до push'а:** путь и имя ветки выше; работа не потеряна,
но её надо ЗАБРАТЬ — сама она не появится. Первое действие новой сессии по M-68 — проверить
`git ls-remote origin refs/heads/feat/M-68-rev4`: если вершина всё ещё `06f6a02`, работа не
доехала.

### 8.2. Числа §0 по диску и веткам устарели — прошла уборка

| | в §0 | после уборки |
|---|---:|---:|
| диск локально | 88 % | **77 %** (98 ГБ свободно) |
| worktree | 47 | **2** |
| ветки кроме `main` | 31 | **15** |

Освобождено **47 ГБ**: четыре молчащих кэша по 11–12 ГБ. Кэш работающего dev'а не тронут.

**Снесено с предъявлением нулевой потери, а не по имени:**
- 42 дерева — у каждого проверено: ноль незапушенных, чистое, без живого процесса;
- 2 грязных дерева — их дифф сравнён с патчами от 26.08 в
  `/home/nous/salvage-2026-08-26-evening/`: **побайтно идентичны**, новой работы нет;
- 6 веток нулевой потери — три предки `main`, три вложены (`M-68-rev3` ⊂ `rev4`,
  обе `M-71-egress-cap*` ⊂ `rev6`);
- 9 веток закрытых предметов — четыре дубля `M-66` (барьер `review-fa` уже в `main` и
  подключён) и четыре `verdict-gate` (работа брошена посреди круга, `C-101` REJECT).

**ВСЕ 22 неслитые ветки закреплены спасательными рефами** `refs/salvage/2026-08-28-cleanup/*`
(проверено: 22 из 22 в `origin`). **Удаление любой из оставшихся теперь ОБРАТИМО** — чистить
можно смело, содержимое не пропадёт.

### 8.3. Что из 15 оставшихся веток НЕ хлам

Три живые, их трогать нельзя:
- `feat/M-71-rev6` (`f50aac7`) и `feat/M-68-rev4` (`06f6a02`) — активные предметы;
- `docs/phase-decision-and-M-70` (2 сут) — спека включения полос, `M-70` впереди;
- `docs/fa-viz-audit-2026-08-25` (3 сут) — аудит FA;
- `feat/M-68-depth-from-book` — **7 своих коммитов ВНЕ `rev4`**, там арбитраж `A-018`.

Остальные (4–37 сут: `M-10`, `M-60`, `M-64`, `M-67`, `harness-doc-integrity`,
`process-package`, `plan-abc`, `pr65-assessment`, `norms-a010`, `salvage/M-59`) — кандидаты на
снос, все спасены рефами. Решение по каждой требует ОТКРЫТЬ содержимое, а не смотреть на имя;
работа дешёвая и отдана следующей сессии сознательно.
