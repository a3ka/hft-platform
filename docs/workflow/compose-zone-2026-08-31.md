# Зона корневого `docker-compose.yml` — почему она названа явно

**Решение founder'а 2026-08-31** (путь A из `milestones/M-45-persist-l2delta.md` §3ter).
Живёт ВНЕ впрыскиваемого ядра намеренно, по тому же основанию, что `reading-map.md` и
`harness-track.md`: у `.claude/rules/scope-guard.md` бюджет 7 600 B и запас был 332 B
(`scripts/check_context_budgets.sh`). Разбор на 3.5 KB внутри нормы вывел бы ядро за лимит —
барьер это и предъявил на первой редакции правки. В норме осталась одна строка; основание — здесь.

---

**Буква нормы отставала от собственного намерения, и это стоило блокера.** Прежняя редакция
перечисляла содержимое зоны как «cron, Dockerfile, **compose**, метрики-scrape», но привязывала
перечень к каталогу `deploy/**` — а `docker-compose.yml` лежит в КОРНЕ репозитория. Роль,
читающая норму буквально, не имела права объявить операторскую ручку; роль, читающая её по
смыслу, объявляла — и проходила ревью.

**Замер, из-за которого правка принята — ПЕРЕСНЯТ по `R-164` Б-1.**

```
$ git log --format='%h %ci %s' --no-merges -- docker-compose.yml | grep -F '[engine-dev]'
8e43cfb 2026-08-28 feat(M-68): task #23 — gateway-checkpoint читает GATEWAY_DEPTH_CADENCE_MS [engine-dev]
255c359 2026-08-27 feat(M-68): task #22 — GATEWAY_DEPTH_CADENCE_MS env-переменная в serve_config [engine-dev]
3419177 2026-08-26 feat(M-71): task #4 — ручка GATEWAY_MAX_RESPONSE_BYTES в docker-compose [engine-dev]
98ea630 2026-08-13 fix(M-65): М-5 — GATEWAY_MAX_SUBSCRIPTIONS внесён в docker-compose [engine-dev]
564aacf 2026-07-28 feat(gateway+journal+ops): checkpoint-coverage retention gate, … (RN-21) [engine-dev]
c2b926a 2026-07-27 feat(M-37): task #7 — GATEWAY_WINDOW_MS env→Selector.window_ms wired [engine-dev]
b69a791 2026-07-26 feat(M-28): deploy — gateway-serve как сервис в docker-compose.yml [engine-dev]
b54b8c8 2026-07-14 feat(M-08): task #14 — доставка ретеншена в прод (D1-D6, TD-020 виток 2) [engine-dev]

$ git log --format='%s' --no-merges -- docker-compose.yml | grep -cF '[engine-dev]'
8
```

**ВОСЕМЬ правок, семь милестоунов (`M-08`, `M-28`, `M-37`, `RN-21`, `M-65`, `M-68`×2, `M-71`),
полтора месяца, все прошли reviewer-циклы.**

**ПЕРВАЯ РЕДАКЦИЯ ЭТОГО БЛОКА БЫЛА ФАБРИКОВАНА-КАК-ПОКАЗАНА, и след оставлен намеренно.**
Здесь стояла команда `git log --format='%h %s' -- docker-compose.yml | head -4` с выпиской из
четырёх строк. Эта команда такого вывода НЕ ДАЁТ и не давала ни на одной ревизии: первой
строкой идёт merge-коммит `7ed83f1`, а `98ea630` в истории седьмой, не четвёртый. Выписка
была скопирована из `M-45` §3ter без перепрогона — то есть «прошлый прогон» выдан за
свежий ровно в документе, который сам ставит на подлинность словами «не „кажется", а лог».

Поймано `R-164` Б-1. Класс — «утверждение о состоянии без команды, выполненной в том же
ответе» (профиль architect; прецеденты `R-114` Б-1, `R-121` Б-1). Молчаливая замена была бы
второй ошибкой поверх первой: документ-основание нормы обязан показывать, как он ошибался.

**И довод от честного лога СИЛЬНЕЕ подменённого:** восемь правок за полтора месяца по семи
милестоунам — практика не эпизодическая, а сплошная. Курированная четвёрка ослабляла ровно то,
что доказывала. Тот же дефектный блок живёт в `M-45` §3ter и в `C-195`; он чинится на своём
круге, на ветке `docs/M-45-rollout-signature`, и здесь только назван.

**Остаточный долг ядра — записан здесь, потому что передачи ещё нет (`R-164` Б-2).** Тело
коммита `29c9823` утверждало «это записано в передачу»; записи не существовало ни на одном
ref'е — утверждение о будущем, поданное как факт, и это тот же класс, что Б-1. Фактическое
состояние бюджета впрыскиваемого ядра после правки:

```
$ bash scripts/check_context_budgets.sh
OK    .claude/rules/gates.md          47864 B / 47900 B (запас 36 B)
OK    .claude/rules/scope-guard.md     7560 B /  7600 B (запас 40 B)
VERDICT: PASS — 7 файлов, 112247 B из 114900 B бюджета (запас 2653 B)
```

Запас 40 B у `scope-guard.md` и 36 B у `gates.md` означает, что СЛЕДУЮЩАЯ правка любой из
двух норм упрётся в барьер. Лечится вычисткой, а не поднятием лимита (`M-60b` §8): оба файла
несут исторические разборы, которым место вне ядра — по тому же основанию, по которому
вынесен этот документ. Кандидат в работу, а не примечание.

**Что правка НЕ разрешает — границы названы, потому что зона операторская:**
- **секреты — нет** (ограничение прежней редакции сохранено дословно);
- **состав записываемых данных — нет без подписи founder'а.** `L2DELTA_CAPTURE_SYMBOLS`,
  `BINANCE_SYMBOLS`, `HL_COINS`, `EPOCH_ID` суть граница C (`gates.md` §0.1: «состав
  записываемых данных (какие инструменты пишем, что удаляем)»). Роль вправе ВНЕСТИ строку,
  исполняя подписанное решение, и не вправе выбрать её значение;
- **чужие сервисы — нет:** ручки объявляются для своих сервисов, а не для соседних.

Reviewer проверяет это в Block-scope так же, как проверяет `Cargo.toml`: диф показывает
только объявления ручек роли, и каждая смена состава данных имеет подписной документ.
