<!-- FACTS: audited_head=d0a11098d9c81bd671e5c5ae3c6404a471bf07a2 collected=2026-09-04 -->

# Передача сессии architect'а — 03-04.09 → следующая сессия

**Предыдущие передачи** (обе в `main`): `docs/workflow/session-handover-2026-09-01.md` (классы
ошибок, инвентарь) и `session-handover-2026-09-01b.md` (состояние на конец 01.09). Читать их
не обязательно — состояние ниже свежее; но классы ошибок из них живы.

---

## 0. Состояние мира — замер, снятый 2026-09-04T23:35Z

```
$ git rev-parse --short origin/main
c368ed6
$ for b in docs/M-70-rev2 docs/M-73-closeout-architect feat/M-72-... feat/M-76-...
docs/M-70-rev2                           d0a1109
docs/M-73-closeout-architect             e758b56    (несёт M-74)
feat/M-72-subscription-terminality       4866212
feat/M-76-raw-output-barrier             8e30921
$ df -h /
/dev/md2  437G  352G  63G  85% /
```

Ярус S обязателен в начале сессии заново — к моменту чтения это уже история.

## 1. ЧТО ВЛИТО В `main` ЗА СЕССИЮ

| предмет | что сделано |
|---|---|
| **`M-75`** расцепление окна heatmap | **ЗАКРЫТ ПОЛНОСТЬЮ.** Код в `main` (`571403d`, PR #147, `R-169` APPROVED), на проде, деплой-гейт §8 пройден. Close-out architect'а: статус ЗАКРЫТ, спека и гейт в `docs/archive/` (PR #149, `1490948`) |
| правка канонического набора полос | PR #150 (`d0125c7`): в sacred-оракуле `M-75` середина набора была ВЫДУМАНА со ссылкой на источник, который списка не содержит |
| `TD-198`, `TD-199` | заведены reviewer'ом, в `main` |

## 2. M-70 — ГДЕ ОН СТОИТ И ЧТО ЕГО ДЕРЖИТ

**Ветка `docs/M-70-rev2` = `d0a1109`.** Набор architect'а полон, задачи dev'а 3/4/5/6/7
исполнены, задачи 4b/4c/4d/6b — мои — закрыты.

```
$ bash scripts/verify_M-70.sh          → VERDICT: FAIL (1), 41 PASS, exit=1
$ cargo test --workspace --no-fail-fast → passed=977 failed=1
единственный красный: gw_i_4_holds_when_the_tail_frame_is_delta_only
```

**Держит его ОДНА внешняя зависимость — `TD-199`, и под неё заведён `M-77`** (см. §3).
`M-70` мержится ПОСЛЕ его починки и ЖДЁТ её как чужую работу (`П-022`, спека §2bis.-2).

**Гейты пройдены:** `C-193` REJECT → `C-208` REJECT → `C-209` NOTE → `C-210` NOTE (узкий
FA-круг); PR-гейт `R-170` → `R-171` → `R-172`, все три REJECT по РАЗНЫМ основаниям, все
закрыты.

**Что ещё не сделано в `M-70`:** задача 8 (`VB-I-10` не ослаблен — проверка прогоном
существующих оракулов). Она ждёт починки `TD-199`, потому что до неё набор всё равно красен.

## 3. `M-77` — НОВЫЙ ПРЕДМЕТ, ЗАВЕДЁН В КОНЦЕ СЕССИИ

`milestones/M-77-frame-book-continuity.md` (на ветке `docs/M-70-rev2`, коммит `d0a1109`).

**Суть:** депт-серия считается ИЗ КНИГИ, а кадр строится редьюсером, у которого книги нет.
Клиент, собравший состояние из `snapshot(C) + frames`, получает 3 точки вместо 4.

**Найдено чтением кода и ШИРЕ, чем числит карточка `TD-199`:** карточка называет только
`frames_since` (offline). Второй носитель — `LiveReducer::pump` (`lib.rs:4070`), и это
**ПРОД-путь** push-цикла (`gateway-serve/src/lib.rs:1374`). Переносятся только скаляры VWAP
(`:4071-4072`), `grep -c book` по блоку переноса → `0`.

**Спека НЕ назначает реализацию** — названы две развязки с ценой каждой (перенос книги в
batch против депт-дельты от живого редьюсера); выбор за кругом критика.

**Первый шаг — мой:** RED-оракул на ПРОД-путь (`pump`), потому что существующий судит
`frames_since`. Без него развязка починит offline и оставит прод сломанным.

## 4. РЕШЕНИЯ, КОТОРЫЕ ЖДУТ FOUNDER'А

1. **Порядок `M-77` относительно `M-70`.** Чинить `M-77` до merge `M-70` либо держать `M-70`
   на паузе. `DESIGN` §17 даёт довод к срочности: инверсия снимок/дифф делает якорь в 10-30
   раз реже, и окно, в котором клиент видит не то, что в журнале, растёт на порядок.
2. **Процессная правка в запертой зоне.** `.claude/agents/reviewer.md` §Handoff говорит
   «REJECT → dev-агент», что расходится с указанием founder'а от 04.09 («весь разбор —
   architect'у»). Нужен токен `FOUNDER-APPROVED` (`gates.md` §11). Нашёл reviewer, править
   не вправе ни он, ни я без основания.
3. **Коллизия бампа с `M-72` СНЯТА фактом:** `M-70` бампнул `GATEWAY_SCHEMA_VERSION` 9 → 10,
   `M-72` ещё не начинал. Если `M-72` тоже бампнет — это будет его предмет. Решения не требует.
4. **Чекпоинтер и состав полос** (§5ter п.1) — инженерно ответ «да, должен», иначе тёплый
   старт вырождается в холодный; но это `deploy/**` и состав выдачи, поэтому спрашиваю, а не
   решаю. Ответ нужен ДО merge `M-70`.

## 5. КЛАССЫ ОШИБОК, ОПЛАЧЕННЫЕ ЭТОЙ СЕССИЕЙ

**1. Форма объявлена без сверки с живым инвариантом — два круга гейта.** Я объявил форму
задачи 4 (`Vec<DepthPoint>`, удаление поля), не сверившись с `VB-I-4` («старые консюмеры v1
не ломаются»). Форма оказалась ЛОМАЮЩЕЙ. `R-171` Б-1 это поймал; переделка стоила двух
кругов. **Приём:** перед объявлением формы — открыть инварианты модуля, а не вспоминать их.

**2. Ложный якорь мутации — трижды за сессию.** Мутацию накладывал на код, который на
проверяемом пути НЕ ИСПОЛНЯЕТСЯ, получал зелёное и чуть не делал вывод «оракул слеп».
Случаи: `ServeConfig`-клон вместо несущего пути (`M-70` B-3); `evict_series_bundle_under_window`
вместо внутреннего состояния редьюсера; и там же второй раз.
**Приём:** проследить путь ДО мутации — от точки входа до наблюдаемого выхода, — а не после.

**3. Страж, самовосстанавливающийся кодом.** `DB-I-4c` сторожил равенство длин; на пути
склейки `zip` МОЛЧА выравнивает длины, разрушая связку «точка ↔ метка». То есть признак,
который код чинит сам, разрушая данные. **Приём:** спрашивать не «наблюдаем ли признак», а
«может ли код сделать признак истинным, не исправив дефект».

**4. Утверждения о состоянии протухают в собственной спеке.** `R-170` нашёл строку «dev не
диспетчеризуется», пережившую четыре ревизии и блокировавшую собственный запуск милестоуна,
и три протухших числа гейта. **Приём:** правка живого состояния — отдельной строкой в шапке,
а история ревизий явно помечена историей.

**5. Правка НАЗВАННОГО места вместо КЛАССА — снова.** `R-172` Н-3 назвал три комментария;
греп класса нашёл четвёртый носитель. `R-170` Н-2 назвал одну строку — греп нашёл вторую.
**Приём:** правка по вердикту НАЧИНАЕТСЯ грепом класса и ЗАКАНЧИВАЕТСЯ предъявлением НУЛЯ.

## 5bis. ВСЕ ШЕСТЬ ПРЕДМЕТОВ — состояние одной таблицей (замер 2026-09-04)

| предмет | где | состояние |
|---|---|---|
| **`M-45`** раскатка L2Delta на ETHUSDT | `main` | **✅ ЗАКРЫТ 2026-09-01.** Эфир пишется с 18:48Z 01.09; эпоха `E-002` закрыта; барьер состава записи (`check_rollout_composition.sh`) в агрегате CI. Долг: sanity ПО ПЛОЩАДКАМ невыполнима наблюдением — `venue` в событии перечисление, декодера на сервере нет; `П-026` §Порядок п.3 исполнен НЕ ПОЛНОСТЬЮ |
| **`M-75`** расцепление окна heatmap | `main` | **✅ ЗАКРЫТ 2026-09-02.** Код `571403d` на проде, close-out architect'а `1490948`, спека и гейт в `docs/archive/`. Долг `TD-197`: окно не объявлено в кадре |
| **`M-70`** полосы глубины | `docs/M-70-rev2` `d0a1109` | Набор полон, задачи 3-7 исполнены. `VERDICT: FAIL (1)`, 41 PASS — держит ТОЛЬКО `TD-199`. Осталась задача 8 (проверка `VB-I-10`), она ждёт починки |
| **`M-77`** кадр строится без книги | там же, `d0a1109` | **НОВЫЙ, заведён в конце сессии.** Спека написана, реализацию НЕ назначает: две развязки с ценой. Первый шаг — мой RED на прод-путь |
| **`M-74`** читаемость офсайт-копии | `docs/M-73-closeout-architect` `e758b56` | Разведка приземлена; **оракулы 1b/6b/1c НЕ написаны**. Счётчика для стража уникальности не хватает — прецедент развязки найден в `reserve_artifact_id.sh`. Проба обёртки всё ещё на снятой конструкции rev4 (задача 1c) |
| **`M-72`** терминальность подписки | `feat/M-72-subscription-terminality` `4866212` | `C-200` REJECT: B-2/B-3/B-4 не закрыты. **Круг 4 ПОСЛЕДНИЙ** по `A-029` §2 — при REJECT предмет идёт founder'у |
| **`M-76`** барьер подлинности выписок | `feat/M-76-raw-output-barrier` `8e30921` | `PROPOSED`. Шапка говорит «запускается ПОСЛЕ `M-45`» — `M-45` закрыт, то есть предмет РАЗБЛОКИРОВАН и ждёт очереди. Гипотезы проверены: структурные признаки отвергнуты замером (17 ложных), исполнение+сравнение подтверждено |

**Что из этого работает на проде:** только `M-45` и `M-75`. Остальные пять — на ветках.

## 5ter. ТРИ ХВОСТА, которые я НЕ закрыл и не имею права умолчать

**1. Вопрос dev'а БЕЗ ОТВЕТА — чекпоинтер и состав полос.** Задача 7 сменила `GATEWAY_BANDS`
в блоке `gateway-serve` на канонические семь полос. У сервиса `gateway-checkpoint` аргумент
`--bands` остался с прежним дефолтом `0.001`; dev спросил, должен ли он тоже стать
каноническим, и я не ответил. **Почему это не мелочь:** чекпоинтер строит состояние, из
которого стартует выдача, а имя файла чекпоинта детерминировано `selector_fingerprint`, куда
полосы ВХОДЯТ (`lib.rs:2941` и далее). Разные наборы у писателя и читателя ⇒ чекпоинт не
найден/отвергнут ⇒ тёплый старт молча вырождается в холодный. Зона — `deploy/**`
(engine-dev). **Решить ДО merge `M-70`.**

**2. `P-022` — идентификатор, которого не существует.** Критик в `C-209` завёл долг под
`P-022`. Замер: `next_artifact_id.sh P` → «неизвестный класс 'P' — допустимы: TD R C A M»;
похожий `П-022` занят решением founder'а от 26.08. Записано в `M-70` §3quinquies с запретом
тиражирования. **Предмет того долга реален и УЖЕ ЗАКРЫТ** (это `R-170` Н-3, протухшие числа
гейта), но ссылка на `P-022` ведёт в пустоту — не цитировать.

**3. Спас-рефы `M-75` больше не нужны — проверено замером, а не предположено.**
`m75-b5-inventory`, `-r2`, `m75-b6-serial` несли приём с `serial()`. На `main`:
`grep -c '^fn serial()'` → `1` во всех четырёх файлах (`red_depth_from_book`,
`red_depth_provenance_by_reach`, `red_heatmap`, `red_egress_cap`). Приём влит, рефы —
история. Сносить не тороплюсь: они дёшевы, решение — founder'а.

## 5quater. ДВА РЕШЕНИЯ АРБИТРА, КОТОРЫХ В `main` НЕТ — читать С ВЕТКИ

Замер 2026-09-05: `git ls-tree origin/main research/arbitration/` даёт `A-024`, `A-025`,
`A-026`, `A-027`, `A-030`, `A-031`. **`A-028` и `A-029` в `main` ОТСУТСТВУЮТ** — они живут
только на ветках своих предметов:

| решение | где лежит | чем обязывает |
|---|---|---|
| `A-028-m74-pre-dispatch-completeness.md` | `origin/docs/M-73-closeout-architect` | полнота набора ДО диспетчеризации (`M-74`); §4 — терминальное правило кругов |
| `A-029-m72-sign-discriminating-power.md` | `origin/feat/M-72-subscription-terminality` | различающая сила признака; **§2:150 «круг 4 — последний»**, дальше предмет идёт founder'у |

**Почему это записано отдельной секцией.** Утверждение «круг 4 последний по `A-029` §2»
кочует по передачам, а грепом в `main` его основание НЕ НАХОДИТСЯ. Следующая сессия либо
решит, что ссылка ведёт в пустоту (как `P-022`, §5ter п.2), либо примет её на веру — оба
исхода плохи. Открыто и проверено: `A-029` §2 действительно содержит «Круг 3 REJECT ⇒
правка, **круг 4 — последний**» и «Круг 4 снова находит класс … ⇒ СТОП, не круг 5».

Решения арбитра обязательны обеим сторонам (`gates.md` §0) — то есть эти два обязывают,
находясь вне `main`. Влить ли их в `main` отдельным PR — вопрос к founder'у: они приедут
вместе со своими предметами при merge, но до тех пор ищутся только по имени ветки.

## 6. ЧТО СТОИТ И ЖДЁТ

| предмет | состояние |
|---|---|
| **`M-74`** читаемость офсайт-копии | Разведка приземлена (`e758b56`): счётчика для стража уникальности НЕ ХВАТАЕТ, прецедент развязки найден в `reserve_artifact_id.sh`; проба обёртки всё ещё на снятой конструкции rev4. **Оракулы 1b/6b/1c НЕ написаны.** Самый дорогой незакрытый: копия уходит, читаемость не доказана |
| **`M-72`** терминальность подписки | `C-200` REJECT, круг 4 ПОСЛЕДНИЙ по `A-029` §2 |
| **`M-76`** барьер подлинности выписок | `PROPOSED`, спека заведена, гипотезы проверены |

## 7. Стартовый промпт следующей сессии

```text
Ты — architect hft-platform. Сессия начинается со стартап-протокола, ПОТОМ работа.

ШАГ 1. СТАРТАП-ПРОТОКОЛ — предъявляется РЕЗУЛЬТАТОМ. Читать С ДИСКА, целиком:
  CLAUDE.md · .claude/rules/*.md ВСЕ ШЕСТЬ · .claude/agents/architect.md
  docs/workflow/reading-map.md
  ЯРУС A: docs/DESIGN.md · docs/04-workflow.md · docs/05-contract-layer.md ·
          docs/SESSION-HANDOFF.md §0/§0ter · grep -n '^## П-' docs/PENDING-SIGNATURE.md
  ПЕРЕДАЧА: docs/workflow/session-handover-2026-09-04.md — ЭТА, целиком
  ПРАВИЛА ОРАКУЛОВ Р-1…Р-4: docs/workflow/oracle-blindness-class-2026-08-28.md §5
  РЕШЕНИЯ АРБИТРА: research/arbitration/A-030-* (форма факта) · A-031-* (выписанная группа)
  ВНИМАНИЕ: A-028 и A-029 в main ОТСУТСТВУЮТ (§5quater передачи). A-028 — на ветке
  docs/M-73-closeout-architect (нужно для M-74), A-029 — на feat/M-72-subscription-terminality
  (там основание «круг 4 последний»). Грепом по main они НЕ находятся — это не пустая ссылка.
  ЯРУС B по предмету: docs/fa/viz-backend.md ЦЕЛИКОМ (для M-70/M-77)
  ДОЛГ в первый Handoff: у gateway, gateway-serve, derive, recorder FA НЕ СУЩЕСТВУЕТ.

ШАГ 2. ЯРУС S — четыре команды, сырой вывод:
  gh run list --branch main --limit 5
  gh run list --workflow=deploy.yml --limit 3
  ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && git rev-parse --short HEAD; df -h /; \
     cat /var/lib/hft/journal-offsite.last-success; \
     cat /var/lib/docker/volumes/hft-platform_journal-data/_data/recorder.heartbeat'
  bash scripts/check_branch_health.sh

ВСЕ ПРЕДМЕТЫ И ИХ СОСТОЯНИЕ — §5bis передачи, таблицей. Коротко:
  M-45 ✅ закрыт (прод) · M-75 ✅ закрыт (прод) · M-70 готов, держит TD-199 ·
  M-77 новый (спека есть) · M-74 разведка есть, оракулов нет · M-72 круг 4 ПОСЛЕДНИЙ ·
  M-76 PROPOSED, разблокирован закрытием M-45.
  Вершины брать git ls-remote — записанные SHA протухают.

СОСТОЯНИЕ: M-70 готов и держится ОДНОЙ внешней зависимостью — TD-199, под неё заведён M-77
(milestones/M-77-frame-book-continuity.md на ветке docs/M-70-rev2 @ d0a1109).
  bash scripts/verify_M-70.sh → VERDICT: FAIL (1), 41 PASS — единственный красный это TD-199.

ПЕРВЫЙ ХОД — спросить founder'а о порядке (§4 передачи), и по его слову:
  · если M-77 первым: написать RED-оракул на ПРОД-путь (LiveReducer::pump), затем круг
    критика по спеке M-77 — она НЕ назначает реализацию, две развязки с ценой названы;
  · если M-70 на паузу: взять M-74 (оракулы 1b/6b/1c) — разведка уже приземлена;
  · M-72 (круг 4 ПОСЛЕДНИЙ) и M-76 (разблокирован) — по слову founder'а.
НЕ ВЫБИРАТЬ САМОМУ: порядок предметов — воля владельца, не инженерная правота.

ЖЁСТКИЕ РАМКИ:
 · Роли цепочки (critic/reviewer/dev/tester/адверсарий/арбитр) запускает founder. Если он
   говорит «запускай сам» — только subagent_type: architect, модель НАЗЫВАТЬ в запуске,
   контекст свежий. Мандат клону ОБЯЗАН первой строкой требовать отчёт В ФАЙЛ: возврат в чат
   терялся дважды за прошлую сессию.
 · «Документ X говорит Y» требует ОТКРЫТИЯ X. Отчёт агента — гипотеза, git — факт.
 · Утверждение о состоянии — ТОЛЬКО с командой в ТОМ ЖЕ сообщении. Время — date -u.
 · PUSH И MERGE СТОЯТ ЗА ПРОВЕРКОЙ КОДА ВОЗВРАТА, а не рядом.
 · Мутация — в ОТДЕЛЬНОМ worktree; ПРОСЛЕДИ ПУТЬ ДО мутации, а не после (класс §5.2).
 · Форма объявляется ПОСЛЕ открытия инвариантов модуля, а не по памяти (класс §5.1).
 · Правка по вердикту НАЧИНАЕТСЯ грепом класса и ЗАКАНЧИВАЕТСЯ предъявлением НУЛЯ.
 · Коммит называет пути ЯВНО; commit -a и add -A запрещены хуком.
 · Номера — ТОЛЬКО scripts/next_artifact_id.sh. Полные SHA — git rev-parse.
 · Ответ заканчивается Handoff-блоком из пяти секций.

ЗАКРЫТИЕ СЕССИИ: bash scripts/check_unreachable_work.sh (FAIL закрывать ЗАПРЕЩАЕТ) →
спасти каждый FAIL спас-рефом → передача и промпт в main своим PR → gc_worktrees.sh.
```
