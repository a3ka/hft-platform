<!-- FACTS: audited_head=721461cb7d41a625b28026f73d1aa5a3e62091cd collected=2026-08-25 -->

# Передача сессии architect'а — 2026-08-25

**Главное в одной строке:** два PR стоят зелёными и ждут разного — один можно мержить
сразу, второй ждёт круга критика; продуктовая работа (M-68 rev3) заблокирована вторым
и стартует немедленно после него.

Продолжение `session-handover-2026-08-24c.md` (в `main`).

## 0. Состояние мира

```
main          721461c   CI+Deploy success
прод          bba42cd   hft-recorder + hft-gateway-serve — healthy
открытых PR   2         #91 (харнесс) · #96 (FA viz-backend)
веток origin  27        worktree 19 · диск 76 %
серия RSS     4/24      /home/nous/rss-series-2026-08-25/series.tsv, сборщик отвязан от сессии
```

## 1. ДВА ОТКРЫТЫХ PR — что с каждым делать

### PR #91 — `harness/three-guards-2026-08-24` — **МЕРЖИТЬ**

Head `6fa0152` (+ вердикт критика `1a94cc4`). Гейт трека пройден **полностью**:
вердикт круга 9 — `C-154`, тир **NOTE**, `audited_head` = `6fa0152`.

Мержит **автор-architect** (`harness-track.md` §3: reviewer в маршруте трека не участвует).
Порядок: `gh pr checks 91 --watch` → по КОДУ ВОЗВРАТА → `gh pr merge 91 --merge
--delete-branch` → деплой-гейт `gates.md` §8 (CI+Deploy до терминального статуса + ssh).

Девять кругов гейта, два арбитража. Предмет: сторож «не удаляй дерево, если в нём живёт
процесс» (инцидент 24.08 — уборщик снёс каталог работавшего критика).

### PR #96 — `docs/fa-viz-truth-2026-08-25` — **ЖДЁТ КРУГА 3 КРИТИКА**

Head `8aa20f6`, merge-base `721461c`, чеки зелёные. Два круга критика (`C-148`, `C-149`)
и арбитраж (`A-019`) — все на ветке. Промпт круга 3 — §4 ниже.
**Не мержить до вердикта:** правка меняет текст инвариантов, `gates.md` §9 требует критика.

## 2. Что закрыто этой сессией

| предмет | как |
|---|---|
| разбор завалов | worktree 76 → 5, локальных веток 151 → 39, грязных 30 → 0; salvage в `/home/nous/salvage-2026-08-24/` |
| `TD-164` | адверсарий `C-139` NOTE → PR #92 влит |
| `TD-165`/`TD-166` | PR #95 влит: оракул `warn_on_default` перестал зависеть от часов (замер: старый 5 ложно-зелёных из 60, новый 0/60) |
| M-69 close-out | PR #88, PR #90 (архивный переезд по норме Р-2) |
| решение founder'а `A-015` | PR #87 |
| M-65 close-out | PR #94, набор `R-118` сошёлся целиком |
| фикс `R-118` N-2/N-3 | PR #89 — влит reviewer'ом, **на проде** |

## 3. ДОЛГИ ARCHITECT'А — очередь

1. **Мерж #91** (см. §1) + деплой-гейт.
2. **Круг 3 критика по #96** → исполнить вердикт → приземлить.
3. **M-68 rev3** — НОВАЯ ветка от `origin/main`, состав в `A-018` §2.3 (шесть пунктов).
   Ветка `feat/M-68-depth-from-book` **ТЕРМИНАЛЬНА, merge запрещён**: несёт `1b9e0c9` с
   неподписанным переходом фаз, история не переписывается.
   **Посылка rev2 ЛОЖНА** — дефект в КАДЕНЦИИ, не в дальности: депт-серия пересчитывается
   только на `L2Snapshot` (1 Гц), дельты (100 мс) её не двигают
   (`crates/gateway/src/lib.rs:986`). Гипотеза уже записана в `fa/viz-backend.md` §4 как
   предусловие (б) `П-014` — не переоткрывать.
   В §Cross-references rev3 записать: **милестоун включения полос (`П-014` пп.1/3/4)
   заводится ПОСЛЕ rev3** (решение founder'а 25.08), с egress-гейтом из `C-094` B5
   (22 МБ/клиента, 220 ГБ на снапшот при 10k).
4. **К-ветка** `К1.0 → К1.1 → К1.2 → К2 → …` — после милестоуна полос. RED для `К1.0`
   найден: `OhlcvRow` объявлен независимо в `crates/gateway/src/lib.rs:181` и
   `crates/research-cli/src/export_io.rs:84`, формы совпадают побайтно; класс `CT-I-1`.
5. **Серия RSS** — досмотреть до 24 точек. Первые четыре дали ПЛАТО (25.8 → 26.0 MB за
   12.7→15.7 ч); ревьюер `R-129` был прав. Долг заводить не нужно, если ряд не сломается.

## 4. Промпт критику круга 3 по PR #96 — paste-ready

```
Круг 3 гейта §9 по PR #96, ветка docs/fa-viz-truth-2026-08-25,
HEAD 8aa20f68ffe09639117acb4af9bdb3f7862e37ab, merge-base 721461cb7d41a625b28026f73d1aa5a3e62091cd.
Модель СИЛЬНАЯ: FA несущего слоя. Дерево: git worktree add /tmp/hft-critic-fa-r3 <ветка>.
Startup: CLAUDE.md · .claude/rules/*.md все шесть · .claude/agents/critic.md ·
reading-map.md · DESIGN §22 · 05-contract-layer §2. Ярус B: fa/viz-backend.md · fa/book.md ·
07-cockpit-backend-roadmap.md · A-002 · depth-verdict.md · тела П-011/П-013/П-014/П-017.
ТВОИ ЖЕ КРУГИ на ветке: C-148, C-149, и АРБИТРАЖ A-019.

Предмет — ОДИН коммит 8aa20f6: механическое исполнение A-019 §3 (два ханка).
Спор по B-2 РАЗРЕШЁН арбитражем и НЕ переоткрывается (gates.md §0).

Проверить исполнением:
1. Дословность: A-019 §3 предписал точный текст. Расхождение — блокер само по себе.
2. Не прибавилось ли НОВЫХ утверждений о коде. Проверь каждое: CobLevel :221-225 без
   provenance · deep.then :1228/:1244 · prov_str :1212 · окно heatmap = max(bands) :1192 ·
   depth_provenance_label :1363.
3. TD-161: в main, Ф4/MINOR, текст врезки ей соответствует?
4. КЛАСС, не экземпляр: нет ли ИНЫХ мест, приписывающих подписи обязательств вне её текста.
   Пройди §4 и §5 перебором.
5. Регресс кругов 1-2: B-1 (VP сужен до SVP) и B-3 (дом GS-I — M-28:49-53, VB-I-9 → 9a/9b).
6. verify_design_claims.sh на ДЕРЕВЕ СЛИЯНИЯ refs/pull/96/merge, не на ветке.

Рамка: блокируешь ложное утверждение или потерю верного, предъявленные ИСПОЛНЕНИЕМ.
Мораторий П-017 A3: новых нормативных единиц не вводить, задача документа — ПРАВДИВОСТЬ,
не полнота. Дом GW-I/GS-I и состав выдачи не решаются.
Зона: РОВНО один research/critiques/C-NNN-*.md. docs/**, crates/** read-only. rebase
запрещён. Номер: reserve_artifact_id.sh C. GATE-META: milestone M-60a,
audited_base=721461cb7d41a625b28026f73d1aa5a3e62091cd,
audited_head=8aa20f68ffe09639117acb4af9bdb3f7862e37ab.
Вердикт ФАЙЛОМ, коммит+push на ветку в ТОМ ЖЕ прогоне. Повтор B-2 недопустим.
```

## 5. Два арбитража этой сессии — читать до касания предметов

**`A-019`** (на ветке #96) — словарь провенанса и граница `П-014`. Ключевое: предмет
`П-014` п.1 — ДЕПТ-СЕРИЯ; heatmap и COB в подписи не упомянуты. Писать «предусловие не
закрыто для heatmap» — расширение чужой подписи. Факт сохранён как `TD-161`.

**`A-020`** (на ветке #91) — текстовая канарейка против структурной изоляции. Установлено
ДОКАЗАТЕЛЬНО: лексический инструмент не может установить провенанс значения в bash
(предъявлены `local X="$X"`, nameref, `${!x}`; дальше `eval`, `printf -v`). Решение —
дочерний процесс + `env -i` с закрытым белым списком.

**Правило границы из `A-020`, применимое ко ВСЕМУ треку** (два признака, каждый наблюдаем):
1. Два круга подряд находят экземпляры ОДНОГО перечислимого класса ⇒ следующий ход —
   смена конструкции или именование предела, НЕ третий экземпляр покрытия.
2. Находка круга живёт в мета-оракуле (страж пробы, канарейка, извлечение тела), а не в
   поведении предмета ⇒ инструмент не на том уровне абстракции.

## 6. Уроки — все мои, все пойманы гейтами

1. **Пять раз подряд я чинил ЭКЗЕМПЛЯР там, где критик показывал КЛАСС.** Перебор по
   построению не доказывает отсутствие зависимости: он покрывает то, о чём вспомнил автор.
2. **Триггер арбитража §0 сработал на `C-145`, арбитра созвали на седьмом круге.** Четыре
   круга прошли при уже сработавшем правиле. Считать круги — обязанность, а не формальность.
3. **Пустой kill-set мутанта — это находка, а не шум.** Дважды за сессию он вскрывал строку,
   которую не пиннит ни один сценарий. Ответ: сценарий на её случай, если случай реален;
   удаление строки, если нет.
4. **Ложное КРАСНОЕ на честной работе опаснее пропуска:** страж, мешающий работать,
   выключат первым же «он мешает».
5. **Отчёт агента — гипотеза.** Три агента подряд вернули «действий не требуется», имея
   готовый результат. Мандат ОБЯЗАН требовать ФАЙЛ: канал возврата ненадёжен, и это класс
   M-49. Мой мандат аудита FA этого не требовал — 242k токенов работы пропали.
6. **Правка о правдивости сама подчиняется правилу правдивости.** В правке FA, снимавшей
   ложные утверждения, я внёс три новых — и получил `REJECT` дважды.

## 7. Ждёт founder'а

- **`П-018`** (в `main`) — два решения арбитра 19.08, до founder'а не дошедшие.
- **`П-003`** — токены Telegram + cron: алертинг написан, канал молчит.
- **`GW-I`/`GS-I` — дом семейств.** Замер: `GW-I-1..12` и `GW-I-14` в `crates/`;
  `DESIGN` §22 числит «заявлено 0 / в оракулах 13» и называет это обратным дрейфом;
  `GS-I-*` объявлены в `milestones/M-28-gateway-serve.md:49-53`, а не в FA. Вариант «новый
  FA-файл» упирается в решение «ничего нового» и требует явного слова founder'а.
- **`TD-161`** (заведён reviewer'ом) — рассогласование словаря метки. Закрывается либо в
  составе милестоуна включения полос, либо отдельным решением.

## 8. ЧТО ЖИВЁТ ВНЕ РЕПОЗИТОРИЯ — пропадёт, если не знать

**1. Одиннадцать спасённых коммитов держатся ТОЛЬКО локальными рефами и бандлом.**
24.08 разбирались 76 worktree; у одиннадцати `HEAD` не был достижим ни из одного
`origin`-ref. Побайтная сверка показала, что ни один ФАЙЛ не отсутствует в `main` (это были
более ранние редакции и отвергнутая альтернатива `M-61` B4Q), но сами коммиты уникальны.

```
git for-each-ref 'refs/salvage/2026-08-24/*'          → 11 рефов, в origin их НЕТ
/home/nous/salvage-2026-08-24/unpushed-worktrees-2026-08-24.bundle   (9.9 MB, verify: complete)
/home/nous/salvage-2026-08-24/README.md               → фактура разбора, 65 строк
/home/nous/salvage-2026-08-24/worktrees/              → 30 диффов рабочих деревьев, 33 MB
```

Рефы живут в `.git` этого клона: переживают сессию, но НЕ переживут пересоздание клона.
Бандл самодостаточен и лежит вне репозитория. **Отдельно:** `salvage/M-61-b4q-alt`
(`57825a3`) и `salvage/M-61-alt-impl` (`04b84ce`) исчезли из `origin` вне записанной
процедуры — `SESSION-HANDOFF` §0bis числит их живыми, это неверно; первый спасён рефом,
второй в деревьях не найден.

Решение о судьбе рефов не принято: держать до конца недели, затем удалить, оставив бандл.

**2. Серия замеров памяти идёт отдельным процессом.**
`/home/nous/rss-series-2026-08-25/collect.sh`, запущен `setsid`, от сессии отвязан.
Пишет `series.tsv` раз в час, 24 точки. Первые четыре — ПЛАТО (26432 → 26612 kB за
12.7 → 15.7 ч), то есть `R-129` §E прав. Если ряд не сломается — долг не заводить.

**3. `.omc/` в корне — untracked и таким остаётся.**
Оснастка `oh-my-claudecode`, не артефакт проекта. Не коммитить, не удалять. Появляется в
`git status` каждой сессии и каждый раз выглядит как «чужой untracked» из
`branch-hygiene.md` п.7 — это известное исключение.

**4. Резервы номеров (`refs/reserved/*`, ~30) висят непотраченными по видимости.**
Проверка «есть ли артефакт в `main`» даёт ложную тревогу: артефакты живут на НЕВЛИТЫХ
ветках (`C-148`/`C-149` на #96, `C-153`/`C-154` и `A-020` на #91, `A-018` на M-68). Резерв
снимается при merge ветки. Гоняться за ними отдельно не нужно.

**5. Два дерева критика — merge-превью, не работа:** `/tmp/hft-critic-fa-pr96-merge`,
`/tmp/hft-critic-fa-pr96-r3-merge`. Их `HEAD` — снимки `refs/pull/96/merge` (родители:
`main` + вершина ветки), содержимое выводимо. Сносить безопасно.
