<!-- FACTS: audited_head=ffb1d0231073f358d78ba7a0c49e1d4e3f8381f1 collected=2026-08-24 -->

# Передача сессии architect'а — 2026-08-23/24

**Главное в одной строке:** харнесс впервые СОКРАТИЛСЯ (23 945 → 22 311 строк), два барьера
на пути доставки чинились под гейтом и один ещё в круге, а самый дорогой урок сессии — про
правило, которое у нас уже есть и которое я не применил.

## 0. Состояние мира

```
origin/main   ffb1d02   CI success · Deploy success · прод обновлён (M-65 в образе)
харнесс       22 311 строк (было 23 945 на 92f6e65)
гейты         scripts/verify_M-*: 21 · docs/archive/verify_M-*: 25
диск          81 %
открытые PR   #75 (TD-163) · #66 (барьер A) · #32 (M-69)
```

## 1. Влито за сессию

| предмет | что |
|---|---|
| **#56** | архивный класс вердиктов (`R-108` PASS) |
| **#28** | барьер ресурсных оракулов (`C-123` PASS) |
| **П-014 круг 2** | `686327f` + close-out `1d4b2fa`; мой RED на дельта-хвост, `R-113` APPROVE |
| **#65** | subject-lock мерил работу `main`а и приписывал ветке — починен (`8d11b74`) |
| **#60** | вынос 24 гейтов закрытых милестоунов, −3 200 строк (`4e1a881`) |
| **TD-156** | закрыт как ПРИНЯТЫЙ ОСТАТОЧНЫЙ РИСК (`R-117`), переименований ноль |
| **M-65 / A-015** | шаги 3-6 маршрута: оракул, impl, §Tasks, `R-118` APPROVE, влит `63c3866`, прод обновлён |

## 2. НЕ ЗАКОНЧЕНО — забирать первым

### 2.1 `#75` — `TD-163`, БЛОКИРУЕТ ДОСТАВКУ ВСЕГО ОСТАЛЬНОГО

Барьер `check_protected_artifacts.sh` красит `main` при merge ЛЮБОЙ ветки, форкнутой до
архивного переезда `bd357df`. Сорвал деплой дважды (`R-118` §10). **Оба оставшихся PR (#66,
#32) форкнуты ДО `bd357df`** — их merge повторит срыв.

- круг 1 адверсария: `C-136` **REJECT** — мой fallback открывал ложное зелёное (древний токен
  благословлял evil merge воссозданного пути);
- круг 2 сделан: сужение «fallback не применяется к пути, ПОЯВИВШЕМУСЯ в диапазоне», сценарий
  `P23`, исправлено ложное утверждение в комментарии барьера. Проба `23/23`, семь барьеров
  дерева `exit=0`, реальный merge `#66` → `exit=0`.
- круг 2 ЗАКОММИЧЕН и ЗАПУШЕН: `3edb657`. `verify_M-60b.sh` → `PASS=60 FAIL=0`, exit=0;
  проба `23/23`; мутация «барьер откачен к `28b9500`» → `FAIL P23`; реальный merge `#66` →
  `exit=0`, 0 FAIL, 24 NOTE.
- **НЕ СДЕЛАНО: круг 2 адверсария.** Без его вердикта харнесс-трек merge не разрешает
  (`harness-track.md` §3). Это ЕДИНСТВЕННОЕ, что отделяет `#75` от вливания.

### 2.2 Порядок доставки — строгий

`#75` → `#66` → `#32`. Иначе каждый merge рвёт деплой.

### 2.3 Хвосты, переданные мне и не сделанные

| что | откуда | зона |
|---|---|---|
| **M-69 factual flip** — синхронизировать `DESIGN.md` §22 под GREEN `GW-I-14` | `A-014` §5 п.3; engine-dev закончил `7ad7acb`, verify PASS | architect, ДО PR-time |
| **close-out M-65** — сверка §Tasks 1-15 с фактом, статус милестоуна | `R-118` §D задача 3. `PROJECT-STATE`/`TECH-DEBT` НЕ трогать | architect |
| `verify_M-65.sh` шаг F: `EXPECT_TESTS=11` отстаёт от факта 12 | `R-118` N-1 | architect |
| `verify_M-65.sh` шаг L: сценарий «absent» без `env -u` — нет setup-guard'а | `R-118` N-4 | architect |
| литерал `16` в тексте `warn!` (`lib.rs:1902`) вместо константы — лог может врать | `R-118` N-2 | **engine-dev**, диспетч founder'а |
| `FA-WAIVER` в `d784e15`: ложная причина, несуществующий `GS-I-3` | `R-118` N-3 | **engine-dev** |
| хвост П-014: `TD-158`/`TD-159` (возможно одна работа), `TD-160`, `TD-161`, `Н-3` | `R-113` §D | architect |
| профиль `a219fe4` (ветка `docs/architect-profile-facts-fix`) ждёт круга критика §9 | зона замка §11, токен founder'а получен | architect |
| запись решения founder'а (а) — ветка `docs/founder-decision-a015-2026-08-23`, `R-116` APPROVE | стоит ЗА `#66`, ссылается на его документ | architect |

## 2.4 ГДЕ ЧТО ЛЕЖИТ — читать ДО того, как искать

**Три ветки несут готовую работу и НЕ ИМЕЮТ PR.** Без этой таблицы следующая сессия их не
найдёт: `gh pr list` их не покажет.

| ветка | вершина | что там | что с ней делать |
|---|---|---|---|
| `docs/architect-profile-facts-fix` | `a219fe4` | правка `.claude/agents/architect.md`: снято ложное «детектора FACTS нет в main». Токен founder'а в теле есть | нужен круг критика по §9 (зона замка §11), затем PR |
| `docs/founder-decision-a015-2026-08-23` | `f920769` | запись решения founder'а (а) в `П-017` A2 + вердикт `R-116` **APPROVE** | стоит ЗА PR #66 (ссылается на его документ); после merge #66 — PR и merge |
| `docs/pr65-assessment-2026-08-23` | `6d9a0dc` | независимая оценка семи кругов `#65` (`docs/plans/pr65-continuation-assessment…`) | приземлить своим PR либо оставить как справку |

**Соответствие PR → ветка → предмет:**

| PR | ветка | предмет |
|---|---|---|
| **#75** | `harness/td-163-protected-artifacts` | `TD-163`, блокирует доставку |
| **#66** | `docs/two-barriers-step1-2026-08-23` | барьер A не строится + свидетель в оракуле; у reviewer'а по решению `A-016` |
| **#32** | `feat/M-69-window-guard` | M-69 `GW-I-14`; impl готов, ждёт factual flip architect'а |
| #76 | `docs/session-handover-2026-08-24` | эта передача |

**ВЕРДИКТЫ, КОТОРЫХ НЕТ В `main`** — ссылка по идентификатору к файлу НЕ приведёт. Читать
`git show <ветка>:<путь>`:

| ID | ветка | путь |
|---|---|---|
| `C-136` | `harness/td-163-protected-artifacts` | `research/critiques/C-136-adversary-td-163-protected-artifacts.md` |
| `A-016` | `docs/two-barriers-step1-2026-08-23` | `research/arbitration/A-016-pr66-routing.md` |
| `R-116` | `docs/founder-decision-a015-2026-08-23` | `research/reviews/R-116-recheck-r2-founder-decision-a015.md` |

`C-135`, `R-118`, `R-117`, `R-113` — уже в `main`, ищутся обычным грепом.

**Мелочь, которую называю, а не молчу** (`branch-hygiene.md` п.7): в рабочем чекауте лежит
untracked-каталог `.omc/` — оснастка oh-my-claudecode, появилась до начала сессии. Не мой
артефакт; не удалял и не коммитил. Reviewer передавал его founder'у в `R-117` §E.

## 3. Решения founder'а этой сессии (BINDING)

1. **«Ничего нового не вводить. Чинить то, что есть, и не лезть дальше.»** Действует на всё.
2. **`A-015` §6 — вариант (а)**, барьер формы §Tasks. Записан в `П-017` A2. **НО** под п.1
   постройка ОТЛОЖЕНА: барьер B — новый харнесс. Конфликт назван, не спрятан.
3. **`TD-156`** — легаси, не разводить. Исполнено.
4. **PR #66** — арбитр `A-016` решил ПРОТИВ моей позиции: не дробить, целиком к reviewer'у.
5. Профиль architect'а — правка разрешена (`FOUNDER-APPROVED` в `a219fe4`).

## 4. Барьер A — НЕ СТРОИТСЯ (это результат, а не пропуск)

Разбор шести кандидатов дал **5 ложных срабатываний из 6**: свидетель у них есть, записан
формой, невидимой детектору. Пятый нашла мутация и опровергла мой же вывод. Расширять шаблон
нельзя — третье сужение одного прокси. Класс «вакуумный ассерт» остаётся `COGNITIVE-ONLY`.
Документ — в `#66`.

## 5. Уроки, дороже предметов

1. **`gates.md` §0 триггер 2 — «три круга по одному предмету → арбитр» — СРАБОТАЛ на круге 3
   `#65` и не был вызван никем.** Семь кругов, ~1.3 млн токенов. Правило написано; я считал
   круги и не сделал вывода. Независимая оценка — pr65-continuation-assessment-2026-08-23.md (в docs/plans, ветка docs/pr65-assessment-2026-08-23 — в main ещё нет)
   (ветка `docs/pr65-assessment-2026-08-23`): настоящих дефектов ПОВЕДЕНИЯ — три, все в кругах
   1-2; круги 3-6 — шесть воспроизведений ОДНОГО класса.
2. **Условие снятия формулируется ПО КЛАССУ, а не по клетке.** Круги 2-3 требовали по одному
   сценарию, я буквально исполнял; класс закрылся, только когда `C-133` потребовал целиком.
3. **Утверждение о состоянии, взятое из ТЕКСТА, а не из репозитория.** Я взял из собственного
   профиля «детектора FACTS нет в main» — он влит двое суток назад. Получил `R-114` REJECT в
   записи, заведённой против этого же класса. Профиль исправлен (`a219fe4`).
4. **Анти-бланкет, промахнувшийся мимо способа обхода.** Мой `P22` удалял путь `git rm` —
   такое удаление попадает в диапазон и fallback не включает. Адверсарий выбросил путь
   merge'ем и предъявил дыру. Сценарий против своего послабления надо строить тем способом,
   каким его будут обходить.
5. **Rebase ветки, несущей вердикт гейта, рвёт `audited_head`.** Стоило восстановления линии.

## Cross-references
- `research/critiques/C-127`…`C-136` · `research/reviews/R-113`…`R-118` · `research/arbitration/A-015`, `A-016`
- pr65-continuation-assessment-2026-08-23.md (в docs/plans, ветка docs/pr65-assessment-2026-08-23 — в main ещё нет) (ветка `docs/pr65-assessment-2026-08-23`)
- two-barriers-step1-2026-08-23.md (в docs/plans, PR #66 — в main ещё нет)
