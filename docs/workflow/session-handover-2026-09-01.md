<!-- FACTS: audited_head=aeba1468f6ad61c3b687a9904d6f931b6220166a collected=2026-09-01 -->

# Передача сессии architect'а — 31.08 вечер → 01.09

**Решение founder'а, определяющее следующую сессию** (уточнено 01.09 утром):

> Милестоуны идут **ПОСЛЕДОВАТЕЛЬНО, по одному**. Жёстко зафиксированы **первые два**:
> **`M-45`, затем `M-76`**. Порядок остальных четырёх (`M-70`, `M-72`, `M-74`, `M-75`)
> **founder определит после `M-76`** — брать их самовольно нельзя.

Параллельное ведение пяти предметов названо причиной того, что за прошлую сессию в `main`
влито два предмета из шести. **Не брать следующий предмет, пока текущий не закрыт или не
заблокирован чужим шагом**; закончив `M-76` — спросить founder'а о порядке, а не выбрать самому.

**Главное в одной строке:** влиты правило `Р-4` и норма зоны compose; вынесено решение
арбитра `A-030` о форме исторического факта; заведён `M-76` (барьер подлинности выписок);
один класс ошибки сработал **семь раз за сутки**, и седьмой — внутри спеки о нём самом.

---

## 0. Состояние мира — замер, не память

Снято **2026-09-01T09:34Z** (первые три) и **09:41Z** (§2). Все блоки ниже — класс
**свидетельство** по `A-030` §1: они неисполнимы задним числом, поэтому несут дату и место,
а не претендуют на вечность. Пометка класса стоит ЗДЕСЬ, вне блоков: внутри блока —
только вывод команды, иначе он перестаёт производиться показанной командой (§4).

```
$ git ls-remote --heads origin main
aeba1468f6ad61c3b687a9904d6f931b6220166a	refs/heads/main
```

```
$ ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && git rev-parse --short HEAD; \
     docker ps --format "{{.Names}} {{.Status}}"; df -h / | tail -1'
7581f60
hft-gateway-serve Up 38 hours (healthy)
hft-recorder Up 38 hours (healthy)
/dev/sda1       150G   93G   51G  65% /
```

```
$ gh run list --branch main --limit 2
completed	success	Deploy to VPS	Deploy to VPS	main	workflow_run	33461604943	13s	2026-09-01T02:11:25Z
completed	success	Merge pull request #136 from a3ka/docs/session-handover-2026-09-01	CI	main	push	33460934306	10m31s	2026-09-01T02:00:52Z
```

Прод отстаёт от `main` на docs-коммиты — деплой на `docs/**` контейнер не пересобирает; это
норма (`SESSION-HANDOFF` §0ter), а не инцидент. **Ярус S обязателен в начале сессии** — эти
три блока к моменту чтения уже история.

## 1. Ветки предметов — вершины на момент записи

| предмет | ветка | вершина | состояние |
|---|---|---|---|
| **M-45** | `docs/M-45-rollout-signature` | `cd7e499` | `A-030` §3 исполнен; ждёт **круга 5 критика — ПОСЛЕДНЕГО** |
| **M-76** | `feat/M-76-raw-output-barrier` | `8e30921` | спека заведена, гипотезы проверены; ждёт круга 1 после `M-45` |
| **M-70** | `docs/M-70-rev2` | `758a362` | набор написан (`DB-I-4` + гейт); **ждёт вливания `M-75`** |
| **M-72** | `feat/M-72-subscription-terminality` | `4866212` | `C-200` REJECT: B-2/B-3/B-4 не закрыты + вторая половина `TD-177` |
| **M-74** | `docs/M-73-closeout-architect` | `22f29c46` | rev5: конструкция отпечатка объявлена; оракулы 1b/6b не написаны |
| **M-75** | `feat/M-75-heatmap-window-decoupling` | `8745d69` | `C-201` B-6/B-7 ЗАКРЫТЫ; замер пересnят трижды в изоляции: `959/1` стабильно |

**Вершины брать командой `git ls-remote --heads origin <ветка>`** — эти SHA протухают.
Проверено 01.09 09:31Z: все шесть веток на месте, вершины совпадают с таблицей.

**Сама передача ВЛИТА в `main`** (`aeba146`, PR #136), ветка `docs/session-handover-2026-09-01`
удалена. Читать файл по пути в `main`, не с ветки:
`docs/workflow/session-handover-2026-09-01.md`.

### Спас-рефы — работа, не влитая в ветки

```
refs/salvage/2026-08-31/m72-sentinel          e7d8ed5   сентинель M-72
refs/salvage/2026-08-31/m75-b5-inventory      1625c139  приём + прототип M-75
refs/salvage/2026-08-31/m75-b5-inventory-r2   5142a141  + serial в red_egress_cap
refs/salvage/2026-09-01/m75-b6-serial         e462c163  + serial во все три файла
```

Они несут ПРОТОТИП impl вместе с правками sacred-тестов, поэтому в feat-ветки не идут:
правки зовут функцию, которой до задачи dev не существует, и коммит уронил бы сборку.

## 2. Что ВЛИТО в `main` за сессию

| предмет | коммит | чем закрыт |
|---|---|---|
| **правило `Р-4`** — признак опознания недоступен миру без события | `af29452` (PR #134) | перепроверка `R-163` NOTE, находки закрыты до merge |
| **норма зоны compose** — корневой `docker-compose.yml` в зоне engine-dev | `2e63a37` (PR #135) | три круга: `R-164` REJECT → `R-165` REJECT → `R-166` APPROVE |
| **эта передача** | `aeba146` (PR #136) | вне зоны §9 (`docs/workflow/**`), приземлена своим PR |

### Что из этого ДОЕХАЛО ДО ПРОДА: НИЧЕГО

Все три предмета — текст: правило, норма, передача. Кода не тронуто, контейнеры не
пересобраны, прод стоит на `7581f60` (§0). **Ни один из шести милестоунов на проде не
работает.**

Проверено на сервере, 01.09T09:41Z (свидетельство — воспроизводится только на проде):

```
$ ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && grep -n "L2DELTA_CAPTURE_SYMBOLS\|EPOCH_ID" docker-compose.yml'
27:      # Смена состава = новая эпоха: требует нового EPOCH_ID (`docs/rfc/CT-RFC-06-l2delta.md` §3).
$ ssh -i … root@167.233.192.131 'cd /root/hft-platform && \
    sed -n "/container_name: hft-recorder/,/^  [a-z]/p" docker-compose.yml | grep -nE "^\s+[A-Z_]+:"'
4:      JOURNAL_DIR: /journal
13:      BINANCE_SYMBOLS: ${BINANCE_SYMBOLS:-BTCUSDT,ETHUSDT}
14:      BINANCE_FUTURES_SYMBOLS: ${BINANCE_FUTURES_SYMBOLS:-BTCUSDT,ETHUSDT}
15:      HL_COINS: ${HL_COINS:-BTC,ETH}
```

Единственное совпадение первой команды — **комментарий**, а не переменная: у сервиса
`hft-recorder` нет ни `L2DELTA_CAPTURE_SYMBOLS`, ни `EPOCH_ID`. Суб-секундная история эфира
как не писалась, так и не пишется, и теряется прямо сейчас.

> **Замечание о самой этой проверке.** Первая редакция абзаца утверждала «есть ТОЛЬКО
> `BINANCE_SYMBOLS`/`BINANCE_FUTURES_SYMBOLS`» — и была бы неверна: в секции ещё
> `JOURNAL_DIR` и `HL_COINS`. Поймано перепроверкой, до коммита. Тот же класс §4, восьмой
> раз; лечится ровно тем, что вывод СНИМАЕТСЯ, а не вспоминается.

## 3. `A-030` — решение арбитра, которое переживёт `M-45`

Созвано по второму REJECT'у одного класса. Утверждено и **не пересуживается**:

**§1 — форма исторического факта. Три класса блоков `$ команда` + вывод:**

| класс | определение | обязанность |
|---|---|---|
| **вечный** | вывод неизменен ПО ПОСТРОЕНИЮ (`git cat-file -t <SHA>`, `git show <rev>:path`) | предпочтителен всегда, когда достижим |
| **ревизионный** | воспроизводим в дереве ОБЪЯВЛЕННОЙ ревизии (маркер `FACTS`) | маркер обязателен; снятие на другом дереве — дефект |
| **свидетельство** | неисполнимо задним числом (`gh`, `ssh`, `docker`, сеть) | несёт дату и место; исполнимый коррелят обязателен там, где на утверждении стоит гейт |

**§2** — литерал вместо подстановки законен, противоречия со спекой нет.
**§3** — компаратор верен, но арбитр нашёл тот же класс УРОВНЕМ ВЫШЕ (см. §5 ниже).
**§4** — круг 5 критика по `M-45` **последний рабочий**; скоуп назван в решении.

## 4. КЛАСС СЕССИИ: показанный вывод не производится показанной командой

**Семь срабатываний за сутки**, из них три — внутри исправлений самого себя, седьмое — в
спеке `M-76`, посвящённой этому классу.

Во всех семи **факты под выпиской были ВЕРНЫ**. Ломается не вывод, а ПРОВЕРЯЕМОСТЬ.

**Приём, который ловит за минуты:**

```
diff <(sed -n '<строки показанного>p' <файл>) <(<та самая команда>)
```

**Механизм заводится `M-76`.** Обе гипотезы проверены прототипом на реальном материале:

- **структурные признаки — ОТВЕРГНУТЫ замером**: 4 из 6 поймано, но **17 ложных** на честных
  файлах. Причина фундаментальна: subject'ы коммитов на русском законно несут стрелки и
  скобки. Воскрешать «уточнением регулярок» запрещено отдельной строкой спеки;
- **исполнение и сравнение — ПОДТВЕРЖДЕНО**: девять расхождений в файлах, сверенных вручную.

## 5. КЛАСС, КОТОРЫЙ ОКАЗАЛСЯ ГЛУБЖЕ: проба мерит участника, а не границу потребителя

Один и тот же дефект ловился **трижды, каждый раз на уровень выше**:

| круг | что нашли | уровень |
|---|---|---|
| `C-199` B-3 | гейт сравнивал подстроки, пропускал неподписанный символ | значение |
| `C-202` B-2 | проба редекларировала логику; ослабление гейта не роняло пробу | компаратор |
| `A-030` §3 замер 4b | мутация склейки в `verify` пропускала состав, проба зелена 13/13 | **проводка шага** |

Закрыто переносом ВСЕЙ логики суждения в один CLI: и шаг, и проба зовут его. Вне пробы
остался только маппинг кода возврата — слой, общий для всех шагов и видимый одним экраном.

**Урок для следующей сессии:** спрашивая «пиннит ли проба гейт», проверяй мутацией НА КАЖДОМ
уровне — значение, компаратор, проводка. Правило `Р-1` («мера на границе потребителя»)
читается именно так.

## 6. Ещё один урок, оплаченный дважды: замер на меняющемся дереве недействителен

Заявленное по `M-75` «959 passed / 1 failed» **не воспроизвелось**: три прогона подряд дали
`959/1`, `953/7` и прерванный. Две причины, обе мои:

1. **флак** — гонка за процессно-глобальное окно в `red_heatmap` (`C-201` B-6), три прогона
   дали `FAILED`, `FAILED`, `ok`;
2. **конфаундинг** — я менял дерево (`stash`, `checkout`) ПОД работающими прогонами.

**Правило:** замер набора с процессно-глобальным состоянием снимается в ИЗОЛИРОВАННОМ дереве,
которое никто не трогает, и не менее трёх раз. Один прогон недетерминированного набора
доказательством не является.

## 7. Порядок работ, назначенный founder'ом

**Зафиксированы ДВА первых предмета и только они.** Порядок остальных четырёх founder
назначит после `M-76`. Ниже они перечислены по номерам — это НЕ очередь, а инвентарь
готовности; выбирать из него самовольно нельзя.

---

### ЗАФИКСИРОВАН · 1) `M-45` — раскатка L2Delta на ETHUSDT · теряются данные

Суб-секундная история эфира теряется каждую секунду и невосстановима. Механизм построен,
подпись `П-026` есть, не хватает двух строк конфига.

- Исполнено `A-030` §3 п.1–4; гейт красен только по нераскатанной задаче 7.
- **Дальше:** круг 5 критика (последний) → при чистом вердикте **диспетч engine-dev на
  задачу 7**: `L2DELTA_CAPTURE_SYMBOLS` и `EPOCH_ID` ЛИТЕРАЛАМИ, одним коммитом → деплой-гейт
  §8 с sanity по каждой площадке → вписать границу эпохи в `E-002` по первому heartbeat'у →
  переснять объём через сутки (**ожидание — РОСТ +5-7 %**, не падение).

### ЗАФИКСИРОВАН · 2) `M-76` — барьер подлинности выписок

Спека заведена, гипотезы проверены (§4 выше). Задача 4 (строка нормы в `testing.md`) требует
`FOUNDER-APPROVED`. Норма формы — `A-030` §1, её таблица классов обязательна к реализации.

---

## 7bis. ЧЕТЫРЕ ОСТАВШИХСЯ — порядок НЕ назначен, спросить founder'а после `M-76`

**ВОПРОС, который придётся решить вместе с порядком.** `M-70` §0.3quater объявляет
зависимость от `M-75`: расцепление меняет предмет задач 0 и 3 на два порядка. Развилка —
ровно две ветви, третьей нет:

| ветвь | что значит |
|---|---|
| **`M-75` раньше `M-70`** | 70-й делается целиком, задачи 0 и 3 в своём настоящем виде |
| **`M-70` раньше `M-75`** | 70-й идёт БЕЗ задач 0 и 3, они переоткрываются после 75-го |

Решение — founder'а: это выбор цены, а не инженерная правота. Не выбирать за него.

### `M-70` — включение полос глубины

Написано (на ветке `docs/M-70-rev2`, в `main` НЕ влито): `DB-I-4` (метка на точку, красен по
`TD-159`) + `scripts/verify_M-70.sh` (FAIL (6)).

### `M-72` — терминальность подписки

`C-200` REJECT (`research/critiques/C-200-M-72-subscription-terminality-round3.md` на ветке
предмета). Заголовки находок — дословно из вердикта, а не по памяти:

| # | заголовок | что требует круг 4 |
|---|---|---|
| **B-2** | E-3 всё ещё допускает терминальную ошибку ВМЕСТЕ с закрытием всего сокета | оракул живого соседа |
| **B-3** | `verify` зеленее предмета на проводе | закрыть все дефекты гейта, включая исход мутации с учётом базовой линии |
| **B-4** | граница терминальной ошибки в T2 не литерал и не разрешена | завести границу явно |
| N-1 | вторая половина `TD-177` противоречит единственному фиксу задачи 3 | — |
| N-2 | паритет с CI независимо красен | — |

Вторая половина `TD-177` изолирована: после переключения приходит один кадр старой цены,
затем пустые кадры с нулевым курсором; сентинель не доходит.
**Круг 4 — последний по `A-029` §2**, дальше предмет идёт founder'у.

> Прежняя редакция называла B-3 «гейт не считает свой оракул», а B-4 — «сигнатура +
> Allowed paths». Оба описания были МОИМИ, из более раннего круга, и не совпадали с
> вердиктом. Поймано перепроверкой при переносе строк в промпт: заголовки взяты открытием
> файла (правило цитаты), а не пересказом.

### `M-74` — доказательство читаемости офсайт-копии

rev5: конструкция сменена по решению founder'а на **отпечаток прочитанного**. Объявлены
формула, поле `digest` в T2-контракте и в сигнатуре читателя. **Не написаны** оракулы задач
1b (отпечаток) и 6b (страж одноразовости). Счёт кругов новый.

### `M-75` — расцепление окна heatmap

`C-201` ЗАКРЫТ обеими находками (`8745d69`):

- **B-6** — гонка была ФЛАКОМ, и моё «959/1» невоспроизводимым: три прогона `red_heatmap`
  дали `FAILED`, `FAILED`, `ok`. `serial()` стоял лишь в одном файле из четырёх. Правки —
  спас-реф `refs/salvage/2026-09-01/m75-b6-serial` (`e462c163`), в ветку не идут: зовут
  сеттер, которого до задачи 2 нет. Применяются задачей 2b вместе с приёмом B-5.
- **B-7** — шаг `2b` зеленел от одного комментария; заменён исполняемым (`chk_named_test`
  на четыре набора + страж «вызов, а не упоминание» + проверка гигиены).
- **Чистый замер, изолированный worktree, три прогона: `959/1` каждый.** Единственный
  красный — оракул задачи 4, красен законно.

**Отдельно записан собственный промах:** коммит `63c99aa` УДАЛИЛ старый шаг `2b`, не положив
замену (`numstat` показал `0 21`), — гейт остался без шага вовсе. Поймано правилом
`branch-hygiene` п.9 (сверка `numstat` с заявленным) сразу после push'а, восстановлено
`8745d69`. Урок: при переносе правки между деревьями сверять диф, а не помнить.

## 8. Долги, названные числом

- **Бюджет впрыскиваемого ядра на исходе:** `scope-guard.md` — запас 40 B, `gates.md` — 36 B.
  Следующая правка любой из двух норм упрётся в барьер. Лечится вычисткой, не поднятием.
- **`Р-4` механизирован наполовину**: страж уникальности живёт внутри оракула и появляется
  лишь когда правило применяют.
- **FA не существует** у `gateway`, `gateway-serve`, `derive`, `recorder`.
- **495 блоков `$ команда`** в 53 файлах; `M-76` останавливает рост, не чинит историю.

## 9. Cross-references

- Решение арбитра: `research/arbitration/A-030-m45-fact-form-literal-comparator.md` — **на ветке
  `docs/M-45-rollout-signature`, в `main` НЕ влито** (`git show origin/docs/M-45-rollout-signature:<путь>`)
- Вердикты сессии: `C-195` `C-197` `C-199` `C-202` (`M-45`) · `C-198` `C-201` (`M-75`) ·
  `C-200` (`M-72`) · `R-163`…`R-166` (нормы)
- Правило `Р-4` — `docs/workflow/oracle-blindness-class-2026-08-28.md` §5, в `main`
- Спека барьера — `milestones/M-76-raw-output-barrier.md` на своей ветке
- Предыдущая передача — **session-handover-2026-08-31c** в `docs/workflow/`, ветка
  `docs/session-handover-2026-08-31c` (в `main` не влита; литеральный путь не записан —
  гейт правдивости справедливо числил бы его мёртвой ссылкой)

## 10. Стартовый промпт следующей сессии — в файле, а не в переписке

Промпт живёт ЗДЕСЬ, потому что переписка не переживает смену сессии, а файл в `main` —
переживает. Копировать целиком.

```text
Ты — architect hft-platform. Сессия начинается со стартап-протокола, ПОТОМ работа.

ШАГ 1. СТАРТАП-ПРОТОКОЛ — предъявляется РЕЗУЛЬТАТОМ. Читать С ДИСКА, целиком:
  CLAUDE.md · .claude/rules/*.md ВСЕ ШЕСТЬ · .claude/agents/architect.md
  docs/workflow/reading-map.md
  ЯРУС A: docs/DESIGN.md · docs/04-workflow.md · docs/05-contract-layer.md ·
          docs/SESSION-HANDOFF.md §0/§0ter · grep -n '^## П-' docs/PENDING-SIGNATURE.md
  ПЕРЕДАЧА (в main, ветка удалена): docs/workflow/session-handover-2026-09-01.md — ЦЕЛИКОМ
  РЕШЕНИЕ АРБИТРА (на ветке docs/M-45-rollout-signature, в main НЕ влито):
          research/arbitration/A-030-m45-fact-form-literal-comparator.md — §1 задаёт три
          класса блоков «команда + вывод» и является нормой для M-76
  ПРАВИЛА ОРАКУЛОВ Р-1…Р-4 (в main):
          docs/workflow/oracle-blindness-class-2026-08-28.md §5
  ЯРУС B — по карте reading-map §2. ДОЛГ в первый Handoff: у gateway, gateway-serve,
  derive, recorder FA НЕ СУЩЕСТВУЕТ.

ШАГ 2. ЯРУС S — четыре команды, сырой вывод:
  gh run list --branch main --limit 5
  gh run list --workflow=deploy.yml --limit 3
  ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && git rev-parse --short HEAD; df -h /; \
     cat /var/lib/hft/journal-offsite.last-success; \
     cat /var/lib/docker/volumes/hft-platform_journal-data/_data/recorder.heartbeat'
  bash scripts/check_branch_health.sh

ПОРЯДОК РАБОТ — решение founder'а, ПОСЛЕДОВАТЕЛЬНО, ПО ОДНОМУ:
  ЗАФИКСИРОВАНЫ ДВА: M-45, затем M-76.
  Порядок остальных (M-70, M-72, M-74, M-75) founder назначит ПОСЛЕ M-76 — не выбирать
  самому, спросить. Вместе с порядком решается развилка M-70 ↔ M-75 (§7bis передачи).
  Не брать следующий предмет, пока текущий не закрыт или не заблокирован чужим шагом.

  ЧТО В ЧЕТЫРЁХ ОСТАВШИХСЯ — по строке, чтобы не начинать вслепую (полностью — §7bis):
    M-70 полосы глубины  — набор написан (DB-I-4 красен по TD-159, verify FAIL (6)),
                           лежит на ветке docs/M-70-rev2; ЗАВИСИТ от M-75 — см. развилку
    M-72 терминальность подписки — три блокирующие находки круга 3 НЕ закрыты, дословно по
                           C-200: B-2 «E-3 всё ещё допускает терминальную ошибку ВМЕСТЕ с
                           закрытием всего сокета» (нужен оракул живого соседа); B-3 «verify
                           зеленее предмета на проводе»; B-4 «граница терминальной ошибки в
                           T2 не литерал и не разрешена». Плюс два замечания: N-1 — вторая
                           половина TD-177 противоречит единственному фиксу задачи 3;
                           N-2 — паритет с CI независимо красен. Круг 4 ПОСЛЕДНИЙ (A-029 §2)
    M-74 читаемость офсайт-копии — конструкция «отпечаток прочитанного» объявлена (формула,
                           поле digest в T2 и в сигнатуре читателя); НЕ написаны оракулы
                           задач 1b (отпечаток) и 6b (страж одноразовости); счёт кругов новый
    M-75 расцепление окна heatmap — C-201 закрыт обеими находками; осталось задачей 2b
                           приземлить спас-реф refs/salvage/2026-09-01/m75-b6-serial вместе
                           с приёмом B-5; замер 959/1 воспроизведён трижды в изоляции

ВЕРШИНЫ ВЕТОК брать git ls-remote — записанные SHA протухают. Таблица — §1 передачи.
СПАС-РЕФЫ (работа вне веток): §1 передачи, забирать git fetch origin refs/salvage/...

ДВА КЛАССА ОШИБОК, ОПЛАЧЕННЫЕ ПРОШЛОЙ СЕССИЕЙ — §4 и §5 передачи:
  1. «Показанный вывод не производится показанной командой» — СЕМЬ раз за сутки, три
     внутри собственных исправлений. Перед КАЖДЫМ коммитом документа-фактуры:
       diff <(sed -n '<строки показанного>p' <файл>) <(<та самая команда>)
     Внутри блока — только вывод команды; прочтения автора выносятся абзацем наружу.
  2. «Проба мерит участника, а не границу потребителя» — ловился ТРИЖДЫ, каждый раз
     уровнем выше: значение → компаратор → проводка шага. Мутацию ставить на КАЖДОМ.
  ЗАМЕР набора с процессно-глобальным состоянием — в ИЗОЛИРОВАННОМ дереве, НЕ МЕНЕЕ
  ТРЁХ раз. Один прогон недетерминированного набора доказательством не является.

ЖЁСТКИЕ РАМКИ:
  · Роли цепочки (critic/reviewer/dev/tester/адверсарий) запускает ТОЛЬКО founder — ему
    Handoff-блок. Architect'у разрешены ЛИШЬ клоны subagent_type: architect.
  · ПРАВИЛО ЦИТАТЫ: «документ X говорит Y» требует ОТКРЫТИЯ X.
  · Утверждение о состоянии — ТОЛЬКО с командой в ТОМ ЖЕ сообщении. Время — date -u.
  · PUSH СТОИТ ЗА ПРОВЕРКОЙ КОДА ВОЗВРАТА, а не рядом.
  · Перед сносом незакоммиченного — спас-реф в origin, НЕ патч в /tmp.
  · Проба мутации — в ОТДЕЛЬНОМ worktree, не на рабочей ветке.
  · Коммит называет пути ЯВНО; commit -a и add -A запрещены хуком.
  · Номера — ТОЛЬКО scripts/next_artifact_id.sh. Полные SHA — git rev-parse.
  · Ответ заканчивается Handoff-блоком из пяти секций.

ПЕРВЫЙ ХОД — M-45, круг 5 критика, ПОСЛЕДНИЙ рабочий по A-030 §4. Скоуп круга назван в
решении: (i) исполнение §3 с обоими мутационными замерами; (ii) предложение об остаточных
каналах; (iii) сверка ТОЛЬКО новых блоков. Чисто или NOTE ⇒ Handoff founder'у на диспетч
engine-dev: задача 7 — две строки конфига ЛИТЕРАЛАМИ, одним коммитом.

САМОЕ ЦЕННОЕ ИЗ НЕЗАКРЫТОГО: суб-секундная история эфира теряется каждую секунду и
невосстановима. Механизм построен, подпись П-026 есть, не хватает двух строк конфига.
На проде их НЕТ — проверено 01.09 (§2 передачи).
```
