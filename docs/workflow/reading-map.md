# Карта чтения — что роль обязана прочесть, и почему не всё

**Введена решением founder'а 2026-08-16.** Живёт ВНЕ впрыскиваемого ядра (`CLAUDE.md` +
`.claude/rules/**`) намеренно — по тому же основанию, что `harness-track.md`: её читает тот,
кто ведёт сессию, а не все девять ролей при каждом сжатии контекста. У ядра запас 3 076 B
на семь файлов (`scripts/check_context_budgets.sh`), и новый раздел туда физически не влезает.

---

## 1. Замер, из-за которого карта появилась

Профиль architect'а требовал «прочитать `DESIGN` + релевантный `fa/<модуль>` +
`PROJECT-STATE` + `TECH-DEBT` перед любым milestone'ом». Замер 2026-08-16 (`stat -c%s`):

| ярус | объём | ≈ токенов |
|---|---:|---:|
| впрыск: `CLAUDE.md` + `.claude/rules/*` | 111 824 B | ~25 k |
| нормативное ядро (`DESIGN`, `04`, `05`, `06`, `03`, профиль) | 180 583 B | ~40 k |
| `docs/fa/*` (20) + `docs/rfc/*` (9) + `research/arbitration/*` (8) | 975 497 B | ~217 k |
| `TECH-DEBT.md` + `PROJECT-STATE.md` | 1 015 489 B | ~226 k |
| **весь корпус** | **2 600 112 B** | **~580 k** |

Требование было невыполнимо: только `TECH-DEBT` + `PROJECT-STATE` втрое превышают то, что
роль может держать вместе с работой. Невыполнимое требование не соблюдается частично — оно
не соблюдается вовсе и заменяется догадкой.

**Цена, замеренная на одной сессии 16.08** (все пять — до того, как карта появилась):

1. `A-002` цитировался пять раз по пересказу спеки; первоисточник не открывался.
2. Развилка объявлена «границей C» со ссылкой на `gates.md`; `docs/03` §3, где эта граница
   определена, прочитан не был.
3. Вопрос «нужен ли contract-RFC» решался без `05-contract-layer.md`; ответ («не нужен»)
   лежал в `DESIGN` §17 и в `05` §2 (T-designate).
4. `DESIGN` §17 — «L2Delta обязателен, предусловие продукта» — найден на втором часу работы
   над милестоуном, целиком посвящённым рыночным данным.
5. `JR-I-10` пересказан с чужих слов и оказался коллизией номеров (`fa/journal.md:131` против
   `CT-RFC-06:207`) — ярлык принят за инвариант.

Общий признак у всех пяти: **утверждение о документе делалось без открытия документа.**

## 2. Три яруса

### Ярус A — ВСЕГДА и ЦЕЛИКОМ (роль, ведущая сессию)

| документ | байт | зачем |
|---|---:|---|
| `CLAUDE.md` + `.claude/rules/*.md` | 111 824 | впрыскивается автоматически |
| `.claude/agents/<своя роль>.md` | `wc -c .claude/agents/<роль>.md` — снимать командой (было записано «~9 500», к 21.08 протухло почти вдвое; барьера на размер профилей НЕТ, `check_context_budgets.sh` их не считает) | зона, запреты, handoff |
| `docs/DESIGN.md` | 114 549 | мастер: инварианты, границы, фазы, §22 карта покрытия |
| `docs/04-workflow.md` · `docs/05-contract-layer.md` | 21 690 | маршрут и T1-governance |
| `docs/SESSION-HANDOFF.md` §0/§0bis (НЕ весь файл) | 28 233 | живой индекс пайплайна |
| `docs/PENDING-SIGNATURE.md` — **только индекс** `grep -n '^## П-'` | ~1 000 | чего нельзя касаться |

**≈ 281 KB ≈ 62 k токенов** (замер: `111 982 + 11 275 + 114 549 + 21 690 + 28 233 + ~1 000`).
Цена ведения сессии названа числом, а не «по мере надобности».

**Почему индекс `П-*` — ярус A, а не C** (`R-087` F-4): грепом нельзя найти то, о
существовании чего не знаешь. Смысл этой очереди обратен поиску по известному предмету —
узнать, чего ты НЕ вправе касаться. Живых записей семь из четырнадцати; среди них `П-005`
(«динамический состав с удалением — архитектурное решение ДО раскатки») — ровно та развилка,
на которую наступают, не зная о ней. Индекс стоит одной команды и ~1 KB; тела записей
остаются в ярусе C.

### Ярус B — ПО ПРЕДМЕТУ и ЦЕЛИКОМ

Открывается по карте ниже — до первого утверждения о предмете, не после.

| зона предмета | обязательное чтение |
|---|---|
| журнал, ретеншен, сегменты, формат записи | `fa/journal.md` · `06-data-layer-and-storage.md` · `CT-RFC-02` · `CT-RFC-07` · `A-001` |
| рыночные данные, состав, venue-адаптеры | `fa/venues.md` · `CT-RFC-01` · `CT-RFC-04`/`CT-RFC-06` · `docs/data-epochs.md` |
| книга, глубина, полосы | `fa/book.md` · `fa/viz-backend.md` §4 · `A-002` · `research/data-quality/depth-verdict.md` |
| выдача, gateway, кокпит | `fa/viz-backend.md` · `07-cockpit-backend-roadmap.md` · `CT-RFC-09` |
| контракты T1 | `05-contract-layer.md` · `fa/contracts.md` · весь `docs/rfc/CT-RFC-*` по теме |
| сигналы, research, анти-оверфит | `fa/signals.md` · `fa/research-cli.md` · `02-quant-desk.md` · `03-integration-contract.md` |
| ops, алерты, деплой | `fa/ops.md` · `DESIGN` §23 |
| risk / killswitch / oms | `fa/risk.md` · `fa/killswitch.md` · `fa/oms.md` · `DESIGN` §4, §21 |
| симулятор, честность бэктеста | `fa/sim.md` · `DESIGN` §5 · семейство `SM-I` |
| alpha / portfolio / strategy | `fa/alpha.md` · `fa/portfolio.md` · `fa/strategy.md` · `fa/strategy-brain.md` · семейство `ST-I` |
| runner, композиция режимов | `fa/runner.md` · `01-engine-architecture.md` §9 |
| AI-слой, копилот | `fa/ai-copilot.md` · `DESIGN` §19 |
| `derive` / `recorder` / `gateway-serve` | **FA не существует** — сам этот факт есть долг, назвать его в Handoff; опора: `fa/viz-backend.md` + `DESIGN` §22 (`GW-I` — 13 оракулов при нуле заявленных; замер `grep -rhoE '\bGW-I-[0-9]+\b' crates/ | sort -uV | wc -l`) |
| фазы, приоритеты, роадмап | `09-roadmap-v2.md` · `DESIGN` §10 · `milestones/BACKLOG.md` |
| харнесс (`scripts/**`, CI) | `docs/workflow/harness-track.md` |
| процессный слой (`.claude/**`, `CLAUDE.md`, `04-workflow.md`) | `gates.md` §9/§11 · `04-workflow.md` · `docs/plans/process-layer-audit-2026-08-13.md` |

Плюс всегда: milestone-файл предмета и **все вердикты его прошлых кругов**
(`research/{critiques,reviews,arbitration}/`).

**Правило отказа — зоны нет в таблице (`R-087` F-5б).** Таблица протекает при каждом новом
крейте, и барьера на её актуальность нет (§6). Поэтому непопадание НЕ означает «читать
нечего»: ярус B = `docs/fa/<крейт>.md` + `DESIGN` §22 (найти семейство инвариантов зоны) +
все RFC, называющие крейт (`grep -l <крейт> docs/rfc/*`). **FA у крейта нет ⇒ это долг сам по
себе; назвать его в Handoff, а не молча продолжить.** Строка добавляется в таблицу тем же
коммитом, что и новый FA/RFC.

**ПРАВИЛО ПРЕДШЕСТВЕННИКА — искать РЕШЁННОЕ прежде, чем проектировать (замер 2026-08-16).**
За одни сутки architect ПЯТЬ раз предложил построить то, что в корпусе уже лежало готовым:

| что предлагалось | где уже было |
|---|---|
| «полосы читают книгу» — новый milestone | `M-67` rev2 §6.1 Этап A — дословно, включая тот же греп и тот же вывод |
| барьер `review-fa` — «построить» | построен на `feat/M-66-fixture`: проба 40/40, четыре круга критика |
| сторож отсутствия деплоя — «спроектировать» | `docs/plans/process-decisions-2026-08-14.md` Р-4: готовый дифф норм + спека механизма, `R-074` APPROVED, `C-084` NOTE |
| флак ресурсного оракула — «нашёл» | `TD-129` и `TD-098`, обе OPEN, обе живы |
| инверсия формата `L2Delta` — «предложить» | `DESIGN` §17, статус «предусловие продукта» с 2026-07-29 |

Общий корень назван прямо: **код проверялся замером, а корпус — памятью.** Отсюда
обязательный шаг ДО любого предложения о новой конструкции — одна команда:

```
grep -rl '<ключевое слово предмета>' docs/plans/ milestones/ docs/rfc/ TECH-DEBT.md
```

Ноль попаданий — проектируй. Есть попадание — **сперва прочти найденное целиком**: во всех
пяти случаях предмет был уже разобран, и работа сводилась к достройке, а не к авторству.
`docs/plans/**` (21 документ) в ярусах до этого не значился ВООБЩЕ — отсюда три промаха из пяти.

### Ярус C — ТОЛЬКО ИНДЕКСНО, никогда целиком

`TECH-DEBT.md` · `PROJECT-STATE.md` · `milestones/BACKLOG.md` · 80 критик · 80 ревью ·
**тела** записей `docs/PENDING-SIGNATURE.md` (их ИНДЕКС — ярус A, см. выше).

Это журналы, а не нормы: они растут append-only и читаются запросом. Правило: обращение —
грепом по предмету (`TD-NNN`, имя крейта, номер milestone'а), и в Handoff **называется, что
именно искали**. «Прочитал `TECH-DEBT`» — заведомо ложное утверждение, его писать нельзя.

### Ярус S — СОСТОЯНИЕ МИРА (не документ, но обязателен)

Самая дорогая находка 16.08 пришла **не из чтения вообще**: `main` был красен от собственного
merge'а architect'а, а прод отставал от `main` на 92 коммита больше тридцати часов — при всех
зелёных liveness-сигналах. Ни один ярус документов этого не содержит и содержать не может:
документы описывают, как ДОЛЖНО быть, а не как есть сейчас.

ЧЕТЫРЕ команды — в начале сессии и перед каждым «готово»:

```
gh run list --branch main --limit 5             # красен ли main ПРЯМО СЕЙЧАС
gh run list --workflow=deploy.yml --limit 3     # доехало ли до прода
ssh … 'cd /root/hft-platform && git rev-parse --short HEAD'   # какой SHA реально крутится
bash scripts/check_branch_health.sh             # висяки, дубли предмета, состояние ВСЕХ веток
```

**Четвёртая добавлена 2026-08-21 по замеру, а не для полноты.** До неё состояние веток
выяснялось руками и называлось по памяти — за одну сессию architect дважды ошибся в нём
вслух (обе строки названы в `docs/workflow/session-handover-2026-08-21.md` §1: «задача 5
M-66 ждёт подписи» — при подписи, лежавшей в `main` пять дней; «acceptance PASS» — снятый
на дереве, которое к моменту отчёта уехало на два коммита). Инструмент лежал в `main` (`scripts/check_branch_health.sh`, PR #48) и не был известен
роли: ни один файл стартового протокола его не упоминал. Это третий вариант семейства —
**построено-не-известно**, после «построено-не-проведено» (`TD-155`: барьер есть, джоба нет)
и «решено-не-построено» (`A-010` §H: маркер ставили, читать некому).

Он НАБЛЮДАТЕЛЬ, не барьер: печатает ВИСЯК (зелёный PR без merge'а), ДУБЛЬ (один предмет на
нескольких ветках), НЕИЗВЕСТНО (состояние чеков не получено) и краснеет только если источник
не ответил вовсе.

**Правило, вытекающее из этого случая:** инструмент попадает в Ярус S ТЕМ ЖЕ PR, что и в
`main`. Иначе он существует и не используется — а роль, не знающая о нём, платит ручным
выяснением того, что стоит одной команды.

**Документ, называющий себя фактурой, несёт ревизию сбора** (`A-010` §H, задача H-4):

```
<!-- FACTS: audited_head=<полный SHA> collected=<YYYY-MM-DD> -->
```

Маркер засчитывается в ГОЛОВЕ файла (первые пять строк — не «первой строкой»: у документа
бывает `#`-заголовок и пустая строка перед ним). Без маркера документ в `docs/plans/**` —
рабочая записка: он исключён из проверки ссылок, и `verify_design_claims.sh` печатает по нему
`NOTE`, **если** в нём ≥20 утверждений вида `путь:строка` (порог: молчание малой записки
шумом не считается). С маркером документ входит в проверку ссылок наравне с остальными.

**Предел назван честно** (`A-010` H-5): `файл:строка` не проверяется ничем и проверяться не
будет. Маркер конвертирует «молча ложно» в «явно датировано» — дрейф он ОБЪЯВЛЯЕТ, а не лечит.

Третья даёт ТОЧНЫЙ ответ, а не приблизительный: на VPS живой git-чекаут, деплой делает
`git reset --hard origin/main`. По дате образа можно ошибиться на время сборки; `rev-parse`
даёт истину без интерпретации.

## 3. Правило цитаты (то, которое нарушалось пять раз)

**Любое утверждение вида «документ X говорит Y» требует открытия X.** Не считается
основанием: пересказ в спеке, шапка чужого оракула, выписка субагента, память о прошлой
сессии. Различать ДЕЙСТВУЮЩИЙ и ИСТОРИЧЕСКИЙ текст обязательно — в этом корпусе снятые
редакции сохраняются намеренно (`viz-backend.md` §4, `DESIGN` §11/§12, `ops.md` §4.3).

Симптом нарушения: в ответе есть `файл:строка`, но в транскрипте сессии нет чтения этого
файла.

## 4. Субагенты получают СРЕЗ, а не корпус

Мандат субагенту обязан **перечислять документы поимённо**. Формулировки «прочитай устав»,
«ознакомься с корпусом» запрещены: клон с полным списком сожжёт контекст на чтение и не
дойдёт до работы, а без списка — начнёт догадываться.

| роль клона | ярус A | ярус B | ярус C |
|---|---|---|---|
| разведка/замер | `CLAUDE.md`+rules (впрыск), свой профиль | — | грепом, если мандат назвал предмет |
| проверка утверждений о коде | то же | документы предмета **поимённо** | — |
| перепроверка `gates.md` §9 / арбитраж §0 | то же + `DESIGN` | предмет спора + все вердикты кругов | — |
| чтец корпуса (как этот круг) | то же | назначенный срез | — |

Оркестратор, а не клон, решает состав среза: клон не знает, чего ему не хватает.

## 5. Что подлежит вычистке — предложение, не исполнение

Карта только распределяет чтение; она не уменьшает корпус. Уменьшают эти три правки:

| предмет | сейчас | что именно | владелец |
|---|---:|---|---|
| `TECH-DEBT.md` | 567 KB / 144 карточки | закрытые карточки → `docs/archive/`; в живом файле — только OPEN | **reviewer** |
| `PROJECT-STATE.md` | 448 KB | закрытые милестоуны → `docs/archive/`; живой файл — состояние, не летопись | **reviewer** |
| `SESSION-HANDOFF.md` | 67 KB | §1–§9 датированы 07-31/08-03 и закрыты; живой индекс — §0/§0bis (28 233 B, 42 % файла) | architect |

Первые две — зона reviewer'а (`scope-guard.md`), architect в них не пишет и передаёт
предложение Handoff'ом.

## 6. Пределы, названные честно

- **`COGNITIVE-ONLY`.** Механической проверки того, что роль ДЕЙСТВИТЕЛЬНО прочла ярус A,
  не существует, и она здесь не изображается. Барьер `check_context_budgets.sh` меряет
  РАЗМЕР ядра, а не факт чтения.
- **Кандидат на механизацию** (не сделан, не выдаётся за сделанный): требовать в Handoff §A
  строку `READ: <ярус B документы>` и сверять её грепом с `файл:строка`-ссылками ответа —
  ссылка на документ, не названный в `READ`, роняет проверку. Это аудит-след, а не
  доказательство: строка подделываема, как и `FOUNDER-APPROVED`.
- **Карта B устаревает.** Она производна от состава `docs/fa/*` и `docs/rfc/*`; при
  добавлении FA/RFC строка добавляется сюда тем же коммитом. Барьера на это тоже нет.

## Cross-references

- `CLAUDE.md` §STARTUP-ПРОТОКОЛ (указатель сюда) · `.claude/agents/architect.md` §Startup reading
- `scripts/check_context_budgets.sh` (бюджет впрыскиваемого ядра, M-60b G1)
- `docs/workflow/harness-track.md` (прецедент документа вне ядра)
- `.claude/rules/gates.md` §9 (уставная правка), §11 (замок процессного слоя)
