<!-- FACTS: audited_head=fe43333e36f0d8ffab41841af4c96a8d9195220a collected=2026-09-01 -->

# Передача сессии architect'а — 01.09 день → следующая сессия

**Предыдущая передача** (утро 01.09) — `docs/workflow/session-handover-2026-09-01.md`,
в `main`, полная редакция со всеми блоками. Она описывает мир ДО этой сессии; ниже — что
изменилось. Читать обе: старая несёт классы ошибок и инвентарь, новая — состояние.

---

## 0. Состояние мира — замер, не память

Снято 2026-09-01T22:0xZ. Блоки ниже — **свидетельство** (`A-030` §1): неисполнимы задним
числом, потому несут дату. Ярус S обязателен в начале сессии заново — к моменту чтения это
уже история.

```
$ git rev-parse --short origin/main
fe43333
```

```
$ ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && git rev-parse --short HEAD; docker ps --format "{{.Names}} {{.Status}}"'
a72ca04
hft-gateway-serve Up 3 hours (healthy)
hft-recorder Up 3 hours (healthy)
```

Прод отстаёт от `main` на docs/CI-коммиты — деплой на эти пути контейнер не пересобирает,
это норма (`SESSION-HANDOFF` §0ter), не инцидент.

## 1. ГЛАВНОЕ: эфир пишется. Потеря остановлена

Суб-секундная история `ETHUSDT` записывается с **2026-09-01T18:48Z** на обеих площадках
Binance. До этого она терялась ежесекундно и невосстановимо начиная с подписи 31.08.

Эффект замерен РАЗЛИЧАЮЩИМ способом (не «книга строится», а доля событий):

| эпоха | сегмент | `BTCUSDT` | `ETHUSDT` | доля ETH/BTC |
|---|---|---:|---:|---:|
| до раскатки | `502` | 74 424 | 26 753 | **0.36** |
| после | `504` | 182 291 | 154 521 | **0.85** |

Граница эпохи `E-002`: **≈ 432 810 660 – 432 810 675**, подтверждена машинным следом —
сегмент `503` закрыт досрочно на 406 МБ вместо гигабайта, `decide_open_segment` отказался
мешать эпохи в одном файле.

## 2. Что ВЛИТО в `main` за сессию

| предмет | PR | чем закрыт |
|---|---|---|
| `M-45` раскатка + гейт | #140 `a72ca04` | `R-168` APPROVED, деплой-гейт §8 пройден |
| close-out reviewer'а | #141 `ceed5ac` | `TD-194`/`TD-195` заведены |
| close-out architect'а | #142 `e1641ca` | `E-002` закрыта, милестоун ЗАКРЫТ |
| **барьер состава записи** | #143 `9e5a366` | `C-206` CLEARED, `TD-194` и `R-168` Н-2 закрыты |
| восстановленная передача | #139 | полная редакция утренней передачи в `main` |

## 3. ЧТО ТЕПЕРЬ ДЕРЖИТ ГРАНИЦУ C — и чего оно НЕ держит

**Барьер `scripts/check_rollout_composition.sh`** — постоянный джоб CI `rollout-composition`,
**в агрегате** `All checks passed`. Сверяет ТРИ места: `docker-compose.yml` ↔ блок
`ACTIVE-COMPOSITION` в `docs/data-epochs.md` ↔ существование подписи. Плюс **связку во
времени**: состав изменился ⇒ эпоха И подпись обязаны измениться.

Зашитых символов НЕТ намеренно: состав меняется с каждой подписью владельца, и барьер с
константой пришлось бы править кодом при каждом решении границы C — то есть совершать
решение владельца правкой кода.

**НЕ ДЕРЖИТ:** что текст названной подписи действительно разрешает именно такой состав.
Механизм гарантирует АУДИТ-СЛЕД, не истинность. Семантику решения машина не читает.

**Проба** `scripts/tests/red_rollout_composition.sh` — 27 сценариев, одиннадцать мутаций.

## 4. КЛАСС СЕССИИ: страж проверяет признак ШИРЕ, чем требование

Сработал **девять раз** за сутки в оракулах `M-45`, все — мои. Разбор и таблица —
`milestones/M-45-persist-l2delta.md` §3septies.

Общий корень одной фразой: **требование говорит «в записи X», страж смотрит «в файле»;
требование «раскатка исполнена» — страж «дефолт в коде»; требование «фикстура есть» — страж
«слово встречается».**

**Решение арбитра `A-031`, обязательное:** класс закрывает не привязка НАЗВАННЫХ стражей,
а **ВЫПИСАННАЯ ГРУППА**. Перечень всех стражей присутствия теперь в шапке
`scripts/verify_M-45.sh` с предметом наблюдения каждого и правилом ведения. Это исполнение
`Р-3` на уровне гейта: «опасна ровно та группа, которая НЕ ВЫПИСАНА».

**Три рецидива внутри собственных фиксов — записаны без смягчения:**
1. Закрывая носитель №6 якорем `^const`, в ТОМ ЖЕ коммите написал пины имён тестов БЕЗ якоря (носитель №9).
2. Проверил ЧЛЕНСТВО джоба в `needs` и объявил «в агрегате» — а агрегат судит то, что в ЕГО ТЕЛЕ. Красный барьер merge бы НЕ удержал (`C-206` Б-1).
3. Скопировал у соседних джобов механику `PR_BASE_SHA`, но не условие её работы (`fetch-depth: 0`) — барьер краснел бы на КАЖДОМ PR (`C-206` N-5).

Во всех трёх ловил не я. Ловили мутация, адверсарий и собственная проба.

## 5. ЛОЖНЫЙ ЯКОРЬ — поймал меня ДВАЖДЫ, предупреждение следующей сессии

Мутация по номеру строки легла в КОММЕНТАРИЙ, цитирующий код. Файл «изменился», `diff`
сказал «мутация состоялась», поведение не изменилось — и я дважды прочитал зелёное как
«механизм не пиннит».

**Приём:** после КАЖДОЙ мутации печатать СТРОКУ ПОСЛЕ ПРАВКИ и убедиться, что это КОД.

Второе того же рода: `setup_fail` внутри `$( )` завершает ПОДОБОЛОЧКУ, а не скрипт —
вместо кода «предмет не установлен» получался код «нарушение». Поймано собственной пробой.

## 6. ⚠ РАЗВИЛКА, КОТОРУЮ РЕШАЕТ FOUNDER — не начинать без ответа

Founder назначил следующим предметом **полосы глубины** (`M-70`). Но `M-70` §0.3quater
объявляет зависимость от `M-75` (расцепление окна heatmap), и это НЕ приоритет, а сами
числа: расцепление меняет предмет задач 0 и 3 **на два порядка**.

| ветвь | что значит |
|---|---|
| **`M-75` раньше** | `M-70` делается ЦЕЛИКОМ, задачи 0 и 3 в настоящем виде |
| **`M-70` раньше** | `M-70` идёт БЕЗ задач 0 и 3, они переоткрываются после |

**Рекомендация architect'а (не решение):** `M-75` первым — он почти закрыт, осталась одна
задача (приземлить спас-реф `refs/salvage/2026-09-01/m75-b6-serial` вместе с приёмом B-5).
Обратный порядок означает делать часть полос дважды.

**Ответа founder'а на момент записи НЕТ.** Вопрос задан 01.09 вечером, сессия сменилась
раньше ответа. Не выбирать самому.

## 7. Четыре оставшихся предмета — состояние на сейчас

Вершины брать `git ls-remote`; записанные SHA протухают.

| предмет | ветка | состояние |
|---|---|---|
| **`M-70`** полосы глубины | `docs/M-70-rev2` | набор написан (`DB-I-4` красен по `TD-159`, verify FAIL (6)); ЗАВИСИТ от `M-75` — §6 |
| **`M-72`** терминальность подписки | `feat/M-72-subscription-terminality` | `C-200` REJECT: B-2 «E-3 всё ещё допускает терминальную ошибку ВМЕСТЕ с закрытием всего сокета» · B-3 «verify зеленее предмета на проводе» · B-4 «граница терминальной ошибки в T2 не литерал и не разрешена». Круг 4 ПОСЛЕДНИЙ (`A-029` §2) |
| **`M-74`** читаемость офсайт-копии | `docs/M-73-closeout-architect` | конструкция «отпечаток прочитанного» объявлена; НЕ написаны оракулы задач 1b и 6b |
| **`M-75`** расцепление окна | `feat/M-75-heatmap-window-decoupling` | `C-201` закрыт обеими находками; осталась задача 2b — приземлить спас-реф вместе с приёмом B-5 |

`M-76` (барьер подлинности выписок) — `feat/M-76-raw-output-barrier`, спека заведена,
гипотезы проверены; был зафиксирован вторым после `M-45`, founder порядок пересмотрел.

## 8. Долг, заведённый и НЕ закрытый

- **Sanity по площадкам НЕВЫПОЛНИМА наблюдением.** `П-026` §Порядок п.3 требует предъявить
  `L2Delta` по `ETHUSDT` отдельно на споте и фьючерсах. Замер: `venue` в событии —
  ПЕРЕЧИСЛЕНИЕ, не строка (маркеры площадок в сырых данных → ноль), декодирующего
  инструмента на сервере нет. Предъявленное ревьюером «book levels на обеих площадках» миры
  НЕ различает: книга по эфиру строилась и ДО раскатки. Подписанное требование исполнено
  НЕ ПОЛНОСТЬЮ. Разбор — `docs/data-epochs.md` `E-002`, `M-45` §4ter.
- **`TD-195`** (MINOR): шаблон verify зеленее CI. Чинить ШАБЛОН, не один скрипт.
- **Бюджет ядра на исходе:** `scope-guard.md` запас **19 B**, `gates.md` — **27 B**.
  Следующая правка любой из двух норм упрётся в барьер и потребует ВЫЧИСТКИ, не добавления.
- **Милестоун-гейт `verify_M-45.sh` не архивирован** намеренно: частично дублирует новый
  барьер. Выносить после того, как новый отработает несколько реальных PR.

## 9. Спас-рефы — работа вне веток

```
refs/salvage/2026-08-31/m72-sentinel                e7d8ed5
refs/salvage/2026-08-31/m75-b5-inventory            1625c13
refs/salvage/2026-08-31/m75-b5-inventory-r2         5142a14
refs/salvage/2026-09-01/m75-b6-serial               e462c16   ← нужен задаче 2b M-75
refs/salvage/2026-09-01/m45-task7-enginedev         f3b84d4   (влит, историч.)
refs/salvage/2026-09-01/recheck-m45-worktree        21a14c1   (состояние чужого дерева)
```

## 10. Стартовый промпт следующей сессии

Живёт ЗДЕСЬ, потому что переписка не переживает смену сессии, а файл в `main` переживает.

```text
Ты — architect hft-platform. Сессия начинается со стартап-протокола, ПОТОМ работа.

ШАГ 1. СТАРТАП-ПРОТОКОЛ — предъявляется РЕЗУЛЬТАТОМ. Читать С ДИСКА, целиком:
  CLAUDE.md · .claude/rules/*.md ВСЕ ШЕСТЬ · .claude/agents/architect.md
  docs/workflow/reading-map.md
  ЯРУС A: docs/DESIGN.md · docs/04-workflow.md · docs/05-contract-layer.md ·
          docs/SESSION-HANDOFF.md §0/§0ter · grep -n '^## П-' docs/PENDING-SIGNATURE.md
  ПЕРЕДАЧИ (обе в main, читать обе):
          docs/workflow/session-handover-2026-09-01.md   — утро, классы ошибок и инвентарь
          docs/workflow/session-handover-2026-09-01b.md  — ЭТА, состояние после
  ПРАВИЛА ОРАКУЛОВ Р-1…Р-4: docs/workflow/oracle-blindness-class-2026-08-28.md §5
  РЕШЕНИЯ АРБИТРА, обязательные: research/arbitration/A-030-* (форма факта) и
          research/arbitration/A-031-* (выписанная группа стражей) — оба в main
  ЯРУС B по предмету — карта reading-map §2. Для полос и расцепления:
          docs/fa/viz-backend.md ЦЕЛИКОМ
  ДОЛГ в первый Handoff: у gateway, gateway-serve, derive, recorder FA НЕ СУЩЕСТВУЕТ.

ШАГ 2. ЯРУС S — четыре команды, сырой вывод:
  gh run list --branch main --limit 5
  gh run list --workflow=deploy.yml --limit 3
  ssh -i /home/nous/.ssh/hft_deploy -o IdentitiesOnly=yes root@167.233.192.131 \
    'cd /root/hft-platform && git rev-parse --short HEAD; df -h /; \
     cat /var/lib/hft/journal-offsite.last-success; \
     cat /var/lib/docker/volumes/hft-platform_journal-data/_data/recorder.heartbeat'
  bash scripts/check_branch_health.sh

ПЕРВЫЙ ХОД — СПРОСИТЬ FOUNDER'А ПРО РАЗВИЛКУ (§6 передачи). Он назначил следующим предметом
ПОЛОСЫ ГЛУБИНЫ, но они зависят от РАСЦЕПЛЕНИЯ ОКНА: расцепление меняет предмет двух задач
полос на два порядка. Две ветви, третьей нет:
  · расцепление раньше — полосы делаются ЦЕЛИКОМ
  · полосы раньше — они идут БЕЗ двух задач, потом переоткрываются
Рекомендация: расцепление первым, там осталась ОДНА задача. НЕ ВЫБИРАТЬ САМОМУ.

ЖЁСТКИЕ РАМКИ:
 · Роли цепочки (critic/reviewer/dev/tester/адверсарий/арбитр) запускает founder. Если он
   говорит «запускай сам» — только subagent_type: architect, модель НАЗЫВАТЬ в запуске,
   контекст свежий (кроме доглядки собственных находок — там тот же агент).
 · «Документ X говорит Y» требует ОТКРЫТИЯ X. Отчёт агента — гипотеза, git — факт.
 · Утверждение о состоянии — ТОЛЬКО с командой в ТОМ ЖЕ сообщении. Время — date -u.
 · PUSH И MERGE СТОЯТ ЗА ПРОВЕРКОЙ КОДА ВОЗВРАТА, а не рядом.
 · Перед сносом незакоммиченного — спас-реф в origin, НЕ патч в /tmp.
 · Мутация — в ОТДЕЛЬНОМ worktree; после КАЖДОЙ печатать СТРОКУ ПОСЛЕ ПРАВКИ и убедиться,
   что это КОД, а не комментарий (ложный якорь ловил дважды за сессию).
 · Правка по вердикту НАЧИНАЕТСЯ грепом класса и ЗАКАНЧИВАЕТСЯ предъявлением НУЛЯ.
 · Коммит называет пути ЯВНО; commit -a и add -A запрещены хуком.
 · Номера — ТОЛЬКО scripts/next_artifact_id.sh. Полные SHA — git rev-parse.
 · Бюджет ядра: запас 19 B и 27 B. Правка норм = вычистка, не добавление.
 · Ответ заканчивается Handoff-блоком из пяти секций.

САМОЕ ЦЕННОЕ ИЗ НЕЗАКРЫТОГО:
 1. Копия журнала уходит по расписанию, но что она ЧИТАЕТСЯ — не доказано ничем (M-74).
    Резервная копия, читаемость которой не проверена, — не резервная копия.
 2. Sanity состава ПО ПЛОЩАДКАМ невыполнима наблюдением (§8 передачи) — подписанное
    требование исполнено не полностью.
```
