# FA — viz-backend: дериватив-слой, read-gateway, экспорт для кокпита (Слой 8)

STATUS: FA v1 (2026-07-22, architect). Планово-архитектурный документ виз-бэкенда для
**Order Flow Intelligence Terminal** (Bookmap+TPP+AI кокпит). Реализация — отдельными milestone'ами.
Источники: `docs/07-cockpit-backend-roadmap.md` (решения сессии), `docs/06-data-layer-and-storage.md`
(даталеер — на котором ЭТОТ слой стоит), `docs/05-contract-layer.md` (контракт-governance),
`research/exports/format.md` (export v1, M-17), `research/data-quality/depth-probe-*.md` (глубина книги).

---

## §1. Место слоя: ПОВЕРХ даталеера, read-only

Виз-бэкенд — **консюмер журнала**, не его писатель. Он НЕ трогает recorder/journal-writer
(`docs/06` §1 journal-first; RC-I-7). Вся связь с данными — через **`journal::stream(dir, EpochFilter)`**
(`docs/06` §5, bounded-memory итератор) и `crates/book` (реконструкция стакана). Это **Граница A**
(`docs/03` §4): всё ниже — чистые редьюсеры над потоком `Event`, без wall-clock/rand/I-O в расчёте.

**Почему так (BINDING):** journal-first + детерминизм (`DET-I-1`, `docs/06` §5, `DESIGN` §1) дают
**live == replay бесплатно** — один код редьюсеров, live кормит хвост журнала → бит-идентичный вывод.
Это фундамент replay-tutor'а и «AI видит то же, что человек».

**Граница market-плоскость ↔ app-плоскость (`docs/07` §5 D6, добавлено 2026-07-23).** Виз-бэкенд —
ТОЛЬКО market-плоскость: `gateway-serve` (транспорт, D1) держит WS, тейлит журнал, отдаёт
snapshot/frames/replay — **read-only, детерминированный, stateless по юзеру**. Он **НЕ владеет** аккаунтами,
стратегиями, чатами, настройками — это **application-плоскость** (Next.js + Postgres, зона founder'а).
Единственная связь: `gateway-serve` **ВЕРИФИЦИРУЕТ** короткоживущий подписанный JWT (выпущенный Next.js;
крейт `jsonwebtoken`, HS256/Ed25519) на WS-connect — stateless проверка подписи, БЕЗ обращения в user-БД.
**Инвариант (VB-I-9):** gateway не читает/не пишет application-БД; user-состояние (стратегии/чаты/настройки)
живёт в Postgres, НЕ в market-журнале (`DET-I-1` цел, тот же класс, что `AI-I-1`).

## §2. Дериватив-слой: индикаторы как чистые редьюсеры

Каждый виз-индикатор = функция `(&[Event] | stream) -> Series`, детерминированная:

| Индикатор | Вход (journal) | Статус |
|---|---|---|
| OHLCV, footprint-δ, **CVD**, footprint-bins, depth-series | Trade+side / L2 | ✅ есть (M-17 export v1) |
| **Heatmap / COB / Volume Bubbles** | L2Delta (M-18) / Trade | ✅ есть (M-23 DONE; `SeriesBundle.{heatmap,cob,volume_bubbles}`) |
| **Volume Profile** — **только SVP** (per-session: POC/VAH/VAL/VA%) | Trade | ✅ есть (M-24 DONE, session-anchored, `VB-I-6`/`VB-I-8`). **НЕ реализованы:** CVP, FRVP, Anchored, Composite, HVN/LVN — прямо вне scope M-24 (`docs/archive/M-24-volume-profile.md:17-18`) |
| **VWAP** (journal-cumulative — семантика пересмотрена M-36, см. VB-I-6; HLC3/σ-полосы НЕ реализованы) | Trade | ✅ есть (M-20→M-36 DONE) |
| **Liquidations/OI/Funding профили** | Liquidation/OI/Funding | 🟡 новый (M-25) |
| **TPP Bid/Ask, Delta** (полосы 1.5/3/5/8/15/30/60%, per side, COIN-scope) | L2 (book depth) | ✅ включение подписано `П-014`; в проде НЕ включено (`GATEWAY_BANDS=0.001`) до `TD-159` (`TD-158` ЗАКРЫТ close-out'ом `M-68`, `R-156`); follow-up 30–60 % (M-33) состоялся, вердикт suspended — `A-002` З-2 |
| TPP TOTAL/TOTAL1-3/OTHERS | — | ✅ состав подписан `П-013` 2026-08-17 (TOTAL=Σ всех; T1=BTC; T2=Σ−BTC; T3=Σ−BTC−ETH; OTHERS=Σ−топ-10); реализации нет |
| Secrets (MLSP/Margin/Ratio/Speed) | — | ⛔ `formula_pending` (founder-спека; VB-I-7 сужен до Secrets — `П-013`) |

**Модель сессии** (00:00 UTC anchor/reset, session templates) — общий примитив для VWAP / SVP / CVD-reset;
живёт в дериватив-слое (не в journal — это интерпретация, не факт).

## §3. Три подсистемы

```
   ┌─ A. INDICATOR ENGINE ──────────┐   ┌─ B. READ GATEWAY (live+replay) ─┐
   │ чистые редьюсеры над journal    │   │ хвост journal → редьюсеры →      │
   │ → серии (детерминированы)       │──▶│ снапшот(REST) + WS-push + replay │──▶ фронт code2alpha
   └─────────────────────────────────┘   │ read-only (RC-I-7), live==replay │   (lightweight-charts)
                                          └──────────────────────────────────┘
   ┌─ C. EXPORT CONTRACT v2 ─────────────────────────────────────────────────┐
   │ версионированная форма серий (export_schema_version), аддитивно поверх v1 │
   └──────────────────────────────────────────────────────────────────────────┘
```

- **A. Indicator Engine** — фактически `crates/gateway` (`Reducer`) + `crates/research-cli`; `crates/derive` остался скелетом (Breadth, 52 строки). Новые агрегаты — новые
  чистые редьюсеры; RED-first на каждый (детерминизм-тест обязателен).
- **B. Read Gateway** (M-22, крейт `crates/gateway` — библиотека; транспорт `gateway-serve` — отд. milestone).
  **enabling-инфра кокпита.** Тейлит журнал live, прогоняет редьюсеры, отдаёт: (1) снапшот при подключении,
  (2) инкрементальный **WS-push** — `gateway-serve` держит WS **напрямую** (tokio-tungstenite, D1/D6; Fastify
  НЕ в горячей полосе), (3) replay (детерминированный проигрыш окна). Тяжёлые серии (heatmap/depth) — бинарные
  фреймы — БЫЛ ПЛАН; факт: провод целиком JSON (`serde_json::to_vec`), postcard отвергнут
  для фронта как Rust-only (`crates/gateway-serve/src/lib.rs:8-9`) и живёт в ЧЕКПОИНТЕ
  (`GW-I-9`). Прежняя строка противоречила `VB-I-6` этого же документа. **Read-only**: не
  пишет журнал, recorder не зависит от gateway.
- **C. Export contract v2** — `research/exports/format.md` расширяется АДДИТИВНО (bump `export_schema_version`,
  без T1-RFC — T-designate, как v1; `docs/05` §governance). Новые серии: heatmap, volume_profile, vwap,
  liq/oi/funding-profiles, tpp (с provenance §4).

## §4. Data-quality дисциплина (замер глубины — BINDING)

Урок C-020/M-10 «сначала провалидируй измеритель, потом доверяй» переносится сюда как гейт.

- **Глубина книги — ДОСТАЁТСЯ ДО 60 %, и это не то же самое, что «валидируется».** Две величины
  здесь разные, и прежняя редакция их смешивала. **ОХВАТ ИСТОЧНИКА:** diff-книга доходит до
  50–59 % от mid (`research/data-quality/depth-probe-binance.md:62-65`: spot BTCUSDT 50.12/59.13 %,
  spot ETHUSDT 50.07/59.16 %, futures 57–59 %), а эмиссия режется порогом
  `MAX_REL_DIST = 0.60` (`crates/venue-binance/src/lib.rs:33`). Полосы до 60 % доступны —
  решено многократно и подтверждено замером. **ЭТАЛОН СВЕРКИ:** независимого источника глубже
  ~1.3 % не существует ни у биржи, ни у вендоров (Binance капит REST-снапшот, Tardis — тот же
  кап); это inherent для всего класса (Bookmap/TPP тоже реконструируют из diff). Отсутствие
  эталона ограничивает ПРОВЕРКУ, а не ОХВАТ, и запретом на включение полос не является.
- **Инвариант провенанса:** каждая серия по книге глубже 1.3% несёт `depth_band_provenance`.
  Живое СОДЕРЖИМОЕ строки после `П-014`: `"diff-reconstructed, liveness=confirmed|unconfirmed"`
  (по стороне) либо `"not-observed band=… reach=…"` (полоса за наблюдённым охватом) —
  `depth_provenance_label`, `crates/gateway/src/lib.rs:1363`. Прежняя редакция называла здесь
  строку `"diff-reconstructed, validated<=1.3%"` — форма поля та же, содержимое сменил фикс
  `П-014`. Фронт/AI не выдают её за биржевой факт.
- **⚠️ ПРОИЗВОДИТЕЛЯ МЕТКИ ДВА, И ОСВЕДОМЛЁННОСТЬ У НИХ РАЗНАЯ (`C-148` B-2 → `A-019`).**
  Сказанное выше — про **депт-серию** (`DepthRow`): `depth_provenance_label(band, side,
  reach)` — три deep-исхода (`liveness=confirmed|unconfirmed` по стороне в охвате,
  `not-observed band=… reach=…` за охватом). **Второй производитель — heatmap**
  (`HeatmapCell.depth_band_provenance`): метка ставится **только по ширине** —
  `crates/gateway/src/lib.rs:1211-1212` (`deep_thr = mid * 0.013`;
  `prov_str = "diff-reconstructed"`), вешается на `:1228`/`:1244` как `deep.then(…)` — без
  стороны и без охвата, одним неизменным значением. **COB метки не несёт вовсе:** `CobLevel`
  (`:221-225`) — только `side`/`price_e8`/`size_e8`. Итого словарь, видимый потребителю, —
  четыре разные строки от двух путей; рассогласование словаря в одной выдаче — **`TD-161`**
  (Ф4, MINOR). Оно перестанет быть теоретическим при включении полос: окно heatmap —
  `max(GATEWAY_BANDS)` (`:1192`), так что исполнение `П-014` п.4 расширит и heatmap, и его
  deep-ячейки выйдут в выдачу с ширинной меткой. Факт сегодняшнего кода, названный здесь,
  а не задача этого документа; предусловием `П-014` он НЕ является (`A-019` §2).
- **⛔ ПРОВЕНАНС И ЖИВОСТЬ РАСЦЕПЛЕНЫ (2026-08-03, `A-002`).** `VB-I-5` мотивирован ДВУМЯ вещами,
  и ни одна из них не есть «полосы живые»: (а) честностью измерителя — метка прямо говорит, что
  глубже 1.3% валидации нет; (б) решением (i) founder'а «эталона глубже 1.3% не существует ни у
  биржи, ни у вендоров» — оно В СИЛЕ и дефектом метрики не затронуто. Прежняя мотивировка через
  Q2 («дальние полосы ЖИВЫЕ») СНЯТА: числа `cancel_fraction` измеряют насыщение, а не живость
  (дефект метрики, `R-031` §A.1). Инвариант остаётся обязательным; **провенанс-метка сама по себе
  разрешением включать полосы не является** — разрешение дала подпись founder'а `П-014` (см. ниже),
  а метка обязана нести то, что реально известно про КАЖДУЮ сторону.
- **Корректность книги — предусловие TPP** (замена «вендор y/n»): (а) эвикция мёртвых уровней (**TD-016**,
  Track A — milestone НЕ ЗАВЕДЁН, номер M-31 из ранней очереди не аллоцирован файлом); (б) целостность при resync (ресинк к мелкому снапшоту не должен ронять восстановимые
  дальние полосы в 0 как «настоящий» факт). RED-спека на venue-book — до включения TPP-полос в контракт.
- **✅ ЗАМОК `A-002` З-1 СНЯТ ПОДПИСЬЮ FOUNDER'А 2026-08-17 (`П-014`). З-2 ОСТАЁТСЯ.**
  Прежняя редакция обещала автоматическое снятие переснятием через M-58 — **этот путь не
  сработал**: исход M-58 СМЕШАННЫЙ, и `research/data-quality/depth-verdict.md` прямо фиксирует
  «замок A-002 ОСТАЁТСЯ». Снят он путём (b) той же процедуры (`A-002` §2 «Что снимает замок») —
  явной подписью, принимающей включение на основании ТОЛЬКО провенанса.
  **Что это значит по пунктам:**
  - **полосы глубже 1.3 % ВКЛЮЧАЮТСЯ** — `GATEWAY_BANDS` перестаёт быть ограничен `0.013`
    (сегодня дефолт `0.001`, `docker-compose.yml:134,203`; смена состава — зона `deploy/**`);
  - **З-2 НЕ снят:** `cancel_fraction` по-прежнему не доказывает живость. Он меряет насыщение,
    и M-58 ОПРОВЕРГ ask на трёх полосах из шести глубже 1.3 %
    (`research/data-quality/depth-verdict.md:80-98`: `[300,500)` 0.419, `[800,1500)` 0.247,
    `[3000,6000)` 0.403 против bid 0.713–0.992 на всех семи);
  - **⇒ РАЗЛИЧЕНИЕ СТОРОН ОБЯЗАТЕЛЬНО.** Метка достоверности вешается ПОСТОРОННЕ: bid —
    подтверждён, ask на глубоких полосах — «не подтверждена, разрежённые данные».
  Абзац ниже сохранён как история решения 2026-07-24, а не как действующий статус.
- **⛔ ВКЛЮЧЕНИЕ ОСТАЁТСЯ ЗАБЛОКИРОВАННЫМ — но СОСТАВ БЛОКЕРОВ СМЕНИЛСЯ (правка 2026-08-25).**
  Прежняя редакция числила два незакрытых предусловия и первым называла слепоту метки. На
  сегодняшнем коде это ЛОЖЬ, и вывод «включать нельзя» держится на других основаниях —
  названы ниже. Ошибку стоит оставить видимой: она пролежала в несущем документе с 20.08 и
  была бы прочитана как задача, которой нет.
  - **(а) ЗАКРЫТО НА УРОВНЕ СТРОКИ — для депт-серии, единственного предмета `П-014` п.1**
    (`A-019` §2; ширинная метка heatmap — см. §4 и `TD-161`, предусловием `П-014` она не
    является). Метка депт-серии знает СТОРОНУ и ОХВАТ:
    `depth_provenance_label(band_pct_e8, side, reach)` — `crates/gateway/src/lib.rs:1363`,
    вызов `:1106`; охват снимается `book.max_reach_pct` пер-сторонне (`:975-976`); оракул —
    `crates/gateway/tests/red_depth_provenance_by_reach.rs`. Полоса ЗА наблюдённым охватом —
    в том числе в окне ресинка после REST-снимка (`REST_DEPTH_LIMIT = "5000"`,
    `crates/venue-binance/src/lib.rs:27` — ~1.3 % spot BTCUSDT, ~4.5 % spot ETHUSDT,
    `research/data-quality/depth-probe-binance.md:15-18`) — получает `"not-observed"`, а не
    заниженное число под меткой достоверности.
    **ОСТАТОК (а), и он реален:** метка ОДНА на ряд точек РАЗНОГО качества — точка из окна
    ресинка описывается меткой последнего наблюдения. Это **`TD-159`** (OPEN, MAJOR), и он
    блокирует `П-014` п.4. Отдельно: барьера, удерживающего САМУ
    эмиссию до восстановления глубины, по-прежнему НЕТ (`crates/venue-binance/src/lib.rs:246`
    — `resyncing` лишь дедуплицирует запрос снапшота).
    Отдельно от этого: охват 60 % верен в УСТАНОВИВШЕМСЯ режиме, но после старта процесса и
    после любого sequence-gap книга пересобирается REST-снимком с жёстким
    `REST_DEPTH_LIMIT = "5000"` (`crates/venue-binance/src/lib.rs:27`), а это ~1.3 % на spot
    BTCUSDT и ~4.5 % на spot ETHUSDT (`depth-probe-binance.md:15-18`; на фьючерсах 500 уровней —
    ~0.09 %/0.26 %). Барьера, удерживающего эмиссию до восстановления глубины, НЕТ. Значит
    расширенные полосы в эти окна отдают заниженные числа, а метка о них молчит — ровно тот
    случай, который §4 выше называет «ронять восстановимые дальние полосы в 0 как настоящий
    факт». RED-спека на это — предусловие включения.
  - **(б) Каденция — ЗАКРЫТО milestone'ом `M-68`.** Депт-серия пересчитывалась ТОЛЬКО в
    ветке `L2Snapshot`: ветка `L2Delta` обновляла книгу и heatmap, а `depth_series` — нет
    (комментарий «депт-серия остаётся snapshot-only»). Это давало 1 Гц против дельт 100 мс,
    и включённые полосы отдавали бы ОТСТАЮЩИЕ числа, о чём выдача молчала (`PL-I-7`).
    `M-68` пересчитывает серию на пути `L2Delta` по СОБСТВЕННОЙ каденции, объявляет её в
    выдаче и отвергает непредставимую каденцию на старте; `TD-158` закрывается его
    close-out'ом. **Предусловие `П-014` п.2 ИСПОЛНЕНО.**

    **Порядок вливания существен, и он назван здесь намеренно.** Эта правка ПРАВДИВА только
    поверх `M-68`: на ревизии, где его нет, `GATEWAY_SCHEMA_VERSION` = 8 и ветка `L2Delta`
    серию не трогает. Ветка настоящей правки вливается ПОСЛЕ `M-68`, а не до и не вместе —
    иначе FA утверждала бы о коде то, чего в нём ещё нет, то есть ровно тот класс, который
    этот документ ловит у других (`TD-138`).
  Смена формы выдачи ⇒ bump `GATEWAY_SCHEMA_VERSION` (прецеденты `VB-I-6` 5→6 и 6→7,
  `VB-I-11` 7→8, **`M-68` 8→9** — каденция депт-серии объявляется в выдаче, значит форма
  сменилась). Замер: `GATEWAY_SCHEMA_VERSION = 9` на ревизии `M-68`, `= 8` до неё. Прежняя
  редакция обрывала цепочку на 7→8 и расходилась с кодом (`R-154` `N-13`).
- **ВЕРИФИКАЦИЯ достоверности глубины (M-32) — ЗАВЕРШЕНА, founder-решение принято (2026-07-24).**
  M-32-depth-verification провалидировал ИЗМЕРИТЕЛЬ (C-020/M-10, доведено до глубины): **Q1** — эталона глубже
  1.3% нет ни у кого (паритет с Bookmap/TPP CONFIRMED); **Q2** — дальние полосы ЖИВЫЕ (cancel_fraction FAR=0.805
  vs NEAR=0.981, order-flow consistency=0.950, gaps=0 на gap-free segment 78) → фантом TD-016 эмпирически снят.
  **✅ FOUNDER APPROVED:** строить TPP-полосы на diff-книге с `depth_band_provenance: "diff-reconstructed,
  validated<=1.3%"` (VB-I-5), **диапазон 1.5–60%**. **Граница верификации:** живость доказана для **1.5–30%**;
  **30–60% — provenance + follow-up live-замер** (расширить band-схему анализатора до `[3000,6000)` bps,
  переснять на segment 78) ДО включения этих полос. См. `research/data-quality/depth-verdict.md`.
- **`formula_pending`** для неподтверждённых формул (TPP TOTAL-состав, Secrets) — бэкенд НЕ выдумывает
  (бриф §27). Серия объявлена, значение — `pending`, пока founder не подпишет методологию.
- **История 2019+** — только через вендора (**Tardis.dev**, Binance spot L2 с 2019-12); НЕ для глубины, для
  временного охвата (backtest/replay). Импорт — `Vendor`-эпоха (CT-RFC-02, `docs/data-epochs.md`), fail-closed
  классификация, в обучение по умолчанию не попадает.

## §5. Инварианты (RED-оракулы; sacred, architect-only)

| ID | Инвариант |
|---|---|
| VB-I-1 | Каждый индикатор — чистый редьюсер над `journal::stream`; детерминизм-тест обязателен (тот же вход → байт-идентичная серия). Нет wall-clock/rand/I-O в расчёте |
| VB-I-2 | **live == replay**: серия, посчитанная на live-хвосте, бит-идентична серии из replay того же окна журнала |
| VB-I-3 | Read Gateway read-only: grep-канарейка — gateway не импортирует journal-writer/recorder-write; recorder не зависит от gateway |
| VB-I-4 | export v2 аддитивен: старые консюмеры v1 не ломаются; форма меняется только с bump `export_schema_version` (CT-I-аналог) |
| VB-I-5 | Серия глубже 1.3% несёт `depth_band_provenance`; отсутствие поля → серия невалидна (честность измерителя) |
| VB-I-6 | **Per-series anchor policy (M-36, founder-decision — пересмотр M-20; M-38a — CVD ledger).** VWAP = **journal-cumulative** (all-time от старта курсора, БЕЗ session-reset — `sum_pv/sum_v` копятся по всему `journal::stream`). SVP + CVD = session-anchored (00:00 UTC, `utc_session_id`). **CVD (M-38a, founder-подпись 2026-07-27, TD-043):** `cumulative_delta` — **per-session ledger ЗЕРКАЛЬНО VP**: running обнуляется на границе 00:00 UTC (каждая UTC-сессия — свой running с нуля), НЕ единая сумма через дни. Состояние `cvd: BTreeMap<session_id, CvdSession{base, bucket_delta}>`; эвикция — бакеты внутри текущей сессии → base ЭТОЙ сессии, целиком прошедшая сессия (критерий `max_time_s < lo`, ТОТ ЖЕ что VP) → удаляется; ОДНА структура session-max-времён на CVD и VP. **Форма v7 — ДВА per-session поля `SeriesBundle` (оба обязательны; TD-047):** (1) `cvd_session_base: Vec<(session_id, base)>` — база CVD-ledger'а; (2) `vp_session_max_time_s: Vec<(session_id, max_time_s)>` — зеркало `Reducer::session_max_time_s`, добавлено фиксом TD-045 в ТУ ЖЕ v7 (второй bump не требовался: v7 ещё не был в main). Оба сортированы по `session_id` возрастанию, оба `#[serde(default)]` (defensive-default, НЕ совместимость — консюмер ОБЯЗАН гейтить на `schema_version == 7`), оба уходят на провод: `gateway-serve` сериализует `Snapshot`/`Frame` целиком (`serde_json::to_vec`), т.е. поля — часть контракта с фронтом (GS-I-4) БЕЗ явного упоминания в коде транспорта. **Семантика `vp_session_max_time_s` (BINDING):** это последний `bucket_time_s`, КОГДА-ЛИБО виденный в сессии; merge-путь применяет по нему whole-session drop VP по критерию `vp_session_max_time_s[sid] < lo_time_s`, **ИДЕНТИЧНОМУ** `Reducer::evict_window_state`. Критерий обязан совпадать буквально: старый предикат «день ушёл вперёд» (`row.session_id < utc_session_id(at)`) НЕ эквивалентен оконному и ронял прошлую сессию сразу после 00:00 UTC, пока окно `[at−W, at]` её ещё пересекало (TD-045). Порядок на merge существенен: whole-session drop выполняется в `Snapshot::apply` **ПОСЛЕ** `merge_volume_profile` (drop внутри эвикции терял бы bins сессии, которую incoming восстанавливает). Сессия без записи — «нет данных о max», используется только в drop-критерии. Якоря VWAP↔SVP/CVD РАЗНЫЕ намеренно; каждый ЯВНО задекларирован и детерминирован (VB-I-1). Смена семантики VWAP (M-36) и переход CVD single-running→session-ledger (M-38a) ⇒ bump `GATEWAY_SCHEMA_VERSION` (M-36: 5→6; M-38a: **6→7**) |
| VB-I-7 | `formula_pending`-серия НЕ эмитит вычисленное значение (только маркер), пока формула не подписана founder'ом. После `П-013` (2026-08-17) действует ТОЛЬКО для Secrets: состав TOTAL-семейства подписан. Оракула нет — серий `formula_pending` в коде пока не существует (`grep -rn formula_pending crates/` → один комментарий, тестов ноль) |
| VB-I-8 | Volume Profile: цены без сделок НЕ выдумываются (как footprint-bins v1, C-016); POC/VAH/VAL детерминированы от разбиения |
| VB-I-9 | **Граница плоскостей (D6):** `gateway-serve` НЕ читает/не пишет application-БД (Postgres); auth = ТОЛЬКО stateless verify подписанного JWT, без user-БД-lookup. grep-канарейка: gateway не импортирует postgres/sqlx/diesel-клиент. User-состояние (стратегии/чаты/настройки) вне market-журнала (`DET-I-1`) |
| VB-I-10 | **Bounded-window snapshot (M-37, TD-039).** Память `snapshot`/`frames_since` ограничена ОКНОМ `[at−W, at]` (`Selector.window_ms=Some(W)`), НЕ числом time-бакетов истории — иначе host-OOM (прод: RSS 7.3GB). Раздельное удержание: **бакет-оконное** (heatmap/ohlcv/depth/bubbles — эвикт бакетов `<at−W`) vs **сессионно-скалярное** (VWAP all-time `sum_pv/sum_v`; CVD per-session ledger — base текущей сессии + эвикт целыми ПРОШЛЫМИ сессиями, M-38a; VP полная ТЕКУЩАЯ сессия, эвикт целыми ПРОШЛЫМИ сессиями). **Whole-session drop VP — ОДИН критерий на ОБОИХ путях (TD-045/TD-047):** в редьюсере `session_max_time_s[sid] < lo_time_s` (`evict_window_state`), на merge-пути — тот же критерий по wire-полю `SeriesBundle.vp_session_max_time_s` (форма v7, см. VB-I-6); merge структурно не может вывести его из `VolumeProfileRow` (строка не несёт времени) — поэтому поле обязано ехать на провод, а не восстанавливаться эвристикой. Окно привязано к курсору `at`, не wall-clock; одно правило в `full`/`snapshot(C)`/свёртке кадров (VB-I-2 live==replay сохраняется). `window_ms=None` — offline-режим (полная свёртка). Оракул ОБЯЗАН давить рост по бакетам (multi-bucket/multi-day), НЕ один бакет (урок слепого `red_gateway_bounded`). Латентность (не память) — Путь Б, M-38 (чекпоинт) |
| VB-I-11 | **Провенанс ИСТОРИИ (M-48, TD-048).** «All-time» ≡ «от самого раннего seq, доступного под данным `EpochFilter`» — потеря префикса журнала (purge M-36, retention-prune `docs/06` §4) штатна и необратима. Система не отказывается отдать то, что есть, но обязана НЕ ВЫДАВАТЬ ЭТО ЗА ДРУГОЕ: `Snapshot` несёт `history_start_seq` (seq первого РЕАЛЬНО свёрнутого события — НЕ `header.first_seq`, который у legacy синтезирован нулём, TD-030) и `history_truncated`. Значения ОДИНАКОВЫ на обоих путях (`snapshot` и `snapshot_from_checkpoint`); бутстрап чекпоинта на усечённом журнале ЛЕГАЛЕН. Отказ — только при разрыве «чекпоинт↔журнал» (`earliest_seq > ckpt.cursor + 1`), где события не свёрнуты ни во что. Тот же класс честности, что `depth_band_provenance` (VB-I-5) и `formula_pending` (VB-I-7). Смена формы ⇒ bump `GATEWAY_SCHEMA_VERSION` **7→8** |

### `MD-I` — инварианты РЫНОЧНОЙ ДЕПТ-СЕРИИ (семейство заводится `M-68`)

| ID | Инвариант |
|---|---|
| MD-I-8 | **Депт-серия пересчитывается ОТ КНИГИ на пути `L2Delta`, а не только на снимке, и по СВОЕЙ каденции.** Инвариант СОСТАВНОЙ: десять обязательств, каждое со своим оракулом — карта ниже. Одна строка их не заменяет, и раньше заменяла (`C-185` B-2) |

#### Карта покрытия `MD-I-8`: обязательство → оракул (`C-185` B-2)

Одна строка определения описывала часть корпуса и молчала об остальном — то есть снова
превращала набор оракулов в ЯРЛЫК, ровно дефект `R-154` `N-9`, против которого семейство и
заводилось. Ниже — проверяемая модель покрытия. Она СНЯТА С КОДА, а не сочинена: замер
`grep -rhoE '^fn md_i8_[a-z0-9_]+' crates/*/tests/*.rs | wc -l` → **21** оракул в **4** файлах.

| # | Обязательство | Оракулы |
|---|---|---|
| 1 | серия следует за дельта-хвостом ОБЕИМИ сторонами, и каждая полоса движется своим содержимым, а не подменяется соседней | `d1`, `d2` (setup-страж), `d3`, `d4` |
| 2 | **ресинк-снимок ЗАМЕЩАЕТ книгу, а не мержит в неё** | `d5` |
| 3 | **граница работы:** счётчик считает реально посещённые уровни, и семь полос стоят ОДИН проход книги, а не семь | `d6a`, `d6b`, `d10` |
| 4 | **число и провенанс сняты ОДНИМ наблюдением** — в обе стороны: и когда дельта ужимает книгу, и когда растит | `d7`, `d7b` |
| 5 | вырожденный вход: односторонняя книга НЕ даёт точку-ноль, двусторонняя даёт точку | `d9`, `d9_c` |
| 6 | каденция есть КОНФИГ, ведомый СОБЫТИЙНЫМ временем и объявленный в выдаче; объявленное равно доставленному | `d12`, `d13`, `d17` |
| 7 | непредставимая каденция отвергается на СТАРТЕ, а не схлопывается молча | `d14` (+ `d18e`/`d18f`/`d18g` — см. оговорку) |
| 8 | смена семантики или каденции ИНВАЛИДИРУЕТ чекпоинт | `d8`, `d15` |
| 9 | **тёплый старт из РЕАЛЬНО ПРОЧИТАННОГО чекпоинта равен полному реплею** на дельта-хвосте | `d8b`, `d16` |
| 10 | живая серия равна перепроигранной ПРИ КАЖДОЙ каденции (`VB-I-2`) | `d20` |

**Оговорка к строке 7, названная, а не умолчанная.** Стартовая валидация живёт ТРЕМЯ
оракулами в файлах, метки `MD-I-8` НЕ несущих (`crates/gateway-serve/tests/red_depth_cadence_from_env.rs`,
`crates/gateway/tests/red_checkpoint_bin_prod_argv.rs`; замер `grep -c 'MD-I-8'` → `0` в обоих).
Они судят ТОЧКУ ВХОДА двух служб, а не саму серию, и метку получать не обязаны — но
обязательство 7 без них покрыто лишь наполовину, и это здесь сказано.

**Счёт в `DESIGN` §22 остаётся `1 / 1` и это НЕ ошибка:** колонки считают РАЗЛИЧНЫЕ
идентификаторы (у `GW-I` — `0 / 13`, у `JR-I` — `12 / 7`), а идентификатор здесь один.
Размер корпуса за ним — 21 оракул; чтобы «1 / 1» не читалось как «одна проверка», число
названо в строке семейства §22 и в этой карте.

**Почему не разделение на `MD-I-8..MD-I-17`.** Критик оставил выбор автору. Разделение
потребовало бы перестановки метки примерно в пятидесяти местах кода, включая
`crates/gateway/src/**` и `crates/gateway-serve/src/**` — зону engine-dev, где architect не
пишет (`scope-guard.md`). Карта даёт ту же проверяемость без правки в чужой зоне: каждое
обязательство предъявимо своим оракулом, и потеря любого из них наблюдаема счётом.

**Почему семейство заводится, а метка не переименовывается** (`R-154` `N-9`). До этой правки
`MD-I-8` существовал ТОЛЬКО как метка в коде и в гейте: `grep -rhoE '\bMD-I-[0-9]+\b' docs/`
давал НОЛЬ, братьев `MD-I-1..7` нет, в карте покрытия `DESIGN` §22 семейства не было. То есть
идентификатор инварианта указывал в пустоту — набор из тридцати с лишним ассертов ссылался на
текст, которого не существует.

Переименование в существующее семейство отвергнуто ЗАМЕРОМ, а не вкусом: метка вплетена
примерно в пятьдесят мест, включая `crates/gateway/src/lib.rs`, `crates/gateway-serve/src/lib.rs`
и `crates/gateway/src/bin/` — зону engine-dev, где architect не пишет (`scope-guard.md`).
Массовое переименование через чужую зону дороже и рискованнее, чем определить инвариант там,
где он и должен был быть определён.

**Разрыв `MD-I-1..7` — дыра, а не нарушение:** `gates.md` §12 требует УНИКАЛЬНОСТИ
идентификатора, а не непрерывности ряда. Номер `8` сохранён именно потому, что уже живёт в
коде и гейте; смена номера ради красоты ряда стоила бы тех же пятидесяти правок в чужой зоне
и не дала бы ничего.

**Зона семейства — стык `gateway`↔`book`:** серия строится редьюсером `gateway` из состояния
книги `book`. Дом выбран здесь, а не в `fa/book.md`, потому что предмет — ВЫДАЧА (форма,
каденция, провенанс), а не реконструкция стакана.

> **Где живут `GW-I-*` и `GS-I-*` — названо, но НЕ решено (`reading-map.md` §2 числит это долгом).**
> Крейты `gateway` и `gateway-serve` собственного FA не имеют, а инварианты у них есть и
> работают: замер на `origin/main` 2026-08-25 — `grep -rhoE '\bGW-I-[0-9]+\b' crates/ | sort -uV`
> даёт `GW-I-1..12` и `GW-I-14` (тринадцатый — дыра, не нарушение: `gates.md` §12 требует
> уникальности, не непрерывности). `DESIGN` §22 при этом числит семейство `GW-I` как
> «заявлено 0 / в оракулах 13» и называет это ОБРАТНЫМ ДРЕЙФОМ — оракулы есть, докс-семейство
> не заведено. `GS-I-*` в §5 ЭТОГО документа **НЕ объявлены** (`grep -cE '^\| GS-I-'
> docs/fa/viz-backend.md` → 0): их дом — `milestones/M-28-gateway-serve.md:49-53`, где
> `GS-I-1` помечен как `VB-I-9a`, а `GS-I-2` как `VB-I-9b` — то есть `VB-I-9` этого FA
> расщеплён там на две половины. Здесь `GS-I-4` лишь УПОМЯНУТ внутри текста `VB-I-6`.
> Канарейка `GS-I-1`/`VB-I-9a` живёт в `scripts/verify_M-28.sh:22-27`, который **CI не
> зовёт** (`grep -c verify_M-28 .github/workflows/ci.yml` → 0) — механизация частичная,
> и это здесь названо, а не подразумевается.
> Куда семейства переселять — отдельное решение (новый FA против расширения этого), и оно
> НЕ принимается этой правкой: правка приводит документ к факту, а формы не меняет.

## §6. Соответствие контракт-слою (`05`) и даталееру (`06`)

- **T1-ядро (`Event`/`EventKind`) НЕ трогаем** — только читаем (Граница A). Виз-контракты — T-designate в
  export/gateway-крейтах; промоушен в `crates/contracts` только при кросс-языковом консюмере (TD-008-паттерн).
- **Даталеер (`06`):** стрим-чтение (`06` §5), эпохи/provenance (CT-RFC-02), ретеншен/компакция (`06` §4) —
  виз-бэкенд их ПОТРЕБИТЕЛЬ. Replay-окно (сколько raw держим локально) — параметр ретеншена (`06` §4);
  Tardis-архив — cold/`Vendor`-эпоха. Read Gateway обязан работать на bounded-memory стриме (не `Vec<Event>`
  на 15 GB — класс TD-011).

## §7. Открытые вопросы
1. TPP `formula_pending`: ТОЛЬКО формулы Secrets (MLSP/Margin/Ratio/Speed) — состав
   TOTAL-семейства подписан `П-013` 2026-08-17.
2. Replay-окно (дни/недели/месяцы) → ретеншен-политика + Storage Box (founder).
3. ~~Транспорт бинарных фреймов heatmap (msgpack/protobuf)~~ — **закрыт фактом:** провод
   MVP целиком JSON; смена кодека предусмотрена бампом `v` (`CT-RFC-09` §3).
4. resync-целостность дальних полос — RED-спека на venue-book (предусловие TPP).

## §8. Cross-references
- `docs/07-cockpit-backend-roadmap.md` (решения), `docs/06-data-layer-and-storage.md` (даталеер),
  `docs/05-contract-layer.md` (governance), `docs/03-integration-contract.md` §4 (Граница A).
- `docs/fa/ai-copilot.md` (AI-слой — потребитель этого дериватив-слоя).
- `docs/rfc/CT-RFC-09-ws-session.md` (WS-сессия: subscribe-протокол v1, лимит 16 — подписан
  2026-08-11; реализация M-65 ЗАКРЫТ 2026-08-24).
- `research/exports/format.md` (export v1→v2), `research/data-quality/depth-probe-*.md` (глубина).
- `TD-016` (эвикция/фантом, OPEN) · `TD-010` (REST-глубина, OPEN; `TD-004` закрыт триажем
  2026-08-17 как ЛОЖНАЯ карточка) · `TD-158` (каденция) — **ЗАКРЫТ** close-out'ом `M-68` 2026-08-30 (вердикт `R-156`;
  карточка переехала в архив, `TECH-DEBT.md:58-59`) · `TD-159` (метка одна на ряд точек
  разного качества, **OPEN — блокирует `П-014` п.4**).
