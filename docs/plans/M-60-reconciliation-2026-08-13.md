# Сверка M-60 (`feat/M-60-mechanisms`) с состоянием `main` на 2026-08-13

**Кто:** architect-клон со свежим контекстом, read-only разбор (ничего не коммичено, не запушено;
пробный merge выполнен в отдельном detached-worktree `/tmp/m60-recon-arch`, откачен `merge --abort`,
дерево удалено). **Вопрос founder'а:** что из задуманного в M-60 уже сделано другими средствами,
что устарело, что противоречит построенному после 7 августа, что осталось нужным.

**Предмет:** `origin/feat/M-60-mechanisms` — 34 коммита поверх merge-base `bb8cf53`, отстаёт от
`origin/main` на 163 коммита, последняя активность 2026-08-07. Числа аудита процессного слоя
(`docs/plans/process-layer-audit-2026-08-13.md`) использованы как данность, не пересчитывались.

---

## 0. Сводная таблица — механизм → статус → доказательство → что делать

Механизмы — по зонтичной спеке `milestones/M-60-mechanisms.md` §3 (на ветке; в `main` её нет)
и по узким спекам `M-60b`/`M-60c` там же.

| # | механизм (назначение) | стадия на ветке | статус против `main` 13.08 | доказательство | что делать |
|---|---|---|---|---|---|
| G0 | `check_docs_freeze.sh` — замок процессного слоя | реализован + гейт пройден (R-041/R-042) | **УЖЕ СДЕЛАН — карвом, в более сильной форме** | merge `71611d0` (`feat/M-60a-only`, карв `567b6c1`); в `main`: `scripts/check_docs_freeze.sh`, проба `red_docs_freeze.sh` **27 сценариев** (на ветке — 9), джоб `docs-freeze` в `ci.yml:80`, норма `gates.md` §11, `verify_M-60a.sh`, вердикт `R-045` APPROVED | ничего; ветвевые версии четырёх файлов M-60a — выбросить (старее карва) |
| G1 | `check_context_budgets.sh` — бюджет обязательного чтения | RED готов (`red_context_budgets.sh`, CB-1..CB-6b, страж setup'а); реализации нет | **ВСЁ ЕЩЁ НУЖЕН — и аудит 13.08 усилил основание** | в `main` скрипта нет (`git ls-tree origin/main -- scripts/` — отсутствует); храповик измерен: сжатие `9a9f533` (−433 строки) откачено на 28 % за 10 дней, темп +105 строк/неделю, ядро 100 461 байт × ≈6.3 впрыска/сутки (аудит §1.2) | взять RED с ветки, ПЕРЕБАЗИРОВАТЬ лимиты (см. §3.2 — числа спеки протухли), реализовать + джоб в CI |
| G2 | `verify_design_claims.sh` + проба → в CI | только строка спеки (скрипт уже был в `main`) | **СДЕЛАН ЧАСТИЧНО: скрипт и проба в `main`, проводки в CI НЕТ — а проводка и есть предмет** | `scripts/verify_design_claims.sh` и `scripts/tests/red_verify_design_claims.sh` в `main` (с 01.08); `git grep design_claims origin/main -- .github/` → **пусто**; `TD-106` (MAJOR) прямо: «Лечится механизмом G2 спеки M-60; до его merge'а долг OPEN»; `TD-062` — тот же корень, OPEN с 01.08 | самый дешёвый пункт: ~8 строк YAML + джоб пробы; координация с M-63 (§2.3) |
| G3 | `check_gate_meta.sh` — GATE-META + subject-lock (вердикт привязан к предмету) | RED готов (`red_gate_meta.sh`, GM-1..GM-15, страж setup'а); реализации нет | **ВСЁ ЕЩЁ НУЖЕН, с одной переработкой под урок M-61** | в `main` скрипта нет; класс жив: `C-062` (вердикт над чужим репо, `audited_base` вне нашей истории) ловится только человеком; `TD-105` OPEN; норма §9 (Fable-перепроверка, 2026-08-11) плодит вердикт-файлы и сама называет «трейлер `FABLE-RECHECKED`» кандидатом на механизацию — GATE-META это смежный, более общий носитель | взять RED с ветки; проверку поля `milestone:` (GM-16 из `C-064` F-064-3) НЕ реализовывать как вычисление предмета — см. конфликт К-3 |
| G4 | §D push-статус как предикат (`AHEAD=$(git log origin/<ветка>..HEAD \| wc -l)`) | только спека (≤6 строк правила) | **УСТАРЕЛ В ЗАЯВЛЕННОЙ ФОРМЕ** (правило-текст) | не реализован (`grep AHEAD .claude/rules/handoff-block.md` на `origin/main` → пусто); аудит 13.08 §4: у текста в правилах измеренная эффективность на дисциплине исполнения **0**, у механизированного правила — 90 %; иерархия носителей ставит «текст в ядре» последним | содержание не выбрасывать: одна строка-предикат в ШАБЛОН §D `handoff-block.md` (носитель 3 — «шаблон, который агент физически заполняет») при чистке M-60c′, не новая норма |
| G5 | прозрачность гейтинга критика (`SKIPPED\|REQUIRED` + триггеры в §B handoff'а) | только спека (≤3 строки) | **УСТАРЕЛ В ЗАЯВЛЕННОЙ ФОРМЕ** (то же основание) | не реализован (`grep SKIPPED .claude/rules/gates.md` на `origin/main` → пусто); тот же замер аудита про текст | строка в шаблон §B при чистке; отдельного механизма не заводить |
| G6.1 | `gc_worktrees.sh --targets-only` — снос кэша независимо от merge-статуса | только спека (задача 9 не начата) | **УЖЕ СДЕЛАН другим средством, шире** | режим `--reclaim [N часов]` смержен `d957eb1` (12.08) после трёх кругов + арбитраж `A-008`; проба `red_gc_reclaim_args.sh` 13 сценариев в CI (`ci.yml:119`); первое боевое применение — 36 GB. Страж «15 минут свежести» из G6 заменён порогом idle-часов с валидацией парсером | ничего; остаточная дыра (72 % кэша живёт ВНЕ `git worktree list` — аудит §1.4 п.1) — НЕ предмет G6, а новая находка аудита: отдельный TD |
| G6.2 | `check_disk_budget.sh` + преамбула в `verify_*.sh` — «названный красный» вместо ENOSPC-мистики | только спека | **ВСЁ ЕЩЁ НУЖЕН** | в `main` скрипта нет; класс уже срабатывал: 2026-08-01 `verify_alerting.sh` дал FAIL `exit=101` от ENOSPC — ложный красный, «заставляет чинить исправное»; диск сегодня 83–85 % (аудит); аудит сам предлагает преамбулу verify как носитель и для проверки `CARGO_TARGET_DIR` — один носитель на оба инварианта | пересобрать: одна преамбула verify с двумя проверками (диск-бюджет + target-dir внутри дерева); RED писать заново (на ветке его нет) |
| M-60c | чистка корпуса до бюджета `A-003` (−279 строк, таблица сохранности по секциям) | спека готова (81 строка), работа не начата | **ВСЁ ЕЩЁ НУЖЕН — но перечень и числа ЗАМЕНЕНЫ аудитом 13.08** | цель жива: корпус 1 183 строки / 100 461 байт против цели ~725+70 — разрыв ВЫРОС с −279 до ≈−458 строк с момента спеки (база `7a163f7`); аудит §5 даёт более сильный план: не «вырезать в архив», а ПЕРЕНОС НОСИТЕЛЯ (testing.md:66–185 → профиль architect'а, −12.4 KB из каждого впрыска), дедуп замера «782 MB/105 GB» из 4 файлов, gates.md — кандидат №1 | пересобрать по аудиту §5; из старой спеки взять таблицу сохранности §3 (посекционно, `C-064` F-064-7) и принцип «B и D — две половины одной проверки» |

**Счёт: 2 УЖЕ СДЕЛАНЫ (G0, G6.1) · 1 ЧАСТИЧНО (G2 — осталась проводка) · 4 НУЖНЫ (G1, G3,
G6.2, M-60c′) · 2 УСТАРЕЛИ В ФОРМЕ (G4, G5 — содержание в шаблоны).**

---

## 1. Что было в ветке — состав, чтобы не читать 34 коммита

```
$ git log origin/main..origin/feat/M-60-mechanisms --oneline | wc -l   → 34
$ git diff --stat origin/main...origin/feat/M-60-mechanisms            → 23 файла, +5316/−2
```

Из 34 коммитов **~26 — история M-60a** (RED-круги C-065..C-068, арбитраж A-005, R-041/R-042,
реализация замка). Всё это УЖЕ в `main` через карв `feat/M-60a-only` (merge `71611d0`), причём
артефакты гейтов (C-064..C-068, A-005, R-041, R-042) в `main` тоже есть — перенесены при карве.
Проверено: `git cherry origin/main origin/feat/M-60-mechanisms` → 0 эквивалентных патчей, но
`check_docs_freeze.sh` и `red_docs_freeze.sh` на ветке **байт-в-байт** равны стартовой версии
карва (в пробном merge разрешились сами; `main` с тех пор ушёл вперёд — 27 сценариев).

**Уникальный груз ветки (в `main` отсутствует):**

| файл | строк | ценность |
|---|---|---|
| `milestones/M-60-mechanisms.md` (зонт) | 337 | таблица «что взято из einhard и почему», запретные списки, класс отказа — исторически ценно, фактологически протухло (§2.2) |
| `milestones/M-60b-gate-mechanisms.md` | 73 | спека G1/G2/G3 с находками `C-064`, включёнными как обязательства — ядро переносится |
| `milestones/M-60c-corpus-cleanup.md` | 81 | таблица сохранности §3 — переносится; числа — нет |
| `scripts/tests/red_context_budgets.sh` | 107 | RED CB-1..CB-6b, страж setup'а, fail-closed на отсутствие файла — переносится |
| `scripts/tests/red_gate_meta.sh` | 213 | RED GM-1..GM-15, машинный реплей C-062 — переносится с правкой (К-3) |
| `scripts/verify_M-60.sh` | 151 | зонтичный гейт «красный по построению» — мёртв как целое; приём нормализации корпуса перед грепом (строки 67–72) — забрать в verify M-60c′ |
| `research/critiques/C-062-M-60-mechanisms.md` | 191 | аудит-трейл инцидента «критик в чужом репо» — единственный экземпляр; на него ссылаются docstring RED-проб и зонт. Перенести в `main` |
| дельты `docs/09-roadmap-v2.md` (+14), `milestones/BACKLOG.md` (+19) | 33 | «Процессный трек» как именованная очередь — идея верная, статусные строки протухли (M-57/M-58/M-59 — все в другом состоянии); писать заново |

---

## 2. Конфликты и слияемость

### 2.1 Физическая слияемость — пробный merge (выполнен и откачен)

```
$ git merge --no-commit --no-ff origin/feat/M-60-mechanisms   (в detached-worktree от origin/main)
CONFLICT (content): .claude/rules/gates.md
CONFLICT (content): .github/workflows/ci.yml
CONFLICT (add/add): milestones/M-60a-docs-freeze.md
CONFLICT (add/add): scripts/verify_M-60a.sh
$ git diff --name-only --diff-filter=U | wc -l   → 4
```

**4 конфликтующих файла, и все четыре разрешаются «взять сторону `main`»** — ветвевые версии
строго старее: `gates.md` ветки несёт §11 в редакции до карва (в `main` — §11 после `a21d820`
плюс §12 от M-61, которого ветка не знает); `ci.yml` ветки несёт только джоб `docs-freeze`
(в `main` сверх него `artifact-ids`, `red_commit_paths`, `red_gc_reclaim_args`);
`M-60a-docs-freeze.md` и `verify_M-60a.sh` в `main` дописаны карвом (`34bde9e` — где живут
M-60b/c; `c6ca995` — mode 100755). То есть merge ветки целиком не даёт НИЧЕГО, кроме
уникального груза §1, — и тащит 26 коммитов чужой истории в граф.

### 2.2 Нормативные конфликты — документ, ложный на дереве слияния (`gates.md` §8)

Ветвевые документы содержат утверждения, которые после merge стали бы ложью в `main`:

- **К-1. «`.githooks` не существует; бутстрап хука в лаунчере инертен по конструкции»**
  (зонт §2, «Дополнительная находка»). Опровергнуто 13.08: `.githooks/pre-commit` +
  `scripts/install_hooks.sh` смержены `2a29644`; `pi-dev.sh:97` теперь ставит `core.hooksPath`
  на СУЩЕСТВУЮЩИЙ каталог. Абзац зонта описывает мир до этого.
- **К-2. G6-обоснование «gc_worktrees оптимизирует не то измерение»** — было верно 05.08,
  снято `--reclaim` (`d957eb1`, A-008). Спека G6 задачи 9 в ветвевом виде предписала бы строить
  уже построенное, с более слабым стражем (15 минут против валидируемого порога idle-часов).
- **К-3. G3/GM-16 против решения founder'а по M-61 (вариант Б).** Шесть блокеров M-61 подряд
  жили на том, что барьер ВЫЧИСЛЯЛ предмет артефакта; решение founder'а 12.08 вычисление
  предмета УПРАЗДНИЛО — «тело файла не читается вовсе» (`PROJECT-STATE.md`, M-61). Требование
  `C-064` F-064-3 «RED на неверный `milestone:`» в M-60b задача 5 толкает G3 ровно в тот же
  класс: валидировать поле `milestone:` против диффа = снова вычислять предмет. Переработка:
  поле `milestone:` остаётся ДЕКЛАРАЦИЕЙ автора (как связь «текстом» в §12); машинно проверяются
  только поля, не требующие суждения о предмете: `audited_repo` (== origin), `audited_base`/
  `audited_head` (существуют, head — предок HEAD), `verdict` (из перечня), subject-lock по
  ПУТЯМ класса «гейт» (это диф-класс, не предмет). GM-16 в этом виде не пишется; отступление
  от находки `C-064` — назвать в новой спеке явно, с этим основанием.
- **К-4. «M-60b закрывает TD-105» — завышено.** GATE-META проверяет вердикты, ПОПАВШИЕ в
  диапазон; merge БЕЗ какого-либо вердикта проходит молча — а `TD-105` ровно про отсутствие
  («merge-коммит без R-NNN уезжает тихо»). Это дефект класса «наблюдает сбой, но не
  ОТСУТСТВИЕ» (`testing.md`, целостность гейта, свойство 4). Новая спека G3 обязана либо
  добавить проверку «merge `M-NN` в `main` ⇒ в дереве слияния есть `research/reviews/R-*`,
  называющий milestone» (это и есть «следующий шаг» из карточки TD-105), либо честно писать
  «TD-105 — частично».
- **К-5. Все числа бюджета (G1, M-60c) сняты на базе `7a163f7`** (05.08): rules 949 строк,
  дельта −279. Факт 13.08 (аудит): rules+CLAUDE 1 183 строки, 100 461 байт, дельта до цели
  `A-003` ≈ −458. Лимиты G1 спека сама велит брать «от факта ПОСЛЕ чистки» — значит сначала
  чистка по аудиту §5, потом фиксация лимитов.
- **К-6. Статусные строки роадмапа/BACKLOG ветки** («M-57 REJECT R-035», «M-59 гейт PASS»)
  противоречат `main` (M-57 смержен и внедрён с недостигнутой целью; M-59 BLOCKED). Дельты
  писать заново, не мержить.
- **К-7 (не конфликт, но координация). Новые CI-джобы G1/G2/G3 против M-63 (`TD-114`).**
  Квота Actions восстановилась (close-out M-60a), но M-63 «CI платит за то, что проверяет»
  стоит в очереди с REJECT `C-073`, и его предусловие (docs-freeze + artifact-ids в `main`)
  теперь ВЫПОЛНЕНО — строка M-63 в `SESSION-HANDOFF` §0 протухла. Три новых джоба —
  секундного doc-gate класса (та же проводка, что `protected-artifacts`), квоту не двигают;
  но их состав должен попасть в перечень джобов M-63 rev3, иначе M-63 снова проверяет
  неполный список (его же собственный класс находки F-4).

### 2.3 Идентификаторы под §12 (M-61)

`M-60`, `M-60b`, `M-60c` заняты носителями на ветке (аллокатор и барьер считают объединение
ref'ов). Пересборка обязана ПЕРЕИСПОЛЬЗОВАТЬ те же пути файлов
(`milestones/M-60b-gate-mechanisms.md`, `milestones/M-60c-corpus-cleanup.md`) — тот же
носитель под тем же идентификатором нарушения не даёт. Файл под НОВЫМ именем
(`M-60b-*-v2.md`) барьер `check_artifact_ids.sh` покраснит как второго носителя. Буква — часть
идентификатора (§12), новые части семьи, если появятся, — `M-60d…` от аллокатора руками автора.

---

## 3. Рекомендация: ПЕРЕСОБРАТЬ ЗАНОВО от `main`, ветку после спасения груза — удалить

**Не ДОВЕСТИ:** merge ветки не даёт ничего сверх восьми файлов §1, при 4 конфликтах, 26
коммитах уже-влитой истории и семи нормативных конфликтах К-1..К-6 — «доводка» здесь означает
ровно ту же пересборку, но с грязным графом. **Не ЗАКРЫТЬ:** четыре механизма и чистка живы,
двумя из них прямо датированы OPEN-долги MAJOR (`TD-106`, `TD-105`), и оба RED-набора (320
строк проб с fail-closed стражами) — готовая, дорогая работа architect'а, которую закрытие
выбросило бы.

### 3.1 Что переносится (salvage-список, по файлам)

С ветки, `git show origin/feat/M-60-mechanisms:<путь>`:

1. `research/critiques/C-062-M-60-mechanisms.md` — как есть (аудит-трейл, единственный экземпляр).
2. `scripts/tests/red_context_budgets.sh` — как есть; лимиты фикстур не зависят от протухших чисел.
3. `scripts/tests/red_gate_meta.sh` — с правкой К-3 (без валидации `milestone:` как предмета)
   и дополнением К-4 (сценарий на ОТСУТСТВИЕ вердикта при merge milestone'а — новый GM).
4. `milestones/M-60b-gate-mechanisms.md` — тот же путь, спека переписывается: G2-проводка +
   G1 (лимиты «от факта после чистки») + G3 (с К-3/К-4); обязательства из `C-064`
   (F-064-2 разбор workflow, F-064-4 default-режим, F-064-6 остаточный риск токена) — сохранить.
5. `milestones/M-60c-corpus-cleanup.md` — тот же путь; §3 таблица сохранности и принцип «B+D»
   остаются; §2 перечень резки ЗАМЕНЯЕТСЯ планом аудита §5 (перенос носителя, дедуп, gates.md);
   числа — от факта 13.08.
6. Из `scripts/verify_M-60.sh` — только приём нормализации корпуса (строки 67–72) в новый
   `verify_M-60c.sh`. Зонтичный verify не переносится.
7. Зонт `milestones/M-60-mechanisms.md` — НЕ переносить в рабочем виде (протух К-1/К-2/К-5);
   если founder хочет сохранить таблицу einhard-переноса и запретные списки — одним коммитом в
   `docs/archive/`, вне startup-пути. Находка зонта «28 из 40 проверок `test result: ok` в
   verify-гейтах не считают исполненные тесты» ПОДТВЕРЖДЕНА живой на 13.08
   (`verify_M-40.sh:129` — `grep -qE 'test result: ok'` без счёта) — завести TD, чтобы не
   потерялась с зонтом.

**Выбрасывается:** ветвевые `check_docs_freeze.sh`, `red_docs_freeze.sh`, `verify_M-60a.sh`,
`M-60a-docs-freeze.md`, дельты `gates.md`/`ci.yml`/роадмапа/BACKLOG, `verify_M-60.sh` как целое.

**Судьба ветки:** после приземления salvage-коммитов в `main` — `git push origin --delete
feat/M-60-mechanisms` (норма §9: ветка — носитель предмета на время гейта, не архив). Решение
о самом удалении — согласовать с инвентаризацией застоявшихся веток (параллельный разбор,
`docs/plans/stale-branches-audit-2026-08-13.md`) и с founder'ом, поскольку `SESSION-HANDOFF` §0
держит ветку с пометкой «не мержить»: пометка исполнена — мержить и не нужно.

### 3.2 Во что превращается — три предмета вместо одного трека

Порядок обратный ветвевому (там: механизмы → чистка; здесь чистка идёт ДО фиксации лимитов,
потому что лимиты берутся «от факта после чистки», а факт с 05.08 уехал на +234 строки):

1. **M-60b′ (тот же файл-носитель): G2-проводка отдельным первым коммитом-задачей** — джоб
   `design-claims` + проба в `ci.yml`, той же проводкой, что `protected-artifacts`;
   закрывает `TD-106`/`TD-062`. Дальше в том же milestone G3 (с К-3/К-4) и G1-реализация
   (лимиты проставляются в конце, после M-60c′ — см. зависимость ниже).
2. **M-60c′ (тот же файл-носитель): чистка по аудиту §5** — перенос testing.md:66–185 в
   профиль architect'а, дедуп «782 MB/105 GB», компрессия gates.md §0/§8, снятие мёртвой
   посылки branch-hygiene §Worktree; G4/G5 входят сюда как строки шаблонов §B/§D. Замок §11
   судит каждый коммит (токен), таблица сохранности стережёт нормы, `red_protected_artifacts`
   20/20 — регресс.
3. **G6.2 — не milestone, а задача при ближайшем касании verify-шаблона:** преамбула
   `check_disk_budget.sh` (диск-бюджет + `CARGO_TARGET_DIR` внутри дерева — второе уже
   рекомендовано аудитом §2.6) с RED на оба отказа.

Зависимость: лимиты G1 фиксируются ПОСЛЕ M-60c′ (иначе узаконивают раздутый корпус — спека
сама запрещает «поднимать вместо дренажа»). Порядок диспетчеризации: M-60b′-G2 (день),
M-60c′, затем G1-фиксация и G3 — или G3 параллельно чистке, они не пересекаются по файлам.

### 3.3 Если бы решение было «ЗАКРЫТЬ» — что пришлось бы править (для полноты)

`TD-106` («лечится механизмом G2 спеки M-60»), `TD-105` («следующий шаг — дизайн проверки»),
`TD-102` (цель объёма), строка M-60b/c в `SESSION-HANDOFF` §0, `milestones/M-60a-docs-freeze.md:8,15,353`
(ссылки на зонт и «кандидат в долг для M-60b G2»). Закрытие НЕ рекомендуется — оно оставляет
два MAJOR-долга без спроектированного лечения и выбрасывает готовые RED-наборы.

---

## 4. Приоритет оставшегося — по цене ошибки, числами аудита

Опора: текст в правилах — эффективность 0 (нарушитель 13.08 — автор обоих предыдущих текстов);
механизированное правило — 90 % даже предупреждающим контуром; ядро правил впрыскивается
≈6.3 раза/сутки. Механизм, чей класс уже срабатывал, — выше косметики.

| приоритет | предмет | класс уже срабатывал? | цена бездействия |
|---|---|---|---|
| 1 | **G2: `design-claims` в CI** | да, многократно: `TD-106` — FAIL(6) на чистом `main`; за сутки 05.08 класс «документ врёт о коде» — трижды; сегодня §8 требует прогон, исполняемый только памятью | MAJOR-долг открыт 12 дней при цене фикса ~8 строк YAML — худшее соотношение в списке |
| 2 | **M-60c′: чистка по аудиту §5** | да: откат сжатия 28 % за 10 дней — храповик работает прямо сейчас | −15 KB из КАЖДОГО впрыска (~94 KB/сутки экономии контекста ролей); без неё G1 фиксирует раздутое |
| 3 | **G1: бюджет-барьер в CI** | да: A-003 заводился из-за роста ×2.7; рецидив измерен | без барьера чистка №2 повторит судьбу `9a9f533` — к ~07.09 сотрётся целиком (темп из аудита) |
| 4 | **G3: GATE-META (+ сценарий отсутствия)** | да: `C-062`; `TD-105` (merge без R-NNN, M-32/33/34) | вердикт без привязки к предмету + молчаливый merge — оба класса сегодня держатся только памятью; §9-перепроверки умножили число вердикт-файлов |
| 5 | **G6.2: диск-преамбула verify** | да: 2026-08-01 ложный красный ENOSPC; диск 83–85 % | один ложный красный ≈ круг разбирательства; но частота ниже, чем у 1–4 |
| 6 | G4/G5 строками шаблонов | нет прямых инцидентов | нулевая себестоимость внутри M-60c′; отдельно не делать |

---

## 5. Ограничения этого разбора

- `cargo` не запускался (запрет мандата): стадия «RED падает на отсутствующем барьере»
  для CB/GM-проб принята по чтению кода (страж `[ -f BARRIER ] || die` делает исход
  детерминированным), не прогоном.
- Число «27 сценариев» main-версии `red_docs_freeze.sh` взято из `PROJECT-STATE.md` (M-60a),
  не пересчитано.
- Судьба ветки названа рекомендацией; решение — founder'а, вместе с инвентаризацией
  застоявшихся веток параллельного клона.
