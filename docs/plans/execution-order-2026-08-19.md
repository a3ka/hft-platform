<!-- FACTS: audited_head=6d3bc69071fbf4418ca47089c3f72663ba02898b collected=2026-08-19 -->

# План исполнения после `C-106` — НА СОГЛАСОВАНИЕ КРИТИКУ

**Статус:** PROPOSED. Ни один шаг не начат. Решение founder'а 2026-08-19: план согласуется с
критиком ДО исполнения, чтобы не создать новых проблем с влитыми ветками.

**Предмет:** порядок оставшихся работ и способ устранения `C-106` REJECT.
**Ветка:** `docs/process-package-2026-08-19`, поверх `6d3bc69` (вершина с `C-106`).

---

## §0. Приём вердикта `C-106` — все пять находок признаны, ни одна не оспаривается

| находка | принято | что делаю |
|---|---|---|
| **F-106-1** A2 — обязывающее правило без механизма | ✅ | строю `scripts/check_work_class.sh` + пробу со стабами; **прозу A1–A3 не приземляю раньше механизма** |
| **F-106-2** `process-polish` небезопасно широк | ✅ | сужаю: `milestones/*.md` — только колонка Status и close-out-пруфы; правка Objective / allowed paths / §Tasks / RED / acceptance / фактического решения ⇒ **substantive** |
| **F-106-3** B даёт ложный PASS при отказе `gh` | ✅ | ввожу состояние `unknown` (≠ «PR нет», ≠ `red`), оно инкрементит `FAILED`; стабы обоих отказов + мутанты |
| **F-106-4** мёртвая cross-reference | ✅ | `.claude/rules/binding-requires-mechanism.md` → `docs/workflow/binding-requires-mechanism.md` |
| **F-106-5** §D смешивает выполненное с запрошенным; D5 опровергнут | ✅ | порядок беру из `A-010` §J дословно (см. §2); §D переписываю в «предусловие → действие» |

**Отдельно про F-106-5.** Проверено мной по первоисточнику в `main`: `A-010` §J перечисляет
M-65 (шаг 4) → M-66 (шаг 5) → M-69 (шаг 6) → харнесс (шаг 7); **M-68 в §J не встречается ни
разу** (`grep -c 'M-68'` по §J → 0). Мой прежний порядок был сочинён, а не взят из решения
арбитра, обязательного к исполнению (`gates.md` §0).

---

## §1. Состояние на момент написания — замер, а не память

```
PR #41 (условия R-095 У-1…У-3)  OPEN     ← НЕ влит
origin/salvage/M-59-research-dev-uncommitted  dc646cb   ← жив
origin/feat/M-10-rebased                     51c21dc   ← жив
origin/feat/M-60-mechanisms                  f0e915b   ← жив
PR #6, #28, #29, #32, #33  OPEN
```

Это прямо подтверждает F-106-5: ни одно из D1–D3 не является свершившимся фактом.

---

## §2. Порядок — по `A-010` §J, дословно

| этап | предмет | основание | цепочка гейтов |
|---|---|---|---|
| **0** | закрыть незавершённое: PR #41, затем три удаления | `R-095` У-1…У-3 | чеки → merge → **факт** → удаления |
| **1** | устранение `C-106` (пакет A1–A3 + B + механизм A2) | `C-106` | **критик круг 2** → merge |
| **2** | **M-65** | `A-010` §J шаг 4 | architect (A-1/A-3) → **критик круг 4** → engine-dev → tester → **reviewer** |
| **3** | **M-66** | §J шаг 5 | architect (пересборка) → **критик круг 5** → reviewer |
| **4** | **M-69** | §J шаг 6 | architect (B-6/B-7/B-8) → **критик** → reviewer |
| **5** | харнесс: `I-4`, `E-3/E-4`, `H-1…H-3`, `I-2`, PR #33, PR #28 | §J шаг 7 | автор → адверсарий → merge |
| **6** | **M-68** | **вне §J** — ставлю сам после этапа 5 | architect → критик |
| **7** | правки норм под токен: `A-5`, `C-1/C-2`, `D-3/D-4`, `G-1`, `I-3` | §J шаг 8 | критик на каждую |

`M-68` помечен как МОЁ решение о месте в очереди, а не арбитра — чтобы следующий читатель не
принял его за предписание.

---

## §3. Этап 0 — необратимое, поэтому расписано пошагово

**Инвариант этапа: удаление ref'а происходит ТОЛЬКО после того, как сохранность стала фактом
в `main`, и ТОЛЬКО с предъявлением доказательства ДО команды удаления.**

| шаг | действие | предусловие (проверяется командой) | доказательство ПОСЛЕ |
|---|---|---|---|
| 0.1 | влить PR #41 | `gh pr checks 41` → exit=0 | `git show origin/main:docs/09-roadmap-v2.md \| grep -c 'Процессный трек'` = 1; guard `C-083` помечен в `M-60b`/`M-60c`; в `BACKLOG` и `07-cockpit` нет ссылок на ветки |
| 0.2 | удалить `salvage/M-59-…` | 0.1 выполнен; M-59 влит (`61f452e`), `R-083` APPROVED | SHA `dc646cb` записан в отчёт; `git ls-remote` не содержит ветки |
| 0.3 | удалить `feat/M-10-rebased` | 0.1; архив на месте; md5 дельты ledger'а совпал | SHA `51c21dc` записан; ссылок в `main` нет |
| 0.4 | удалить `feat/M-60-mechanisms` | 0.1; дельта роадмапа в `main` **байт-в-байт** | SHA `f0e915b` записан; guard помечен |
| 0.5 | снять из инвентаря преждевременное «удалена 2026-08-19» | — | `verify_design_claims` exit=0 |

Условие `R-095` («предъявлять ancestry сырым выводом ДО удаления и выдерживать окно для
независимой перепроверки») исполняется так: доказательства печатаются в отчёт, удаление —
отдельным шагом после них, SHA сохраняются, восстановление — `git push origin <sha>:refs/heads/<имя>`.

---

## §4. Этап 1 — устранение `C-106`, состав работ

1. **F-106-4** — правка пути (1 строка).
2. **F-106-3** — в `check_branch_health.sh`: убрать `|| true` у `gh pr list`; различать
   `completed-red` и `unavailable`; `unknown` инкрементит `FAILED` ⇒ exit≠0. В пробе — два
   герметичных стаба (`gh` падает на `list`; `gh` падает на `checks`) и мутанты, доказывающие,
   что каждый сценарий роняет СВОЮ проверку.
3. **F-106-2** — сузить `process-polish` в тексте A2.
4. **F-106-1** — `scripts/check_work_class.sh`: ровно одна декларация `РАБОТА-КЛАСС` в
   диапазоне; пути диапазона не выходят за объявленный класс; отсутствие ⇒ `substantive`.
   Проба: нет декларации · две декларации · ложный класс (объявлен `process-polish`, тронут
   `crates/*/src`) · смешанный · честный. Проводка в `ci.yml`.
5. Круг 2 критика по всему набору.
6. После APPROVE: харнесс (B + `check_work_class.sh`) вливаю как автор; **нормы A1–A3 —
   отдельным коммитом с токеном** `FOUNDER-APPROVED` (разрешение founder'а от 2026-08-19
   «оформляй A1-A3 одним пакетом»).

---

## §5. Что я НЕ делаю и почему

- **Не приземляю прозу A1–A3 до её механизма** — прямое требование F-106-1.
- **Не удаляю ни одного ref'а до merge'а PR #41** — F-106-5.
- **Не мержу PR #28** — `R-095` держит его до закрытия H-11; попадает в этап 5.
- **Не открываю вопросы `M-67`** (граница C: формат L3, топ-50, Storage Box) — `M-67` в
  очереди `A-010` §J отсутствует, до него черёд не дошёл.
- **Не меняю порядок `A-010` §J по своему усмотрению** — он обязателен к исполнению.

---

## §6. Вопросы, на которые прошу ответ критика

1. **Порядок §2** — верно ли прочитан `A-010` §J, и корректно ли, что `M-68` ставится после
   этапа 5 моим решением, а не выводится из §J?
2. **Этап 0** — достаточно ли предусловий, чтобы удаления не создали проблем? Нужно ли окно
   между публикацией доказательств и удалением, и какой длины?
3. **§4 п.2** — достаточно ли двух стабов отказа `gh`, или требуется третий (частичный отказ:
   `list` отработал, `checks` упал на части PR)?
4. **§4 п.4** — достаточен ли объём пробы `check_work_class.sh`, и не порождает ли сам
   классификатор новый класс ложных отказов на легитимных смешанных цепочках?
5. **§4 п.6** — верно ли, что механизм (харнесс) и норма (`.claude/rules/`) должны идти
   РАЗНЫМИ коммитами, и должен ли механизм быть влит РАНЬШЕ нормы?
6. Не пропущено ли в плане что-либо из уже вынесенных вердиктов — `C-104`, `C-101`, `C-098`,
   `C-094`, `C-089`, `R-095`?

---

## Cross-references

- `research/critiques/C-106-process-package-2026-08-19.md` — вердикт, который план устраняет
- `research/arbitration/A-010-nine-disputes-2026-08-18.md` §J — источник порядка (в `main`)
- `research/reviews/R-095-branch-disposition-recheck.md` §У-1…У-6 — условия удалений
- `docs/plans/process-package-2026-08-19.md` — предмет `C-106`
- `docs/plans/branch-inventory-2026-08-19.md` — инвентарь 23 веток
- `docs/workflow/binding-requires-mechanism.md` — требование механизма под обязывающее правило
