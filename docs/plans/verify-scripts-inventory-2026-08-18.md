# Инвентарь 49 acceptance-гейтов `scripts/verify_M-*.sh` (замер 2026-08-18)

**Происхождение.** Разведка выполнена read-only агентом (дешёвая модель) по заданию founder'а
18.08: «прежде чем платить Fable, дать дешёвой модели проверить, что это за 45 гейтов, какую
роль выполняют, надо ли они ещё и что надо, чтобы влить». Источники агента: сами скрипты,
`milestones/M-NN-*.md`, `PROJECT-STATE.md` + `docs/archive/PROJECT-STATE-closed-2026-08-16.md`,
фактические ссылки через `grep -rl`. **Ни один verify-скрипт не запускался.**

**Почему этот файл существует.** Отчёт вернулся в переписку и НИГДЕ не был закреплён. Это тот
самый класс, который закрыт нормой `gates.md` §4 («вердикт, существующий только в переписке,
аудит-трейлом не является»; инцидент `M-49` — два REJECT'а испарились вместе с транскриптом).
Здесь он сохранён в **сжатом** виде: числа, вердикты и поимённые списки рисков перенесены
дословно, полные таблицы по каждому файлу — нет. Это названо явно, а не выдано за полный текст.

---

## 1. Числа

| категория | шт. | строк | что это |
|---|---:|---:|---|
| **архив** — милестоун CLOSED, ссылка только в своём milestone-файле | 12 | 1 020 | `verify_M-04, -07, -17, -20, -22, -23, -24, -29, -30, -32, -33, -41` |
| **оставить** — милестоун открыт | 12 | 1 919 | `verify_M-05, -08, -09, -36, -46, -52, -53, -54, -56, -57, -60b, -60c` |
| **спорно** — CLOSED, но ссылка в живом документе вне milestone-файла | 21 | 3 038 | `verify_M-06, -18, -28, -34, -35, -38a, -38b, -40, -45, -47, -48, -49, -50, -51, -58, -59, -60a, -61, -62, -alerting` |

Сумма: 12 + 12 + 21 = 45 ✓ · 1 020 + 1 919 + 3 038 = **5 977 строк** ✓
(45 из 49; четыре заведены в CI: `verify_contracts`, `verify_ct_rfc_atomic`,
`verify_delivery_M-08`, `verify_design_claims`.)

## 2. Главный вывод — гипотеза `A-010` §G неполна

> «Вывести закрытые в `docs/archive/`» **нельзя одним движением**: 21 из 45 имеют ссылки в
> живых документах, часть — sacred. Чистый перенос создаст битые ссылки.

**Вызовов `bash verify_M-XX.sh` из кода или скриптов НЕТ ни одного** — все упоминания либо в
комментариях крейтов, либо в milestone/RFC. Ломаются только текстовые ссылки, не исполнение.
Это делает риск управляемым, но требует пакетной правки ссылок.

## 3. Что сломается при переносе — поимённо

### Tier-1: sacred / процессные (перенос = правка нормы)

| скрипт | где сломается |
|---|---|
| `verify_M-09.sh` | `docs/DESIGN.md` (sacred) + 5 файлов `crates/ops/**` (комментарии) |
| `verify_M-18.sh` | `docs/rfc/CT-RFC-04-l2delta.md`, `CT-RFC-08-time-axis.md:34` (T1 sacred, конкретная строка) |
| `verify_M-35.sh` | `docs/rfc/CT-RFC-08-time-axis.md:44` (T1 sacred, конкретная строка) |
| `verify_M-45.sh` | `docs/rfc/CT-RFC-06-l2delta.md` (T1 sacred) |
| `verify_M-48.sh` | `deploy/cron.d/journal-retention`, `deploy/bin/journal-retention-cron.sh`, `deploy/bin/gateway-checkpoint-cron.sh` — **работающие на проде** ссылаются на его канарейку композиции |
| `verify_M-61.sh` | `scripts/reserve_artifact_id.sh` (рабочий скрипт, комментарий о герметичности) |

### Tier-2: reviewer-owned живые файлы
`verify_M-09, -28, -34, -46, -52, -53, -57, -60a, -61` → `TECH-DEBT.md` / `PROJECT-STATE.md` /
`milestones/BACKLOG.md`. Правка — зона reviewer, не architect.

### Tier-3: живой milestone ссылается как на регресс-якорь
`verify_M-28` ← `M-46` · `verify_M-38a`/`-38b` ← `M-48`, `M-38-roadmap` ·
`verify_M-40`/`-51`/`-59` ← `M-60b`/`M-60c` (PROPOSED, открыты) ·
`verify_M-46`/`-47` ← `M-53` (открыт) · `verify_alerting` ← `M-60b`, `crates/ops/tests/**`.
Отдельно: `verify_M-38b.sh` упомянут в `crates/gateway/Cargo.toml` — **это зависимость**.

### Tier-4: только doc-комментарии в тестах (не ломает, оставит битые пути)
`verify_M-17, -22, -35, -45, -49, -50, -52, -54, -57, -62` — 10 штук.

## 4. Трогать нельзя — 12 открытых милестоунов

`M-05` (task 5 B1 PENDING) · `M-08` (cron компакции не установлен, §8 нет) ·
`M-09` (ACTIVE, `TD-027` MAJOR OPEN) · `M-36` (`TD-039`) · `M-46` (REJECT close-out `R-025`) ·
`M-52` (`TD-075..078` OPEN) · `M-53` (§8 не GREEN) · `M-54` (FAIL close-out `R-029`) ·
`M-56` (§6 критерий не выполнен, `R-030` §3) · `M-57` (цель не достигнута: пол 19.2 % против
≤5 %) · `M-60b`, `M-60c` (PROPOSED, dev ждёт).

## 5. Побочные наблюдения агента

- **Дублирование CI:** «общие» проверки (`fmt` + `clippy` + `test --all`) есть в 11 скриптах —
  все дублируют CI-джоб `build-test`. Из них архивируемы без потери смысла: `verify_M-59`,
  `-60a`, `-61`. После закрытия милестоуна их полезность — 0.
- **`docker-compose`-канарейки** (реальный `grep`, не комментарий): `verify_M-38b`, `-47`,
  `-58`. Первые две — регресс-якоря для открытых предметов; **`verify_M-58` — единственная
  полезная после закрытия**, кандидат на «оставить и завести в CI» (дефолт `GATEWAY_BANDS` не
  должен протухать молча).
- **Тяжеловесы (≥300 строк):** `verify_M-61` (501), `-60b` (399), `-60c` (316), `-52` (308).
  Три из четырёх открыты; единственный архивируемый — `verify_M-61`, и тот «спорный».

## 6. Что из этого следует для плана

Начинать с **12 безусловных** (1 020 строк уходит из зоны `gates.md` §9, ноль битых ссылок).
21 спорный — отдельным движением с пакетной правкой ссылок; Tier-1 (sacred T1-документы и
прод-скрипты) не трогать без отдельного решения.
