<!-- FACTS: audited_head=f6e9337 collected=2026-08-20 -->

# План A → B → C: заморозка, чистка, возврат на продуктовый трек

**Автор:** architect · **Дата:** 2026-08-20 · **База:** `origin/main` @ `f6e9337`
**Основание:** решения founder'а 2026-08-20 (раздел §1).

## §0. Что в этом документе судится, а что НЕТ — читать первым

`A-011` §1.2 (решение арбитра, BINDING): **«критику предъявляется не РАСПИСАНИЕ, а КАЖДЫЙ
НЕОБРАТИМЫЙ ШАГ в момент исполнения, со своим доказательством»**; §6.2 п.3 велел расформировать
«план работ» как предмет гейта — расписание устаревает быстрее круга, и именно оно дало три
несошедшихся круга 19.08.

Founder 20.08 попросил провести этот план через критика. Противоречия нет ровно в той мере,
в какой предъявляется **не очерёдность**, а следующее:

| # | предмет гейта | почему судимо |
|---|---|---|
| **P-1** | замер §4.1 «источник НЕ обрезан на 1.3 %» | утверждение О КОДЕ, проверяется исполнением |
| **P-2** | вывод §4.2: обоснование `M-68` ложно ⇒ предмет переопределяется | отменяет принятую веху |
| **P-3** | список необратимых удалений §6.2 | **необратимый шаг** — ровно то, что §1.2 требует предъявлять |
| **P-4** | правка нормы «владелец merge» §6.5 | зона §11, меняет ФОРМУ ⇒ критик по §9 обязателен |
| **P-5** | утверждение §3.1 «M-65 нужен продукту» | обосновывает трату кругов гейта |

**НЕ судится:** порядок полос A→B→C, оценки сроков, распределение по сессиям. Это рабочая
записка автора. Вердикт по расписанию не выносится (`A-011` §6.2 п.3).

---

## §1. Решения founder'а 2026-08-20 (данность, не предмет спора)

| № | решение |
|---|---|
| **A1** | offsite-копия журнала + restore-drill — **отложено** (НЕ отменено) |
| **A2** | заморозка харнесса — только на НОВОЕ; с уже написанным разобраться |
| **A3** | мораторий на новые нормативные единицы во впрыскиваемом ядре |
| **B** | вычистить ветки; план чистки — через критика |
| **B-owner** | **владелец merge — architect**, после тестов, критиков и ревьюеров |
| **C** | перепроверить решение «не обрезает» и зафиксировать; доказывать сигналы |
| **M-65** | сперва проверить нужность; если нужен — снять REJECT, дождаться подтверждений, потом merge |
| **охват C** | прицельный по предмету глубины (не полный аудит FA) |

Требование founder'а: решения обязаны быть видны architect'у СЛЕДУЮЩЕЙ сессии.

---

## §2. Замер, из-за которого план существует

`origin/main`, 14 суток (команды — §9 Done Block):

| величина | значение |
|---|---|
| коммитов в `main` | 433 |
| тронули `crates/*/src` | **14**; уникальных исходных файлов — **4** |
| новых артефактов гейта | **80** — 5.7 вердикта на один кодовый коммит |
| повторные круги | 43 из 80 (54 %); REJECT / APPROVE = 19 / 4 |
| последний продуктовый merge | **15.08** |
| `gates.md` | 31 804 → 47 883 B (+50.6 %), запас **17 байт** |
| корпус вердиктов | 4.42 MB = **×3.2 от всего продуктового исходника** |
| невлитых веток | 24; `M-66` на 4 ветках; 2 зелёных PR не влиты |

Прод `a9f0bf5`, healthy, журнал 50 G / 342 сегмента / ~27 M событий; `/mnt/journal-cold` —
**0 файлов** (ворота Ф0 не пройдены, `TD-020`).

---

## §3. Находка 1 — «много кода на многих ветках» НЕ подтвердилось

Замер от merge-base (что изменила САМА ветка, а не что уехало в `main`):
`crates/*/src` трогают **три** ветки из 24; остальные — харнесс (8), RED-оракулы (6),
документы (4), пусто-влито (3).

### 3.1 M-65 — единственный живой продуктовый предмет; нужен ли: **ДА** (предмет P-5)

```
crates/gateway-serve/src/lib.rs      +1132 -8   · src/wire_v1.rs  +197
crates/gateway-serve/src/session.rs   +106      · src/test_sync.rs +174   ⇒ 1609 строк
tests/red_ws_session.rs +1381 · red_ws_session_battery.sh +549 · verify_M-65.sh +182
+ 7 вердиктов гейта (C-077, C-078, C-098, R-057, R-075, R-080, R-086, R-093)
```

`milestones/M-65-ws-session.md` §1: селектор читается из окружения процесса, клиентские
сообщения **выбрасываются** (`crates/gateway-serve/src/lib.rs:507-510`, «MVP: replay-only»)
⇒ пользователь не может переключить символ или таймфрейм ⇒ **терминала не существует как
продукта**. Две подписи founder'а по границе C получены 11.08. Ветка ответвлена от `a9f0bf5`
— ровно того SHA, что крутится на проде.

---

## §4. Находка 2 — узел C: «не обрезает» ПОДТВЕРЖДЕНО замером

### 4.1 Замер по коду (предмет P-1)

| факт | источник |
|---|---|
| пейлоад `L2Snapshot` строит `venue-binance` **из своей diff-книги**: «сжат в 0.02 %-бакеты, **обрезан на ±60 % от mid**» | `crates/venue-binance/src/lib.rs:359-360` |
| вход — `bucket_levels(book.bids…)`, **не** ответ REST | `crates/venue-binance/src/lib.rs:398` |
| `MAX_REL_DIST = 0.60`; `REST_DEPTH_LIMIT = "5000"` — бутстрап и gap-resync | `crates/venue-binance/src/lib.rs:27,33` |
| `depth_within` вызывается **ровно один раз**, в ветке `L2Snapshot` | `crates/gateway/src/lib.rs:929` |
| ветка `L2Delta`: «depth_series (полосы) **НЕ апдейтится** — snapshot-only» | `crates/gateway/src/lib.rs:938-940` |
| метка `validated<=1.3%` вешается при `band_pct_e8 > 1_300_000` — она про **валидацию**, не про наличие данных | `crates/gateway/src/lib.rs:1035-1036` |

**Вывод:** источник НЕ обрезан на 1.3 %. Реальный дефект — **КАДЕНЦИЯ**: полосы
пересчитываются только на снапшоте (≈1 Гц) против дельт 100 мс. Совпадает с амендментом
`П-011` (перепроверен `R-090`/`R-091`) — то есть «решение и проверка», о которых помнит
founder, существуют и держатся.

### 4.2 Следствие: обоснование `M-68` ложно (предмет P-2)

`M-68` заявляет, что полосы читают «снапшот, обрезанный биржей на `limit=5000` ≈ 1.3 %».
Замером опровергнуто. Предмет подлежит **переопределению на каденцию** либо растворению в
исполнении `П-014` (§7.2). `C-094` REJECT (три блокера, главный — нет `scripts/verify_M-68.sh`)
закрывается тем же движением.

### 4.3 `П-014` ПОДПИСАНО 17.08 и НЕ ИСПОЛНЕНО

Требует четырёх вещей: (1) метка достоверности **по сторонам** (сегодня только по глубине);
(2) каденция названа честно; (3) bump `GATEWAY_SCHEMA_VERSION`; (4) канонический
`GATEWAY_BANDS`. Замер прода: `GATEWAY_BANDS=0.001` — **одна полоса 0.1 %**.

---

## §5. Ограничение, диктующее КУДА писать нормы

`bash scripts/check_context_budgets.sh` (джоб в агрегате):

```
gates.md            47883 / 47900   запас     17 B   ← новая норма туда не влезает
commit-discipline    8579 /  9000   запас    421 B
CLAUDE.md            8874 /  9100   запас    226 B
ИТОГО              111974 / 114900  запас   2926 B
```

Правки формулируются в `commit-discipline.md` и в носителях ВНЕ ядра; `gates.md` не трогаем.

---

## §6. Полосы A и B

### 6.1 A — заморозка и фиксация решений

**A-1.** Записать решения §1 в `docs/PENDING-SIGNATURE.md` новой записью `П-NNN` (номер —
`scripts/reserve_artifact_id.sh`). Обоснование выбора носителя: его **индекс**
(`grep -n '^## П-'`) входит в ярус A startup-протокола (`docs/workflow/reading-map.md` §2),
он вне впрыскиваемого ядра (бюджет §5 не тратится) и уже является каноническим носителем
решений границы C. Приземляет architect своим PR (§9 — изложение).

**A-2.** Заморозить `verdict-gate` (круг 2); развилку `supersedes:` не открывать.

**A-3.** Разрезать `feat/harness-verdict-gate`: влить ТОЛЬКО `check_branch_health.sh` +
`red_branch_health.sh` + джоб `branch-health` (вне агрегата). Наблюдатель зелёный (22
сценария, 4 мутанта) и висит лишь потому, что делит ветку с отвергнутым барьером — класс,
названный `A-011` §1.1. Это осознанное исключение из A2: работа сделана и проверена.

### 6.2 B — чистка веток (предмет P-3, НЕОБРАТИМО)

Условие удаления — **артефакт полноты** (`A-011` §4в), не срок. Уникальный груз считается
**ДВУХточечным** диффом: трёхточечный с `--diff-filter=A` даёт неверный ответ (`A-011` §1.8).

Кандидаты, **проверенные лично** (команды в §9):

```
docs/branch-disposition-2026-08-19   ahead=0  предок main: ДА   (PR #41 MERGED)
docs/founder-decisions-2026-08-17    ahead=0  предок main: ДА   (PR #19 MERGED)
fix/premature-deletion-claims        ahead=0  предок main: ДА   (PR #43 MERGED)
critic/C-110|C-111|C-112             строгий предок feat/harness-verdict-gate: ДА
feat/M-66, feat/M-66-fix-td097       строгий предок feat/M-66-fixture: ДА
feat/M-10-rebased                    груз в docs/archive/M-10-obi-killscreen-retired-2026-07/
salvage/M-59-research-dev-uncommitted  вытеснен влитым M-59
```

**Ни один ref не удаляется до вердикта критика.** Открытый остаток по
`feat/M-60-mechanisms` — 19 строк `milestones/BACKLOG.md` (`A-011` §1.8), они обязаны быть
перенесены или поимённо объявлены выброшенными ДО удаления.

**Названный предел:** `scripts/check_protected_artifacts.sh` защищает артефакты от удаления
КОММИТОМ; удаление **ref'а** не покрыто барьером вовсе (`A-011` §4). Механизма нет, и он здесь
не изображается.

### 6.3 B — M-65: довести, не нарушая воркфлоу

Порядок ровно по решению founder'а: снять `C-098` (круг 4) → пересобрать от свежего `main` →
цепочка подтверждает (dev → tester → reviewer) → **только потом** merge. Зона
`crates/gateway-serve/**`; risk-critic не требуется (не order-path, `gates.md` §5 carve-out).

### 6.4 B — PR #6 закрыть и пересобрать от `main`

Решение `A-010` §B принято и не исполнено 5 суток; ветка CONFLICTING, 0 чеков, 206 позади —
требуемый зелёный чек недостижим ни при каком действии, кроме пересборки.

### 6.5 B — владелец merge = architect (предмет P-4)

Правка `commit-discipline.md` пп.4/5 (запас 421 B): architect приземляет после того, как
тесты, критик и reviewer дали своё. Сегодня действует иное: `commit-discipline.md:89`
«именно **reviewer** вливает работу после APPROVED», `gates.md:313` «feat-ветку **reviewer**
мержит». Зона §11 ⇒ токен `FOUNDER-APPROVED: <причина>` в теле коммита + критик по §9.

**Механизм под норму** (`binding-requires-mechanism.md` — иначе прозу писать нельзя):
строка в Handoff §D `Приземление: ✅ влито <SHA> | ⏸ передано <роль> | ⚠ держит <gate-id>`;
`handoff-block.md` п.4 уже требует push-статус, добавляется четвёртое значение.
Механического барьера нет ⇒ секция помечается **`COGNITIVE-ONLY`** честно.

---

## §7. Полоса C — возврат на продукт

**C-1.** Зафиксировать перепроверку §4 записью в `docs/PENDING-SIGNATURE.md` + правкой
обоснования `M-68`. Прицельное чтение (охват по решению founder'а): `docs/fa/viz-backend.md`
(§4 + `VB-I-*`), `docs/fa/book.md`, `docs/fa/venues.md`, `research/arbitration/A-002-*`,
`research/data-quality/depth-verdict.md`, `П-011`/`П-014`. Плюс замер на живом проде:
расширенный `GATEWAY_BANDS` на тестовой сессии — показать числами, что полосы глубже 1.3 %
не пусты, а **отстают**.

**C-2. Исполнить `П-014` — ближайшая продуктовая веха.** Четыре требования §4.3.
Зона `crates/gateway/src/**` (engine-dev) + `deploy/**`. RED-оракулы пишет architect. Живые
инварианты `VB-I-2` (live == replay), `VB-I-5` (честность провенанса), `VB-I-10` (bounded
window) — не ослаблять.

**C-3.** `M-68` переобосновать (каденция) либо растворить в C-2.

**C-4. Первый доказанный сигнал на 50 G.** Гипотеза → `research/hypotheses/H-*` → time-split
→ trials-ledger → отчёт → анти-оверфит-гейт `gates.md` §6 → risk-critic. Сегодня
`research/registry/` пуст, продуктовых отчётов ноль, данные копятся пятую неделю.

---

## §8. Чего план НЕ делает — названо явно

- не строит новых барьеров (`verdict-gate` заморожен);
- не трогает `gates.md` (17 байт запаса);
- не удаляет ни одного ref'а до вердикта критика;
- **не делает A1** — отложено founder'ом; риск остаётся: 50 G в одной копии, ворота Ф0 не
  пройдены. Возврат к нему после B;
- не читает весь корпус FA — охват C прицельный; полный аудит FA остаётся отдельным предметом.

---

## §9. Done Block — команды, которыми получены числа

```text
$ git log origin/main --since='7 days ago' --format='%H' | wc -l
289
$ git log origin/main --since='7 days ago' --format='%H' -- 'crates/*/src/**' | wc -l
7
$ git log origin/main -1 --format='%h %ci' -- 'crates/*/src/**'
61f452e 2026-08-15 09:24:30 +0000

$ for b in $(git ls-remote --heads origin | awk '{print $2}' | sed 's#refs/heads/##' | grep -v '^main$'); do
    mb=$(git merge-base origin/main "origin/$b")
    git diff --name-only "$mb" "origin/$b" | grep -c '^crates/.*/src/'
  done
# ненулевые: fix/M-65-battery-recalibration=4, feat/M-10-rebased=5, salvage/M-59=1; остальные 0

$ git diff --numstat $(git merge-base origin/main origin/fix/M-65-battery-recalibration) \
    origin/fix/M-65-battery-recalibration | awk '$3 ~ /src\//'
1132  8  crates/gateway-serve/src/lib.rs
106   0  crates/gateway-serve/src/session.rs
174   0  crates/gateway-serve/src/test_sync.rs
197   0  crates/gateway-serve/src/wire_v1.rs

$ grep -nE 'REST_DEPTH_LIMIT|MAX_REL_DIST' crates/venue-binance/src/lib.rs
27:const REST_DEPTH_LIMIT: &str = "5000";
33:const MAX_REL_DIST: f64 = 0.60;
$ sed -n '359,360p' crates/venue-binance/src/lib.rs
/// Чистое вычисление `L2Snapshot`-эффектов из states (без I/O): полный стакан сжат в
/// 0.02%-от-mid бакеты, обрезан на ±60% от mid; …
$ grep -n 'depth_within' crates/gateway/src/lib.rs
929:  .insert(time_s, depth_within(bids, asks, row.side, row.band));
1241: fn depth_within(bids: &[Level], asks: &[Level], side: Side, band: f64) -> i64 {
$ sed -n '938,940p' crates/gateway/src/lib.rs
// … depth_series (полосы) НЕ апдейтится — депт-серия остаётся snapshot-only (M-22 семантика).

$ ssh … 'grep -n GATEWAY_BANDS /root/hft-platform/docker-compose.yml'
134:      GATEWAY_BANDS: ${GATEWAY_BANDS:-0.001}

$ bash scripts/check_context_budgets.sh | tail -3
OK    CLAUDE.md   8874 B / 9100 B (запас 226 B)
VERDICT: PASS — 7 файлов, 111974 B из 114900 B бюджета (запас 2926 B)
# gates.md: 47883 B / 47900 B — запас 17 B

$ for b in docs/branch-disposition-2026-08-19 docs/founder-decisions-2026-08-17 \
           fix/premature-deletion-claims; do
    git rev-list --count origin/main..origin/$b; git merge-base --is-ancestor origin/$b origin/main; echo $?
  done
0 / 0 · 0 / 0 · 0 / 0          # ahead=0 и предок main у всех трёх

$ git merge-base --is-ancestor origin/critic/C-110-verdict-gate origin/feat/harness-verdict-gate; echo $?
0                              # то же для C-111, C-112, feat/M-66, feat/M-66-fix-td097

$ ssh … 'ls /mnt/journal-cold | wc -l'
0
```
