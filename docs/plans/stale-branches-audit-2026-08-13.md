# Аудит застоявшихся веток — 2026-08-13

**Автор:** architect-клон (свежий контекст, read-only разведка; ничего не менялось, не
коммитилось, не пушилось). **Место замера:** worktree `--detach origin/main` @ `0f907d1`
(`docs(TD): TD-129 …`). Сводная таблица рекомендаций — §7 (founder читает её первой).

Метод: `git rev-list --left-right --count origin/main...origin/<b>` (отставание/уникальные
коммиты) · `git log origin/main..origin/<b>` (состав) · `git diff --stat
origin/main...origin/<b>` (файлы от merge-base) · `git merge-tree --write-tree --name-only
origin/main origin/<b>` (конфликты числом) · `git apply --check <patch>` (применимость
сохранённых патчей). `cargo` не запускался (вне зоны аудита) — всё, что требует компиляции,
помечено «не установлено».

---

## §1. `feat/M-64-export-contract` — контракт экспорта бэк↔фронт

**Замер:**
```
$ git rev-list --left-right --count origin/main...origin/feat/M-64-export-contract
90      11        # main ушёл на 90, на ветке 11 уникальных
$ git log -1 --format='%ci %h' origin/feat/M-64-export-contract
2026-08-11 12:27:21 +0000 93bfb34
$ git merge-tree --write-tree --name-only origin/main origin/feat/M-64-export-contract; echo exit=$?
exit=0            # НОЛЬ конфликтующих файлов
```

**Что лежит.** Чисто документная ветка, кода НЕТ:
```
milestones/M-64-export-contract.md         | 358 +
research/critiques/C-074-M-64.md           | 165 +
research/critiques/C-075-M-64-rev3.md      | 296 +
research/critiques/C-076-M-64-rev3-b1m1.md | 156 +
4 files changed, 975 insertions(+)
```
Спека milestone'а (rev3) + три вердикта критика. История кругов: `C-074` REJECT (rev1) →
rev2 → `C-075` REJECT (F1/F2/F3 закрыты, выкатил B1+M1) → правки `6d68697`/`20f9267` →
**`C-076` (круг 3, узкий B1/M1): «Вердикт: NOTE… B1/M1 closed»** → advisory-фикс `93bfb34`.

**Почему встала.** Не гейтом — решением founder'а. `docs/PENDING-SIGNATURE.md` П-012:
«2026-08-11 вы решили: **сперва миграция форм экспорта в `crates/contracts` (`К1`/`К6b`,
приоритет P0 в `docs/plans/plan-design-migration.md`), потом гейт M-64** — "чтобы гейт
строился сразу на конечном месте". Статус M-64 переведён в `BLOCKED`». Гейтовая очередь при
этом ПРОЙДЕНА дальше, чем записано в индексе: `SESSION-HANDOFF` §0 говорит «Ждёт УЗКОГО
круга 3» и «@ `20f9267`», а узкий круг 3 (`C-076`, NOTE) уже на ветке, tip `93bfb34` —
индекс §0 протух (см. §8.1).

**Актуальна ли.** ДА, напрямую. Экспорт-контракт — «кросс-языковая граница №1 SaaS»
(`plan-design-migration.md` К1, приоритет **P0**); это путь Ф2/Ф4 роадмапа. Отдельно: ветка —
**единственный носитель полной записи решения founder'а от 11.08** — П-012 прямо фиксирует:
«Развёрнуто оно изложено в `milestones/M-64-export-contract.md` §2.1, а этот файл существует
**только на ветке** … и в `main` его НЕТ». Удаление ветки уничтожит запись решения.

**Слияемость.** 0 конфликтующих файлов (merge-tree exit=0). Физически вливается в любой момент.

**Рекомендация: ДОВЕСТИ** — в порядке, который founder уже назначил. Кого/чего не хватает:
1. **architect** — спека миграции `К1`/`К6b` (CT-RFC-06 по `plan-design-migration.md` Б2.4);
   П-012: «миграция своей спеки пока не имеет» — это и есть недостающий первый шаг;
2. **founder** — ответ на П-012 (класс строгости T1 vs T-designate, варианты (а)/(б)/(в)) —
   нужен «к моменту, когда миграция `К1`/`К6b` дойдёт до спеки», не сейчас;
3. после миграции — узкое обновление rev3 на конечное место и merge через reviewer.
Ветку не трогать до тех пор; она дешёвая (975 строк докс, 0 конфликтов).

---

## §2. `feat/M-59-lifetime-memory` — граница памяти per-life анализатора

**Замер:**
```
$ git rev-list --left-right --count origin/main...origin/feat/M-59-lifetime-memory
174     7
$ git log -1 --format='%ci %h' origin/feat/M-59-lifetime-memory
2026-08-06 20:54:39 +0000 e0e56a3
$ git merge-tree … ; echo exit=$?   → exit=0   # 0 конфликтов
```

**Что лежит.** Спека + RED + реализация + вердикт reviewer'а — полный milestone-набор
(1059+/92−): `milestones/M-59-lifetime-memory.md` · RED-оракулы
`crates/research-cli/tests/red_lifetime_memory_bounded.rs` (DV-I-15, 228 строк) и
`red_life_before_attribution.rs` (DV-I-16, 123 строки) · impl-фикс
`crates/research-cli/src/depth_lifetime.rs` (research-dev, TD-107/TD-108) ·
`scripts/verify_M-59.sh` (146 строк) · `research/reviews/R-038-M-59.md` (320 строк).

**Почему встала.** `R-038-M-59`: «**Вердикт: REJECT (CHANGES REQUESTED)**» с двумя
измеренными блокерами — F-1 «`TD-107` НЕ закрыт: на пути без mid память по-прежнему
O(жизней), **×4.00**» и F-2 «молчаливая потеря завершённой жизни; §Forbidden милестоуна
нарушен». Architect ответил на оба (`6c1dfe4` RED DV-I-16 на F-2; `e0e56a3` «решение по F-1 —
граница НАЗВАНА»), research-dev написал фикс, но НЕ закоммитил — он спасён отдельно
(`dc646cb`, §3). Текущий статус по `SESSION-HANDOFF` §0: «**BLOCKED.** Фикс написан, но не
принят: Done Block отсутствует, прогонов после поломки среды не было». Что за «поломка
среды» — в стартовых доках не расшифровано (не установлено).

**Актуальна ли.** ДА. Оба долга-основания открыты в `main` сегодня: `TECH-DEBT.md` —
**TD-107 OPEN, Severity MAJOR** («650 372 жизни за 3.4 часа ОДНОГО символа»; «Спека фикса
готова: `milestones/M-59-lifetime-memory.md`») и **TD-108 OPEN, MINOR** («Уместно забрать в
`M-59`»). Инструмент `depth_lifetime` — доказательная база под founder-решением П-011
(полосы глубже 1.3 %, замок `A-002` остаётся): будущие пересъёмки требуют bounded-версию.
Срочность низкая (офлайн-инструмент, не прод-путь), актуальность — полная.

**Слияемость.** 0 конфликтов; `main` с 2026-08-05 файл `depth_lifetime.rs` не трогал
(последний коммит по нему в main — `4cea090` M-58). Компилируемость поверх свежего main —
не установлено (cargo вне зоны).

**Рекомендация: ДОВЕСТИ** — вместе с §3 как ОДНА работа. Осталось: (1) research-dev/tester —
перенести `dc646cb` в рабочую ветку, прогнать `verify_M-59.sh` + DV-I-15/16 + регресс
DV-I-1..14 на чистом чекауте, Done Block; (2) reviewer — круг 2 после REJECT `R-038`.
Кого не хватает: **tester + reviewer** (спека и RED уже есть, фикс написан).

---

## §3. `salvage/M-59-research-dev-uncommitted` — спасённый фикс к §2

**Замер:**
```
$ git merge-base --is-ancestor origin/feat/M-59-lifetime-memory origin/salvage/M-59-research-dev-uncommitted && echo YES
YES               # salvage = feat/M-59 + ровно один коммит
$ git diff --stat origin/feat/M-59-lifetime-memory origin/salvage/M-59-research-dev-uncommitted
 crates/research-cli/src/depth_lifetime.rs | 177 ++++---  (121+/56-)
$ git merge-tree … ; echo exit=$?   → exit=0
```

**Что лежит.** Строгое надмножество §2: те же 7 коммитов плюс `dc646cb` «salvage(M-59):
сохранение НЕЗАКОММИЧЕННОЙ работы research-dev» — свежайшая версия фикса `depth_lifetime.rs`
(предположительно ответ на F-1/F-2; гейтом не проверялась — не установлено).

**Почему встала.** Это не рабочая ветка, а сейф: architect 2026-08-09 спас незакоммиченную
работу research-dev из брошенного worktree. `SESSION-HANDOFF`: «Salvage-ветки в `origin`
(не мержить, держать до решения)».

**Актуальна ли.** ДА — ровно настолько, насколько актуален §2: это единственный носитель
последней редакции фикса.

**Слияемость.** 0 конфликтов.

**Рекомендация: ДОВЕСТИ** (в связке с §2). Технически: работу продолжать ОТ этой ветки (она
надмножество), либо перенести `dc646cb` на `feat/M-59-lifetime-memory`. После merge M-59 в
`main` обе ветки (§2 и §3) удаляются одним действием — держать две копии одной работы
незачем.

---

## §4. `feat/M-10-rebased` — kill-screen OBI, отчёт R-001 (Track A)

**Замер:**
```
$ git rev-list --left-right --count origin/main...origin/feat/M-10-rebased
930     12        # merge-base 0cd447d от 2026-07-21
$ git log -1 --format='%ci %h' origin/feat/M-10-rebased
2026-07-22 09:17:10 +0000 51c21dc
$ git merge-tree --write-tree --name-only origin/main origin/feat/M-10-rebased; echo exit=$?
exit=0            # НОЛЬ текстовых конфликтов — вопреки ожиданию «930 позади = переписывание»
```

**Что лежит** (2547+/35−, 17 файлов) — ПОЛНЫЙ цикл исследования, с кодом и артефактами:
- **код** (research-cli): kill-screen classifier + R-001 runner (`report.rs` +556,
  `red_killscreen.rs` 333, `red_stack_honesty.rs` 122, правки grid/ledger/main/types);
- **артефакты гейтов**: `C-019` (REJECT → rev2 note) · `C-020` (risk-critic: «CONCERNS —
  OBI Track A KILL accepted; KS-I-1 se_sharpe defeated») · `D-001-obi-trackA-kill.md`
  (KILL-черновик решения);
- **результат исследования**: `research/reports/R-001-obi-trackA.{md,json}` (реальный
  own-capture прогон) · `research/data-quality/gaps-own-2026-07.json`;
- **276 записей `research/trials-ledger.jsonl`** — замер: в merge-base 4 строки, в
  сегодняшнем `main` ПО-ПРЕЖНЕМУ 4 строки, на ветке 280. Append-only ledger (`INTG-I-6`)
  всех grid-трейлов OBI живёт ТОЛЬКО здесь.

**Почему встала.** Двухслойно. (1) Гейт: `C-020` — вердикт KILL по сигналу ПРИНЯТ (это
результат, не провал), но merge заблокирован дефектом самого механизма: «KS-I-1 (ядро M-10)
фактически защиты не даёт: ложный PASS достижим на коротком окне» (цитата по
`SESSION-HANDOFF` §9). (2) Фаза: пивот AlphaQuant (2026-07-29) отложил весь квант-путь.

**Актуальна ли.** **ПО ФАЗЕ — НЕТ.** `DESIGN.md` §10 «DEFERRED, не отменено»: execution
(risk/killswitch/oms, live-ордера) — после GA отдельным founder-решением;
`09-roadmap-v2.md` §DEFERRED: «**Quant-в-продукте** … ПЕРВЫЙ кандидат после GA»,
«Research swarms / marketplace … после GA». Kill-screen для промоушена сигналов — часть
именно этого отложенного конвейера; в фазах Ф0–Ф6 у него потребителя нет. НО: ветка несёт
невосполнимое — результат KILL-исследования (ledger-записи, отчёт на реальных данных,
подписной черновик D-001), а `main` держит висячую открытую задачу: `PROJECT-STATE.md:552`
«**ОТКРЫТО (задача 8 M-04):** прогон OBI Трек A/B → `research/reports/R-001*` гейтится» —
самого `R-001` в `main` нет. `SESSION-HANDOFF` §0: «Удержано намеренно, ждёт решения
founder'а».

**Слияемость.** Текстово — чистая (0 конфликтов; уцелела потому, что почти всё — НОВЫЕ
файлы, а 6 коммитов main по `crates/research-cli/src/` за эти 930 легли в другие модули).
Семантически — не установлено: код ветки на 22 дня старше M-32/M-33/M-51/M-58-правок
research-cli; компиляция поверх main не проверялась (cargo вне зоны). Вливать КОД в любом
случае нельзя — у него известный дефект KS-I-1 и RED-набор без зелёной реализации
(`gates.md` §8: RED не живёт в main).

**Рекомендация: ЖДАТЬ РЕШЕНИЯ FOUNDER'А** (статус-кво и есть его прежняя воля). Точный
вопрос: **«Нужен ли результат KILL-исследования OBI Track A в `main` сейчас — или ветка
ждёт execution-фазы целиком?»** Что меняется от ответа:
- **«нужен след»** → узкий docs-only merge артефактов (R-001, D-001, C-019/C-020,
  gaps-json, 276 ledger-записей) БЕЗ кода kill-screen; код остаётся на ветке до
  execution-фазы; reviewer закрывает `PROJECT-STATE.md:552` ссылкой на D-001 KILL. Ledger
  при этом получает свою историю трейлов, deflated-Sharpe семейства OBI перестаёт стоять
  на N=4;
- **«ждёт целиком»** → ветка замораживается как архив (ничего не делать) либо патч в
  `/home/nous/salvage-…` + удаление; тогда reviewer обязан пометить `PROJECT-STATE.md:552`
  как DEFERRED-по-фазе, иначе «ОТКРЫТО» продолжит врать.

---

## §5. `feat/M-60-mechanisms` — стоянка спек M-60b/M-60c

**Замер:**
```
$ git rev-list --left-right --count origin/main...origin/feat/M-60-mechanisms
163     34
$ git log -1 --format='%ci %h' origin/feat/M-60-mechanisms
2026-08-07 14:53:25 +0000 f0e915b
$ git merge-tree --write-tree --name-only origin/main origin/feat/M-60-mechanisms; echo exit=$?
exit=1
.claude/rules/gates.md
.github/workflows/ci.yml
milestones/M-60a-docs-freeze.md
scripts/verify_M-60a.sh          # 4 конфликтующих файла
```

**Что лежит** (5316+/2−, 23 файла). Пофайловая сверка с `main` даёт три класса:
- **IDENTICAL-in-main (10 файлов)** — весь M-60a уехал в `main` карвом (`R-041` F-1, merge
  `71611d0`, замок действует): `check_docs_freeze.sh`, `red_docs_freeze.sh`,
  `verify_M-60a.sh` (blob идентичен, конфликт — только mode 644↔755), `A-005`,
  `C-064`…`C-068`, `R-041`, `R-042`;
- **ONLY-on-branch (8 файлов) — уникальная ценность ветки:** зонт
  `milestones/M-60-mechanisms.md` (337) · `M-60b-gate-mechanisms.md` (73: механизмы G1
  бюджет чтения / G2 `verify_design_claims` в CI / G3 GATE-META+subject-lock) ·
  `M-60c-corpus-cleanup.md` (81: корпус 949→~725 строк к цели `A-003`) · RED-наборы
  `red_context_budgets.sh` (107) · `red_gate_meta.sh` (213) · `verify_M-60.sh` (151) ·
  `C-062` · черновая ревизия зонта;
- **DIFFERS (4)** — старые версии `gates.md`/`ci.yml`/BACKLOG/roadmap, в `main` уже новее.

**Почему встала.** Намеренная стоянка, не тупик. `SESSION-HANDOFF` §0: «**M-60b / M-60c** …
**не начаты, стоянка.** Зонтичный гейт красный по построению: их RED-наборы без реализации |
спеки на ветке | `feat/M-60-mechanisms` (не мержить)». Сам M-60a внутри ветки объясняет
(шапка на `main`): «части `M-60b`/`M-60c` вместе с их RED-наборами … войдут в `main` СВОИМИ
milestone'ами, когда станут зелёными: `gates.md` §8 запрещает нереализованный RED в `main`».

**Актуальна ли.** ДА — `main` на неё опирается: `TECH-DEBT.md` TD-106 («документ врёт о
коде» ловится только руками) — «Лечится механизмом `G2` спеки M-60; до его merge'а долг
**OPEN**»; G3 закрывает TD-105. Предусловие M-60b («M-60a смержен») с 2026-08-09 ВЫПОЛНЕНО —
ветка готова к возобновлению, ждёт только места в очереди.

**Слияемость.** 4 конфликтующих файла, все четыре — тот самый уже-влитый M-60a-слой
(разрешение тривиально: в пользу `main`). Правильная механика возобновления — не merge всей
ветки, а карв M-60b своей веткой от свежего `main` (по образцу M-60a), забрав 8
ONLY-on-branch файлов.

**Рекомендация: ДОВЕСТИ** (когда встанет в очередь). Кого не хватает: **постановки в
очередь** (architect предлагает, founder приоритизирует против M-62/M-63/миграции К1) +
**engine-dev** на реализацию G1/G2/G3 (`check_context_budgets.sh`, `check_gate_meta.sh`,
проводка `verify_design_claims` в CI). До того — не удалять и не мержить: стоянка легальна
по построению.

---

## §6. Удалённые сегодня ветки и висячая ссылка TD-016 → M-31

Патчи сохранены: `/home/nous/salvage-2026-08-13/deleted-branches/` (4 файла, из них два по
предмету этого раздела: `feat-M-31-book-eviction.patch` 21 125 B, `feat-M-10-r001-obi.patch`
27 500 B). В `git branch -r` обеих веток больше нет.

### (а) Открыт ли TD-016 и что в нём про M-31

**TD-016 — OPEN, дважды подтверждено текстом сегодняшнего `main`** (`TECH-DEBT.md`, blob
`0f907d1`): «M-32 = валидация ИЗМЕРЕНИЯ, M-31 = корректность ПОДДЕРЖКИ; ортогональны.
TD-016 **OPEN**» (строка 2265) и «TD-016 остаётся **OPEN**» (2253). Про M-31 — в
сопутствующей находке TD-103 (строки 760–768): «переадресуют корректность поддержки книги
(эвикция, TD-016) на **M-31** … Milestone'а M-31 в `main` НЕТ: `ls milestones/ | grep M-31`
→ пусто, коммит `01be192 docs+test(M-31): book eviction (TD-016)` в `origin/main` не входит.
То есть follow-up … не приземлён, и TD-016 висит без носителя-milestone'а … отдельно —
судьба M-31 (приземлить или переспецировать)». Плюс `PROJECT-STATE.md:615,657,660` — «M-31
(эвикция/TD-016) … остаётся в очереди».

Важное уточнение: `TECH-DEBT.md` ИМЕНИ ветки не называет — он ссылается на КОММИТ `01be192`
и на будущий milestone M-31. Ссылки на само ИМЯ `feat/M-31-book-eviction` висят в
`docs/SESSION-HANDOFF.md:39` («Удержано намеренно, ждёт решения founder'а») и `:417`
(таблица §9 «почему НЕ удалена») — обе теперь ложны фактом удаления (зона architect, §8.2).

### (б) Применяются ли патчи к текущему `main`

```
$ git apply --check feat-M-31-book-eviction.patch   # на worktree @ 0f907d1
→ exit=0, clean   (crates/book/tests/red_eviction.rs · milestones/M-31-book-eviction.md · scripts/verify_M-31.sh)
$ git apply --check feat-M-10-r001-obi.patch
→ exit=0, clean   (crates/research-cli/tests/red_killscreen.rs · milestones/M-10-r001-obi-killscreen.md · scripts/verify_M-10.sh)
```
Оба патча — не «запись намерения», а живой RED-набор, накладывающийся на сегодняшний `main`
без единого отказа. Коммит `01be192` пока существует в локальных объектах
(`git cat-file -e` → есть), но после `git gc` единственным долговечным носителем станет
патч. По `feat/M-10-r001-obi` дополнительно действует оговорка `SESSION-HANDOFF`:
«"перекрыта полностью" верно как оценка РЕДАКЦИЙ, но НЕ как построчное включение — общих
отсортированных строк 50 из 73» относительно живой `feat/M-10-rebased`; патч — вторичный
архив при живой полной ветке.

### (в) Текст правки для reviewer (`TECH-DEBT.md` — его зона; здесь только формулировка)

В хвост сопутствующей находки TD-103 (после «судьба M-31 (приземлить или переспецировать)»)
и зеркально в хвост TD-016 добавить:

> **Обновление 2026-08-13.** Ветка `feat/M-31-book-eviction` (единственный носитель коммита
> `01be192`: спека M-31 + `crates/book/tests/red_eviction.rs` + `scripts/verify_M-31.sh`)
> удалена при чистке origin. RED-набор сохранён патчем
> `/home/nous/salvage-2026-08-13/deleted-branches/feat-M-31-book-eviction.patch` —
> проверено: `git apply --check` на `main@0f907d1` чист. Носителем долга остаётся
> БУДУЩИЙ milestone M-31: при постановке в очередь architect приземляет набор из патча
> (или переспецирует), ссылок на ветку больше не существует.

### (г) `feat/M-10-r001-obi` (OBI kill-screen, ранняя ревизия)

Долг в `TECH-DEBT.md` на неё не ссылается — правки там не нужно. Судьба содержимого
покрывается решением founder'а по §4 (полная ветка `feat/M-10-rebased` жива и является
надмножеством по файлам); патч — страховка на случай удаления полной ветки.

---

## §7. СВОДНАЯ ТАБЛИЦА (главная)

| ветка | отстаёт / своих | конфликтов | рекомендация | кого/чего не хватает |
|---|---|---|---|---|
| `feat/M-64-export-contract` | 90 / 11 | **0** | **ДОВЕСТИ** (порядок назначен founder'ом 11.08) | architect: спека миграции К1/К6b (CT-RFC-06) — её ещё нет; founder: ответ П-012 к моменту спеки; потом reviewer-merge |
| `feat/M-59-lifetime-memory` | 174 / 7 | **0** | **ДОВЕСТИ** (одной работой с salvage) | tester: прогон `dc646cb`-версии + Done Block; reviewer: круг 2 после REJECT `R-038` |
| `salvage/M-59-research-dev-uncommitted` | 174 / 8 | **0** | **ДОВЕСТИ** (это надмножество feat/M-59 + свежий фикс) | те же; после merge M-59 удалить ОБЕ M-59-ветки |
| `feat/M-10-rebased` | 930 / 12 | **0** текстовых (семантика кода не проверена; код с дефектом KS-I-1 не вливать) | **ЖДАТЬ РЕШЕНИЯ FOUNDER'А** | вопрос: «нужен ли KILL-след OBI (артефакты + 276 ledger-записей) в `main` сейчас?» да → docs-only merge без кода; нет → архив + пометить `PROJECT-STATE.md:552` DEFERRED |
| `feat/M-60-mechanisms` | 163 / 34 | **4** (все — уже-влитый M-60a-слой; разрешение в пользу `main`) | **ДОВЕСТИ** (стоянка легальна; возобновлять карвом M-60b от свежего `main`) | постановка в очередь (founder приоритизирует); engine-dev на G1/G2/G3 |
| ~~`feat/M-31-book-eviction`~~ (удалена) | — | патч чист (`git apply --check` exit=0) | **правка TECH-DEBT** (текст в §6в, зона reviewer) + M-31 в очередь при возврате к TD-016 | reviewer: внести §6в; architect: судьба M-31 «приземлить или переспецировать» (уже открытый вопрос TD-103) |
| ~~`feat/M-10-r001-obi`~~ (удалена) | — | патч чист | ничего не делать (надмножество живо в `feat/M-10-rebased`) | покрывается решением по §4 |

Ни одна из пяти живых веток не подлежит ЗАКРЫТИЮ: три — рабочие с назваными недостающими
шагами, одна — сейф свежайшего фикса, одна — заблокирована волей founder'а и несёт
уникальные данные.

## §8. Побочные находки (не предмет аудита, но найдено замером)

1. **`SESSION-HANDOFF` §0 протух по M-64:** записано «Ждёт УЗКОГО круга 3 … @ `20f9267`»,
   фактически круг 3 пройден (`C-076`, NOTE, «B1/M1 closed»), tip ветки `93bfb34`
   (+2 коммита). Обновление §0 — зона architect.
2. **`SESSION-HANDOFF:39` и `:417`** утверждают, что `feat/M-31-book-eviction` «удержана
   намеренно» / «НЕ удалена» — с сегодняшнего дня ложь; та же секция называет
   `feat/M-10-r001-obi` живой в origin. Обе строки требуют правки architect'ом (файл —
   `docs/`, не reviewer).
3. **276 ledger-записей существуют только на `feat/M-10-rebased`** — при любом решении по §4
   их судьба должна быть названа явно: append-only ledger (`INTG-I-6`) в `main` держит
   4 строки.
