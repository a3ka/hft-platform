<!-- FACTS: audited_head=eb373d1c50f2ee6ca50f1ffff592de0cd9ed3016 collected=2026-09-05 -->

# Судьба трёх веток — независимая рекомендация (2026-09-05)

**Роль:** независимый советник со свежим контекстом (`subagent_type: architect`, Fable), по
запросу founder'а: ведущий architect сессии — заинтересованная сторона, часть работы на
ветках его. **Полномочия:** читать, судить, рекомендовать. Ничего не мержил, не удалял, не
правил содержимое веток. Единственный артефакт — этот файл, на ветке
`docs/branch-disposition-0905`, созданной от `origin/main`.

**Что открывал** (правило цитаты, `reading-map.md` §3): впрыск (`CLAUDE.md`, шесть правил,
профиль) · `docs/04-workflow.md` целиком · `docs/workflow/reading-map.md` целиком ·
`docs/workflow/session-handover-2026-09-05.md` целиком · `SESSION-HANDOFF.md` §0 и §0ter ·
индекс `П-*` и тела `П-007`/`П-013`/`П-014`/`П-023`/`П-027`(шапка) · спека `M-67` rev2
целиком · `C-091` (шапка, F-1…F-3, Done Block) · `verify_M-67.sh` целиком · замер ёмкости
(шапка, оглавление) · `fa/viz-backend.md:105-150, 250-270` · `DESIGN.md:925` ·
`06-data-layer-and-storage.md:110-122` · `05-contract-layer.md:36-44` ·
`docs/plans/branch-inventory-2026-08-19.md` §4, §6 · `founder-page-2026-08-17.md:116-178` ·
`BACKLOG.md:405-420` · передачи `08-30c`, `08-31`, `09-01` (фрагменты по предмету) · обе
передачи `31b`/`31c` целиком · `C-101` (шапка, оглавление) · `C-173:28-36` · README архива
24.08 · `check_branch_health.sh` (прогон) · `next_artifact_id.sh:20-27,86-91` ·
`check_rollout_composition.sh:1-14`. Ярус C — грепом: `M-67`, `C-101`, `MD-I-`,
`handover-2026-08-31[bc]`, `salvage/2026-08-28-cleanup`.

Внутри каждого fenced-блока — только то, что напечатала команда (`A-030` §1). Где вывод
обрезан, это сказано словами рядом с блоком.

---

## 0. Состояние мира на момент разбора (Ярус S)

```
$ date -u
Sat Sep  5 10:24:51 PM UTC 2026
$ git rev-parse origin/main
eb373d1c50f2ee6ca50f1ffff592de0cd9ed3016
```

`gh run list --branch main --limit 5` — все пять записей `completed success` (три `Deploy to
VPS`, два `CI` — PR #161 и #160); `gh run list --workflow=deploy.yml --limit 3` — три
`completed success`. Локальный диск: `/dev/md2 437G 341G 74G 83% /`.

```
$ ssh … root@167.233.192.131 'cd /root/hft-platform && git rev-parse --short HEAD; df -h / | tail -1'
571403d
/dev/sda1       150G  111G   33G  78% /
```

`bash scripts/check_branch_health.sh` → `VERDICT: PASS`, 12 веток кроме `main`, замечаний 0.
Строки пяти предметов из его таблицы (колонки: ОТСТ · СВОИХ · НОВЫХ · PR · ВОЗРАСТ):

```
docs/M-67-rev2                              974      8      7 —        20 сут
docs/session-handover-2026-08-31b           141      1      1 —        4 сут
docs/session-handover-2026-08-31c           137      1      1 —        4 сут
feat/harness-doc-integrity                  884      4      3 —        18 сут
feat/harness-milestone-shape                404     14      8 —        6 сут
```

Замечание к наблюдателю, не к предмету: `ok    ДУБЛЬ: ни один предмет не живёт больше чем на
одной ветке` — при том, что `feat/harness-doc-integrity` и `feat/harness-milestone-shape`
несут один предмет (§2 ниже). Дубль он ищет по имени, не по содержанию.

Вершины пяти веток (`git ls-remote --heads origin`, обрезано до пяти строк предмета):

```
eaef386e04765b6fcc27ed74d948c995acedcf54	refs/heads/docs/M-67-rev2
eb49910123a54971039602ea2582e5887559a22b	refs/heads/docs/session-handover-2026-08-31b
30cd2d1ef72848728177fc194836b988bb37a4eb	refs/heads/docs/session-handover-2026-08-31c
d6e6c31416848f165a0b785aadbb03940943116c	refs/heads/feat/harness-doc-integrity
dcab8fb90482e6b3a58ec4de2588a4077374c493	refs/heads/feat/harness-milestone-shape
```

---

## 1. Вопрос 1 — `docs/M-67-rev2`

### Рекомендация одной строкой

**Предложение ведущего architect'а отклонить как неполное и в двух пунктах неверное: в
`main` сейчас не вливать ничего, кроме замера ёмкости с маркером фактуры; ветку не сносить,
пока указатели `main` на неё не перенаправлены; спека идёт в `main` только как rev3 — три из
семи её предусловий протухли, а один из трёх оракулов противоречит подписанному решению.**

### 1.1. Оракулы — предложение о них молчит, и молчать нельзя

Проверено исполнением, не по тексту спеки: файлы взяты из ветки в чистое дерево от
`origin/main`, собраны и запущены.

```
$ cargo test -p gateway --test red_md_i6_journal_first --no-run
error[E0063]: missing field `depth_cadence_ms` in initializer of `Selector`
   --> crates/gateway/tests/red_md_i6_journal_first.rs:123:5
error: could not compile `gateway` (test "red_md_i6_journal_first") due to 1 previous error
exit_i6=101
$ cargo test -p gateway --test red_md_i7_band_lock --no-run
error[E0063]: missing field `depth_cadence_ms` in initializer of `Selector`
   --> crates/gateway/tests/red_md_i7_band_lock.rs:118:5
exit_i7=101
$ cargo test -p journal --test red_md_i2_hot_window
test md_i2_b1_no_event_younger_than_window_is_deleted ... FAILED
test md_i2_b2_stale_data_is_actually_removed ... ok
test result: FAILED. 1 passed; 1 failed; 0 ignored; 0 measured; 0 filtered out; finished in 2.21s
exit=101
```

(строки компиляции зависимостей и `panicked at …:201:5` опущены.)

Итог по трём оракулам:

| оракул | спека говорит | на сегодняшнем `main` | судьба в `main` |
|---|---|---|---|
| `MD-I-6` | «сторож, зелёный» | **не компилируется** — `Selector` получил поле `depth_cadence_ms` (`crates/gateway/src/lib.rs:213-216`, M-68 задача 15) | нельзя: это «сломанный билд», а не сторож — определение самой спеки §10 |
| `MD-I-7` | «RED `n1`,`n2`» | **не компилируется** (та же причина) — и, важнее, **инвариант отменён подписью** (см. 1.3) | нельзя дважды |
| `MD-I-2` | «RED `b1`, зелёный `b2`» | подтверждено: `b1 FAILED`, `b2 ok` | честный RED — и именно поэтому в `main` не живёт (`gates.md` §8: «RED до реализации не живёт в `main`») |

Утверждение спеки о `MD-I-6` («зелёный сторож») на сегодняшней ревизии ложно не по смыслу,
а по факту: сторож, который не собирается, не сторожит. Оба gateway-оракула чинятся одним
полем — но чинить их вправе только architect, и это правка RED-спеки, то есть работа, а не
merge.

### 1.2. `verify_M-67.sh` в `main` без предмета — гейт, который никогда не пройдёт

Гейт начинается setup-guard'ом, требующим все шесть файлов, включая три оракула
(`verify_M-67.sh:42-45`: `for f in "$SPEC" "$MEAS" "$O_HOT" "$O_BAND" "$O_JF" "$O_ALLOW"; …
fail "предмет ОТСУТСТВУЕТ"`). Без оракулов он завершается `VERDICT: FAIL (setup не состоялся)`
при каждом запуске. С оракулами — краснит `cargo test --all` (`.github/workflows/ci.yml:24:
run: cargo test --all`). Третьего состояния у него нет.

Из CI гейты милестоунов не зовутся (единственное совпадение — комментарий):

```
$ grep -n 'verify_M-' .github/workflows/ci.yml
536:      # `verify_M-45.sh`, который CI не зовёт. Защита истекала в момент merge'а: следующий
```

Норма `04-workflow.md` §2 «Close-out» знает два дома для гейта: `scripts/` у ЖИВОГО
милестоуна и `docs/archive/` у ЗАКРЫТОГО (сегодня там 27 гейтов: `ls docs/archive | grep -c
verify_M-` → `27`). Гейт НЕНАЧАТОГО предмета в `scripts/` — третья категория, которой норма не
предусматривает, и по замеру той же нормы («45 гейтов, 6 183 строки, из CI не зовётся ни
один») она весит харнесс и ничего не проверяет. **Предложение влить гейт отдельно от
оракулов создаёт ложный гейт по построению.**

Плюс шаг B того же гейта проверяет `GATEWAY_BANDS ≤ 0.013` и печатает `замок СНЯТ МОЛЧА` при
любом более глубоком дефолте (`verify_M-67.sh:76-82`). Он сработает ровно в момент
исполнения `П-014` п.4 и `M-70` — против ПОДПИСАННОГО решения (1.3).

### 1.3. Предусловия — четыре из семи строк таблицы §8 протухли, и не все в сторону «открыто»

| # | спека (rev2, 16.08) | сегодня — с командой | вывод |
|---|---|---|---|
| **P1** развилка §5 (L3 — факт или производная) | ОТКРЫТО, граница C | `docs/plans/founder-page-2026-08-17.md:139` «Развилка `M-67` §5 — снялась чтением, тебе решать нечего»; первоисточник открыт: `docs/06-data-layer-and-storage.md:116-117` «**Журнал бессмертен** (JR-I-6/CT-I-3): старые сегменты уходят в COLD, не удаляются»; `05-contract-layer.md:38-43` — виз-бэкенд формы T-DESIGNATE, без contract-RFC; в коде `crates/journal/src/segments.rs:2118-2128` `ColdCopyProof`, конструктор приватный | **СНЯТА**: вариант A запрещён инвариантом, L3 — производная T-designate. Следом снимается P2 |
| **P3** `CT-RFC-07` фаза 1 (шарды) | ждёт решения начать сбор | `git grep -c -iE 'shard\|шард' origin/main -- crates/journal/src` → нет совпадений (`grep_exit=1`) | **ОТКРЫТО** — верно |
| **P4** периметр ретеншена «по данным» | ОТКРЫТО | конструкция «L2 удаляется через 48 ч» противоречит `06`:116-119 и `П-023` п.3 («доступ КО ВСЕМ данным») — холодное уходит на коробку, не удаляется; свойство гранулярности (`MD-I-2`) остаётся верным для любого ретеншена | **ПЕРЕФОРМУЛИРОВАНО**: окна 48 ч больше нет как цели, оракул `MD-I-2` жив, его рамка — нет |
| **P5** замок `A-002` (полосы >1.3 %) | ОТКРЫТО, граница C | `docs/PENDING-SIGNATURE.md:749` «П-014 — ПОДПИСАНО 2026-08-17: полосы глубины ВКЛЮЧАЮТСЯ»; `fa/viz-backend.md:120` «✅ ЗАМОК `A-002` З-1 СНЯТ ПОДПИСЬЮ FOUNDER'А 2026-08-17» | **ЗАКРЫТ на следующий день после rev2** ⇒ задача A1 и оракул `MD-I-7` («`validate_selector` отказывает на полосе >1.3 %») требуют замка, которого founder снял. Живой предмет `M-70` («включение полос глубины», `docs/M-70-rev2`) делает обратное |
| **P6** fail-closed «состав ≠ `epoch_id`» | ОТКРЫТО | `scripts/check_rollout_composition.sh` в `main`, сверяет compose ↔ `docs/data-epochs.md` `ACTIVE-COMPOSITION` ↔ подписи (`:1-14`) | **ЗАКРЫТ для статического allow-list**; для динамического топ-50 не проверялся |
| **P7** Storage Box | ОТКРЫТО: «58.4 ГБ свободно ⇒ 8 суток» | `ssh прод → ssh -p 23 … storagebox 'df -h .'` → `u659392-sub1    5.0T  177G  4.9T   4% /home` | **ЗАКРЫТ** (`П-023`, 29.08) |

Новый блокер, которого спека знать не могла — снят мной сегодня, не взят из мандата:

```
$ ssh … 'grep -n "RETENTION_MODE\|RETAIN_DAYS" /etc/cron.d/hft-journal-retention; tail -3 /var/log/hft/journal-retention.log; ls -la /mnt/journal-cold | head -4; …'
38:RETENTION_RETAIN_DAYS=14
41:RETENTION_MODE=dry-run
  offloaded: 0  pruned: 0  failed: 0
  freed_bytes: 0
total 8
drwxr-xr-x 2 root root 4096 Jul 14 18:43 .
drwxr-xr-x 3 root root 4096 Jul 14 18:43 ..
556
2026-07-15
2026-09-05
98G
2026-09-05T22:22:03Z
```

Читать так: ретеншен в `dry-run` при «хранить 14», сегментов 556 на 98 ГБ, самый старый —
15 июля (52 дня), `/mnt/journal-cold` пуст, офсайт-копия обновлялась в 22:22Z сегодня. Это
не дефект спеки — по `П-023` удаление включается ОТДЕЛЬНЫМ решением после `R1b`
(`M-74`, не начат). Но для M-67 это значит: раскатка L1 (7.23 ГБ/сут по замеру спеки) на
диск с 33 ГБ свободного при выключенном ретеншене — вопрос дней, и Storage Box сам по себе
его не решает, пока нет двухъярусного чтения (`П-023` «форма не выбрана»).

### 1.4. Идентификаторы — коллизия есть, но не та, которую искали

По классу `M` конфликта нет:

```
$ git ls-tree -r origin/main --name-only | grep -i 'M-67'
exit=1
```

По семейству инвариантов — есть. В `main` семейство `MD-I` заведено `M-68` для ДРУГОГО
предмета и с явным утверждением, что младших номеров не существует:

```
$ git grep -ho 'MD-I-[0-9]\+' origin/main | sort -uV | tr '\n' ' '
MD-I-1 MD-I-8 MD-I-17
```

`DESIGN.md:925`: «`MD-I` | депт-серия (стык `gateway`↔`book`) | … заводится `M-68`: `MD-I-8`
определён текстом в `docs/fa/viz-backend.md` §5». `fa/viz-backend.md:255`: «братьев
`MD-I-1..7` нет»; `:265`: «Разрыв `MD-I-1..7` — дыра, а не нарушение». (`MD-I-1` и `MD-I-17`
в выводе выше — упоминания диапазона в этих же фразах, не инварианты.)

Спека `M-67` кладёт под тот же префикс `MD-I-2` (ретеншен журнала, крейт `journal`) и
`MD-I-6`/`MD-I-7` (выдача). Влить её как есть — сделать `fa/viz-backend.md:255` ложным и
поместить журнальный инвариант в семейство «стык gateway↔book». `gates.md` §12 требует
уникальности идентификатора артефакта; здесь уникальность номера соблюдена, а ЗНАЧЕНИЕ
префикса — нет. Перенумерация в своё семейство — правка формы спеки и оракулов.

### 1.5. Замер ёмкости — рабочая записка, а не фактура

Файл `M-67-capacity-2026-08-16.md` лежит в `docs/plans/` ТОЛЬКО на ветке; литеральный путь
здесь не записан намеренно — гейт правдивости (`4-МЁРТВЫЕ-ФАЙЛЫ`) справедливо числит его
мёртвой ссылкой, и на первой редакции этого раздела он сработал. Команды: `git show
origin/docs/M-67-rev2:<путь файла> | head -5 | grep -c 'FACTS:'` и тот же `git show` через
`grep -oE '[A-Za-z0-9_./-]+\.(rs|sh|md|yml|toml):[0-9]+' | wc -l`. Вывод, по одной строке на
команду:

```
0
5
```

Маркера нет; утверждений `путь:строка` пять — ниже порога 20, `verify_design_claims.sh`
промолчит. Без маркера документ в `docs/plans/**` — рабочая записка (`reading-map.md` §Ярус
S). Его числа о проде (58.4 ГБ свободно, 63 % занято, «8 суток запаса») устарели 29.08.
Числа о РЫНКЕ (1002 инструмента, 845 989 B/s, 2.447×, 7.873×) — единственный коммитнутый
замер с командами, и его стоит сохранить датированным.

### 1.6. Предшественники — предмет решался трижды, каждый раз «держать»

- `docs/plans/branch-inventory-2026-08-19.md:109` — «ни один критик rev2 не смотрел; открыть
  draft-PR и диспетчеризовать»; §6 — три вопроса founder'у по M-67 (P1, P3, P7): два из трёх
  сегодня закрыты (1.3).
- `session-handover-2026-08-30c.md:189` — `docs/M-67-rev2` **ДЕРЖАТЬ**; `-08-31.md:186` —
  «держим намеренно»; `BACKLOG.md:416` — «предмет не заброшен — он ждёт очереди; решение
  founder'а о месте в очереди»; там же: «Ветки и спас-рефы НЕ удалять: на них живёт работа,
  не воспроизводимая из `main`».
- `docs/PENDING-SIGNATURE.md:837-838` (`П-013`): «Файла спеки в `main` НЕТ: он живёт на
  ветке `docs/M-67-rev2` … носитель, который переживёт merge, ровно один и назван здесь».
  Подписанное решение founder'а опирается на §7.1/§7.4 этой спеки ПО ИМЕНИ ВЕТКИ.
- Спас-реф уже есть и совпадает с вершиной:

```
$ git ls-remote origin 'refs/salvage/*' | grep -iE 'm-67'
eaef386e04765b6fcc27ed74d948c995acedcf54	refs/salvage/2026-08-28-cleanup/docs-M-67-rev2
```

Следствие: **снос ветки на уровне git ничего не теряет** (реф пиннит ту же ревизию), но
ломает два живых указателя в `main` — `П-013` и `BACKLOG` — на имя ветки. Их надо
перенаправить (на спас-реф или на приземлённый файл) ДО сноса, а не после. Правка
`PENDING-SIGNATURE.md` — зона перепроверки `gates.md` §9.

### 1.7. Что рекомендую вместо предложения

1. **Оракулы — не вливать.** Два не собираются, один честно красен, один противоречит
   `П-014`. Остаются на спас-рефе как материал.
2. **`verify_M-67.sh` — не вливать** (1.2). Гейт идёт вместе с предметом, когда предмет
   пойдёт.
3. **Замер ёмкости — влить**, добавив первой строкой маркер с ревизией сбора
   (`audited_head=1e5b85b2b5f7692982e3078e10e00d472931d256` — merge-base ветки с `main` на
   16.08, `collected=2026-08-16`) и одну датированную строку под шапкой: «числа о диске прода
   протухли 29.08 (`П-023`), см. `df` коробки». Это единственный кусок ветки, годный в `main`
   сегодня без работы над содержанием. Изложение, не форма — свой PR автора по §9.
4. **Спека — только как rev3**, и rev3 — работа, а не редактура шапки: таблица §8 (P1 снята,
   P5/P7 закрыты, P4 переформулирована, P6 частично, новый блокер — ретеншен), задача A1 и
   `MD-I-7` снимаются или переворачиваются в сторону `П-014` (поле-сторонняя метка
   достоверности уже стоит в `main`: `fa/viz-backend.md:143`,
   `depth_provenance_label(band_pct_e8, side, reach)`), семейство инвариантов
   перенумеровывается (1.4), §6.1 переписывается под снятый замок. Это изменение
   инварианта/предусловий/acceptance ⇒ critic по `gates.md` §9; спека сама назначает себе
   RAW-гейт на сильной модели (§13 п.2) — рамка журнала (§6.2/§6.3) в ней остаётся.
5. **Ветку держать** до одного из двух событий: rev3 влита, либо указатели `П-013`/`BACKLOG`
   перенаправлены на спас-реф. После любого из них — снести (`git push origin --delete`).
6. Отдельно и ценнее спеки: §7.1/§7.4 (семантика TOTAL) уже ПОДПИСАНЫ `П-013`, а сама
   `П-013` говорит: «правка `docs/fa/viz-backend.md` идёт отдельным предметом через гейт §9».
   Подписанный текст, живущий только на невлитой ветке, — тот же класс, что «решение,
   живущее в переписке». Перенос §7.1/§7.4 в FA — маленький предмет с ясным владельцем.

### 1.8. Цена

- **Если сделать по предложению ведущего architect'а** (спека + гейт + замер в `main`,
  оракулы — ?): в `main` появляется документ с четырьмя ложными строками предусловий и
  задачей, исполнение которой нарушает подпись founder'а; гейт, который либо всегда
  `FAIL (setup)`, либо краснит CI; и разрыв семейства `MD-I` в FA. Спеки — то, из чего
  исполняют агенты: ошибка в них не падает в CI, а тиражируется (`gates.md` §9, первая фраза).
- **Если сделать по моей рекомендации:** M-67 остаётся «работой в очереди» ещё на один круг
  (rev3 + критик), и это стоит круга на сильной модели; до тех пор продукт по-прежнему видит
  два инструмента из 1002. Но это уже так с 16.08, и предложение это не меняло — оно
  приземляло документ, а не предмет.
- **Если снести ветку сейчас без перенаправления указателей:** git ничего не теряет
  (спас-реф), теряет корпус — `П-013` и `BACKLOG` указывают в пустоту, тот же класс, что
  четырежды сработал на §0ter.

### 1.9. Кто решает

- **Founder (граница C / очередь):** платить ли круг за rev3 сейчас — против очереди
  `M-77 → M-74 → M-72 → M-76` и его же решения 24.08 «не заводи новые задачи»; P3 (начать
  сбор топ-50 — состав записываемых данных); включение ретеншена (что удаляем; по `П-023` —
  после `R1b`); форма двухъярусного чтения (`П-023`, «выбор — отдельный милестоун»).
- **Инженерная правота, решения не требует:** оракулы не собираются; гейт без предмета ложен;
  `MD-I-7` противоречит `П-014`; префикс `MD-I` занят другим семейством; маркер фактуры на
  замер; указатели перенаправить до сноса.

---

## 2. Вопрос 2 — `feat/harness-doc-integrity`

### Рекомендация одной строкой

**Снести (со спас-рефом, который уже существует): переносить нечего — единственный
уникальный по содержанию файл, вердикт `C-101`, лежит в `main` побайтно, а барьер и проба
восьмикруговой ветки закрыли его четыре блокера и ушли на 799 строк вперёд.**

### 2.1. Что уникально — по содержанию, не по именам

Четыре файла ветки против `main`: `ci.yml` (+25/−2), `C-101` (194), `check_milestone_shape.sh`
(96), `red_milestone_shape.sh` (158). Проверка каждого:

```
$ git diff --stat origin/main:docs/archive/abandoned-gate-verdicts-2026-08-24/C-101-harness-milestone-shape.md origin/feat/harness-doc-integrity:research/critiques/C-101-harness-milestone-shape.md
diff_exit=0
```

Пустой дифф: вердикт в `main` побайтно тот же, что на ветке (архив 24.08; README архива
`:87-92` фиксирует и известный дефект — внутри файл озаглавлен `C-100`, след разводки
коллизии по `A-010` §E, не правлен намеренно).

```
$ git merge-base --is-ancestor origin/feat/harness-doc-integrity origin/feat/harness-milestone-shape
exit=1
$ git diff --stat origin/feat/harness-doc-integrity origin/feat/harness-milestone-shape -- scripts/check_milestone_shape.sh scripts/tests/red_milestone_shape.sh .github/workflows/ci.yml
 .github/workflows/ci.yml             | 128 ++++++++-
 scripts/check_milestone_shape.sh     | 159 ++++++++++-
 scripts/tests/red_milestone_shape.sh | 537 ++++++++++++++++++++++++++++++++++-
 3 files changed, 799 insertions(+), 25 deletions(-)
```

Живая ветка не потомок (пересобрана от `main`, `679b114 … четыре блокера C-101 закрыты`), и
покрытие блокеров подтверждено не автором, а следующим критиком — открыт архивный
первоисточник: `docs/archive/abandoned-gate-verdicts-2026-08-29/C-173-…:31-32` «The C-101
normal-fence/comment, prose-substring, and rename cases are now covered» (дальше `C-173`
находит новые дефекты — B-5 и далее — но это уже жизнь живой ветки, восемь кругов).
Барьера в `main` по-прежнему нет: `grep -c 'milestone-shape' .github/workflows/ci.yml` → `0`.

Итого: в старой ветке нет ни строки, которой не было бы либо в `main` (вердикт), либо в
более сильной редакции на живой ветке (барьер, проба, проводка).

### 2.2. Номер, спас-реф, PR

```
$ git ls-remote origin 'refs/salvage/*' | grep -iE 'doc-integrity'
d6e6c31416848f165a0b785aadbb03940943116c	refs/salvage/2026-08-28-cleanup/feat-harness-doc-integrity
$ bash scripts/next_artifact_id.sh C
C-212
```

Спас-реф совпадает с вершиной ветки. Аллокатор даёт `max+1` по `refs/remotes/origin ∪
refs/heads` (`next_artifact_id.sh:20-27`) и разрывов не занимает: `C-101` — разрыв в глубине
диапазона при максимуме 211, снос ветки ничего не освобождает и ничем не грозит
(так же сказано в `session-handover-2026-08-30c.md:142-143`). PR ветки — `#33 CLOSED`.

### 2.3. Предшественники расходятся, и один из них опирался на ложное основание

`08-30c:186` — «снести СО СПАС-РЕФОМ»; `08-31:186` — «держим намеренно (несёт `C-101`)».
Второе основание неверно: `C-101` в `main` есть (`08-30c:149-150` это уже говорил). Держать
ветку ради вердикта, который лежит в `main`, — не основание.

### 2.4. Цена

- **Снести:** нулевая потеря по git (реф) и по содержанию (2.1). Побочный эффект: комментарий
  `scripts/check_branch_health.sh:30` («первую блокирует `C-101` REJECT») протухнет — строка
  комментария, правится при следующем касании скрипта.
- **Держать:** предмет живёт на двух ветках, чего наблюдатель не видит (§0), и каждая новая
  сессия заново тратит время на вопрос «что там уникального». Эта записка — четвёртый по
  счёту разбор той же ветки (19.08, 30.08, 31.08, сегодня).
- **Переносить уникальное в живую ветку:** переносить нечего; попытка переноса старого
  барьера в новый — регресс на 799 строк.

### 2.5. Кто решает

Инженерно — снести. По сложившейся практике удаление ветки в `origin` — действие founder'а
(`31b` §4 числит снос веток в его таблице как «необратимо»; с спас-рефом оно обратимо, но
практику я не переопределяю).

---

## 3. Вопрос 3 — `docs/session-handover-2026-08-31b` и `…-31c`

### Рекомендация одной строкой

**Влить обе как есть, одним PR, без критика (§9: изложение, датированное маркером) — не
ради классов ошибок, которые уже в `main`, а ради целостности цепи передач и
воспроизводимости вердикта `R-164`, который цитирует `31b` командой `git show origin/…`.**

### 3.1. Содержание — что в них есть и где это уже живёт

Обе читаны целиком (244 и 148 строк), обе несут маркер фактуры (`d77398d…`, `af29452…`).

| что несёт | где уже в `main` — с командой |
|---|---|
| `31b` §2 — класс «признак без различающей силы», шесть вердиктов | правило `Р-4`: `oracle-blindness-class-2026-08-28.md:217`; `A-030`; `git grep -l 'различающ' origin/main -- docs/workflow research/arbitration/A-030*` → три файла |
| `31b` §4 — что ждёт founder'а | восстановлено дословно: `session-handover-2026-09-01.md:276` «§7ter … восстановлено из передачи 31b, проверено на живость» |
| `31b` §5 — две правки харнесса | `09-01:304` «§7quater … (из 31b §5)» |
| `31b` §1 — пять предметов на гейтах | история: `M-45`/`M-75` закрыты (`09-05` §2bis), `M-70` rev9, `M-72`/`M-74` — актуальное в `09-05` |
| `31b` §3 — пять процессных нарушений | правила уже есть: `gates.md` §3/§8 (код возврата), `branch-hygiene.md` п.6 (salvage), запрет `commit -a` |
| `31c` §2 — класс «вывод не производится командой», приём `diff <(sed …)` | `A-030` §1; `09-01` (`git grep -l 'diff <(sed' origin/main` → `09-01`, `A-030`); предмет `M-76` (на своей ветке) |
| `31c` §5 — бюджет ядра 40 B / 36 B | снято: `09-05` §7 «запас 2062 B» |
| `31c` §6 — мутация не на рабочей ветке | `09-05` §7 «Мутация — в ОТДЕЛЬНОМ worktree» |

Итого: ни один класс и ни один долг из двух передач не потерян — все унесены `09-01`,
`A-030` и `Р-4`. Спорить о «ценности классов» тут не о чём: она уже реализована.

### 3.2. Что теряется, если НЕ влить — и это не классы

```
$ git grep -n 'handover-2026-08-31[bc]' origin/main
docs/workflow/session-handover-2026-09-01.md:278:Раздел был в прошлой передаче (`session-handover-2026-08-31b` §4)
docs/workflow/session-handover-2026-09-01.md:352:- Предыдущая передача — **session-handover-2026-08-31c** в `docs/workf
docs/workflow/session-handover-2026-09-01.md:353:  `docs/session-handover-2026-08-31c` (в `main` не влита; литеральный п
research/reviews/R-164-scope-guard-compose-recheck.md:87:$ git show origin/docs/session-handover-2026-08-31b:docs/workflow/session-handover-
research/reviews/R-164-scope-guard-compose-recheck.md:94:Ни передача 31.08 (`docs/session-handover-2026-08-31b`), ни `main`, н…
```

(строки обрезаны справа.) Цепь «предыдущая передача» в `main` сегодня: `09-01 → 31c (нет) →
31b (нет) → 30c (есть)`. Два звена отсутствуют, и `09-01` честно пишет «в `main` не влита;
литеральный путь не записан — гейт правдивости числил бы его мёртвой ссылкой». Вердикт
`R-164` (в `main`) воспроизводит цитату из `31b` командой `git show origin/docs/…` — снос
ветки без вливания делает команду в аудит-трейле невоспроизводимой.

Норма, по которой передачи существуют, требует их в `main`: `09-05` §7 «передача и промпт —
в main СВОИМ PR, ветка ОТ MAIN». Обе ветки — от `main` своего дня (merge-base
`d77398d`/`af29452`, оба 31.08), по одному коммиту, по одному новому файлу; конфликтовать
нечему.

### 3.3. Разрастание корпуса — проблема настоящая, но не здесь

`ls docs/workflow | grep -c session-handover` → `21`; `du -ch docs/workflow/session-handover-*.md
| tail -1` → `488K total`. `reading-map.md` §5 называет три предмета вычистки — `TECH-DEBT.md`
(567 KB), `PROJECT-STATE.md` (448 KB), `SESSION-HANDOFF.md` §1–§9 — и ни одного из них эти
два файла не касаются. Передачи не входят ни в один ярус чтения, кроме ПОСЛЕДНЕЙ (Ярус A
читает `09-05` целиком); +2 файла к 21 не меняют ничью цену чтения. Аргумент «корпус
растёт» здесь правдив и не относится к делу; относится — отдельный вопрос, не заводить ли
`docs/archive/handovers/` для передач старше N дней. Это предложение, не рекомендация.

### 3.4. Цена

- **Влить:** +392 строки в каталог, который читается только по последнему файлу; два
  `FACTS`-маркера входят в проверку ссылок `verify_design_claims.sh` — обе передачи
  намеренно не записывали литеральных путей к невлитым файлам (это сказано в них самих),
  прогон на дереве слияния обязателен перед merge (`gates.md` §8, `--merge-preview`).
- **Снести:** нужен спас-реф (сегодняшний набор `refs/salvage/2026-09-05/*` их не содержит —
  они уже были в `origin` как ветки); цепь передач остаётся с дырой навсегда; `R-164:87`
  становится ссылкой в пустоту.

### 3.5. Кто решает

Инженерно — влить; это изложение по `gates.md` §9, автор приземляет сам своим PR. Founder'у
решать нечего, кроме права сказать «не хочу этих двух файлов в `main`» — тогда снос только
со спас-рефом и с пометкой в следующей передаче, где искать `31b`/`31c`.

---

## 4. Сводка

| ветка | рекомендация | инженерно / founder |
|---|---|---|
| `docs/M-67-rev2` | предложение отклонить; влить только замер ёмкости с маркером; спека — как rev3 через критика; оракулы и гейт — не вливать; ветку держать до перенаправления `П-013`/`BACKLOG` или до rev3 | инженерно — всё, кроме: место rev3 в очереди, P3, включение ретеншена, форма двухъярусного чтения — founder |
| `feat/harness-doc-integrity` | снести (спас-реф `d6e6c31` есть; `C-101` в `main` побайтно; барьер превзойдён на 799 строк) | инженерно — снести; акт удаления — founder по практике |
| `docs/session-handover-2026-08-31b`/`c` | влить обе одним PR как изложение | инженерно — влить; founder'у решать нечего |

**Где я расхожусь с ведущим architect'ом по вопросу 1 — прямо:** его предложение приземлить
«спеку, гейт и артефакт ёмкости» и снести ветку (а) не называет судьбы трёх оракулов, без
которых гейт не проходит setup; (б) приземляет гейт, который на первом же исполнении `П-014`
объявит «замок СНЯТ МОЛЧА»; (в) приземляет спеку, у которой три предусловия из семи закрыты
founder'ом до сегодняшнего дня и одна задача противоречит его подписи; (г) сносит ветку,
на которую по имени ссылается подписанная `П-013`. Предмет он держит закрытым правильно —
но документ о закрытом предмете обязан быть правдив на день вливания, иначе это спека,
из которой следующий agent исполнит отменённое.

## 5. Пределы этого разбора — названы

- `verify_M-67.sh` целиком я не гонял (он требует все оракулы, два из которых не
  собираются); утверждения о нём — по чтению кода гейта, с номерами строк.
- Ёмкость коробки снята одной командой через прод (`/root/.ssh/storagebox`, ключ из
  `/etc/cron.d/hft-journal-offsite`); не проверял, что копия на ней ЧИТАЕТСЯ — это и есть
  незакрытый `M-74`.
- Спека `M-70` открыта только шапкой (25 строк): её отношение к `MD-I-7` выведено из
  названия предмета и `П-014`, не из тела спеки.
- `C-091` прочитан не целиком (шапка, F-1…F-3, Done Block); мои выводы на его содержание не
  опираются.
- Я не судил ВОСЕМЬ кругов `feat/harness-milestone-shape` — только то, что старая ветка
  относительно неё не несёт ничего уникального.
