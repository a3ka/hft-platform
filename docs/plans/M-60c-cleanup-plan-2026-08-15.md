# M-60c — план чистки корпуса правил: замер и пошаговый разбор гейта (2026-08-15)

**Автор:** architect (клон, мандат замера) · **Статус:** план, исполнения нет
**Предмет:** `milestones/M-60c-corpus-cleanup.md` + `scripts/verify_M-60c.sh`
**База замера:** `origin/main` = `d29b9ee` (все числа и номера строк — оттуда)
**База ветки этого файла:** `7155871` (ветка спеки `docs/M-60-rebuild-2026-08-14`)

> Документ НИЧЕГО не правит в `.claude/**` и `CLAUDE.md`. Исполняет чистку architect лично.
> Номера строк — от `origin/main` `d29b9ee`; на дереве исполнения их надо переснять
> (`grep -n '^## '`), периметр фиксируется ЗАГОЛОВКАМИ секций, как требует спека §3.

---

## 1. Замер по факту — корпус ВЫРОС относительно чисел спеки

### 1.1. Пофайлово

| файл | спека 14.08 (`c6c62b8`) | факт 15.08 (`d29b9ee`) | дельта за сутки |
|---|---:|---:|---:|
| `gates.md` | 422 | **466** (45 641 B) | **+44** |
| `testing.md` | 224 | 224 (20 589 B) | 0 |
| `handoff-block.md` | 118 | 118 (6 610 B) | 0 |
| `commit-discipline.md` | 112 | **119** (8 579 B) | **+7** |
| `branch-hygiene.md` | 108 | 108 (12 171 B) | 0 |
| `scope-guard.md` | 85 | 85 (7 268 B) | 0 |
| **`.claude/rules/*.md` итого** | **1069** | **1120** | **+51** |
| `CLAUDE.md` | 100 | 100 (8 716 B) | 0 |
| **впрыскиваемое ядро** | **1169** | **1220** | **+51** |

### 1.2. Новая дельта до бюджета

| | факт 15.08 | цель `A-003` | дельта СЕЙЧАС | дельта в спеке |
|---|---:|---:|---:|---:|
| `.claude/rules/*.md` | 1120 | 725 | **−395** | −344 |
| `CLAUDE.md` | 100 | 70 | **−30** | −30 |
| ядро | 1220 | 795 | **−425** | −374 |

**Резать надо на 51 строку больше, чем считала спека.** Прирост целиком приходится на
нормы 15.08 про branch protection и PR-флоу (`fc53ed7`, `af3d12b`): `gates.md` +44,
`commit-discipline.md` +7. Это не история и режется не будет (§5 ниже).

Замер зоны шире бюджета: за сутки процессный слой вырос на 364 строки
(`git diff --stat c6c62b8 origin/main -- CLAUDE.md .claude/`), из них в бюджетируемое ядро
попал 51. Остальное — `.claude/skills/harness-track/SKILL.md` (143) и
`.claude/wrappers/codex-critic.sh` (152), которые шаг B не считает. Храповик действует и
там, где бюджет его не видит; для G1 (M-60b′) это отдельный вход.

### 1.3. Прогон гейта на реальной базе чистки

`origin/main` `d29b9ee`, cargo-шаги нейтрализованы таймаутом 1 с:

```
$ CARGO_FMT_TIMEOUT_SECONDS=1 CARGO_CLIPPY_TIMEOUT_SECONDS=1 CARGO_TEST_TIMEOUT_SECONDS=1 \
    bash scripts/verify_M-60c.sh
FAIL  B .claude/rules = 1120 строк, бюджет 725 — превышение на 395
FAIL  B CLAUDE.md = 100 строк, бюджет 70 — превышение на 30
FAIL  M «Дегенерированный вход обязателен» не переехала в профиль architect'а (в профиле нет)
FAIL  M «Форма прода снимается ЗАМЕРОМ» не переехала в профиль architect'а (в профиле нет)
FAIL  M «мерить ТО, ЧТО ОБЕЩАЕТ» не переехала в профиль architect'а (в профиле нет)
FAIL  M «Целостность гейта — 4 свойства» не переехала в профиль architect'а (в профиле нет)
FAIL  M «RED сравнения ДВУХ источников» не переехала в профиль architect'а (в профиле нет)
FAIL  Dd дубль замера в впрыскиваемом ядре: handoff-block.md branch-hygiene.md gates.md
FAIL  A архива нет или пуст — вырезанное должно переезжать, а не исчезать
FAIL  T G4: предиката AHEAD= в шаблоне §D нет
FAIL  T G5: строки прозрачности гейтинга критика в шаблоне §B нет
FAIL  CI cargo clippy -D warnings timeout 1s     ← артефакт нейтрализации, не находка
FAIL  CI cargo test --all timeout 1s             ← артефакт нейтрализации, не находка
VERDICT: FAIL (13)
```

Зелёными на нетронутом корпусе идут: `B-setup` (предмет на месте), **`D` — 40/40 норм**,
`Dd` (канонический носитель `gc_worktrees.sh` жив), `S` (замок §11 на диапазоне), `P` (пять
проб: 20 / 27 / 8 / 48 / 28 исполнено), `Q` (защита артефактов и идентификаторов),
`CI cargo fmt`.

**Красных процессных шагов — 11: B×2, M×5, Dd×1, A×1, T×2.** Тот же набор, что на ветке
спеки; отличается только число в B (там 1069/344, здесь 1120/395).

---

## 2. Пошаговый план по каждому красному шагу

Проверено симуляцией: план применён к песочнице-копии корпуса `d29b9ee`, гейт прогнан
СВОИМ кодом. Результат — §4.

### Шаг M (5 красных) — перенос пяти секций `testing.md` → профиль architect'а

Периметр — сплошной блок `testing.md:66–159` (94 строки), пять секций подряд:

| секция | строки | размер |
|---|---:|---:|
| `## Дегенерированный вход обязателен — фикстура «счастливого пути» есть дефект оракула` | 66–91 | 26 |
| `## Форма прода снимается ЗАМЕРОМ, а не воображается` | 92–110 | 19 |
| `## Оракул обязан мерить ТО, ЧТО ОБЕЩАЕТ` | 111–125 | 15 |
| `## Целостность гейта — 4 свойства` | 126–142 | 17 |
| `## RED сравнения ДВУХ источников обязан моделировать live-режим` | 143–159 | 17 |

Приёмник — `.claude/agents/architect.md` (80 строк на `d29b9ee`), в конец, отдельным
разделом. Перенос **move-only**: спека §6 запрещает менять смысл при переносе, шаг M
сверяет нормализованное вхождение ключевой строки в обеих половинах.

**Ловушка, проверенная мутацией (см. §4, М-2): указатель, оставляемый в `testing.md`, НЕ
СМЕЕТ цитировать заголовки переехавших секций дословно** — шаг M ищет их отсутствие в
`testing.md` и краснеет именно на цитате. Формулировка указателя без запретных строк:

```
## Чек-листы конструирования оракулов — в профиле architect'а

Оракулы пишет ТОЛЬКО architect (`scope-guard.md`: `*/tests/**` sacred). Пять чек-листов
конструирования переехали в `.claude/agents/architect.md` — там их единственный носитель.
```

**Ссылки, которые обязана обновить эта же правка** (шаг M их НЕ ловит — он читает только
`testing.md` и профиль):

- `gates.md:460` — cross-reference `\`.claude/rules/testing.md\` — детали RED-first
  дисциплины + **«Целостность гейта — 4 свойства»**`. После переноса это ВИСЯЧАЯ ссылка;
  ни один шаг гейта её не покрасит. Перенаправить на `.claude/agents/architect.md`.
- `gates.md:148` — ссылка на `testing.md` §«Канарейка ops-пути…». **Уже висячая ДО чистки:**
  секции с таким заголовком в `testing.md` нет, реальная называется
  `## Механизм несущего пути обязан иметь оракул точки входа` (строка 182). Дефект не
  создан чисткой, но чистка — момент, когда он в руках.

Итог по файлу: `testing.md` 224 → 135 (−94 вырезано, +5 указатель).

### Шаг Dd (1 красный) — дедуп замера «782 MB / 105 GB»

Три вхождения в ядре, все убрать; канонический носитель `scripts/gc_worktrees.sh` не
трогать (шаг Dd проверяет ОБЕ стороны — мутация М-3).

| файл:строка | контекст | что делать |
|---|---|---|
| `handoff-block.md:66` | п.6 «Кэш сборки убран», хвост абзаца 61–67 | сжать 61–67 → 4 строки; норма «кэш убран» (строка 62) обязана остаться |
| `branch-hygiene.md:92` | §Worktree lifecycle п.4 | входит в правку §Worktree lifecycle ниже — НЕ считать дважды |
| `gates.md:279` | §8 п.4 Worktree-GC, абзац 276–281 | сжать 276–281 → 3 строки |

### Шаг A (1 красный) — архив вырезанного

Создать `docs/archive/rules-history-2026-08.md`, непустой. Приёмник ВСЕГО вырезанного
нарратива (спека §6: «вырезанное переезжает, а не исчезает»). Вне startup-пути, в бюджет
не считается. Шаг A проверяет `-s` — пустой файл красен (мутация М-4).

### Шаг T (2 красных) — строки G4/G5 в шаблонах `handoff-block.md`

`COGNITIVE-ONLY`, носитель — шаблон, барьера нет и он не изображается.

- **G4**, в шаблон §D (после строки 33, рядом с push-статусом), ≤1 строки:
  `- Push-статус: AHEAD=$(git log origin/<ветка>..HEAD --oneline | wc -l) = <N>, SHA <sha>`
  Шаг T ищет подстроку `AHEAD=`.
- **G5**, в шаблон §B (после строки 25), ≤1 строки:
  `- критик: REQUIRED (<триггер gates.md §1 поимённо>) | SKIPPED (ни один триггер не сработал)`
  Шаг T ищет подстроку `SKIPPED`.

Обе строки — ЧИСТОЕ ДОБАВЛЕНИЕ (+2 к бюджету), их надо заложить в арифметику B.

### Шаг B (2 красных) — бюджет

**`CLAUDE.md` закрывается, `.claude/rules/*.md` — НЕТ.** Разбор — §3 и §5.

- `CLAUDE.md` 100 → 70: сходится ровно, без запаса (симуляция: `PASS B CLAUDE.md = 70`).
- `.claude/rules` 1120 → 917 при полном исполнении плана. **Бюджет 725 не достигается:
  недобор 192 строки.**

---

## 3. Таблица «секция → судьба → приёмник → строк»

Знак «−» — вырезано/сжато, «+» — добавлено. Номера строк — `d29b9ee`.

### `gates.md` 466 → 427 (−39)

| секция, строки | содержимое | судьба | приёмник | строк |
|---|---|---|---|---:|
| §0, 9–14 | нарратив «на M-45 было ТРИ REJECT подряд» | сжать до 1 (норма «architect — СТОРОНА, а не судья» остаётся) | архив | −5 |
| §0, 33–34 | «на M-45 и на CT-RFC-07 rev1 так и было» | сжать до 1 | архив | −1 |
| §1, 82–88 | обоснование RAW-гейта (асимметрия горизонтов) | сжать до 3 | архив | −4 |
| §4, 158–162 | инцидент M-49 (300k токенов) | вырезать целиком | архив | −5 |
| §4, 164–166 | «`R-031`: M-32/33/34 уехали в прод» | сжать до 2 | архив | −1 |
| §6, 223–233 | эпохи ledger'а `TD-015`, разбор | сжать до 4 | архив | −7 |
| §8, 266–274 | нарративы `TD-011`/`TD-021`/`TD-031` | сжать до 5 | архив | −4 |
| §8, 276–281 | Worktree-GC + замер 782 MB / 105 GB | сжать до 3 | архив + `gc_worktrees.sh` | −3 |
| §9, 380–381 | замер зоны `R-047` «110 из 625» | вырезать | архив | −2 |
| §9, 399–403 | «за один день 11.08 сработал ТРИЖДЫ» + три ссылки | сжать до 1; **рез по ПРЕДЛОЖЕНИЮ, не по строке** — 399 начинается нормой 15.08, 403 заканчивается нормой «ветка удаляется тем, кто мержил» | архив | −4 |
| §9, 408–409 | «вердикт вернулся пустым ДВАЖДЫ (`R-047`)» | вырезать | архив | −2 |
| §9, 460 | cross-ref на переехавшую секцию | ПЕРЕНАПРАВИТЬ на профиль | — | 0 |
| §4, 148 | висячая ссылка «Канарейка ops-пути…» | исправить на реальный заголовок | — | 0 |

### `testing.md` 224 → 135 (−89)

| секция, строки | судьба | приёмник | строк |
|---|---|---|---:|
| 66–159, пять секций конструирования оракулов | ПЕРЕНОС move-only | `.claude/agents/architect.md` | −94 |
| указатель на новый носитель | добавить (без дословных заголовков!) | — | +5 |

### `handoff-block.md` 118 → 82 (−36)

| секция, строки | судьба | приёмник | строк |
|---|---|---|---:|
| п.6, 61–67 (кэш; замер 782 MB на 66) | сжать до 4; норма «кэш убран» (62) остаётся | архив | −3 |
| `## Пример (dev → tester)`, 78–112 | вырезать целиком (форма описана текстом выше) | архив | −35 |
| шаблоны §B/§D | добавить строки G5/G4 | — | +2 |

### `branch-hygiene.md` 108 → 83 (−25)

| секция, строки | судьба | приёмник | строк |
|---|---|---|---:|
| §Правила п.6, 25–29 | сжать до 3 (замер «все 14 worktree» → архив; норма «метка в subject'е» на 25 остаётся) | архив | −2 |
| §Правила п.9, 35–44 | сжать до 7 (`870ab9d` 188 строк, `91530de`/`R-028` → архив) | архив | −3 |
| §Коммит-пути, 48–63 | сжать до 14 (пределы `R-062` остаются) | архив | −2 |
| §Worktree lifecycle, 64–104 | сжать до 23: «61 worktree × 3.5 GB ≈ 190 GB» и разбор `--reclaim` → архив; нормы п.1–п.6 остаются | архив | −18 |

### `commit-discipline.md` 119 → 105 (−14)

| секция, строки | судьба | приёмник | строк |
|---|---|---|---:|
| §Роль в коммите, 23–35 | сжать до 8 (замер «все 14 worktree», `R-031` §C.3 — дубль `branch-hygiene` п.6) | архив | −5 |
| пример Done Block, 42–60 | сжать до 15 | — | −4 |
| «Сырой ≠ ВЕСЬ», 63–77 | сжать до 11 (инцидент tester'а 2026-07-14 → архив; **норма остаётся**) | архив | −4 |
| §Что нарушение выглядит как, 106–114 | сжать до 8 | — | −1 |
| §Auto-push, 79–105 | **НЕ ТРОГАТЬ** — PR-флоу 15.08 | — | 0 |

### `scope-guard.md` 85 → 85 (0)

Спека §6 запрещает: контрольный образец храповика (0 % роста за месяц), полный по
содержанию. Только обновление ссылок, если появятся.

### `CLAUDE.md` 100 → 70 (−30)

| секция, строки | судьба | приёмник | строк |
|---|---|---|---:|
| 9–11, преамбула startup-протокола | сжать до 1 | — | −2 |
| 23–29, врезка про `ORCHESTRATION-STATE` | вырезать целиком (журнал закрытого эксперимента) | архив | −7 |
| 31–39, «Для КАЖДОГО запускаемого субагента» | сжать до 3 (норма «мандат начинается со Startup-протокола» остаётся) | архив | −6 |
| 41–46, «Почему это вынесено в начало файла» | сжать до 1 | архив | −5 |
| 48–69, операционные принципы | сжать до 18 (нормы `RiskApproved` 57 и «LLM НЕ в горячем цикле» 59 остаются) | — | −4 |
| 82–88, scope-guard кратко | сжать до 4 (полное — в `scope-guard.md`) | — | −3 |
| 94–100, cross-references | сжать до 4 | — | −3 |
| 71–80, таблица маршрутизации моделей | **НЕ ТРОГАТЬ** — норма §5 сохранности | — | 0 |

### Свод

| файл | было | станет | дельта |
|---|---:|---:|---:|
| `gates.md` | 466 | 427 | −39 |
| `testing.md` | 224 | 135 | −89 |
| `handoff-block.md` | 118 | 82 | −36 |
| `branch-hygiene.md` | 108 | 83 | −25 |
| `commit-discipline.md` | 119 | 105 | −14 |
| `scope-guard.md` | 85 | 85 | 0 |
| **rules итого** | **1120** | **917** | **−203** |
| `CLAUDE.md` | 100 | 70 | −30 |

---

## 4. Проверка плана прогоном: симуляция + мутационный контроль

План применён к песочнице — копии корпуса `d29b9ee` в схеме репозитория
(`.claude/rules/`, `.claude/agents/`, `CLAUDE.md`, `docs/archive/`, `scripts/gc_worktrees.sh`)
— и гейт прогнан СВОИМ кодом, без переписывания его логики.

**Setup-guard:** песочница до правок воспроизвела ровно те же красные, что живое дерево
(B 1120/395, M×5, Dd, A, T×2) — значит она не суррогат.

**После применения плана:**

```
PASS  B CLAUDE.md = 70 строк (бюджет 70)
FAIL  B .claude/rules = 917 строк, бюджет 725 — превышение на 192
PASS  D все 40 контрольных норм на месте
PASS  M «Дегенерированный вход обязателен» переехала: в профиле есть, в testing.md нет
PASS  M «Форма прода снимается ЗАМЕРОМ» переехала: в профиле есть, в testing.md нет
PASS  M «мерить ТО, ЧТО ОБЕЩАЕТ» переехала: в профиле есть, в testing.md нет
PASS  M «Целостность гейта — 4 свойства» переехала: в профиле есть, в testing.md нет
PASS  M «RED сравнения ДВУХ источников» переехала: в профиле есть, в testing.md нет
PASS  Dd замер живёт только в комментарии gc_worktrees.sh
PASS  Dd канонический носитель замера (gc_worktrees.sh) существует
PASS  A docs/archive/rules-history-2026-08.md существует и непуст
PASS  T G4: предикат AHEAD= в шаблоне §D
PASS  T G5: строка REQUIRED|SKIPPED в шаблоне §B
```

Шаги `S`/`P`/`Q`/`CI` в песочнице краснеют по построению (она не git-репозиторий и не несёт
`scripts/tests/`); на живом дереве они зелёные — прогон §1.3. Это артефакт песочницы,
а не находка.

**Мутационный контроль — гейт краснеет против подделки в каждом направлении:**

| мутация | шаг | результат |
|---|---|---|
| М-1: секции СКОПИРОВАНЫ (остались и в `testing.md`) | M | `FAIL M «…» СКОПИРОВАНА, а не перенесена — два носителя разойдутся` ×5 |
| М-2: указатель цитирует 2 из 5 заголовков дословно | M | `FAIL` ровно на этих двух, `PASS` на трёх — частичный отказ |
| М-3: замер вычищен заодно из `gc_worktrees.sh` | Dd | `FAIL Dd замер исчез И из gc_worktrees.sh — это уже не дедуп, а потеря` |
| М-4: архив заведён пустым | A | `FAIL A архива нет или пуст` |
| М-5: вырезана НОРМА («ветка пушится НЕМЕДЛЕННО») | D | `FAIL D чистка снесла 1 норм(ы) из 40` |
| М-6: бюджет добран сносом `handoff-block.md` | B | `FAIL B-setup предмет ИСЧЕЗ или урезан по составу (файлов правил 5, ожидалось 6)` |
| М-7: строки G4/G5 не добавлены | T | `FAIL T G4: предиката AHEAD= нет` + `FAIL T G5` |

---

## 5. Риски шага D — что он ловит и чего НЕ ловит

Все 40 контрольных фраз локализованы на `d29b9ee` (нормализация повторяет гейт:
переносы → пробелы, схлопывание). **Ни одна не лежит внутри планируемого к вырезанию
региона** — план не роняет D, и симуляция это подтвердила (`D 40/40 PASS`).

### 5.1. Фразы на ГРАНИЦЕ реза — правило удержания головы

Восемь фраз стоят первой строкой сжимаемого региона или вплотную к нему. Для КАЖДОГО
сжатия «N → K строк» удерживаются K строк ОТ НАЧАЛА региона: во всех этих случаях норма
стоит в первом предложении, а история — в хвосте.

| фраза | file:line | регион правки | зазор |
|---|---|---|---|
| «Сырой ≠ ВЕСЬ» | `commit-discipline.md:63` | 63–77 | ПЕРВАЯ строка региона |
| «commit -a\` / \`add -A\` запрещены» | `branch-hygiene.md:48` | 48–63 | ПЕРВАЯ строка региона |
| «Индекс проверяется ДО коммита, диф — ПОСЛЕ» | `branch-hygiene.md:35` | 35–44 | ПЕРВАЯ строка региона |
| «меткой в конце subject» | `branch-hygiene.md:25` | 25–29 | ПЕРВАЯ строка региона |
| «кэш убран» | `handoff-block.md:62` | 61–67 | вторая строка региона |
| «RiskApproved» / «LLM НЕ в горячем торговом цикле» | `CLAUDE.md:57` / `:59` | 48–69 | внутри региона |
| «ветка пушится НЕМЕДЛЕННО» | `gates.md:393` | 399–403 | 6 строк до реза |
| «Push — не конец цикла» | `gates.md:256` | 266–274 | 10 строк до реза |

### 5.2. Слепое пятно №1: шаг D — греп по СКЛЕЕННОМУ корпусу, не пофайловый

`CORPUS="$(cat .claude/rules/*.md CLAUDE.md …)"` — фраза, выжившая ГДЕ УГОДНО, засчитывается
за всех. Значит удаление нормы из файла, которому она принадлежит, D НЕ покрасит, если она
дублируется в другом:

| фраза | вхождения |
|---|---|
| «COGNITIVE-ONLY» | `gates.md:410,440,442` + **`branch-hygiene.md:82`** (внутри региона 64–104!) |
| «--merge-preview origin/main» | `gates.md:315`, `gates.md:340` |
| «по КОДУ ВОЗВРАТА, а не по тексту вывода» | `gates.md:112`, `gates.md:306` |
| «RiskApproved» | `gates.md:98`, `testing.md:214`, `CLAUDE.md:57` |
| «меткой в конце subject» | `commit-discipline.md:26`, `branch-hygiene.md:25` |
| «последняя секция ответа» | `handoff-block.md:6`, `handoff-block.md:48` |
| «GREEN против заглушки» | `gates.md:97`, `testing.md:29` |

Практический вывод: `branch-hygiene.md:82` (`COGNITIVE-ONLY` у п.3 Worktree lifecycle) лежит
ровно в самом крупном сжимаемом регионе плана и удерживается РУКАМИ — гейт его потерю
пропустит.

### 5.3. Слепое пятно №2 (главное): норм в корпусе БОЛЬШЕ сорока

Симуляция сделана МЕХАНИЧЕСКИ («сжать регион N→K, удержать голову»). Шаг D дал 40/40
зелёных — и при этом из корпуса выпали три нормы:

| норма | было | стало | policed шагом D? |
|---|---:|---:|---|
| «ветка предмета удаляется тем, кто мержил» (`gates.md` §9) | 1 | **0** | нет |
| «Память меряется как `RssAnon`, а не `docker stats`» (`gates.md` §8) | 1 | **0** | нет |
| «смешение эпох = KILL» (`gates.md` §6) | 1 | **0** | нет |

**Это не дефект симуляции, а измеренное свойство гейта: D пиннит 40 фраз, а корпус несёт
норм в разы больше.** Отсюда следует режим исполнения:

1. Чистка — **редакторская построчная работа, а не механическое удаление регионов.**
   Каждое «сжать N → K» держит норму первого предложения и убирает ссылку/инцидент/замер.
2. Таблица сохранности спеки §5 сверяется ПОСЕКЦИОННО руками; зелёный D — необходимое
   условие, не достаточное (ровно как B без D, спека §7: «по отдельности каждая половина
   ложна»).
3. Три нормы выше — именной чек-лист исполнителя: после чистки предъявить их наличие
   грепом отдельно от прогона гейта.

### 5.4. Ложный ход спеки: §9 и §11 нельзя «слить в одно перечисление»

Спека §3 п.4 предлагает: «перечни зон §9 и §11 даны дважды почти дословно — слить в одно
перечисление со ссылкой». **Замер это опровергает — перечни РАЗНЫЕ, и разница нормативна:**

- §9 (перепроверка Fable), `gates.md:376–379` — 14 членов: `CLAUDE.md` · `.claude/rules/**` ·
  `.claude/agents/**` · `.claude/wrappers/**` · `docs/04-workflow.md` · `docs/DESIGN.md` ·
  `docs/0[0-9]-*.md` · `docs/fa/**` · `milestones/BACKLOG.md` · `docs/PENDING-SIGNATURE.md`
  + харнесс (`scripts/check_*.sh`, `scripts/verify_*.sh`, `scripts/tests/**`,
  `.github/workflows/**`).
- §11 (замок `FOUNDER-APPROVED`), `gates.md:422–423` — **5 членов**: `.claude/rules/**` ·
  `.claude/agents/**` · `.claude/wrappers/**` · `CLAUDE.md` · `docs/04-workflow.md`.

Зона §11 — строгое подмножество §9, и она задана НЕ текстом, а барьером:
`scripts/check_docs_freeze.sh:63` — `case: .claude/rules/*|.claude/agents/*|.claude/wrappers/*|CLAUDE.md|docs/04-workflow.md`.
Слияние перечней либо расширит текст замка за пределы того, что барьер реально ловит (текст
обещает больше механизма — ровно то, что `gates.md` §11 запрещает изображать), либо сузит
зону перепроверки §9. **Экономия здесь — 0 строк, а цена — расхождение текста с барьером.**
Пункт из плана исключён; в §3 вместо него взяты нарративы §9 (380–381, 399–403, 408–409).

---

## 6. Чего чистка НЕ КАСАЕТСЯ — свежие нормы 15.08

Это не история: нормы моложе суток, введены замером и вердиктом `R-085`, и часть из них
описывает механизм, ставший БАРЬЕРОМ (branch protection). Резать их — не чистка, а откат.

| файл, строки | норма | почему нельзя |
|---|---|---|
| `gates.md:290–319` | «В `main` попадает только SHA с зелёным `All checks passed`»; форма `gh pr create` → `gh pr checks --watch` → `gh pr merge`; предел `strict: false` | описывает действующий барьер; без неё роли предписан невозможный прямой push |
| `gates.md:356–359` | «автор приземляет сам, СВОИМ PR» (замена «self-push автора») | без неё у мелкой doc-правки не остаётся разрешённого исполнителя (`R-085` B-1) |
| `gates.md:394–398` | «после перепроверки ветка вливается в `main` СВОИМ PR» (снятие «fast-forward») | `R-085` B-2: §9 иначе противоречит §8 |
| `gates.md:430–441` | §11 «Но merge он теперь БЛОКИРУЕТ» + названный предел (подделываемость токена) | зафиксирован переход `COGNITIVE-ONLY` → барьер |
| `commit-discipline.md:90–100` | PR-флоу в §Auto-push, п.4/п.5 | то же основание |
| `docs/workflow/harness-track.md`, `.claude/skills/harness-track/SKILL.md` | харнесс-трек | вне ядра, шагом B не считаются |

Отдельно: `gates.md` §0.1 (граница C) и §5 (RISK-BLOCK) — запрет спеки §6, сверх ссылок не
трогать. `scope-guard.md` — контрольный образец, не трогать.

---

## 7. Вывод: сколько реально надо вырезать и что закроется

### 7.1. Закрывается сегодня

| правка | закрывает | остаток |
|---|---|---|
| перенос `testing.md:66–159` → профиль + указатель без цитат + правка `gates.md:460`/`:148` | **M — все 5 красных** | — |
| убрать замер из `handoff-block.md:66`, `branch-hygiene.md:92`, `gates.md:279` | **Dd** | — |
| создать непустой `docs/archive/rules-history-2026-08.md` | **A** | — |
| строки G4/G5 в шаблоны §D/§B `handoff-block.md` | **T — оба красных** | — |
| `CLAUDE.md` 100 → 70 (§3) | **половина B** | сходится ровно, без запаса |
| весь §3 целиком | половина B по `rules` — **НЕ закрывает** | 917 против 725 |

Итого: **11 красных процессных шагов → 1.** Останется один: `B .claude/rules`.

### 7.2. Сколько строк

- **По плану §3 из `.claude/rules/*.md` вырезается 210 строк и добавляется 7** (+5 указатель
  в `testing.md`, +2 строки G4/G5) — **нетто −203**. `CLAUDE.md` — нетто **−30**.
  Всего из впрыскиваемого ядра **−233** (1220 → 987).
- **Требуется по бюджету: −425.**
- **Недобор: 192 строки по `.claude/rules/*.md`.** `CLAUDE.md` сходится.

### 7.3. Что делать с недобором — по спеке, а не импровизацией

Спека §3 (хвост) и §6 предписывают исход прямо: «недобор добирается ТОЛЬКО историей и
дублями, найденными при исполнении; **не сходится ⇒ менять цель через founder'а (ревизия
`A-003`), не резать нормы** (`C-064` F-064-8)».

Замер говорит, что не сходится, и называет причину: **истории в корпусе осталось на ~205
строк, а бюджет требует 425.** Оставшиеся 192 строки — это нормы, а не нарратив; в
`gates.md` из 466 строк на историю приходится 39 (8 %), остальное — действующие правила,
причём +44 строки нормы приехали 15.08 и растут дальше.

Три развязки, решение — founder (граница «чего мы ХОТИМ», `gates.md` §0.1):

1. **Ревизия `A-003`** — поднять бюджет `rules` с 725 до достижимого (917 при этом плане).
   Риск, названный честно: бюджет, снятый с раздутого корпуса, узаконивает его — это
   ровно дефект К-5, из-за которого спека §2 п.1 поставила чистку ПЕРЕД G1.
2. **Расширить перенос-по-роли** (тот же приём, что дал −94 на `testing.md`): вынести в
   профили ролей секции, действовать по которым остальным восьми ролям запрещено
   scope-guard'ом. Кандидат №1 — `gates.md` §6 (анти-оверфит, 31 строка): исполняют его
   risk-critic и research-dev, впрыскивается девяти. Кандидат №2 — §5 RISK-BLOCK (24
   строки), но спека §6 запрещает его трогать сверх ссылок ⇒ нужно снятие запрета.
   Это НЕ сокращение норм — это смена носителя, и мерить его надо шагом M-образным
   («появилось там И исчезло здесь»), иначе получаются две расходящиеся копии.
3. **Признать бюджет двухуровневым:** `A-003` считал ядро, впрыскиваемое ВСЕМ; после
   переноса в профили корпус перестал быть однородным. Тогда цель — не «725 строк
   `.claude/rules`», а «≤N строк на РОЛЬ», и её надо переформулировать до G1, иначе G1
   зафиксирует лимит от величины, которую больше никто не читает целиком.

Пока решение не принято, **milestone не может закрыться зелёным гейтом**: шаг B красен по
арифметике, а обходить его правкой `RULES_BUDGET` в `verify_M-60c.sh` запрещено — файл
sacred, и шапка гейта называет такую правку «дефектом класса анти-плацебо, не фиксом».
