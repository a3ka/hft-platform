<!-- FACTS: audited_head=91a4b84d6f8a71760767e9ca3cfa9a0d1d27fa23 collected=2026-09-07 -->

# Аудит фаз — где мы РЕАЛЬНО по дорожной карте (2026-09-07)

Метод: колонка «Статус» в milestone-файлах и `PROJECT-STATE.md` НЕ источник (см. находки
ниже — она несколько раз прямо противоречит фактам). Каждое утверждение снято командой,
прогнанной в рамках этой же сессии. Канон фаз — `docs/DESIGN.md` §10 (прочитан целиком,
строки 279–678); рабочая декомпозиция — `docs/09-roadmap-v2.md` (прочитан целиком).

---

## Ответ на вопрос 1 — какая фаза РЕАЛЬНО в работе

**Формально, по решению founder'а `П-018(б)` (2026-08-25, дословно «нет, вначале закрываем
предыдущую фазу»): Ф0 «Необратимость данных» — единственная фаза, которая ДОЛЖНА быть в
работе**, Ф1 не открывается до закрытия ворот Ф0(а).

**Фактически на 2026-09-07 работа идёт одновременно в Ф0, Ф1 и Ф2**, и это не домысел, а
видно по датам мержа:

```
$ git log -1 --format='%h %ad %s' --date=short -- docs/PENDING-SIGNATURE.md
```
(последняя запись индекса — `П-027`, 2026-08-31, включение полос глубины Ф2/Ф4)

```
$ git log --oneline -3 --grep='M-77'
d0a1109 spec(M-77): дефект main выделен в предмет — кадр строится БЕЗ книги, задет ПРОД-путь [architect]
```
`M-77` (Ф2/Ф4, книго-зависимые серии кадра) закрыт мержем `b008f5e` (PR #167) **СЕГОДНЯ,
2026-09-07** — это видно и по live git HEAD на проде (см. ниже), и по `TD-199` в
`TECH-DEBT.md`: `~~TD-199~~ ✅ ЗАКРЫТ 2026-09-07`.

Это разрешено самим же `П-018(б)`: там прямо сказано, что `M-68`/`M-70` (депт-серии/полосы,
предмет Ф2) **предусловием Ф0(а) не связаны** — они не расширяют состав записываемых данных.
Но `M-45`→`П-026` (2026-08-31, расширение состава L2Delta-записи на `ETHUSDT` спот+фьючерсы)
— это уже explicitly Ф1-предмет («данные продукта»), и он раскатан ПОСЛЕ подписи `П-018(б)`,
при НЕ закрытых воротах Ф0(а). Формальный порядок «Ф0 закрывается первой» де-факто нарушен
для Ф1, и это не находка о качестве кода — это факт последовательности решений, который
architect обязан назвать, а не смолчать.

**Вывод:** называть текущей фазой одну Ф0 — неточно. Правильнее: **Ф0 не закрыта и держит
формальный вход в Ф1/Ф2, но инженерная работа фактически ведётся по всем трём** (Ф0
хвостами, Ф1 расширением состава, Ф2 — активнее всего: 5 milestone'ов дошли до прода за
последние 10 дней — `M-68`, `M-71`, `M-75`, `M-77`, депт-band'ы). Единственная фаза, где
работа НЕ идёт вообще, — Ф3 и далее (см. §Ф3+ ниже, нулевой инженерный след).

---

## Ф0 — Необратимость данных

Ворота по `docs/09-roadmap-v2.md` Ф0 §Acceptance (дословно): (а) restore-drill журнала,
(б) алерт вне машины при инъецированном отказе, (в) ребут-дрилл, (г) venue-hl suite GREEN.

| ворота | выполнено | команда-доказательство | что держит |
|---|---|---|---|
| (а) restore-drill: журнал восстановлен из offsite-копии, ПРОВЕРЕНА ЧИТАЕМОСТЬ | **НЕТ** | см. блок A ниже | drill не назначен; owner — architect (спека дрилла) + founder (доступ/окно на проде) |
| (г) venue-hyperliquid RED-суита GREEN | **ДА** | см. блок D ниже | закрыто, не пересчитывается |
| (б) push-алертинг вне машины, доставлен при инъецированном отказе | **НЕТ** | см. блок B ниже | подключение к несущему пути (cron/systemd/compose) — инженерная работа; токены — после |
| (в) ребут-дрилл (VPS ребутнулся → само поднялось → алерты подтвердили) | **НЕТ** | см. блок C ниже | не назначен, не проводился ни разу; зависит от (б) (иначе подтверждать нечем) |

**Блок A — offsite-копия ЕСТЬ и растёт, restore-drill НЕТ:**
```
$ ssh -i /home/nous/.ssh/hft_deploy root@167.233.192.131 'cat /var/lib/hft/journal-offsite.last-success'
2026-09-07T21:22:03Z

$ ssh ... 'ls -la /var/lib/hft/journal-offsite.alert'
ls: cannot access '/var/lib/hft/journal-offsite.alert': No such file or directory

$ ssh ... 'crontab -l'   # (root; фактически задачи в /etc/cron.d)
no crontab for root
$ ssh ... 'ls /etc/cron.d/'
e2scrub_all  hft-builder-prune  hft-journal-offsite  hft-journal-retention
$ ssh ... 'cat /etc/cron.d/hft-journal-offsite' | tail -3
22 * * * * root /root/hft-platform/deploy/bin/journal-offsite-cron.sh

$ grep -n 'TD-193' TECH-DEBT.md
132:| TD-193 | OPS-I-3 (холодная копия периодически ВОССТАНАВЛИВАЕТСЯ и читается — drill)
    не выполнялся НИ РАЗУ. После M-73 OPS-I-2 выполнен по букве (85 ГБ на приёмнике, копия
    по расписанию), и это делает пробел ОПАСНЕЕ, а не безопаснее ... | Ф0 | MAJOR |

$ grep -n 'TD-020.*OPEN\|TD-020 остаётся OPEN' TECH-DEBT.md
3679:  никем». TD-020 остаётся OPEN до первого успешного `apply` со Storage Box.
```
Копия работает по расписанию (раз в час, `:22`), покрывает 583 файла / ~106 ГБ по данным
последнего rsync-прогона, приёмник (Hetzner Storage Box) держит 207 ГБ суммарно
(`ssh ... storagebox 'du -sh journal'` → `207G journal`). **Но копия ни разу не была
скачана обратно и прогнана через restore + replay** — это ровно то, что превращает
«копию» в «бэкап» (`DESIGN` §23.1 п.4), и этого шага не было. `TD-020`/`TD-193` оба OPEN.

**Блок B — push-алертинг: код есть, ЗАПУСКАТЕЛЯ НЕТ (не «ждёт токена»):**
```
$ ls crates/ops/src/bin/
ops-watchdog.rs

$ grep -c 'TELEGRAM_BOT_TOKEN\|TELEGRAM_CHAT_ID' crates/ops/src/transport.rs
8

$ grep -n 'ops-watchdog\|watchdog' docker-compose.yml
(нет совпадений)

$ ssh ... 'ls /etc/cron.d/' | grep -i watchdog
(нет совпадений — только offsite/retention/builder-prune выше)

$ ssh ... 'systemctl list-timers --all' | grep -i hft
(нет совпадений)

$ ssh ... 'docker ps --format "{{.Names}} {{.Status}}"'
hft-gateway-serve Up 8 hours (healthy)
hft-recorder Up 8 hours (healthy)
```
Ровно два прод-контейнера, ни один не watchdog. Бинарь `ops-watchdog` собирается из
исходников, но НИЧЕМ не запускается на проде — ни cron-строкой, ни systemd-таймером, ни
сервисом в `docker-compose.yml`. Это класс «построено-не-подключено» (`gates.md` §4, DoD),
и именно так это названо в `docs/PENDING-SIGNATURE.md П-018(б)` (2026-08-25) — прежняя
формулировка «ждём токен от founder'а» (`П-003`, 2026-08-02) описывает только половину
дефекта и сама протухла: запускателя не существует, выдать токены сейчас было бы закрытием
ворот на бумаге.

**Блок C — ребут-дрилл:**
```
$ grep -rn 'ребут-дрилл\|reboot.dril' TECH-DEBT.md PROJECT-STATE.md milestones/M-43-ops-hardening.md
(нет совпадений — ни отдельной карточки долга, ни записи о проведении)

$ grep -n 'Статус\|STATUS' milestones/M-43-ops-hardening.md
(строка отсутствует — заголовок милестоуна: "Статус: PLANNED (стаб)")
```
`M-43-ops-hardening.md` (несёт задачи mem_limit/порт/watchdog-cron) остаётся файлом-стабом
с 2026-07: `head -3` файла показывает «PLANNED (стаб)» без единого выполненного пункта
§Tasks (таблицы задач с отметками в файле нет вообще). Работа по факту происходила, но НЕ
под этим milestone'ом, а под `M-73`/`П-023`/`П-026` — то есть сам milestone-файл Ф0 (в) не
отражает вообще ничего из происходящего.

**Блок D — venue-hyperliquid RED-суита, закрыто:**
```
$ ls docs/archive/ | grep M-41
M-41-venue-hyperliquid-tests.md
docs/archive/verify_M-41.sh

$ ls crates/venue-hyperliquid/tests/
red_fail_closed_values.rs  red_malformed_envelope.rs  red_parse_l2book.rs
red_parse_trades.rs        red_provenance_md_only.rs

$ cargo test -p venue-hyperliquid 2>&1 | grep -E '^test result'
test result: ok. 7 passed; 0 failed ...   (несколько блоков, все ok)
```
40 оракулов, milestone заархивирован, ✅ закрыт, не пересчитывается.

**Отдельная находка — M-43 (compose mem_limit/порт) НЕ СДЕЛАН вообще:**
```
$ grep -c 'mem_limit\|deploy:\|resources:' docker-compose.yml
0
```
Ни одного сервиса в compose не имеет ресурс-лимита (задача (а) M-43). `gateway-serve` порт
по-прежнему на `127.0.0.1:8080` (loopback, задача (б) M-43) — реверс-прокси/туннель не
заведён никаким кодом в репозитории (только упоминание в прозе `deploy/README.md:303`).

---

## Ф1 — Данные продукта (L2Delta)

Ворота `09-roadmap-v2.md` Ф1: L2Delta пишется для всех символов кокпита; book из
(якорь+диффы) ≡ book из снапшотов; malformed diff → fail-closed; провенанс сохранён; ИНВЕРСИЯ
ролей снапшот↔дифф (`DESIGN` §17 п.2).

| ворота | выполнено | команда-доказательство | что держит |
|---|---|---|---|
| L2Delta пишется для BTCUSDT + ETHUSDT (спот+фьючерсы Binance) | **ДА, частично** — только 2 инструмента из «всех символов кокпита» | блок E | HL (`HL_COINS`) диффами не пишется — только Binance |
| Malformed diff → fail-closed | **ДА** | наследуется из M-18/M-45 (53/53 оракула GREEN по `П-026`) | — |
| **Инверсия ролей снапшот↔дифф** (`DESIGN` §17 п.2, обязательное предусловие продукта) | **НЕТ** | блок F | не начата вообще — ни спеки, ни кода |
| Перемер объёма после инверсии | **НЕТ** (инверсии нет — мерить нечего) | — | зависит от инверсии |

**Блок E — состав записи сегодня:**
```
$ grep -n 'L2DELTA_CAPTURE_SYMBOLS\|EPOCH_ID' docker-compose.yml
L2DELTA_CAPTURE_SYMBOLS: BTCUSDT,ETHUSDT
EPOCH_ID: own-2026-09-m45-ethusdt
```
Подписано `П-026` (2026-08-31, founder дословно: «вопросы по 45 и 70 реши с фейблом. мне
неважно как, главное, чтобы работало» — трактовано architect'ом как согласие на вариант
«включить и на споте, и на фьючерсах», что и сделано).

**Блок F — инверсия ролей НЕ СДЕЛАНА (проверено замером опорной точки задания):**
```
$ grep -n 'const EMIT_PERIOD' crates/venue-binance/src/lib.rs
35:const EMIT_PERIOD: Duration = Duration::from_secs(1);
```
Снапшот по-прежнему эмитится **раз в секунду** (константа в коде, не 10–30-секундный редкий
якорь, как предписывает `DESIGN` §17 п.2). Т.е. сегодня L2Delta — ДОПОЛНИТЕЛЬНЫЙ поток
поверх секундных снимков, а не наоборот, как задумано архитектурой продукта.
```
$ grep -n 'инверси' TECH-DEBT.md
3891: ... вместе с ростом честности данных, ради которого инверсия и делается ...
3923: Срочность растёт со временем. DESIGN §17 предписывает инверсию ролей L2Delta/L2Snapshot ...
```
`TECH-DEBT.md` (запись у `TD-199`/`TD-200`, добавлена architect'ом 2026-09-04) сама называет
это прямо: пока якорь идёт 1 Гц, класс дефектов «кадр без якорного снимка в окне» встречается
редко; **после инверсии её частота вырастет на порядок** — то есть откладывание инверсии не
снижает риск, а копит его на потом. Это прямое подтверждение опорной точки задания: инверсия
не сделана, и цена задержки растёт со временем, а не падает.

**Что это значит для стоимости долга (вопрос задания про «несколько долгов»):** `TD-200`
(стоимость `pump` растёт с размером книги, MAJOR) и класс кадра-без-якоря (закрыт `M-77`
сегодня, но именно потому, что якорь редкий) — оба явно указывают инверсию как усилитель
severity. `TD-201` (сторож `book_series` слабее заявленного) — тоже Ф2, но в том же узле кода.

---

## Ф2 — Read-path под SaaS

Порядок работ и статус объявлены `DESIGN` §16.2 восемью шагами. Проверка каждого — по коду,
не по таблице (таблица в `DESIGN.md` сама не обновлялась с 2026-08-хх и часть шагов уже
обогнала реальность в обе стороны: шаг 3 в доке помечен «⏳ следующий», но по факту
milestone на него смёржен и на проде — просто не достиг цели).

| # | шаг | выполнено | команда-доказательство |
|---|---|---|---|
| 0 | учащение чекпоинта `*/15` | **ДА** | `ssh ... crontab hft-gateway-checkpoint` — строка `*/15 * * * *` живая, см. блок A |
| 1 | подключение читает хвост один раз (M-54) | **ДА, код смёржен**, но НЕ closed по цели | `PROJECT-STATE.md:1328` — «milestone НЕ ЗАКРЫТ; close-out FAIL» |
| 2 | снапшот без клонирования состояния (M-56) | **ДА, код смёржен**, цель НЕ достигнута | `PROJECT-STATE.md:1379` — «§6 критерий НЕ выполнен (R-030 §3)» |
| 3 | устранение per-tick пересканирования (M-57 tail-follower) | **ЧАСТИЧНО** — гейт пройден, механизм на проде, но «ЦЕЛЬ НЕ ДОСТИГНУТА» | `PROJECT-STATE.md:12` дословно |
| 4 | многопоточный рантайм | **НЕТ — мёртвый код** | блок G |
| 5 | RFC на разделяемую проекцию (per-key редьюсеры) | **НЕТ** — RFC про wire-формат прошёл критик-гейт, но shared-tailer/per-key-reducer НЕ реализован | блок H |
| 6 | сжатие на проводе | **НЕТ** | блок I |
| 7 | cursor-resume в `subscribe` | **НЕТ** | блок I |
| 8 | нагрузочный стенд не на проде | **НЕТ** | блок J |

**Блок G — п.4 многопоточность: код есть, НЕ ПОДКЛЮЧЁН (built-not-wired):**
```
$ sed -n '17p;60,65p' crates/gateway-serve/src/main.rs
#[tokio::main(flavor = "current_thread")]
...
#[allow(dead_code)]
fn _runtime_hook() -> tokio::runtime::Runtime {
    tokio::runtime::Builder::new_multi_thread()
        .enable_all()
        .worker_threads(2)
```
Прод-бинарь стартует на `current_thread`-рантайме. Функция, строящая multi-thread рантайм,
помечена `#[allow(dead_code)]` — компилятор сам подтверждает, что она нигде не вызывается.
Это класс «built-not-wired» из профиля architect'а (`gates.md` §4 DoD), найденный именно там,
где задание просило искать специально (вопрос 4).

**Блок H — «разделяемая проекция» (shared-tailer / per-key редьюсер) НЕ реализована:**
```
$ grep -n 'SessionInner {' crates/gateway-serve/src/lib.rs | head -1
695:        let mut inner = SessionInner {

$ sed -n '/pub struct Sub/,/pub live/p' crates/gateway-serve/src/session.rs | head -8
pub struct Sub {
    pub id: String,
    pub selector: Selector,
    pub live: Option<gateway::LiveReducer>,
```
Каждая ПОДПИСКА на КАЖДОМ соединении несёт собственный `gateway::LiveReducer` — то есть
собственный пробег по журналу (`spawn_blocking`-pump). `CT-RFC-09-ws-session.md` («rev2 —
критик-гейт пройден») закрывает ФОРМУ КАДРА и протокол подписки, но не строит один общий
tail-процесс на N клиентов: архитектура «N клиентов = N сканов» из `DESIGN` §16 (R7) СТОИТ
НА МЕСТЕ. Это ядро всей Ф2 и одновременно самый нетронутый её пункт.

**Блок I — сжатие и cursor-resume: не найдены вообще:**
```
$ grep -rn 'compression\|gzip\|zstd\|permessage-deflate' crates/gateway-serve/src/*.rs
(нет совпадений в src/, кроме файла тестов multi_thread — не по теме)

$ grep -rn 'cursor.resume\|CURSOR_RESUME' crates/gateway-serve/ crates/gateway/
(нет совпадений — одна строка комментария-заглушки в тестовом файле)
```

**Блок J — нагрузочный стенд:**
```
$ ls scripts/ | grep -i measure
measure_M-54_connect.sh
$ find . -iname '*loadtest*' -o -iname '*load_test*' | grep -v target
(нет совпадений)
```
Единственный измерительный инструмент — точечный скрипт под M-54 (латентность одного
подключения), не нагрузочный стенд на тысячи сокетов. Арендная машина под стенд
(`DESIGN` §16.2 шаг 8) не заведена — упоминаний в `deploy/**`/CI нет.

**Остаточный факт про Ф2, подтверждающий срочность (TD-097, MAJOR, до сих пор OPEN):**
```
$ grep -n '\*\*TD-097\*\*' TECH-DEBT.md | head -1
83:| **TD-097** | первый экран панели: подключение 1.8–3.8 s при ЛЮБОМ backlog'е — цель M-54
    не достигнута, регресс замерен на проде | Ф2/Ф4 | MAJOR |
```

---

## Ф3–Ф6 — не начаты (проверено, не предположено)

```
$ find . -iname 'package.json' -not -path './node_modules/*'
(нет совпадений)
$ find . -type d -iname 'app' -o -iname 'web' | grep -v target
(нет совпадений)
$ grep -A30 '\[workspace\]' Cargo.toml | grep -E '"crates/'
"crates/contracts" "crates/derive" "crates/journal" "crates/book" "crates/ops"
"crates/venue-binance" "crates/venue-binance-futures" "crates/venue-hyperliquid"
"crates/recorder" "crates/signals" "crates/alpha" "crates/portfolio" "crates/strategy"
"crates/sim" "crates/research-cli" "crates/gateway" "crates/gateway-serve"
(без пропущенных строк — risk/killswitch/oms там тоже нет: DEFERRED, вне зоны этого аудита)
```
Ни одной строки Node.js/Next.js/Postgres-кода в репозитории нет. Ф3 (app-плоскость), Ф4
(терминал), Ф5 (AI Tutor), Ф6 (монетизация) — нулевой инженерный след. Это ожидаемо и не
находка (порядок фаз это и предполагает), но задание требовало установить факт, а не
предполагать его.

---

## Ответ на вопрос 2 — ближайший рубеж и что до него осталось

**Формально ближайший рубеж — закрытие Ф0(а)+(б)+(в).** Список работ:
1. **Restore-drill журнала** (Ф0-а): скачать offsite-копию (или её часть) обратно, поднять
   `journal::open`/replay поверх неё, подтвердить читаемость и целостность. Инженерная спека
   + acceptance-скрипт — работа architect'а; окно на проде и, возможно, доступ к Storage Box
   — founder.
2. **Подключить `ops-watchdog` к несущему пути** (Ф0-б): cron-строка ЛИБО systemd-таймер
   ЛИБО сервис в `docker-compose.yml`, с оракулом точки входа (`testing.md`). Это чисто
   инженерная задача (нет решения, требующего founder'а, до токенов).
3. **После (2) — токены** `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` на VPS + строка cron
   (`scripts/watchdog_cron.sh`, `docs/runbooks/alerting.md`) — действие founder'а.
4. **Ребут-дрилл** (Ф0-в): провести после (2)/(3) — иначе алерты нечем подтвердить.

**Если считать рубежом Ф2 (то, что реально ближе всего по объёму сделанной работы)** —
следующий шаг явно назван самим `DESIGN` §16.2: **шаг 3/4 — устранение per-tick
пересканирования до конца + подключение уже написанного multi-thread рантайма**. Стоимость
минимальна (код рантайма УЖЕ существует, нужно снять `#[allow(dead_code)]` и включить),
эффект по документу — ×4 CPU. Но многопоточность «недостаточна» (см. `DESIGN` §16.1) — без
шага 5 (shared-tailer/per-key редьюсер) она не снимает память (каждое соединение всё ещё
держит свой `LiveReducer`).

---

## ТРЕБУЕТ ПОДПИСИ FOUNDER'А

Список — ТОЛЬКО то, что по `gates.md` §0.1 (или явному прецеденту в `docs/PENDING-SIGNATURE.md`)
есть Граница C (состав данных, деньги/лимиты, фазы, юр./тарифная модель). Инженерная работа
рядом НЕ включена сюда, даже если она блокирует ту же фазу.

1. **Доступ/окно на проде для restore-drill (Ф0-а)** — операция трогает единственную копию
   продовых данных на живом сервере; прецедент — все прошлые founder-действия со Storage Box
   вводились как §0.1-решения (`П-023`).
2. **`TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` на VPS** (Ф0-б) — секрет на живом сервере,
   `П-003` называет это явным действием владельца, не агента.
3. **Снапшоты Storage Box** — сегодня выключены; `П-023` называет это открытым риском
   («один наш ключ способен снести и оригинал, и копию»). Включение — операционное решение
   с ценой (доп. стоимость Storage Box), Граница C по духу §0.1 (защита единственного актива).
4. **Реальное включение ретеншена** (`RETENTION_MODE: apply`) — прямо названо в `П-023`:
   «отдельное решение — после `R1b`» (после restore-drill). Сегодня `dry-run` подтверждён
   командой (см. блок A выше).
5. **HL-инструменты в диффах / состав символов сверх ETHUSDT** — `П-026` закрывает только
   `BTCUSDT,ETHUSDT` на Binance; любое расширение (HL, топ-300 и т.п.) — новая Граница C по
   тому же классу, что и сама `П-026`.
6. **Инверсия ролей снапшот↔дифф (Ф1 п.2)** формально инженерная (спека `M-45` §4bis это
   предусматривала), но снижение частоты якоря — это изменение того, ЧТО и КАК записывается
   постоянно, а это тот же класс решения, что и `П-026`; architect обязан пронести её через
   founder явным решением, а не тихой правкой константы, даже если код для этого не требует
   contract-RFC.
7. **Ф2 предусловие Ф1** (`09-roadmap-v2.md`) и порядок «Ф0 закрывается первой» (`П-018б`) —
   расхождение между декларированным порядком и фактическим ходом работы (см. вопрос 1)
   само по себе требует явного решения: founder либо переподтверждает «Ф0 первой» и
   останавливает Ф1/Ф2-работу до восстановления порядка, либо явно снимает это требование.
   Сегодня это не решено ни в одну, ни в другую сторону — это САМА НАХОДКА, а не рекомендация.

Всё остальное в списках выше (multi-thread рантайм, shared-tailer, сжатие, cursor-resume,
ops-watchdog wiring, mem_limit в compose, restore-drill спека) — инженерная очередь, не
требует founder-подписи по существу задачи, только приоретизации.

---

## Ответ на вопрос 4 — ворота, объявленные выполненными, но НЕ выполненные на деле

Собрано отдельно, как просило задание — это самая ценная часть.

1. **`M-73` (офсайт-копия по расписанию) выглядит НЕЗАКРЫТЫМ в собственном milestone-файле**,
   хотя код давно смёржен (`7581f60`, `R-158` APPROVED, круг 4) и на проде работает уже дней
   как минимум неделю (`last-success` растёт каждый час). Файл `milestones/M-73-offsite-schedule.md`
   в `main` (не в ветке!) до сих пор несёт `STATUS: PROPOSED` и таблицу задач со всеми
   пунктами `⏳ OPEN`, ссылаясь на давно закрытый круг `R-151`:
   ```
   $ git merge-base --is-ancestor 7b9bf66 HEAD && echo YES || echo NO
   NO
   ```
   (`7b9bf66` — коммит close-out'а, приводящий STATUS → DONE — существует в истории, но НЕ
   является предком текущего `main`). Это ОБРАТНЫЙ случай относительно предупреждения
   задания («31 из 35 выглядят незакрытыми, хотя часть давно на проде») — тот же класс
   дефекта, что и предупреждённый, только на предмете этого аудита он подтверждён напрямую.

2. **`M-70` (включение полос глубины)** несёт в шапке `Статус: rev1 — спека написана, dev НЕ
   диспетчеризуется`, но `git log` показывает РЕАЛЬНЫЕ dev-коммиты `feat(M-70): task #3`,
   `feat(M-70): task #7` и т.д. — то есть диспетчеризация ФАКТИЧЕСКИ произошла, а статус-шапка
   файла с этим не синхронизирована ни разу.

3. **Ф2 шаг 4 «многопоточный рантайм» — заявлен как ⏳ (ещё предстоит) в `DESIGN.md` §16.2**,
   но код рантайма уже НАПИСАН и ЛЕЖИТ в дереве, просто помечен `#[allow(dead_code)]` и не
   вызывается. Это не «предстоит написать» — это «написано и забыто», в точности класс
   built-not-wired, который профиль architect'а требует ловить специально.

4. **Push-алертинг (§23.1) — не «ждёт токена», как написано в `П-003` (2026-08-02) и как всё
   ещё гласит верхний, самый заметный абзац в `docs/PENDING-SIGNATURE.md`.** Более поздняя
   и более точная запись `П-018(б)` (2026-08-25) прямо опровергает эту рамку: запускателя не
   существует вовсе. Верхняя запись `П-003` НЕ помечена как устаревшая и продолжает вводить в
   заблуждение любого, кто читает индекс сверху вниз, а не грепом по свежей дате.

5. **`TD-020` числится в «замороженной, вне пути Ф1–Ф4» корзине долга** (`TECH-DEBT.md`,
   таблица групп, строка «Ф0 … вне пути Ф1–Ф4») — при этом та же карточка прямо названа
   «acceptance-ворота Ф0» в шапке той же строки. Заморозка (`gates.md`-класс «долг подождёт»)
   и статус «ворота фазы, официально в работе» — противоречащие друг другу ярлыки на одной
   строке одной таблицы.

---

## ПРЕДЕЛЫ РАЗБОРА

- **Полный `cargo test --workspace` не был чист:** один прогон дал `ct_i_1_single_definition_canary`
  FAILED (0 hits вместо 1 при поиске `enum Venue {`); изолированный повторный прогон
  (`cargo test -p contracts --test ct_rfc01`) — GREEN за 0.03s. Рабочий каталог
  `/home/nous/hft-platform` — ОБЩИЙ чекаут (`branch-hygiene.md`), в нём одновременно могли
  работать другие роли; трактую это как транзиентный артефакт параллельной активности на
  дереве, а не регрессию контракта, но НЕ проверял это повторным полным прогоном (заняло бы
  ещё ~5 минут compile+test каждого из ~25 крейтов) — если кто-то видит это FAILED в CI, это
  повод перепроверить отдельно, не доверять моему единственному прогону как окончательному.
- **Ребут-дрилл (Ф0-в) и M-43-задачи проверены ТОЛЬКО грепом и текущим состоянием compose/cron.**
  Не пытался спровоцировать реальный инцидент/симуляцию на проде — это было бы прямым
  нарушением зоны (только чтение, ничего не чиню и не запускаю активно).
  Uptime VPS не снимался — не нужен был для установления факта «дрилл не проводился»,
  но и не даёт точной даты последнего органического ребута.
- **Storage Box снапшоты, реальная защита от двойного удаления** — проверено ТОЛЬКО через
  прозу `П-023` (что снапшоты выключены). Не проверял через API/панель Hetzner напрямую —
  доступа к веб-консоли из этой сессии нет, только ssh к VPS и к самому box'у по rsync-ключу.
- **Ф2 «shared-tailer/per-key редьюсер» проверен по коду двух файлов** (`lib.rs`, `session.rs`)
  и структуре `Sub`. Не читал `crates/gateway/src/lib.rs` целиком (2379+ строк в
  gateway-serve, десятки тысяч строк суммарно в gateway) — вывод «каждое соединение = свой
  `LiveReducer`» опирается на структуру данных `Sub.live: Option<LiveReducer>`, а не на полный
  трейс каждого пути выполнения; для milestone-уровня точности этого достаточно, для
  contract-RFC уровня — потребовалось бы дочитать весь `gateway::LiveReducer::resume/pump`.
- **Ф1 «HL-диффы» не проверял отдельно** — задание сфокусировано на инверсии BTC/ETH-Binance
  пути; `HL_COINS` состав диффов не менялся по `П-026` явно («HL_COINS не трогается»), принял
  это на слово документа без отдельного замера кода `venue-hyperliquid`.
- **Execution-фаза (risk/killswitch/oms), ворота §21.1 — сознательно НЕ разбирались**, по
  прямому ограничению задания («вне зоны»). Их отсутствие в `Cargo.toml` workspace
  зафиксировано только как побочное наблюдение при проверке Ф3+, не как самостоятельный вывод.
- **Все ssh-команды выполнялись READ-ONLY** (`cat`, `ls`, `crontab -l`, `du -sh`, `docker ps`,
  `systemctl list-timers`) — ничего на проде не менялось, не перезапускалось, не создавалось.
- **`docs/PENDING-SIGNATURE.md` (2352 строк) прочитан НЕ целиком** — только индекс `## П-`
  (ярус A) и записи `П-003`, `П-018(б)`, `П-023`, `П-026`, прямо относящиеся к предмету
  Ф0/Ф1. Остальные ~20 записей (тарифы, полосы вне предмета, харнесс-заморозки) не открывались.
- **`TECH-DEBT.md` (3900+ строк) прочитан ТОЛЬКО грепом по предмету** (`TD-020`, `TD-097`,
  `TD-193`–`TD-201`, группировочная таблица) — per правилу ярус C; полный список ~150 карточек
  не пересчитывался, могут существовать другие карточки, относящиеся к Ф0–Ф2 фазовым воротам,
  которых этот грепом-проход не поймал.
