//! RED `PL-I-5` / `VB-I-2` (sacred, architect-only) — **ПРЕДЕЛ ДЕЙСТВУЕТ ОДИНАКОВО НА ОБОИХ
//! ПУТЯХ, И ПРИНЯТЫЙ ОТВЕТ ПОЛОН.**
//!
//! Милестоун `milestones/M-71-egress-cap.md`. Исполнение вердикта
//! `research/reviews/R-133-M-71-egress-cap-impl.md` (PR-time REJECT, круг 1): блокеры
//! **B-2** (живой путь усекает там, где библиотечный отказывает), **B-3** (флаг
//! `history_truncated` захвачен под чужой смысл) и находка **N-3** (щель набора: уровень 1
//! судит библиотечный `snapshot`, уровень 2 судит БАЙТЫ с сокета, а прод-путь
//! `resume → pump → snapshot_checked` не судится на ПОЛНОТУ ничем).
//!
//! # Решение architect'а, которое эти оракулы пиннят
//!
//! Спека допускала два исхода при превышении: явный отказ ИЛИ явно помеченное усечение.
//! **Выбран ОТКАЗ, одинаково на обоих путях.** Основания:
//!
//! 1. `VB-I-2` («живое == перепроигранное») — инвариант FA тронутого модуля
//!    (`docs/fa/viz-backend.md:189`), и §4.1 спеки прямо запрещает его ослаблять. Усечение
//!    на ОДНОМ пути ломает равенство немедленно.
//! 2. Отказ fail-closed: клиент знает, что не получил ничего. Усечение fail-open по
//!    существу: клиент получает часть, считая её целым. Для продукта, продающего
//!    доказуемость, это хуже отказа (`PL-I-7`: деградация никогда не выдаётся за норму).
//! 3. Честное помеченное усечение потребовало бы НОВОГО поля на проводе, а не захвата
//!    чужого ⇒ смена формы выдачи ⇒ bump `GATEWAY_SCHEMA_VERSION`. Его в ту же версию
//!    поднимает соседний милестоун `M-68` (8→9) — столкновение на ровном месте.
//!
//! # Анти-плацебо: набор красен ПРОТИВ ОБЕИХ крайностей
//!
//! `P1`/`P2` краснеют против заглушки «всегда `Ok`» (сегодняшнее усечение) и против
//! заглушки «всегда `Err`» — последнюю валит КОНТРОЛЬ `P-C1`, идущий первым по той же
//! причине, по какой контроли идут первыми в `red_egress_cap_wire.rs`: страж, ломающий
//! честную работу, выключат, и защиты не станет.
//!
//! # Что здесь НЕ судится
//!
//! Байты, уходящие в сокет — это уровень 2 (`red_egress_cap_wire.rs`). Здесь объект
//! ОБЪЯВЛЕН: собственные объекты крейта `gateway` на двух его путях. Смешивать единицы
//! запрещено `A-022`, и здесь они не смешиваются.

use contracts::{to_fixed, DataSource, EventKind, MdPayload, Side, Venue};
use gateway::{Cursor, LiveReducer, Selector, Snapshot};
use journal::{EpochFilter, Journal, WriterConfig};

const MID: f64 = 65_000.0;
const T0: i64 = 1_752_000_000_000;

/// Заведомо ПОД пределом: контроль честной работы.
const HONEST_TRADES: usize = 200;

/// Заведомо СВЕРХ предела. `M-71` §0: 25 000 сделок дают 2 804 765 Б при пределе 2 000 000.
const DENSE_TRADES: usize = 25_000;

fn cfg() -> WriterConfig {
    WriterConfig {
        max_segment_bytes: 1 << 26,
        min_free_bytes: 0,
        source: DataSource::OwnCapture,
        provenance: "PL-I-5 two-path fixture".to_string(),
        epoch_id: "own-test".to_string(),
    }
}

fn sel() -> Selector {
    Selector {
        venue: Venue::Binance,
        symbol: "BTCUSDT".to_string(),
        timeframe_ms: 1_000,
        bands: vec![0.001],
        window_ms: None,
        // M-68 задача 22: поле добавлено в Selector; `None` = пер-событийно,
        // то есть НЕЙТРАЛ — прежняя семантика этого теста сохранена бит-в-бит.
        depth_cadence_ms: None,
    }
}

/// Журнал из `n` сделок с РАЗНЫМИ ценами — каждая растит ответ предсказуемым шагом.
/// Ни одного L2-события: heatmap пуст, и предмет — та часть ответа, которую предел
/// первой редакции не видел вовсе (`C-157` R1).
fn journal_of_trades(n: usize) -> tempfile::TempDir {
    let dir = tempfile::tempdir().expect("SETUP: tempdir");
    let mut j = Journal::open_with(dir.path(), cfg()).expect("SETUP: open_with");
    for i in 0..n as i64 {
        j.append(EventKind::md(
            Venue::Binance,
            "BTCUSDT",
            MdPayload::Trade {
                price: to_fixed(MID + i as f64 * 0.01),
                size: to_fixed(1.0),
                side: if i % 2 == 0 { Side::Buy } else { Side::Sell },
                ts_exch_ms: T0 + i,
            },
        ))
        .expect("SETUP: append");
    }
    j.flush().expect("SETUP: flush");
    dir
}

/// Библиотечный путь — тот, которым ходят чекпоинтер, shared-tailer, `research-cli`, replay.
fn lib_path(dir: &std::path::Path) -> std::io::Result<Snapshot> {
    gateway::snapshot(dir, EpochFilter::OwnCaptureOnly, &sel(), Cursor::LATEST)
}

/// Размер партии догона. **Не украшение фикстуры, а условие достижимости предмета.**
///
/// Первая редакция этого файла качала весь журнал ОДНИМ `pump(usize::MAX)`. На переполненной
/// фикстуре кадр целиком выходил за предел, `pump` возвращал `Err` — и ветка усечения в
/// `snapshot_checked` НЕ ДОСТИГАЛАСЬ НИКОГДА. Все четыре оракула были зелены против дефектной
/// реализации: `P1` проходил по неверной причине, `P2`/`P3` молча пропускали сценарий через
/// `continue`. Замер, вскрывший это (`n=25 000`):
///
/// ```text
/// lib  = Err(PL-I-5: limit=2000000 observed=2804856)
/// live = PUMP-ERR(PL-I-5: limit=2000000 observed=2804856)   ← не snapshot_checked!
/// ```
///
/// Мелкая партия делает КАДРЫ малыми, а СОСТОЯНИЕ — большим: ровно то состояние, в котором
/// живёт дефект `B-2`. Оставляю след, потому что это класс «фикстура не давит на инвариант»,
/// и поймал его прогон, а не рассуждение (`testing.md`, анти-плацебо).
const PUMP_BATCH: usize = 256;

/// Эффективный предел — ПРОЦЕССНОЕ значение (`gateway::set_effective_max_response_bytes`).
/// Тесты этого файла его читают, а `P4` его ТРОГАЕТ, поэтому все они обязаны идти
/// ПОСЛЕДОВАТЕЛЬНО — иначе меряют друг друга. Тот же приём и та же причина, что в
/// `crates/gateway-serve/tests/red_egress_cap_governed.rs:66` и `red_max_subs_config.rs:70`.
///
/// **Внесено по ЗАМЕРУ, а не для порядка.** Первая редакция `P4` шла без guard'а, и прогон
/// показал: `--test-threads=1` роняет ТОЛЬКО `P4` (настоящий дефект), а параллельный прогон
/// роняет ЕЩЁ и `P1` — то есть новый оракул течёт глобальным состоянием в соседа. Оракул,
/// роняющий соседей, хуже отсутствующего: он делает набор нечитаемым.
fn serial() -> std::sync::MutexGuard<'static, ()> {
    static LOCK: std::sync::OnceLock<std::sync::Mutex<()>> = std::sync::OnceLock::new();
    LOCK.get_or_init(|| std::sync::Mutex::new(()))
        .lock()
        .unwrap_or_else(|e| e.into_inner())
}

/// Чем кончился ПРОД-путь живой сессии. Три исхода РАЗЛИЧАЮТСЯ намеренно: «отказал `pump`» и
/// «отказал `snapshot_checked`» — разные события, и оракул, который их смешивает, зелен при
/// недостигнутом предмете (`testing.md`, «Целостность гейта» свойство 3).
#[derive(Debug)]
// Полезная нагрузка вариантов читается через `Debug` в тексте ассерта `P1` — она называет
// ПРИЧИНУ отказа и отличает достигнутый предмет от вакуума. `dead_code` синтаксичен и
// чтения через `Debug` не видит; глушим точечно и с объяснением, а не молча.
#[allow(dead_code)]
enum Live {
    PumpRefused(String),
    SnapshotRefused(String),
    Served(Box<Snapshot>),
}

/// ПРОД-путь живой сессии: `resume → pump* → snapshot_checked`. Именно он кладёт байты на
/// провод в `gateway-serve`, и именно его не исполнял ни один оракул набора (`R-133` N-3).
fn live_path(dir: &std::path::Path) -> Live {
    let ckpt = tempfile::tempdir().expect("SETUP: ckpt tempdir");
    let (mut r, _) = LiveReducer::resume(dir, EpochFilter::OwnCaptureOnly, &sel(), ckpt.path())
        .unwrap_or_else(|e| setup_failed(&format!("resume не собрался: {e}")));
    loop {
        match r.pump(dir, EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Ok((frames, _, _)) if frames.is_empty() => break,
            Ok(_) => continue,
            Err(e) => return Live::PumpRefused(e.to_string()),
        }
    }
    match r.snapshot_checked() {
        Ok(s) => Live::Served(Box::new(s)),
        Err(e) => Live::SnapshotRefused(e.to_string()),
    }
}

/// Диагностика несостоявшегося setup'а — отдельно от вердикта (`testing.md`, «Целостность
/// гейта» свойство 3: проба, молча тестирующая не тот сценарий, есть плацебо самой себя).
fn setup_failed(what: &str) -> ! {
    panic!(
        "SETUP НЕ СОСТОЯЛСЯ: {what}. Это НЕ вердикт о пределе: фикстура не воспроизвела \
         сценарий, ради которого оракул написан."
    )
}

// ═══════════════════════════════════════════════════════════════════════════════════════════
// КОНТРОЛЬ — первым: заглушка «всегда Err» обязана валиться здесь, иначе страж выключат
// ═══════════════════════════════════════════════════════════════════════════════════════════

/// **P-C1 — честная нагрузка проходит ОБОИМИ путями и даёт ОДИНАКОВЫЙ результат.**
///
/// Ловит переширокую реализацию («всегда `Err`»), которая зелена во всех остальных
/// оракулах этого файла. Ложное КРАСНОЕ дороже пропуска: набор уже болел им дважды
/// (`M-71` §0ter R2).
#[test]
fn pl_i_5_p_c1_honest_load_passes_both_paths_identically() {
    let _g = serial();
    let dir = journal_of_trades(HONEST_TRADES);

    let lib = lib_path(dir.path())
        .unwrap_or_else(|e| setup_failed(&format!("честная нагрузка отвергнута lib-путём: {e}")));
    let Live::Served(live) = live_path(dir.path()) else {
        setup_failed("честная нагрузка отвергнута live-путём — контроль не на чем ставить")
    };

    assert_eq!(
        lib.series.volume_bubbles.len(),
        live.series.volume_bubbles.len(),
        "PL-I-5/VB-I-2 P-C1: на ЧЕСТНОЙ нагрузке пути разошлись по составу — lib отдал {} \
         пузырей, live {}. Живое и перепроигранное обязаны совпадать (VB-I-2, \
         docs/fa/viz-backend.md:189)",
        lib.series.volume_bubbles.len(),
        live.series.volume_bubbles.len()
    );
    assert!(
        !live.series.volume_bubbles.is_empty(),
        "PL-I-5 P-C1 SETUP: фикстура не произвела ни одного пузыря — сравнивать нечего"
    );
}

// ═══════════════════════════════════════════════════════════════════════════════════════════
// ПРЕДМЕТ
// ═══════════════════════════════════════════════════════════════════════════════════════════

/// **P1 (`R-133` B-2) — ВЕРДИКТ путей совпадает: если lib отказал, live обязан отказать.**
///
/// Краснеет против сегодняшней реализации: `LiveReducer::snapshot_checked` при превышении
/// чистит `volume_bubbles` и возвращает `Ok`, тогда как `gateway::snapshot` на том же
/// ресурсе даёт `Err` (замер `R-133`: lib `Err(2 804 856 Б)` против live
/// `Ok(654 765 Б, bubbles 25 000 → 0)`).
#[test]
fn pl_i_5_p1_over_cap_is_refused_on_both_paths() {
    let _g = serial();
    let dir = journal_of_trades(DENSE_TRADES);

    let lib = lib_path(dir.path());
    if lib.is_ok() {
        setup_failed(&format!(
            "{DENSE_TRADES} сделок обслужены lib-путём без отказа — предел недостижимо велик \
             либо его нет, и сравнивать вердикты не на чем"
        ));
    }

    let live = live_path(dir.path());
    assert!(
        matches!(live, Live::SnapshotRefused(_)),
        "PL-I-5/VB-I-2 B-2: lib-путь ОТКАЗАЛ, а live-путь дал {live:?}. Предмет достигнут \
         только при SnapshotRefused: PumpRefused означает, что кадр не поместился РАНЬШЕ, и \
         ветка усечения в snapshot_checked не исполнялась (вакуумный оракул — см. PUMP_BATCH); \
         Served означает молчаливое усечение живого пути, то есть ослабление VB-I-2, которое \
         §4.1 спеки прямо запрещает"
    );
}

/// **P2 (`R-133` B-3) — `history_truncated` означает РОВНО потерю префикса журнала.**
///
/// `VB-I-11` (`M-48`) объявляет эквивалентность `history_truncated == (history_start_seq > 0)`;
/// она записана нормативным комментарием на самом поле (`crates/gateway/src/lib.rs:2490`) и
/// читается потребителями (`wsprobe.rs:494,506`). Использовать флаг как метку усечения
/// НАГРУЗКИ — захват чужого смысла: дежурный, увидев его, пойдёт искать несуществующую
/// проблему с ретеншеном.
///
/// Краснеет против сегодняшней реализации: `history_truncated = true` при
/// `history_start_seq = 0`.
#[test]
fn pl_i_5_p2_history_truncated_is_not_hijacked_by_the_cap() {
    let _g = serial();
    for n in [HONEST_TRADES, DENSE_TRADES] {
        let dir = journal_of_trades(n);
        let live_snap = match live_path(dir.path()) {
            Live::Served(s) => Ok(*s),
            other => Err(format!("{other:?}")),
        };
        for (name, got) in [
            ("lib", lib_path(dir.path()).map_err(|e| e.to_string())),
            ("live", live_snap),
        ] {
            let Ok(s) = got else { continue };
            assert_eq!(
                s.history_truncated,
                s.history_start_seq > 0,
                "VB-I-11 B-3 ({name}-путь, {n} сделок): history_truncated={} при \
                 history_start_seq={}. Эквивалентность «true ⇔ префикс журнала потерян» \
                 объявлена на самом поле (lib.rs:2490) и читается потребителями. Флаг занят \
                 под усечение НАГРУЗКИ — это другой смысл, и он делает ответ противоречивым",
                s.history_truncated,
                s.history_start_seq
            );
        }
    }
}

/// **P3 (`R-133` N-3, щель набора) — ПРИНЯТЫЙ ответ ПОЛОН, а не урезан молча.**
///
/// Уровень 1 судит библиотечный `snapshot`; уровень 2 судит БАЙТЫ, пришедшие в сокет — и
/// усечённый ответ проходит его ПО ПОСТРОЕНИЮ, потому что он стал МЕНЬШЕ. Полнота не судится
/// нигде, и это дало `B-2` прожить мимо зелёного гейта.
///
/// **Эталон взят из НЕЗАВИСИМОГО источника — из фикстуры, а не из соседнего пути**
/// (`testing.md`: «зависимый эталон мутация ловит плохо»). Каждая сделка несёт СВОЮ цену,
/// значит рождает свой пузырь: журнал из `n` сделок обязан дать ровно `n` пузырей. Замер,
/// подтверждающий тождество на честной нагрузке: `n=200 → bubbles=200`.
///
/// Первая редакция сравнивала live с lib и требовала пары `Ok`/`Ok`. Такая пара при
/// сегодняшнем дефекте НЕ ВОЗНИКАЕТ (lib отказывает, live отдаёт усечённое), и оракул был
/// вакуумен — второй экземпляр того же класса в одном файле. След оставлен намеренно.
#[test]
fn pl_i_5_p3_accepted_response_is_complete_not_silently_truncated() {
    let _g = serial();
    for n in [HONEST_TRADES, DENSE_TRADES] {
        let dir = journal_of_trades(n);
        let Live::Served(live) = live_path(dir.path()) else {
            continue; // отказ живого пути — предмет P1, не этого оракула
        };
        assert_eq!(
            live.series.volume_bubbles.len(),
            n,
            "PL-I-5 N-3 ({n} сделок): живой путь ОТДАЛ ответ, но в нём {} пузырей вместо {n}. \
             Значит ответ урезан и выдан за полный: клиент получил часть, считая её целым \
             (PL-I-7 — деградация не выдаётся за норму). Предел обязан ОТКАЗЫВАТЬ, а не \
             тихо выбрасывать сущности",
            live.series.volume_bubbles.len()
        );
    }
}

/// **P4 — ОТКАЗ НЕ СМЕЕТ ПОРТИТЬ СОСТОЯНИЕ: повторный `pump` не складывает события дважды**
/// (`R-139` B-1).
///
/// # Дефект, ради которого оракул написан
///
/// `LiveReducer::pump` кормит ПЕРСИСТЕНТНЫЙ аккумулятор покадрово внутри цикла
/// (`self.full.apply(&event)`, `self.vwap.sum_* = batch.vwap.sum_*`), а курсор двигает ОДИН
/// раз в конце (`self.cursor = cursor`). `enforce_response_limit(..)?` стоит МЕЖДУ ними и
/// выходит из функции. При отказе состояние уехало вперёд, закладка — нет.
///
/// Все три прод-вызывателя возвращают редьюсер ВНУТРЬ ветки ошибки
/// (`gateway-serve/src/lib.rs:1240`, `:1656`, `:1764` — `Err(e) => Err(Box::new((live, e, ..)))`)
/// и переиспользуют его. Значит следующий `pump` перечитывает те же события от старого
/// курсора и складывает их ВТОРОЙ раз. Замер `R-139`: три отказа → `volume_profile`
/// 400 000 000 против правильных 100 000 000, `vwap` разъехался, а ДЛИНА сериализации
/// совпала до байта (224 646 Б обе) — поэтому набор, судивший длину, был зелен 44/0 два
/// круга подряд при живом дефекте.
///
/// # Почему прежние оракулы этого не видели — щель названа, а не подразумевается
///
/// `live_path` (`:134-150`) выходит на ПЕРВОМ `Err` (`return Live::PumpRefused`). Оракул без
/// повторного вызова слеп к порче по построению: он судит ИСХОД отказа и не судит СОСТОЯНИЕ
/// после него. Ровно щель, названная `R-133` N-3 и не закрытая двумя кругами.
///
/// # Что судится и чем
///
/// ЗНАЧЕНИЯ, а не длина — длина при этом дефекте совпадает. Эталон НЕЗАВИСИМ: полный реплей
/// тем же `gateway::snapshot`, которым живёт `VB-I-2` («живое == перепроигранное»), — другой
/// путь, а не пересчёт того же состояния (`testing.md`: «зависимый эталон мутация ловит плохо»).
///
/// # Конструкцию оракул НЕ выбирает
///
/// Транзакционность `pump`, продвижение курсора по последнему состоявшемуся batch'у,
/// терминальность отказа для сессии — решает реализация. Требование одно: **после отказов
/// свёрнутое состояние равно тому, что даёт независимый реплей.**
#[test]
fn pl_i_5_p4_refused_pump_does_not_double_apply_on_retry() {
    let _g = serial();
    let dir = journal_of_trades(DENSE_TRADES);

    // Эталон — НЕЗАВИСИМЫЙ путь, снят ДО всяких отказов и при заведомо достаточном пределе.
    gateway::set_effective_max_response_bytes(usize::MAX);
    let reference = lib_path(dir.path())
        .unwrap_or_else(|e| setup_failed(&format!("эталонный реплей не собрался: {e}")));

    // Крошечный предел — чтобы `pump` ОТКАЗЫВАЛ. Величина не «магическая»: она заведомо
    // меньше любого delta-кадра на этой фикстуре и заведомо больше нуля (нулевой предел
    // отвергается стартовым гвардом и судится оракулом D, не здесь).
    const TINY: usize = 20_000;
    const RETRIES: usize = 3;

    let ckpt = tempfile::tempdir().expect("SETUP: ckpt tempdir");
    gateway::set_effective_max_response_bytes(TINY);
    let (mut r, _) =
        LiveReducer::resume(dir.path(), EpochFilter::OwnCaptureOnly, &sel(), ckpt.path())
            .unwrap_or_else(|e| setup_failed(&format!("resume не собрался: {e}")));

    // ПОВТОРНЫЕ вызовы после отказа — то, чего не делал ни один прежний оракул.
    let mut refusals = 0usize;
    for _ in 0..RETRIES {
        match r.pump(dir.path(), EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Err(_) => refusals += 1,
            Ok(_) => break,
        }
    }
    // SETUP-GUARD: без состоявшихся отказов предмет не воспроизведён, и зелёный тест был бы
    // плацебо (`testing.md` «Целостность гейта» св. 3).
    if refusals < RETRIES {
        setup_failed(&format!(
            "предмет не воспроизведён: из {RETRIES} попыток отказов {refusals}. Предел {TINY} Б \
             обязан валить delta-кадр на фикстуре из {DENSE_TRADES} сделок; если он его \
             пропускает, оракул судит не тот сценарий"
        ));
    }

    // Предел поднимаем и ДОГОНЯЕМ ЖУРНАЛ ДО КОНЦА. Без догона сравнение было бы ложным:
    // после трёх отказов при партии PUMP_BATCH свёрнута лишь часть журнала, и оракул мерил бы
    // НЕПОЛНОТУ вместо ПОРЧИ. Поймано прогоном первой редакции: она дала 0.03× — состояние
    // МЕНЬШЕ эталона, тогда как искомый дефект даёт БОЛЬШЕ. След оставлен намеренно: это
    // ровно класс «оракул мерит не то, что обещает» (`testing.md`).
    gateway::set_effective_max_response_bytes(usize::MAX);
    loop {
        match r.pump(dir.path(), EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Ok((frames, _, _)) if frames.is_empty() => break,
            Ok(_) => continue,
            Err(e) => setup_failed(&format!("догон при снятом пределе отказал: {e}")),
        }
    }
    let after = r
        .snapshot_checked()
        .unwrap_or_else(|e| setup_failed(&format!("снимок при снятом пределе отвергнут: {e}")));

    // ── ПРЕДМЕТ: значения, а не длина ────────────────────────────────────────────────
    // Объём профиля — сумма по бинам `(price_e8, volume_e8)` ВСЕХ строк: `VolumeProfileRow`
    // несёт `bins: Vec<(i64, i64)>`, отдельного поля объёма у строки нет. Форма подсказана
    // компилятором (`E0609`), а не догадкой.
    let sum_vp = |s: &gateway::SeriesBundle| -> i128 {
        s.volume_profile
            .iter()
            .flat_map(|r| r.bins.iter())
            .map(|(_, v)| *v as i128)
            .sum()
    };
    let ref_vp = sum_vp(&reference.series);
    let got_vp = sum_vp(&after.series);
    assert_eq!(
        got_vp,
        ref_vp,
        "PL-I-5 P4 (R-139 B-1): после {refusals} ОТКАЗАВШИХ pump суммарный объём профиля \
         {got_vp} против {ref_vp} у независимого реплея — кратность {:.2}×. Отказ оставил \
         состояние впереди курсора, и события свернулись повторно. ДЛИНА сериализации при \
         этом совпадает, поэтому ни один оракул, судивший размер, этого не видит",
        got_vp as f64 / ref_vp.max(1) as f64
    );
    assert_eq!(
        after.series.vwap, reference.series.vwap,
        "PL-I-5 P4 (R-139 B-1): VWAP после отказов разошёлся с независимым реплеем. \
         `VB-I-2` («живое == перепроигранное») ломается не при стечении обстоятельств, \
         а по построению: `self.vwap.sum_*` двигается покадрово, курсор — в конце"
    );
    assert_eq!(
        after.series.volume_bubbles.len(),
        reference.series.volume_bubbles.len(),
        "PL-I-5 P4 (R-139 B-1): число пузырей после отказов не совпало с реплеем — события \
         свёрнуты повторно"
    );

    // Возврат процессного значения к подписанной норме: guard защищает от параллельных
    // тестов, но оставлять глобальное состояние сдвинутым после себя нельзя — следующий
    // тест этого файла имеет право на чистое окружение.
    gateway::set_effective_max_response_bytes(gateway::DEFAULT_MAX_RESPONSE_BYTES);
}

/// **P5 — СЕРИЯ ПОТРЕБИТЕЛЯ, а не состояние сервера: отказ не смеет ТЕРЯТЬ кадры**
/// (`R-140`, круг 3).
///
/// # Почему `P4` этого не ловит и почему одного его мало
///
/// `P4` судит `r.snapshot_checked()` — состояние СЕРВЕРА. Оно после круга 3 верно
/// (`1.00×`), и `P4` зелен. Но клиент собирает свою серию из ДОСТАВЛЕННЫХ кадров, а кадр,
/// на котором сработал предел, не доставляется НИКОГДА: `self.cursor` двигается ДО
/// `enforce_response_limit(..)?` (`crates/gateway/src/lib.rs:3486`, `:3527`), а `frames.push`
/// стоит ПОСЛЕ (`:3488`, `:3529`). События помечены потреблёнными, кадра нет.
///
/// Три прод-вызывателя кадры при отказе выбрасывают
/// (`crates/gateway-serve/src/lib.rs:1240`, `:1656`, `:1764`), ресинка снапшотом на
/// push-канале нет (`:1815`), а `Snapshot::apply` (`:1499`) непрерывность
/// `frame.from ↔ frame.to` не проверяет — дыра проходит молча. Замер `R-140`:
/// `25 000 − 24 232 = 768 = 3 × 256` сделок не придут никогда, клиентская серия `0.9693×`
/// реплея ПРИ ИСПРАВНОМ СЕРВЕРЕ.
///
/// # Что судится
///
/// `VB-I-2` (`docs/fa/viz-backend.md:189`) — «серия, посчитанная на live-хвосте,
/// **бит-идентична** серии из replay того же окна». Инвариант милестоуна (§4bis.5, переписан
/// по `R-140`): **после любого числа отказавших `pump` серия, собранная из ДОСТАВЛЕННЫХ
/// кадров, бит-идентична реплею того же окна.** Прежняя редакция инварианта говорила о
/// серверном аккумуляторе — это сужение и есть корень находки круга 3.
///
/// # Анти-плацебо: `P5` ОДНОСТОРОНЕН, и это названо ЗАМЕРОМ, а не оценкой
///
/// Первая редакция этого комментария утверждала, что `P5` падает против ОБЕИХ поломок.
/// **Замер опроверг:** против реализации круга 2 (двойная свёртка) `P5` — `ok`, красен
/// только `P4`. Причина в конструкции самого оракула: цикл отказов выходит по первому `Ok`,
/// поэтому повторная свёртка серверного аккумулятора до потребительских кадров в этой
/// фикстуре не доезжает.
///
/// Двусторонность даёт ПАРА, и порознь ни один из двух не достаточен:
/// · `P4` — состояние СЕРВЕРА: красен против двойной свёртки (круг 2), зелен против потери;
/// · `P5` — серия ПОТРЕБИТЕЛЯ: красен против потери кадров (круг 3), зелен против двойной.
/// Удаление любого из них снимает половину защиты молча. Шаг `A4` гейта пиннит состав
/// числом именно поэтому.
///
/// # Конструкцию оракул НЕ выбирает
///
/// Дробление партии, терминальность подписки с ресинком снапшотом — решает реализация
/// (§4bis.5). Требование одно: то, что СОБРАЛ клиент, равно реплею.
#[test]
fn pl_i_5_p5_delivered_frames_reconstruct_the_replay_series() {
    let _g = serial();
    let dir = journal_of_trades(DENSE_TRADES);

    // Эталон — НЕЗАВИСИМЫЙ путь (полный реплей), снят при заведомо достаточном пределе.
    gateway::set_effective_max_response_bytes(usize::MAX);
    let reference = lib_path(dir.path())
        .unwrap_or_else(|e| setup_failed(&format!("эталонный реплей не собрался: {e}")));

    const TINY: usize = 20_000;
    const RETRIES: usize = 3;

    let ckpt = tempfile::tempdir().expect("SETUP: ckpt tempdir");
    // Стартовый снапшот потребителя снимается ДО отказов и при достаточном пределе: это
    // `snapshot(C)`, с которого клиент начинает и на который кладёт кадры.
    let (mut r, _) =
        LiveReducer::resume(dir.path(), EpochFilter::OwnCaptureOnly, &sel(), ckpt.path())
            .unwrap_or_else(|e| setup_failed(&format!("resume не собрался: {e}")));
    let mut consumer = r
        .snapshot_checked()
        .unwrap_or_else(|e| setup_failed(&format!("стартовый снимок потребителя отвергнут: {e}")));

    // ── Фаза отказов: клиент получает ТОЛЬКО те кадры, что реально отданы ────────────
    gateway::set_effective_max_response_bytes(TINY);
    let mut refusals = 0usize;
    for _ in 0..RETRIES {
        match r.pump(dir.path(), EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Ok((frames, _, _)) => {
                for f in &frames {
                    consumer.apply(f);
                }
                break;
            }
            Err(_) => refusals += 1, // кадров НЕТ — клиенту не досталось ничего
        }
    }
    if refusals < RETRIES {
        setup_failed(&format!(
            "предмет не воспроизведён: из {RETRIES} попыток отказов {refusals}. Предел \
             {TINY} Б обязан валить delta-кадр на фикстуре из {DENSE_TRADES} сделок"
        ));
    }

    // ── Фаза догона при снятом пределе: всё, что сервер ещё отдаст, клиент применяет ──
    gateway::set_effective_max_response_bytes(usize::MAX);
    loop {
        match r.pump(dir.path(), EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Ok((frames, _, _)) if frames.is_empty() => break,
            Ok((frames, _, _)) => {
                for f in &frames {
                    consumer.apply(f);
                }
            }
            Err(e) => setup_failed(&format!("догон при снятом пределе отказал: {e}")),
        }
    }

    // ── ПРЕДМЕТ: то, что СОБРАЛ клиент, равно реплею ─────────────────────────────────
    let sum_vp = |s: &gateway::SeriesBundle| -> i128 {
        s.volume_profile
            .iter()
            .flat_map(|r| r.bins.iter())
            .map(|(_, v)| *v as i128)
            .sum()
    };
    let ref_vp = sum_vp(&reference.series);
    let got_vp = sum_vp(&consumer.series);
    assert_eq!(
        got_vp,
        ref_vp,
        "PL-I-5 P5 / VB-I-2 (R-140): серия, СОБРАННАЯ КЛИЕНТОМ из доставленных кадров, несёт \
         объём {got_vp} против {ref_vp} у независимого реплея — {:.4}× при {refusals} \
         отказавших pump. Сервер при этом может быть исправен (P4 зелен): предмет здесь — \
         ПРОВОД. Меньше единицы — кадры потеряны безвозвратно (курсор двинулся, кадра нет); \
         больше — события свёрнуты повторно. VB-I-2 требует БИТ-ИДЕНТИЧНОСТИ, а не «не хуже»",
        got_vp as f64 / ref_vp.max(1) as f64
    );
    assert_eq!(
        consumer.series.volume_bubbles.len(),
        reference.series.volume_bubbles.len(),
        "PL-I-5 P5 (R-140): клиент собрал {} пузырей против {} у реплея — разница {} \
         событий, не доехавших до потребителя ни одним кадром",
        consumer.series.volume_bubbles.len(),
        reference.series.volume_bubbles.len(),
        reference.series.volume_bubbles.len() as i64 - consumer.series.volume_bubbles.len() as i64
    );

    gateway::set_effective_max_response_bytes(gateway::DEFAULT_MAX_RESPONSE_BYTES);
}

/// **`P6` — ПОД СТОЯЩИМ ПРЕДЕЛОМ ПОДПИСКА ОБЯЗАНА ЛИБО ПРОДВИГАТЬСЯ, ЛИБО ЗАВЕРШИТЬСЯ.**
///
/// Исполнение вердикта `R-143` блокер **`B-2`**: реализована «чистая транзакционность»
/// (`crates/gateway/src/lib.rs:3465`), которую §4bis.5 спеки в ЭТОМ ЖЕ круге пометила
/// `ИСКЛЮЧЁН` с причиной «livelock: события не доедут никогда». Предъявлено исполнением:
/// восемь тиков подряд, курсор неподвижен, кадров ноль, подписка мертва навсегда.
///
/// # Почему `P5` к этому слеп ПО ПОСТРОЕНИЮ
///
/// `P5` делает три отказа, затем `set_effective_max_response_bytes(usize::MAX)` и судит
/// сходимость ПОСЛЕ снятия предела (`:539`). Он проверяет, что серия сходится, когда предел
/// снят, и НИЧЕГО не говорит о поведении, пока предел стоит. **На проде предел не снимает
/// никто.** Разбор класса (`docs/workflow/oracle-blindness-class-2026-08-28.md` §5, правило
/// Р-2) называет это прямо: оракул, снимающий проверяемое ограничение внутри прогона, судит
/// ДРУГОЙ предмет. `P5` от этого не становится ложным — он остаётся честным на своей оси
/// («догон сходится»), и здесь не переписывается; `P6` закрывает ось, которой у него нет.
///
/// # Инвариант, и чего он НЕ предписывает
///
/// Требование одно: **состояние подписки не имеет права оставаться НЕИЗМЕННЫМ неограниченно
/// долго при живом сервере и растущем журнале.** Конструкцию оракул не выбирает — §4bis.5
/// назвала общим кандидатом «терминальность отказа для сессии + ресинк снапшотом», и любой
/// из трёх исходов ниже засчитывается как исполнение:
///
/// 1. **курсор сдвинулся** — реализация дробит партию до укладывающейся в предел;
/// 2. **клиент получил кадр** — то же, наблюдённое со стороны потребителя;
/// 3. **исход ИЗМЕНИЛСЯ между тиками** — подписка объявила себя терминальной, и вызыватель
///    может это различить.
///
/// Провал — конъюнкция всех трёх отрицаний, то есть ровно сигнатура livelock'а.
///
/// # Предел, названный честно
///
/// Пункт 3 различает исходы ПО ТЕКСТУ ошибки: типа «терминально» в API сегодня нет, и
/// вводить его оракул не вправе (это конструкция, зона реализации). Значит реализация,
/// меняющая текст ошибки, не меняя поведения, формально пройдёт. Это тот же класс предела,
/// что у `FOUNDER-APPROVED` (`gates.md` §11) — контур ловит ОТСУТСТВИЕ изменения, а не его
/// осмысленность, — и он назван здесь, а не изображён строгостью. Пункты 1 и 2 подделать
/// нельзя: они наблюдаются со стороны потребителя.
#[test]
fn pl_i_5_p6_standing_cap_must_not_livelock_the_subscription() {
    let _g = serial();
    let dir = journal_of_trades(DENSE_TRADES);

    const TINY: usize = 20_000;
    /// Тиков под стоящим пределом. Восемь — число, которым `R-143` предъявил дефект; меньше
    /// трёх не отличает «медленно» от «никогда».
    const TICKS: usize = 8;

    let ckpt = tempfile::tempdir().expect("SETUP: ckpt tempdir");
    gateway::set_effective_max_response_bytes(usize::MAX);

    // ── SETUP-GUARD на ФИКСТУРЕ, а не на исходе ─────────────────────────────────────
    // Сценарий «предел стоит» реален, если ДАННЫЕ не укладываются в предел одним ответом.
    // Это свойство ФИКСТУРЫ и предела, и оно не зависит от того, как реализация
    // отреагирует.
    //
    // ПЕРВАЯ РЕДАКЦИЯ ЭТОГО GUARD'А ТРЕБОВАЛА ОТКАЗА — и была неверна. Проба достижимости
    // GREEN (кандидат §4bis.5 «дробление партии») показала: честная реализация не
    // отказывает ВООБЩЕ, она делит партию и доставляет. Guard на исход делал оракул
    // КРАСНЫМ ПРОТИВ ПРАВИЛЬНОГО ФИКСА — ровно тот класс, что уже дал ложное красное
    // барьеру дверей (`session-handover-2026-08-29.md` §4). Оставляю след, а не молчаливую
    // замену: guard обязан проверять, что СЦЕНАРИЙ достижим, а не что реализация повела
    // себя ожидаемым образом — иначе он пиннит конструкцию, которую оракул не выбирает.
    let reference = lib_path(dir.path())
        .unwrap_or_else(|e| setup_failed(&format!("эталонный реплей не собрался: {e}")));
    let full_bytes = serde_json::to_vec(&reference.series)
        .unwrap_or_else(|e| setup_failed(&format!("эталонная серия не сериализуется: {e}")))
        .len();
    if full_bytes <= TINY {
        setup_failed(&format!(
            "фикстура из {DENSE_TRADES} сделок даёт {full_bytes} Б при пределе {TINY} Б — \
             данные укладываются в предел, и сценарий «предел стоит» недостижим в принципе"
        ));
    }

    let (mut r, _) =
        LiveReducer::resume(dir.path(), EpochFilter::OwnCaptureOnly, &sel(), ckpt.path())
            .unwrap_or_else(|e| setup_failed(&format!("resume не собрался: {e}")));
    let start_cursor = r.cursor();

    // ── Предел СТОИТ на протяжении всего прогона и НЕ снимается ─────────────────────
    gateway::set_effective_max_response_bytes(TINY);

    let mut frames_total = 0usize;
    let mut outcomes: Vec<String> = Vec::with_capacity(TICKS);
    let mut cursors: Vec<Cursor> = Vec::with_capacity(TICKS);
    for _ in 0..TICKS {
        match r.pump(dir.path(), EpochFilter::OwnCaptureOnly, PUMP_BATCH) {
            Ok((frames, _, _)) => {
                frames_total += frames.len();
                outcomes.push(format!("Ok(frames={})", frames.len()));
            }
            Err(e) => outcomes.push(format!("Err({e})")),
        }
        cursors.push(r.cursor());
    }
    let end_cursor = r.cursor();
    gateway::set_effective_max_response_bytes(gateway::DEFAULT_MAX_RESPONSE_BYTES);

    let cursor_moved = end_cursor != start_cursor;
    let client_got_frames = frames_total > 0;
    let outcome_changed = outcomes.iter().any(|o| o != &outcomes[0]);

    assert!(
        cursor_moved || client_got_frames || outcome_changed,
        "PL-I-5 P6 / R-143 B-2: за {TICKS} тиков ПОД СТОЯЩИМ пределом ({TINY} Б) курсор не \
         сдвинулся ({start_cursor:?} → {end_cursor:?}), клиент получил {frames_total} кадров, \
         и все {TICKS} исходов побайтно одинаковы: «{}». Подписка мертва навсегда при живом \
         сервере и растущем журнале — это livelock, который §4bis.5 спеки назвала причиной \
         вычеркнуть кандидата «чистая транзакционность». PL-I-5 fail-closed требует ОТВЕРГНУТЬ \
         сверхлимитный ответ, а не ПРЕКРАТИТЬ обслуживание подписки без пути восстановления. \
         Курсоры по тикам: {cursors:?}",
        outcomes[0]
    );
}
