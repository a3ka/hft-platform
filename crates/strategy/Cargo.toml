[package]
name = "strategy"
version = "0.0.0"
edition.workspace = true
license.workspace = true

# Слой 4 (docs/fa/strategy-brain.md §6): target vs current → OrderIntent.
# СТРУКТУРНО не зависит от sim/venue-*/journal/risk/tokio/reqwest (ST-I-6): один и тот же
# код решений исполняют и бэктест (sim), и live (runner) — сам он не знает, кто его гоняет.
# ВЛАДЕЕТ формой OrderIntent/OrderKind (M-07 D1; sim ре-экспортирует).

[dependencies]
contracts = { path = "../contracts" }
book = { path = "../book" }
signals = { path = "../signals" }
alpha = { path = "../alpha" }
portfolio = { path = "../portfolio" }
