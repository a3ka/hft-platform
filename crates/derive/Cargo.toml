[package]
name = "derive"
version = "0.0.0"
edition.workspace = true
license.workspace = true

# derive — ЧИСТЫЕ детерминированные downstream-аналитики над T1-потоком (journal-first:
# сырьё в contracts/journal, агрегаты считаются ЗДЕСЬ, не в T1). Без I/O, без wall-clock.
# M-06: funding-breadth. Потребители: research-cli / signals.

[dependencies]
contracts = { path = "../contracts" }
