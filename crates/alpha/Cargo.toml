[package]
name = "alpha"
version = "0.0.0"
edition.workspace = true
license.workspace = true

# Слой 3 (docs/fa/strategy-brain.md): ансамбль сигналов → Forecast.
# Чистый редьюсер: НЕТ tokio/reqwest/rand/wall-clock (ST-I-6 грепы в
# crates/strategy/tests/structural.rs покрывают все три крейта мозга).

[dependencies]
contracts = { path = "../contracts" }
signals = { path = "../signals" }
