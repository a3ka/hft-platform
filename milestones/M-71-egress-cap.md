# M-71 — предел объёма ответа: первые оракулы `PL-I-4`/`PL-I-5`

**Статус:** rev1 · **Автор:** architect · **Дата:** 2026-08-25
**Фаза:** Ф2 (read-path) — предмет не расширяет состав данных и не зависит от Ф0/Ф1
**Класс:** дыра, достижимая СЕГОДНЯ; не подготовка к будущему

---

## 1. Objective

Ответ, отдаваемый клиенту, обязан иметь **предел, действующий fail-closed**: запрос, который
его превышает, отклоняется явной ошибкой, а не обслуживается и не усекается молча. Предел
живёт в `crates/gateway` — там, где строится ответ, — а не только в транспорте, иначе остаётся
байпас-поверхность.

Это **первые оракулы `PL-I-4` и `PL-I-5`** — инвариантов, объявленных в `DESIGN` §22 и до сих
пор числящихся там со статусом «будущие RED-оракулы, PENDING». `PL-I-5` дословно: «Тарифные
лимиты enforce'ятся сервером; **отсутствие/невалидность лимита = отказ, не unbounded**».
`PL-I-4`: «Пользовательский запрос не читает event-store напрямую — только через
проекции/gateway **с cap и квотами**».

## 2. Замер — дыра работает СЕЙЧАС, а не появится при включении полос

### 2.1. Ширину окна задаёт КЛИЕНТ, и она не ограничена сверху

```
crates/gateway-serve/src/wire_v1.rs:5      {"op":"subscribe", …, "selector":{…}} — селектор
                                           приходит ОТ КЛИЕНТА в теле подписки
crates/gateway-serve/src/wire_v1.rs:120    parse_selector(value) — Value → gateway::Selector
crates/gateway-serve/src/session.rs:79-82  ЕДИНСТВЕННАЯ проверка полосы: 0 < b < 1,
                                           плюс сортировка и отсутствие дублей
crates/gateway/src/lib.rs:1878-1917        gateway::validate_selector о `bands` НЕ ЗНАЕТ вовсе
crates/gateway/src/lib.rs:1192             окно heatmap = max(selector.bands)
```

Ни одного ограничения на РАЗМЕР ответа в проекте нет:

```
$ grep -rniE 'max_frame|max_message|max_size|frame_size|max_send|write_buffer' \
      crates/gateway-serve/src/*.rs
(ноль совпадений)
```

Существующий предел `DEFAULT_MAX_SUBSCRIPTIONS = 16` (`crates/gateway-serve/src/lib.rs:191`)
ограничивает **число** подписок, а не **размер** каждой — то есть умножает проблему, а не
ограничивает её.

### 2.2. Собственный замер усиления (не чужая арифметика)

Фикстура воспроизводит ГЕОМЕТРИЮ прод-книги, названную кодом venue: бакеты 0.02 % от mid,
охват ±60 % (`MAX_REL_DIST`) ⇒ 2999 уровней на сторону; 60 временных бакетов при
`timeframe_ms = 1000`. Прогон `gateway::snapshot` + `serde_json::to_vec`:

| `bands` | ячеек heatmap | COB | размер ответа |
|---|---:|---:|---:|
| `[0.001]` — прод-дефолт | 540 | 9 | **62 688 Б** (0.06 МБ) |
| `[0.013]` — граница валидации глубины | 7 740 | 129 | 854 810 Б (0.82 МБ) |
| `[0.015 … 0.60]` — канонический набор | 359 880 | 5 998 | **45 261 937 Б (43.2 МБ)** |
| `[0.99]` — **одна полоса, принимается сегодня** | 359 880 | 5 998 | **45 242 536 Б (43.2 МБ)** |

**Усиление ×722 одним сообщением подписки.** Строка `[0.99]` — ключевая: она не требует ни
смены прод-конфига, ни включения полос. Клиент присылает её сам, и проверка `0 < b < 1`
пропускает.

**Честная граница замера:** книга синтетическая (равномерная), число бакетов — 60. Прод-замер
того же порядка есть независимо: `heatmap len=1697` при `GATEWAY_BANDS=0.001`
(`PROJECT-STATE.md:1894-1895`), и `C-094` B5 оценил канонический набор в ≈176 000 ячеек
≈22 МБ на клиента. Два независимых счёта дают один вывод; точное прод-число снимается стендом,
а не этой спекой.

### 2.3. Почему это опаснее, чем кажется по одной цифре

`DESIGN` §16 фиксирует замером: потолок сегодняшней конструкции ≈700 сессий, и **прод
деградирует уже при 1–2 зрителях**. 43 МБ на подписку × 16 подписок на соединение ≈ 690 МБ на
одно соединение. Машина — 7.5 ГБ. Злого умысла не требуется: достаточно клиента, который
«попросил поглубже».

## 3. Предмет — что и где

**Два слоя, и оба обязательны.** Один без другого либо не ловит, либо ловит не то.

| слой | что | почему нужен именно он |
|---|---|---|
| **дверь** | предел ширины полосы из конфигурации, проверяемый при старте и при подписке | дёшево, отсекает абсурд сразу, даёт клиенту внятную ошибку до всякой работы |
| **предел ресурса** | жёсткий потолок на РАЗМЕР построенного ответа, fail-closed | ширина — ПРОКСИ; при уплотнении книги тот же 0.013 даст другой объём. `testing.md`: «оракул границы ресурса меряет ресурс, а не прокси» |

Предел ресурса живёт в `crates/gateway` — там, где ответ строится. Гвард только в
`gateway-serve` оставил бы байпас: `Selector` собирают напрямую чекпоинтер (M-38b),
shared-tailer, `research-cli` и replay. **Тот же довод, которым `GW-I-14` посажен в
`gateway::validate_selector`** (`crates/gateway/src/lib.rs:1893-1905`), с парным
библиотечным оракулом — прецедент `M-69`, структура которого здесь воспроизводится.

## 4. Allowed / Forbidden paths

**engine-dev:** `crates/gateway/src/**`, `crates/gateway-serve/src/**`, `docker-compose.yml`
(объявление предела как env — по образцу `GATEWAY_WINDOW_MS`/`GATEWAY_MAX_SUBSCRIPTIONS`).
**architect:** `crates/gateway/tests/**`, `crates/gateway-serve/tests/**`,
`milestones/M-71-*.md`, `scripts/verify_M-71.sh`.

| путь | почему нельзя |
|---|---|
| `crates/contracts/**` | форма событий не меняется |
| `crates/venue-*/**`, `crates/journal/**` | состав ЗАПИСИ — граница C |
| `crates/book/src/**` | предмет — выдача, не книга |
| `GATEWAY_BANDS` в `docker-compose.yml` | состав ВЫДАЧИ — граница C и предмет `M-70`; здесь ставится ПРЕДЕЛ, а не меняется состав |
| `TECH-DEBT.md`, `PROJECT-STATE.md` | reviewer-owned |

### 4.1. ЗАПРЕЩЕНО (правка существующего кода)

| запрещено | почему |
|---|---|
| **молча усекать** ответ при превышении | `PL-I-7`: деградация не выдаётся за норму. Усечённый ответ зелен во всех liveness-проверках и врёт клиенту. Либо явная ошибка, либо явно помеченное усечение — тихого пути нет |
| ставить предел ТОЛЬКО в `gateway-serve` | байпас-поверхность: `Selector` собирают напрямую четыре потребителя (§3) |
| трактовать отсутствие/невалидность конфигурации предела как «без предела» | `PL-I-5` дословно; ровно дефект, который `M-69` уже чинил для окна (`GW-I-14`) |
| менять `GATEWAY_BANDS`, `MAX_REL_DIST`, `BUCKET_WIDTH` | состав данных и выдачи — не предмет |
| ослаблять `VB-I-10` (окно памяти) | предел ВЫДАЧИ не заменяет предел ПАМЯТИ; они про разное |
| ослаблять `VB-I-2` (live == replay) | предел обязан действовать ОДИНАКОВО на обоих путях, иначе реплей перестанет воспроизводить live |
| менять форму `error`-сообщения протокола | `invalid_selector` (`wire_v1.rs:156`, `CT-RFC-09` §2.7) уже покрывает «селектор непригоден»; переиспользование аддитивно и не трогает контракт с фронтом |

## 5. §Tasks

| # | Задача | Зона | Статус | Оракул |
|---|---|---|---|---|
| 1 | Предел РАЗМЕРА ответа существует и действует в `crates/gateway`: превышение → `Err`, не обслуживание | engine-dev | ⏳ OPEN | `PL-I-5` A |
| 2 | Отказ ЯВНЫЙ: молчаливого усечения не существует ни на одном пути | engine-dev | ⏳ OPEN | `PL-I-5` B |
| 3 | Байпаса нет: `Selector`, собранный напрямую, отвергается так же, как пришедший по проводу | engine-dev | ⏳ OPEN | `PL-I-4` C |
| 4 | Отсутствие/невалидность конфигурации предела → отказ СТАРТА прод-бинаря, не unbounded | engine-dev | ⏳ OPEN | `PL-I-5` D |
| 5 | **Честная работа не ломается:** прод-дефолт и рабочий диапазон принимаются | engine-dev | ⏳ OPEN | `PL-I-5` E (анти-ложное-КРАСНОЕ) |
| 6 | Предел объявлен клиенту внятно: код `invalid_selector` + сообщение, называющее предел и полученную величину | engine-dev | ⏳ OPEN | `PL-I-5` F |
| 7 | `scripts/verify_M-71.sh` — паритет с CI, ≥1 проверка на задачу | architect | ⏳ OPEN | сам себе |

### 5.1. Величина предела — ПРЕДЛАГАЕТСЯ спекой, УТВЕРЖДАЕТСЯ founder'ом

Спека требует, чтобы предел СУЩЕСТВОВАЛ и был fail-closed. Его ЧИСЛО — продуктовое решение
того же класса, что `DEFAULT_MAX_SUBSCRIPTIONS = 16` («подписанная норма»,
`crates/gateway-serve/src/lib.rs:176-191`), и назначается founder'ом, а не architect'ом.

Предложение, выведенное из замера §2.2: **20 000 ячеек** на ответ. Основание — прод сегодня
отдаёт 1 697 ячеек (`PROJECT-STATE.md:1894-1895`), то есть предложение даёт ≈×12 запаса на
рост плотности и окна, и при этом отсекает случай `[0.99]` (359 880 ячеек) с трёхкратным
порядком.

**Это же число делает явной коллизию с `M-70`, и в этом половина ценности спеки.** Канонический
набор полос даёт 359 880 ячеек — то есть при любом разумном пределе включение полос «как есть»
НЕВОЗМОЖНО. Значит `M-70` обязан нести либо снижение плотности на глубине (более грубые бакеты
дальше от mid), либо явное решение founder'а о величине предела. Сегодня это противоречие
существует и молчит; после `M-71` оно упрётся в красный тест на этапе плана, а не в инцидент
на проде.

### 5.2. Якорь мутации — часть задачи 1

Шаг `C` гейта ИСПОЛНЯЕТ мутацию, а не грепает (урок `M-68`: шаг, обещавший «предъявлено
ПРОГОНОМ», предъявлял существование строки — `C-138` п.2). Мутацию нельзя внести без
стабильной точки приложения, поэтому спека её называет:

```rust
// MUT-ANCHOR M-71-LIMIT — точка мутации гейта (spec M-71 §7 шаг C); сигнатура зафиксирована.
fn enforce_response_limit(cells: usize, limit: usize) -> io::Result<()>
```

Гейт вносит в КОПИЮ дерева первой строкой тела `return Ok(());` — предел перестаёт действовать
— и прогоняет набор ТАМ. **Требование ДВУСТОРОННЕЕ, и в этом суть:** набор обязан ПОКРАСНЕТЬ,
а оракул `E` (анти-ложное-КРАСНОЕ) — остаться ЗЕЛЁНЫМ. Мутация, роняющая всё подряд,
доказывала бы лишь то, что тесты реагируют на код, а не что они привязаны к дефекту.

Шаг fail-closed: якоря нет ⇒ FAIL с явным «SETUP НЕ СОСТОЯЛСЯ».

## 6. RED-оракулы — форма

`crates/gateway/tests/red_egress_cap.rs` (A, B, C, E, F) +
`crates/gateway-serve/tests/red_egress_cap_startup.rs` (D) — структура зеркалит `M-69`
(`red_window_guard_startup.rs` + парный `red_window_selector_guard.rs`).

| ID | пиннит | обязан краснеть против |
|---|---|---|
| A | запрос сверх предела → `Err`; **размер меряется в РЕСУРСЕ** (ячейки/байты построенного ответа), не в ширине полосы | сегодняшнего состояния: `[0.99]` обслуживается 43 МБ |
| B | отказ ЯВНЫЙ; принятый запрос отдаёт ПОЛНЫЙ ответ | «фикса» через тихое усечение — он зелен во всех liveness-проверках |
| C | `Selector`, собранный напрямую (без транспорта), отвергается так же | гварда, посаженного только в `gateway-serve` |
| D | невалидная/отсутствующая конфигурация предела → бинарь НЕ СТАРТУЕТ | «parse-error → None → unbounded» — дефект того же класса, что `GW-I-14` чинил для окна |
| E | **прод-дефолт и рабочий диапазон принимаются** | переширокой заглушки «всегда `Err`»; ложное КРАСНОЕ на честной работе опаснее пропуска, и это единственный оракул, который его ловит |
| F | сообщение об отказе называет ПРЕДЕЛ и ПОЛУЧЕННУЮ величину | немому `Err`, по которому клиент не может понять, что просить |

**Обязательные свойства набора** (`testing.md`):

- **дегенерированный вход:** пустая книга · односторонняя книга · ответ РОВНО на пределе и на
  единицу за ним · одна полоса против семи при равном итоговом объёме;
- **предел меряется РЕСУРСОМ:** оракул A считает величину построенного ответа, а не проверяет
  ширину полосы — иначе он пиннит прокси, а при уплотнении книги пропустит превышение;
- **парный vantage (A + E):** заглушка «всегда `Err`» валит E, заглушка «всегда `Ok`» валит A;
- **мутационный контроль:** нейтрализация проверки предела обязана ронять A и C, но НЕ E;
  результат — в Done Block.

## 7. Acceptance — `scripts/verify_M-71.sh`

Паритет с CI целиком (`fmt` + `clippy --all-targets --all-features -D warnings` + `test --all`);
по шагу на каждую задачу §5; исполняемый мутационный контроль предела; `crates/contracts` и
`GATEWAY_BANDS` не тронуты диапазоном. Решение — по КОДУ ВОЗВРАТА.

**Деплой-гейт `gates.md` §8 обычный:** предмет не меняет поведение данных, только отвергает
неподъёмные запросы. Достаточно штатных проверок плюс подтверждения, что прод-дефолт
`GATEWAY_BANDS=0.001` после выката обслуживается как раньше (задача 5 на живом проде).

### 7.1. Базовая линия СНЯТА ПРОГОНОМ до работы dev'а

`bash scripts/verify_M-71.sh` → **`VERDICT: FAIL (5)`, `exit=1`.** Красные — поимённо:

| # | шаг | почему красен СЕЙЧАС | что зазеленит |
|---|---|---|---|
| 1 | `#0` `cargo test --all` | агрегат включает оба новых RED-набора | задачи 1-6 |
| 2 | `A` библиотечные оракулы | 4 из 6 красны: `A` (запрос сверх предела обслуживается — замер в сообщении: 59 980 ячеек против 100, ×600), `B` (нет отказа ⇒ нет и явности), `C` (`frames_since` и warm-путь тоже обслуживают), `F` (текста отказа не существует). Зелены 2 КОНТРОЛЯ: `E` (прод-дефолт обслуживается) и `E-2` (вырожденные книги) | задачи 1,2,3,6 |
| 3 | `B` стартовый гвард | 8 из 10 красны — переменная не читается вовсе, конфиг собирается, старт разрешён. Зелены 2 парных vantage: `valid_limits_start`, `absent_limit_starts_with_default` | задача 4 |
| 4 | `C` мутационный контроль | якоря `MUT-ANCHOR M-71-LIMIT` в реализации нет ⇒ мутацию нельзя ВНЕСТИ; шаг fail-closed и говорит это дословно | задача 1 |
| 5 | `D` доставка ручки | `GATEWAY_MAX_RESPONSE_CELLS` не объявлен в `docker-compose.yml` — предел, не видимый оператору, есть «построено-не-подключено» (`gates.md` §4 DoD) | задача 4 |

**`cargo clippy --all-targets` ЗЕЛЁН, и это осознанная разница с `M-68`.** Ни один оракул не
COMPILE-RED: они не ссылаются на ещё не существующие типы и константы, поэтому предъявимы
КРАСНЫМИ, а не «не собралось». Цену смешения этих двух состояний уже платили (`M-68` rev2,
`C-138` п.3).

**Зелёные шаги — КОНТРОЛИ, а не украшение:** `E` целиком (`VB-I-10` окно памяти,
`red_snapshot_noclone`, `VB-I-2` live == replay, существующий cap подписок, стартовый гвард
окна `GW-I-14`), `F`/`G`/`H` (Block-C, состав выдачи, зона). Покраснел хоть один по ходу
реализации ⇒ фикс куплен ценой соседнего инварианта, и это блокер.

## 8. Пределы, названные честно

- **`M-71` не делает выдачу быстрее и не решает Ф2.** Разделяемая проекция, квоты, сжатие —
  отдельная работа. Здесь ставится один предел, без которого всё остальное строится на песке.
- **Предел не заменяет квоты.** `PL-I-5` шире: тарифные лимиты, per-user cap'ы. Здесь —
  минимальная его часть, закрывающая достижимую сегодня дыру.
- **Точное прод-число даёт стенд, а не эта спека** (§5.1). Замер §2.2 — синтетический по книге
  и честно это объявляет.

## 9. Handoff

**Следующий:** `critic`, plan-time (триггер `gates.md` §1 — новый механизм на несущем пути;
`risk-critic` НЕ триггерится: read-only, order-путь не задет). Затем `engine-dev` →
`tester` → `reviewer`.

**founder** — величина предела (§5.1) и следствие для `M-70` (включение полос «как есть»
невозможно ни при каком разумном пределе).

## 10. Cross-references

- `docs/DESIGN.md` §22 (`PL-I-4`, `PL-I-5` — здесь получают первые оракулы), §16 (потолок
  read-path'а, замеры прода)
- `milestones/M-69-window-guard.md` + `crates/gateway-serve/tests/red_window_guard_startup.rs`
  — прецедент структуры (fail-closed конфиг + парный библиотечный анти-байпас)
- `milestones/M-70-depth-bands-enablement.md` §0.3 — та же дыра, названная при разборе
  включения полос; `M-71` выносит её вперёд и отдельно
- `research/critiques/C-094-M-68.md` B5 — первая оценка объёма выдачи
- `docs/rfc/CT-RFC-09-ws-session.md` §2.7 — коды ошибок протокола (`invalid_selector`)
