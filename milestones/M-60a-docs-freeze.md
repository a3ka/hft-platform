# M-60a — замок процессного слоя: правка правил только с разрешения founder'а

**Статус:** PROPOSED · **Дата:** 2026-08-06 · **Автор:** architect
**Ветка:** `feat/M-60-mechanisms` · **Зонт:** `milestones/M-60-mechanisms.md` (класс отказа,
что взято из einhard и почему, бюджет корпуса)
**Гейты:** critic (триггер `gates.md` §1: правка гейтов + `.claude/rules/**`) → dev → tester → reviewer

---

## 1. Objective

**Инструкция founder'а 2026-08-05, дословно:** «наложить запрет на дальнейшие правки этих
документов без моего разрешения». Речь о процессном слое.

Milestone делает запрет **проверяемым**, а не записанным. Причина ровно та, что и у всего
M-60: норма, живущая прозой, нарушается не злым умыслом, а под сжатием контекста. Мера —
`TD-101`: предписание «git-личность = роль» прожило три недели и не сработало ни разу.

**Зона замка:** `.claude/rules/**`, `.claude/agents/**`, `.claude/wrappers/**`, `CLAUDE.md`,
`docs/04-workflow.md`.

`.claude/wrappers/**` добавлен по `C-065` (блокер 3): это ЖИВОЙ носитель процесса —
`pi-dev.sh` бутстрапит worktree и инжектит `dispatch-mandate.md` в систем-промт пяти
внешних ролей. Запереть профили, оставив обвязку открытой, непоследовательно: поведение
агента задаётся и тем, и другим. Там же живёт норма, прямо противоречащая `gates.md` §8
(«НЕ пушь» против «intra-chain push обязателен») — то есть цена открытой обвязки уже
предъявлена фактом.
`docs/DESIGN.md` и `docs/fa/*` НЕ включены: это архитектура, а не процесс, у неё свой
маршрут (`gates.md` §9).

**Разрешение = след, а не разговор.** Коммит, трогающий зону, обязан нести в СВОЁМ теле
`FOUNDER-APPROVED: <причина>` (причина не короче 12 символов). Тело коммита читается через
год; переписка — нет. Механика та же, что у действующего `ALLOW-ARTIFACT-DELETE`.

## 2. Allowed / Forbidden paths

**architect:** `milestones/M-60a-*.md` · `scripts/tests/red_docs_freeze.sh` (✅ готов) ·
`scripts/verify_M-60a.sh` · `.claude/rules/gates.md` §11 (норма, ≤10 строк — с токеном).
**dev:** `scripts/check_docs_freeze.sh` · `.github/workflows/ci.yml` (джоб + `status-check.needs`).
**Forbidden:** `crates/**` (milestone не трогает код), `research/{critiques,reviews,arbitration}/*`,
`scripts/check_protected_artifacts.sh` и его проба (соседний работающий барьер).

**Contract impact:** нет.

## 3. §Tasks

| # | Задача | Зона | Статус | Оракул |
|---|---|---|---|---|
| 1 | `scripts/check_docs_freeze.sh` — база из события, fail-closed, токен в КАЖДОМ коммите, **все виды изменения зоны: M, D, R** | dev | ⏳ OPEN | `red_docs_freeze.sh` FR-1..FR-14 |
| 2 | Джоб в `ci.yml` + проба + `status-check.needs` **И сверка `needs.<job>.result`** | dev | ⏳ OPEN | шаг W (разбором workflow) |
| 3 | Норма `gates.md` §11 (≤10 строк) + `COGNITIVE-ONLY` как легальный исход | **architect** | ⏳ OPEN | шаг S (самореференция) |
| 4 | `scripts/verify_M-60a.sh` | **architect** | ⏳ OPEN | — |
| 5 | **TD (MAJOR):** branch protection на `main` обязан требовать `status-check`. Вне гейта по §3ter; проверяется настройками/API GitHub, то есть действием founder'а или reviewer'а | **reviewer** заводит при close-out | ⏳ OPEN | — |

Задача 3 идёт ПОСЛЕ зелёных 1–2: норма о механизмах, приземляемая без механизма, была бы
собственным контрпримером.

## 3bis. Критерий достаточности оракула (rev4 — причина, по которой круги не сходились)

Четыре круга дали четыре REJECT, и ни один не был о замысле замка: все — о пробе. Разбор
`R-038` называет механику точно: **планка двигалась каждый круг, потому что критерий
достаточности не был назван ни одной стороной.** Критик предъявлял очередной обход,
architect закрывал именно его, круг повторялся. Перечисление обходов дна не имеет:
обходов бесконечно много, сценариев конечное число.

Отсюда — критерий, вписанный в спеку ДО правки пробы. Он есть проверяемое утверждение о
КЛАССЕ реализаций, а не список случаев.

### 3bis.1 Инвариант формулируется от РЕЗУЛЬТАТА

> **Ни одно изменение зоны замка не попадает в проверяемый диапазон без токена
> `FOUNDER-APPROVED` в СВОЁМ коммите** — независимо от способа изменения, от типа события и
> от формы истории.

Формулировка от результата, а не от способа, — урок соседнего барьера
`check_protected_artifacts.sh`, добытый им за три итерации критика: пока инвариант звучал
как «не удалять», находились rename-out, evil-merge и `-s ours`; переформулированный «файл
обязан существовать на HEAD» закрыл все сразу. В `6081ce7` я эту формулировку процитировал —
и применил только к оси удаления, оставив прочие оси перечислением случаев.

### 3bis.2 Оси вариативности названы явно — их ТРИ

| ось | значения | почему это ось, а не случай |
|---|---|---|
| **1. Способ изменения зоны** | `M` изменён · `A` добавлен · `D` удалён · `R` уведён переименованием | git различает их статусами; реализация может обработать один и пропустить другой |
| **2. Тип события** | `push` · `pull_request` | база диапазона берётся из разных полей события; реализация может знать одно поле |
| **3. Форма истории** | прямой коммит · side-commit + merge · недостоверная база (zero-SHA / пустая / не-предок) | токен может лежать не в том коммите; база может не устанавливаться |
| **5. Носитель токена** | тело коммита (легитимный) · subject · содержимое диффа/файла · путь файла | спека §1 требует токен в ТЕЛЕ; реализация, ищущая шире, авторизует не то. Крайний случай — **самоавторизующийся коммит**: строка `FOUNDER-APPROVED:` в СОДЕРЖИМОМ запертого файла разрешает саму себя |
| **4. Член зоны** | `.claude/rules/**` · `.claude/agents/**` · `.claude/wrappers/**` · `CLAUDE.md` · `docs/04-workflow.md` | зона — не один путь, а пять; реализация может стеречь префикс `.claude/rules/` и пропускать остальные |

Матрица — 4 × 2 × 5 × 5 × 4 (ось 5 добавлена по `A-005` §4). Полнота заявляется **относительно этого перечня**, никогда абсолютно.

> **Ось 4 найдена architect'ом при перестройке набора (rev4), а не критиком.** Организовав
> сценарии по способу изменения, я потерял покрытие членов зоны: `agents`, `wrappers` и
> `04-workflow.md` не остались ни в одном случае, и реализация, стерегущая только префикс
> `.claude/rules/`, прошла бы почти весь набор. По правилу §3bis.4 это категория **(ii) —
> новая ось**, поэтому перечень пересмотрен, а не залатан. Это первая проверка критерия на
> себе: он сказал, КАК классифицировать находку и ЧТО с ней делать.

### 3bis.3 Условия достаточности

Набор достаточен относительно §3bis.2, когда выполнено всё:

1. **каждое значение каждой оси** покрыто ≥1 сценарием-НАРУШЕНИЕМ (обязан краснеть);
2. **каждая ось** покрыта ≥1 ЛЕГИТИМНЫМ сценарием (обязан зеленеть) — иначе набор проходит
   реализация «запретить всё», что уже случалось (`stub-always-1` → FAIL 2);
3. **манифест** отображает `сценарий ⇒ ось ⇒ значение`, и проба сверяет манифест с
   фактически исполненным (не число, а состав — `C-067` F-3: согласованная правка счётчика
   скрывает потерю сценария);
4. заявление о полноте в спеке и в шапке пробы **называет перечень осей**.

### 3bis.4 Правило остановки — когда круг ЗАКАНЧИВАЕТСЯ

Новая находка о полноте набора классифицируется в одну из двух категорий, и категория
определяет, что делать:

| категория | что это | как чинится |
|---|---|---|
| **(i) новое ЗНАЧЕНИЕ на известной оси** | дыра в матрице покрытия | добавить сценарий; круг законен и короток |
| **(ii) новая ОСЬ** | перечень осей был неполон | пересмотреть §3bis.2 и критерий; это находка ДРУГОГО рода, а не заплата |

Находка, не относящаяся ни к (i), ни к (ii), **не является находкой о полноте набора** — она
может быть верной по существу, но её место не в этом цикле.

Отсюда прямое следствие, ради которого критерий и вводится: **предъявленная дырявая
реализация обязана указывать ось и значение.** «Вот шестой обход» без этого — не
опровержение полноты, потому что полнота заявлена относительно осей, а не относительно
воображения.

## 3ter. Судьба W-шага — решение (б): статический разбор НЕ доказывает блокировку

Ось 1 разбора `R-038` — четыре круга подряд об одном шаге. Планка двигалась: греп → «имя
шага не считается запуском» → «сравнение + `exit 1`» → «а если `exit 1` в другом шаге,
отключённом через `if: ${{ false }}`». Каждое ужесточение было по-своему верным и каждое
оставляло следующую форму.

**Причина не в регулярном выражении. Утверждение «красное блокирует merge» НЕДОКАЗУЕМО из
содержимого репозитория, потому что этого факта в репозитории нет.** Падение джоба
`status-check` само по себе merge не блокирует: блокировку включает **branch protection** —
настройка GitHub («Require status checks to pass»), которая живёт в настройках репозитория,
а не в файлах. Никакой разбор `ci.yml` её не увидит.

Четыре круга ужесточали доказательство утверждения, которое артефакт подтвердить не может.

**Что остаётся в шаге W (и это гейт доказать МОЖЕТ) — предусловия блокировки со стороны
репозитория:**

1. скрипты исполняются реальным телом `run:` какого-то джоба (не именем шага, не комментарием);
2. этот джоб объявлен в `status-check.needs`;
3. `status-check` сравнивает его `result` и завершается ненулевым кодом.

Три пункта называются **предусловиями**, а не доказательством. Их отсутствие означает, что
блокировки точно нет; их наличие означает, что со стороны репозитория сделано всё возможное.

**Вторая половина выносится ЗА границу гейта** — с записью долга, по прецеденту `gates.md` §4
«built-not-wired»: требуется, чтобы branch protection на `main` требовал прохождения
`status-check`. Проверяется через настройки/API GitHub, то есть действием founder'а или
reviewer'а, а не прогоном скрипта. Severity **MAJOR**: без этого весь наш CI-контур —
предупреждение, а не барьер, и это касается не только замка.

Это решение закрывает ось 1 навсегда: пятое ужесточение регулярки было бы четвёртой
попыткой доказать недоказуемое.

### 3bis.5 Семантика per-commit нормативна (`A-005` §4.5)

Токен требуется **в теле СВОЕГО коммита**, и проверка идёт покоммитно, а не по итоговому
диффу диапазона. Отсюда следствие, названное явно, чтобы не всплыло находкой: **реверт-пара**
(правка зоны и её реверт в одном диапазоне, net-diff чист) — **нарушение**. Коммит без токена
в диапазоне был, и этого достаточно.

### 3ter.1 Дополнение по `A-005`: branch protection НЕДОСТУПЕН вовсе

Разбор проверил фактом: настройка branch protection на этом репозитории возвращает **403** —
репозиторий private на бесплатном плане. То есть решение (б) сильнее, чем я заявлял: вопрос
не «включить забыли», а «включить НЕЛЬЗЯ на текущем плане».

**Долг (MAJOR), формулировка для reviewer'а при close-out:** «блокировка merge отсутствует
как класс: `status-check` может краснеть, но не препятствовать merge, потому что branch
protection недоступен (403, private + free plan). Весь CI-контур — предупреждение, а не
барьер». Три опции — **граница C, решает founder**: платный план · публичный репозиторий ·
принять как есть с записанным риском.

## 4. RED-оракул (rev3 по `C-066`)

`scripts/tests/red_docs_freeze.sh` — **24 сценария** по пяти осям. Число НЕ дублируется в спеке и в
гейте: проба несёт МАНИФЕСТ «имя ⇒ ось ⇒ значение» и валит прогон при расхождении СОСТАВА.
Прежде оно жило в трёх местах (шапка «12», фактических 13, порог гейта «≥11») и разошлось
во всех трёх — `C-066` это зафиксировал. Литерал, живущий отдельно от предмета, врёт всегда.

Проверено ЧЕТЫРЬМЯ подделками, ни одна не проходит:

| подделка | вердикт | что ловит |
|---|---|---|
| гейт `exit 0` всегда | **FAIL (13)** | отсутствие замка |
| гейт `exit 1` всегда | **FAIL (2)** | ложные срабатывания (FR-2, FR-3) |
| токен ищется только в HEAD-коммите | **FAIL (4)** | FR-10, FR-11, FR-12, FR-13 |
| **смотрит только ИЗМЕНЁННЫЕ файлы** (`--diff-filter=M`) | **FAIL (5)** | FR-5, FR-9, FR-11, **FR-13, FR-14** |

Четвёртая — находка `C-066`: реализация, слепая к УДАЛЕНИЮ и ПЕРЕИМЕНОВАНИЮ запертого
файла, проходила пробу целиком, потому что ни одного такого сценария не было. Урок соседнего
барьера `check_protected_artifacts.sh` — «инвариант формулируется от РЕЗУЛЬТАТА, а не от
способа исчезновения» — к замку применён не был; исправлено FR-13/FR-14.

Покрытие: правка правил без токена (FR-1) · с токеном (FR-2) · вне зоны (FR-3) · токен без
внятной причины (FR-4) · токен из ЧУЖОГО коммита (FR-5) · профили (FR-6) · `04-workflow.md`
(FR-7) · база zero-SHA и пустая (FR-8/8b) · merge не лазейка (FR-9) · токен не действует
задним числом (FR-10) · токен merge не покрывает side (FR-11) · обвязка агентов (FR-12) ·
**удаление запертого файла (FR-13)** · **увод из зоны переименованием (FR-14)** ·
самопроверка покрытия.

Страж setup'а: пока гейта нет, `bash` вернёт 127, и негативные сценарии позеленели бы на
пустом месте. Проба это ловит и умирает с «SETUP НЕ СОСТОЯЛСЯ».

## 5. Запретный список

| Запрещено | Почему |
|---|---|
| База сравнения `origin/main` вместо базы из события | блокер B1 (`C-006`): на push-событии диапазон пуст ⇒ гейт зелён ВСЕГДА |
| Проверять проводку в CI грепом или только по `needs` | `C-064` F-064-2 + `C-065` блокер 2. Греп ловится комментарием; разбор без учёта тела `run:` ловится `- name:`; членство в `needs` при `if: always()` merge не блокирует. Все три формы ложной подключённости проверены прогоном |
| Ослаблять `check_protected_artifacts.sh` ради прохождения | соседний барьер, проба 20/20 |
| Расширять зону замка на `docs/DESIGN.md`, `docs/fa/*` | архитектура ≠ процесс; свой маршрут |
| Считать токен ДОКАЗАТЕЛЬСТВОМ действия founder'а | это аудит-след, не подпись. Подделываем ⇒ остаточный риск назван явно, а не замаскирован |

## 6. Acceptance — `scripts/verify_M-60a.sh`

Паритет с CI (`fmt --check`, `clippy --all-targets -D warnings`, `test --all`), плюс:

| шаг | проверка |
|---|---|
| A | `check_docs_freeze.sh` существует, исполним, парсится; fail-closed на пустой базе |
| F | `red_docs_freeze.sh` → exit 0. Порог в гейте НЕ дублируется: покрытие сверяет сама проба (`EXPECTED_SCENARIOS`) |
| S | **самореференция**: собственный диф M-60a проходит замок (токен на месте) |
| W | **ПРЕДУСЛОВИЯ** блокировки (§3ter, решение «б»): скрипты исполняются телом `run:` · джоб в `status-check.needs` · `status-check` сравнивает его `result` и завершается ненулевым кодом. Гейт НЕ доказывает блокировку — её включает branch protection вне репозитория |
| P | регресс: `red_protected_artifacts.sh` → 20/20 |

Решение — по коду возврата; финальная строка `VERDICT: PASS`/`FAIL`.

## 7. Что этот milestone НЕ делает

Бюджет корпуса, `GATE-META`/subject-lock, проводка `verify_design_claims`, чистка на 279
строк — в **M-60b** и **M-60c**. Разрез сделан после того, как замер показал: M-60 простоял
10.5 часов без работы, а его набор написан за 19 минут (`M-60-mechanisms.md` §«Разрез»).
Крупный предмет под сильным критиком ходит кругами, потому что каждая находка
переоткрывает всё; узкий — закрывается за круг.
