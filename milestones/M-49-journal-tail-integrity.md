# M-49 — честность `next_seq`: нечитаемый хвост не даёт старт с `meta_seq` (TD-049)

**Статус:** SPEC rev3 (после ДВУХ REJECT'ов PR-гейта; RED обновлён; ждёт dev)
**Риск:** класс **R2b**, оставшийся жить на пути ПОРЧИ после закрытия M-40.
**Severity:** TD-049 переоценён MINOR → **CRITICAL по последствию** (необратимая порча
append-only журнала, необнаружимая в моменте). Достижимость — restore-drill R1
(~2026-08-10, founder-действие): частичная выкачка или bit-rot.
**Гейты:** critic (касание `crates/journal`, ≥5 коммитов) → engine-dev → tester → reviewer.
risk-critic НЕ требуется (нет order-path; данные/ops).


## ⚠️ rev2 — почему rev1 отвергнут PR-гейтом (читать ПЕРЕД реализацией)

Реализация rev1 прошла ВСЕ оракулы и была завёрнута reviewer'ом: она ослабила `segments()` —
строгий прод-путь чтения журнала стал молча пропускать повреждённый хвостовой сегмент.
Формально зелено, фактически — «тихая ложь в данных»: `list_segments`/`stream` отдали бы
research и бэктесту журнал С ДЫРОЙ без единой ошибки.

**Корень — дефект спеки architect'а, не работа dev'а.** Цепочка была такая:

```
оракулы rev1 звали stream() на КАТАЛОГЕ С ПОРЧЕЙ (нужен был «максимальный читаемый seq»)
        ↓
внутренний readable_max_seq тоже был реализован через stream()  ← строгий путь
        ↓
чтобы валидация операторской декларации заработала на битом каталоге,
пришлось ослабить segments()                                     ← ЖЕРТВА соседнего инварианта
```

**Разделение путей (уже существует в крейте — изобретать не нужно):**

| Путь | Семантика | Кому |
|---|---|---|
| `segments()` / `stream()` / `list_segments` | **строгий, fail-closed** | потребителям данных: кокпит, research, бэктест |
| `iter_segments_sorted()` / `recover()` / `read_all()` | **терпимый, диагностический** | офлайн-работе; наружу данные не отдаёт |

**Правило rev2 (BINDING):** внутренняя нужда M-49 — «каков максимальный читаемый `seq`» для
валидации операторской декларации — обслуживается ТЕРПИМЫМ путём внутри крейта. Ослаблять
строгий путь ради неё ЗАПРЕЩЕНО. Оракул `ti_6` это пиннит: при повреждённом сегменте
`list_segments` обязан вернуть `Err`, а `stream` — отказать либо честно упасть в итерации,
но НЕ дочитать молча.

**Что изменено в оракулах rev2:**
- `red_tail_integrity_operator.rs` — эталон «максимального читаемого seq» снимается ДО
  внесения порчи (через `list_segments` на здоровом каталоге), а не после. Оракул больше
  не требует терпимости от строгого пути.
- `red_tail_integrity.rs` — добавлен **`ti_6`**: строгий путь остаётся строгим.

**Проходимость rev2 доказана прототипом:** `segments()` возвращён к строгому виду +
внутренний `readable_max_seq` переведён на `iter_segments_sorted` с сознательным пропуском
нечитаемых сегментов → **9/9 оракулов зелёные**, весь крейт `journal` зелёный (25 блоков,
0 FAILED). Прототип откачен.

**Процессный вывод (заносится в `.claude/rules/testing.md` отдельной правкой):** мутационный
контроль обязан отвечать не только на вопрос «падает ли оракул при откате фикса», но и на
вопрос **«какой смежный инвариант пришлось ослабить, чтобы пройти оракул»**. Первый вопрос
rev1 проходил, второй — нет.


## ⚠️ rev3 — второй REJECT и запретный список (читать ВМЕСТЕ с rev2)

PR-гейт завернул и rev2. Разбор architect'а нашёл дефект, которого не было ни в спеке, ни в
оракулах: реализация при применении операторской декларации **физически перемещала**
повреждённый сегмент в `quarantine/` — прямо внутри `open_with`.

**Почему это блокер:**
1. `open_with` — горячий путь старта recorder'а. Его контракт: открыть/создать сегмент и
   определить `next_seq`. Перемещение файлов ДАННЫХ в этот контракт не входит.
2. Оператор разбирает инцидент по состоянию каталога. Молчаливая перестановка файлов
   показывает ему НЕ ту картину, которую описывает runbook.
3. Ответственность продублирована: `docs/ops-journal-tail-unreadable.md` §1 предписывает
   карантин РУКАМИ. Два механизма на одно действие — гарантированное расхождение.
4. Поведение не было покрыто НИ ОДНИМ оракулом (проверено: `grep -c quarantine` по
   `red_tail_integrity_operator.rs` = 0). Непокрытый побочный эффект на данных ломается
   незаметно — наш повторяющийся класс.

**Контракт rev3:** карантин — РУЧНОЕ действие оператора по runbook. `open_with` файлы данных
не перемещает, не переименовывает, не удаляет. Единственное исключение — сама декларация
(`journal.force-next-seq.json` → `.applied.json`, одноразовость). Пиннится оракулом **`op_4`**.

### Запретный список правки (BINDING — корень всех трёх кругов M-49)

Три REJECT'а подряд имеют одну причину: спека перечисляла, ЧТО СДЕЛАТЬ, и не перечисляла,
чего делать НЕЛЬЗЯ. Пробел в спеке реализация заполняет по-своему — и имеет на это право.
Для оставшейся работы по M-49 запрещено без отдельного согласования с architect'ом:

| Запрещено | Почему |
|---|---|
| Ослаблять строгие пути чтения (`segments`, `stream`, `list_segments`, `classify_segment`, `dedup_indexed_paths`) | rev1 завернули за это; пиннится `ti_6` |
| Любые побочные эффекты `open_with` на файлах данных (перемещение, переименование, удаление, создание подкаталогов) | rev2 завернули за это; пиннится `op_4` |
| Менять сигнатуры публичных и `pub(crate)` функций сверх необходимого | изменение потока данных между функциями — проектное решение, зона architect'а |
| Трогать `crates/contracts/**` | T1 в этом milestone не меняется |
| Расширять область правки за `crates/journal/src/**` + runbook | scope milestone'а |

Если по ходу реализации выясняется, что задачу иначе не решить — это `!!! SCOPE VIOLATION
REQUEST !!!` и СТОП, а не самостоятельное расширение.

## Objective

`resolve_next_seq_with` берёт `tail_last_seq_of(последний сегмент)` и при `Ok(None)` молча
падает на `meta_seq`. Но `tail_last_seq_of` возвращает `Ok(None)` в **восьми** случаях, из
которых легитимны только два (файла нет; файл нулевой длины). Остальные — порча: битый
v2-заголовок сжатого сегмента, обрыв zstd-потока, полностью невалидный хвост сырого
сегмента. Все они трактуются вызывающим как «сегментов нет, начинай с меты».

**При restore из холодного хранилища `journal.meta` ОТСУТСТВУЕТ** (ретеншен выгружает
сегменты, не мету — замерено в M-40) ⇒ `meta_seq = 0` ⇒ запись идёт с нуля поверх истории.

## Замеры architect'а (main @ ddc105c, ПОСЛЕ мержа M-40)

```text
история 0..341 в сжатых сегментах, journal.meta отсутствует (прод-режим restore)

усечённый .zst (обрыв выкачки)      → open_with = Ok, новые seq [0, 1]  ← перекрытие 342 событий
валидный zstd + битый v2-заголовок  → open_with = Ok, новый seq  [0]    ← то же
сырой сегмент с битым хвостом       → open_with = Ok, seq 342           ← КОРРЕКТНО (ресинк нашёл 341)
```

**Уточнение к формулировке TD-049.** Reviewer описал дефект как «recorder НЕ СТАРТУЕТ»
(асимметрия терпимости raw/zst, `?` пробрасывает Err). Замер это **опроверг**: recorder
стартует в обоих случаях порчи. Реальное последствие ХУЖЕ отказа — молчаливый seq-reuse.
Отказ старта был бы громким и исправимым; порча append-only журнала — необратима, и реплей
воспроизведёт её бит-в-бит.

## Контракт (architect)

**JR-I-8 (честность `next_seq`).** `open_with` не имеет права начать запись с `seq`,
меньшего либо равного максимальному `seq`, уже присутствующему в журнале. Если сегмент с
максимальным индексом СУЩЕСТВУЕТ и НЕ ПУСТ, но его последний `seq` установить НЕ УДАЛОСЬ —
`open_with` обязан вернуть `Err` с диагностикой (файл + причина + действие), а не стартовать
с `meta_seq`.

**Легитимных `Ok(None)` ровно три:**
1. файла нет;
2. файл нулевой длины;
3. **валидный заголовок, ноль событий** — recorder создал сегмент и упал до первой записи
   (найдено architect'ом при вычитке собственного оракула: без этого случая ужесточение
   сломало бы штатный перезапуск; закрыто `ti_4` п. «д»).

**Почему fail-closed, а не терпимость.** Терпимость = запись поверх существующего диапазона
`seq` = необратимая порча. Отказ = минуты простоя сбора. Выбор зафиксирован, а не оставлен
на усмотрение реализации.

**Что НЕ ужесточается.** Ресинхронизация сырого хвоста (побайтовый поиск последнего
ВАЛИДНОГО фрейма) сохраняется: она возвращает конкретный `seq`, позиция известна, reuse
невозможен. Ломать работающее поведение не нужно (`ti_4` п. «г»).

**Операторский выход обязателен.** Fail-closed без выхода = вечно остановленный сбор данных,
и единственный доступный оператору шаг — удалить каталог, то есть потерять историю (ровно
то, от чего защищаемся). Форма — **файловая декларация**
`journal.force-next-seq.json` (не новый Rust-API): повторяет знакомый оператору
`journal.legacy.json`, не расширяет публичный API крейта, остаётся следом для аудита.

```json
{ "next_seq": 512,
  "reason": "segment-00000001.jrnl.zst невосстановим: bit-rot, холодной копии нет",
  "declared_at_ms": 1785362203969 }
```

Правила декларации (все под оракулами): действует ТОЛЬКО при нечитаемом хвосте (иначе
забытый файл молча переопределял бы честный `next_seq`); `next_seq` строго больше
максимального ЧИТАЕМОГО `seq` (иначе escape-hatch сам станет каналом seq-reuse);
одноразовая (после применения → `journal.force-next-seq.applied.json` с сохранением
`reason`). Тот же принцип, что `pruned_without_checkpoint_coverage` в M-38b: обход разрешён,
но обязан быть НАЗВАН и не может быть тихим.

## Allowed paths

- **dev (engine-dev):** `crates/journal/src/segments.rs`, `crates/journal/src/lib.rs`,
  `crates/journal/src/bin/journal-retention.rs` (задачи 7–8), `docs/ops-journal-tail-unreadable.md` (runbook, задача 6).
- **architect-only (sacred):** `crates/journal/tests/**`, `scripts/verify_M-49.sh`, этот файл
  (кроме колонки Status).
- Вне зоны: другие крейты, `crates/contracts` (T1 не меняется — новых типов нет),
  `PROJECT-STATE.md`, `TECH-DEBT.md`.

## Задачи (RED-first; RED закоммичен и КРАСНЫЙ)

| # | Задача | Оракул | Status |
|---|---|---|---|
| 1 | `tail_last_seq_of`: различать «нет/пусто/нет событий» от «нечитаем». На нечитаемости → `Err` с диагностикой (имя файла + причина + действие). Покрыть ОБА формата: сжатый (ошибка zstd, нераспознанный v2-заголовок, битый фрейм) и сырой (усечённый заголовок, crc заголовка, непустой хвост без валидных фреймов) | `ti_1`, `ti_2`, `ti_3`, `ti_5` | ✅ DONE |
| 2 | Сохранить терпимость и легитимные пути: ресинк сырого хвоста; каталог пуст; файл 0 байт; **валидный заголовок + 0 событий** | `ti_4` | ✅ DONE |
| 3 | Чтение операторской декларации `journal.force-next-seq.json`; применение ТОЛЬКО при нечитаемом хвосте | `op_1`, `op_3` | ✅ DONE |
| 4 | Валидация декларации: `next_seq` > максимального читаемого `seq`, иначе `Err` с объяснением | `op_2` | ✅ DONE |
| 5 | Одноразовость + аудит: переименование в `journal.force-next-seq.applied.json` с сохранением `reason` | `op_1` | ✅ DONE |
| 6 | Runbook `docs/ops-journal-tail-unreadable.md`: что делает оператор, когда recorder не поднялся (проверить целостность `zstd -t` → перекачать сегмент из cold → крайний случай: карантин + декларация) | verify (наличие + ссылка из текста ошибки) | ✅ DONE |
| 7 | **(TD-051)** `print_plan` печатает ФАКТИЧЕСКИЙ `seg_ts` (величину, по которой принято решение о возрасте), а не `created_wall_ms` под подписью `ts_exch/created=` — в обеих корзинах | — (косметика отчёта; проверяется вручную reviewer'ом) | ✅ DONE |
| 8 | **(TD-050)** файлы `*.jrnl` с непаттерным именем попадают в отчёт оператора (сейчас выпадают совсем) | — (на проде недостижимо: declarations=[], непаттерных файлов 0) | ✅ DONE |

## Состояние RED (проверено прогоном, не рассуждением)

- `red_tail_integrity.rs`: `ti_1`, `ti_2`, `ti_3`, `ti_5` — **КРАСНЫЕ**; `ti_4` — зелёный
  (парный vantage, он и должен быть зелёным сейчас).
- `red_tail_integrity_operator.rs`: `op_1`, `op_2` — **КРАСНЫЕ**; `op_3` — зелёный.
  ⚠️ `op_3` сейчас зелёный ТРИВИАЛЬНО (декларация вообще не читается, поэтому инертна);
  силу он приобретает после задачи 3. Это отмечено сознательно — не считать его
  доказательством инертности до реализации.
- **Проходимость доказана ФАКТИЧЕСКИ:** architect собрал временный прототип фикса —
  5/5 `ti_*` зелёные, **весь крейт `journal` зелёный (24 блока, 0 FAILED)**, включая
  `red_recover`, `red_prod_migration`, `red_open_bounded`, `red_restore_from_cold`,
  `red_retention*`, `red_compaction`. Прототип ОТКАЧЕН
  (`git diff -- crates/journal/src/` пуст). Это защита от дефекта M-48, где оракул оказался
  непроходимым в принципе.
- Дефект фикстуры, пойманный этим же прогоном: первая редакция `ti_3` портила байты с
  фиксированного 256-го, из-за чего события между заголовком и точкой порчи оставались
  валидными и ресинк их находил — оракул проверял НЕ ТО. Исправлено: граница заголовка
  вычисляется из самого файла (`magic(8) + len(4) + payload + crc(4)`).

## Acceptance

`bash scripts/verify_M-49.sh; echo exit=$?` → `VERDICT: PASS`, exit 0. Гейт содержит:
- канарейку «в `tail_last_seq_of` есть путь отказа» + **setup-guard** (тело функции не
  извлеклось ⇒ FAIL, а не молчаливый PASS — на этом уже падал гейт M-40);
- канарейку «сообщение отказа подсказывает ДЕЙСТВИЕ» (runbook/перекачать/объявить);
- канарейку «операторская декларация обрабатывается»;
- прогон RED-набора M-49;
- **регресс всего крейта** `journal`: M-49 правит функцию, на которой стоят восстановление
  после сбоя, прод-миграция legacy, bounded open (TD-011) и весь restore-путь M-40;
- `clippy -p journal --all-targets -D warnings`.

**§8 eyes-on (reviewer, после merge):** прод не должен заметить изменения — recorder
перезапускается на здоровом каталоге штатно. Предъявить: контейнеры healthy, heartbeat
свежий, `next_seq` продолжает расти без разрыва.

## Известное ограничение (НЕ закрывается этим milestone'ом)

Оракулы покрывают случаи, когда последний `seq` **не установлен**. Отдельный, более тонкий
случай — когда усечённый сжатый поток читается «тихо короче» (декодер трактует обрыв как
конец): тогда `last_seq` НЕДООЦЕНЁН, но не `None`, и запись начнётся внутри уже занятого
диапазона. Надёжная защита требует проверки целостности `.zst` (размер/sha256 из манифеста
компакции) при открытии. Вынести в отдельный TD после M-49 — здесь фиксируется как
известное ограничение, чтобы не создавать ложного ощущения полноты.

## Cross-ref

TD-049 (переоценён), TD-050, TD-051 · M-40 (R2/R2b, merge `984f6f9`) · TD-011 (bounded
open) · M-38b (C-030, образец escape-hatch) · `declare_legacy` (образец файловой
декларации) · `.claude/rules/testing.md` (чек-лист деградированного входа, мутационный
контроль).
