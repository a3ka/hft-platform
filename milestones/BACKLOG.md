# BACKLOG — очередь milestone'ов (architect-owned, планово)

STATUS: **PROPOSED** (v1, 2026-07-14, architect). Проходит doc-гейт (`gates.md` §9):
critic → reviewer → **founder ★** — очередь и приоритеты это РЕШЕНИЕ, а не описание.

**Планирование, не запуск.** Ни один пункт не стартует без явного решения founder'а.
Порядок — по «цена ошибки ÷ стоимость работы», а не по интересности.

Текущий в работе: **ПИВОТ P-COCKPIT (2026-07-22)** — виз-бэкенд + AI-копилот для Bookmap-подобного
кокпита (Order Flow Intelligence Terminal) на Binance+HL; фронт — founder (`code2alpha`), мы даём
бэкенд+экспорт.

> ⚠ **ПОРЯДОК ОЧЕРЕДИ НИЖЕ ПЕРЕОПРЕДЕЛЁН ПИВОТОМ.** Авторитетный порядок + контекст+решения —
> **`docs/07-cockpit-backend-roadmap.md`** (§8 milestone-последовательность). Кратко:
> **виз-first** — Трек B (MVP-1): **M-22 Read Gateway → M-23 heatmap/COB/bubbles → M-20 VWAP →
> M-24 Volume Profile → M-25 liq/OI/funding-профили**; MVP-2: **TPP COIN (после корректности книги
> TD-016/resync) → M-26 Event Engine → M-27 AI-Context+Audit**; Трек A: **корректность книги (TD-016),
> Tardis-импорт истории 2019+**; сходятся → **M-19 фронт** (founder). **ОТЛОЖЕНО:** M-11 (risk/ks/oms),
> M-12 (runner/подпись), M-13 (HL depth), M-14 (DET-I-1), M-10 стек-фиксы сигналов. Разделы ниже —
> **торговый трек: ОТЛОЖЕН, НЕ удалён** — возобновляется ПОСЛЕ кокпита (данные+виз готовы → торговля
> строится на них). Приоритет понижен до после-кокпита; спеки/RED сохранены (напр. `feat/M-10-rebased`).

---

## Снимок готовности (2026-07-14)

| Слой | Готово | Не готово |
|---|---|---|
| 0 журнал | append-only, ротация, эпохи/provenance, стрим O(1), ретеншен, disk fail-closed | `state_hash`, снапшоты, полный `DET-I-1` (TD-007) |
| 1 venues/book | Binance spot full-book, Binance futures MD, HL (тонкий), book-примитивы | HL глубина ≤20 уровней → полосы невычислимы (TD-005); recon с биржей — нет |
| 2 signals | trait, SignalBank, registry-loader, OBI №1 | сигнал ровно один; промоушен-флоу нет |
| 3 alpha/portfolio | ансамбль → forecast → target (детерминированно) | мультисигнал-веса из реестра, netting |
| 4 strategy/oms | strategy (taker v1) | **oms — нет** |
| 5 risk/killswitch | — | **нет вообще** |
| 6 sim/runner | честный бэктест + StrategyBacktest | **runner (paper/live) — нет** |
| 7 research/ops | research-cli полный (grid/WF/ledger/DSR/gaps) | **отчётов R-* — ноль**; **ops: метрик/алертов/бэкапа/recon — нет** |
| границы B/C | реестр читается | **подпись Ed25519 не существует** |

Фаза по `DESIGN.md` §10: **P0 ✅ · P1 ✅ · P2 ≈80%** (движок research готов, отчёта нет) · P3+ 0%.

---

## Очередь

### M-09 — Data safety net (бэкап + recon + метрики) 🔴 ПЕРВЫЙ
**Почему первым.** Пять инцидентов подряд (TD-011/013/014/016 + C1) нашли **глазами**, ни один —
автоматикой; при этом 15.0 GB боевых данных лежат **в одном экземпляре на одном диске**.
Мы строим торговую систему на активе, который (а) не защищён от потери, (б) не проверяется на
правильность. Всё остальное можно потерять и переписать — данные нельзя.

- Cold-copy журнала offsite (Storage Box) + **restore-drill** («бэкап, который не восстанавливали,
  — не бэкап», `OPS-I-2/3`). Legacy-сегмент 15.0 GB — первым.
- **Recon с биржей** (`OPS-I-1`): REST-снапшот vs локальная книга, расхождение > ε → алерт +
  ресинк + `Sys`-событие. **Это единственная проверка, которая ловит C1-класс** (порча данных
  при зелёном healthcheck).
- `/metrics` + алерты P0/P1/P2 (`docs/fa/ops.md` §3/§7 + матрица §7.1).
- **БЛОКИРУЮЩАЯ подзадача: `CT-RFC-03`** (T1) — аудит-событие recon/ресинка в журнал требует
  нового варианта `SysEvent`; `EventKind` — T1, значит atomic contract-RFC обязателен
  (C-006 M5: «возможно нужен» — недопустимая формулировка для милестоуна, который будет писать
  RED-тесты). Без RFC подзадача recon не стартует.
- Гейты: **contract-RFC + critic** (T1-триггер, `gates.md` §1.1); **прод не инертен** → §8.
- Acceptance (исполняемые ворота, C-006 M4/M6/M7):
  1. **`ε_test`** (не калибруется): инъецированная порча книги — удалённый/искажённый уровень,
     расхождение best bid/ask — ОБЯЗАНА поднять алерт. `ε_prod` калибруется отдельно, но не
     выше `ε_max` (fail-closed потолок).
  2. **`OPS-I-9` rate-budget/backoff:** инъецированный поток REST-ошибок НЕ даёт hot-loop;
     418/429/`Retry-After` соблюдаются; бюджет запросов на venue не превышается (урок TD-013 —
     recon добавляет ровно тот трафик, который нас уже банил).
  3. **restore-drill:** холодная копия СКАЧИВАЕТСЯ и ЧИТАЕТСЯ (`journal::stream`, `seq`
     непрерывен) — на реальном сегменте, а не на фикстуре.
  4. **Двусторонний паритет алертов:** каждая строка матрицы §7.1 имеет правило; каждое
     правило ссылается на существующую метрику.
- **Хвост M-09 (НЕ блокирует закрытие task 2 / дефекта B): офлайн объёмная сверка** — `research-dev`,
  `research/data-quality/**`. После B2 (founder ★ 2026-07-18, `docs/fa/ops.md` §4.3.2) near-touch ОБЪЁМ
  снят с рантайм-recon (REST-неверифицируем: систематический WS(T1)-vs-REST(T2) bias, три §8-провала).
  Объёмную сверку вести ОФЛАЙН над записанной книгой из журнала (полная книга/объёмы/глубина пишутся БЕЗ
  изменений): реконструкция книги + объёмная дельта как ТРЕНД/диагностика рядом с gap-статистикой (M-08).
  **Прото-дизайн — `ops.md` §4.3** (дискриминатор churn↔порча по персистентности знака), НО фикстуры
  ОБЯЗАНЫ быть ПРОД-ПОДОБНЫМИ (ненулевой остаточный bias), а не синтетический zero-mean churn — ровно то,
  что дало green-unit/live-flood в рантайме (`testing.md` урок B2). Возможный platform-reference для
  глубоких 6–60% полос (§5.1 магнитудная загадка) — сюда же. Отдельная RED-спека research-dev, когда
  дойдёт очередь; НЕ P2.5-блокер.

### M-10 — Отчёт R-001 (валидация OBI) 🟠
**Почему рано, хотя «просто прогон».** У нас есть grid, walk-forward, trials-ledger, deflated
Sharpe, стресс-режимы — и **ни одного отчёта**. Пока нет ни одного R-*, весь research-стек
недоказан, а мы уже строим на нём торговлю. Даже «сигнал мёртв» — ценный результат за дёшево.

- Прогон OBI (Трек A/B) на strategy-пайплайне; TD-015 (эпохи ledger'а) соблюсти.
- Обязательно: gap-артефакт эпохи (E8) + `data_span_days` + SE(Sharpe) в отчёте — на 3–7 днях
  данных вердикт «PASS» физически недостоверен (SE годового Sharpe ≈ ±11), поэтому **kill-screen,
  без касания test-окна и без подписи**.
- Гейты: risk-critic (анти-оверфит §6) → founder ★ (принять/убить по пре-рег. критериям).

### M-11 — Risk + Killswitch + OMS 🔴 (дорогой, но неизбежный)
Единственный путь к деньгам. `RiskApproved<Order>` встаёт **между** `strategy` и `oms`;
`portfolio::size` — это pre-trade sanity, а **не** риск-гейт.
- RISK-BLOCK (`gates.md` §5): critic + **risk-critic** обязательны; RED-suite `RK-I-1..10` +
  `INTG-I-*` обязан падать на заглушках.
- Killswitch — **отдельный процесс** со своими кредами (работает при мёртвом движке).
- **Acceptance (исправлено, C-006 M8):** `RK-I-1..10` + `INTG-I-*` GREEN (падали на заглушках);
  disconnect-drill (после разрыва наших заявок на бирже нет); kill-switch срабатывает при
  мёртвом движке. **48 ч живого testnet — ПЕРЕЕХАЛО в M-12**, потому что для торговли нужен
  `runner` (композиция режимов + arming), а он живёт в M-12. Прежняя формулировка требовала
  торговать раньше, чем появится то, чем торгуют, — и толкала к временному harness'у в обход
  `runner`-инвариантов, то есть к разрыву «backtest == paper == live».

### M-12 — Runner (paper/testnet) + границы B/C (подпись Ed25519) 🟠
- `runner`: композиция режимов (backtest/paper/live), arming; исполняется **только**
  `status==live && weight>0` (граница B).
- **48 ч HL testnet** (переехало из M-11): первая живая торговля идёт ЧЕРЕЗ `runner`, а не через
  временный harness — иначе мы валидируем не тот код, который пойдёт в live.
- **Подпись решений** (граница C): `Ctl(ParamChange{signed})` + `decisions/D-NNN.md`. Без неё
  любое изменение весов/лимитов — «на доверии», то есть ровно тот путь, который заканчивается
  правкой параметра на живом процессе.
- Paper-карантин обязателен (`02-quant-desk.md` §4.5): ни один сигнал не идёт train→live.

### M-13 — Hyperliquid depth 🔴 ПРЕДУСЛОВИЕ, а не «потом» (исправлено, C-006 M9)
**Валидируем сигнал на Binance, а торговать собираемся на HL**, где книга ≤20 уровней и полосы
OBI **невычислимы** (TD-005). Это не уборка после P3 — это **предусловие любого testnet/live с
OBI-полосами**: иначе на HL нечего считать.

**Развилка (решение founder'а ★, обе ветки честны):**
- **(а) закрыть глубину HL** (другой эндпоинт / `nSigFigs` / иной источник) — тогда OBI-полосы
  остаются первым сигналом, и M-13 обязан стоять **ПЕРЕД** живой торговлей (M-12);
- **(б) сменить первый сигнал/площадку** — торговать на Binance (там книга есть) ЛИБО взять на
  HL сигнал, не требующий глубины (top-N imbalance, funding/basis, liquidation-flow). Тогда
  M-13 можно отложить, но это надо ЗАПИСАТЬ как решение, а не «получиться само».

Пока развилка не выбрана, M-11/M-12 не могут честно назвать свой первый live-сигнал.

### M-14 — DET-I-1 полностью (`state_hash` + снапшоты) 🟡
«`replay(journal) == реальность`» сейчас держится на честном слове: бит-идентичность не
проверяется (TD-007). Это фундамент форензики («каждый цент объясняется реплеем») и обязателен
до live-micro (P4), но не блокирует research.

### M-15 — Импорт купленной истории 🟡
Форма готова (CT-RFC-02: `Vendor`-эпохи, манифест, fail-closed классификация). Нужен парсер
вендора + **per-source артефакты латентности/издержек** (у вендора другая глубина и другие часы —
одна `sim`-таблица на две эпохи = ложь). Триггер: founder называет поставщика.
Обязательное условие: `schemars` → JSON Schema 2020-12 (D-1) — первый момент, когда схему читает
не наш код.

### Order-flow трек (данные Фабио + фронт) — данные-first, founder ★ приоритет 🟠
Разбивка build-vs-integrate (анализ architect 2026-07-20): внешние MCP/сервисы — просмотрщики с
лицензией «не хранить»; владеть order-flow можно только СВОИМ захватом. Историч. данные для бэктеста —
бесплатны/дёшевы (Binance `data.binance.vision` free; HL CC0-зеркала). Сторону агрессора мы УЖЕ пишем →
trade-flow сигналы вычислимы сейчас; book-дельты (absorption/DOM) выбрасываем → нужен захват.

- **M-16 — историч. импорт** (research-only): Binance free + HL CC0 → журнал-формат. Шире окно M-10 +
  глубина HL для OBI Трек B. Provenance-изоляция, без T1. → milestone готов, RED следующим.
- **M-17 — order-flow Phase A (бэкенд):** trade-flow сигналы (footprint/cumulative delta, чистые
  редьюсеры) + **экспорт под `code2alpha`** (UDF 1s-бары + lightweight-charts серии). Виз — НЕ здесь.
  Без T1. → milestone готов, RED следующим.
- **M-18 — order-flow Phase B (захват, BUILD):** персист сырых @depth book-дельт (`CT-RFC-04 L2Delta`) →
  absorption/DOM. **T1 + sacred live-path + risk-critic.** Time-sensitive («начать собирать» — дельты
  теряются ежедневно). → не написан.
- **M-19 — frontend cockpit (ПОЗЖЕ, после бэка):** дорабатываем `a3ka/code2alpha` (Next+lightweight-charts
  v5, UDF DataFeed) в research+monitoring cockpit: свечи+order-flow(footprint/delta/DOM/heatmap)+сигналы+
  бэктест-отчёты+**детерминированный replay журнала**+мониторинг /metrics. Тир 1/2 (native/custom-series)
  → после M-17; Тир 3 (DOM/heatmap bespoke canvas) → после M-18. Фронт-работа founder'а, не Rust-цепочка.
  Полный список отображаемого — `milestones/M-19-frontend-cockpit.md`.

**Порядок трека:** M-17 (бэкенд сигналы+экспорт) ∥ M-18-CT-RFC (начать захват дельт) ∥ M-16 (импорт) →
затем M-19 (фронт на готовых данных). OBI (M-10) — ортогонален, простейший depth-сигнал, обкатка стека.

---

## Порядок исполнения (сквозной roadmap, 2026-07-20, founder ★)

Логическая последовательность по зависимостям + queue-rule (данные → доказательство сигнала →
барьер к деньгам → исполнение). Блоки 1–3 — параллелятся; блок 4 — строго после барьера.

**Блок 1 — Order-flow данные + сигналы (СЕЙЧАС, параллельно):**
1. **M-18 CT-RFC-04 + захват дельт** — START (time-sensitive: сырые book-дельты теряются ежедневно).
   T1 + sacred live-path → critic + risk-critic, аккуратно. Разблокирует absorption/DOM.
2. **M-17 order-flow бэкенд** — trade-flow сигналы (footprint/cumulative delta) + депт-ряды BID/ASK
   по полосам/таймфреймам + экспорт под `code2alpha` (UDF+lightweight-charts). Из УЖЕ собираемых данных.
3. **M-16 историч. импорт** — Binance free + HL CC0 → журнал (шире окно + глубина HL).

**Блок 2 — Доказать стек + сигналы (индикаторы/сигналы после инфры):**
4. **M-10 OBI kill-screen** — первый сквозной прогон research-стека (обкатка; kill-screen, не промоушен).
5. **Прогон order-flow сигналов** (footprint/delta/OBI) через kill-screen на накопленных+импортированных
   данных → R-002+ отчёты. risk-critic анти-оверфит §6 → founder ★.
6. **M-20 VWAP** (индикатор + кандидат-сигнал) — производный из уже собираемого `Trade`, БЕЗ T1/захвата.
   Дисплей-VWAP (линия в cockpit'е, экспорт-контракт M-17) — дёшево; VWAP-deviation сигнал — через
   анти-оверфит §6 + kill-screen. Устанавливает паттерн «производный индикатор» (TWAP/MA-семейство далее).
   `milestones/M-20-vwap.md` (PROPOSED/QUEUED — стартует по founder ★ в фазе индикаторов).

**Блок 3 — Фронт (визуализация):**
6. **M-19 cockpit** — дораб. `code2alpha`. Тир1/2 (свечи+delta+депт-линии+сигналы+бэктест) после M-17;
   Тир3 (DOM-ladder+Bookmap-heatmap) после M-18; replay журнала + мониторинг /metrics — параллельно.

**Блок 4 — Путь к деньгам (строго после барьера, queue-rule):**
7. **M-11 Risk + Killswitch + OMS** — fail-closed барьер (`RiskApproved`), самый дорогой, неизбежный.
8. **M-13 HL depth** (если HL — первая площадка исполнения; частично снято HL CC0).
9. **M-12 Runner** (paper/testnet) + границы B/C (Ed25519-подпись).
10. **M-14 DET-I-1 полный** (`state_hash`+снапшоты) — до P4 live-micro.

**Кросс-cutting / founder ★ (когда удобно, не блокируют блоки 1–3):**
- 🔴 **ДОСТАВКА РЕТЕНШЕНА (TD-020 / M-08 task 14) — САМЫЙ СРОЧНЫЙ ДОЛГ.** Диск ~25 дней до disk-guard
  (L2Delta BTC-only +0.8 GB/сут ⇒ ~3.8 GB/сут); ретеншен написан (M-08), но в прод НЕ доставлен
  (бинарь `journal-retention` не в образе, холодного хранилища нет, cron нет). Нужно: спека architect
  (доставка бинаря + монтирование Storage Box + cron + алерт на exit≠0) → engine-dev. **Приоритет выше
  всего journal-hardening.**
- **Storage Box** (провижининг) → разблокирует ретеншен-apply выше + M-09 task 3 (restore-drill).
  Durability — принятый single-copy риск.
- **M-21 journal-hardening** (`milestones/M-21-journal-hardening.md`, PROPOSED/QUEUED) — машинные
  барьеры целостности (TD-032 provenance-forensics / TD-033 schema-bump энфорсмент / TD-029 startup
  schema-guard / TD-030 reader first_seq-guard). **СТРОГО после ретеншена** (не срочно; изоляция эпох
  уже работает через schema-гейт, §8-подтверждено).
- **Live alerting** (Prometheus/Alertmanager→Telegram) — метрики+правила готовы (§O).
- **M-15 vendor import** — когда founder назовёт поставщика купленной истории.

**Текущий фокус:** блок 1 — M-17 RED (в процессе) + M-18 CT-RFC (запустить). M-10 готов к прогону.

---

## Что НЕ делаем (осознанно)

- **MM-котирование** — до `oms` + `risk` (двусторонние квоты без rate-budget и kill-switch = слив).
- **Мультисигнальный ансамбль** — пока не доказан ОДИН сигнал (R-001).
- **Оптимизация производительности** — мы в mid-freq, узкое место сейчас не CPU, а доказуемость.
- **Новые venue** — пока не закрыт разрыв «валидируем на Binance / торгуем на HL».

## Граф зависимостей (что чего требует — по факту, не по желанию)

```
M-09 (data safety)  ──требует──▶ CT-RFC-03 (T1: Sys-варианты для recon-аудита)
M-11 (risk/ks/oms)  ──требует──▶ ничего сверх текущего (RED на sim/harness, без живой торговли)
M-12 (runner)       ──требует──▶ M-11 (риск-барьер) + M-13-развилку (чем торгуем на HL)
   └─ 48ч testnet   ──требует──▶ runner (M-12), НЕ раньше
M-13 (HL depth)     ──БЛОКИРУЕТ─▶ M-12, ЕСЛИ первый сигнал = OBI-полосы
M-10 (R-001)        ──независим (compute на записанных данных)
M-14 (DET-I-1)      ──обязателен до P4 (live-micro), не блокирует research
M-15 (вендор)       ──требует──▶ schemars → JSON Schema 2020-12 (D-1)
```

## Правило очереди

Приоритет = **цена ошибки**, не близость к прибыли. Данные (M-09) → доказательство сигнала (M-10)
→ барьер к деньгам (M-11) → исполнение (M-12). Любая перестановка означает: мы торгуем на
активе, который не защищён, сигналом, который не проверен, через путь, у которого нет тормоза.

---

# Реестр находок — сверка состояния 2026-08-11 (architect)

**Это НЕ очередь и НЕ приоритет.** Порядок пунктов — порядок обнаружения, не важности.
Ни один пункт не становится milestone'ом без явного решения founder'а (правило шапки этого
файла). Раздел заведён по прямому указанию founder'а «сначала предъявить долг по находкам».

Каждое утверждение ниже снято КОМАНДОЙ в общем чекауте на `origin/main` = `775ccb9`
и перепроверено architect'ом лично после субагентского замера.

## Н-1 — `TD-126` называет неверную корневую причину; замер занижен

| заявлено в `TD-126` | факт | команда |
|---|---|---|
| 44 скрипта `verify_*.sh` | **44** ✅ | `ls scripts/verify_*.sh \| wc -l` |
| у нуля страж свободного места | **0** ✅ | `grep -lE '(^\|[^a-z])df( \|$)\|statvfs\|ENOSPC\|no space left' scripts/verify_*.sh \| wc -l` |
| 10 выбрасывают вывод `cargo` | **16** ❌ занижено | 11 инлайн (`grep -lE 'cargo.*>\s*/dev/null'`) + 5 через обёртку `run() { … "$@" >/dev/null 2>&1 … }` (`M-05/06/09/17/18`, 4–12 вызовов `cargo` каждый) |

**Главное — причина инцидента названа неверно, и из-за этого предложенный фикс не чинит
ничего.** `TD-126` и `R-045` F-10 говорят «вывод уходит в `/dev/null`». Для
`verify_M-60a.sh` — чей прогон и стал предъявлением долга — это не так:

```
scripts/verify_M-60a.sh:362
cargo test --all >/tmp/m60a-test.log 2>&1 && pass … || { fail …; grep -E '^test .* FAILED' /tmp/m60a-test.log | head -5 …; }
```

Лог **сохранялся**. Молчание дал **узкий фильтр печати**: при падении `cargo` на загрузке
манифеста (исчезнувший worktree) строк `^test … FAILED` не существует, и `grep` напечатал
пусто. Класс дефекта — «сохранено и не прочитано»; так устроены **13** скриптов. Значит
фикс «сохранять вывод в файл» бесполезен: файл уже есть.

**Два ограничения конструкции, которые обязаны войти в спеку, если она будет заведена:**

1. `/`, `/tmp`, `/home` — **один том** (`df --output=source / /tmp /home` → `/dev/md2` ×3).
   При настоящем ENOSPC лог шага в `/tmp` не запишется ⇒ страж обязан стоять **до** `cargo`
   и печатать до попытки записи, а не после.
2. Число «10» из формулировки долга соответствует **другому множеству** — 10 скриптов с
   незащищённым `cd` без `set -e` (`contracts`, `M-05/06/09/17/18/32/33/34/35`). При
   исчезнувшем дереве `cd` там молча провалится и `cargo` пойдёт в чужом каталоге: это
   третий режим отказа из `TD-119`, и он не ловится стражем места.

**Зона:** architect (`scripts/verify_*.sh` sacred). Формулировку самого `TD-126` в
`TECH-DEBT.md` правит reviewer — файл reviewer-owned, architect только предъявляет находку.

## Н-2 — предусловия для механизма нет: общей преамбулы у verify-скриптов не существует

```
scripts/lib/                     → не существует
source|. в scripts/verify_*.sh   → 0 из 44
set -uo pipefail 29 · set -e 7 · set -euo pipefail 5 ⇒ БЕЗ set вовсе — 3 файла
```

Ни одна из 5 RED-проб в `scripts/tests/` не свипает КЛАСС файлов: каждая пиннит ровно один
барьер. Любой страж по `Н-1` без общей преамбулы тиражируется копией в 44 файла и
разъезжается на первой же новой спеке. **Это не отдельная задача, а предусловие `Н-1`.**

## Н-3 — ни один `verify_M-NN.sh` не исполняется в CI

```
$ grep -nE 'verify_[A-Za-z0-9_-]*\.sh' .github/workflows/ci.yml
45: verify_delivery_M-08.sh · 117: verify_contracts.sh · 144: verify_ct_rfc_atomic.sh
```

Acceptance-гейт milestone'а исполняется РОВНО ОДИН РАЗ — руками того, кто его прогнал, и
больше никогда. «Паритет с CI» (`gates.md` §3) держит одно направление (verify ⊇ CI);
обратного направления не держит ничто. Следствие: регрессия, которую поймал бы
`verify_M-NN.sh` смерженного milestone'а, в `main` не ловится вовсе.
**Решение founder'а требуется потому, что цена — минуты CI** (ср. `TD-114`: квота уже
кончалась однажды, и это остановило деплой на сутки) и предмет пересекается с **M-63**.

## Н-4 — GC worktree: признака ВЛАДЕНИЯ нет, потребление растёт быстрее чистки

Замер за один час этой сессии: `git worktree list` → **15** деревьев; `du -sh /tmp` → **73 GB**;
свободно на `/`: **108 GB** (09:44) → **98 GB** (09:49). Два тестера, запущенных на ОДИН
предмет (M-61), за 15 минут дали +9 GB: `/tmp/hft-tester-m61b` 662 MB → 8.7 GB.

`TD-119` (дополнение reviewer'а) уже назвал вывод: признак «нет процесса» права на снос не
даёт ни при каком интервале — агент между командами ЖИВ. Нужен признак владения (lock-файл
с PID, снимаемый при освобождении) либо снос только по явной команде владельца.
Правило GC (`TD-119`) числится в списке ожидающих токена founder'а.

## Н-5 — работа вне `origin`: инвентаризация замером, а не на слух

| объект | вердикт | основание |
|---|---|---|
| `fix/M-61-b4q-quotedname-alt` `57825a3` | **спасено** → `origin/salvage/M-61-b4q-alt` | решение founder'а 2026-08-11; `git ls-remote` подтверждает ref |
| `my-alt/M-61` `04b84ce` | **спасено** → `origin/salvage/M-61-alt-impl` | 3 блоба альт-реализации (`check_artifact_ids.sh`, `next_artifact_id.sh`, `ci.yml`) не встречались ни в одном origin-ref; то же решение, идентичный случай; отменяется `git push origin --delete` |
| `/tmp/hft-fable-rules` — «8 незакоммиченных правок процессного слоя» | **риска НЕТ** | блобы всех 8 файлов побайтно совпадают с `8879bf1`/`a52a6b7`/`9a9f533`, и все три коммита содержатся в `origin/main`. Worktree протух на 8 дней, уникального содержимого не несёт |
| detached `1342075` (merge M-57) | **риска НЕТ** | `git diff --stat 1342075 origin/main -- crates/` пусто |
| detached `573d41b` (close-out M-58) | **риска НЕТ** | блоб `R-035-M-58-rev2.md` идентичен блобу в `main` |
| detached `6fef108` | **риска НЕТ** | однострочная тест-фикстура `.claude/rules/правила.md` для оси «квотируемое имя» |

Метод важнее результата: **пять из шести «потерянных работ» оказались протухшими копиями**.
Признак «нет в `origin`» сам по себе тревогой не является — сверяется СОДЕРЖИМОЕ (блоб),
а не достижимость ветки.

## Н-7 — вынесено из M-64 rev3 по решению founder'а «урезать до вынужденного» (11.08)

Адверсарная проверка rev2 (пять независимых агентов) нашла больше, чем требовал вердикт
`C-074`. Founder решил не тащить это в M-64. Находки живут здесь и ждут отдельного решения;
в самой спеке они перечислены §6bis, чтобы следующий круг критика не принял их отсутствие за
незнание. **Все пять привязаны к миграции `К1`/`К6b`** (решение founder'а: сперва миграция).

| # | находка | замер, которым предъявлена |
|---|---|---|
| Н-7.1 | **Корень схемы обязан быть `ServeMsg`, а не `SeriesBundle`** — клиент парсит конверт ПЕРВЫМ | `crates/gateway-serve/src/lib.rs`, модуль `wire`: `enum ServeMsg{Snapshot,Frame,Error}`. Предмет `К6b` (P0) |
| Н-7.2 | **Ось «зона покрытия объявления»** — гейт может быть верен на всём, что видит, и слеп к непокрытому | вторая шкала версий `research-cli::EXPORT_SCHEMA_VERSION = 1` (`crates/research-cli/src/export_io.rs:37`) не покрыта ничем |
| Н-7.3 | **Носитель объявленной семантики** (`unit`/`anchor`/`provenance` → генерируемый реестр, fail-closed на поле без аннотации) | без него шаг `S` M-64 неисполним; `description` не годится — `diff_contract_schema.py:91` относит его к doc-only |
| Н-7.4 | Значения оси 2 про КОНТЕЙНЕР: перестановка в позиционном кортеже, `null` вместо отсутствия ключа | `vwap: Vec<(i64,i64)>` — позиционный кортеж на проводе |
| Н-7.5 | Значение оси «носитель»: **код сериализации** (`serde(rename)` / `skip_serializing_if`) расходится и с типом, и со схемой | на провод уходит результат `serde`, а не тип |

**Побочный вывод метода, дороже самих находок.** Три утверждения спеки M-64, прошедшие круг
критика, оказались ФАКТИЧЕСКИ ЛОЖНЫМИ на `origin/main` («форма менялась восемь раз»;
«снапшот не входит в сверку»; «правила ломающее ⇒ bump нет»). Критик их не поймал — он
проверял грамматику инварианта, а не соответствие замера коду. Кандидат в правило: **утверждение
спеки О КОДЕ обязано нести команду-пруф прямо в таблице** (как это сделано в M-64 rev3 §1.1) —
тогда ложь ловится одним прогоном, а не вторым кругом. Решение — за founder'ом.

## Н-6 — индекс пайплайна `SESSION-HANDOFF` §0 отставал (устранено architect'ом)

Строка M-61 гласила «ждёт dev (задачи 1-3)» при двух пройденных кругах reviewer'а
(`R-046` rev1/rev2); строки M-64 не было вовсе, хотя rev2 запушен. Приведено к факту тем
же заходом — это изложение, не форма (`gates.md` §9), решения founder'а не требует.
