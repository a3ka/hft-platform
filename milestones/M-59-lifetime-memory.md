# M-59 — граница ПАМЯТИ per-life анализатора: агрегаты вместо накопления жизней

STATUS: **IN_PROGRESS, круг 3** (заведён 2026-08-04, architect; шапка приведена к факту
2026-08-14 по `R-076` N-3 — стояло `PROPOSED` при трёх задачах ✅ DONE и созданной ветке).
Порождён `R-033` F-5 (PR-гейт M-58). Ветка `feat/M-59-lifetime-memory` создана и живёт.
Гейты: `R-038` REJECT (круг 1, семь условий) → `R-076` REJECT (круг 2: блокер `F-2` ЗАКРЫТ
замером reviewer'а, блокер `B-1` не доведён до кода + четыре MINOR).

## Objective

M-58 сменил единицу учёта с цены на жизнь и вместе с этим сменил класс памяти.
`crates/research-cli/src/depth_lifetime.rs:143` вводит `finished: Vec<(Option<i64>, Fate)>`,
куда пишется запись на КАЖДУЮ завершённую жизнь (`:166`, `:188`, `:219`, `:228`), и который не
сбрасывается до конца прогона.

- **до M-58:** память O(distinct-цен) — `states` держал одну запись на цену;
- **после M-58:** память O(ЖИЗНЕЙ), а жизней на порядок больше. Замер того же сегмента:
  **650 372 жизни за 3.4 часа ОДНОГО символа** (`lives_born` по всем полосам).

Агрегаты по полосам можно инкрементировать в момент завершения жизни, не накапливая вектор:
на выходе нужны четыре счётчика на полосу, а не история эпизодов.

**Почему это не поймал существующий оракул.** `DV-I-7` (`tests/red_depth_scale.rs`) меряет
**время** (бюджет 15 с), а его фикстура рождает уровни, которые НИКОГДА не отменяются ⇒
`finished` в ней остаётся пуст. Классический TD-021/TD-011: ресурс меряется прокси, а
деградировавший ресурс в фикстуре не возникает вовсе.

## Contract impact (T1) — НЕТ

Внутреннее представление редьюсера. Публичный контракт (`BandReport.lives_*`,
`cancel_fraction`, баланс `lives_born == cancelled + frozen + censored`) НЕ меняется, и это
проверяется регрессом DV-I-1..14.

## Allowed / Forbidden paths

| агент | разрешено |
|---|---|
| architect | `crates/research-cli/tests/red_lifetime_memory_bounded.rs`, `scripts/verify_M-59.sh`, этот файл |
| research-dev | `crates/research-cli/src/depth_lifetime.rs` + `crates/research-cli/examples/depth_lifetime.rs` (call-site прибора; расширено по `R-038` F-4 — правка TD-108 была сделана вне объявленной зоны) (только внутреннее представление) |

**Запрещено:** менять публичные поля/сигнатуры `BandReport`/`LifetimeReport`; менять семантику
жизни (рождение/отмена/цензура/заморозка) — это контракт M-58; трогать `crates/research-cli/tests/**`
кроме нового файла (sacred); ослаблять DV-I-1..14 ради прохождения нового оракула.

## §Tasks

| # | Status | задача | агент | verify |
|---|---|---|---|---|
| 1 | ✅ DONE | Заменить накопление `finished` инкрементом агрегатов в момент завершения жизни | research-dev | DV-I-15 GREEN; DV-I-1..14 остаются GREEN |
| 2 | ✅ DONE | Прогон на segment 78 — числа обязаны СОВПАСТЬ с §M-58 до разряда (рефакторинг не меняет результат) | research-dev | сравнение таблиц 14 строк, побитово равны |
| 3 | ✅ DONE | **Регрессия собственного фикса (`DV-I-16`).** Задача 1 заменила накопление инкрементом — и вместе с этим ввела потерю ЗАВЕРШЁННОЙ жизни в сценарии «рождение → отмена → рождение ТОЙ ЖЕ цены ДО mid-атрибуции»: `depth_lifetime.rs:249-252` возвращает старое `LevelState` в `states` и тут же безусловно затирает новорождённым, не дождавшись атрибуции. Учесть завершённую жизнь в момент АТРИБУЦИИ, а не только в момент завершения | research-dev | `DV-I-16` GREEN (`crates/research-cli/tests/red_life_before_attribution.rs`); `DV-I-1..15` остаются GREEN; числа задачи 2 не меняются, кроме `cancel_fraction` — его сдвиг ОБЯЗАН быть назван и обоснован |

## Задача 3 — почему фикс входит В ЭТОТ milestone, и что делать ЗАПРЕЩЕНО

**Решение architect'а 2026-08-14, по двум независимым прогонам tester'а** (оба на чистом
worktree от `origin/feat/M-59-lifetime-memory` @ `e0e56a3`, оба воспроизвели одно):
`cargo test --all` → 573 passed / **1 failed** — `dv_i_16_life_completed_before_attribution_is_counted`.

**Почему это задача M-59, а не следующего milestone'а.** Дефект **внесён задачей 1 этого же
milestone'а** (`687f92a`, TD-107): замена накопления инкрементом и есть та правка, которая
перестала учитывать завершённую жизнь до атрибуции. Регрессия собственного изменения
чинится там, где внесена, — иначе milestone закрывается зелёным поверх дефекта, который сам
и создал.

**Почему `DV-I-1..14` молчат и почему это опасно.** Баланс `lives_born == cancelled + frozen
+ censored` остаётся ВЕРНЫМ: падают оба слагаемых синхронно. Смещается ровно
`cancel_fraction` — метрика под founder-решением `П-011`, — и смещается ВНИЗ. То есть
существующие оракулы не краснеют по построению, а цифра под подписью тихо занижается.

**Две отвергнутые развязки — названы, чтобы их не «дочинили» по инициативе:**

| развязка | почему отвергнута |
|---|---|
| вернуть `DV-I-16` в `expected-to-fail` | RED, которому разрешено остаться красным, — это НОРМАЛИЗОВАННЫЙ дефект. Прямое нарушение анти-плацебо (`gates.md` §2): гейт перестаёт отличать «инвариант держится» от «мы согласились, что не держится» |
| удалить коммит `6c1dfe4` с ветки | выбрасывает оракул, который поймал реальную потерю данных подписанной метрики. Цена оракула уже заплачена; удаление возвращает нас в состояние, где дефект есть и невидим |

### Запрещено / Почему (`testing.md`: спека правки существующего кода обязана нести список)

| запрещено | почему |
|---|---|
| менять `crates/research-cli/tests/**` | RED-оракулы sacred (`scope-guard.md`); «тест неправильный» ⇒ `!!! SCOPE VIOLATION REQUEST !!!`, не правка |
| менять публичный контракт `BandReport.lives_*` | его стережёт T6; смена формы — отдельный предмет, не побочный эффект фикса |
| трогать `crates/contracts/**` | M-59 не T1-milestone; правка ⇒ авто-REJECT (`gates.md` §4 Block-C) |
| «починить» баланс, подогнав `censored` | баланс и так верен — он не симптом; подгонка спрячет дефект, а не устранит |
| менять числа задачи 2 где-либо, кроме `cancel_fraction` | 14 полос уже сверены побитово с артефактом M-58; любое иное расхождение = смена семантики, а не фикс памяти |

**Ожидаемый сдвиг `cancel_fraction` обязан быть НАЗВАН в Done Block** — с обеих сторон
(NEAR/FAR) и с объяснением направления. Молчаливое изменение подписанной метрики
недопустимо: `П-011` стоит на её значении.

## Круг 3 — решения architect'а по `R-076` (dev к ним не допущен)

### Решение 1 — `B-1`: формулировка квалификатора задаётся ЗДЕСЬ, дословно

`R-076` намерил на пути БЕЗ mid **×4.00** (1 049 240 → 4 194 968 B), а абзац
`depth_lifetime.rs` утверждает безусловное «Память O(число полос), расход O(1) на завершённую
жизнь». Оба числа правдивы и относятся к РАЗНЫМ путям — поэтому reviewer прав: абзац лжёт не
величиной, а отсутствием квалификатора. Замер шага T3 на этом же дереве:
`DV-I-15: … рост памяти ×1.00 (бюджет <1.50)` — это путь ПОСЛЕ атрибуции.

Квалификатор задаю я, чтобы dev не импровизировал (`R-076` §D). Текст обязан сказать ровно это:

> Память O(число полос) и расход O(1) на завершённую жизнь — **после того, как mid известен**.
> До первого mid в окне завершённые жизни копятся в `pending_completed`, и расход растёт с их
> числом: замер `R-076` — ×4.00 на одностороннем потоке против ×1.00 на пути с mid
> (`DV-I-15`). Граница здесь НЕ достигнута сознательно (решение `e0e56a3`): милестоун
> обязывает её НАЗВАТЬ, а не устранить. Устранение — отдельный предмет; до него **расширение
> на топ-300 упирается именно в этот путь**.

Последняя фраза обязательна: без неё квалификатор прочтут как оговорку, а не как границу
применимости.

### Решение 2 — `N-1`: `flush_delayed_states` НЕ УДАЛЯЕТСЯ. Обоснование против очевидного

`R-076` доказал мутацией (`panic!` первой строкой тела цикла): весь набор research-cli 64/64
GREEN, ноль срабатываний. Напрашивается вывод «код мёртв, удалить». **Отклоняю, и вот почему.**

Замер доказывает отсутствие в ТЕСТОВОМ КОРПУСЕ, а не недостижимость в ПРОДЕ. Это разные
утверждения, и подмена одного другим — ровно тот класс, который мы весь день чиним: вывод о
свойстве из замера, который этого свойства не мерит. `flush_delayed_states` обслуживает случай
«mid не появился до конца окна» — односторонний поток; на неликвидном инструменте в коротком
окне он правдоподобен, а фикстуры на него в корпусе НЕТ. Удалить путь на основании «тесты его
не достигают» значит выбросить обработку прод-случая, который тесты не покрывают.

**Что делается вместо удаления:**
1. **Фикстура на окно БЕЗ mid** — она превращает вопрос из мнения в замер. Если и она даст
   ноль срабатываний, удаление станет обоснованным ЗАМЕРОМ, а не отсутствием улик.
2. **Пустой `if` чинится немедленно** — он хуже и мёртвого кода, и живого. Тело
   `if state.fate == Fate::Alive { }` ПУСТО, а комментарий рядом утверждает «считаем как
   Cancelled, чтобы баланс `born == c + f + ce` не разъехался». Страховка описана и не
   существует: попади поток в эту ветку, баланс разъедется молча. Развязка — одна из двух,
   выбор за dev'ом с обоснованием в Done Block: либо реализовать заявленное комментарием, либо
   сделать ветку явным отказом (`debug_assert!`/диагностика), если она действительно
   недостижима. **Оставить пустой `if` с ложным комментарием — не развязка.**
3. **Комментарии, описывающие несуществующий путь** (`:442-445`, `:507-510` по нумерации
   `R-076`), приводятся к факту тем же коммитом.

### Решение 3 — `N-4`: шаг `ЗАМЕР` печатает или краснеет

Исполнено architect'ом в этом же круге (`scripts/verify_M-59.sh` — sacred, зона моя). Прежняя
редакция грепала лог `run_oracle`, который зовёт `cargo test` без `--nocapture`; харнесс отдаёт
stdout только у УПАВШИХ тестов, поэтому в зелёном прогоне грепу нечего было найти и строка
печаталась ПУСТОЙ. Шаг выглядел исполненным, не исполнившись.

Молчащая строка хуже отсутствующей — она создаёт видимость измерения. Теперь отдельный прогон
с `--nocapture` и **fail-closed на отсутствие числа**. Предъявлено исполнением:
`DV-I-15: N=20000 жизней … рост памяти ×1.00 (бюджет <1.50)`.

### Состояние гейта на 2026-08-14 — что для исполнителя означает «зелёно»

Мутационный контроль шага T5 после фикса, все три состояния входа прогнаны исполнением:

| `M59_JOURNAL` | исход | что это значит |
|---|---|---|
| путь до **файла** `…/segment-00000078.jrnl` | `FAIL T5 … — не КАТАЛОГ` | отказ ВХОДА назван прямо; прежняя редакция здесь рапортовала «ЧИСЛА РАЗОШЛИСЬ» и обвиняла семантику фикса |
| не задан | `FAIL T5 пересъёмка НЕ выполнена` | fail-closed сохранён |
| **каталог** `/home/nous/fixtures/m59-segment78` | **`PASS T5 числа пересъёмки идентичны артефакту`** | числа НЕ поехали: фикс задачи 1 изменил расход, не результат |

Итог полного прогона в верной форме — `VERDICT: FAIL (1 нарушений)`, и это нарушение
**ровно одно: `T2c` → `dv_i_16`**, то есть предмет задачи 3. Всё остальное (T0, T1, T2,
T2b, T3, T4, T5, T6) зелено.

**Для исполнителя задачи 3 это и есть определение «готово»:** тот же прогон с тем же
каталогом обязан дать `VERDICT: PASS`. Ни одного другого шага чинить не требуется — если
покраснел ещё какой-то, значит фикс задел соседний инвариант, и это находка, а не шум.

## RED-оракул DV-I-15 — обязательные свойства

Файл `crates/research-cli/tests/red_lifetime_memory_bounded.rs`, **ровно один тест в файле**:
счётчик аллокаций глобален для процесса, а `cargo` гонит тесты одного бинаря параллельно
(урок TD-040) — отдельный тест-бинарь даёт изоляцию замера без `Mutex`. Образец обвязки —
`crates/journal/tests/red_restore_next_seq_bounded.rs`.

1. **Мерит ПАМЯТЬ, а не время.** Пик аллокаций во время `analyze(...)` через
   `#[global_allocator]`-счётчик (`CUR`/`PEAK` на `AtomicUsize`).
2. **Конфаундер держится КОНСТАНТНЫМ, варьируется только измеряемое** (`testing.md`,
   «целостность гейта», свойство 2): число distinct-ЦЕН фиксировано (например 64) в обоих
   прогонах; варьируется только число ЖИЗНЕЙ — `N` и `4N` циклов «рождение → отмена» на тех же
   ценах. Иначе рост `states` спутается с ростом `finished`.
3. **Ассерт — отсутствие пропорционального роста:** `peak(4N) < peak(N) × 1.5`. На сегодняшней
   реализации `finished` растёт вчетверо ⇒ оракул ПАДАЕТ (анти-плацебо выполняется по
   построению, проверить прогоном ДО фикса).
4. **Одновременно ассертится КОРРЕКТНОСТЬ** — `lives_born`/`lives_cancelled` равны ожидаемым по
   фикстуре. Без этого реализация «ничего не считаем, укладываемся в ноль байт» прошла бы
   бюджет, потеряв данные.
5. **Фикстура порождает ЗАВЕРШЁННЫЕ жизни** — то, чего нет в фикстуре DV-I-7 и из-за чего
   дефект не наблюдаем существующими оракулами.

## Решение по `R-038` F-1 — граница НАЗВАНА, а не объявлена достигнутой

**Замер reviewer'а (воспроизводим):** на пути, где `mid` ещё не определён, очередь
`unattributed` растёт по ЖИЗНЯМ — 787 KB → 3.15 MB (×4.00). Это тот же класс расхода, что
и до фикса; изменилась константа, не класс. При этом doc-комментарий `SideBook` утверждает
безусловное «Память O(число полос)».

**Решение: путь НЕ закрывается в M-59, ограничение НАЗЫВАЕТСЯ.** Основания:

1. **Заявленная цель достигнута там, где она имеет смысл.** После определения `mid` расход
   константен — замер `DV-I-15`: 4 696 B и при N, и при 4N жизней (×1.00). Это и есть
   рабочий режим анализатора.
2. **Ложь в комментарии — отдельный дефект, и он чинится сразу.** Безусловное «O(число
   полос)» неверно; формулировка обязана назвать условие. Это задача dev'а (зона `src/`).
3. **Длительность окна «до mid» на реальных данных НЕ ИЗМЕРЕНА.** Оценивать её рассуждением
   («обычно обе стороны приходят сразу») я не буду — сегодня этот проект уже трижды
   поплатился за уверенность без замера. Замер и есть условие закрытия долга.

**Что обязан различать оракул** (`R-038` F-1 прямо этого требует): `DV-I-15` пиннит границу
ПОСЛЕ определения `mid` — и обязан остаться зелёным. Отдельная запись фиксирует, что до
`mid` границы нет; если кто-то позже заявит безусловную границу, утверждение разойдётся с
записью, а не с молчанием.

**Долг (заводит reviewer при close-out, severity MAJOR):** «граница памяти per-life
анализатора не распространяется на путь до определения `mid`; длительность окна на реальных
данных не измерена». Прецедент — `gates.md` §4 «built-not-wired»: механизм есть, но его
область действия уже заявленной.

## Acceptance

`scripts/verify_M-59.sh`: паритет с CI (`fmt --check`, `clippy --all-targets -D warnings`,
`test --all`), DV-I-15 GREEN, регресс DV-I-1..14 GREEN, совпадение чисел прогона с §M-58,
`VERDICT: PASS/FAIL` + exit-код.

## Почему отдельный milestone, а не правка внутри M-58

Оракул обязан быть КРАСНЫМ до фикса, а `main` держится зелёным (`gates.md` §8: RED до
реализации не живёт в `main`). Класть падающий тест на ветку M-58, которая идёт в merge,
нельзя. M-58 закрывается с ИМЕНОВАННЫМ долгом (`R-033` F-5), M-59 его снимает.

**Приоритет — низкий.** `research-cli` — офлайн-инструмент, не несущий путь: прогон разовый,
на машине разработчика, 650 тыс. записей это десятки МБ. Долг именован, чтобы он не всплыл
внезапно при расширении на несколько символов или на многосуточное окно, где то же
O(жизней) станет гигабайтами.
