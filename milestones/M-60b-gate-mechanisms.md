# M-60b — механизмы гейтов: бюджет чтения, подключённость, привязка вердикта к предмету

**Статус:** PROPOSED · **Дата:** 2026-08-06 · **Автор:** architect
**Ветка:** `feat/M-60-mechanisms` · **Зонт:** `milestones/M-60-mechanisms.md`
**Предусловие:** **M-60a смержен** (замок работает — иначе правки правил в этом milestone'е
идут без следа разрешения)
**Гейты:** critic (правка гейтов + `ci.yml`) → dev → tester → reviewer

---

## 1. Objective

Три механизма, каждый закрывает названный и уже проявившийся класс отказа. Прозы почти не
добавляют — вся работа в скриптах и проводке.

| механизм | закрывает | доказательство, что класс живой |
|---|---|---|
| **G1 бюджет обязательного чтения** | startup-протокол растёт молча | замер аудита §1.4: **~1068 KB** на каждую роль при каждом старте; из них ledger'ы 765 KB |
| **G2 `verify_design_claims` в CI** | «документ врёт о коде» ловится только руками | корень `TD-106`. За сутки 05.08 класс проявился **трижды**: аудит про лаунчеры, спека M-57 про несуществующие `fn`, мои висячие ссылки |
| **G3 GATE-META + subject-lock** | вердикт не привязан к предмету; после проходного вердикта меняется скоуп | `TD-105`. `C-062` вынесен над историей ЧУЖОГО репозитория и это заметил человек, а не гейт |

**Что закрывается по долгам:** `TD-105` (пропуск PR-гейта обнаружим, но не предотвратим),
корень `TD-106` (симптом снят `cf24aac`, причина — гейт вне CI).

## 2. Allowed / Forbidden paths

**architect:** `milestones/M-60b-*.md` · `scripts/tests/red_context_budgets.sh` (✅ готов) ·
`scripts/tests/red_gate_meta.sh` (✅ готов) · `scripts/verify_M-60b.sh` · шаблон шапки
`GATE-META` в `gates.md` §4 (≤8 строк, **с токеном `FOUNDER-APPROVED`** — зона под замком M-60a).
**dev:** `scripts/check_context_budgets.sh` · `scripts/check_gate_meta.sh` ·
`.github/workflows/ci.yml` (три джоба + пробы + `status-check.needs`).
**Forbidden:** `crates/**`; существующие вердикты; `check_protected_artifacts.sh` и его проба.

**Contract impact:** нет.

## 3. §Tasks

| # | Задача | Зона | Статус | Оракул |
|---|---|---|---|---|
| 1 | `check_context_budgets.sh` — таблица лимитов ВСТРОЕНА в скрипт (default-режим), `BUDGET_TABLE` только для проб | dev | ⏳ OPEN | `red_context_budgets.sh` CB-1..CB-6b |
| 2 | `check_gate_meta.sh` — проверки 1–4 (форма, репозиторий, существование ревизий, предок) | dev | ⏳ OPEN | `red_gate_meta.sh` GM-1..GM-9 |
| 3 | `check_gate_meta.sh` — проверка 5 (subject-lock) + оба легальных выхода | dev | ⏳ OPEN | GM-10..GM-15 |
| 4 | Проводка: три джоба + `verify_design_claims.sh` + его проба + все в `status-check.needs` | dev | ⏳ OPEN | шаг W (прогоном) |
| 5 | Шаблон `GATE-META` в `gates.md` §4 + RED на неверный `milestone:` | **architect** | ⏳ OPEN | GM-16 (новый) |
| 6 | `scripts/verify_M-60b.sh` | **architect** | ⏳ OPEN | — |

## 4. Находки `C-064`, которые обязаны быть закрыты здесь

| находка | требование |
|---|---|
| **F-064-2** BLOCKER | проводка проверяется РАЗБОРОМ workflow (id джоба, реальная `run:`-команда, ПОЛНЫЙ состав `status-check.needs`), а не грепом: имена в комментарии удовлетворяют греп |
| **F-064-3** BLOCKER | либо сузить заявку subject-lock'а до гейт-класса ЧЕСТНО, либо накрыть весь диф; плюс RED на неверный `milestone:` — сейчас его нет, и реализация может игнорировать поле |
| **F-064-4** MAJOR | acceptance обязан гонять `check_context_budgets.sh` в **default-режиме на корне репо**: проба гоняет его только через `BUDGET_TABLE`, поэтому пустая встроенная таблица прошла бы всё |
| **F-064-6** MAJOR | `FOUNDER-APPROVED` — аудит-след, НЕ доказательство действия founder'а. Добавить сценарии side-commit-под-tokened-merge и удаление/переименование запертого файла либо назвать остаточный риск |
| моя находка 05.08 | 28 из 40 проверок «`test result: ok`» в гейтах не считают исполненные тесты; 2 критичны (`verify_M-40.sh:129`, `verify_M-51.sh:151`). Правка формы гейтов — сюда, не заплаткой |

## 5. Запретный список

| Запрещено | Почему |
|---|---|
| Проверять подключённость грепом | `F-064-2`: комментарий с именем скрипта проходит |
| Тестировать бюджет только через `BUDGET_TABLE` | `F-064-4`: реальная встроенная таблица останется непроверенной |
| Требовать `GATE-META` от вердиктов ВНЕ диапазона события | ретроспективная правка 60+ защищённых артефактов ради формы |
| Применять subject-lock к REJECT/KILL/CONCERNS/ESCALATE | после них правки штатны; лок, красящий нормальный круг, вреднее отсутствующего |
| Поднимать бюджет вместо дренажа | бюджет сужается вычисткой, а не смягчением; урок einhard: их R13 красный и терпится |

## 6. Acceptance — `scripts/verify_M-60b.sh`

Паритет с CI, плюс: C `red_context_budgets` 6/6 · G `red_gate_meta` 15/15 + GM-16 ·
**D** `check_context_budgets.sh` в default-режиме на корне репо → жёсткие лимиты для
`.claude/rules/*` и `CLAUDE.md` присутствуют · W проводка **разбором** workflow ·
P регресс `red_protected_artifacts.sh` 20/20. Число исполненных тестов сверяется с
объявленным в файле (`declared_tests`), а не с константой — урок `verify_M-59.sh`.
