<!-- FACTS: audited_head=d1221b1ca932d0b8e95403c2849308ed6e7b9ce2 collected=2026-08-29 -->

# M-72 — терминальность подписки: одно свойство, три носителя

STATUS: PROPOSED (2026-08-29, architect; **rev 2 — 2026-08-31, закрытие `C-190` B-1/B-2/B-4**;
`B-3` — в работе тем же кругом). Предмет — **шов завершения подписки на push-пути**
`gateway-serve`. Закрывает `TD-177`, `TD-178`, `TD-179` и решает форму `TD-180`.

**Почему одним милестоуном, а не четырьмя задачами.** Шов ОДИН: «pump вернулся с отказом —
что стало с подпиской и с уже собранными кадрами». Три карточки описывают его с трёх сторон
(снятие без сверки generation · отсутствие оракула точки входа · потеря кадров на журнальном
отказе). Дробление дало бы четыре круга гейта по одному и тому же коду.

**Почему СЕЙЧАС.** Все три сегодня недостижимы: прод-ответ ≈62 КБ против предела 2 000 000 Б
(`П-020`), полосы глубины выключены (`GATEWAY_BANDS=0.001`). Они оживают РОВНО с включением
`M-70`. `TECH-DEBT.md` `TD-177` формулирует это обязанностью: «до включения полос карточка
обязана быть закрыта». То есть `M-72` — предусловие `M-70`, а не параллельная работа.

## Objective

Завершение подписки становится **наблюдаемым и не задевает чужую подписку**:

1. снятие подписки по терминальному отказу сверяет generation ровно так же, как её сохранение;
2. push-путь `v1` и legacy покрыт оракулом ТОЧКИ ВХОДА — той формой вызова, какой его зовёт
   прод, а не библиотечными строителями;
3. не-cap отказ середины `pump` не теряет уже собранные кадры молча;
4. `Snapshot` не объявляет курсор, доставки которого не было.

**Инвариант достаточности:** после `M-72` не существует пути, на котором подписка исчезает,
деградирует или теряет кадры, а потребитель об этом не извещён. Проверяется оракулами
задач 1–4, каждый — на границе ПОТРЕБИТЕЛЯ (провод), а не участника
(`docs/workflow/oracle-blindness-class-2026-08-28.md`, правило Р-1).

## Contract impact

- **T1 (`crates/contracts`) НЕ трогается.** Предмет — поведение транспорта, не форма события.
- **Открытый вопрос к критику (задача 5).** Задача 4 требует сообщить клиенту причину
  завершения, отличную от предела объёма. Сегодня обе причины ушли бы одним кодом
  `invalid_selector` (`crates/gateway-serve/src/lib.rs:1409`), и это было бы ложью о причине —
  тот же класс, что `TD-138`. Развилка: (а) новый код ошибки протокола ⇒ аддитивная правка
  `docs/rfc/CT-RFC-09-ws-session.md` и bump `GATEWAY_SCHEMA_VERSION` 9→10; (б) тот же код с
  различающим полем. **Выбор — за кругом критика, не за автором спеки.**
- `GATEWAY_SCHEMA_VERSION` меняется ТОЛЬКО если победит (а).

## Allowed paths

| путь | кто пишет |
|---|---|
| `crates/gateway/tests/**` · `crates/gateway-serve/tests/**` | **architect** (sacred RED) |
| `scripts/verify_M-72.sh` | **architect** |
| `milestones/M-72-subscription-terminality.md` | **architect** (dev правит ТОЛЬКО колонку Status) |
| `crates/gateway/src/lib.rs` · `crates/gateway-serve/src/lib.rs` | **engine-dev** (impl) |
| `docs/rfc/CT-RFC-09-ws-session.md` | **architect**, и только при исходе (а) задачи 5 |

## Forbidden paths

`crates/contracts/**` · `crates/risk/**` · `crates/killswitch/**` · `crates/oms/**` ·
`crates/venue-*/**` · `crates/journal/**` · `crates/book/**` · `deploy/**` ·
`docker-compose.yml` · `PROJECT-STATE.md` · `TECH-DEBT.md`.

## Запрещено / Почему — обязательный список для правки СУЩЕСТВУЮЩЕГО кода

`testing.md` §«Спека правки существующего кода обязана содержать ЗАПРЕТНЫЙ СПИСОК»: пробел в
спеке реализация заполняет по-своему и имеет на это право.

| запрещено | почему |
|---|---|
| менять терминальность **cap**-отказа (`refuse_by_cap`, `is_cap_terminal`) | это результат `R-143` B-2 и §4bis.5 `M-71`: «чистая транзакционность» на cap-пути даёт **livelock** — восемь тиков без единого кадра. Предмет `M-72` — НЕ-cap путь |
| различать причину по `io::ErrorKind` | журнал отдаёт `Other` в четырёх местах (`crates/journal/src/segments.rs:654,657,660,663`); матч по виду ошибки объявит повреждение журнала «пределом объёма» (`R-143` B-3). Причина различается ФЛАГОМ РЕДЬЮСЕРА и МЕСТОМ вызова |
| снимать двухпозиционную конструкцию `full_applied_seq` | она гасит двойную свёртку при повторном проходе (`R-139` B-1, оракул `P4`) |
| возвращать `self.cursor` к началу тика («откатить всё») | ровно та развязка, которую `M-71` §4bis.5 вычеркнула: на постоянном отказе даёт бесконечное переигрывание |
| трогать `snapshot(&self) -> Snapshot` (сигнатуру) | её зовут sacred-тесты для подсчёта байт в panic-сообщении; менять возврат запрещено scope-guard'ом. Правится `snapshot_checked` либо появляется третий строитель |
| «чинить» оракулы `P4`, `P6`, `W5`, `F-036` | они честны по своим осям; предмет `M-72` — то, чего они НЕ покрывают, а не они сами |
| менять `GATEWAY_BANDS` в `docker-compose.yml` | включение полос — граница C (`П-014` п.4), отдельное решение founder'а, не побочный эффект этого милестоуна |

## §Tasks

| # | Status | задача | кто | verify | файлы |
|---|---|---|---|---|---|
| 1 | ✅ DONE | **Оракул ТОЧКИ ВХОДА на push-пути** (`TD-178`) — `E-1` vantage + `E-2` предмет; мутационный контроль исполнен в изолированной копии: снятие терминальности роняет `E-2`, `E-1` цел. Живая WS-сессия, прод-форма вызова: `subscribe → pump → frame` через `run_v1_session_loop` и legacy. Пиннит: кадры доходят до провода; терминальный отказ ЗАВЕРШАЕТ подписку и извещает клиента. Мутационный контроль обязателен: отключение терминальности роняет оракул | architect | `cargo test -p gateway-serve --test red_ws_terminality_entrypoint` | `crates/gateway-serve/tests/red_ws_terminality_entrypoint.rs` |
| 2 | ✅ DONE (rev 2) | **RED на `TD-177` — ИСПОЛНИМ и КРАСЕН**, не COMPILE-RED. Переписан по `C-190` B-1 против СУЩЕСТВУЮЩЕГО шва `test_sync::rendezvous`; выдуманный `test_seam` снят. По `C-190` B-2 добавлена СВЕЖАЯ дописка ETH ПОСЛЕ switch+release и опознание кадра ПО ЦЕНЕ (`Frame` селектора не несёт), плюс страж «кадр старого инструмента под чужим id». Выполнимость предъявлена прогоном в ОБЕ стороны: против текущего кода `FAILED` с телом ошибки, против прототипа фикса — `3 passed`; `E-1`/`E-2` зелены в обеих конфигурациях | architect | `cargo test -p gateway-serve --features testing --test red_ws_terminality_entrypoint td177_stale_pump_does_not_kill_new_sub` | там же |
| 3 | ⏳ OPEN | **Фикс `TD-177` — ТОЛЬКО сверка поколения.** Половина «внести тестовый шов» СНЯТА (`C-190` B-1): шов существует и подключён. Снятие подписки сверяет generation теми же тремя условиями, что и сохранение (`!drained && current_gen == Some(gen_at_pump)`). ОБА пути: `crates/gateway-serve/src/lib.rs:1401` (v1) и `:1806`-класс (legacy). Комментарий `:1290-1306`, объявляющий свойство действующим, приводится к коду. Форма фикса на v1-пути предъявлена прототипом architect'а и роняет красное в зелёное; legacy-путь dev закрывает сам, тем же условием | engine-dev | задача 2 GREEN под `--features testing` | `crates/gateway-serve/src/lib.rs` |
| 4 | ✅ DONE | **RED на `TD-179`** (написан и КРАСЕН: `frame crc mismatch`, курсор `None → Some(767)` при нуле отданных кадров; позитивный контроль `M-1` зелёный): `Err` из середины `pump` (не по пределу) при ≥1 уже закрытом батче. Ожидание: собранные кадры НЕ теряются, серия потребителя равна реплею того же окна (`VB-I-2`), либо подписка терминируется с извещением. Сегодня падает: `self.cursor = cursor` на `:3579` уже продвинут, `frames` уносится `?` на `:3551` | architect | `cargo test -p gateway --test red_pump_midstream_failure` | `crates/gateway/tests/red_pump_midstream_failure.rs` |
| 5 | ⏳ OPEN | **Решение формы извещения** (см. Contract impact) + фикс `TD-179` по выбранной форме | critic решает форму → engine-dev impl | задача 4 GREEN | `crates/gateway/src/lib.rs`, при (а) — `docs/rfc/CT-RFC-09-ws-session.md` |
| 6 | 🚧 RED ГОТОВ | **`TD-180`** (MINOR): `snapshot()` отдаёт `cursor: self.cursor` (`crates/gateway/src/lib.rs:3714`) в окне, где батч свёрнут в состояние, но отвергнут пределом. **Форма РЕШЕНА** (§«Форма задачи 6»), **оракул написан и КРАСЕН** (`S-1` ok, `S-2` FAILED: `None` против `Some(24999)`); выполнимость предъявлена — против фикса оба зелены, соседние оракулы не ломаются. Осталось: фикс | ~~architect форма~~ → engine-dev | `cargo test -p gateway --test red_snapshot_cursor_honesty` | `crates/gateway/tests/red_snapshot_cursor_honesty.rs` |
| 7 | ✅ DONE | **Приземлён оракул `P7`** (`3c91816`; `red_egress_cap_paths` 8 passed, `EXPECT_P` 7→8) со спас-рефа `refs/salvage/2026-08-28-P7/m71-terminality-oracle` (`27e8a5f`) — правдивость признака терминальности. Мутационный контроль исполнен на прошлом круге, предел чувствительности назван внутри самого оракула | architect | `cargo test -p gateway --test red_egress_cap_paths` | `crates/gateway/tests/` |
| 8 | ⏳ OPEN | **Снять ЛИТЕРАЛЬНЫЙ путь `scripts/verify_M-71.sh`** из комментария `crates/gateway/src/lib.rs:1964`. Якорь `MUT-ANCHOR M-71-LIMIT` функционален и ОСТАЁТСЯ — снимается только литеральный путь. **Второй носитель СНЯТ architect'ом этим же кругом** (`C-190` B-4: обещание починить не является закоммиченным артефактом): в `crates/gateway-serve/tests/red_ws_terminality_entrypoint.rs` литерала больше нет, замер `grep -c` → 0. Критерий задачи теперь ДОСТИЖИМ исполнителем | engine-dev | `git grep -c 'scripts/verify_M-71' crates/ deploy/` → 0 | `crates/gateway/src/lib.rs` |

## Определённость гонки задачи 2 — требование, а не деталь реализации

`TD-177` живёт в ГОНКЕ: `subscribe` приходит, пока `pump` старого селектора В ПОЛЁТЕ. Оракул,
воспроизводящий её ТАЙМИНГОМ (сделать `pump` медленным большим backlog'ом и «успеть» послать
`subscribe`), был бы флаком: он мерил бы планировщик, а не инвариант — прямой запрет
`testing.md` §«Целостность гейта», свойство 2. Флаковый оракул хуже отсутствующего: его
объявляют шумом и отключают.

**Поэтому шов ОБЪЯВЛЯЕТСЯ здесь, а не выбирается реализацией.** Задача 3 обязана внести
тестовый шов под `#[cfg(feature = "testing")]` (фича ОБЪЯВЛЕНА в `crates/gateway-serve/Cargo.toml:25`,
но НИ РАЗУ не использована — замер: `grep -c 'cfg(feature = "testing")' crates/gateway-serve/src/lib.rs`
→ `0`; прежняя редакция писала «крейт его уже использует», и это было неверно), дающий тесту
удержать `pump` между «стартовал» и «вернулся»:

| элемент | требование |
|---|---|
| видимость | ТОЛЬКО `#[cfg(feature = "testing")]`; в прод-сборке шва не существует — проверяется грепом в `verify_M-72.sh` |
| семантика | тест получает сигнал «pump стартовал, `gen_at_pump` захвачен» и держит возврат, пока не разрешит |
| запрет | шов НЕ меняет порядок операций прод-пути и не добавляет ветвлений в него — иначе оракул судит не тот код (класс `R-133` `N-3`: дефект между уровнями) |

**Порядок из этого следует и он необычен:** сигнатуру шва объявляет architect (эта таблица),
вносит engine-dev задачей 3, а оракул задачи 2 пишется против объявленной сигнатуры. Это НЕ
нарушение RED-first: тест по-прежнему предшествует ФИКСУ поведения; предшествовать самому
себе он не может, и притворяться иначе было бы бухгалтерией вместо защиты
(`harness-track` §«Что убрано», строка про RED-first по форме).

### ШОВ УЖЕ СУЩЕСТВУЕТ — правка `C-190` B-1, и она отменяет прежний раздел целиком

**Прежняя редакция объявляла здесь НОВЫЙ шов `test_seam` на пять функций и поручала его
внесение задаче 3. Шов был ВЫДУМАН.** Детерминированная точка встречи существует, подключена
к проду и уже используется:

```
$ git show HEAD:crates/gateway-serve/src/test_sync.rs | wc -l          → 174
$ grep -n 'pub fn ' crates/gateway-serve/src/test_sync.rs
    93: arm(id)   109: pump_signal_and_wait(id)   137: test_wait_for_pump(id, timeout) -> bool
   158: test_release(id)                          170: test_remove(id)
$ grep -n 'mod test_sync\|test_sync::' crates/gateway-serve/src/lib.rs
    77: #[cfg(any(test, feature = "testing"))]     78: pub mod test_sync;
  1228: crate::test_sync::rendezvous::pump_signal_and_wait(&id_for_sync);   ← v1
  1693: crate::test_sync::rendezvous::pump_signal_and_wait(&id_for_sync);   ← legacy
$ grep -n 'test_sync' crates/gateway-serve/tests/red_ws_session.rs
  1325: use gateway_serve::test_sync::rendezvous;                          ← оракул O-12
```

**Как это произошло — записано, потому что класс дороже экземпляра.** Обоснованием выдумки
служил ЗАМЕР, и в этом суть:

| в спеке | на самом деле |
|---|---|
| `grep -c 'cfg(feature = "testing")' … ` → `0`, вывод «фича объявлена, но НИ РАЗУ не использована» | настоящее ограждение — `cfg(any(test, feature = "testing"))`, его `grep -c` → **4** |

Узкий шаблон дал ноль; ноль был предъявлен как доказательство отсутствия. Хуже того, ВЕРНОЕ
утверждение предыдущей редакции («крейт его уже использует») было этим заменено на ложное —
коммитом `d4778f8`, озаглавленным «снято ложное утверждение о коде». То есть измерение
послужило установке неправды, а не её опровержению. Это `reading-map` §2 ПРАВИЛО
ПРЕДШЕСТВЕННИКА: искать РЕШЁННОЕ прежде, чем проектировать. Шестой случай подряд.

**Действующий контракт шва — тот, что в коде** (`test_sync::rendezvous`), и он ДОСТАТОЧЕН:

| требование | как выполнено существующим швом |
|---|---|
| видимость | `cfg(any(test, feature = "testing"))`; в прод-сборке модуля нет. Шаг `S` гейта проверяет ОТСУТСТВИЕ имён вне cfg-гейта и собирает крейт БЕЗ фичи |
| семантика | `pump_signal_and_wait` сигналит «вошёл» и БЛОКИРУЕТ возврат до `test_release` — оба хука в одном вызове, ровно то окно, что нужно `TD-177` |
| ключ | канал ключуется ID ПОДПИСКИ (`lib.rs:1214`, `id_for_pump = id.clone()`), и при переподписке id ТОТ ЖЕ. Это не помеха: `test_release` не сбрасывает `release`, поэтому удержан только ПЕРВЫЙ pump, а pump новой подписки проходит насквозь |
| детерминизм | `test_wait_for_pump` с таймаутом и явным SETUP-отказом; ни одного `sleep` |
| запрет менять порядок | вызов стоит ОДНОЙ строкой перед `live.pump(...)` на обоих путях; без фичи блока не существует |

**Следствие для §Tasks:** задача 3 теряет половину «внести шов» — вносить нечего. Остаётся
ТОЛЬКО фикс сверки поколения. Оракул задачи 2 переписан против существующего шва, **исполним
и КРАСЕН** (не COMPILE-RED), выполнимость предъявлена прогоном в обе стороны.

## Форма задачи 6 (`TD-180`) — РЕШЕНА architect'ом 2026-08-29, обоснование ниже

Решение принимается здесь, потому что таблица §Tasks отдаёт форму architect'у. Оракул
задачи 6 пишется ПОСЛЕ этого решения и против него.

**Что именно расходится.** У редьюсера ДВЕ позиции, и они разные по смыслу
(`crates/gateway/src/lib.rs:3249-3255`, объявлено самим кодом): `cursor` — до какого seq кадр
УЖЕ ОТДАН потребителю; `full_applied_seq` — до какого seq СОСТОЯНИЕ уже свёрнуто. Инвариант
`full_applied_seq >= cursor.upto_seq`; равенство — установившийся режим, строгое неравенство —
окно «батч свёрнут в состояние, но отвергнут пределом и потому не отдан» (`pump`, `:3571`
— `self.cursor = cursor` исполняется ТОЛЬКО после успешной проверки предела).

`snapshot()` (`:3708`) берёт `series` из `self.full`, то есть из состояния на
`full_applied_seq`, а `cursor` кладёт `self.cursor` — позицию ДОСТАВКИ. В окне расхождения
снимок внутренне несогласован: серии описывают одно, курсор — другое.

**Почему это дефект, а не деталь.** Клиент, получив `snapshot(C)`, подписывается «с C».
Если `C` — закладка ДОСТАВКИ (позади состояния), отвергнутый батч уже лежит в сериях снимка
И будет доставлен ещё раз ⇒ двойное применение у клиента (`GW-I-4`). Если `C` — позиция
СОСТОЯНИЯ, тот же батч в снимке есть и повторно не придёт.

**РЕШЕНИЕ: `snapshot()` объявляет позицию СОСТОЯНИЯ, а не доставки.**
`cursor` снимка строится из `full_applied_seq`. Это не «ещё одно поле», а приведение поля к
тому, что снимок и есть: дамп СОСТОЯНИЯ, и его курсор обязан описывать состояние.

| проверено | как |
|---|---|
| сигнатура не меняется | `snapshot(&self) -> Snapshot` цела — §Запрещено требует именно этого (её зовут sacred-тесты для подсчёта байт) |
| схема не меняется | новых полей нет ⇒ `GATEWAY_SCHEMA_VERSION` не бампается. Существенно: `docs/fa-viz-M-68-close` уже двигает схему 8→9, и второй одновременный бамп дал бы коллизию версий на двух ветках |
| журнал не читается | `full_applied_seq` — поле в памяти; второго прохода по хвосту не появляется (`VB-I-10`, оракул `F-036`) |

**Отвергнутые формы — с причиной, а не умолчанием:**

| форма | почему отвергнута |
|---|---|
| добавить поле `state_seq` рядом с `cursor` | бамп схемы ради MINOR-долга плюс коллизия с бампом `M-68`; и оставляет потребителю выбор, какое поле считать точкой возобновления — то есть переносит дефект на него |
| третий строитель `snapshot_at_cursor()`, сворачивающий состояние ровно до `cursor` | невозможен по построению: у метода нет доступа к журналу (`:3697-3700` — это его заявленное свойство), а «отмотать» уже свёрнутое состояние нечем |
| откатывать состояние при отказе | прямо вычеркнуто `M-71` §4bis.5: на постоянном отказе даёт бесконечное переигрывание |

**Оракул задачи 6 обязан (пишется architect'ом следующим кругом):** построить окно
расхождения (батч свёрнут, отвергнут пределом — фикстура класса `red_egress_cap_paths`),
снять `snapshot()` и утверждать, что его `cursor` равен `full_applied_seq`, а НЕ закладке
доставки. Позитивный контроль обязателен: в установившемся режиме обе величины совпадают, и
оракул, зелёный только там, ничего не пиннит.

**`TD-179` и `TD-180` НЕ ПРОТИВОРЕЧАТ ДРУГ ДРУГУ — поверхности разные, и это сказано
здесь, чтобы следующий круг не «примирял» их правкой.** Оракул `TD-179` (`M-2`) требует,
чтобы ЗАКЛАДКА ДОСТАВКИ (`r.cursor()`) НЕ уходила вперёд при отказе. Решение `TD-180`
меняет то, что объявляет `snapshot().cursor`, — другую поверхность. Закладка остаётся на
месте; меняется только то, какую из двух позиций СНИМОК называет своей.

Проверено прогоном, а не рассуждением (фикс внесён в изолированную копию, `cmp` подтверждён,
копия удалена — impl-код architect не коммитит):

```
без фикса:  td_180_s1 ok · td_180_s2 FAILED (None против Some(24999))
с фиксом:   td_180_s1 ok · td_180_s2 ok            → оракул выполним, не вечно-красный
весь cargo test -p gateway С ФИКСОМ: единственный красный —
  td_179_m2_failed_pump_must_not_leave_cursor_ahead_of_delivered
и он красен ТОЧНО ТАК ЖЕ БЕЗ фикса: та же строка :268, то же
  «закладка доставки ушла вперёд (None → Some(804))», отказ «frame length absurd»
```

То есть фикс `TD-180` не покупается ценой соседнего инварианта; `TD-179` остаётся открытым
по СВОЕЙ причине (не-предельный отказ посреди `pump`), и закрывает его задача 5.

**Предел решения назван:** оно чинит СНИМОК, а не расхождение. Расхождение штатно и остаётся —
`M-71` §4bis.5 запретил его устранять откатом. Через прод-пути окно сегодня недостижимо
(все три вызывателя берут снимок сразу после `resume`, `TD-180` это замерил), поэтому
задача 6 — MINOR и стоит последней в милестоуне.

## RED-тесты (пути)

- `crates/gateway-serve/tests/red_ws_terminality_entrypoint.rs` — задачи 1, 2
- `crates/gateway/tests/red_pump_midstream_failure.rs` — задача 4
- `crates/gateway/tests/red_snapshot_cursor_honesty.rs` — задача 6

**Достижимость харнесса предъявлена, а не предположена.** Живая WS-сессия уже гоняется
девятнадцатью тестами в `crates/gateway-serve/tests/` (`red_ws_session.rs`,
`red_ws_protocol.rs`, `red_ws_series_vs_replay.rs`, `red_ws_liveness_under_load.rs`,
`smoke_ws.rs` и др.), плотная фикстура предела — в `red_egress_cap_wire.rs`.
Объявлять оракул задачи 1 недостижимым было бы повторением класса, за который автор
получил `R-145` `Б-2`: требование гейта снято обоснованием, опровергаемым одним прогоном.

## Acceptance

`scripts/verify_M-72.sh` — `set -euo pipefail`, ≥1 проверка на задачу, финальная строка
`VERDICT: PASS`/`VERDICT: FAIL`, решение по КОДУ ВОЗВРАТА. Обязательный состав:

1. паритет с CI: `cargo fmt --all -- --check` · `cargo clippy --all-targets --all-features -- -D warnings` · `cargo test --all`;
2. по одной проверке на задачи 1–8, каждая — прогоном именованного теста;
3. **мутационный контроль** задачи 1: нейтрализация терминальности роняет оракул точки входа — с предъявлением `BUILD_EXIT`, а не статуса пайпа;
4. **сверка состава набора**: литерал числа оракулов И счётный регексп обновляются ТЕМ ЖЕ коммитом (регексп без цифр в именах дал ложное КРАСНОЕ на `M-68` — `d18e`/`d18f`);
5. проверка задачи 8 грепом: `scripts/verify_M-71` в `crates/`/`deploy/` — ноль совпадений.

## Маршрут и гейты

- **Полный цикл `04-workflow` §2**, не харнесс-трек: код исполняется прод-процессом
  (`harness-track` §4 это прямо запрещает).
- **critic обязателен** — триггер `gates.md` §1 п.3 (≥5 атомарных коммитов) и §9 (новая
  milestone-спека).
- **risk-critic НЕ требуется**: `risk`/`killswitch`/`oms`/`venue-*`/`contracts` не тронуты.
- **RAW-гейт НЕ требуется**: раскладка и формат журнала не меняются.
- Порядок: architect коммитит спеку + RED + verify ⇒ critic ⇒ founder диспетчеризует
  engine-dev ⇒ tester ⇒ reviewer ⇒ merge ⇒ деплой-гейт `gates.md` §8.

## Handoff

Следующий — `critic` (средняя модель; RAW-гейт не применяется). Предмет: закоммиченный набор
спека + RED + verify на ветке `feat/M-72-subscription-terminality`. Отдельный вопрос круга —
форма извещения задачи 5 (см. Contract impact).
