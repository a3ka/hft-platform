# M-45 — расширение L2Delta-эмиссии: allow-list из хардкода в конфиг

**Статус:** СПЕКА ГОТОВА; **раскатка блокирована Границей C** (две подписи founder'а — §Граница C).
Реализация и merge — НЕ блокированы (см. «Ключевое решение» ниже).
**Founder-подпись на сам milestone:** 2026-07-27 (объём — «расширить охват», механизм есть с M-18).
**Основание объёма:** `docs/rfc/CT-RFC-06-l2delta.md` §8.1 (после critic `C-045` PASS и
reviewer `R-019`/`R-021`).
**Гейты:** critic (если ≥5 коммитов) → venue-dev → tester → reviewer → §8 деплой-гейт.
**risk-critic НЕ требуется** — MD-only carve-out (`.claude/rules/gates.md` §5): дифф трогает
только read-only MD-путь (нормализация → `MdEvent`), никакого order-egress (submit/cancel/
подпись торговых действий). Reviewer обязан подтвердить MD-only в Block-scope — иначе
RISK-BLOCK применяется полностью.

---

## 0. Что НЕ является предметом этого milestone'а (проверено, не предположено)

`CT-RFC-06` §0.2 опроверг посылку «M-45 вводит новый вариант T1». Проверено командами:

```text
$ grep -n "L2Delta {" crates/contracts/src/lib.rs
293:    L2Delta {
$ git log --oneline -1 -S "L2Delta {" -- crates/contracts/src/lib.rs
6af0aef feat(M-18): CT-RFC-04 — MdPayload::L2Delta T1 (сырые book-дельты)
```

Вариант 6 `MdPayload::L2Delta` существует в T1 с `CT-RFC-04`/M-18 (CLOSED 2026-07-21).
Следствия, каждое — отрицание работы, которую делать НЕ надо:

- **contract-пакет `docs/05-contract-layer.md` §4 НЕ собирается** (форма T1 не меняется);
- **`SCHEMA_VERSION` НЕ бампается** (остаётся 4) — бинарная совместимость не затронута;
- **карта из ПЯТИ exhaustive-`match` (`CT-RFC-06` §8.2) к этому milestone'у НЕ применяется** —
  она про введение НОВОГО варианта (HL-дельта, §9.3). Новый вариант здесь не вводится ⇒
  E0004 не возникает ни в одном из пяти мест. `cargo build --workspace` в verify всё равно
  обязателен (§4) — но как защита от РЕГРЕССИИ, а не как ожидаемая поломка.

**Предмет milestone'а — ровно одно:** какие символы эмитят уже существующий вариант.

## 1. Ключевое решение — allow-list из хардкода в конфиг (развязка merge от раскатки)

**Проблема, которую оно решает.** Сегодня набор символов — хардкод-константа:

```text
$ grep -rn "L2DELTA_CAPTURE_SYMBOLS" crates/ --include=*.rs
crates/venue-binance/src/lib.rs:485:        const L2DELTA_CAPTURE_SYMBOLS: &[&str] = &["BTCUSDT"];
crates/venue-binance/src/lib.rs:251:        if L2DELTA_CAPTURE_SYMBOLS.contains(&symbol.as_str()) {
crates/venue-binance-futures/src/lib.rs:460:  const L2DELTA_CAPTURE_SYMBOLS: &[&str] = &["BTCUSDT"];
crates/venue-binance-futures/src/lib.rs:642:  if L2DELTA_CAPTURE_SYMBOLS.contains(&symbol.as_str()) {
```

Doc-comment у обеих констант говорит прямо: «расширение набора требует отдельного решения
founder'а». Значит правка константы = совершение решения Границы C **кодом**, а merge такого
кода = раскатка. Это сцепляет две разные вещи: инженерную готовность и продуктовое решение о
составе данных.

**Решение (нормативное для этого milestone'а).** Набор символов становится **конфигурацией с
дефолтом, равным сегодняшнему поведению**:

- источник — env `L2DELTA_CAPTURE_SYMBOLS` (список через запятую), как уже сделано для
  `EPOCH_ID` (`crates/recorder/src/main.rs:481`) — тот же операторский рычаг, та же форма;
- **дефолт при отсутствии/пустом значении — `["BTCUSDT"]`**, байт-в-байт текущее поведение;
- нормализация регистра при разборе (см. оракул O-4 — это не косметика, а дефект-кандидат).

Что это даёт:

1. **Merge безопасен** — прод после деплоя эмитит ровно то же, что до него. §8-eyes-on
   проверяет отсутствие изменения, а не наличие.
2. **Раскатка — операторский шаг**, а не коммит: founder называет состав, оператор выставляет
   env + новый `EPOCH_ID` и перезапускает. Граница C соблюдена буквально: **ни один агент не
   меняет состав данных**.
3. **Откат — рестарт без env**, а не revert коммита в проде.

Цена: одна ветка разбора конфига и её тесты. Альтернатива «править константу в milestone'е»
отвергнута — она требует founder-подписи ДО написания кода и делает откат деплоем.

## 2. Эпохи — обязательное условие раскатки (`CT-RFC-06` §3)

Изменение состава эмиссии — **режимный сдвиг**: форма не меняется, семантика потока меняется.
Ровно класс E-001, где границу эпохи пришлось восстанавливать постфактум по heartbeat'ам на
123 млн событий. Механизм маркировки существует end-to-end (T1-поле `SegmentHeader.epoch_id` →
`decide_open_segment` не переиспользует сегмент чужой эпохи → env `EPOCH_ID`), поэтому:

- **новый `EPOCH_ID` выставляется ДО рестарта** с новым составом; конвенция значения —
  `own-YYYY-MM-m45-<slug>`;
- **запись в `docs/data-epochs.md` делается ДО раскатки**, класс «режимный сдвиг», не «дефект»;
- `SCHEMA_VERSION` НЕ бампается — он про бинарную совместимость, не про семантику (§0).

**Остаточный класс, названный явно** (`CT-RFC-06` §3, находка R-019 F4): машинного fail-closed
на «состав эмиссии изменился, а `epoch_id` — нет» **не существует** — код сверяет `epoch_id` со
своим же конфигом, а не с фактическим составом потока. Забыли выставить → сегмент
переиспользуется и две эпохи молча смешаются. Здесь это закрывается **дисциплиной и verify-гейтом**
(задача #5), не кодом; машинная сверка состава — кандидат в отдельный milestone, сознательно вне
объёма.

## 3. §Tasks

| # | Задача | Зона | Статус | Оракул / гейт |
|---|---|---|---|---|
| 1 | `parse_capture_symbols` + `should_capture_l2delta` в `venue-binance` (дефолт `["BTCUSDT"]`, нормализация регистра) | venue-dev | ⏳ OPEN | T3, T4 |
| 2 | То же в `venue-binance-futures`; семантика `pu` НЕ трогается | venue-dev | ⏳ OPEN | T3, T4, T6 |
| 3 | Emit-путь обоих крейтов зовёт `should_capture_l2delta` вместо `const … .contains()`; хардкод-`const` со списком удалён | venue-dev | ⏳ OPEN | T5 |
| 4 | Фикстура `L2Delta` в оракул `DET-I-1` — закрытие R-019 F6 / TD-072 | **architect** (sacred) | ✅ DONE (`det_9`) | T8 |
| 5 | `scripts/verify_M-45.sh` | **architect** (sacred) | ✅ DONE | — |
| 6 | Запись эпохи в `docs/data-epochs.md` — ТОЛЬКО при раскатке (Граница C), не при merge | architect | ⛔ ждёт подписи | T9 |

Задачи 4–6 — architect-only (`*/tests/**` и `scripts/verify_*.sh` — sacred,
`.claude/rules/scope-guard.md`). Задачи 1–3 — venue-dev, **строго по RED-оракулам**; тесты и
verify-скрипт dev НЕ правит (странный тест → `!!! SCOPE VIOLATION REQUEST !!!`).

### API-контракт (задачи 1–2) — форма зафиксирована оракулом, не свободна

```rust
/// Разбор allow-list из СЫРОЙ строки конфигурации. ЧИСТАЯ функция.
pub fn parse_capture_symbols(raw: Option<&str>) -> Vec<String>;

/// Решение об эмиссии: wire-символ против разобранного списка.
pub fn should_capture_l2delta(symbols: &[String], symbol: &str) -> bool;
```

**Разбор обязан быть чистой функцией, а чтение `env` — тонкой обёрткой над ним.** Причина не
стилистическая: `env` — глобальное состояние процесса, а `cargo test` гоняет тесты
параллельно в потоках. Оракул, дёргающий `set_var`, стал бы гонкой и давал бы разный
результат при разном порядке запуска — прямое нарушение принципа «в доменном коде нет
недетерминизма» (`CLAUDE.md`). Поэтому оракулы бьют в чистое ядро.

Семантика, закреплённая оракулами (отклонение = красный гейт):
- `None` и пустая/вырожденная строка (`""`, `"   "`, `","`) → **дефолт `["BTCUSDT"]`**;
  ни «эмитить всё» (взрыв объёма + необъявленная эпоха), ни «не эмитить ничего»
  (тихая потеря forward-only данных);
- пустые элементы отбрасываются, пробелы обрезаются, порядок сохраняется;
- сравнение регистронезависимое, но **не подстрочное**: `BTC` и `BTCUSDT_PERP` не совпадают
  с `BTCUSDT`.

### Оракулы (написаны, GREEN только после задач 1–3)

- `crates/venue-binance/tests/red_l2delta_allowlist.rs` — O-1..O-4, O-6;
- `crates/venue-binance-futures/tests/red_l2delta_allowlist.rs` — то же + перп-специфика
  (allow-list не имеет права ронять `prev_final_update_id` в `None` — урок TD-014);
- `crates/journal/tests/red_det_replay_digest.rs::det_9` — DET-I-1 на смешанном журнале.

Дублирование спот/перп намеренно: `L2DELTA_CAPTURE_SYMBOLS` объявлена в двух крейтах
независимо, тесты крейт-локальны, и оракул только на споте пропустил бы половину дефекта
(«в споте регистр нормализовали, в перпах забыли»).

### RED-статус на момент коммита набора (анти-плацебо)

`bash scripts/verify_M-45.sh` → **VERDICT: FAIL (7 нарушений)**, exit=1 — падает по существу
(нет `parse_capture_symbols`/`should_capture_l2delta`, жив хардкод-`const`), а не по setup.
Зелёные T0/T1/T6/T7/T9 — это проверки «ничего не сломано», они и обязаны быть зелёными до
реализации.

## 4. RED-оракулы — спецификация (architect пишет ДО кода)

Чек-лист `.claude/rules/testing.md` «фикстура счастливого пути — дефект оракула» пройден по всем
пяти пунктам; каждый оракул обязан ПАДАТЬ на заглушке.

**O-1 (множественность + позитив).** В одном такте приходят диффы по ДВУМ символам, оба в
allow-list → эмитятся ДВА `L2Delta`. Анти-плацебо: реализация, обрабатывающая только первый
символ такта, обязана упасть. Покрывает пункт «множественность».

**O-2 (негатив — главный анти-плацебо).** Символ ВНЕ allow-list → `L2Delta` НЕ эмитится, поток
`L2Snapshot` для него не меняется. Без этого оракула реализация «эмитить всегда» проходит O-1
зелёным. Тест обязан проверять ОТСУТСТВИЕ события, а не наличие проверки.

**O-3 (границы).** Три входа: (а) env не задан → дефолт ровно `["BTCUSDT"]`, поведение
байт-в-байт как до milestone'а; (б) env задан пустой строкой → трактуется как дефолт, НЕ как
«эмитить всё» и НЕ как «эмитить ничего» молча; (в) список с пробелами/пустыми элементами
(`"BTCUSDT, ,ETHUSDT"`) → разбирается предсказуемо, мусорный элемент не становится символом.
Покрывает «границы» и «отсутствие».

**O-4 (регистр — дефект-кандидат, не косметика).** Сегодня сравнение точное:
`L2DELTA_CAPTURE_SYMBOLS.contains(&symbol.as_str())`. Если оператор напишет env в нижнем
регистре (`btcusdt`), эмиссия **молча выключится для BTC** — тихая потеря данных, forward-only
и невосстановимая. Оракул: env `"btcusdt"` даёт эмиссию для символа, который приходит из wire
как `BTCUSDT`. Реализация без нормализации обязана упасть.

**O-5 (`DET-I-1` на смешанном потоке — закрытие R-019 F6).** Замер, а не предположение:

```text
$ grep -c L2Delta crates/journal/tests/red_det_replay_digest.rs
0
```

У оракула `DET-I-1` (`replay_digest`) фикстур с `L2Delta` **ноль** — детерминизм на смешанном
журнале сегодня аргументирован (свёртка тип-агностична), но не проверен. Оракул: журнал из
чередующихся `L2Snapshot` + `L2Delta` (обе стороны, включая **асимметричный** дифф — обновлена
одна сторона, и дифф с `size == 0` = remove), реплей ×3 бит-идентичен. Покрывает «асимметрию»
и «отсутствие» (уровень, не упомянутый в диффе, НЕ удаляется).

**O-6 (детерминизм эмиссии).** Один и тот же wire-diff → бит-идентичный `Event`, независимо от
порядка обработки символов. Обязателен для любого пути, попадающего в журнал (`DET-I-1`).

## 5. Allowed paths

**Разрешено:**
- `crates/venue-binance/src/**`, `crates/venue-binance-futures/src/**` (venue-dev, задачи 1–3);
- `crates/venue-binance/tests/**`, `crates/venue-binance-futures/tests/**`,
  `crates/journal/tests/red_det_replay_digest.rs` (**architect only**, задачи 4);
- `scripts/verify_M-45.sh` (**architect only**);
- `docs/data-epochs.md`, этот файл.

**Запрещено безусловно:** `crates/contracts/**` (T1 — форма не меняется, §0) ·
`crates/risk/**`, `crates/killswitch/**` (sacred) · любой order-path (submit/cancel/auth
торговли) · `crates/recorder/src/**` сверх передачи env (маршрутизация `L2Delta` уже есть с
M-18 — трогать её не требуется; понадобилось → SCOPE VIOLATION REQUEST, не самостоятельная
правка) · семантика `pu`/`U`/`u` (урок TD-014: путаница спот/фьючерс ломает gap-детекцию).

## 6. Acceptance — `scripts/verify_M-45.sh`

`set -euo pipefail` либо явный FAIL-счётчик с `exit 1`; финальная строка `VERDICT: PASS|FAIL`;
минимум одна проверка на задачу. Обязательные пункты:

1. `cargo build --workspace` **и** `cargo clippy --workspace --all-targets -- -D warnings` —
   урок RN-8/RN-18: узкий `-p` слеп к E0004 и не компилирует `examples/**`/`src/bin/**`.
2. `cargo test -p venue-binance -p venue-binance-futures -p journal` — O-1..O-6 GREEN.
3. **Дефолт неизменности:** без env `L2DELTA_CAPTURE_SYMBOLS` набор символов ровно `["BTCUSDT"]`
   (машинная проверка, не глазами) — гарантия, что merge не меняет прод.
4. **Запись эпохи:** если в диффе milestone'а меняется состав по умолчанию — в
   `docs/data-epochs.md` обязана быть запись с новым `epoch_id`; отсутствие = FAIL.
5. Grep-канарейка: `L2DELTA_CAPTURE_SYMBOLS` больше не объявлен как хардкод-`const` со списком
   символов ни в одном крейте (иначе задачи 1–2 сделаны наполовину).

## 7. §8 деплой-гейт (`.claude/rules/gates.md` §8)

Изменение касается формата/объёма записи ⇒ eyes-on обязателен и решающий:

- `gh run list` — CI и Deploy `completed success`;
- ssh на VPS: контейнеры `(healthy)`, heartbeat свежий, сегмент растёт;
- **проверка неизменности:** после деплоя БЕЗ выставленного env состав эмиссии прежний —
  в свежих событиях `L2Delta` есть только для BTC. Это главный пункт: milestone обязан быть
  «невидимым» для прода до операторского решения.
- При последующей раскатке (уже по подписи founder'а): первое `L2Delta` нового символа лежит
  в сегменте с **новым** `epoch_id`.

## Граница C — что ждёт founder'а (`docs/PENDING-SIGNATURE.md`)

Реализация и merge НЕ блокированы. Блокирована **раскатка** — два независимых решения:

1. **Порядок относительно R1 (Storage Box).** `CT-RFC-06` §8.3 ставит R1 первым: 148 млн
   событий существуют в ОДНОЙ копии, а расширение эмиссии увеличивает объём под риском.
   Решение founder'а: раскатывать состав после R1 либо принять риск явно.
2. **Состав allow-list** (`CT-RFC-06` §9.4) — какие символы кокпита. Продуктовое решение с
   ценой в байтах/сутки (§7 RFC), не инженерное. Формат подписи —
   `{what, from, to, rationale, report_ref}` (`docs/03-integration-contract.md` §3).

Ни один агент не выставляет env и не меняет дефолт вместо founder'а.

## Cross-references
- `docs/rfc/CT-RFC-06-l2delta.md` §0.2 (посылка), §2 (класс изменения), §3 (эпохи),
  §8.1 (объём), §9 (решения founder'а)
- `research/measurements/m-45-l2delta-impact.md` §1.1 (карта `match` — для §8.2, не для этого milestone'а)
- `milestones/M-18-l2delta-capture.md` (механизм эмиссии, CLOSED)
- `docs/data-epochs.md` (E-001 — почему эпоха обязательна)
- `.claude/rules/testing.md` (чек-лист деградированного входа), `.claude/rules/gates.md` §5/§8
